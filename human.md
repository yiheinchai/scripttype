ok those repos is not scripttype, but to build up to scripttype. scripttype is a project aiming to make typescript type system easy to write. the key reason why typescript is hard to write and understand is because of it's expression based, no-declaration syntax. but what if we can write typescript type system just like how we write javascript, with variable declarations, loops etc. and then that compiles into typescript. this will make extremely difficult to reason typescript type system logic very easy to write. that is the goal of this project and the typescript examples is the test-set for self-verification that this project is working and compiling to the same typescript as all these project. now this project will be fully driven autonomously without any human intervention, and the end result is that we can a project that allows the user to write normal code in javascript/typescript style that compiles into typescript type system, and the end result should be that every single type system code in all the sub repos should have the corresponding normal javascript/typescript style code that compiles exactly 1-1 match to the original as a self-verification mechanism. until it's fully covered and working, the project is complete

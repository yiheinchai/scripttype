/**
 * ScriptType for 01-type-level-programming/expect-type/src/branding.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 6 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ConstructorOverloadParameters: any
declare const DeepBrandOptions: any
declare const DeepBrandOptionsDefaults: any
declare const DeepBrandPropNotesOptions: any
declare const IsAny: any
declare const IsNever: any
declare const IsRecord: any
declare const IsTuple: any
declare const IsUnknown: any
declare const MutuallyExtends: any
declare const NumOverloads: any
declare const OptionalKeys: any
declare const OverloadsInfoUnion: any
declare const ReadonlyKeys: any
declare const RequiredKeys: any
declare const TupleToRecord: any
declare const UnionToIntersection: any
declare const UnionToTuple: any
declare const X: any
type ConstructorOverloadParameters<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DeepBrandOptions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DeepBrandOptionsDefaults<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DeepBrandPropNotesOptions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsAny<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsNever<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsRecord<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsTuple<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsUnknown<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MutuallyExtends<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NumOverloads<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OptionalKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OverloadsInfoUnion<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ReadonlyKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RequiredKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TupleToRecord<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnionToIntersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnionToTuple<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type X<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ NominalType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NominalType(T, Options: DeepBrandOptions) {
  const m1 = matches<Hole<"N">>(Options['nominalTypes'])
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.N)) {
      out[K] = matches<true>(MutuallyExtends(m1.N[K], T)) ? K : never
    }
    return out[keyof(m1.N)]
  }
  return never
}
/* compiles to:
 * export type NominalType<T, Options extends DeepBrandOptions> =
 *   Options['nominalTypes'] extends infer N
 *     ? { [K in keyof N]: MutuallyExtends<N[K], T> extends true ? K : never }[keyof N]
 *     : never
 */

// ✓ DeepBrand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepBrand(T, Options: DeepBrandOptions) {
  if (matches<true>(IsNever(T))) {
    return { type: 'never' }
  }
  if (matches<true>(IsAny(T))) {
    return { type: 'any' }
  }
  if (matches<true>(IsUnknown(T))) {
    return { type: 'unknown' }
  }
  const m1 = matches<Hole<"Nominal">>(NominalType(T, Options))
  if (m1) {
    if (matches<true>(IsNever(m1.Nominal))) {
      if (matches<string | number | boolean | symbol | bigint | null | undefined | void>(T)) {
        return { type: 'primitive', value: T }
      }
      if (matches<new (...args: any[]) => any>(T)) {
        return { type: 'constructor', params: ConstructorOverloadParameters(T), instance: DeepBrand(InstanceType(Extract(T, ctorType([any], any))), Options) }
      }
      const m2 = matches<(...args: Hole<"P">) => Hole<"R">>(T)
      if (m2) {
        if (matches<1>(NumOverloads(T))) {
          return { type: 'function', params: DeepBrand(m2.P, Options), return: DeepBrand(m2.R, Options), this: DeepBrand(ThisParameterType(T), Options), props: DeepBrand(Omit(T, keyof(t<Function>())), Options) }
        }
        const m3 = matches<Hole<"OverloadsTuple">>(UnionToTuple(OverloadsInfoUnion(T)))
        if (m3) {
          const out = emptyObject
          for (const K in keyof(m3.OverloadsTuple)) {
            out[K] = DeepBrand(m3.OverloadsTuple[K], Options)
          }
          return { type: 'overloads', overloads: out }
        }
        return never
      }
      if (Array.isArray(T)) {
        if (matches<true>(IsTuple(T))) {
          const out2 = emptyObject
          for (const K in keyof(T)) {
            out2[K] = DeepBrand(T[K], Options)
          }
          return { type: 'tuple', items: out2 }
        }
        return { type: 'array', items: DeepBrand(T[number], Options) }
      }
      if (matches<true>(IsRecord(T))) {
        return { type: 'record', keys: keyof(T), values: DeepBrand(T[keyof(T)], Options) }
      }
      const out3 = emptyObject
      for (const K in keyof(T)) {
        out3[K] = DeepBrand(T[K], Options)
      }
      const m4 = matches<Hole<"P">>(ConstructorOverloadParameters(T))
      return { type: 'object', properties: out3, readonly: ReadonlyKeys(T), required: RequiredKeys(T), optional: OptionalKeys(T), constructorParams: m4 ? (matches<true>(IsNever(m4.P)) ? never : DeepBrand(m4.P, Options)) : never }
    }
    return { type: m1.Nominal }
  }
  return never
}
/* compiles to:
 * export type DeepBrand<T, Options extends DeepBrandOptions> =
 *   IsNever<T> extends true ? { type: 'never' }
 *   : IsAny<T> extends true ? { type: 'any' }
 *   : IsUnknown<T> extends true ? { type: 'unknown' }
 *   : NominalType<T, Options> extends infer Nominal
 *     ? IsNever<Nominal> extends true
 *       ? T extends string | number | boolean | symbol | bigint | null | undefined | void
 *         ? { type: 'primitive'; value: T }
 *       : T extends new (...args: any[]) => any
 *         ? {
 *           type: 'constructor'
 *           params: ConstructorOverloadParameters<T>
 *           instance: DeepBrand<InstanceType<Extract<T, new (a0: any) => any>>, Options>
 *         }
 *       : T extends (...args: infer P) => infer R
 *         ? NumOverloads<T> extends 1
 *           ? {
 *             type: 'function'
 *             params: DeepBrand<P, Options>
 *             return: DeepBrand<R, Options>
 *             this: DeepBrand<ThisParameterType<T>, Options>
 *             props: DeepBrand<Omit<T, keyof Function>, Options>
 *           }
 *         : UnionToTuple<OverloadsInfoUnion<T>> extends infer OverloadsTuple
 *           ? {
 *             type: 'overloads'
 *             overloads: { [K in keyof OverloadsTuple]: DeepBrand<OverloadsTuple[K], Options> }
 *           }
 *         : never
 *       : T extends any[]
 *         ? IsTuple<T> extends true
 *           ? { type: 'tuple'; items: { [K1 in keyof T]: DeepBrand<T[K1], Options> } }
 *           : { type: 'array'; items: DeepBrand<T[number], Options> }
 *       : IsRecord<T> extends true
 *         ? { type: 'record'; keys: keyof T; values: DeepBrand<T[keyof T], Options> }
 *       : {
 *         type: 'object'
 *         properties: { [K2 in keyof T]: DeepBrand<T[K2], Options> }
 *         readonly: ReadonlyKeys<T>
 *         required: RequiredKeys<T>
 *         optional: OptionalKeys<T>
 *         constructorParams: ConstructorOverloadParameters<T> extends infer P
 *           ? IsNever<P> extends true ? never : DeepBrand<P, Options>
 *           : never
 *       }
 *       : { type: Nominal }
 *   : never
 */

// ✓ StrictEqualUsingBranding: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StrictEqualUsingBranding(Left, Right, Options: DeepBrandOptions) {
  return MutuallyExtends(DeepBrand(Left, Options), DeepBrand(Right, Options))
}
/* compiles to:
 * export type StrictEqualUsingBranding<Left, Right, Options extends DeepBrandOptions> = MutuallyExtends<
 *   DeepBrand<Left, Options>,
 *   DeepBrand<Right, Options>
 * >
 */

// ✗ _DeepPropTypesOfBranded: does not compile yet
//   'FindType' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function _DeepPropTypesOfBranded(T, PathTo: string, FindType: string) {
  if (matches<true>(IsNever(T))) {
    return {}
  }
  if (typeof T === 'string') {
    return {}
  }
  if (matches<{ type: typeof FindType; }>(T)) {
    const out = emptyObject
    for (const K in keySet(PathTo)) {
      out[K] = T['type']
    }
    return merge(out, { deepBrandLeafNode: true })
  }
  if (Array.isArray(T)) {
    return _DeepPropTypesOfBranded(TupleToRecord(T), PathTo, FindType)
  }
  const out2 = emptyObject
  for (const K in keyof(T)) {
    out2[K] = Extract(_DeepPropTypesOfBranded(T[K], `${PathTo}${DeepBrandPropPathSuffix(T, Prop(K))}`, FindType), { deepBrandLeafNode: true })
  }
  return UnionToIntersection(out2[keyof(T)])
}
/* compiles to:
 * export type _DeepPropTypesOfBranded<T, PathTo extends string, FindType extends string> =
 *   IsNever<T> extends true ? {}
 *   : T extends string ? {}
 *   : T extends { type: typeof FindType; }
 *     ? { [K in PathTo]: T['type'] } & { deepBrandLeafNode: true }
 *   : T extends any[] ? _DeepPropTypesOfBranded<TupleToRecord<T>, PathTo, FindType>
 *   : UnionToIntersection<
 *     {
 *       [K1 in keyof T]: Extract<
 *         _DeepPropTypesOfBranded<
 *           T[K1],
 *           `${PathTo}${DeepBrandPropPathSuffix<T, Prop<K1>>}`,
 *           FindType
 *         >,
 *         { deepBrandLeafNode: true }
 *       >
 *     }[keyof T]
 *   >
 */

// ✓ DeepBrandPropNotes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepBrandPropNotes(T, Options: DeepBrandPropNotesOptions) {
  const m1 = matches<Hole<"X">>(_DeepPropTypesOfBranded(DeepBrand(T, merge(DeepBrandOptionsDefaults, Options)), '', Options['findType']))
  if (m1) {
    if (matches<typeof m1.X>({})) {
      return Record(string | number | symbol, 'No flagged props found!')
    }
    const out = emptyObject
    for (const K in keySet(Exclude(keyof(m1.X), 'deepBrandLeafNode'))) {
      out[K] = m1.X[K]
    }
    return out
  }
  return never
}
/* compiles to:
 * export type DeepBrandPropNotes<T, Options extends DeepBrandPropNotesOptions> =
 *   _DeepPropTypesOfBranded<DeepBrand<T, DeepBrandOptionsDefaults & Options>, '', Options['findType']> extends infer X
 *     ? {} extends X
 *       ? Record<string | number | symbol, 'No flagged props found!'>
 *       : { [K in Exclude<keyof X, 'deepBrandLeafNode'>]: X[K] }
 *     : never
 */

// ✓ Prop: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prop(K) {
  if (matches<string | number>(K)) {
    return K
  }
  return 'UNEXPECTED_NON_LITERAL_PROP'
}
/* compiles to:
 * export type Prop<K> = K extends string | number ? K : 'UNEXPECTED_NON_LITERAL_PROP'
 */

// ✓ DeepBrandPropPathSuffix: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepBrandPropPathSuffix(T, K) {
  if (matches<{ type: string; }>(T)) {
    if (matches<'items'>(K)) {
      return '[number]'
    }
    if (matches<'properties'>(K)) {
      return ''
    }
    return `(${Prop(K)})`
  }
  return `.${Prop(K)}`
}
/* compiles to:
 * export type DeepBrandPropPathSuffix<T, K> =
 *   T extends { type: string; }
 *     ? K extends 'items' ? '[number]' : K extends 'properties' ? '' : `(${Prop<K>})`
 *     : `.${Prop<K>}`
 */

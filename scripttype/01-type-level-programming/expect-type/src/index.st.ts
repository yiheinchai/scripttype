/**
 * ScriptType for 01-type-level-programming/expect-type/src/index.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { NegativeExpectTypeOf, PositiveExpectTypeOf } from '../../../../01-type-level-programming/expect-type/src/index.js'
declare const NegativeExpectTypeOf: any
declare const PositiveExpectTypeOf: any
// ✓ ExpectTypeOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExpectTypeOf(Actual, Options: { positive: boolean; }): any {
  if (matches<true>(Options['positive'])) {
    return PositiveExpectTypeOf(Actual)
  }
  return NegativeExpectTypeOf(Actual)
}
/* compiles to:
 * export type ExpectTypeOf<Actual, Options extends { positive: boolean; }> =
 *   Options['positive'] extends true ? PositiveExpectTypeOf<Actual> : NegativeExpectTypeOf<Actual>
 */

/**
 * ScriptType for 01-type-level-programming/expect-type/src/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 28 type-level aliases: 28 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Digit } from '../../../../01-type-level-programming/expect-type/src/utils.js'
declare namespace Array {
  export type isArray<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type Intersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Digit: any
declare const Mismatch: any
declare const Secret: any
type IsNever<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Mismatch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Secret<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Not: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Not(T: boolean): any {
  if (matches<true>(T)) {
    return false
  }
  return true
}
/* compiles to:
 * export type Not<T extends boolean> = T extends true ? false : true
 */

// ✓ Or: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Or(Types: boolean[]): any {
  if (matches<false>(Types[number])) {
    return false
  }
  return true
}
/* compiles to:
 * export type Or<Types extends boolean[]> = Types[number] extends false ? false : true
 */

// ✓ And: verified type-identical to the original
/* @scripttype preserveParamNames */
export function And(Types: boolean[]): any {
  if (matches<true>(Types[number])) {
    return true
  }
  return false
}
/* compiles to:
 * export type And<Types extends boolean[]> = Types[number] extends true ? true : false
 */

// ✓ Eq: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Eq(Left: boolean, Right: boolean): any {
  if (matches<true>(Left)) {
    return Right
  }
  return Not(Right)
}
/* compiles to:
 * export type Eq<Left extends boolean, Right extends boolean> =
 *   Left extends true ? Right : Not<Right>
 */

// ✓ Xor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Xor(Types: [ boolean, boolean ]): any {
  return Not(Eq(Types[0], Types[1]))
}
/* compiles to:
 * export type Xor<Types extends [boolean, boolean]> = Not<Eq<Types[0], Types[1]>>
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  if (matches<[ Secret ]>([T])) {
    return Not(IsNever(T))
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = [T] extends [Secret] ? Not<IsNever<T>> : false
 */

// ✓ IsUnknown: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnknown(T): any {
  if (matches<[ typeof T ]>([unknown])) {
    return Not(IsAny(T))
  }
  return false
}
/* compiles to:
 * export type IsUnknown<T> = [unknown] extends [T] ? Not<IsAny<T>> : false
 */

// ✓ IsNeverOrAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNeverOrAny(T): any {
  return Or([IsNever(T), IsAny(T)])
}
/* compiles to:
 * export type IsNeverOrAny<T> = Or<[IsNever<T>, IsAny<T>]>
 */

// ✓ UsefulKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UsefulKeys(T): any {
  if (Array.isArray(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = K
    }
    return out[number]
  }
  return keyof(T)
}
/* compiles to:
 * export type UsefulKeys<T> = T extends any[] ? { [K in keyof T]: K }[number] : keyof T
 */

// ✓ RequiredKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredKeys(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = required(matches<Pick<typeof T, typeof K>>({}) ? never : K)
  }
  return Extract(out[keyof(T)], keyof(T))
}
/* compiles to:
 * export type RequiredKeys<T> = Extract<
 *   { [K in keyof T]-?: {} extends Pick<T, K> ? never : K }[keyof T],
 *   keyof T
 * >
 */

// ✓ OptionalKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionalKeys(T): any {
  return Exclude(keyof(T), RequiredKeys(T))
}
/* compiles to:
 * export type OptionalKeys<T> = Exclude<keyof T, RequiredKeys<T>>
 */

// ✓ ReadonlyKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyKeys__v1(T, K): any {
  const out = emptyObject
  for (const _K in keySet(K)) {
    out[_K] = T[K]
  }
  return out
}

export function ReadonlyKeys__v2(T, K): any {
  const out = emptyObject
  for (const _K in keySet(K)) {
    out[_K] = mutable(T[K])
  }
  return out
}
export function ReadonlyKeys(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = required(matches<true>(ReadonlyEquivalent(ReadonlyKeys__v1(T, K), ReadonlyKeys__v2(T, K))) ? never : K)
  }
  return Extract(out[keyof(T)], keyof(T))
}
/* compiles to:
 * export type ReadonlyKeys__v1<T, K> = { [_K in K]: T[K] }
 * export type ReadonlyKeys__v2<T, K> = { -readonly [_K in K]: T[K] }
 * export type ReadonlyKeys<T> = Extract<
 *   {
 *     [K in keyof T]-?: ReadonlyEquivalent<ReadonlyKeys__v1<T, K>, ReadonlyKeys__v2<T, K>> extends true
 *       ? never
 *       : K
 *   }[keyof T],
 *   keyof T
 * >
 */

// ✓ ReadonlyEquivalent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyEquivalent(X, Y): any {
  return Extends(genericFnType(['T'], [], matches<typeof X>(T) ? true : false), genericFnType(['T'], [], matches<typeof Y>(T) ? true : false))
}
/* compiles to:
 * export type ReadonlyEquivalent<X, Y> = Extends<
 *   <T>() => T extends X ? true : false,
 *   <T>() => T extends Y ? true : false
 * >
 */

// ✓ Extends: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Extends(Left, Right): any {
  if (matches<true>(IsNever(Left))) {
    return IsNever(Right)
  }
  if (matches<[ typeof Right ]>([Left])) {
    return true
  }
  return false
}
/* compiles to:
 * export type Extends<Left, Right> =
 *   IsNever<Left> extends true ? IsNever<Right> : [Left] extends [Right] ? true : false
 */

// ✓ ExtendsExcludingAnyOrNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtendsExcludingAnyOrNever(Left, Right): any {
  if (matches<true>(IsAny(Left))) {
    return IsAny(Right)
  }
  return Extends(Left, Right)
}
/* compiles to:
 * export type ExtendsExcludingAnyOrNever<Left, Right> =
 *   IsAny<Left> extends true ? IsAny<Right> : Extends<Left, Right>
 */

// ✓ StrictEqualUsingTSInternalIdenticalToOperator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StrictEqualUsingTSInternalIdenticalToOperator(L, R): any {
  if (matches<<T>() => T extends (typeof R & T) | T ? true : false>(genericFnType(['T'], [], matches<(typeof L & T) | T>(T) ? true : false))) {
    if (matches<IsNever<typeof R>>(IsNever(L))) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type StrictEqualUsingTSInternalIdenticalToOperator<L, R> =
 *   (<T>() => T extends L & T | T ? true : false) extends <T>() => T extends (R & T) | T ? true : false
 *     ? IsNever<L> extends IsNever<R> ? true : false
 *     : false
 */

// ✓ MutuallyExtends: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutuallyExtends(Left, Right): any {
  return And([Extends(Left, Right), Extends(Right, Left)])
}
/* compiles to:
 * export type MutuallyExtends<Left, Right> = And<[Extends<Left, Right>, Extends<Right, Left>]>
 */

// ✓ MismatchArgs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MismatchArgs(ActualResult: boolean, ExpectedResult: boolean): any {
  if (matches<true>(Eq(ActualResult, ExpectedResult))) {
    return []
  }
  return [Mismatch]
}
/* compiles to:
 * export type MismatchArgs<ActualResult extends boolean, ExpectedResult extends boolean> =
 *   Eq<ActualResult, ExpectedResult> extends true ? [] : [Mismatch]
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(Union): any {
  const m1 = matches<(mergedIntersection: Hole<"Intersection">) => void>(matches<any>(Union) ? fnType([Union], voidType()) : never)
  if (m1) {
    return m1.Intersection
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<Union> =
 *   (Union extends any ? (a0: Union) => void : never) extends (mergedIntersection: infer Intersection) => void
 *     ? Intersection
 *     : never
 */

// ✓ LastOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LastOf(Union): any {
  const m1 = matches<() => Hole<"R">>(UnionToIntersection(matches<any>(Union) ? fnType([], Union) : never))
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type LastOf<Union> =
 *   UnionToIntersection<Union extends any ? () => Union : never> extends () => infer R ? R : never
 */

// ✓ TuplifyUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TuplifyUnion(Union, LastElement = LastOf(Union)): any {
  if (matches<true>(IsNever(Union))) {
    return []
  }
  return [...TuplifyUnion(Exclude(Union, LastElement)), LastElement]
}
/* compiles to:
 * export type TuplifyUnion<Union, LastElement = LastOf<Union>> =
 *   IsNever<Union> extends true ? [] : [...TuplifyUnion<Exclude<Union, LastElement>>, LastElement]
 */

// ✓ UnionToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToTuple(Union): any {
  return TuplifyUnion(Union)
}
/* compiles to:
 * export type UnionToTuple<Union> = TuplifyUnion<Union>
 */

// ✓ TupleToRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {any[]} T
 */
export function TupleToRecord(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[`${Extract(K, `${Digit}${string}`)}`] = T[K]
  }
  return out
}
/* compiles to:
 * export type TupleToRecord<T extends any[]> = {
 *   [K in keyof T as `${Extract<K, `${Digit}${string}`>}`]: T[K]
 * }
 */

// ✓ IsTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsTuple(T: readonly any[]): any {
  if (matches<(typeof T)['length']>(number)) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsTuple<T extends readonly any[]> = number extends T['length'] ? false : true
 */

// ✓ IsRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsRecord(T): any {
  return Or([Extends(string, keyof(T)), Extends(number, keyof(T))])
}
/* compiles to:
 * export type IsRecord<T> = Or<[Extends<string, keyof T>, Extends<number, keyof T>]>
 */

// ✓ IsUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnion(T): any {
  return Not(Extends(UnionToTuple(T)['length'], 1))
}
/* compiles to:
 * export type IsUnion<T> = Not<Extends<UnionToTuple<T>['length'], 1>>
 */

// ✓ DeepPickMatchingProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepPickMatchingProps(Left, Right): any {
  if (matches<Record<string, unknown>>(Left)) {
    const out = emptyObject
    for (const K in keyof(Left)) {
      out[K] = K in Right ? DeepPickMatchingProps(Left[K], Right[K]) : never
    }
    return Pick(out, Extract(keyof(Left), keyof(Right)))
  }
  return Left
}
/* compiles to:
 * export type DeepPickMatchingProps<Left, Right> =
 *   Left extends Record<string, unknown>
 *     ? Pick<
 *       {
 *         [K in keyof Left]: K extends keyof Right
 *           ? DeepPickMatchingProps<Left[K], Right[K]>
 *           : never
 *       },
 *       Extract<keyof Left, keyof Right>
 *     >
 *     : Left
 */

/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/core/Core.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 10 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ExcludePlaceholders: any
declare const Fn: any
declare const Head: any
declare const _: any
declare const arg: any
declare const rawArgs: any
type ExcludePlaceholders<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Fn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Head<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type _<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type arg<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type rawArgs<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ arg0: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg0(Constraint = unknown) {
  return arg(0, Constraint)
}
/* compiles to:
 * export type arg0<Constraint = unknown> = arg<0, Constraint>
 */

// ✓ arg1: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg1(Constraint = unknown) {
  return arg(1, Constraint)
}
/* compiles to:
 * export type arg1<Constraint = unknown> = arg<1, Constraint>
 */

// ✓ arg2: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg2(Constraint = unknown) {
  return arg(2, Constraint)
}
/* compiles to:
 * export type arg2<Constraint = unknown> = arg<2, Constraint>
 */

// ✓ arg3: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg3(Constraint = unknown) {
  return arg(3, Constraint)
}
/* compiles to:
 * export type arg3<Constraint = unknown> = arg<3, Constraint>
 */

// ✓ Apply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Apply(fn: Fn, args: unknown[]) {
  return (merge(fn, { [rawArgs]: args }))['return']
}
/* compiles to:
 * export type Apply<fn extends Fn, args extends unknown[]> = (fn & { [rawArgs]: args })['return']
 */

// ✓ Call: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Call(fn: Fn, arg0 = _, arg1 = _, arg2 = _, arg3 = _) {
  return (merge(fn, { [rawArgs]: ExcludePlaceholders([arg0, arg1, arg2, arg3]) }))['return']
}
/* compiles to:
 * export type Call<fn extends Fn, arg0 = _, arg1 = _, arg2 = _, arg3 = _> = (fn & { [rawArgs]: ExcludePlaceholders<[arg0, arg1, arg2, arg3]> })['return']
 */

// ✓ Pipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Pipe(acc, xs: Fn[]) {
  let acc_ = acc
  let xs_ = xs
  while (true) {
    const m1 = matches<[ Hole<"first", Fn>, ...Hole<"rest", Fn[]> ]>(xs_)
    if (!m1) {
      break
    }
    acc_ = Call(m1.first, acc_)
    xs_ = m1.rest
  }
  return acc_
}
/* compiles to:
 * export type Pipe<acc, xs extends Fn[]> = Pipe__loop<acc, xs>
 * type Pipe__loop<Acc_, Xs_ extends Fn[]> = Xs_ extends [infer first extends Fn, ...(infer rest extends Fn[])] ? Pipe__loop<Call<first, Acc_>, rest> : Acc_
 */

// ✓ PipeRight: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PipeRight(xs: Fn[], acc) {
  let xs_ = xs
  let acc_ = acc
  while (true) {
    const m1 = matches<[ ...Hole<"rest", Fn[]>, Hole<"last", Fn> ]>(xs_)
    if (!m1) {
      break
    }
    xs_ = m1.rest
    acc_ = Call(m1.last, acc_)
  }
  return acc_
}
/* compiles to:
 * export type PipeRight<xs extends Fn[], acc> = PipeRight__loop<xs, acc>
 * type PipeRight__loop<Xs_ extends Fn[], Acc_> = Xs_ extends [...(infer rest extends Fn[]), infer last extends Fn] ? PipeRight__loop<rest, Call<last, Acc_>> : Acc_
 */

// ✓ ComposeImpl: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ComposeImpl(fns: Fn[], args: any[]) {
  let fns_ = fns
  let args_ = args
  while (true) {
    const m1 = matches<[ ...Hole<"rest", Fn[]>, Hole<"last", Fn> ]>(fns_)
    if (!m1) {
      break
    }
    fns_ = m1.rest
    args_ = [Apply(m1.last, args_)]
  }
  return Head(args_)
}
/* compiles to:
 * export type ComposeImpl<fns extends Fn[], args extends any[]> = ComposeImpl__loop<fns, args>
 * type ComposeImpl__loop<Fns_ extends Fn[], Args_ extends any[]> = Fns_ extends [...(infer rest extends Fn[]), infer last extends Fn] ? ComposeImpl__loop<rest, [Apply<last, Args_>]> : Head<Args_>
 */

// ✓ ComposeLeftImpl: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ComposeLeftImpl(fns: Fn[], args: any[]) {
  let fns_ = fns
  let args_ = args
  while (true) {
    const m1 = matches<[ Hole<"first", Fn>, ...Hole<"rest", Fn[]> ]>(fns_)
    if (!m1) {
      break
    }
    fns_ = m1.rest
    args_ = [Apply(m1.first, args_)]
  }
  return Head(args_)
}
/* compiles to:
 * export type ComposeLeftImpl<fns extends Fn[], args extends any[]> = ComposeLeftImpl__loop<fns, args>
 * type ComposeLeftImpl__loop<Fns_ extends Fn[], Args_ extends any[]> = Fns_ extends [infer first extends Fn, ...(infer rest extends Fn[])] ? ComposeLeftImpl__loop<rest, [Apply<first, Args_>]> : Head<Args_>
 */

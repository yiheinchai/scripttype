/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/core/Core.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 10 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ arg0: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg0(Constraint: unknown = unknown) {
  return arg(0, Constraint)
}
/* compiles to:
 * export type arg0<Constraint = unknown> = arg<0, Constraint>
 */

// ✓ arg1: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg1(Constraint: unknown = unknown) {
  return arg(1, Constraint)
}
/* compiles to:
 * export type arg1<Constraint = unknown> = arg<1, Constraint>
 */

// ✓ arg2: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg2(Constraint: unknown = unknown) {
  return arg(2, Constraint)
}
/* compiles to:
 * export type arg2<Constraint = unknown> = arg<2, Constraint>
 */

// ✓ arg3: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arg3(Constraint: unknown = unknown) {
  return arg(3, Constraint)
}
/* compiles to:
 * export type arg3<Constraint = unknown> = arg<3, Constraint>
 */

// ✓ Apply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Apply(fn: Fn, args: unknown[]) {
  return (fn & { [rawArgs]: args })['return']
}
/* compiles to:
 * export type Apply<fn extends Fn, args extends unknown[]> = (fn & { [rawArgs]: args })['return']
 */

// ✓ Call: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Call(fn: Fn, arg0: unknown = _, arg1: unknown = _, arg2: unknown = _, arg3: unknown = _) {
  return (fn & { [rawArgs]: ExcludePlaceholders([arg0, arg1, arg2, arg3]) })['return']
}
/* compiles to:
 * export type Call<fn extends Fn, arg0 = _, arg1 = _, arg2 = _, arg3 = _> = (fn & { [rawArgs]: ExcludePlaceholders<[arg0, arg1, arg2, arg3]> })['return']
 */

// ✓ Pipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Pipe(acc: unknown, xs: Fn[]) {
  if (matches<[ infer first extends Fn, ...infer rest extends Fn[] ]>(xs)) {
    return Pipe(Call(first, acc), rest)
  }
  return acc
}
/* compiles to:
 * export type Pipe<acc, xs extends Fn[]> = xs extends [infer first extends Fn, ...(infer rest extends Fn[])] ? Pipe<Call<first, acc>, rest> : acc
 */

// ✓ PipeRight: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PipeRight(xs: Fn[], acc: unknown) {
  if (matches<[ ...infer rest extends Fn[], infer last extends Fn ]>(xs)) {
    return PipeRight(rest, Call(last, acc))
  }
  return acc
}
/* compiles to:
 * export type PipeRight<xs extends Fn[], acc> = xs extends [...(infer rest extends Fn[]), infer last extends Fn] ? PipeRight<rest, Call<last, acc>> : acc
 */

// ✓ ComposeImpl: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ComposeImpl(fns: Fn[], args: any[]) {
  if (matches<[ ...infer rest extends Fn[], infer last extends Fn ]>(fns)) {
    return ComposeImpl(rest, [Apply(last, args)])
  }
  return Head(args)
}
/* compiles to:
 * export type ComposeImpl<fns extends Fn[], args extends any[]> = fns extends [...(infer rest extends Fn[]), infer last extends Fn] ? ComposeImpl<rest, [Apply<last, args>]> : Head<args>
 */

// ✓ ComposeLeftImpl: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ComposeLeftImpl(fns: Fn[], args: any[]) {
  if (matches<[ infer first extends Fn, ...infer rest extends Fn[] ]>(fns)) {
    return ComposeLeftImpl(rest, [Apply(first, args)])
  }
  return Head(args)
}
/* compiles to:
 * export type ComposeLeftImpl<fns extends Fn[], args extends any[]> = fns extends [infer first extends Fn, ...(infer rest extends Fn[])] ? ComposeLeftImpl<rest, [Apply<first, args>]> : Head<args>
 */

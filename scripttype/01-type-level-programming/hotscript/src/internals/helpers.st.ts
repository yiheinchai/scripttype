/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/helpers.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 25 type-level aliases: 25 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace Tuple {
  export type unshift<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace it {
  export type unshift<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type Last<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type T<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type first<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type intersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type rest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const UppercaseLetter: any
type UppercaseLetter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Equal: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equal(a, b): any {
  if (matches<<T>() => T extends typeof b ? 1 : 2>(genericFnType(['T'], [], matches<typeof a>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equal<a, b> =
 *   (<T>() => T extends a ? 1 : 2) extends <T>() => T extends b ? 1 : 2 ? true : false
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ Expect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Expect(a: true): any {
  return a
}
/* compiles to:
 * export type Expect<a extends true> = a
 */

// ✓ Some: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Some(bools: boolean[]): any {
  if (matches<(typeof bools)[number]>(true)) {
    return true
  }
  return false
}
/* compiles to:
 * export type Some<bools extends boolean[]> = true extends bools[number] ? true : false
 */

// ✓ Every: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Every(bools: boolean[]): any {
  if (matches<true>(bools[number])) {
    return true
  }
  return false
}
/* compiles to:
 * export type Every<bools extends boolean[]> = bools[number] extends true ? true : false
 */

// ✓ Extends: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Extends(a, b): any {
  if (matches<[ typeof b ]>([a])) {
    return true
  }
  return false
}
/* compiles to:
 * export type Extends<a, b> = [a] extends [b] ? true : false
 */

// ✓ Not: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Not(a: boolean): any {
  if (matches<true>(a)) {
    return false
  }
  return true
}
/* compiles to:
 * export type Not<a extends boolean> = a extends true ? false : true
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(union): any {
  const m1 = matches<(k: Hole<"intersection">) => void>(matches<any>(union) ? fnType([union], voidType()) : never)
  if (m1) {
    return m1.intersection
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<union> =
 *   (union extends any ? (a0: union) => void : never) extends (k: infer intersection) => void
 *     ? intersection
 *     : never
 */

// ✓ Prettify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prettify(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return out | never
}
/* compiles to:
 * export type Prettify<T> = { [K in keyof T]: T[K] }
 */

// ✓ RecursivePrettify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecursivePrettify(T): any {
  if (matches<true>(IsArrayStrict(T))) {
    return arrayOf(RecursivePrettify(Extract(T, readonlyArrayOf(any))[number]))
  }
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = RecursivePrettify(T[K])
  }
  return out
}
/* compiles to:
 * export type RecursivePrettify<T> =
 *   IsArrayStrict<T> extends true
 *     ? RecursivePrettify<Extract<T, readonly any[]>[number]>[]
 *     : { [K in keyof T]: RecursivePrettify<T[K]> }
 */

// ✓ Get: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Get(it: readonly any[]): any {
  return it['length']
}
/* compiles to:
 * export type Get<it extends readonly any[]> = it['length']
 */

// ✓ Iterator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Iterator(n: number): any {
  let it: any[] = []
  while (true) {
    if (matches<typeof n>(it['length'])) {
      break
    }
    it.unshift(any)
  }
  return it
}
/* compiles to:
 * export type Iterator<n extends number> = Iterator__loop<[], n>
 * type Iterator__loop<It extends any[], N extends number> =
 *   It['length'] extends N ? It : Iterator__loop<[any, ...It], N>
 */

// ✓ Next: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Next(it: any[]): any {
  return [any, ...it]
}
/* compiles to:
 * export type Next<it extends any[]> = [any, ...it]
 */

// ✓ Prev: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prev(it: any[]): any {
  const m1 = matches<readonly [ any, ...Hole<"tail"> ]>(it)
  if (m1) {
    return m1.tail
  }
  return []
}
/* compiles to:
 * export type Prev<it extends any[]> = it extends readonly [any, ...infer tail] ? tail : []
 */

// ✓ KebabToCamel: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KebabToCamel(str): any {
  const m1 = matches<`${Hole<"first">}-${Hole<"rest">}`>(str)
  if (m1) {
    return `${m1.first}${KebabToCamel(Capitalize(m1.rest))}`
  }
  return str
}
/* compiles to:
 * export type KebabToCamel<str> =
 *   str extends `${infer first}-${infer rest}` ? `${first}${KebabToCamel<Capitalize<rest>>}` : str
 */

// ✓ SnakeToCamel: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SnakeToCamel(str): any {
  const m1 = matches<`${Hole<"first">}_${Hole<"rest">}`>(str)
  if (m1) {
    return `${m1.first}${SnakeToCamel(Capitalize(m1.rest))}`
  }
  return str
}
/* compiles to:
 * export type SnakeToCamel<str> =
 *   str extends `${infer first}_${infer rest}` ? `${first}${SnakeToCamel<Capitalize<rest>>}` : str
 */

// ✓ CamelCase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CamelCase(str): any {
  return KebabToCamel(SnakeToCamel(str))
}
/* compiles to:
 * export type CamelCase<str> = KebabToCamel<SnakeToCamel<str>>
 */

// ✓ SnakeCase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SnakeCase(str, output: string = ''): any {
  const m1 = matches<`${Hole<"first">}${Hole<"rest">}`>(str)
  if (m1) {
    if (matches<UppercaseLetter>(m1.first)) {
      if (matches<"">(output)) {
        return SnakeCase(m1.rest, Lowercase(m1.first))
      }
      return SnakeCase(m1.rest, `${output}_${Lowercase(m1.first)}`)
    }
    if (matches<"-">(m1.first)) {
      return SnakeCase(m1.rest, `${output}_`)
    }
    return SnakeCase(m1.rest, `${output}${m1.first}`)
  }
  if (matches<"">(output)) {
    return str
  }
  return output
}
/* compiles to:
 * export type SnakeCase<str, output extends string = ''> =
 *   str extends `${infer first}${infer rest}`
 *     ? first extends UppercaseLetter
 *       ? output extends ''
 *         ? SnakeCase<rest, Lowercase<first>>
 *         : SnakeCase<rest, `${output}_${Lowercase<first>}`>
 *     : first extends '-' ? SnakeCase<rest, `${output}_`>
 *     : SnakeCase<rest, `${output}${first}`>
 *   : output extends '' ? str
 *   : output
 */

// ✓ KebabCase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KebabCase(str, output: string = ''): any {
  const m1 = matches<`${Hole<"first">}${Hole<"rest">}`>(str)
  if (m1) {
    if (matches<UppercaseLetter>(m1.first)) {
      if (matches<"">(output)) {
        return KebabCase(m1.rest, Lowercase(m1.first))
      }
      return KebabCase(m1.rest, `${output}-${Lowercase(m1.first)}`)
    }
    if (matches<"_">(m1.first)) {
      return KebabCase(m1.rest, `${output}-`)
    }
    return KebabCase(m1.rest, `${output}${m1.first}`)
  }
  if (matches<"">(output)) {
    return str
  }
  return output
}
/* compiles to:
 * export type KebabCase<str, output extends string = ''> =
 *   str extends `${infer first}${infer rest}`
 *     ? first extends UppercaseLetter
 *       ? output extends ''
 *         ? KebabCase<rest, Lowercase<first>>
 *         : KebabCase<rest, `${output}-${Lowercase<first>}`>
 *     : first extends '_' ? KebabCase<rest, `${output}-`>
 *     : KebabCase<rest, `${output}${first}`>
 *   : output extends '' ? str
 *   : output
 */

// ✓ IsTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsTuple(a: readonly any[]): any {
  if (matches<readonly [ ] | readonly [ any, ...any ] | readonly [ ...any, any ]>(a)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsTuple<a extends readonly any[]> =
 *   a extends readonly [] | readonly [any, ...any] | readonly [...any, any] ? true : false
 */

// ✓ IsArrayStrict: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsArrayStrict(a): any {
  if (matches<readonly any[]>(a)) {
    return Not(IsTuple(a))
  }
  return false
}
/* compiles to:
 * export type IsArrayStrict<a> = a extends readonly any[] ? Not<IsTuple<a>> : false
 */

// ✓ GetUnionLast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetUnionLast(Union): any {
  const m1 = matches<() => Hole<"Last">>(UnionToIntersection(matches<any>(Union) ? fnType([], Union) : never))
  if (m1) {
    return m1.Last
  }
  return never
}
/* compiles to:
 * export type GetUnionLast<Union> =
 *   UnionToIntersection<Union extends any ? () => Union : never> extends () => infer Last
 *     ? Last
 *     : never
 */

// ✓ UnionToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToTuple(Union): any {
  let Tuple: any[] = []
  let union = Union
  while (true) {
    if (matches<[ never ]>([union])) {
      break
    }
    union = Exclude(union, GetUnionLast(union))
    Tuple.unshift(GetUnionLast(union))
  }
  return Tuple
}
/* compiles to:
 * export type UnionToTuple<Union> = UnionToTuple__loop<Union, []>
 * type UnionToTuple__loop<Union, Tuple extends any[]> =
 *   [Union] extends [never]
 *     ? Tuple
 *     : UnionToTuple__loop<
 *       Exclude<Union, GetUnionLast<Union>>,
 *       [GetUnionLast<Exclude<Union, GetUnionLast<Union>>>, ...Tuple]
 *     >
 */

// ✓ Split: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Split(Str, Sep: string, Acc: string[] = []): any {
  if (matches<"">(Str)) {
    return Acc
  }
  const m1 = matches<`${Hole<"T">}${typeof Sep}${Hole<"U">}`>(Str)
  if (m1) {
    return Split(m1.U, Sep, [...Acc, m1.T])
  }
  return [...Acc, Str]
}
/* compiles to:
 * export type Split<Str, Sep extends string, Acc extends string[] = []> =
 *   Str extends '' ? Acc
 *   : Str extends `${infer T}${Sep}${infer U}` ? Split<U, Sep, [...Acc, T]>
 *   : [...Acc, Str]
 */

// ✓ Head: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Head(xs): any {
  const m1 = matches<[ Hole<"first">, ...any ]>(xs)
  if (m1) {
    return m1.first
  }
  return never
}
/* compiles to:
 * export type Head<xs> = xs extends [infer first, ...any] ? first : never
 */

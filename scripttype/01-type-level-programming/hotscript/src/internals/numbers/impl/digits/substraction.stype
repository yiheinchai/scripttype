/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/digits/substraction.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ SubDigit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubDigit(T: Digit, U: Digit, Carry: 0 | 1 = 0) {
  return SubDigitTable[Carry][T][U]
}
/* compiles to:
 * export type SubDigit<T extends Digit, U extends Digit, Carry extends 0 | 1 = 0> = SubDigitTable[Carry][T][U]
 */

// ✓ SubCarryDigit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubCarryDigit(T: Digit, U: Digit, Carry: 0 | 1 = 0) {
  return SubDigitCarryTable[Carry][T][U]
}
/* compiles to:
 * export type SubCarryDigit<T extends Digit, U extends Digit, Carry extends 0 | 1 = 0> = SubDigitCarryTable[Carry][T][U]
 */

// ✓ SubDigits: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubDigits(T: Digit[], U: Digit[], Carry: 0 | 1 = 0, Acc: Digit[] = []) {
  if (matches<[ ...infer R extends Digit[], infer N extends Digit ]>(T)) {
    if (matches<[ ...infer S extends Digit[], infer M extends Digit ]>(U)) {
      return SubDigits(R, S, SubCarryDigit(N, M, Carry), [SubDigit(N, M, Carry), ...Acc])
    }
    return SubDigits(R, [], SubCarryDigit(N, 0, Carry), [SubDigit(N, 0, Carry), ...Acc])
  }
  if (matches<[ ...infer S extends Digit[], infer M extends Digit ]>(U)) {
    return SubDigits([], S, SubCarryDigit(0, M, Carry), [SubDigit(0, M, Carry), ...Acc])
  }
  if (matches<1>(Carry)) {
    return [...Acc, 9]
  }
  return Acc
}
/* compiles to:
 * export type SubDigits<T extends Digit[], U extends Digit[], Carry extends 0 | 1 = 0, Acc extends Digit[] = []> = T extends [...(infer R extends Digit[]), infer N extends Digit] ? U extends [...(infer S extends Digit[]), infer M extends Digit] ? SubDigits<R, S, SubCarryDigit<N, M, Carry>, [SubDigit<N, M, Carry>, ...Acc]> : SubDigits<R, [], SubCarryDigit<N, 0, Carry>, [SubDigit<N, 0, Carry>, ...Acc]> : U extends [...(infer S extends Digit[]), infer M extends Digit] ? SubDigits<[], S, SubCarryDigit<0, M, Carry>, [SubDigit<0, M, Carry>, ...Acc]> : Carry extends 1 ? [...Acc, 9] : Acc
 */

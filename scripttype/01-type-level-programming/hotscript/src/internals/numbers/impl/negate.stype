/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/negate.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Negate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Negate(T: number | bigint) {
  if (matches<`-${infer U extends number | bigint}`>(`${T}`)) {
    return U
  }
  if (matches<`${infer U extends number | bigint}`>(`-${T}`)) {
    return U
  }
  return never
}
/* compiles to:
 * export type Negate<T extends number | bigint> = `${T}` extends `-${infer U extends number | bigint}` ? U : `-${T}` extends `${infer U extends number | bigint}` ? U : never
 */

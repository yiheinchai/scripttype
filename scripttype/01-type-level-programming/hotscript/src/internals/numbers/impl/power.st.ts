/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/power.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { PowerDigits } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/digits/power.js'
import type { Digit, DigitNumber, FromDigitNumber, MakeDigitNumber, Normalize, Num, Sign, ToDigitNumber, ToNumber, ToString } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/utils.js'
declare const Digit: any
declare const DigitNumber: any
declare const FromDigitNumber: any
declare const MakeDigitNumber: any
declare const Normalize: any
declare const Num: any
declare const PowerDigits: any
declare const Sign: any
declare const ToDigitNumber: any
declare const ToNumber: any
declare const ToString: any
// ✓ PowerSign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PowerSign(S: "" | "-", U: DigitNumber): any {
  if (matches<"-">(S)) {
    if (matches<[ ...Digit[], 0 | 2 | 4 | 6 | 8 ]>(Num(U))) {
      return ''
    }
    return '-'
  }
  return ''
}
/* compiles to:
 * export type PowerSign<S extends '' | '-', U extends DigitNumber> =
 *   S extends '-' ? Num<U> extends [...Digit[], 0 | 2 | 4 | 6 | 8] ? '' : '-' : ''
 */

// ✓ PowerDigitNumbers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PowerDigitNumbers(T: DigitNumber, U: DigitNumber): any {
  if (matches<"-">(Sign(U))) {
    return MakeDigitNumber(Sign(T), [0])
  }
  return MakeDigitNumber(PowerSign(Sign(T), U), PowerDigits(Num(T), Num(U)))
}
/* compiles to:
 * export type PowerDigitNumbers<T extends DigitNumber, U extends DigitNumber> =
 *   Sign<U> extends '-'
 *     ? MakeDigitNumber<Sign<T>, [0]>
 *     : MakeDigitNumber<PowerSign<Sign<T>, U>, PowerDigits<Num<T>, Num<U>>>
 */

// ✓ Power: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Power(T: number | bigint, U: number | bigint): any {
  return ToNumber(FromDigitNumber(Normalize(PowerDigitNumbers(ToDigitNumber(ToString(T)), ToDigitNumber(ToString(U))))))
}
/* compiles to:
 * export type Power<T extends number | bigint, U extends number | bigint> = ToNumber<
 *   FromDigitNumber<
 *     Normalize<PowerDigitNumbers<ToDigitNumber<ToString<T>>, ToDigitNumber<ToString<U>>>>
 *   >
 * >
 */

/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/substraction.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { CompareDigits } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/compare.js'
import type { AddDigits } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/digits/addition.js'
import type { SubDigits } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/digits/substraction.js'
import type { DigitNumber, FromDigitNumber, InvertSign, MakeDigitNumber, Normalize, Num, Sign, ToDigitNumber, ToNumber, ToString } from '../../../../../../../01-type-level-programming/hotscript/src/internals/numbers/impl/utils.js'
declare const AddDigits: any
declare const CompareDigits: any
declare const DigitNumber: any
declare const FromDigitNumber: any
declare const InvertSign: any
declare const MakeDigitNumber: any
declare const Normalize: any
declare const Num: any
declare const Sign: any
declare const SubDigits: any
declare const ToDigitNumber: any
declare const ToNumber: any
declare const ToString: any
// ✓ SubDigitNumbers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubDigitNumbers(T: DigitNumber, U: DigitNumber): any {
  if (matches<Sign<typeof U>>(Sign(T))) {
    if (matches<1>(CompareDigits(Num(T), Num(U)))) {
      return MakeDigitNumber(Sign(T), SubDigits(Num(T), Num(U)))
    }
    return MakeDigitNumber(InvertSign(T), SubDigits(Num(U), Num(T)))
  }
  return MakeDigitNumber(Sign(T), AddDigits(Num(T), Num(U)))
}
/* compiles to:
 * export type SubDigitNumbers<T extends DigitNumber, U extends DigitNumber> =
 *   Sign<T> extends Sign<U>
 *     ? CompareDigits<Num<T>, Num<U>> extends 1
 *       ? MakeDigitNumber<Sign<T>, SubDigits<Num<T>, Num<U>>>
 *       : MakeDigitNumber<InvertSign<T>, SubDigits<Num<U>, Num<T>>>
 *     : MakeDigitNumber<Sign<T>, AddDigits<Num<T>, Num<U>>>
 */

// ✓ Sub: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Sub(T: number | bigint, U: number | bigint): any {
  return ToNumber(FromDigitNumber(Normalize(SubDigitNumbers(ToDigitNumber(ToString(T)), ToDigitNumber(ToString(U))))))
}
/* compiles to:
 * export type Sub<T extends number | bigint, U extends number | bigint> = ToNumber<
 *   FromDigitNumber<
 *     Normalize<SubDigitNumbers<ToDigitNumber<ToString<T>>, ToDigitNumber<ToString<U>>>>
 *   >
 * >
 */

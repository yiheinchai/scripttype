/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/substraction.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ SubDigitNumbers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubDigitNumbers(T: DigitNumber, U: DigitNumber) {
  if (matches<Sign<U>>(Sign(T))) {
    if (matches<1>(CompareDigits(Num(T), Num(U)))) {
      return MakeDigitNumber(Sign(T), SubDigits(Num(T), Num(U)))
    }
    return MakeDigitNumber(InvertSign(T), SubDigits(Num(U), Num(T)))
  }
  return MakeDigitNumber(Sign(T), AddDigits(Num(T), Num(U)))
}
/* compiles to:
 * export type SubDigitNumbers<T extends DigitNumber, U extends DigitNumber> = Sign<T> extends Sign<U> ? CompareDigits<Num<T>, Num<U>> extends 1 ? MakeDigitNumber<Sign<T>, SubDigits<Num<T>, Num<U>>> : MakeDigitNumber<InvertSign<T>, SubDigits<Num<U>, Num<T>>> : MakeDigitNumber<Sign<T>, AddDigits<Num<T>, Num<U>>>
 */

// ✓ Sub: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Sub(T: number | bigint, U: number | bigint) {
  return ToNumber(FromDigitNumber(Normalize(SubDigitNumbers(ToDigitNumber(ToString(T)), ToDigitNumber(ToString(U))))))
}
/* compiles to:
 * export type Sub<T extends number | bigint, U extends number | bigint> = ToNumber<FromDigitNumber<Normalize<SubDigitNumbers<ToDigitNumber<ToString<T>>, ToDigitNumber<ToString<U>>>>>>
 */

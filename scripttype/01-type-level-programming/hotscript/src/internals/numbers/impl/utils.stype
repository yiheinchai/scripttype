/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/numbers/impl/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 13 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ ToNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToNumber(T: string) {
  if (matches<`${infer N extends number | bigint}`>(T)) {
    return N
  }
  return never
}
/* compiles to:
 * export type ToNumber<T extends string> = T extends `${infer N extends number | bigint}` ? N : never
 */

// ✓ ToString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToString(T: number | bigint) {
  return `${T}`
}
/* compiles to:
 * export type ToString<T extends number | bigint> = `${T}`
 */

// ✓ MakeDigitNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MakeDigitNumber(S: "-" | "", N: Digit[]) {
  return { sign: S, num: N }
}
/* compiles to:
 * export type MakeDigitNumber<S extends '-' | '', N extends Digit[]> = { sign: S; num: N }
 */

// ✓ ToDigits: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToDigits(T: string, Acc: Digit[] = []) {
  if (matches<`${infer N extends Digit}${infer R}`>(T)) {
    return ToDigits(R, [...Acc, N])
  }
  return Acc
}
/* compiles to:
 * export type ToDigits<T extends string, Acc extends Digit[] = []> = T extends `${infer N extends Digit}${infer R}` ? ToDigits<R, [...Acc, N]> : Acc
 */

// ✓ ToDigitNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToDigitNumber(T: string) {
  if (matches<`-${infer R}`>(T)) {
    return { sign: '-', num: ToDigits(R) }
  }
  return { sign: '', num: ToDigits(T) }
}
/* compiles to:
 * export type ToDigitNumber<T extends string> = T extends `-${infer R}` ? { sign: '-'; num: ToDigits<R> } : { sign: ''; num: ToDigits<T> }
 */

// ✓ FromDigits: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FromDigits(T: unknown, Acc: string = '') {
  if (matches<[ infer N extends Digit, ...infer R ]>(T)) {
    return FromDigits(R, `${Acc}${N}`)
  }
  return Acc
}
/* compiles to:
 * export type FromDigits<T, Acc extends string = ''> = T extends [infer N extends Digit, ...infer R] ? FromDigits<R, `${Acc}${N}`> : Acc
 */

// ✓ Sign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Sign(T: DigitNumber) {
  return T['sign']
}
/* compiles to:
 * export type Sign<T extends DigitNumber> = T['sign']
 */

// ✓ InvertSign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvertSign(T: DigitNumber) {
  if (matches<"-">(Sign(T))) {
    return ''
  }
  return '-'
}
/* compiles to:
 * export type InvertSign<T extends DigitNumber> = Sign<T> extends '-' ? '' : '-'
 */

// ✓ MulSign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MulSign(S1: "-" | "", S2: "-" | "") {
  if (matches<"-">(S1)) {
    if (matches<"-">(S2)) {
      return ''
    }
    return '-'
  }
  if (matches<"-">(S2)) {
    return '-'
  }
  return ''
}
/* compiles to:
 * export type MulSign<S1 extends '-' | '', S2 extends '-' | ''> = S1 extends '-' ? S2 extends '-' ? '' : '-' : S2 extends '-' ? '-' : ''
 */

// ✓ Num: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Num(T: DigitNumber) {
  return T['num']
}
/* compiles to:
 * export type Num<T extends DigitNumber> = T['num']
 */

// ✓ FromDigitNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FromDigitNumber(T: DigitNumber) {
  return `${Sign(T)}${FromDigits(Num(T))}`
}
/* compiles to:
 * export type FromDigitNumber<T extends DigitNumber> = `${Sign<T>}${FromDigits<Num<T>>}`
 */

// ✓ TrimZeros: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrimZeros(T: Digit[]) {
  if (matches<[ 0 ]>(T)) {
    return [0]
  }
  if (matches<[ 0, ...infer R extends Digit[] ]>(T)) {
    return TrimZeros(R)
  }
  return T
}
/* compiles to:
 * export type TrimZeros<T extends Digit[]> = T extends [0] ? [0] : T extends [0, ...(infer R extends Digit[])] ? TrimZeros<R> : T
 */

// ✓ Normalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Normalize(T: DigitNumber, Trim: Digit[] = TrimZeros(Num(T))) {
  if (matches<[ 0 ]>(Trim)) {
    return MakeDigitNumber('', Trim)
  }
  return MakeDigitNumber(Sign(T), Trim)
}
/* compiles to:
 * export type Normalize<T extends DigitNumber, Trim extends Digit[] = TrimZeros<Num<T>>> = Trim extends [0] ? MakeDigitNumber<'', Trim> : MakeDigitNumber<Sign<T>, Trim>
 */

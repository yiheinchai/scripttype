/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/std/Std.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 11 mismatch, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const PropertyKey: any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ _Pick: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Pick(a, k: keyof typeof a) {
  return Pick(a, k)
}
/* compiles to:
 * export type _Pick<a, k extends keyof a> = Pick<a, k>
 */

// ✗ _Omit: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Omit(a, k: PropertyKey) {
  return Omit(a, k)
}
/* compiles to:
 * export type _Omit<a, k extends PropertyKey> = Omit<a, k>
 */

// ✓ _Extract: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _Extract(a, b) {
  return Extract(a, b)
}
/* compiles to:
 * export type _Extract<a, b> = Extract<a, b>
 */

// ✗ _Exclude: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Exclude(a, b) {
  return Exclude(a, b)
}
/* compiles to:
 * export type _Exclude<a, b> = Exclude<a, b>
 */

// ✗ _Uppercase: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Uppercase(a: string) {
  return Uppercase(a)
}
/* compiles to:
 * export type _Uppercase<a extends string> = Uppercase<a>
 */

// ✗ _Lowercase: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Lowercase(a: string) {
  return Lowercase(a)
}
/* compiles to:
 * export type _Lowercase<a extends string> = Lowercase<a>
 */

// ✗ _Capitalize: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Capitalize(a: string) {
  return Capitalize(a)
}
/* compiles to:
 * export type _Capitalize<a extends string> = Capitalize<a>
 */

// ✗ _Uncapitalize: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Uncapitalize(a: string) {
  return Uncapitalize(a)
}
/* compiles to:
 * export type _Uncapitalize<a extends string> = Uncapitalize<a>
 */

// ✗ _Record: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Record(k: PropertyKey, v) {
  return Record(k, v)
}
/* compiles to:
 * export type _Record<k extends PropertyKey, v> = Record<k, v>
 */

// ✗ _Readonly: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Readonly(a) {
  return Readonly(a)
}
/* compiles to:
 * export type _Readonly<a> = Readonly<a>
 */

// ✗ _Required: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Required(a) {
  return Required(a)
}
/* compiles to:
 * export type _Required<a> = Required<a>
 */

// ✗ _Partial: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _Partial(a) {
  return Partial(a)
}
/* compiles to:
 * export type _Partial<a> = Partial<a>
 */

// ✓ _NonNullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _NonNullable(a) {
  return NonNullable(a)
}
/* compiles to:
 * export type _NonNullable<a> = NonNullable<a>
 */

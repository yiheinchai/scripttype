/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/strings/impl/length.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Add: any
declare const Iterator: any
declare const StrIter: any
type Add<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Iterator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StrIter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ LengthUp: the ScriptType does not itself typecheck as TypeScript
//   LengthUp.st.ts(2:72) TS2702: 'StrIter' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function LengthUp(Str: string, Length: number | bigint = 0, It: StrIter.Iterator = StrIter.Init) {
  const m1 = matches<Hole<"$DoubleIt", StrIter.Iterator>>(StrIter.Double(It))
  if (m1) {
    const m2 = matches<`${Hole<"$Rest">}`>(StrIter.CutAt(Str, m1.$DoubleIt))
    if (m2) {
      if (matches<12>(StrIter.Size(It))) {
        return LengthDown(m2.$Rest, Add(Length, StrIter.Value(m1.$DoubleIt)), m1.$DoubleIt)
      }
      return LengthUp(m2.$Rest, Add(Length, StrIter.Value(m1.$DoubleIt)), m1.$DoubleIt)
    }
    const m3 = matches<`${Hole<"$Rest">}`>(StrIter.CutAt(Str, It))
    if (m3) {
      return LengthUp(m3.$Rest, Add(Length, StrIter.Value(It)), It)
    }
    return LengthDown(Str, Length, StrIter.Prev(It))
  }
  return never
}
/* compiles to:
 * export type LengthUp<Str extends string, Length extends number | bigint = 0, It extends StrIter.Iterator = StrIter.Init> = StrIter.Double<It> extends (infer $DoubleIt extends StrIter.Iterator) ? StrIter.CutAt<Str, $DoubleIt> extends `${infer $Rest}` ? StrIter.Size<It> extends 12 ? LengthDown<$Rest, Add<Length, StrIter.Value<$DoubleIt>>, $DoubleIt> : LengthUp<$Rest, Add<Length, StrIter.Value<$DoubleIt>>, $DoubleIt> : StrIter.CutAt<Str, It> extends `${infer $Rest}` ? LengthUp<$Rest, Add<Length, StrIter.Value<It>>, It> : LengthDown<Str, Length, StrIter.Prev<It>> : never
 */

// ✗ LengthDown: the ScriptType does not itself typecheck as TypeScript
//   LengthDown.st.ts(3:15) TS2702: 'StrIter' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function LengthDown(Str: string, Length: number | bigint, It) {
  if (matches<StrIter.Iterator>(It)) {
    const m1 = matches<`${Hole<"$Rest">}`>(StrIter.CutAt(Str, It))
    if (m1) {
      return LengthDown(m1.$Rest, Add(Length, StrIter.Value(It)), It)
    }
    return LengthDown(Str, Length, StrIter.Prev(It))
  }
  return Length
}
/* compiles to:
 * export type LengthDown<Str extends string, Length extends number | bigint, It> = It extends StrIter.Iterator ? StrIter.CutAt<Str, It> extends `${infer $Rest}` ? LengthDown<$Rest, Add<Length, StrIter.Value<It>>, It> : LengthDown<Str, Length, StrIter.Prev<It>> : Length
 */

// ✓ Length: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Length(T: string) {
  if (matches<"">(T)) {
    return 0
  }
  return LengthUp(T)
}
/* compiles to:
 * export type Length<T extends string> = T extends '' ? 0 : LengthUp<T>
 */

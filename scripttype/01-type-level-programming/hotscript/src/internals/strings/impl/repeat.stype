/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/strings/impl/repeat.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ RepeatX2: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RepeatX2(T: string) {
  return `${T}${T}`
}
/* compiles to:
 * export type RepeatX2<T extends string> = `${T}${T}`
 */

// ✓ Repeat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Repeat(T: string, N: number, Acc: string = '', Calc: { Quotient: number; Remainder: number } = DivMod(N, 2)) {
  if (matches<0>(N)) {
    return Acc
  }
  if (matches<1>(N)) {
    return `${Acc}${T}`
  }
  if (matches<0>(Calc['Remainder'])) {
    return Repeat(RepeatX2(T), Calc['Quotient'], Acc)
  }
  return Repeat(T, Sub(N, 1), `${Acc}${T}`)
}
/* compiles to:
 * export type Repeat<T extends string, N extends number, Acc extends string = '', Calc extends { Quotient: number; Remainder: number } = DivMod<N, 2>> = N extends 0 ? Acc : N extends 1 ? `${Acc}${T}` : Calc['Remainder'] extends 0 ? Repeat<RepeatX2<T>, Calc['Quotient'], Acc> : Repeat<T, Sub<N, 1>, `${Acc}${T}`>
 */

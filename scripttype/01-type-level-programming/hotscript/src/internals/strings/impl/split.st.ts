/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/strings/impl/split.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const H: any
type H<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ ConcatSplits: the ScriptType does not itself typecheck as TypeScript
//   ConcatSplits.st.ts(11:5) TS2322: Type 'any[]' is not assignable to type 'never[]'.   Type 'any' is not assignable to type 'never'.
/* @scripttype preserveParamNames */
export function ConcatSplits(Parts: string[], Seps: string[]) {
  let Acc = []
  let parts = Parts
  while (true) {
    const m1 = matches<[ Hole<"First", string>, ...Hole<"Rest", string[]> ]>(parts)
    if (!m1) {
      break
    }
    parts = m1.Rest
    Acc = [...Acc, ...SplitManySep(m1.First, Seps)]
  }
  return Acc
}
/* compiles to:
 * export type ConcatSplits<Parts extends string[], Seps extends string[]> = ConcatSplits__loop<Parts, [], Seps>
 * type ConcatSplits__loop<Parts extends string[], Acc extends readonly unknown[], Seps extends string[]> = Parts extends [infer First extends string, ...(infer Rest extends string[])] ? ConcatSplits__loop<Rest, [...Acc, ...SplitManySep<First, Seps>], Seps> : Acc
 */

// ✓ SplitManySep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SplitManySep(Str: string, Sep: string[], Acc: string[] = []) {
  const m1 = matches<[ Hole<"FirstSep", string>, ...Hole<"RestSep", string[]> ]>(Sep)
  if (m1) {
    return ConcatSplits(H.Split(Str, m1.FirstSep), m1.RestSep)
  }
  return [Str, ...Acc]
}
/* compiles to:
 * export type SplitManySep<Str extends string, Sep extends string[], Acc extends string[] = []> = Sep extends [infer FirstSep extends string, ...(infer RestSep extends string[])] ? ConcatSplits<H.Split<Str, FirstSep>, RestSep> : [Str, ...Acc]
 */

// ✓ Split: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Split(Str, Sep: string, Seps = H.UnionToTuple(Sep)) {
  if (matches<string[]>(Seps)) {
    if (matches<string>(Str)) {
      return SplitManySep(Str, Seps)
    }
    return []
  }
  return []
}
/* compiles to:
 * export type Split<Str, Sep extends string, Seps = H.UnionToTuple<Sep>> = Seps extends string[] ? Str extends string ? SplitManySep<Str, Seps> : [] : []
 */

// ✓ StringToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StringToTuple(Str, Acc: string[] = []) {
  if (matches<string>(Str)) {
    const m1 = matches<`${Hole<"First">}${Hole<"Rest">}`>(Str)
    if (m1) {
      return StringToTuple(m1.Rest, [...Acc, m1.First])
    }
    return Acc
  }
  return []
}
/* compiles to:
 * export type StringToTuple<Str, Acc extends string[] = []> = Str extends string ? Str extends `${infer First}${infer Rest}` ? StringToTuple<Rest, [...Acc, First]> : Acc : []
 */

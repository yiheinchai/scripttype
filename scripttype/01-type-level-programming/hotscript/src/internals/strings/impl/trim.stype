/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/strings/impl/trim.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ TrimLeft: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrimLeft(Str: unknown, Sep: string) {
  if (matches<`${Sep}${infer Rest}`>(Str)) {
    return TrimLeft(Rest, Sep)
  }
  return Str
}
/* compiles to:
 * export type TrimLeft<Str, Sep extends string> = Str extends `${Sep}${infer Rest}` ? TrimLeft<Rest, Sep> : Str
 */

// ✓ TrimRight: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrimRight(Str: unknown, Sep: string) {
  if (matches<`${infer Rest}${Sep}`>(Str)) {
    return TrimRight(Rest, Sep)
  }
  return Str
}
/* compiles to:
 * export type TrimRight<Str, Sep extends string> = Str extends `${infer Rest}${Sep}` ? TrimRight<Rest, Sep> : Str
 */

// ✓ Trim: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Trim(Str: unknown, Sep: string) {
  return TrimLeft(TrimRight(Str, Sep), Sep)
}
/* compiles to:
 * export type Trim<Str, Sep extends string> = TrimLeft<TrimRight<Str, Sep>, Sep>
 */

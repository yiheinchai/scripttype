/**
 * ScriptType for 01-type-level-programming/hotscript/src/internals/strings/impl/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 7 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✗ String: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function String(It: Iterator) {
  return It[0][0]
}
/* compiles to:
 * export type String<It extends Iterator> = It[0][0]
 */

// ✗ Value: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function Value(It: Iterator) {
  return It[0][1]
}
/* compiles to:
 * export type Value<It extends Iterator> = It[0][1]
 */

// ✗ Size: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function Size(It: Iterator) {
  return It['length']
}
/* compiles to:
 * export type Size<It extends Iterator> = It['length']
 */

// ✗ Next: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function Next(It: Iterator) {
  return [[`${String(It)}${string}`, Add(Value(It), 1)], ...It]
}
/* compiles to:
 * export type Next<It extends Iterator> = [[`${String<It>}${string}`, Add<Value<It>, 1>], ...It]
 */

// ✗ Prev: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function Prev(It: Iterator) {
  if (matches<[ unknown, ...infer Rest extends Iterator ]>(It)) {
    return Rest
  }
  return undefined
}
/* compiles to:
 * export type Prev<It extends Iterator> = It extends [unknown, ...(infer Rest extends Iterator)] ? Rest : undefined
 */

// ✗ Double: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function Double(It: Iterator) {
  if (matches<`$${infer pattern}`>(`${String(It)}_`)) {
    if (matches<`${infer double}_`>(`${String(It)}${pattern}`)) {
      return [[double, Mul(Value(It), 2)], ...It]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type Double<It extends Iterator> = `${String<It>}_` extends `$${infer pattern}` ? `${String<It>}${pattern}` extends `${infer double}_` ? [[double, Mul<Value<It>, 2>], ...It] : never : never
 */

// ✗ CutAt: does not compile yet
//   Generic type 'Iterator<T, TReturn, TNext>' requires between 1 and 3 type arguments.
/* @scripttype preserveParamNames */
export function CutAt(T: string, It: Iterator) {
  if (matches<`${String<It>}${infer $Rest}`>(`$${T}`)) {
    return $Rest
  }
  return undefined
}
/* compiles to:
 * export type CutAt<T extends string, It extends Iterator> = `$${T}` extends `${String<It>}${infer $Rest}` ? $Rest : undefined
 */

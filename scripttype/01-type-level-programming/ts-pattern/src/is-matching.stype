/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/is-matching.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ PatternConstraint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PatternConstraint(T: unknown) {
  if (matches<readonly any[]>(T)) {
    return P.Pattern(T)
  }
  if (matches<object>(T)) {
    return P.Pattern(T) & UnknownProperties
  }
  return P.Pattern(T)
}
/* compiles to:
 * export type PatternConstraint<T> = T extends readonly any[] ? P.Pattern<T> : T extends object ? P.Pattern<T> & UnknownProperties : P.Pattern<T>
 */

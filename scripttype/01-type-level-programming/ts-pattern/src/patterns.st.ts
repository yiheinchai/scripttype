/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/patterns.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 9 covered, 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ExtractPreciseValue } from '../../../../01-type-level-programming/ts-pattern/src/types/ExtractPreciseValue.js'
import type { InvertPattern } from '../../../../01-type-level-programming/ts-pattern/src/types/InvertPattern.js'
import type { CustomP } from '../../../../01-type-level-programming/ts-pattern/src/types/Pattern.js'
declare namespace m1 {
  export type k<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type v<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type x<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const CustomP: any
declare const ExtractPreciseValue: any
declare const InvertPattern: any
declare const matcher: any
type matcher<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ unstable_Matchable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unstable_Matchable(narrowedOrFn, input = unknown, pattern = never): any {
  return CustomP(input, pattern, narrowedOrFn)
}
/* compiles to:
 * export type unstable_Matchable<narrowedOrFn, input = unknown, pattern = never> = CustomP<
 *   input,
 *   pattern,
 *   narrowedOrFn
 * >
 */

// ✓ unstable_Matcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unstable_Matcher(narrowedOrFn, input = unknown, pattern = never): any {
  return ReturnType(CustomP(input, pattern, narrowedOrFn)[matcher])
}
/* compiles to:
 * export type unstable_Matcher<narrowedOrFn, input = unknown, pattern = never> = ReturnType<
 *   CustomP<input, pattern, narrowedOrFn>[matcher]
 * >
 */

// ✗ infer: compiles but is not type-identical yet
//   eq=any
/* @scripttype preserveParamNames */
export function infer(pattern): any {
  return InvertPattern(NoInfer(pattern), unknown)
}
/* compiles to:
 * export type infer<pattern> = InvertPattern<NoInfer<pattern>, unknown>
 */

// ✗ narrow: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function narrow(input, pattern): any {
  return ExtractPreciseValue(input, InvertPattern(pattern, input))
}
/* compiles to:
 * export type narrow<input, pattern> = ExtractPreciseValue<input, InvertPattern<pattern, input>>
 */

// ✓ UnwrapArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapArray(xs): any {
  const m1 = matches<readonly (Hole<"x">)[]>(xs)
  if (m1) {
    return m1.x
  }
  return never
}
/* compiles to:
 * export type UnwrapArray<xs> = xs extends readonly (infer x)[] ? x : never
 */

// ✓ UnwrapSet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapSet(xs): any {
  const m1 = matches<Set<Hole<"x">>>(xs)
  if (m1) {
    return m1.x
  }
  return never
}
/* compiles to:
 * export type UnwrapSet<xs> = xs extends Set<infer x> ? x : never
 */

// ✓ UnwrapMapKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapMapKey(xs): any {
  const m1 = matches<Map<Hole<"k">, any>>(xs)
  if (m1) {
    return m1.k
  }
  return never
}
/* compiles to:
 * export type UnwrapMapKey<xs> = xs extends Map<infer k, any> ? k : never
 */

// ✓ UnwrapMapValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapMapValue(xs): any {
  const m1 = matches<Map<any, Hole<"v">>>(xs)
  if (m1) {
    return m1.v
  }
  return never
}
/* compiles to:
 * export type UnwrapMapValue<xs> = xs extends Map<any, infer v> ? v : never
 */

// ✓ UnwrapRecordKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapRecordKey(xs): any {
  const m1 = matches<Record<Hole<"k">, any>>(xs)
  if (m1) {
    return m1.k
  }
  return never
}
/* compiles to:
 * export type UnwrapRecordKey<xs> = xs extends Record<infer k, any> ? k : never
 */

// ✓ UnwrapRecordValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapRecordValue(xs): any {
  const m1 = matches<Record<any, Hole<"v">>>(xs)
  if (m1) {
    return m1.v
  }
  return never
}
/* compiles to:
 * export type UnwrapRecordValue<xs> = xs extends Record<any, infer v> ? v : never
 */

// ✓ WithDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDefault(a, b): any {
  if (matches<[ never ]>([a])) {
    return b
  }
  return a
}
/* compiles to:
 * export type WithDefault<a, b> = [a] extends [never] ? b : a
 */

/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/patterns.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 10 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const CustomP: any
declare const ExtractPreciseValue: any
declare const InvertPattern: any
declare const NoInfer: any
declare const matcher: any
type CustomP<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractPreciseValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InvertPattern<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NoInfer<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type matcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ unstable_Matchable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unstable_Matchable(narrowedOrFn, input = unknown, pattern = never) {
  return CustomP(input, pattern, narrowedOrFn)
}
/* compiles to:
 * export type unstable_Matchable<narrowedOrFn, input = unknown, pattern = never> = CustomP<input, pattern, narrowedOrFn>
 */

// ✓ unstable_Matcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unstable_Matcher(narrowedOrFn, input = unknown, pattern = never) {
  return ReturnType(CustomP(input, pattern, narrowedOrFn)[matcher])
}
/* compiles to:
 * export type unstable_Matcher<narrowedOrFn, input = unknown, pattern = never> = ReturnType<CustomP<input, pattern, narrowedOrFn>[matcher]>
 */

// ✗ infer: the ScriptType does not itself typecheck as TypeScript
//   infer.st.ts(3:24) TS2693: 'NoInfer' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function infer(pattern) {
  return InvertPattern(NoInfer(pattern), unknown)
}
/* compiles to:
 * export type infer<pattern> = InvertPattern<NoInfer<pattern>, unknown>
 */

// ✓ narrow: verified type-identical to the original
/* @scripttype preserveParamNames */
export function narrow(input, pattern) {
  return ExtractPreciseValue(input, InvertPattern(pattern, input))
}
/* compiles to:
 * export type narrow<input, pattern> = ExtractPreciseValue<input, InvertPattern<pattern, input>>
 */

// ✓ UnwrapArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapArray(xs) {
  const m1 = matches<readonly (Hole<"x">)[]>(xs)
  if (m1) {
    return m1.x
  }
  return never
}
/* compiles to:
 * export type UnwrapArray<xs> = xs extends readonly (infer x)[] ? x : never
 */

// ✓ UnwrapSet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapSet(xs) {
  const m1 = matches<Set<Hole<"x">>>(xs)
  if (m1) {
    return m1.x
  }
  return never
}
/* compiles to:
 * export type UnwrapSet<xs> = xs extends Set<infer x> ? x : never
 */

// ✓ UnwrapMapKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapMapKey(xs) {
  const m1 = matches<Map<Hole<"k">, any>>(xs)
  if (m1) {
    return m1.k
  }
  return never
}
/* compiles to:
 * export type UnwrapMapKey<xs> = xs extends Map<infer k, any> ? k : never
 */

// ✓ UnwrapMapValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapMapValue(xs) {
  const m1 = matches<Map<any, Hole<"v">>>(xs)
  if (m1) {
    return m1.v
  }
  return never
}
/* compiles to:
 * export type UnwrapMapValue<xs> = xs extends Map<any, infer v> ? v : never
 */

// ✓ UnwrapRecordKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapRecordKey(xs) {
  const m1 = matches<Record<Hole<"k">, any>>(xs)
  if (m1) {
    return m1.k
  }
  return never
}
/* compiles to:
 * export type UnwrapRecordKey<xs> = xs extends Record<infer k, any> ? k : never
 */

// ✓ UnwrapRecordValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapRecordValue(xs) {
  const m1 = matches<Record<any, Hole<"v">>>(xs)
  if (m1) {
    return m1.v
  }
  return never
}
/* compiles to:
 * export type UnwrapRecordValue<xs> = xs extends Record<any, infer v> ? v : never
 */

// ✓ WithDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDefault(a, b) {
  if (matches<[ never ]>([a])) {
    return b
  }
  return a
}
/* compiles to:
 * export type WithDefault<a, b> = [a] extends [never] ? b : a
 */

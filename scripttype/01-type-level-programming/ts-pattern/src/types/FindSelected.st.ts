/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/FindSelected.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 8 covered, 2 raw, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyMatcher: any
declare const Equal: any
declare const IsUnion: any
declare const Matcher: any
declare const MergeUnion: any
declare const Pattern: any
declare const Primitives: any
declare const SelectionsRecord: any
declare const ValueOf: any
declare const anonymousSelectKey: any
declare const symbols: any
type AnyMatcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Equal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Matcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MergeUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Pattern<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Primitives<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SelectionsRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Some<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValueOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type anonymousSelectKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type symbols<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Some: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Some(key: string) {
  return { type: 'some', key: key }
}
/* compiles to:
 * export type Some<key extends string> = { type: 'some'; key: key }
 */

// ✗ MapOptional: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function MapOptional(selections) {
  const out = emptyObject
  for (const k in keyof(selections)) {
    out[k] = raw('selections[k] extends [ infer v, infer subpath ] ? [ v | undefined, subpath ] : never')
  }
  return out
}

// ✗ MapList: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function MapList(selections) {
  const out = emptyObject
  for (const k in keyof(selections)) {
    out[k] = raw('selections[k] extends [ infer v, infer subpath ] ? [ v[], subpath ] : never')
  }
  return out
}

// ✓ ReduceFindSelectionUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReduceFindSelectionUnion(i, ps: readonly any[]) {
  let output = never
  let ps_ = ps
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(ps_)
    if (!m1) {
      break
    }
    ps_ = m1.tail
    output = output | FindSelectionUnion(i, m1.head)
  }
  return output
}
/* compiles to:
 * export type ReduceFindSelectionUnion<i, ps extends readonly any[]> = ReduceFindSelectionUnion__loop<ps, never, i>
 * type ReduceFindSelectionUnion__loop<Ps_ extends readonly any[], Output, I> = Ps_ extends readonly [infer head, ...infer tail] ? ReduceFindSelectionUnion__loop<tail, Output | FindSelectionUnion<I, head>, I> : Output
 */

// ✓ FindSelectionUnionInArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FindSelectionUnionInArray(i, p, path: any[] = [], output = never) {
  const m1 = matches<readonly (Hole<"iItem">)[]>(i)
  if (m1) {
    if (matches<readonly [ ]>(p)) {
      return output
    }
    const m2 = matches<readonly [ Hole<"p1">, ...Hole<"pRest"> ]>(p)
    if (m2) {
      const m3 = matches<readonly [ Hole<"i1">, ...Hole<"iRest"> ]>(i)
      if (m3) {
        return FindSelectionUnionInArray(m3.iRest, m2.pRest, [...path, p['length']], output | FindSelectionUnion(m3.i1, m2.p1, [...path, p['length']]))
      }
      return FindSelectionUnionInArray(arrayOf(m1.iItem), m2.pRest, [...path, p['length']], output | FindSelectionUnion(m1.iItem, m2.p1, [...path, p['length']]))
    }
    const m4 = matches<readonly [ ...Hole<"pInit">, Hole<"p1"> ]>(p)
    if (m4) {
      const m5 = matches<readonly [ ...Hole<"iInit">, Hole<"i1"> ]>(i)
      if (m5) {
        return FindSelectionUnionInArray(m5.iInit, m4.pInit, [...path, p['length']], output | FindSelectionUnion(m5.i1, m4.p1, [...path, p['length']]))
      }
      return FindSelectionUnionInArray(arrayOf(m1.iItem), m4.pInit, [...path, p['length']], output | FindSelectionUnion(m1.iItem, m4.p1, [...path, p['length']]))
    }
    const m6 = matches<readonly [ ...(readonly (Hole<"pRest"> & AnyMatcher)[]) ]>(p)
    if (m6) {
      return output | FindSelectionUnion(i, m6.pRest, [...path, p['length']])
    }
    return output | FindSelectionUnion(m1.iItem, ValueOf(p), [...path, Extract(p, readonlyArrayOf(any))['length']])
  }
  return output
}
/* compiles to:
 * export type FindSelectionUnionInArray<i, p, path extends any[] = [], output = never> = i extends readonly (infer iItem)[] ? p extends readonly [] ? output : p extends readonly [infer p1, ...infer pRest] ? i extends readonly [infer i1, ...infer iRest] ? FindSelectionUnionInArray<iRest, pRest, [...path, p['length']], output | FindSelectionUnion<i1, p1, [...path, p['length']]>> : FindSelectionUnionInArray<iItem[], pRest, [...path, p['length']], output | FindSelectionUnion<iItem, p1, [...path, p['length']]>> : p extends readonly [...infer pInit, infer p1] ? i extends readonly [...infer iInit, infer i1] ? FindSelectionUnionInArray<iInit, pInit, [...path, p['length']], output | FindSelectionUnion<i1, p1, [...path, p['length']]>> : FindSelectionUnionInArray<iItem[], pInit, [...path, p['length']], output | FindSelectionUnion<iItem, p1, [...path, p['length']]>> : p extends readonly [...readonly (infer pRest & AnyMatcher)[]] ? output | FindSelectionUnion<i, pRest, [...path, p['length']]> : output | FindSelectionUnion<iItem, ValueOf<p>, [...path, Extract<p, readonly any[]>['length']]> : output
 */

// ✓ FindSelectionUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FindSelectionUnion(i, p, path: any[] = []) {
  if (matches<1 & typeof i>(0)) {
    return never
  }
  if (matches<1 & typeof p>(0)) {
    return never
  }
  if (matches<Primitives>(p)) {
    return never
  }
  const m1 = matches<Matcher<any, Hole<"pattern">, Hole<"matcherType">, Hole<"sel">>>(p)
  if (m1) {
    const m2 = matches<Some<Hole<"k">>>(m1.sel)
    const out = emptyObject
    for (const kk in keySet(m2.k)) {
      out[kk] = [i, path]
    }
    const m3 = matches<readonly (Hole<"iItem">)[]>(i)
    const m4 = matches<[ Record<Hole<"k">, Hole<"v">>, [ Hole<"pkey">, Hole<"pvalue"> ] ]>([i, m1.pattern])
    const m5 = matches<[ Map<Hole<"k">, Hole<"v">>, [ Hole<"pkey">, Hole<"pvalue"> ] ]>([i, m1.pattern])
    const m6 = matches<Set<Hole<"v">>>(i)
    const m7 = matches<Some<Hole<"k">>>(m1.sel)
    const out2 = emptyObject
    for (const kk in keySet(m7.k)) {
      out2[kk] = [i, path]
    }
    return { select: m2 ? (out | FindSelectionUnion(i, m1.pattern, path)) : never, array: m3 ? MapList(FindSelectionUnion(m3.iItem, m1.pattern)) : never, record: m4 ? (MapList(FindSelectionUnion(m4.k, m4.pkey, path)) | MapList(FindSelectionUnion(m4.v, m4.pvalue, path))) : never, map: m5 ? (MapList(FindSelectionUnion(m5.k, m5.pkey, path)) | MapList(FindSelectionUnion(m5.v, m5.pvalue, path))) : never, set: m6 ? MapList(FindSelectionUnion(m6.v, m1.pattern, path)) : never, optional: MapOptional(FindSelectionUnion(i, m1.pattern)), or: MapOptional(ReduceFindSelectionUnion(i, Extract(m1.pattern, readonlyArrayOf(any)))), and: ReduceFindSelectionUnion(i, Extract(m1.pattern, readonlyArrayOf(any))), not: never, default: m7 ? out2 : never, custom: never }[m1.matcherType]
  }
  if (matches<readonly any[]>(p)) {
    return FindSelectionUnionInArray(i, p)
  }
  if (matches<{}>(p)) {
    if (matches<{}>(i)) {
      const out3 = emptyObject
      for (const k in keyof(p)) {
        out3[k] = matches<keyof typeof i>(k) ? FindSelectionUnion(i[k], p[k], [...path, k]) : never
      }
      return out3[keyof(p)]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type FindSelectionUnion<i, p, path extends any[] = []> = 0 extends 1 & i ? never : 0 extends 1 & p ? never : p extends Primitives ? never : p extends Matcher<any, infer pattern, infer matcherType, infer sel> ? { select: sel extends Some<infer k> ? { [Kk in k]: [i, path] } | FindSelectionUnion<i, pattern, path> : never; array: i extends readonly (infer iItem)[] ? MapList<FindSelectionUnion<iItem, pattern>> : never; record: [i, pattern] extends [Record<infer k, infer v>, [infer pkey, infer pvalue]] ? MapList<FindSelectionUnion<k, pkey, path>> | MapList<FindSelectionUnion<v, pvalue, path>> : never; map: [i, pattern] extends [Map<infer k, infer v>, [infer pkey, infer pvalue]] ? MapList<FindSelectionUnion<k, pkey, path>> | MapList<FindSelectionUnion<v, pvalue, path>> : never; set: i extends Set<infer v> ? MapList<FindSelectionUnion<v, pattern, path>> : never; optional: MapOptional<FindSelectionUnion<i, pattern>>; or: MapOptional<ReduceFindSelectionUnion<i, Extract<pattern, readonly any[]>>>; and: ReduceFindSelectionUnion<i, Extract<pattern, readonly any[]>>; not: never; default: sel extends Some<infer k> ? { [Kk1 in k]: [i, path] } : never; custom: never }[matcherType] : p extends readonly any[] ? FindSelectionUnionInArray<i, p> : p extends {} ? i extends {} ? { [K in keyof p]: K extends keyof i ? FindSelectionUnion<i[K], p[K], [...path, K]> : never }[keyof p] : never : never
 */

// ✓ SeveralAnonymousSelectError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SeveralAnonymousSelectError(a = 'You can only use a single anonymous selection (with `select()`) in your pattern. If you need to select multiple values, give them names with `select(<name>)` instead') {
  return merge({ __error: never }, a)
}
/* compiles to:
 * export type SeveralAnonymousSelectError<a = 'You can only use a single anonymous selection (with `select()`) in your pattern. If you need to select multiple values, give them names with `select(<name>)` instead'> = { __error: never } & a
 */

// ✓ MixedNamedAndAnonymousSelectError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MixedNamedAndAnonymousSelectError(a = 'Mixing named selections (`select("name")`) and anonymous selections (`select()`) is forbiden. Please, only use named selections.') {
  return merge({ __error: never }, a)
}
/* compiles to:
 * export type MixedNamedAndAnonymousSelectError<a = 'Mixing named selections (`select("name")`) and anonymous selections (`select()`) is forbiden. Please, only use named selections.'> = { __error: never } & a
 */

// ✗ SelectionToArgs: the ScriptType does not itself typecheck as TypeScript
//   SelectionToArgs.st.ts(7:17) TS2702: 'symbols' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SelectionToArgs(selections: SelectionsRecord) {
  if (matches<keyof typeof selections>(symbols.anonymousSelectKey)) {
    if (matches<true>(IsUnion(selections[symbols.anonymousSelectKey][1]))) {
      return SeveralAnonymousSelectError
    }
    if (matches<symbols.anonymousSelectKey>(keyof(selections))) {
      return selections[symbols.anonymousSelectKey][0]
    }
    return MixedNamedAndAnonymousSelectError
  }
  const out = emptyObject
  for (const k in keyof(selections)) {
    out[k] = selections[k][0]
  }
  return out
}
/* compiles to:
 * export type SelectionToArgs<selections extends SelectionsRecord> = symbols.anonymousSelectKey extends keyof selections ? IsUnion<selections[symbols.anonymousSelectKey][1]> extends true ? SeveralAnonymousSelectError : keyof selections extends symbols.anonymousSelectKey ? selections[symbols.anonymousSelectKey][0] : MixedNamedAndAnonymousSelectError : { [K in keyof selections]: selections[K][0] }
 */

// ✓ Selections: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Selections(i, p) {
  const m1 = matches<Hole<"u">>(FindSelectionUnion(i, p))
  if (m1) {
    if (matches<[ never ]>([m1.u])) {
      return i
    }
    return SelectionToArgs(Extract(MergeUnion(m1.u), SelectionsRecord))
  }
  return i
}
/* compiles to:
 * export type Selections<i, p> = FindSelectionUnion<i, p> extends infer u ? [u] extends [never] ? i : SelectionToArgs<Extract<MergeUnion<u>, SelectionsRecord>> : i
 */

// ✓ FindSelected: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FindSelected(i, p) {
  if (matches<true>(Equal(p, Pattern(i)))) {
    return i
  }
  return Selections(i, p)
}
/* compiles to:
 * export type FindSelected<i, p> = Equal<p, Pattern<i>> extends true ? i : Selections<i, p>
 */

/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/InvertPattern.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 12 type-level aliases: 9 covered, 2 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const All: any
declare const AnyMatcher: any
declare const Call: any
declare const Compute: any
declare const DeepExclude: any
declare const Equal: any
declare const Extends: any
declare const ExtractPlainObject: any
declare const Fn: any
declare const GetKey: any
declare const IsLiteral: any
declare const IsPlainObject: any
declare const IsReadonlyArray: any
declare const MapKey: any
declare const MapValue: any
declare const Matcher: any
declare const MaybeAddReadonly: any
declare const NonLiteralPrimitive: any
declare const Not: any
declare const Override: any
declare const Pattern: any
declare const Primitives: any
declare const PropertyKey: any
declare const ReadonlyArrayValue: any
declare const RecordKey: any
declare const RecordValue: any
declare const SetValue: any
declare const ValueOf: any
declare const WithDefault: any
declare const matchableInput: any
type All<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyMatcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Call<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Compute<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DeepExclude<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Equal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Extends<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractPlainObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Fn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GetKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsPlainObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsReadonlyArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MapKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MapValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Matcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MaybeAddReadonly<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonLiteralPrimitive<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Not<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Override<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Pattern<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Primitives<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlyArrayValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RecordKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RecordValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SetValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValueOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WithDefault<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type matchableInput<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ OptionalKeys: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function OptionalKeys(p) {
  const out = emptyObject
  for (const k in keyof(p)) {
    out[k] = matches<1 & (typeof p)[typeof k]>(0) ? never : (raw('p[k] extends Matcher<any, any, infer matcherType> ? matcherType extends \'optional\' ? k : never : never'))
  }
  return ValueOf(out)
}

// ✓ ReduceUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReduceUnion(tuple: readonly any[], i) {
  let output = never
  let tuple_ = tuple
  while (true) {
    const m1 = matches<readonly [ Hole<"p">, ...Hole<"tail"> ]>(tuple_)
    if (!m1) {
      break
    }
    tuple_ = m1.tail
    output = output | InvertPatternInternal(m1.p, i)
  }
  return output
}
/* compiles to:
 * export type ReduceUnion<tuple extends readonly any[], i> = ReduceUnion__loop<tuple, never, i>
 * type ReduceUnion__loop<Tuple_ extends readonly any[], Output, I> = Tuple_ extends readonly [infer p, ...infer tail] ? ReduceUnion__loop<tail, Output | InvertPatternInternal<p, I>, I> : Output
 */

// ✓ ReduceIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReduceIntersection(tuple: readonly any[], i) {
  let output = unknown
  let tuple_ = tuple
  while (true) {
    const m1 = matches<readonly [ Hole<"p">, ...Hole<"tail"> ]>(tuple_)
    if (!m1) {
      break
    }
    tuple_ = m1.tail
    output = output & InvertPatternInternal(m1.p, i)
  }
  return output
}
/* compiles to:
 * export type ReduceIntersection<tuple extends readonly any[], i> = ReduceIntersection__loop<tuple, unknown, i>
 * type ReduceIntersection__loop<Tuple_ extends readonly any[], Output, I> = Tuple_ extends readonly [infer p, ...infer tail] ? ReduceIntersection__loop<tail, Output & InvertPatternInternal<p, I>, I> : Output
 */

// ✓ InvertArrayPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvertArrayPattern(p, i, startOutput: any[] = [], endOutput: any[] = []) {
  const m1 = matches<readonly (Hole<"ii">)[]>(i)
  if (m1) {
    if (matches<readonly [ ]>(p)) {
      return [...startOutput, ...endOutput]
    }
    const m2 = matches<readonly [ Hole<"p1">, ...Hole<"pRest"> ]>(p)
    if (m2) {
      const m3 = matches<readonly [ Hole<"i1">, ...Hole<"iRest"> ]>(i)
      if (m3) {
        return InvertArrayPattern(m2.pRest, m3.iRest, [...startOutput, InvertPatternInternal(m2.p1, m3.i1)], endOutput)
      }
      return InvertArrayPattern(m2.pRest, arrayOf(m1.ii), [...startOutput, InvertPatternInternal(m2.p1, m1.ii)], endOutput)
    }
    const m4 = matches<readonly [ ...Hole<"pInit">, Hole<"p1"> ]>(p)
    if (m4) {
      const m5 = matches<readonly [ ...Hole<"iInit">, Hole<"i1"> ]>(i)
      if (m5) {
        return InvertArrayPattern(m4.pInit, m5.iInit, startOutput, [...endOutput, InvertPatternInternal(m4.p1, m5.i1)])
      }
      return InvertArrayPattern(m4.pInit, arrayOf(m1.ii), startOutput, [...endOutput, InvertPatternInternal(m4.p1, m1.ii)])
    }
    const m6 = matches<readonly [ ...(readonly (Hole<"pRest"> & AnyMatcher)[]) ]>(p)
    if (m6) {
      return [...startOutput, ...Extract(InvertPatternInternal(m6.pRest, i), readonlyArrayOf(any)), ...endOutput]
    }
    return [...startOutput, ...arrayOf(InvertPatternInternal(ValueOf(p), m1.ii)), ...endOutput]
  }
  return never
}
/* compiles to:
 * export type InvertArrayPattern<p, i, startOutput extends any[] = [], endOutput extends any[] = []> = i extends readonly (infer ii)[] ? p extends readonly [] ? [...startOutput, ...endOutput] : p extends readonly [infer p1, ...infer pRest] ? i extends readonly [infer i1, ...infer iRest] ? InvertArrayPattern<pRest, iRest, [...startOutput, InvertPatternInternal<p1, i1>], endOutput> : InvertArrayPattern<pRest, ii[], [...startOutput, InvertPatternInternal<p1, ii>], endOutput> : p extends readonly [...infer pInit, infer p1] ? i extends readonly [...infer iInit, infer i1] ? InvertArrayPattern<pInit, iInit, startOutput, [...endOutput, InvertPatternInternal<p1, i1>]> : InvertArrayPattern<pInit, ii[], startOutput, [...endOutput, InvertPatternInternal<p1, ii>]> : p extends readonly [...readonly (infer pRest & AnyMatcher)[]] ? [...startOutput, ...Extract<InvertPatternInternal<pRest, i>, readonly any[]>, ...endOutput] : [...startOutput, ...InvertPatternInternal<ValueOf<p>, ii>[], ...endOutput] : never
 */

// ✓ InvertPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvertPattern(p, input) {
  if (matches<true>(Equal(Pattern(input), p))) {
    return never
  }
  return InvertPatternInternal(p, input)
}
/* compiles to:
 * export type InvertPattern<p, input> = Equal<Pattern<input>, p> extends true ? never : InvertPatternInternal<p, input>
 */

// ✗ InvertPatternInternal: the ScriptType does not itself typecheck as TypeScript
//   InvertPatternInternal.st.ts(10:294) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function InvertPatternInternal(p, input) {
  if (matches<1 & typeof p>(0)) {
    return never
  }
  const m1 = matches<Matcher<Hole<"_input">, Hole<"subpattern">, Hole<"matcherType">, any, Hole<"narrowedOrFn">>>(p)
  if (m1) {
    const m2 = matches<[ Hole<"pk">, Hole<"pv"> ]>(m1.subpattern)
    const m3 = matches<[ Hole<"pk">, Hole<"pv"> ]>(m1.subpattern)
    return { not: DeepExclude(input, InvertPatternInternal(m1.subpattern, input)), select: InvertPatternInternal(m1.subpattern, input), array: arrayOf(InvertPatternInternal(m1.subpattern, ReadonlyArrayValue(input))), record: m2 ? Record(Extract(InvertPatternInternal(m2.pk, RecordKey(input)), PropertyKey), InvertPatternInternal(m2.pv, RecordValue(input))) : never, map: m3 ? Map(InvertPatternInternal(m3.pk, MapKey(Extract(input, Map(any, any)))), InvertPatternInternal(m3.pv, MapValue(Extract(input, Map(any, any))))) : never, set: Set(InvertPatternInternal(m1.subpattern, SetValue(Extract(input, Set(any))))), optional: anyOf(InvertPatternInternal(m1.subpattern, Exclude(input, Undefined)), Undefined), and: ReduceIntersection(Extract(m1.subpattern, readonlyArrayOf(any)), input), or: ReduceUnion(Extract(m1.subpattern, readonlyArrayOf(any)), input), default: matches<[ never ]>([m1.subpattern]) ? input : m1.subpattern, custom: Override(matches<Fn>(m1.narrowedOrFn) ? Call(m1.narrowedOrFn, input) : m1.narrowedOrFn) }[m1.matcherType]
  }
  if (matches<Primitives>(p)) {
    return p
  }
  if (matches<readonly any[]>(p)) {
    return InvertArrayPattern(p, WithDefault(Extract(input, readonlyArrayOf(any)), arrayOf(unknown)))
  }
  if (matches<true>(IsPlainObject(p))) {
    const m4 = matches<Hole<"optKeys">>(OptionalKeys(p))
    if (m4) {
      if (matches<[ never ]>([m4.optKeys])) {
        const out = emptyObject
        for (const k in keySet(Exclude(keyof(p), m4.optKeys))) {
          out[k] = InvertPatternInternal(p[k], WithDefault(GetKey(ExtractPlainObject(input), k), unknown))
        }
        return out
      }
      const out = emptyObject
      for (const k in keySet(Exclude(keyof(p), m4.optKeys))) {
        out[k] = InvertPatternInternal(p[k], WithDefault(GetKey(ExtractPlainObject(input), k), unknown))
      }
      const out2 = emptyObject
      for (const k in keySet(Extract(m4.optKeys, keyof(p)))) {
        out2[k] = optional(InvertPatternInternal(p[k], WithDefault(GetKey(ExtractPlainObject(input), k), unknown)))
      }
      return Compute(out & out2)
    }
    return never
  }
  return p
}
/* compiles to:
 * export type InvertPatternInternal<p, input> = 0 extends 1 & p ? never : p extends Matcher<unknown, infer subpattern, infer matcherType, any, infer narrowedOrFn> ? { not: DeepExclude<input, InvertPatternInternal<subpattern, input>>; select: InvertPatternInternal<subpattern, input>; array: InvertPatternInternal<subpattern, ReadonlyArrayValue<input>>[]; record: subpattern extends [infer pk, infer pv] ? Record<Extract<InvertPatternInternal<pk, RecordKey<input>>, PropertyKey>, InvertPatternInternal<pv, RecordValue<input>>> : never; map: subpattern extends [infer pk, infer pv] ? Map<InvertPatternInternal<pk, MapKey<Extract<input, Map<any, any>>>>, InvertPatternInternal<pv, MapValue<Extract<input, Map<any, any>>>>> : never; set: Set<InvertPatternInternal<subpattern, SetValue<Extract<input, Set<any>>>>>; optional: InvertPatternInternal<subpattern, Exclude<input, undefined>> | undefined; and: ReduceIntersection<Extract<subpattern, readonly any[]>, input>; or: ReduceUnion<Extract<subpattern, readonly any[]>, input>; default: [subpattern] extends [never] ? input : subpattern; custom: Override<narrowedOrFn extends Fn ? Call<narrowedOrFn, input> : narrowedOrFn> }[matcherType] : p extends Primitives ? p : p extends readonly any[] ? InvertArrayPattern<p, WithDefault<Extract<input, readonly any[]>, unknown[]>> : IsPlainObject<p> extends true ? OptionalKeys<p> extends infer optKeys ? [optKeys] extends [never] ? { [K in Exclude<keyof p, optKeys>]: InvertPatternInternal<p[K], WithDefault<GetKey<ExtractPlainObject<input>, K>, unknown>> } : Compute<{ [K1 in Exclude<keyof p, optKeys>]: InvertPatternInternal<p[K1], WithDefault<GetKey<ExtractPlainObject<input>, K1>, unknown>> } & { [K2 in Extract<optKeys, keyof p>]?: InvertPatternInternal<p[K2], WithDefault<GetKey<ExtractPlainObject<input>, K2>, unknown>> }> : never : p
 */

// ✓ ReduceIntersectionForExclude: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReduceIntersectionForExclude(tuple: readonly any[], i) {
  let output = unknown
  let tuple_ = tuple
  while (true) {
    const m1 = matches<readonly [ Hole<"p">, ...Hole<"tail"> ]>(tuple_)
    if (!m1) {
      break
    }
    tuple_ = m1.tail
    output = output & InvertPatternForExcludeInternal(m1.p, i, unknown)
  }
  return output
}
/* compiles to:
 * export type ReduceIntersectionForExclude<tuple extends readonly any[], i> = ReduceIntersectionForExclude__loop<tuple, unknown, i>
 * type ReduceIntersectionForExclude__loop<Tuple_ extends readonly any[], Output, I> = Tuple_ extends readonly [infer p, ...infer tail] ? ReduceIntersectionForExclude__loop<tail, Output & InvertPatternForExcludeInternal<p, I, unknown>, I> : Output
 */

// ✓ ReduceUnionForExclude: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReduceUnionForExclude(tuple: readonly any[], i) {
  let output = never
  let tuple_ = tuple
  while (true) {
    const m1 = matches<readonly [ Hole<"p">, ...Hole<"tail"> ]>(tuple_)
    if (!m1) {
      break
    }
    tuple_ = m1.tail
    output = output | InvertPatternForExcludeInternal(m1.p, i, never)
  }
  return output
}
/* compiles to:
 * export type ReduceUnionForExclude<tuple extends readonly any[], i> = ReduceUnionForExclude__loop<tuple, never, i>
 * type ReduceUnionForExclude__loop<Tuple_ extends readonly any[], Output, I> = Tuple_ extends readonly [infer p, ...infer tail] ? ReduceUnionForExclude__loop<tail, Output | InvertPatternForExcludeInternal<p, I, never>, I> : Output
 */

// ✓ ExcludeIfExists: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeIfExists(a, b) {
  if (matches<[ never ]>([b])) {
    return never
  }
  if (matches<typeof a>(unknown)) {
    return never
  }
  if (matches<true>(All([Extends(a, NonLiteralPrimitive), Not(IsLiteral(a)), IsLiteral(b)]))) {
    return never
  }
  return DeepExclude(a, b)
}
/* compiles to:
 * export type ExcludeIfExists<a, b> = [b] extends [never] ? never : unknown extends a ? never : All<[Extends<a, NonLiteralPrimitive>, Not<IsLiteral<a>>, IsLiteral<b>]> extends true ? never : DeepExclude<a, b>
 */

// ✓ InvertArrayPatternForExclude: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvertArrayPatternForExclude(p, i, empty, isReadonly: boolean, startOutput: any[] = [], endOutput: any[] = []) {
  const m1 = matches<readonly (Hole<"ii">)[]>(i)
  if (m1) {
    if (matches<readonly [ ]>(p)) {
      return MaybeAddReadonly([...startOutput, ...endOutput], isReadonly)
    }
    const m2 = matches<readonly [ Hole<"p1">, ...Hole<"pRest"> ]>(p)
    if (m2) {
      const m3 = matches<readonly [ Hole<"i1">, ...Hole<"iRest"> ]>(i)
      if (m3) {
        return InvertArrayPatternForExclude(m2.pRest, m3.iRest, empty, isReadonly, [...startOutput, InvertPatternForExcludeInternal(m2.p1, m3.i1, empty)], endOutput)
      }
      return InvertArrayPatternForExclude(m2.pRest, arrayOf(m1.ii), empty, isReadonly, [...startOutput, InvertPatternForExcludeInternal(m2.p1, m1.ii, empty)], endOutput)
    }
    const m4 = matches<readonly [ ...Hole<"pInit">, Hole<"p1"> ]>(p)
    if (m4) {
      const m5 = matches<readonly [ ...Hole<"iInit">, Hole<"i1"> ]>(i)
      if (m5) {
        return InvertArrayPatternForExclude(m4.pInit, m5.iInit, empty, isReadonly, startOutput, [...endOutput, InvertPatternForExcludeInternal(m4.p1, m5.i1, empty)])
      }
      return InvertArrayPatternForExclude(m4.pInit, arrayOf(m1.ii), empty, isReadonly, startOutput, [...endOutput, InvertPatternForExcludeInternal(m4.p1, m1.ii, empty)])
    }
    const m6 = matches<readonly [ ...(readonly (Hole<"pRest"> & AnyMatcher)[]) ]>(p)
    if (m6) {
      return MaybeAddReadonly([...startOutput, ...Extract(InvertPatternForExcludeInternal(m6.pRest, i, empty), readonlyArrayOf(any)), ...endOutput], isReadonly)
    }
    return MaybeAddReadonly([...startOutput, ...arrayOf(InvertPatternForExcludeInternal(ValueOf(p), m1.ii, empty)), ...endOutput], isReadonly)
  }
  return empty
}
/* compiles to:
 * export type InvertArrayPatternForExclude<p, i, empty, isReadonly extends boolean, startOutput extends any[] = [], endOutput extends any[] = []> = i extends readonly (infer ii)[] ? p extends readonly [] ? MaybeAddReadonly<[...startOutput, ...endOutput], isReadonly> : p extends readonly [infer p1, ...infer pRest] ? i extends readonly [infer i1, ...infer iRest] ? InvertArrayPatternForExclude<pRest, iRest, empty, isReadonly, [...startOutput, InvertPatternForExcludeInternal<p1, i1, empty>], endOutput> : InvertArrayPatternForExclude<pRest, ii[], empty, isReadonly, [...startOutput, InvertPatternForExcludeInternal<p1, ii, empty>], endOutput> : p extends readonly [...infer pInit, infer p1] ? i extends readonly [...infer iInit, infer i1] ? InvertArrayPatternForExclude<pInit, iInit, empty, isReadonly, startOutput, [...endOutput, InvertPatternForExcludeInternal<p1, i1, empty>]> : InvertArrayPatternForExclude<pInit, ii[], empty, isReadonly, startOutput, [...endOutput, InvertPatternForExcludeInternal<p1, ii, empty>]> : p extends readonly [...readonly (infer pRest & AnyMatcher)[]] ? MaybeAddReadonly<[...startOutput, ...Extract<InvertPatternForExcludeInternal<pRest, i, empty>, readonly any[]>, ...endOutput], isReadonly> : MaybeAddReadonly<[...startOutput, ...InvertPatternForExcludeInternal<ValueOf<p>, ii, empty>[], ...endOutput], isReadonly> : empty
 */

// ✓ InvertPatternForExclude: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvertPatternForExclude(p, i) {
  if (matches<true>(Equal(Pattern(i), p))) {
    return never
  }
  return InvertPatternForExcludeInternal(p, i)
}
/* compiles to:
 * export type InvertPatternForExclude<p, i> = Equal<Pattern<i>, p> extends true ? never : InvertPatternForExcludeInternal<p, i>
 */

// ✗ InvertPatternForExcludeInternal: the ScriptType does not itself typecheck as TypeScript
//   InvertPatternForExcludeInternal.st.ts(23:298) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function InvertPatternForExcludeInternal(p, i, empty = never) {
  if (matches<typeof p>(unknown)) {
    return i
  }
  if (matches<[ Primitives ]>([p])) {
    if (matches<true>(IsLiteral(p))) {
      return p
    }
    if (matches<true>(IsLiteral(i))) {
      return p
    }
    return empty
  }
  const m1 = matches<Matcher<Hole<"matchableInput">, Hole<"subpattern">, Hole<"matcherType">, any, Hole<"excluded">>>(p)
  if (m1) {
    const m2 = matches<readonly (Hole<"ii">)[]>(i)
    const m3 = matches<[ Hole<"pk">, Hole<"pv"> ]>(m1.subpattern)
    const m4 = matches<[ Hole<"pk">, Hole<"pv"> ]>(m1.subpattern)
    const m5 = matches<Map<Hole<"ik">, Hole<"iv">>>(i)
    const m6 = matches<Set<Hole<"iv">>>(i)
    const m7 = matches<Hole<"narrowedOrFn", Fn>>(m1.excluded)
    return { select: InvertPatternForExcludeInternal(m1.subpattern, i, empty), array: m2 ? MaybeAddReadonly(arrayOf(InvertPatternForExcludeInternal(m1.subpattern, m2.ii, empty)), IsReadonlyArray(i)) : empty, record: m3 ? Record(Extract(InvertPatternForExcludeInternal(m3.pk, RecordKey(i), empty), PropertyKey), InvertPatternForExcludeInternal(m3.pv, RecordValue(i), empty)) : empty, map: m4 ? (m5 ? Map(InvertPatternForExcludeInternal(m4.pk, m5.ik, empty), InvertPatternForExcludeInternal(m4.pv, m5.iv, empty)) : empty) : empty, set: m6 ? Set(InvertPatternForExcludeInternal(m1.subpattern, m6.iv, empty)) : empty, optional: anyOf(InvertPatternForExcludeInternal(m1.subpattern, i, empty), Undefined), and: ReduceIntersectionForExclude(Extract(m1.subpattern, readonlyArrayOf(any)), i), or: ReduceUnionForExclude(Extract(m1.subpattern, readonlyArrayOf(any)), i), not: ExcludeIfExists(matches<typeof m1.matchableInput>(unknown) ? i : m1.matchableInput, InvertPatternForExcludeInternal(m1.subpattern, i)), default: m1.excluded, custom: m7 ? Call(m7.narrowedOrFn, i) : m1.excluded }[m1.matcherType]
  }
  if (matches<readonly any[]>(p)) {
    const m8 = matches<Hole<"arrayInput">>(Extract(i, readonlyArrayOf(any)))
    if (m8) {
      return InvertArrayPatternForExclude(p, m8.arrayInput, empty, IsReadonlyArray(m8.arrayInput))
    }
    return never
  }
  if (matches<true>(IsPlainObject(p))) {
    if (matches<true>(Equal({}, p))) {
      return {}
    }
    if (matches<object>(i)) {
      if (matches<[ never ]>([keyof(p) & keyof(i)])) {
        return empty
      }
      const m9 = matches<Hole<"optKeys">>(OptionalKeys(p))
      if (m9) {
        if (matches<[ never ]>([m9.optKeys])) {
          const out = emptyObject
          for (const k in keyof(p)) {
            out[k] = readonlyProp(matches<keyof typeof i>(k) ? InvertPatternForExcludeInternal(p[k], i[k], empty) : InvertPatternInternal(p[k], unknown))
          }
          return out
        }
        const out = emptyObject
        for (const k in keySet(Exclude(keyof(p), m9.optKeys))) {
          out[k] = readonlyProp(matches<keyof typeof i>(k) ? InvertPatternForExcludeInternal(p[k], i[k], empty) : InvertPatternInternal(p[k], unknown))
        }
        const out2 = emptyObject
        for (const k in keySet(Extract(m9.optKeys, keyof(p)))) {
          out2[k] = readonlyProp(optional(matches<keyof typeof i>(k) ? InvertPatternForExcludeInternal(p[k], i[k], empty) : InvertPatternInternal(p[k], unknown)))
        }
        return Compute(out & out2)
      }
      return empty
    }
    return empty
  }
  return empty
}
/* compiles to:
 * export type InvertPatternForExcludeInternal<p, i, empty = never> = unknown extends p ? i : [p] extends [Primitives] ? IsLiteral<p> extends true ? p : IsLiteral<i> extends true ? p : empty : p extends Matcher<infer matchableInput, infer subpattern, infer matcherType, any, infer excluded> ? { select: InvertPatternForExcludeInternal<subpattern, i, empty>; array: i extends readonly (infer ii)[] ? MaybeAddReadonly<InvertPatternForExcludeInternal<subpattern, ii, empty>[], IsReadonlyArray<i>> : empty; record: subpattern extends [infer pk, infer pv] ? Record<Extract<InvertPatternForExcludeInternal<pk, RecordKey<i>, empty>, PropertyKey>, InvertPatternForExcludeInternal<pv, RecordValue<i>, empty>> : empty; map: subpattern extends [infer pk, infer pv] ? i extends Map<infer ik, infer iv> ? Map<InvertPatternForExcludeInternal<pk, ik, empty>, InvertPatternForExcludeInternal<pv, iv, empty>> : empty : empty; set: i extends Set<infer iv> ? Set<InvertPatternForExcludeInternal<subpattern, iv, empty>> : empty; optional: InvertPatternForExcludeInternal<subpattern, i, empty> | undefined; and: ReduceIntersectionForExclude<Extract<subpattern, readonly any[]>, i>; or: ReduceUnionForExclude<Extract<subpattern, readonly any[]>, i>; not: ExcludeIfExists<unknown extends matchableInput ? i : matchableInput, InvertPatternForExcludeInternal<subpattern, i>>; default: excluded; custom: excluded extends (infer narrowedOrFn extends Fn) ? Call<narrowedOrFn, i> : excluded }[matcherType] : p extends readonly any[] ? Extract<i, readonly any[]> extends infer arrayInput ? InvertArrayPatternForExclude<p, arrayInput, empty, IsReadonlyArray<arrayInput>> : never : IsPlainObject<p> extends true ? Equal<{}, p> extends true ? {} : i extends object ? [keyof p & keyof i] extends [never] ? empty : OptionalKeys<p> extends infer optKeys ? [optKeys] extends [never] ? { readonly [K in keyof p]: K extends keyof i ? InvertPatternForExcludeInternal<p[K], i[K], empty> : InvertPatternInternal<p[K], unknown> } : Compute<{ readonly [K1 in Exclude<keyof p, optKeys>]: K1 extends keyof i ? InvertPatternForExcludeInternal<p[K1], i[K1], empty> : InvertPatternInternal<p[K1], unknown> } & { readonly [K2 in Extract<optKeys, keyof p>]?: K2 extends keyof i ? InvertPatternForExcludeInternal<p[K2], i[K2], empty> : InvertPatternInternal<p[K2], unknown> }> : empty : empty : empty
 */

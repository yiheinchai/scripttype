/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/Match.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 2 raw, 2 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const DeepExclude: any
declare const InvertPatternForExclude: any
declare const symbols: any
declare const unset: any
type DeepExclude<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InvertPatternForExclude<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type symbols<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type unset<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ PickReturnValue: the ScriptType does not itself typecheck as TypeScript
//   PickReturnValue.st.ts(3:15) TS2702: 'symbols' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function PickReturnValue(a, b) {
  if (matches<symbols.unset>(a)) {
    return b
  }
  return a
}
/* compiles to:
 * export type PickReturnValue<a, b> = a extends symbols.unset ? b : a
 */

// ✗ Match: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function Match(i, o, handledCases: any[] = [], inferredOutput = never) {
  return raw('{ with<const p extends Pattern<i>, c, value extends MatchedValue<i, InvertPattern<p, i>>>(pattern: IsNever<p> extends true ? Pattern<i> : p, handler: (selections: FindSelected<value, p>, value: value) => PickReturnValue<o, c>): InvertPatternForExclude<p, value> extends infer excluded ? Match<Exclude<i, excluded>, o, [ ...handledCases, excluded ], Union<inferredOutput, c>> : never; with<const p1 extends Pattern<i>, const p2 extends Pattern<i>, c, p extends p1 | p2, value extends p extends any ? MatchedValue<i, InvertPattern<p, i>> : never>(p1: p1, p2: p2, handler: (value: value) => PickReturnValue<o, c>): [ InvertPatternForExclude<p1, value>, InvertPatternForExclude<p2, value> ] extends [ infer excluded1, infer excluded2 ] ? Match<Exclude<i, excluded1 | excluded2>, o, [ ...handledCases, excluded1, excluded2 ], Union<inferredOutput, c>> : never; with<const p1 extends Pattern<i>, const p2 extends Pattern<i>, const p3 extends Pattern<i>, const ps extends readonly Pattern<i>[], c, p extends p1 | p2 | p3 | ps[number], value extends MatchedValue<i, InvertPattern<p, i>>>(...args: [ p1: p1, p2: p2, p3: p3, ...patterns: ps, handler: (value: value) => PickReturnValue<o, c> ]): [ InvertPatternForExclude<p1, value>, InvertPatternForExclude<p2, value>, InvertPatternForExclude<p3, value>, MakeTuples<ps, value> ] extends [ infer excluded1, infer excluded2, infer excluded3, infer excludedRest ] ? Match<Exclude<i, excluded1 | excluded2 | excluded3 | Extract<excludedRest, any[]>[number]>, o, [ ...handledCases, excluded1, excluded2, excluded3, ...Extract<excludedRest, any[]> ], Union<inferredOutput, c>> : never; with<const pat extends Pattern<i>, pred extends (value: MatchedValue<i, InvertPattern<pat, i>>) => unknown, c, value extends GuardValue<pred>>(pattern: pat, predicate: pred, handler: (selections: FindSelected<value, pat>, value: value) => PickReturnValue<o, c>): pred extends (value: any) => value is infer narrowed ? Match<Exclude<i, narrowed>, o, [ ...handledCases, narrowed ], Union<inferredOutput, c>> : Match<i, o, handledCases, Union<inferredOutput, c>>; when<pred extends (value: i) => unknown, c, value extends GuardValue<pred>>(predicate: pred, handler: (value: value) => PickReturnValue<o, c>): pred extends (value: any) => value is infer narrowed ? Match<Exclude<i, narrowed>, o, [ ...handledCases, narrowed ], Union<inferredOutput, c>> : Match<i, o, handledCases, Union<inferredOutput, c>>; otherwise<c>(handler: (value: i) => PickReturnValue<o, c>): PickReturnValue<o, Union<inferredOutput, c>>; exhaustive: DeepExcludeAll<i, handledCases> extends infer remainingCases ? [ remainingCases ] extends [ never ] ? Exhaustive<o, inferredOutput> : NonExhaustiveError<remainingCases> : never; run(): PickReturnValue<o, inferredOutput>; returnType: [ inferredOutput ] extends [ never ] ? <output>() => Match<i, output, handledCases> : TSPatternError<\'calling `.returnType<T>()` is only allowed directly after `match(...)`.\'>; narrow(): Match<DeepExcludeAll<i, handledCases>, o, [ ], inferredOutput>; }')
}

// ✓ DeepExcludeAll: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepExcludeAll(a, tupleList: any[]) {
  if (matches<[ never ]>([a])) {
    return never
  }
  const m1 = matches<[ Hole<"excluded">, ...Hole<"tail"> ]>(tupleList)
  if (m1) {
    return DeepExcludeAll(DeepExclude(a, m1.excluded), m1.tail)
  }
  return a
}
/* compiles to:
 * export type DeepExcludeAll<a, tupleList extends any[]> = [a] extends [never] ? never : tupleList extends [infer excluded, ...infer tail] ? DeepExcludeAll<DeepExclude<a, excluded>, tail> : a
 */

// ✓ MakeTuples: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MakeTuples(ps: readonly any[], value) {
  const out = emptyObject
  for (const index in keyof(ps)) {
    out[index] = mutable(InvertPatternForExclude(ps[index], value))
  }
  return out
}
/* compiles to:
 * export type MakeTuples<ps extends readonly any[], value> = { -readonly [Index in keyof ps]: InvertPatternForExclude<ps[Index], value> }
 */

// ✗ Exhaustive: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature
/* @scripttype preserveParamNames */
export function Exhaustive(output, inferredOutput) {
  return raw('{ (): PickReturnValue<output, inferredOutput>; <otherOutput>(handler: (unexpectedValue: unknown) => PickReturnValue<output, otherOutput>): PickReturnValue<output, Union<inferredOutput, otherOutput>>; }')
}

/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/Pattern.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 27 type-level aliases: 27 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ExtractPreciseValue } from '../../../../../01-type-level-programming/ts-pattern/src/types/ExtractPreciseValue.js'
import type { None, SelectionType } from '../../../../../01-type-level-programming/ts-pattern/src/types/FindSelected.js'
import type { MatchResult, Matcher, MatcherType, UnknownValuePattern } from '../../../../../01-type-level-programming/ts-pattern/src/types/Pattern.js'
import type { MergeUnion, Primitives, WithDefault } from '../../../../../01-type-level-programming/ts-pattern/src/types/helpers.js'
declare namespace m1 {
  export type excluded1<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type excluded2<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type i<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type narrowed1<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type narrowed2<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace symbols {
  export type anonymousSelectKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ExtractPreciseValue: any
declare const I: any
declare const MatchResult: any
declare const Matcher: any
declare const MatcherType: any
declare const MergeUnion: any
declare const None: any
declare const Primitives: any
declare const SelectionType: any
declare const Some: any
declare const UnknownValuePattern: any
declare const WithDefault: any
declare const anonymousSelectKey: any
declare const end: any
declare const expr: any
declare const len: any
declare const max: any
declare const min: any
declare const p2: any
declare const start: any
declare const substr: any
declare const symbols: any
type GuardExcludeP<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Some<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type anonymousSelectKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type end<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type expr<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type len<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type max<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type min<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type p2<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type start<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type substr<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type symbols<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ MatcherProtocol: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MatcherProtocol(input, narrowed, matcherType: MatcherType, selections: SelectionType, excluded): any {
  return { match: genericFnType(['I'], [I | input], MatchResult), getSelectionKeys: optional(fnType([], arrayOf(string))), matcherType: optional(matcherType) }
}
/* compiles to:
 * export type MatcherProtocol<
 *   input,
 *   narrowed,
 *   matcherType extends MatcherType,
 *   selections extends SelectionType,
 *   excluded
 * > =
 *   {
 *     match: <I>(a0: I | input) => MatchResult
 *     getSelectionKeys?: () => string[]
 *     matcherType?: matcherType
 *   }
 */

// ✓ PatternMatcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PatternMatcher(input): any {
  return Matcher(input, unknown, any, any)
}
/* compiles to:
 * export type PatternMatcher<input> = Matcher<input, unknown, any, any>
 */

// ✓ MatchedValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MatchedValue(a, invpattern): any {
  return WithDefault(ExtractPreciseValue(a, invpattern), a)
}
/* compiles to:
 * export type MatchedValue<a, invpattern> = WithDefault<ExtractPreciseValue<a, invpattern>, a>
 */

// ✓ CustomP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CustomP(input, pattern, narrowedOrFn): any {
  return Matcher(input, pattern, 'custom', None, narrowedOrFn)
}
/* compiles to:
 * export type CustomP<input, pattern, narrowedOrFn> = Matcher<
 *   input,
 *   pattern,
 *   'custom',
 *   None,
 *   narrowedOrFn
 * >
 */

// ✓ ArrayP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayP(input, p): any {
  return Matcher(input, p, 'array')
}
/* compiles to:
 * export type ArrayP<input, p> = Matcher<input, p, 'array'>
 */

// ✓ RecordP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecordP(input, pkey, pvalue): any {
  return Matcher(input, [pkey, pvalue], 'record')
}
/* compiles to:
 * export type RecordP<input, pkey, pvalue> = Matcher<input, [pkey, pvalue], 'record'>
 */

// ✓ OptionalP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionalP(input, p): any {
  return Matcher(input, p, 'optional')
}
/* compiles to:
 * export type OptionalP<input, p> = Matcher<input, p, 'optional'>
 */

// ✓ MapP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapP(input, pkey, pvalue): any {
  return Matcher(input, [pkey, pvalue], 'map')
}
/* compiles to:
 * export type MapP<input, pkey, pvalue> = Matcher<input, [pkey, pvalue], 'map'>
 */

// ✓ SetP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SetP(input, p): any {
  return Matcher(input, p, 'set')
}
/* compiles to:
 * export type SetP<input, p> = Matcher<input, p, 'set'>
 */

// ✓ AndP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AndP(input, ps): any {
  return Matcher(input, ps, 'and')
}
/* compiles to:
 * export type AndP<input, ps> = Matcher<input, ps, 'and'>
 */

// ✓ OrP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OrP(input, ps): any {
  return Matcher(input, ps, 'or')
}
/* compiles to:
 * export type OrP<input, ps> = Matcher<input, ps, 'or'>
 */

// ✓ NotP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotP(input, p): any {
  return Matcher(input, p, 'not')
}
/* compiles to:
 * export type NotP<input, p> = Matcher<input, p, 'not'>
 */

// ✓ GuardP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardP(input, narrowed): any {
  return Matcher(input, narrowed)
}
/* compiles to:
 * export type GuardP<input, narrowed> = Matcher<input, narrowed>
 */

// ✓ GuardExcludeP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardExcludeP(input, narrowed, excluded): any {
  return Matcher(input, narrowed, 'default', None, excluded)
}
/* compiles to:
 * export type GuardExcludeP<input, narrowed, excluded> = Matcher<
 *   input,
 *   narrowed,
 *   'default',
 *   None,
 *   excluded
 * >
 */

// ✓ SelectP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectP(key: string, input = unknown, p = Matcher(unknown, unknown)): any {
  return Matcher(input, p, 'select', Some(key))
}
/* compiles to:
 * export type SelectP<key extends string, input = unknown, p = Matcher<unknown, unknown>> = Matcher<
 *   input,
 *   p,
 *   'select',
 *   Some<key>
 * >
 */

// ✓ Pattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Pattern(a = unknown): any {
  if (matches<typeof a>(unknown)) {
    return UnknownValuePattern
  }
  return KnownPattern(a)
}
/* compiles to:
 * export type Pattern<a = unknown> = unknown extends a ? UnknownValuePattern : KnownPattern<a>
 */

// ✓ KnownPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KnownPattern(a): any {
  return KnownPatternInternal(a)
}
/* compiles to:
 * export type KnownPattern<a> = KnownPatternInternal<a>
 */

// ✓ KnownPatternInternal: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KnownPatternInternal(a, objs = Exclude(a, anyOf(Primitives, t<Map<any, any>>(), t<Set<any>>(), readonlyArrayOf(any))), arrays = Extract(a, readonlyArrayOf(any)), primitives = Extract(a, Primitives)): any {
  return primitives | PatternMatcher(a) | (matches<[ never ]>([objs]) ? never : ObjectPattern(Readonly(MergeUnion(objs)))) | (matches<[ never ]>([arrays]) ? never : ArrayPattern(arrays))
}
/* compiles to:
 * export type KnownPatternInternal<
 *   a,
 *   objs = Exclude<a, Primitives | Map<any, any> | Set<any> | readonly any[]>,
 *   arrays = Extract<a, readonly any[]>,
 *   primitives = Extract<a, Primitives>
 * > =
 *   | primitives
 *   | PatternMatcher<a>
 *   | ([objs] extends [never] ? never : ObjectPattern<Readonly<MergeUnion<objs>>>)
 *   | ([arrays] extends [never] ? never : ArrayPattern<arrays>)
 */

// ✓ ObjectPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectPattern(a): any {
  const out = emptyObject
  for (const k in keyof(a)) {
    out[k] = readonlyProp(optional(Pattern(a[k])))
  }
  return out | never
}
/* compiles to:
 * export type ObjectPattern<a> = { readonly [K in keyof a]?: Pattern<a[K]> }
 */

// ✓ ArrayPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayPattern(a): any {
  const m1 = matches<readonly (Hole<"i">)[]>(a)
  if (m1) {
    if (matches<readonly [ any, ...any ]>(a)) {
      const out = emptyObject
      for (const index in keyof(a)) {
        out[index] = readonlyProp(Pattern(a[index]))
      }
      return out
    }
    return asReadonly([]) | asReadonly([Pattern(m1.i), ...arrayOf(Pattern(m1.i))]) | asReadonly([...arrayOf(Pattern(m1.i)), Pattern(m1.i)])
  }
  return never
}
/* compiles to:
 * export type ArrayPattern<a> =
 *   a extends readonly (infer i)[]
 *     ? a extends readonly [any, ...any]
 *       ? { readonly [Index in keyof a]: Pattern<a[Index]> }
 *       : | readonly []
 *       | readonly [Pattern<i>, ...Pattern<i>[]]
 *       | readonly [...Pattern<i>[], Pattern<i>]
 *     : never
 */

// ✓ MergeGuards: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeGuards(input, guard1, guard2): any {
  const m1 = matches<[ GuardExcludeP<any, Hole<"narrowed1">, Hole<"excluded1">>, GuardExcludeP<any, Hole<"narrowed2">, Hole<"excluded2">> ]>([guard1, guard2])
  if (m1) {
    return GuardExcludeP(input, merge(m1.narrowed1, m1.narrowed2), merge(m1.excluded1, m1.excluded2))
  }
  return never
}
/* compiles to:
 * export type MergeGuards<input, guard1, guard2> =
 *   [guard1, guard2] extends [GuardExcludeP<any, infer narrowed1, infer excluded1>, GuardExcludeP<any, infer narrowed2, infer excluded2>]
 *     ? GuardExcludeP<input, narrowed1 & narrowed2, excluded1 & excluded2>
 *     : never
 */

// ✓ Chainable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Chainable(p, omitted: string = never): any {
  return p & Omit({ optional: genericMethodType(['input'], [], Chainable(OptionalP(input, p), anyOf(omitted, 'optional'))), and: genericMethodType(['input', 'const p2 extends Pattern<input>'], [p2], Chainable(AndP(input, [p, p2]), omitted)), or: genericMethodType(['input', 'const p2 extends Pattern<input>'], [p2], Chainable(OrP(input, [p, p2]), omitted)), ...genericMethodType('select', ['input'], [], Chainable(SelectP(t<symbols.anonymousSelectKey>(), input, p), anyOf(omitted, 'select', 'or', 'and'))), ...genericMethodType('select', ['input', 'k extends string'], [k], Chainable(SelectP(k, input, p), anyOf(omitted, 'select', 'or', 'and'))) }, omitted)
}
/* compiles to:
 * export type Chainable<p, omitted extends string = never> =
 *   & p
 *   & Omit<
 *       {
 *         optional<input>(): Chainable<OptionalP<input, p>, omitted | 'optional'>
 *         and<input, const p2 extends Pattern<input>>(a0: p2): Chainable<
 *           AndP<input, [p, p2]>,
 *           omitted
 *         >
 *         or<input, const p2 extends Pattern<input>>(a0: p2): Chainable<
 *           OrP<input, [p, p2]>,
 *           omitted
 *         >
 *         select<input>(): Chainable<
 *           SelectP<symbols.anonymousSelectKey, input, p>,
 *           omitted | 'select' | 'or' | 'and'
 *         >
 *         select<input, k extends string>(a0: k): Chainable<
 *           SelectP<k, input, p>,
 *           omitted | 'select' | 'or' | 'and'
 *         >
 *       },
 *       omitted
 *     >
 */

// ✓ StringChainable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StringChainable(p: Matcher<any, any, any, any, any>, omitted: string = never): any {
  return Chainable(p, omitted) & Omit({ startsWith: genericMethodType(['input', 'const start extends string'], [start], StringChainable(MergeGuards(input, p, GuardP(unknown, `${start}${string}`)), anyOf(omitted, 'startsWith'))), endsWith: genericMethodType(['input', 'const end extends string'], [end], StringChainable(MergeGuards(input, p, GuardP(unknown, `${string}${end}`)), anyOf(omitted, 'endsWith'))), minLength: genericMethodType(['input', 'const min extends number'], [min], StringChainable(MergeGuards(input, p, GuardExcludeP(unknown, string, never)), anyOf(omitted, 'minLength'))), length: genericMethodType(['input', 'const len extends number'], [len], StringChainable(MergeGuards(input, p, GuardExcludeP(unknown, string, never)), anyOf(omitted, 'length', 'minLength', 'maxLength'))), maxLength: genericMethodType(['input', 'const max extends number'], [max], StringChainable(MergeGuards(input, p, GuardExcludeP(unknown, string, never)), anyOf(omitted, 'maxLength'))), includes: genericMethodType(['input', 'const substr extends string'], [substr], StringChainable(MergeGuards(input, p, GuardExcludeP(unknown, string, never)), omitted)), regex: genericMethodType(['input', 'const expr extends string | RegExp'], [expr], StringChainable(MergeGuards(input, p, GuardExcludeP(unknown, string, never)), omitted)) }, omitted)
}
/* compiles to:
 * export type StringChainable<
 *   p extends Matcher<any, any, any, any, any>,
 *   omitted extends string = never
 * > =
 *   & Chainable<p, omitted>
 *   & Omit<
 *       {
 *         startsWith<input, const start extends string>(a0: start): StringChainable<
 *           MergeGuards<input, p, GuardP<unknown, `${start}${string}`>>,
 *           omitted | 'startsWith'
 *         >
 *         endsWith<input, const end extends string>(a0: end): StringChainable<
 *           MergeGuards<input, p, GuardP<unknown, `${string}${end}`>>,
 *           omitted | 'endsWith'
 *         >
 *         minLength<input, const min extends number>(a0: min): StringChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, string, never>>,
 *           omitted | 'minLength'
 *         >
 *         length<input, const len extends number>(a0: len): StringChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, string, never>>,
 *           omitted | 'length' | 'minLength' | 'maxLength'
 *         >
 *         maxLength<input, const max extends number>(a0: max): StringChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, string, never>>,
 *           omitted | 'maxLength'
 *         >
 *         includes<input, const substr extends string>(a0: substr): StringChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, string, never>>,
 *           omitted
 *         >
 *         regex<input, const expr extends string | RegExp>(a0: expr): StringChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, string, never>>,
 *           omitted
 *         >
 *       },
 *       omitted
 *     >
 */

// ✓ NumberChainable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NumberChainable(p, omitted: string = never): any {
  return Chainable(p, omitted) & Omit({ between: genericMethodType(['input', 'const min extends number', 'const max extends number'], [min, max], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), omitted)), lt: genericMethodType(['input', 'const max extends number'], [max], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), omitted)), gt: genericMethodType(['input', 'const min extends number'], [min], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), omitted)), lte: genericMethodType(['input', 'const max extends number'], [max], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), omitted)), gte: genericMethodType(['input', 'const min extends number'], [min], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), omitted)), int: genericMethodType(['input'], [], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), anyOf(omitted, 'int'))), finite: genericMethodType(['input'], [], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), anyOf(omitted, 'finite'))), positive: genericMethodType(['input'], [], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), anyOf(omitted, 'positive', 'negative'))), negative: genericMethodType(['input'], [], NumberChainable(MergeGuards(input, p, GuardExcludeP(unknown, number, never)), anyOf(omitted, 'positive', 'negative', 'negative'))) }, omitted)
}
/* compiles to:
 * export type NumberChainable<p, omitted extends string = never> =
 *   & Chainable<p, omitted>
 *   & Omit<
 *       {
 *         between<input, const min extends number, const max extends number>(a0: min, a1: max): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted
 *         >
 *         lt<input, const max extends number>(a0: max): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted
 *         >
 *         gt<input, const min extends number>(a0: min): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted
 *         >
 *         lte<input, const max extends number>(a0: max): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted
 *         >
 *         gte<input, const min extends number>(a0: min): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted
 *         >
 *         int<input>(): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted | 'int'
 *         >
 *         finite<input>(): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted | 'finite'
 *         >
 *         positive<input>(): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted | 'positive' | 'negative'
 *         >
 *         negative<input>(): NumberChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, number, never>>,
 *           omitted | 'positive' | 'negative'
 *         >
 *       },
 *       omitted
 *     >
 */

// ✓ BigIntChainable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BigIntChainable(p, omitted: string = never): any {
  return Chainable(p, omitted) & Omit({ between: genericMethodType(['input', 'const min extends bigint', 'const max extends bigint'], [min, max], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), omitted)), lt: genericMethodType(['input', 'const max extends bigint'], [max], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), omitted)), gt: genericMethodType(['input', 'const min extends bigint'], [min], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), omitted)), lte: genericMethodType(['input', 'const max extends bigint'], [max], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), omitted)), gte: genericMethodType(['input', 'const min extends bigint'], [min], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), omitted)), positive: genericMethodType(['input'], [], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), anyOf(omitted, 'positive', 'negative'))), negative: genericMethodType(['input'], [], BigIntChainable(MergeGuards(input, p, GuardExcludeP(unknown, bigint, never)), anyOf(omitted, 'positive', 'negative', 'negative'))) }, omitted)
}
/* compiles to:
 * export type BigIntChainable<p, omitted extends string = never> =
 *   & Chainable<p, omitted>
 *   & Omit<
 *       {
 *         between<input, const min extends bigint, const max extends bigint>(a0: min, a1: max): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted
 *         >
 *         lt<input, const max extends bigint>(a0: max): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted
 *         >
 *         gt<input, const min extends bigint>(a0: min): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted
 *         >
 *         lte<input, const max extends bigint>(a0: max): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted
 *         >
 *         gte<input, const min extends bigint>(a0: min): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted
 *         >
 *         positive<input>(): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted | 'positive' | 'negative'
 *         >
 *         negative<input>(): BigIntChainable<
 *           MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>,
 *           omitted | 'positive' | 'negative'
 *         >
 *       },
 *       omitted
 *     >
 */

// ✓ Variadic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Variadic(pattern): any {
  return pattern & t<Iterable<typeof pattern>>()
}
/* compiles to:
 * export type Variadic<pattern> = pattern & Iterable<pattern>
 */

// ✓ ArrayChainable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayChainable(pattern, omitted: string = never): any {
  return Variadic(pattern) & Omit({ optional: genericMethodType(['input'], [], ArrayChainable(OptionalP(input, pattern), anyOf(omitted, 'optional'))), ...genericMethodType('select', ['input'], [], ArrayChainable(SelectP(t<symbols.anonymousSelectKey>(), input, pattern), anyOf(omitted, 'select'))), ...genericMethodType('select', ['input', 'k extends string'], [k], ArrayChainable(SelectP(k, input, pattern), anyOf(omitted, 'select'))) }, omitted)
}
/* compiles to:
 * export type ArrayChainable<pattern, omitted extends string = never> =
 *   & Variadic<pattern>
 *   & Omit<
 *       {
 *         optional<input>(): ArrayChainable<OptionalP<input, pattern>, omitted | 'optional'>
 *         select<input>(): ArrayChainable<
 *           SelectP<symbols.anonymousSelectKey, input, pattern>,
 *           omitted | 'select'
 *         >
 *         select<input, k extends string>(a0: k): ArrayChainable<
 *           SelectP<k, input, pattern>,
 *           omitted | 'select'
 *         >
 *       },
 *       omitted
 *     >
 */

/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/Pattern.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 27 type-level aliases: 19 covered, 6 raw, 2 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ExtractPreciseValue: any
declare const Iterable: any
declare const Matcher: any
declare const MatcherType: any
declare const MergeUnion: any
declare const None: any
declare const Primitives: any
declare const SelectionType: any
declare const Some: any
declare const UnknownValuePattern: any
declare const WithDefault: any
type ExtractPreciseValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GuardExcludeP<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Iterable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Matcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MatcherType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MergeUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type None<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Primitives<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SelectionType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Some<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownValuePattern<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WithDefault<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ MatcherProtocol: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function MatcherProtocol(input, narrowed, matcherType: MatcherType, selections: SelectionType, excluded) {
  return { match: raw('<I>(value: I | input) => MatchResult'), getSelectionKeys: optional(fnType([], arrayOf(string))), matcherType: optional(matcherType) }
}

// ✓ PatternMatcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PatternMatcher(input) {
  return Matcher(input, unknown, any, any)
}
/* compiles to:
 * export type PatternMatcher<input> = Matcher<input, unknown, any, any>
 */

// ✓ MatchedValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MatchedValue(a, invpattern) {
  return WithDefault(ExtractPreciseValue(a, invpattern), a)
}
/* compiles to:
 * export type MatchedValue<a, invpattern> = WithDefault<ExtractPreciseValue<a, invpattern>, a>
 */

// ✓ CustomP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CustomP(input, pattern, narrowedOrFn) {
  return Matcher(input, pattern, 'custom', None, narrowedOrFn)
}
/* compiles to:
 * export type CustomP<input, pattern, narrowedOrFn> = Matcher<input, pattern, 'custom', None, narrowedOrFn>
 */

// ✓ ArrayP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayP(input, p) {
  return Matcher(input, p, 'array')
}
/* compiles to:
 * export type ArrayP<input, p> = Matcher<input, p, 'array'>
 */

// ✓ RecordP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecordP(input, pkey, pvalue) {
  return Matcher(input, [pkey, pvalue], 'record')
}
/* compiles to:
 * export type RecordP<input, pkey, pvalue> = Matcher<input, [pkey, pvalue], 'record'>
 */

// ✓ OptionalP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionalP(input, p) {
  return Matcher(input, p, 'optional')
}
/* compiles to:
 * export type OptionalP<input, p> = Matcher<input, p, 'optional'>
 */

// ✓ MapP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapP(input, pkey, pvalue) {
  return Matcher(input, [pkey, pvalue], 'map')
}
/* compiles to:
 * export type MapP<input, pkey, pvalue> = Matcher<input, [pkey, pvalue], 'map'>
 */

// ✓ SetP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SetP(input, p) {
  return Matcher(input, p, 'set')
}
/* compiles to:
 * export type SetP<input, p> = Matcher<input, p, 'set'>
 */

// ✓ AndP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AndP(input, ps) {
  return Matcher(input, ps, 'and')
}
/* compiles to:
 * export type AndP<input, ps> = Matcher<input, ps, 'and'>
 */

// ✓ OrP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OrP(input, ps) {
  return Matcher(input, ps, 'or')
}
/* compiles to:
 * export type OrP<input, ps> = Matcher<input, ps, 'or'>
 */

// ✓ NotP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotP(input, p) {
  return Matcher(input, p, 'not')
}
/* compiles to:
 * export type NotP<input, p> = Matcher<input, p, 'not'>
 */

// ✓ GuardP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardP(input, narrowed) {
  return Matcher(input, narrowed)
}
/* compiles to:
 * export type GuardP<input, narrowed> = Matcher<input, narrowed>
 */

// ✓ GuardExcludeP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardExcludeP(input, narrowed, excluded) {
  return Matcher(input, narrowed, 'default', None, excluded)
}
/* compiles to:
 * export type GuardExcludeP<input, narrowed, excluded> = Matcher<input, narrowed, 'default', None, excluded>
 */

// ✓ SelectP: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectP(key: string, input = unknown, p = Matcher(unknown, unknown)) {
  return Matcher(input, p, 'select', Some(key))
}
/* compiles to:
 * export type SelectP<key extends string, input = unknown, p = Matcher<unknown, unknown>> = Matcher<input, p, 'select', Some<key>>
 */

// ✓ Pattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Pattern(a = unknown) {
  if (matches<typeof a>(unknown)) {
    return UnknownValuePattern
  }
  return KnownPattern(a)
}
/* compiles to:
 * export type Pattern<a = unknown> = unknown extends a ? UnknownValuePattern : KnownPattern<a>
 */

// ✓ KnownPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KnownPattern(a) {
  return KnownPatternInternal(a)
}
/* compiles to:
 * export type KnownPattern<a> = KnownPatternInternal<a>
 */

// ✗ KnownPatternInternal: the ScriptType does not itself typecheck as TypeScript
//   KnownPatternInternal.st.ts(2:72) TS2348: Value of type 'MapConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function KnownPatternInternal(a, objs = Exclude(a, Primitives | Map(any, any) | Set(any) | readonlyArrayOf(any)), arrays = Extract(a, readonlyArrayOf(any)), primitives = Extract(a, Primitives)) {
  return primitives | PatternMatcher(a) | (matches<[ never ]>([objs]) ? never : ObjectPattern(Readonly(MergeUnion(objs)))) | (matches<[ never ]>([arrays]) ? never : ArrayPattern(arrays))
}
/* compiles to:
 * export type KnownPatternInternal<a, objs = Exclude<a, Primitives | Map<any, any> | Set<any> | readonly any[]>, arrays = Extract<a, readonly any[]>, primitives = Extract<a, Primitives>> = primitives | PatternMatcher<a> | ([objs] extends [never] ? never : ObjectPattern<Readonly<MergeUnion<objs>>>) | ([arrays] extends [never] ? never : ArrayPattern<arrays>)
 */

// ✓ ObjectPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectPattern(a) {
  const out = emptyObject
  for (const k in keyof(a)) {
    out[k] = readonlyProp(optional(Pattern(a[k])))
  }
  return out | never
}
/* compiles to:
 * export type ObjectPattern<a> = { readonly [K in keyof a]?: Pattern<a[K]> }
 */

// ✓ ArrayPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayPattern(a) {
  const m1 = matches<readonly (Hole<"i">)[]>(a)
  if (m1) {
    if (matches<readonly [ any, ...any ]>(a)) {
      const out = emptyObject
      for (const index in keyof(a)) {
        out[index] = readonlyProp(Pattern(a[index]))
      }
      return out
    }
    return asReadonly([]) | asReadonly([Pattern(m1.i), ...arrayOf(Pattern(m1.i))]) | asReadonly([...arrayOf(Pattern(m1.i)), Pattern(m1.i)])
  }
  return never
}
/* compiles to:
 * export type ArrayPattern<a> = a extends readonly (infer i)[] ? a extends readonly [any, ...any] ? { readonly [Index in keyof a]: Pattern<a[Index]> } : readonly [] | readonly [Pattern<i>, ...Pattern<i>[]] | readonly [...Pattern<i>[], Pattern<i>] : never
 */

// ✓ MergeGuards: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeGuards(input, guard1, guard2) {
  const m1 = matches<[ GuardExcludeP<any, Hole<"narrowed1">, Hole<"excluded1">>, GuardExcludeP<any, Hole<"narrowed2">, Hole<"excluded2">> ]>([guard1, guard2])
  if (m1) {
    return GuardExcludeP(input, m1.narrowed1 & m1.narrowed2, m1.excluded1 & m1.excluded2)
  }
  return never
}
/* compiles to:
 * export type MergeGuards<input, guard1, guard2> = [guard1, guard2] extends [GuardExcludeP<any, infer narrowed1, infer excluded1>, GuardExcludeP<any, infer narrowed2, infer excluded2>] ? GuardExcludeP<input, narrowed1 & narrowed2, excluded1 & excluded2> : never
 */

// ✗ Chainable: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function Chainable(p, omitted: string = never) {
  return p & Omit(raw('{ optional<input>(): Chainable<OptionalP<input, p>, omitted | \'optional\'>; and<input, const p2 extends Pattern<input>>(pattern: p2): Chainable<AndP<input, [ p, p2 ]>, omitted>; or<input, const p2 extends Pattern<input>>(pattern: p2): Chainable<OrP<input, [ p, p2 ]>, omitted>; select<input>(): Chainable<SelectP<symbols.anonymousSelectKey, input, p>, omitted | \'select\' | \'or\' | \'and\'>; select<input, k extends string>(key: k): Chainable<SelectP<k, input, p>, omitted | \'select\' | \'or\' | \'and\'>; }'), omitted)
}

// ✗ StringChainable: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function StringChainable(p: Matcher<any, any, any, any, any>, omitted: string = never) {
  return Chainable(p, omitted) & Omit(raw('{ startsWith<input, const start extends string>(start: start): StringChainable<MergeGuards<input, p, GuardP<unknown, `${start}${string}`>>, omitted | \'startsWith\'>; endsWith<input, const end extends string>(end: end): StringChainable<MergeGuards<input, p, GuardP<unknown, `${string}${end}`>>, omitted | \'endsWith\'>; minLength<input, const min extends number>(min: min): StringChainable<MergeGuards<input, p, GuardExcludeP<unknown, string, never>>, omitted | \'minLength\'>; length<input, const len extends number>(len: len): StringChainable<MergeGuards<input, p, GuardExcludeP<unknown, string, never>>, omitted | \'length\' | \'minLength\' | \'maxLength\'>; maxLength<input, const max extends number>(max: max): StringChainable<MergeGuards<input, p, GuardExcludeP<unknown, string, never>>, omitted | \'maxLength\'>; includes<input, const substr extends string>(substr: substr): StringChainable<MergeGuards<input, p, GuardExcludeP<unknown, string, never>>, omitted>; regex<input, const expr extends string | RegExp>(expr: expr): StringChainable<MergeGuards<input, p, GuardExcludeP<unknown, string, never>>, omitted>; }'), omitted)
}

// ✗ NumberChainable: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function NumberChainable(p, omitted: string = never) {
  return Chainable(p, omitted) & Omit(raw('{ between<input, const min extends number, const max extends number>(min: min, max: max): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted>; lt<input, const max extends number>(max: max): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted>; gt<input, const min extends number>(min: min): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted>; lte<input, const max extends number>(max: max): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted>; gte<input, const min extends number>(min: min): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted>; int<input>(): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted | \'int\'>; finite<input>(): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted | \'finite\'>; positive<input>(): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted | \'positive\' | \'negative\'>; negative<input>(): NumberChainable<MergeGuards<input, p, GuardExcludeP<unknown, number, never>>, omitted | \'positive\' | \'negative\' | \'negative\'>; }'), omitted)
}

// ✗ BigIntChainable: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function BigIntChainable(p, omitted: string = never) {
  return Chainable(p, omitted) & Omit(raw('{ between<input, const min extends bigint, const max extends bigint>(min: min, max: max): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted>; lt<input, const max extends bigint>(max: max): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted>; gt<input, const min extends bigint>(min: min): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted>; lte<input, const max extends bigint>(max: max): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted>; gte<input, const min extends bigint>(min: min): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted>; positive<input>(): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted | \'positive\' | \'negative\'>; negative<input>(): BigIntChainable<MergeGuards<input, p, GuardExcludeP<unknown, bigint, never>>, omitted | \'positive\' | \'negative\' | \'negative\'>; }'), omitted)
}

// ✗ Variadic: the ScriptType does not itself typecheck as TypeScript
//   Variadic.st.ts(3:20) TS2693: 'Iterable' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function Variadic(pattern) {
  return pattern & Iterable(pattern)
}
/* compiles to:
 * export type Variadic<pattern> = pattern & Iterable<pattern>
 */

// ✗ ArrayChainable: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function ArrayChainable(pattern, omitted: string = never) {
  return Variadic(pattern) & Omit(raw('{ optional<input>(): ArrayChainable<OptionalP<input, pattern>, omitted | \'optional\'>; select<input>(): ArrayChainable<SelectP<symbols.anonymousSelectKey, input, pattern>, omitted | \'select\'>; select<input, k extends string>(key: k): ArrayChainable<SelectP<k, input, pattern>, omitted | \'select\'>; }'), omitted)
}

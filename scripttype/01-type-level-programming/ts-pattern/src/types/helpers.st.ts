/**
 * ScriptType for 01-type-level-programming/ts-pattern/src/types/helpers.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 48 type-level aliases: 44 covered, 2 typecheck-error, 1 compile-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltInObjects: any
declare const Fn: any
declare const PropertyKey: any
type BuiltInObjects<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Equal<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Fn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnionToIntersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ValueOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValueOf(a) {
  if (matches<readonly any[]>(a)) {
    return a[number]
  }
  return a[keyof(a)]
}
/* compiles to:
 * export type ValueOf<a> = a extends readonly any[] ? a[number] : a[keyof a]
 */

// ✓ Values: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Values(a: object) {
  return UnionToTuple(ValueOf(a))
}
/* compiles to:
 * export type Values<a extends object> = UnionToTuple<ValueOf<a>>
 */

// ✓ LeastUpperBound: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LeastUpperBound(a, b) {
  if (matches<typeof a>(b)) {
    return b
  }
  if (matches<typeof b>(a)) {
    return a
  }
  return never
}
/* compiles to:
 * export type LeastUpperBound<a, b> = b extends a ? b : a extends b ? a : never
 */

// ✗ Contains: the ScriptType does not itself typecheck as TypeScript
//   Contains.st.ts(4:115) TS2538: Type 'symbol' cannot be used as an index type.
/* @scripttype preserveParamNames */
export function Contains(a, b) {
  if (matches<any>(a)) {
    if (matches<{ [k in keyof typeof a]-?: Equal<(typeof a)[k], typeof b> extends true ? 'exclude' : 'include'; }[keyof typeof a]>('exclude')) {
      return true
    }
    return false
  }
  return never
}
/* compiles to:
 * export type Contains<a, b> =
 *   a extends any
 *     ? 'exclude' extends { [k in keyof typeof a]-?: Equal<(typeof a)[k], typeof b> extends true ? 'exclude' : 'include'; }[keyof a]
 *       ? true
 *       : false
 *     : never
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(union) {
  const m1 = matches<(k: Hole<"intersection">) => void>(matches<any>(union) ? fnType([union], voidType()) : never)
  if (m1) {
    return m1.intersection
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<union> =
 *   (union extends any ? (a0: union) => void : never) extends (k: infer intersection) => void
 *     ? intersection
 *     : never
 */

// ✓ IsUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnion(a) {
  if (matches<[ UnionToIntersection<typeof a> ]>([a])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsUnion<a> = [a] extends [UnionToIntersection<a>] ? false : true
 */

// ✓ UnionToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToTuple(union) {
  let output: any[] = []
  let union_ = union
  while (true) {
    const m1 = matches<(_: any) => Hole<"elem">>(UnionToIntersection(matches<any>(union_) ? fnType([union_], union_) : never))
    if (!m1) {
      break
    }
    union_ = Exclude(union_, m1.elem)
    output.unshift(m1.elem)
  }
  return output
}
/* compiles to:
 * export type UnionToTuple<union> = UnionToTuple__loop<union, []>
 * type UnionToTuple__loop<Union_, Output extends any[]> =
 *   UnionToIntersection<Union_ extends any ? (a0: Union_) => Union_ : never> extends (_: any) => infer elem
 *     ? UnionToTuple__loop<Exclude<Union_, elem>, [elem, ...Output]>
 *     : Output
 */

// ✓ Flatten: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Flatten(xs: readonly any[]) {
  let output: any[] = []
  let xs_ = xs
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(xs_)
    if (!m1) {
      break
    }
    xs_ = m1.tail
    output = [...output, ...Extract(m1.head, readonlyArrayOf(any))]
  }
  return output
}
/* compiles to:
 * export type Flatten<xs extends readonly any[]> = Flatten__loop<xs, []>
 * type Flatten__loop<Xs_ extends readonly any[], Output extends any[]> =
 *   Xs_ extends readonly [infer head, ...infer tail]
 *     ? Flatten__loop<tail, [...Output, ...Extract<head, readonly any[]>]>
 *     : Output
 */

// ✗ Equal: does not compile yet
//   'b' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function Equal(a, b) {
  if (matches<<T>() => T extends typeof b ? 1 : 2>(genericFnType(['T'], [], matches<typeof a>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equal<a, b> =
 *   (<T>() => T extends a ? 1 : 2) extends <T>() => T extends typeof b ? 1 : 2 ? true : false
 */

// ✓ Expect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Expect(a: true) {
  return a
}
/* compiles to:
 * export type Expect<a extends true> = a
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(a) {
  if (matches<1 & typeof a>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<a> = 0 extends 1 & a ? true : false
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T) {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ Length: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Length(it: readonly any[]) {
  return it['length']
}
/* compiles to:
 * export type Length<it extends readonly any[]> = it['length']
 */

// ✓ Iterator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Iterator(n: number) {
  let it: any[] = []
  while (true) {
    if (matches<typeof n>(it['length'])) {
      break
    }
    it.unshift(any)
  }
  return it
}
/* compiles to:
 * export type Iterator<n extends number> = Iterator__loop<[], n>
 * type Iterator__loop<It extends any[], N extends number> =
 *   It['length'] extends N ? It : Iterator__loop<[any, ...It], N>
 */

// ✓ Next: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Next(it: any[]) {
  return [any, ...it]
}
/* compiles to:
 * export type Next<it extends any[]> = [any, ...it]
 */

// ✓ Prev: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prev(it: any[]) {
  const m1 = matches<readonly [ any, ...Hole<"tail"> ]>(it)
  if (m1) {
    return m1.tail
  }
  return []
}
/* compiles to:
 * export type Prev<it extends any[]> = it extends readonly [any, ...infer tail] ? tail : []
 */

// ✓ Take: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Take(xs: readonly any[], it: any[], output: any[] = []) {
  if (matches<0>(Length(it))) {
    return output
  }
  const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(xs)
  if (m1) {
    return Take(m1.tail, Prev(it), [...output, m1.head])
  }
  return output
}
/* compiles to:
 * export type Take<xs extends readonly any[], it extends any[], output extends any[] = []> =
 *   Length<it> extends 0 ? output
 *   : xs extends readonly [infer head, ...infer tail] ? Take<tail, Prev<it>, [...output, head]>
 *   : output
 */

// ✓ Drop: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Drop(xs: readonly any[], n: any[]) {
  if (matches<0>(Length(n))) {
    return xs
  }
  const m1 = matches<readonly [ any, ...Hole<"tail"> ]>(xs)
  if (m1) {
    return Drop(m1.tail, Prev(n))
  }
  return []
}
/* compiles to:
 * export type Drop<xs extends readonly any[], n extends any[]> =
 *   Length<n> extends 0 ? xs : xs extends readonly [any, ...infer tail] ? Drop<tail, Prev<n>> : []
 */

// ✓ UpdateAt: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UpdateAt(tail: readonly any[], n: any[], value, inits: readonly any[] = []) {
  if (matches<0>(Length(n))) {
    const m1 = matches<readonly [ any, ...Hole<"tail"> ]>(tail)
    if (m1) {
      return [...inits, value, ...m1.tail]
    }
    return inits
  }
  const m2 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(tail)
  if (m2) {
    return UpdateAt(m2.tail, Prev(n), value, [...inits, m2.head])
  }
  return inits
}
/* compiles to:
 * export type UpdateAt<
 *   tail extends readonly any[],
 *   n extends any[],
 *   value,
 *   inits extends readonly any[] = []
 * > =
 *   Length<n> extends 0
 *     ? tail extends readonly [any, ...infer tail] ? [...inits, value, ...tail] : inits
 *   : tail extends readonly [infer head, ...infer tail]
 *     ? UpdateAt<tail, Prev<n>, value, [...inits, head]>
 *   : inits
 */

// ✓ IsPlainObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPlainObject(o, excludeUnion = BuiltInObjects) {
  if (matches<object>(o)) {
    if (matches<string | typeof excludeUnion>(o)) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsPlainObject<o, excludeUnion = BuiltInObjects> =
 *   o extends object ? o extends string | excludeUnion ? false : true : false
 */

// ✓ Compute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Compute(a: any) {
  if (matches<BuiltInObjects>(a)) {
    return a
  }
  const out = emptyObject
  for (const k in keyof(a)) {
    out[k] = a[k]
  }
  return out
}
/* compiles to:
 * export type Compute<a extends any> = a extends BuiltInObjects ? a : { [K in keyof a]: a[K] }
 */

// ✗ IntersectObjects: the ScriptType does not itself typecheck as TypeScript
//   IntersectObjects.st.ts(6:48) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function IntersectObjects(a) {
  const m1 = matches<Hole<"allKeys">>(matches<any>(a) ? keyof(a) : never)
  if (m1) {
    const out = emptyObject
    for (const k in keySet(Extract(m1.allKeys, PropertyKey))) {
      out[k] = matches<any>(a) ? (k in a ? a[k] : never) : never
    }
    return out
  }
  return never
}
/* compiles to:
 * export type IntersectObjects<a> =
 *   (a extends any ? keyof a : never) extends infer allKeys
 *     ? {
 *       [K in Extract<allKeys, PropertyKey>]: a extends any
 *         ? K extends keyof a ? a[K] : never
 *         : never
 *     }
 *     : never
 */

// ✓ WithDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDefault(a, def) {
  if (matches<[ never ]>([a])) {
    return def
  }
  return a
}
/* compiles to:
 * export type WithDefault<a, def> = [a] extends [never] ? def : a
 */

// ✓ IsLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsLiteral(a) {
  if (matches<[ null | undefined ]>([a])) {
    return true
  }
  if (matches<[ string ]>([a])) {
    if (matches<typeof a>(string)) {
      return false
    }
    return true
  }
  if (matches<[ number ]>([a])) {
    if (matches<typeof a>(number)) {
      return false
    }
    return true
  }
  if (matches<[ boolean ]>([a])) {
    if (matches<typeof a>(boolean)) {
      return false
    }
    return true
  }
  if (matches<[ symbol ]>([a])) {
    if (matches<typeof a>(symbol)) {
      return false
    }
    return true
  }
  if (matches<[ bigint ]>([a])) {
    if (matches<typeof a>(bigint)) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsLiteral<a> =
 *   [a] extends [null | undefined] ? true
 *   : [a] extends [string] ? string extends a ? false : true
 *   : [a] extends [number] ? number extends a ? false : true
 *   : [a] extends [boolean] ? boolean extends a ? false : true
 *   : [a] extends [symbol] ? symbol extends a ? false : true
 *   : [a] extends [bigint] ? bigint extends a ? false : true
 *   : false
 */

// ✓ Union: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Union(a, b) {
  if (matches<[ typeof a ]>([b])) {
    return a
  }
  if (matches<[ typeof b ]>([a])) {
    return b
  }
  return a | b
}
/* compiles to:
 * export type Union<a, b> = [b] extends [a] ? a : [a] extends [b] ? b : a | b
 */

// ✓ GuardValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardValue(fn) {
  const m1 = matches<(value: any) => value is Hole<"b">>(fn)
  if (m1) {
    return m1.b
  }
  const m2 = matches<(value: Hole<"a">) => unknown>(fn)
  if (m2) {
    return m2.a
  }
  return never
}
/* compiles to:
 * export type GuardValue<fn> =
 *   fn extends (value: any) => value is infer b ? b
 *   : fn extends (value: infer a) => unknown ? a
 *   : never
 */

// ✗ GuardFunction: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function GuardFunction(input, narrowed) {
  return fnType([input], raw('value is Extract<narrowed, input>')) | fnType([input], boolean)
}

// ✓ Some: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Some(bools: boolean[]) {
  if (matches<(typeof bools)[number]>(true)) {
    return true
  }
  return false
}
/* compiles to:
 * export type Some<bools extends boolean[]> = true extends bools[number] ? true : false
 */

// ✓ All: verified type-identical to the original
/* @scripttype preserveParamNames */
export function All(bools: boolean[]) {
  if (matches<true>(bools[number])) {
    return true
  }
  return false
}
/* compiles to:
 * export type All<bools extends boolean[]> = bools[number] extends true ? true : false
 */

// ✓ Extends: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Extends(a, b) {
  if (matches<[ typeof b ]>([a])) {
    return true
  }
  return false
}
/* compiles to:
 * export type Extends<a, b> = [a] extends [b] ? true : false
 */

// ✓ Not: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Not(a: boolean) {
  if (matches<true>(a)) {
    return false
  }
  return true
}
/* compiles to:
 * export type Not<a extends boolean> = a extends true ? false : true
 */

// ✓ AllKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AllKeys(a) {
  if (matches<any>(a)) {
    return keyof(a)
  }
  return never
}
/* compiles to:
 * export type AllKeys<a> = a extends any ? keyof a : never
 */

// ✓ MergeUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeUnion(a) {
  const out = emptyObject
  for (const k in keySet(AllKeys(a))) {
    out[k] = matches<any>(a) ? (k in a ? a[k] : never) : never
  }
  return out | never
}
/* compiles to:
 * export type MergeUnion<a> = {
 *   [K in AllKeys<a>]: a extends any ? K extends keyof a ? a[K] : never : never
 * }
 */

// ✓ IsFixedSizeTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsFixedSizeTuple(a: readonly any[]) {
  return IsLiteral(Length(a))
}
/* compiles to:
 * export type IsFixedSizeTuple<a extends readonly any[]> = IsLiteral<Length<a>>
 */

// ✓ IsTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsTuple(a) {
  if (matches<readonly [ ] | readonly [ any, ...any ] | readonly [ ...any, any ]>(a)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsTuple<a> =
 *   a extends readonly [] | readonly [any, ...any] | readonly [...any, any] ? true : false
 */

// ✓ IsStrictArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsStrictArray(a: readonly any[]) {
  return Not(IsTuple(a))
}
/* compiles to:
 * export type IsStrictArray<a extends readonly any[]> = Not<IsTuple<a>>
 */

// ✓ IsReadonlyArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsReadonlyArray(a) {
  if (matches<readonly any[]>(a)) {
    if (Array.isArray(a)) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsReadonlyArray<a> =
 *   a extends readonly any[] ? a extends any[] ? false : true : false
 */

// ✓ MaybeAddReadonly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeAddReadonly(a, shouldAdd: boolean) {
  if (matches<true>(shouldAdd)) {
    return Readonly(a)
  }
  return a
}
/* compiles to:
 * export type MaybeAddReadonly<a, shouldAdd extends boolean> =
 *   shouldAdd extends true ? Readonly<a> : a
 */

// ✓ MapKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapKey(T) {
  const m1 = matches<Map<Hole<"K">, any>>(T)
  if (m1) {
    return m1.K
  }
  return never
}
/* compiles to:
 * export type MapKey<T> = T extends Map<infer K, any> ? K : never
 */

// ✓ MapValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapValue(T) {
  const m1 = matches<Map<any, Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
/* compiles to:
 * export type MapValue<T> = T extends Map<any, infer V> ? V : never
 */

// ✓ SetValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SetValue(T) {
  const m1 = matches<Set<Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
/* compiles to:
 * export type SetValue<T> = T extends Set<infer V> ? V : never
 */

// ✓ RecordKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecordKey(T) {
  const m1 = matches<Record<Hole<"K">, any>>(T)
  if (m1) {
    return m1.K
  }
  return never
}
/* compiles to:
 * export type RecordKey<T> = T extends Record<infer K, any> ? K : never
 */

// ✓ RecordValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecordValue(T) {
  const m1 = matches<Record<any, Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
/* compiles to:
 * export type RecordValue<T> = T extends Record<any, infer V> ? V : never
 */

// ✓ ReadonlyArrayValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyArrayValue(T) {
  const m1 = matches<ReadonlyArray<Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
/* compiles to:
 * export type ReadonlyArrayValue<T> = T extends ReadonlyArray<infer V> ? V : never
 */

// ✓ ExtractPlainObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractPlainObject(T) {
  if (matches<any>(T)) {
    if (matches<true>(IsPlainObject(T))) {
      return T
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractPlainObject<T> =
 *   T extends any ? IsPlainObject<T> extends true ? T : never : never
 */

// ✓ GetKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetKey(O, K) {
  if (matches<any>(O)) {
    if (K in O) {
      return O[K]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type GetKey<O, K> = O extends any ? K extends keyof O ? O[K] : never : never
 */

// ✓ Call: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Call(fn: Fn, input) {
  return (merge(fn, { input: input }))['output']
}
/* compiles to:
 * export type Call<fn extends Fn, input> = (fn & { input: input })['output']
 */

// ✓ IsOptionalKeysOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsOptionalKeysOf(obj, key: keyof typeof obj) {
  if (matches<Pick<typeof obj, typeof key>>({})) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsOptionalKeysOf<obj, key extends keyof obj> =
 *   {} extends Pick<obj, key> ? true : false
 */

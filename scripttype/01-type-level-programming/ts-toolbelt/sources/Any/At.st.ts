/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Any/At.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Key: any
declare const List: any
type Key<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type List<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ At: verified type-identical to the original
/* @scripttype preserveParamNames */
export function At(A: any, K: Key) {
  if (matches<List>(A)) {
    if (matches<(typeof A)['length']>(number)) {
      if (matches<number | `${number}`>(K)) {
        return anyOf(A[never], Undefined)
      }
      return Undefined
    }
    if (matches<keyof typeof A>(K)) {
      return A[K]
    }
    return Undefined
  }
  if (matches<typeof A>(unknown)) {
    return unknown
  }
  if (matches<keyof typeof A>(K)) {
    return A[K]
  }
  return Undefined
}
/* compiles to:
 * export type At<A extends any, K extends Key> = A extends List ? number extends A['length'] ? K extends number | `${number}` ? A[never] | undefined : undefined : K extends keyof A ? A[K] : undefined : unknown extends A ? unknown : K extends keyof A ? A[K] : undefined
 */

/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Any/If.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ If: verified type-identical to the original
/* @scripttype preserveParamNames */
export function If(B: Boolean, Then, Else = never): any {
  if (matches<1>(B)) {
    return Then
  }
  return Else
}
/* compiles to:
 * export type If<B extends Boolean, Then, Else = never> = B extends 1 ? Then : Else
 */

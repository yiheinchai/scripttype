/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Any/Is.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Contains } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Contains.js'
import type { Equals } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Equals.js'
import type { Extends } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Extends.js'
import type { Match } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/_Internal.js'
declare const Contains: any
declare const Equals: any
declare const Extends: any
declare const Match: any
// ✓ Is: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Is(A: any, A1: any, match: Match = 'default'): any {
  return { 'default': Extends(A, A1), 'contains->': Contains(A, A1), 'extends->': Extends(A, A1), '<-contains': Contains(A1, A), '<-extends': Extends(A1, A), 'equals': Equals(A1, A) }[match]
}
/* compiles to:
 * export type Is<A extends any, A1 extends any, match extends Match = 'default'> =
 *   {
 *     default: Extends<A, A1>
 *     'contains->': Contains<A, A1>
 *     'extends->': Extends<A, A1>
 *     '<-contains': Contains<A1, A>
 *     '<-extends': Extends<A1, A>
 *     equals: Equals<A1, A>
 *   }[match]
 */

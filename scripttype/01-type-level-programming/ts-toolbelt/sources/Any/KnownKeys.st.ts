/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Any/KnownKeys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Keys: any
type Keys<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ KnownKeys: the ScriptType does not itself typecheck as TypeScript
//   KnownKeys.st.ts(3:9) TS2451: Cannot redeclare block-scoped variable 'm1'.
/* @scripttype preserveParamNames */
export function KnownKeys(O: object) {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = matches<typeof K>(string) ? never : (matches<typeof K>(number) ? never : K)
  }
  const m1 = matches<{ [K in keyof typeof O]: Hole<"U">; }>(out)
  if (m1) {
    return m1.U & Keys(O)
  }
  return never
}
/* compiles to:
 * export type KnownKeys<O extends object> = { [K in keyof O]: string extends K ? never : number extends K ? never : K } extends { [K in keyof typeof O]: infer U; } ? U & Keys<O> : never
 */

/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/List/NonNullable.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 5 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltIn: any
declare const Cast: any
declare const Depth: any
declare const Key: any
declare const List: any
declare const UNonNullable: any
type BuiltIn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Cast<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Depth<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Key<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type List<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UNonNullable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ NonNullableFlat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonNullableFlat(O, K: Key = Key) {
  const out = emptyObject
  for (const P in keyof(O)) {
    out[P] = matches<typeof K>(P) ? UNonNullable(O[P]) : O[P]
  }
  return merge(out, {})
}
/* compiles to:
 * export type NonNullableFlat<O, K extends Key = Key> = { [P in keyof O]: P extends K ? UNonNullable<O[P]> : O[P] } & {}
 */

// ✓ _NonNullableDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _NonNullableDeep(O) {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = matches<BuiltIn>(O[K]) ? O[K] : NonNullableDeep(O[K], Key)
  }
  return out
}
/* compiles to:
 * export type _NonNullableDeep<O> = { [K in keyof O]: O[K] extends BuiltIn ? O[K] : NonNullableDeep<O[K], Key> }
 */

// ✓ NonNullableDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonNullableDeep(O, K: Key = Key) {
  return _NonNullableDeep(NonNullableFlat(O, K))
}
/* compiles to:
 * export type NonNullableDeep<O, K extends Key = Key> = _NonNullableDeep<NonNullableFlat<O, K>>
 */

// ✓ NonNullablePart: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonNullablePart(O: object, K: Key, depth: Depth) {
  return { 'flat': NonNullableFlat(O, K), 'deep': NonNullableDeep(O, K) }[depth]
}
/* compiles to:
 * export type NonNullablePart<O extends object, K extends Key, depth extends Depth> = { flat: NonNullableFlat<O, K>; deep: NonNullableDeep<O, K> }[depth]
 */

// ✓ NonNullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonNullable(L: List, K: Key = Key, depth: Depth = 'flat') {
  return Cast(NonNullablePart(L, anyOf(`${merge(K, number)}`, K), depth), List)
}
/* compiles to:
 * export type NonNullable<L extends List, K extends Key = Key, depth extends Depth = 'flat'> = Cast<NonNullablePart<L, `${K & number}` | K, depth>, List>
 */

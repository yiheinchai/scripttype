/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/FilterKeys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 compile-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Is } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Is.js'
import type { Match } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/_Internal.js'
declare const Is: any
declare const Match: any
// ✗ _FilterKeys: does not compile yet
//   Type 'Is<O[K], M, match>' cannot be used to index type '{ '1': never; '0': K; }'.
/* @scripttype preserveParamNames */
/**
 * @param {object} O
 */
export function _FilterKeys(O, M: any, match: Match): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = required({ 1: never, 0: K }[Is(O[K], M, match)])
  }
  return out[keyof(O)]
}
/* compiles to:
 * export type _FilterKeys<O extends object, M extends any, match extends Match> =
 *   { [K in keyof O]-?: { '1': never; '0': K }[Is<O[K], M, match>] }[keyof O]
 */

// ✓ FilterKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FilterKeys(O: object, M: any, match: Match = 'default'): any {
  if (matches<unknown>(O)) {
    return _FilterKeys(O, M, match)
  }
  return never
}
/* compiles to:
 * export type FilterKeys<O extends object, M extends any, match extends Match = 'default'> =
 *   O extends unknown ? _FilterKeys<O, M, match> : never
 */

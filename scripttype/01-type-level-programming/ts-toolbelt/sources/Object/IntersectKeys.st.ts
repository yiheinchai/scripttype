/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/IntersectKeys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Is } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Is.js'
import type { Keys } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Keys.js'
import type { Match } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/_Internal.js'
declare const At: any
declare const Is: any
declare const Keys: any
declare const Match: any
type At<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ _IntersectMatch: does not compile yet
//   Type 'Is<O[K], At<O1, K>, match>' cannot be used to index type '{ '1': K; '0': never; }'.
/* @scripttype preserveParamNames */
/**
 * @param {object} O
 */
export function _IntersectMatch(O, O1: object, match: Match): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = required({ 1: K, 0: never }[Is(O[K], At(O1, K), match)])
  }
  return out[keyof(O)]
}
/* compiles to:
 * export type _IntersectMatch<O extends object, O1 extends object, match extends Match> =
 *   { [K in keyof O]-?: { '1': K; '0': never }[Is<O[K], At<O1, K>, match>] }[keyof O]
 */

// ✓ IntersectMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectMatch(O: object, O1: object, match: Match): any {
  if (matches<unknown>(O)) {
    return _IntersectMatch(O, O1, match)
  }
  return never
}
/* compiles to:
 * export type IntersectMatch<O extends object, O1 extends object, match extends Match> =
 *   O extends unknown ? _IntersectMatch<O, O1, match> : never
 */

// ✓ IntersectKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectKeys(O: object, O1: object, match: Match = 'default'): any {
  return { 'default': Keys(O) & Keys(O1), 'contains->': IntersectMatch(O, O1, 'contains->'), 'extends->': IntersectMatch(O, O1, 'extends->'), '<-contains': IntersectMatch(O, O1, '<-contains'), '<-extends': IntersectMatch(O, O1, '<-extends'), 'equals': IntersectMatch(O, O1, 'equals') }[match]
}
/* compiles to:
 * export type IntersectKeys<
 *   O extends object,
 *   O1 extends object,
 *   match extends Match = 'default'
 * > =
 *   {
 *     default: Keys<O> & Keys<O1>
 *     'contains->': IntersectMatch<O, O1, 'contains->'>
 *     'extends->': IntersectMatch<O, O1, 'extends->'>
 *     '<-contains': IntersectMatch<O, O1, '<-contains'>
 *     '<-extends': IntersectMatch<O, O1, '<-extends'>
 *     equals: IntersectMatch<O, O1, 'equals'>
 *   }[match]
 */

/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/IntersectKeys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const At: any
declare const Is: any
declare const Keys: any
declare const Match: any
type At<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Is<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Keys<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Match<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ _IntersectMatch: does not compile yet
//   Type 'Is<O[K], At<O1, K>, match>' cannot be used to index type '{ '1': K; '0': never; }'.
/* @scripttype preserveParamNames */
export function _IntersectMatch(O: object, O1: object, match: Match) {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = required({ 1: K, 0: never }[Is(O[K], At(O1, K), match)])
  }
  return out[keyof(O)]
}
/* compiles to:
 * export type _IntersectMatch<O extends object, O1 extends object, match extends Match> = { [K in keyof O]-?: { '1': K; '0': never }[Is<O[K], At<O1, K>, match>] }[keyof O]
 */

// ✓ IntersectMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectMatch(O: object, O1: object, match: Match) {
  if (matches<unknown>(O)) {
    return _IntersectMatch(O, O1, match)
  }
  return never
}
/* compiles to:
 * export type IntersectMatch<O extends object, O1 extends object, match extends Match> = O extends unknown ? _IntersectMatch<O, O1, match> : never
 */

// ✓ IntersectKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectKeys(O: object, O1: object, match: Match = 'default') {
  return { 'default': Keys(O) & Keys(O1), 'contains->': IntersectMatch(O, O1, 'contains->'), 'extends->': IntersectMatch(O, O1, 'extends->'), '<-contains': IntersectMatch(O, O1, '<-contains'), '<-extends': IntersectMatch(O, O1, '<-extends'), 'equals': IntersectMatch(O, O1, 'equals') }[match]
}
/* compiles to:
 * export type IntersectKeys<O extends object, O1 extends object, match extends Match = 'default'> = { default: Keys<O> & Keys<O1>; 'contains->': IntersectMatch<O, O1, 'contains->'>; 'extends->': IntersectMatch<O, O1, 'extends->'>; '<-contains': IntersectMatch<O, O1, '<-contains'>; '<-extends': IntersectMatch<O, O1, '<-extends'>; equals: IntersectMatch<O, O1, 'equals'> }[match]
 */

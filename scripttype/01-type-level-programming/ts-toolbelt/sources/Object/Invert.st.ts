/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/Invert.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ComputeRaw } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Compute.js'
import type { IntersectOf } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Union/IntersectOf.js'
declare const ComputeRaw: any
declare const IntersectOf: any
// ✓ _Invert: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<Key, Key>} O
 */
export function _Invert(O): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = Record(O[K], K)
  }
  return ComputeRaw(IntersectOf(out[keyof(O)]))
}
/* compiles to:
 * export type _Invert<O extends Record<Key, Key>> = ComputeRaw<
 *   IntersectOf<{ [K in keyof O]: Record<O[K], K> }[keyof O]>
 * >
 */

// ✓ Invert: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<keyof typeof O, Key>} O
 */
export function Invert(O): any {
  if (matches<unknown>(O)) {
    return _Invert(O)
  }
  return never
}
/* compiles to:
 * export type Invert<O extends Record<keyof O, Key>> = O extends unknown ? _Invert<O> : never
 */

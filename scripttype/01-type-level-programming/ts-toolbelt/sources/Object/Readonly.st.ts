/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/Readonly.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 5 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BuiltIn } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Misc/BuiltIn.js'
import type { PatchFlat } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Object/Patch.js'
import type { Depth } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Object/_Internal.js'
declare const BuiltIn: any
declare const Depth: any
declare const Key: any
declare const PatchFlat: any
declare const _Pick: any
type Key<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type _Pick<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ReadonlyFlat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyFlat(O): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = readonlyProp(O[K])
  }
  return merge(out, {})
}
/* compiles to:
 * export type ReadonlyFlat<O> = { readonly [K in keyof O]: O[K] } & {}
 */

// ✓ ReadonlyDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyDeep(O): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = readonlyProp(matches<BuiltIn>(O[K]) ? O[K] : ReadonlyDeep(O[K]))
  }
  return out
}
/* compiles to:
 * export type ReadonlyDeep<O> = {
 *   readonly [K in keyof O]: O[K] extends BuiltIn ? O[K] : ReadonlyDeep<O[K]>
 * }
 */

// ✓ ReadonlyPart: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyPart(O: object, depth: Depth): any {
  return { 'flat': ReadonlyFlat(O), 'deep': ReadonlyDeep(O) }[depth]
}
/* compiles to:
 * export type ReadonlyPart<O extends object, depth extends Depth> =
 *   { flat: ReadonlyFlat<O>; deep: ReadonlyDeep<O> }[depth]
 */

// ✓ _Readonly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _Readonly(O: object, K: Key, depth: Depth): any {
  return PatchFlat(ReadonlyPart(_Pick(O, K), depth), O)
}
/* compiles to:
 * export type _Readonly<O extends object, K extends Key, depth extends Depth> = PatchFlat<
 *   ReadonlyPart<_Pick<O, K>, depth>,
 *   O
 * >
 */

// ✓ Readonly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Readonly(O: object, K: Key = Key, depth: Depth = 'flat'): any {
  if (matches<unknown>(O)) {
    return _Readonly(O, K, depth)
  }
  return never
}
/* compiles to:
 * export type Readonly<O extends object, K extends Key = Key, depth extends Depth = 'flat'> =
 *   O extends unknown ? _Readonly<O, K, depth> : never
 */

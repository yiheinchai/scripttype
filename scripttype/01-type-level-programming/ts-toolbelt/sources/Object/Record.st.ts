/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/Record.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Modx } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Object/_Internal.js'
declare const Key: any
declare const Modx: any
type Key<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Record: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Record(K: Key, A: any = unknown, modx: Modx = ['!', 'W']): any {
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P] = readonlyProp(A)
  }
  const out2 = emptyObject
  for (const P in keySet(K)) {
    out2[P] = A
  }
  const out3 = emptyObject
  for (const P in keySet(K)) {
    out3[P] = readonlyProp(optional(A))
  }
  const out4 = emptyObject
  for (const P in keySet(K)) {
    out4[P] = optional(A)
  }
  return { '!': { 'R': out, 'W': out2 }, '?': { 'R': out3, 'W': out4 } }[modx[0]][modx[1]]
}
/* compiles to:
 * export type Record<K extends Key, A extends any = unknown, modx extends Modx = ['!', 'W']> =
 *   {
 *     '!': { R: { readonly [P in K]: A }; W: { [P1 in K]: A } }
 *     '?': { R: { readonly [P2 in K]?: A }; W: { [P3 in K]?: A } }
 *   }[modx[0]][modx[1]]
 */

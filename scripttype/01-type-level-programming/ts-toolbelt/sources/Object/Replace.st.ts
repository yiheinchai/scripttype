/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/Replace.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 compile-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Is } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Is.js'
import type { Match } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/_Internal.js'
declare const Is: any
declare const Match: any
// ✗ _Replace: does not compile yet
//   Type 'Is<M, O[K], match>' cannot be used to index type '{ '1': A; '0': O[K]; }'.
/* @scripttype preserveParamNames */
/**
 * @param {object} O
 */
export function _Replace(O, M: any, A: any, match: Match): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = { 1: A, 0: O[K] }[Is(M, O[K], match)]
  }
  return merge(out, {})
}
/* compiles to:
 * export type _Replace<O extends object, M extends any, A extends any, match extends Match> =
 *   { [K in keyof O]: { '1': A; '0': O[K] }[Is<M, O[K], match>] } & {}
 */

// ✓ Replace: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Replace(O: object, M: any, A: any, match: Match = 'default'): any {
  if (matches<unknown>(O)) {
    return _Replace(O, M, A, match)
  }
  return never
}
/* compiles to:
 * export type Replace<
 *   O extends object,
 *   M extends any,
 *   A extends any,
 *   match extends Match = 'default'
 * > =
 *   O extends unknown ? _Replace<O, M, A, match> : never
 */

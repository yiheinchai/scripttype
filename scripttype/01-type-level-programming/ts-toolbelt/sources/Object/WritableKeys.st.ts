/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Object/WritableKeys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Equals: any
type Equals<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ _WritableKeys: the ScriptType does not itself typecheck as TypeScript
//   _WritableKeys.st.ts(4:26) TS2304: Cannot find name 'K'.
/* @scripttype preserveParamNames */
export function _WritableKeys(O: object) {
  const out2 = emptyObject
  for (const Q in keySet(K)) {
    out2[Q] = mutable(O[K])
  }
  const out3 = emptyObject
  for (const Q in keySet(K)) {
    out3[Q] = O[K]
  }
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = required({ 1: K, 0: never }[Equals(out2, out3)])
  }
  return out[keyof(O)]
}
/* compiles to:
 * export type _WritableKeys<O extends object> = { [K in keyof O]-?: { '1': K; '0': never }[Equals<{ -readonly [Q in K]: O[K] }, { [Q1 in K]: O[K] }>] }[keyof O]
 */

// ✓ WritableKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WritableKeys(O: object) {
  if (matches<unknown>(O)) {
    return _WritableKeys(O)
  }
  return never
}
/* compiles to:
 * export type WritableKeys<O extends object> = O extends unknown ? _WritableKeys<O> : never
 */

/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/String/Join.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Cast: any
declare const List: any
declare const Literal: any
type Cast<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type List<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Literal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ _Join: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _Join(T: List, D: string) {
  if (matches<[ ]>(T)) {
    return ''
  }
  if (matches<[ Literal ]>(T)) {
    return `${T[0]}`
  }
  const m1 = matches<[ Literal, ...Hole<"R"> ]>(T)
  if (m1) {
    return `${T[0]}${D}${_Join(m1.R, D)}`
  }
  return string
}
/* compiles to:
 * export type _Join<T extends List, D extends string> = T extends [] ? '' : T extends [Literal] ? `${T[0]}` : T extends [Literal, ...infer R] ? `${T[0]}${D}${_Join<R, D>}` : string
 */

// ✓ Join: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Join(T: List<Literal>, D: string = '') {
  const m1 = matches<Hole<"X">>(_Join(T, D))
  if (m1) {
    return Cast(m1.X, string)
  }
  return never
}
/* compiles to:
 * export type Join<T extends List<Literal>, D extends string = ''> = _Join<T, D> extends infer X ? Cast<X, string> : never
 */

/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/String/Split.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Cast: any
declare const Pop: any
type Cast<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Pop<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ __Split: the ScriptType does not itself typecheck as TypeScript
//   __Split.st.ts(11:12) TS2345: Argument of type 'any' is not assignable to parameter of type 'never'.
/* @scripttype preserveParamNames */
export function __Split(S: string, D: string) {
  let T = []
  let s = S
  while (true) {
    const m1 = matches<`${Hole<"BS">}${typeof D}${Hole<"AS">}`>(s)
    if (!m1) {
      break
    }
    s = m1.AS
    T.push(m1.BS)
  }
  return [...T, s]
}
/* compiles to:
 * export type __Split<S extends string, D extends string> = __Split__loop<S, [], D>
 * type __Split__loop<S extends string, T extends readonly unknown[], D extends string> = S extends `${infer BS}${D}${infer AS}` ? __Split__loop<AS, [...T, BS], D> : [...T, S]
 */

// ✓ _Split: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _Split(S: string, D: string = '') {
  if (matches<''>(D)) {
    return Pop(__Split(S, D))
  }
  return __Split(S, D)
}
/* compiles to:
 * export type _Split<S extends string, D extends string = ''> = D extends '' ? Pop<__Split<S, D>> : __Split<S, D>
 */

// ✓ Split: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Split(S: string, D: string = '') {
  const m1 = matches<Hole<"X">>(_Split(S, D))
  if (m1) {
    return Cast(m1.X, arrayOf(string))
  }
  return never
}
/* compiles to:
 * export type Split<S extends string, D extends string = ''> = _Split<S, D> extends infer X ? Cast<X, string[]> : never
 */

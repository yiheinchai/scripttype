/**
 * ScriptType for 01-type-level-programming/ts-toolbelt/sources/Union/Select.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Is } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/Is.js'
import type { Match } from '../../../../../01-type-level-programming/ts-toolbelt/sources/Any/_Internal.js'
declare const Is: any
declare const Match: any
// ✗ Select: does not compile yet
//   Type 'Is<U, M, match>' cannot be used to index type '{ '1': U & M; '0': never; }'.
/* @scripttype preserveParamNames */
export function Select(U: any, M: any, match: Match = 'default'): any {
  if (matches<unknown>(U)) {
    return { 1: merge(U, M), 0: never }[Is(U, M, match)]
  }
  return never
}
/* compiles to:
 * export type Select<U extends any, M extends any, match extends Match = 'default'> =
 *   U extends unknown ? { '1': U & M; '0': never }[Is<U, M, match>] : never
 */

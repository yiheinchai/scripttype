/**
 * ScriptType for 01-type-level-programming/type-fest/source/absolute.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const StringToNumber: any
type StringToNumber<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Absolute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Absolute(N: number | bigint) {
  if (matches<bigint>(N)) {
    const m1 = matches<`-${Hole<"Magnitude", bigint>}`>(`${N}`)
    if (m1) {
      return m1.Magnitude
    }
    return N
  }
  const m2 = matches<`-${Hole<"Magnitude">}`>(`${N}`)
  if (m2) {
    return StringToNumber(m2.Magnitude)
  }
  return N
}
/* compiles to:
 * export type Absolute<N extends number | bigint> = N extends bigint ? `${N}` extends `-${infer Magnitude extends bigint}` ? Magnitude : N : `${N}` extends `-${infer Magnitude}` ? StringToNumber<Magnitude> : N
 */

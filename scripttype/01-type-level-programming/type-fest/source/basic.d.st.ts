/**
 * ScriptType for 01-type-level-programming/type-fest/source/basic.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✗ Class: uses raw() — language gap, does not count as covered
//   gap: object member ConstructSignature
/* @scripttype preserveParamNames */
export function Class(T, Arguments: unknown[] = arrayOf(any)) {
  return raw('{ prototype: Pick<T, keyof T>; new (...arguments_: Arguments): T; }')
}

// ✗ Constructor: uses raw() — language gap, does not count as covered
//   gap: type node ConstructorType
/* @scripttype preserveParamNames */
export function Constructor(T, Arguments: unknown[] = arrayOf(any)) {
  return raw('new (...arguments_: Arguments) => T')
}

// ✗ AbstractConstructor: uses raw() — language gap, does not count as covered
//   gap: type node ConstructorType
/* @scripttype preserveParamNames */
export function AbstractConstructor(T, Arguments: unknown[] = arrayOf(any)) {
  return raw('abstract new (...arguments_: Arguments) => T')
}

/**
 * ScriptType for 01-type-level-programming/type-fest/source/basic.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Class: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Class(T, Arguments: unknown[] = arrayOf(any)): any {
  return { prototype: Pick(T, keyof(T)), ...ctorSig([...Arguments], T) }
}
/* compiles to:
 * export type Class<T, Arguments extends unknown[] = any[]> = {
 *   prototype: Pick<T, keyof T>
 *   new (...a0: Arguments): T
 * }
 */

// ✓ Constructor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Constructor(T, Arguments: unknown[] = arrayOf(any)): any {
  return ctorType([...Arguments], T)
}
/* compiles to:
 * export type Constructor<T, Arguments extends unknown[] = any[]> = new (...a0: Arguments) => T
 */

// ✓ AbstractConstructor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbstractConstructor(T, Arguments: unknown[] = arrayOf(any)): any {
  return ctorType([...Arguments], T)
}
/* compiles to:
 * export type AbstractConstructor<T, Arguments extends unknown[] = any[]> =
 *   new (...a0: Arguments) => T
 */

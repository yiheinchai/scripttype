/**
 * ScriptType for 01-type-level-programming/type-fest/source/extract-strict.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ ExtractStrict: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {[ typeof U ] extends [ typeof U extends unknown ? (Extract<typeof T, typeof U> extends never ? never : typeof U) : never ] ? unknown : never} U
 */
export function ExtractStrict(T, U): any {
  return Extract(T, U)
}
/* compiles to:
 * export type ExtractStrict<
 *   T,
 *   U extends [ typeof U ] extends [ typeof U extends unknown ? (Extract<T, typeof U> extends never ? never : typeof U) : never ] ? unknown : never
 * > =
 *   Extract<T, U>
 */

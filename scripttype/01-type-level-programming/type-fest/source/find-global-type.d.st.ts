/**
 * ScriptType for 01-type-level-programming/type-fest/source/find-global-type.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✗ FindGlobalType: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function FindGlobalType(Name: string) {
  const m1 = matches<Record<typeof Name, Hole<"T">>>(t<typeof globalThis>())
  if (m1) {
    return m1.T
  }
  return never
}
/* compiles to:
 * export type FindGlobalType<Name extends string> =
 *   globalThis extends Record<Name, infer T> ? T : never
 */

// ✗ FindGlobalInstanceType: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function FindGlobalInstanceType(Name: string) {
  if (typeof Name === 'string') {
    const m1 = matches<Record<typeof Name, abstract new (...arguments_: any[]) => Hole<"T">>>(t<typeof globalThis>())
    if (m1) {
      return m1.T
    }
    return never
  }
  return never
}
/* compiles to:
 * export type FindGlobalInstanceType<Name extends string> =
 *   Name extends string
 *     ? globalThis extends Record<Name, abstract new (...arguments_: any[]) => infer T>
 *       ? T
 *       : never
 *     : never
 */

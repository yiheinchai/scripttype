/**
 * ScriptType for 01-type-level-programming/type-fest/source/internal/object.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 13 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace m1 {
  export type NumberK<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type NumberKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Result<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const FilterDefinedKeys: any
declare const FilterOptionalKeys: any
declare const If: any
declare const IfNotAnyOrNever: any
declare const IsAny: any
declare const IsEqual: any
declare const IsNever: any
declare const KeysOfUnion: any
declare const MapsSetsOrArrays: any
declare const Merge: any
declare const NonRecursiveType: any
declare const Primitive: any
declare const PropertyKey: any
declare const Simplify: any
declare const StringToNumber: any
declare const ToString: any
type FilterDefinedKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FilterOptionalKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type If<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IfNotAnyOrNever<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsAny<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsEqual<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsNever<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type KeysOfUnion<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MapsSetsOrArrays<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Merge<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NonRecursiveType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Primitive<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Simplify<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StringToNumber<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ToString<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ BuildObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuildObject(Key: PropertyKey, Value, CopiedFrom: object = {}): any {
  if (Key in CopiedFrom) {
    const out = emptyObject
    for (const _ in keyof(CopiedFrom)) {
      out[_] = Value
    }
    return Pick(out, Key)
  }
  const m1 = matches<`${Hole<"NumberKey", number>}`>(Key)
  if (m1) {
    if (m1.NumberKey in CopiedFrom) {
      const out2 = emptyObject
      for (const _ in keyof(CopiedFrom)) {
        out2[_] = Value
      }
      return Pick(out2, m1.NumberKey)
    }
    const out3 = emptyObject
    for (const _ in keySet(Key)) {
      out3[_] = Value
    }
    return out3
  }
  const out4 = emptyObject
  for (const _ in keySet(Key)) {
    out4[_] = Value
  }
  return out4
}
/* compiles to:
 * export type BuildObject<Key extends PropertyKey, Value, CopiedFrom extends object = {}> =
 *   Key extends keyof CopiedFrom ? Pick<{ [_ in keyof CopiedFrom]: Value }, Key>
 *   : Key extends `${infer NumberKey extends number}`
 *     ? NumberKey extends keyof CopiedFrom
 *       ? Pick<{ [_1 in keyof CopiedFrom]: Value }, NumberKey>
 *       : { [_2 in Key]: Value }
 *   : { [_3 in Key]: Value }
 */

// ✓ IsPlainObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPlainObject(T): any {
  if (matches<true>(IsNever(T))) {
    return false
  }
  if (matches<NonRecursiveType | MapsSetsOrArrays>(T)) {
    return false
  }
  if (matches<object>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsPlainObject<T> =
 *   IsNever<T> extends true ? false
 *   : T extends NonRecursiveType | MapsSetsOrArrays ? false
 *   : T extends object ? true
 *   : false
 */

// ✓ ObjectValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectValue(T, K): any {
  if (K in T) {
    return T[K]
  }
  if (ToString(K) in T) {
    return T[ToString(K)]
  }
  const m1 = matches<`${Hole<"NumberK", number>}`>(K)
  if (m1) {
    if (m1.NumberK in T) {
      return T[m1.NumberK]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ObjectValue<T, K> =
 *   K extends keyof T ? T[K]
 *   : ToString<K> extends keyof T ? T[ToString<K>]
 *   : K extends `${infer NumberK extends number}` ? NumberK extends keyof T ? T[NumberK] : never
 *   : never
 */

// ✓ UndefinedToOptional: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} T
 */
export function UndefinedToOptional(T): any {
  const out = emptyObject
  for (const Key in keyof(Pick(T, FilterDefinedKeys(T)))) {
    out[Key] = T[Key]
  }
  const out2 = emptyObject
  for (const Key in keyof(Pick(T, FilterOptionalKeys(T)))) {
    out2[Key] = optional(Exclude(T[Key], Undefined))
  }
  return Simplify(merge(out, out2))
}
/* compiles to:
 * export type UndefinedToOptional<T extends object> = Simplify<
 *   & { [Key in keyof Pick<T, FilterDefinedKeys<T>>]: T[Key] }
 *   & { [Key1 in keyof Pick<T, FilterOptionalKeys<T>>]?: Exclude<T[Key1], undefined> }
 * >
 */

// ✓ HomomorphicPick: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {KeysOfUnion<typeof T>} Keys
 */
export function HomomorphicPick(T, Keys): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[Extract(P, Keys)] = T[P]
  }
  return out
}
/* compiles to:
 * export type HomomorphicPick<T, Keys extends KeysOfUnion<T>> = {
 *   [P in keyof T as Extract<P, Keys>]: T[P]
 * }
 */

// ✓ ValueOfUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {KeysOfUnion<typeof Union>} Key
 */
export function ValueOfUnion(Union, Key): any {
  if (matches<unknown>(Union)) {
    if (Key in Union) {
      return Union[Key]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ValueOfUnion<Union, Key extends KeysOfUnion<Union>> =
 *   Union extends unknown ? Key extends keyof Union ? Union[Key] : never : never
 */

// ✓ ReadonlyKeysOfUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyKeysOfUnion__v1(Union, Key): any {
  const out = emptyObject
  for (const K in keySet(Key)) {
    out[K] = Union[Key]
  }
  return out
}

export function ReadonlyKeysOfUnion__v2(Union, Key): any {
  const out = emptyObject
  for (const K in keySet(Key)) {
    out[K] = readonlyProp(Union[Key])
  }
  return out
}
export function ReadonlyKeysOfUnion(Union): any {
  if (matches<unknown>(Union)) {
    const out = emptyObject
    for (const Key in keyof(Union)) {
      out[matches<true>(IsEqual(ReadonlyKeysOfUnion__v1(Union, Key), ReadonlyKeysOfUnion__v2(Union, Key))) ? Key : never] = never
    }
    return keyof(out)
  }
  return never
}
/* compiles to:
 * export type ReadonlyKeysOfUnion__v1<Union, Key> = { [K in Key]: Union[Key] }
 * export type ReadonlyKeysOfUnion__v2<Union, Key> = { readonly [K in Key]: Union[Key] }
 * export type ReadonlyKeysOfUnion<Union> =
 *   Union extends unknown
 *     ? keyof {
 *       [Key in keyof Union as IsEqual<ReadonlyKeysOfUnion__v1<Union, Key>, ReadonlyKeysOfUnion__v2<Union, Key>> extends true ? Key : never]: never
 *     }
 *     : never
 */

// ✓ ApplyDefaultOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} Options
 * @param {Simplify<Omit<Required<typeof Options>, RequiredKeysOf<typeof Options>> & Partial<Record<RequiredKeysOf<typeof Options>, never>>>} Defaults
 * @param {typeof Options} SpecifiedOptions
 */
export function ApplyDefaultOptions(Options, Defaults, SpecifiedOptions): any {
  const m1 = matches<Hole<"Result", Required<typeof Options>>>(_ApplyDefaultOptions(Options, Defaults, SpecifiedOptions))
  if (m1) {
    return m1.Result
  }
  return never
}
/* compiles to:
 * export type ApplyDefaultOptions<
 *   Options extends object,
 *   Defaults extends Simplify<Omit<Required<Options>, RequiredKeysOf<Options>> & Partial<Record<RequiredKeysOf<Options>, never>>>,
 *   SpecifiedOptions extends Options
 * > =
 *   _ApplyDefaultOptions<Options, Defaults, SpecifiedOptions> extends (infer Result extends Required<Options>)
 *     ? Result
 *     : never
 */

// ✓ _ApplyDefaultOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _ApplyDefaultOptions(Options, Defaults, SpecifiedOptions): any {
  const out = emptyObject
  for (const Key in keyof(SpecifiedOptions)) {
    out[matches<Required<typeof Options>[typeof Key & keyof typeof Options]>(Undefined) ? Key : (matches<(typeof SpecifiedOptions)[typeof Key]>(Undefined) ? never : Key)] = SpecifiedOptions[Key]
  }
  return If(IsAny(SpecifiedOptions), Defaults, If(IsNever(SpecifiedOptions), Defaults, Merge(Defaults, out)))
}
/* compiles to:
 * export type _ApplyDefaultOptions<Options, Defaults, SpecifiedOptions> = If<
 *   IsAny<SpecifiedOptions>,
 *   Defaults,
 *   If<
 *     IsNever<SpecifiedOptions>,
 *     Defaults,
 *     Merge<
 *       Defaults,
 *       {
 *         [Key in keyof SpecifiedOptions as undefined extends Required<Options>[Key & keyof Options] ? Key : undefined extends SpecifiedOptions[Key] ? never : Key]: SpecifiedOptions[Key]
 *       }
 *     >
 *   >
 * >
 */

// ✓ CollapseLiterals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CollapseLiterals(T): any {
  if (matches<typeof T>({})) {
    return T
  }
  const m1 = matches<Hole<"U"> & {}>(T)
  if (m1) {
    return m1.U
  }
  return T
}
/* compiles to:
 * export type CollapseLiterals<T> = {} extends T ? T : T extends infer U & {} ? U : T
 */

// ✓ UnwrapBrand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapBrand(T): any {
  return IfNotAnyOrNever(T, { ifNot: _UnwrapBrand(T, Primitive) })
}
/* compiles to:
 * export type UnwrapBrand<T> = IfNotAnyOrNever<T, { ifNot: _UnwrapBrand<T, Primitive> }>
 */

// ✓ _UnwrapBrand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _UnwrapBrand(T, Base): any {
  if (matches<Primitive>(T)) {
    const m1 = matches<Hole<"U"> & Pick<typeof T, Exclude<keyof typeof T, KeysOfUnion<typeof Base>>>>(T)
    if (m1) {
      return m1.U
    }
    return T
  }
  return T
}
/* compiles to:
 * export type _UnwrapBrand<T, Base> =
 *   T extends Primitive
 *     ? T extends infer U & Pick<T, Exclude<keyof T, KeysOfUnion<Base>>> ? U : T
 *     : T
 */

// ✓ NormalizedKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizedKeys(Keys: PropertyKey): any {
  return anyOf(Keys, matches<typeof Keys>(string) ? number : never, StringToNumber(merge(Keys, string)), ToString(merge(Keys, number)))
}
/* compiles to:
 * export type NormalizedKeys<Keys extends PropertyKey> =
 *   | Keys
 *   | (string extends Keys ? number : never)
 *   | StringToNumber<Keys & string>
 *   | ToString<Keys & number>
 */

/**
 * ScriptType for 01-type-level-programming/type-fest/source/is-equal.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const G: any
type G<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ IsEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsEqual(A, B) {
  if (matches<[ typeof B ]>([A])) {
    if (matches<[ typeof A ]>([B])) {
      return _IsEqual(A, B)
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsEqual<A, B> = [A] extends [B] ? [B] extends [A] ? _IsEqual<A, B> : false : false
 */

// ✗ _IsEqual: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function _IsEqual(A, B) {
  if (matches<(<G>() => G extends (typeof B & G) | G ? 1 : 2)>(raw('<G>() => G extends A & G | G ? 1 : 2'))) {
    return true
  }
  return false
}

/**
 * ScriptType for 01-type-level-programming/type-fest/source/is-float.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace m1 {
  export type E<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
// ✓ IsFloat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsFloat(T): any {
  if (typeof T === 'number') {
    const m1 = matches<`${number}e${Hole<"E", '-' | '+'>}${number}`>(`${T}`)
    if (m1) {
      if (matches<'-'>(m1.E)) {
        return true
      }
      return false
    }
    if (matches<`${number}.${number}`>(`${T}`)) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsFloat<T> =
 *   T extends number
 *     ? `${T}` extends `${number}e${infer E extends '-' | '+'}${number}`
 *       ? E extends '-' ? true : false
 *     : `${T}` extends `${number}.${number}` ? true
 *     : false
 *     : false
 */

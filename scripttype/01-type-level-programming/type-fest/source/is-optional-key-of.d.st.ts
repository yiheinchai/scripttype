/**
 * ScriptType for 01-type-level-programming/type-fest/source/is-optional-key-of.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const IsAny: any
type IsAny<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ IsOptionalKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsOptionalKeyOf(Type: object, Key: keyof typeof Type) {
  if (matches<true>(IsAny(anyOf(Type, Key)))) {
    return never
  }
  if (matches<keyof typeof Type>(Key)) {
    if (matches<Record<typeof Key, (typeof Type)[typeof Key]>>(Type)) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsOptionalKeyOf<Type extends object, Key extends keyof Type> = IsAny<Type | Key> extends true ? never : Key extends keyof Type ? Type extends Record<Key, Type[Key]> ? false : true : false
 */

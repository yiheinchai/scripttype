/**
 * ScriptType for 01-type-level-programming/type-fest/source/literal-to-primitive.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ LiteralToPrimitive: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LiteralToPrimitive(T) {
  if (matches<number>(T)) {
    return number
  }
  if (matches<bigint>(T)) {
    return bigint
  }
  if (matches<string>(T)) {
    return string
  }
  if (matches<boolean>(T)) {
    return boolean
  }
  if (matches<symbol>(T)) {
    return symbol
  }
  if (matches<null>(T)) {
    return Null
  }
  if (matches<undefined>(T)) {
    return Undefined
  }
  return never
}
/* compiles to:
 * export type LiteralToPrimitive<T> = T extends number ? number : T extends bigint ? bigint : T extends string ? string : T extends boolean ? boolean : T extends symbol ? symbol : T extends null ? null : T extends undefined ? undefined : never
 */

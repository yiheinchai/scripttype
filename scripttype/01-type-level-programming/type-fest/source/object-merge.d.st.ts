/**
 * ScriptType for 01-type-level-programming/type-fest/source/object-merge.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const If: any
declare const IfNotAnyOrNever: any
declare const IsExactOptionalPropertyTypesEnabled: any
declare const IsNever: any
declare const IsOptionalKeyOf: any
declare const MapsSetsOrArrays: any
declare const NormalizedKeys: any
declare const OmitIndexSignature: any
declare const PickIndexSignature: any
declare const PropertyKey: any
declare const Simplify: any
type If<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IfNotAnyOrNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsExactOptionalPropertyTypesEnabled<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsOptionalKeyOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MapsSetsOrArrays<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NormalizedKeys<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OmitIndexSignature<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PickIndexSignature<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Simplify<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ObjectMerge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectMerge(First: object, Second: object) {
  return IfNotAnyOrNever(First, { ifNot: IfNotAnyOrNever(Second, { ifNot: matches<unknown>(First) ? (matches<unknown>(Second) ? (matches<MapsSetsOrArrays>(First) ? unknown : (matches<MapsSetsOrArrays>(Second) ? unknown : _ObjectMerge(First, Second, NormalizedLiteralKeys(First), NormalizedLiteralKeys(Second), matches<true>(IsExactOptionalPropertyTypesEnabled) ? Required(First) : First, matches<true>(IsExactOptionalPropertyTypesEnabled) ? Required(Second) : Second))) : never) : never }), ifAny: merge(First, Second) })
}
/* compiles to:
 * export type ObjectMerge<First extends object, Second extends object> = IfNotAnyOrNever<First, { ifNot: IfNotAnyOrNever<Second, { ifNot: First extends unknown ? Second extends unknown ? First extends MapsSetsOrArrays ? unknown : Second extends MapsSetsOrArrays ? unknown : _ObjectMerge<First, Second, NormalizedLiteralKeys<First>, NormalizedLiteralKeys<Second>, IsExactOptionalPropertyTypesEnabled extends true ? Required<First> : First, IsExactOptionalPropertyTypesEnabled extends true ? Required<Second> : Second> : never : never }>; ifAny: First & Second }>
 */

// ✗ _ObjectMerge: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function _ObjectMerge(First: object, Second: object, NormalizedFirstLiteralKeys: PropertyKey, NormalizedSecondLiteralKeys: PropertyKey, NormalizedFirst: object, NormalizedSecond: object) {
  const out = emptyObject
  for (const P in keyof(Second)) {
    out[matches<typeof NormalizedSecondLiteralKeys>(P) ? (matches<typeof NormalizedFirstLiteralKeys>(P) ? If(IsOptionalKeyOf(Second, P), never, P) : P) : never] = mutable(Second[P] | (matches<NormalizedKeys<keyof PickIndexSignature<typeof First>>>(P) ? If(IsOptionalKeyOf(Second, P), First[NormalizedKeys(P) & keyof(First)], never) : never))
  }
  const out2 = emptyObject
  for (const P in keyof(First)) {
    out2[matches<typeof NormalizedFirstLiteralKeys>(P) ? (matches<typeof NormalizedSecondLiteralKeys>(P) ? never : P) : never] = mutable(First[P] | (matches<NormalizedKeys<keyof typeof Second>>(P) ? Second[NormalizedKeys(P) & keyof(Second)] : never))
  }
  const out3 = emptyObject
  for (const P in keyof(Second)) {
    out3[matches<typeof NormalizedSecondLiteralKeys>(P) ? never : P] = mutable(Second[P] | (raw('NormalizedKeys<P> & Exclude<keyof First, NormalizedKeys<RequiredKeysOf<OmitIndexSignature<Second>>>> extends infer NonOverwrittenKeysOfFirst ? If<IsNever<NonOverwrittenKeysOfFirst>, never, NormalizedFirst[NonOverwrittenKeysOfFirst & keyof NormalizedFirst]> : never')))
  }
  const out4 = emptyObject
  for (const P in keyof(First)) {
    out4[matches<typeof NormalizedFirstLiteralKeys>(P) ? never : P] = mutable(First[P] | If(IsNever(NormalizedKeys(P) & keyof(Second)), never, NormalizedSecond[NormalizedKeys(P) & keyof(NormalizedSecond)]))
  }
  const out5 = emptyObject
  for (const P in keyof(First)) {
    out5[matches<typeof NormalizedFirstLiteralKeys>(P) ? (matches<typeof NormalizedSecondLiteralKeys>(P) ? If(IsOptionalKeyOf(Second, NormalizedKeys(P) & keyof(Second)), P, never) : never) : never] = mutable(First[P] | NormalizedSecond[NormalizedKeys(P) & keyof(NormalizedSecond)])
  }
  return Simplify(out & out2 & out3 & out4 & out5)
}

// ✓ NormalizedLiteralKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizedLiteralKeys(Type) {
  if (matches<unknown>(Type)) {
    return NormalizedKeys(keyof(OmitIndexSignature(Type)))
  }
  return never
}
/* compiles to:
 * export type NormalizedLiteralKeys<Type> = Type extends unknown ? NormalizedKeys<keyof OmitIndexSignature<Type>> : never
 */

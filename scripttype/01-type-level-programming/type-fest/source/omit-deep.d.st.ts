/**
 * ScriptType for 01-type-level-programming/type-fest/source/omit-deep.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 4 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ArrayIndex: any
declare const ArraySplice: any
declare const ExactKey: any
declare const IsArrayReadonly: any
declare const IsEqual: any
declare const IsNever: any
declare const LiteralUnion: any
declare const NonRecursiveType: any
declare const Paths: any
declare const PropertyKey: any
declare const SetArrayAccess: any
declare const SimplifyDeep: any
declare const ToString: any
declare const UnionToTuple: any
declare const UnknownArray: any
type ArrayIndex<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ArraySplice<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExactKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsArrayReadonly<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsEqual<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LiteralUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonRecursiveType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Paths<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SetArrayAccess<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SimplifyDeep<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ToString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnionToTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ OmitDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitDeep(T, PathUnion: LiteralUnion<Paths<typeof T>, string>) {
  return SimplifyDeep(OmitDeepHelper(T, UnionToTuple(PathUnion)), UnknownArray)
}
/* compiles to:
 * export type OmitDeep<T, PathUnion extends LiteralUnion<Paths<T>, string>> = SimplifyDeep<OmitDeepHelper<T, UnionToTuple<PathUnion>>, UnknownArray>
 */

// ✓ OmitDeepHelper: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitDeepHelper(T, PathTuple: UnknownArray) {
  let t = T
  let pathTuple = PathTuple
  while (true) {
    const m1 = matches<[ Hole<"Path">, ...Hole<"RestPaths"> ]>(pathTuple)
    if (!m1) {
      break
    }
    t = OmitDeepWithOnePath(t, merge(m1.Path, string | number))
    pathTuple = m1.RestPaths
  }
  return t
}
/* compiles to:
 * export type OmitDeepHelper<T, PathTuple extends UnknownArray> = OmitDeepHelper__loop<T, PathTuple>
 * type OmitDeepHelper__loop<T, PathTuple extends UnknownArray> = PathTuple extends [infer Path, ...infer RestPaths] ? OmitDeepHelper__loop<OmitDeepWithOnePath<T, Path & (string | number)>, RestPaths> : T
 */

// ✓ OmitDeepWithOnePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitDeepWithOnePath(T, Path: string | number) {
  if (matches<NonRecursiveType>(T)) {
    return T
  }
  if (matches<UnknownArray>(T)) {
    return SetArrayAccess(OmitDeepArrayWithOnePath(T, Path), IsArrayReadonly(T))
  }
  if (matches<object>(T)) {
    return OmitDeepObjectWithOnePath(T, Path)
  }
  return T
}
/* compiles to:
 * export type OmitDeepWithOnePath<T, Path extends string | number> = T extends NonRecursiveType ? T : T extends UnknownArray ? SetArrayAccess<OmitDeepArrayWithOnePath<T, Path>, IsArrayReadonly<T>> : T extends object ? OmitDeepObjectWithOnePath<T, Path> : T
 */

// ✗ OmitDeepObjectWithOnePath: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function OmitDeepObjectWithOnePath(ObjectT: object, P: string | number) {
  const m1 = matches<`${Hole<"RecordKeyInPath">}.${Hole<"SubPath">}`>(P)
  if (m1) {
    const out = emptyObject
    for (const Key in keyof(ObjectT)) {
      out[Key] = matches<true>(IsEqual(m1.RecordKeyInPath, ToString(Key))) ? (raw('ExactKey<ObjectT, Key> extends infer RealKey ? RealKey extends keyof ObjectT ? OmitDeepWithOnePath<ObjectT[RealKey], SubPath> : ObjectT[Key] : ObjectT[Key]')) : ObjectT[Key]
    }
    return out
  }
  const m2 = matches<Hole<"Key">>(ExactKey(ObjectT, P))
  if (m2) {
    if (matches<true>(IsNever(m2.Key))) {
      return ObjectT
    }
    if (matches<PropertyKey>(m2.Key)) {
      return Omit(ObjectT, m2.Key)
    }
    return ObjectT
  }
  return ObjectT
}

// ✓ OmitDeepArrayWithOnePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitDeepArrayWithOnePath(ArrayType: UnknownArray, P: string | number) {
  const m1 = matches<`${Hole<"ArrayIndex", number>}.${Hole<"SubPath">}`>(P)
  if (m1) {
    if (matches<typeof m1.ArrayIndex>(number)) {
      return Array(OmitDeepWithOnePath(NonNullable(ArrayType[number]), m1.SubPath))
    }
    return ArraySplice(ArrayType, m1.ArrayIndex, 1, [OmitDeepWithOnePath(NonNullable(ArrayType[m1.ArrayIndex]), m1.SubPath)])
  }
  const m2 = matches<`${Hole<"ArrayIndex", number>}`>(P)
  if (m2) {
    if (matches<typeof m2.ArrayIndex>(number)) {
      return []
    }
    return ArraySplice(ArrayType, m2.ArrayIndex, 1, [unknown])
  }
  return ArrayType
}
/* compiles to:
 * export type OmitDeepArrayWithOnePath<ArrayType extends UnknownArray, P extends string | number> = P extends `${infer ArrayIndex extends number}.${infer SubPath}` ? number extends ArrayIndex ? Array<OmitDeepWithOnePath<NonNullable<ArrayType[number]>, SubPath>> : ArraySplice<ArrayType, ArrayIndex, 1, [OmitDeepWithOnePath<NonNullable<ArrayType[ArrayIndex]>, SubPath>]> : P extends `${infer ArrayIndex extends number}` ? number extends ArrayIndex ? [] : ArraySplice<ArrayType, ArrayIndex, 1, [unknown]> : ArrayType
 */

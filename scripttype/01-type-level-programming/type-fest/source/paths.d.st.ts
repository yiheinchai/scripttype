/**
 * ScriptType for 01-type-level-programming/type-fest/source/paths.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ApplyDefaultOptions: any
declare const DefaultPathsOptions: any
declare const IsAny: any
declare const MapsSetsOrArrays: any
declare const NonRecursiveType: any
declare const PathsOptions: any
declare const UnknownArray: any
type ApplyDefaultOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DefaultPathsOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsAny<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MapsSetsOrArrays<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonRecursiveType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PathsOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Paths: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Paths(T, Options: PathsOptions = {}) {
  return _Paths(T, ApplyDefaultOptions(PathsOptions, DefaultPathsOptions, Options))
}
/* compiles to:
 * export type Paths<T, Options extends PathsOptions = {}> = _Paths<T, ApplyDefaultOptions<PathsOptions, DefaultPathsOptions, Options>>
 */

// ✓ _Paths: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _Paths(T, Options: Required<PathsOptions>, CurrentDepth: number = 0) {
  if (matches<NonRecursiveType | Exclude<MapsSetsOrArrays, UnknownArray>>(T)) {
    return never
  }
  if (matches<true>(IsAny(T))) {
    return never
  }
  if (matches<object>(T)) {
    return InternalPaths(Required(T), Options, CurrentDepth)
  }
  return never
}
/* compiles to:
 * export type _Paths<T, Options extends Required<PathsOptions>, CurrentDepth extends number = 0> = T extends NonRecursiveType | Exclude<MapsSetsOrArrays, UnknownArray> ? never : IsAny<T> extends true ? never : T extends object ? InternalPaths<Required<T>, Options, CurrentDepth> : never
 */

// ✗ InternalPaths: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function InternalPaths(T, Options: Required<PathsOptions>, CurrentDepth: number) {
  const out = emptyObject
  for (const Key in keyof(T)) {
    out[Key] = matches<string | number>(Key) ? (raw('(And<Options[\'bracketNotation\'], IsNumberLike<Key>> extends true ? `[${Key}]` : CurrentDepth extends 0 ? Key | ToString<Key> : `.${(Key | ToString<Key>)}`) extends infer TransformedKey extends string | number ? ((Options[\'leavesOnly\'] extends true ? Options[\'maxRecursionDepth\'] extends CurrentDepth ? TransformedKey : IsNever<T[Key]> extends true ? TransformedKey : T[Key] extends infer Value ? (Value extends readonly [ ] | NonRecursiveType | Exclude<MapsSetsOrArrays, UnknownArray> ? TransformedKey : IsNever<keyof Value> extends true ? TransformedKey : never) : never : TransformedKey) extends infer _TransformedKey ? CurrentDepth extends Options[\'depth\'] ? _TransformedKey : never : never) | (GreaterThan<Options[\'maxRecursionDepth\'], CurrentDepth> extends true ? `${TransformedKey}${_Paths<T[Key], Options, Sum<CurrentDepth, 1>> & (string | number)}` : never) : never')) : never
  }
  return out[keyof(T) & (matches<UnknownArray>(T) ? number : unknown)]
}

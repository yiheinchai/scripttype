/**
 * ScriptType for 01-type-level-programming/type-fest/source/readonly-deep.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 covered, 2 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltIns: any
declare const HasMultipleCallSignatures: any
declare const ReadonlyMap: any
declare const ReadonlySet: any
type BuiltIns<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HasMultipleCallSignatures<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlyMap<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlySet<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type _ReadonlyObjectDeep<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ReadonlyDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyDeep(T) {
  if (matches<BuiltIns>(T)) {
    return T
  }
  if (matches<new (...arguments_: any[]) => unknown>(T)) {
    return T
  }
  if (matches<(...arguments_: any[]) => unknown>(T)) {
    if (matches<_ReadonlyObjectDeep<typeof T>>({})) {
      return T
    }
    if (matches<true>(HasMultipleCallSignatures(T))) {
      return T
    }
    return fnType([Parameters(T)], ReturnType(T)) & _ReadonlyObjectDeep(T)
  }
  const m1 = matches<Readonly<ReadonlyMap<Hole<"KeyType">, Hole<"ValueType">>>>(T)
  if (m1) {
    return ReadonlyMapDeep(m1.KeyType, m1.ValueType)
  }
  const m2 = matches<Readonly<ReadonlySet<Hole<"ItemType">>>>(T)
  if (m2) {
    return ReadonlySetDeep(m2.ItemType)
  }
  if (matches<readonly [ ] | readonly [ ...never[] ]>(T)) {
    return asReadonly([])
  }
  const m3 = matches<readonly [ Hole<"U">, ...Hole<"V"> ]>(T)
  if (m3) {
    return asReadonly([ReadonlyDeep(m3.U), ...ReadonlyDeep(m3.V)])
  }
  const m4 = matches<readonly [ ...Hole<"U">, Hole<"V"> ]>(T)
  if (m4) {
    return asReadonly([...ReadonlyDeep(m4.U), ReadonlyDeep(m4.V)])
  }
  const m5 = matches<ReadonlyArray<Hole<"ItemType">>>(T)
  if (m5) {
    return ReadonlyArray(ReadonlyDeep(m5.ItemType))
  }
  if (matches<object>(T)) {
    return _ReadonlyObjectDeep(T)
  }
  return unknown
}
/* compiles to:
 * export type ReadonlyDeep<T> = T extends BuiltIns ? T : T extends new (...arguments_: any[]) => unknown ? T : T extends (...arguments_: any[]) => unknown ? {} extends _ReadonlyObjectDeep<T> ? T : HasMultipleCallSignatures<T> extends true ? T : ((a0: Parameters<T>) => ReturnType<T>) & _ReadonlyObjectDeep<T> : T extends Readonly<ReadonlyMap<infer KeyType, infer ValueType>> ? ReadonlyMapDeep<KeyType, ValueType> : T extends Readonly<ReadonlySet<infer ItemType>> ? ReadonlySetDeep<ItemType> : T extends readonly [] | readonly [...never[]] ? readonly [] : T extends readonly [infer U, ...infer V] ? readonly [ReadonlyDeep<U>, ...ReadonlyDeep<V>] : T extends readonly [...infer U, infer V] ? readonly [...ReadonlyDeep<U>, ReadonlyDeep<V>] : T extends ReadonlyArray<infer ItemType> ? ReadonlyArray<ReadonlyDeep<ItemType>> : T extends object ? _ReadonlyObjectDeep<T> : unknown
 */

// ✗ ReadonlyMapDeep: the ScriptType does not itself typecheck as TypeScript
//   ReadonlyMapDeep.st.ts(3:29) TS2693: 'ReadonlyMap' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function ReadonlyMapDeep(KeyType, ValueType) {
  return merge({}, Readonly(ReadonlyMap(ReadonlyDeep(KeyType), ReadonlyDeep(ValueType))))
}
/* compiles to:
 * export type ReadonlyMapDeep<KeyType, ValueType> = {} & Readonly<ReadonlyMap<ReadonlyDeep<KeyType>, ReadonlyDeep<ValueType>>>
 */

// ✗ ReadonlySetDeep: the ScriptType does not itself typecheck as TypeScript
//   ReadonlySetDeep.st.ts(3:29) TS2693: 'ReadonlySet' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function ReadonlySetDeep(ItemType) {
  return merge({}, Readonly(ReadonlySet(ReadonlyDeep(ItemType))))
}
/* compiles to:
 * export type ReadonlySetDeep<ItemType> = {} & Readonly<ReadonlySet<ReadonlyDeep<ItemType>>>
 */

// ✓ _ReadonlyObjectDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _ReadonlyObjectDeep(ObjectType: object) {
  const out = emptyObject
  for (const KeyType in keyof(ObjectType)) {
    out[KeyType] = readonlyProp(ReadonlyDeep(ObjectType[KeyType]))
  }
  return out
}
/* compiles to:
 * export type _ReadonlyObjectDeep<ObjectType extends object> = { readonly [KeyType in keyof ObjectType]: ReadonlyDeep<ObjectType[KeyType]> }
 */

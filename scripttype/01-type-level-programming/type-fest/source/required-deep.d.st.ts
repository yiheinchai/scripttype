/**
 * ScriptType for 01-type-level-programming/type-fest/source/required-deep.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltIns: any
declare const HasMultipleCallSignatures: any
declare const IsNever: any
declare const ReadonlyMap: any
declare const ReadonlySet: any
declare const Simplify: any
type BuiltIns<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HasMultipleCallSignatures<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlyMap<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlySet<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Simplify<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ RequiredDeep: the ScriptType does not itself typecheck as TypeScript
//   RequiredDeep.st.ts(8:12) TS2348: Value of type 'MapConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function RequiredDeep(T) {
  if (matches<BuiltIns>(T)) {
    return T
  }
  const m1 = matches<Map<Hole<"KeyType">, Hole<"ValueType">>>(T)
  if (m1) {
    return Map(RequiredDeep(m1.KeyType), RequiredDeep(m1.ValueType))
  }
  const m2 = matches<Set<Hole<"ItemType">>>(T)
  if (m2) {
    return Set(RequiredDeep(m2.ItemType))
  }
  const m3 = matches<ReadonlyMap<Hole<"KeyType">, Hole<"ValueType">>>(T)
  if (m3) {
    return ReadonlyMap(RequiredDeep(m3.KeyType), RequiredDeep(m3.ValueType))
  }
  const m4 = matches<ReadonlySet<Hole<"ItemType">>>(T)
  if (m4) {
    return ReadonlySet(RequiredDeep(m4.ItemType))
  }
  const m5 = matches<WeakMap<Hole<"KeyType">, Hole<"ValueType">>>(T)
  if (m5) {
    return WeakMap(RequiredDeep(m5.KeyType), RequiredDeep(m5.ValueType))
  }
  const m6 = matches<WeakSet<Hole<"ItemType">>>(T)
  if (m6) {
    return WeakSet(RequiredDeep(m6.ItemType))
  }
  const m7 = matches<Promise<Hole<"ValueType">>>(T)
  if (m7) {
    return Promise(RequiredDeep(m7.ValueType))
  }
  if (matches<(...arguments_: any[]) => unknown>(T)) {
    if (matches<true>(IsNever(keyof(T)))) {
      return T
    }
    if (matches<true>(HasMultipleCallSignatures(T))) {
      return T
    }
    return fnType([Parameters(T)], ReturnType(T)) & RequiredObjectDeep(T)
  }
  if (matches<object>(T)) {
    return Simplify(RequiredObjectDeep(T))
  }
  return unknown
}
/* compiles to:
 * export type RequiredDeep<T> = T extends BuiltIns ? T : T extends Map<infer KeyType, infer ValueType> ? Map<RequiredDeep<KeyType>, RequiredDeep<ValueType>> : T extends Set<infer ItemType> ? Set<RequiredDeep<ItemType>> : T extends ReadonlyMap<infer KeyType, infer ValueType> ? ReadonlyMap<RequiredDeep<KeyType>, RequiredDeep<ValueType>> : T extends ReadonlySet<infer ItemType> ? ReadonlySet<RequiredDeep<ItemType>> : T extends WeakMap<infer KeyType, infer ValueType> ? WeakMap<RequiredDeep<KeyType>, RequiredDeep<ValueType>> : T extends WeakSet<infer ItemType> ? WeakSet<RequiredDeep<ItemType>> : T extends Promise<infer ValueType> ? Promise<RequiredDeep<ValueType>> : T extends (...arguments_: any[]) => unknown ? IsNever<keyof T> extends true ? T : HasMultipleCallSignatures<T> extends true ? T : ((a0: Parameters<T>) => ReturnType<T>) & RequiredObjectDeep<T> : T extends object ? Simplify<RequiredObjectDeep<T>> : unknown
 */

// ✓ RequiredObjectDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredObjectDeep(ObjectType: object) {
  const out = emptyObject
  for (const KeyType in keyof(ObjectType)) {
    out[KeyType] = required(RequiredDeep(ObjectType[KeyType]))
  }
  return out
}
/* compiles to:
 * export type RequiredObjectDeep<ObjectType extends object> = { [KeyType in keyof ObjectType]-?: RequiredDeep<ObjectType[KeyType]> }
 */

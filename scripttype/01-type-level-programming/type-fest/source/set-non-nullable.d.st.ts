/**
 * ScriptType for 01-type-level-programming/type-fest/source/set-non-nullable.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ SetNonNullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SetNonNullable(BaseType, Keys: keyof typeof BaseType = keyof(BaseType)) {
  const out = emptyObject
  for (const Key in keyof(BaseType)) {
    out[Key] = matches<typeof Keys>(Key) ? NonNullable(BaseType[Key]) : BaseType[Key]
  }
  return out
}
/* compiles to:
 * export type SetNonNullable<BaseType, Keys extends keyof BaseType = keyof BaseType> = {
 *   [Key in keyof BaseType]: Key extends Keys ? NonNullable<BaseType[Key]> : BaseType[Key]
 * }
 */

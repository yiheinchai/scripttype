/**
 * ScriptType for 01-type-level-programming/type-fest/source/set-parameter-type.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 raw, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const IsUnknown: any
declare const UnknownArray: any
declare const VariablePartOfArray: any
type IsUnknown<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type VariablePartOfArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ MergeObjectToArray: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function MergeObjectToArray(TArray: UnknownArray, TObject, TArrayCopy: UnknownArray = TArray) {
  if (matches<UnknownArray>(TObject)) {
    if (matches<(typeof TObject)['length']>(number)) {
      return TObject
    }
    const out = emptyObject
    for (const K in keyof(TArray)) {
      out[K] = matches<typeof K>(number) ? VariablePartOfArray(TArray)[number] : (matches<keyof typeof TObject>(K) ? TObject[K] : TArray[K])
    }
    return out
  }
  if (matches<object>(TObject)) {
    const out = emptyObject
    for (const K in keyof(TArray)) {
      out[K] = raw('K extends `${infer NumberK extends number}` ? NumberK extends keyof TObject ? TObject[NumberK] : TArray[K] : number extends K ? StaticPartOfArray<TArrayCopy>[\'length\'] extends keyof TObject ? TObject[StaticPartOfArray<TArrayCopy>[\'length\']] : TArray[K] : never')
    }
    return out
  }
  return never
}

// ✗ SetParameterType: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function SetParameterType(Function_: (...arguments_: any[]) => unknown, P: Record<number, unknown>) {
  const m1 = matches<(this: Hole<"ThisArgument">, ...arguments_: Hole<"Arguments">) => unknown>(Function_)
  if (m1) {
    if (matches<true>(IsUnknown(m1.ThisArgument))) {
      return fnType([MergeObjectToArray(m1.Arguments, P)], ReturnType(Function_))
    }
    return fnType([m1.ThisArgument, MergeObjectToArray(m1.Arguments, P)], ReturnType(Function_))
  }
  return Function_
}
/* compiles to:
 * export type SetParameterType<Function_ extends (...arguments_: any[]) => unknown, P extends Record<number, unknown>> = Function_ extends (this: infer ThisArgument, ...arguments_: infer Arguments) => unknown ? IsUnknown<ThisArgument> extends true ? (a0: MergeObjectToArray<Arguments, P>) => ReturnType<Function_> : (a0: ThisArgument, a1: MergeObjectToArray<Arguments, P>) => ReturnType<Function_> : Function_
 */

/**
 * ScriptType for 01-type-level-programming/type-fest/source/tagged.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 8 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const PropertyKey: any
declare const tag: any
type Opaque<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Tag<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TagContainer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Tagged<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type tag<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ TagContainer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TagContainer(Token) {
  return { [tag]: readonlyProp(Token) }
}
/* compiles to:
 * export type TagContainer<Token> = { readonly [tag]: Token }
 */

// ✓ Tag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tag(Token: PropertyKey, TagMetadata) {
  const out = emptyObject
  for (const K in keySet(Token)) {
    out[K] = TagMetadata
  }
  return TagContainer(out)
}
/* compiles to:
 * export type Tag<Token extends PropertyKey, TagMetadata> = TagContainer<
 *   { [K in Token]: TagMetadata }
 * >
 */

// ✓ Tagged: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tagged(Type, TagName: PropertyKey, TagMetadata = never) {
  return Type & Tag(TagName, TagMetadata)
}
/* compiles to:
 * export type Tagged<Type, TagName extends PropertyKey, TagMetadata = never> =
 *   Type & Tag<TagName, TagMetadata>
 */

// ✓ GetTagMetadata: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetTagMetadata(Type: Tag<typeof TagName, unknown>, TagName: PropertyKey) {
  return Type[t<typeof tag>()][TagName]
}
/* compiles to:
 * export type GetTagMetadata<Type extends Tag<TagName, unknown>, TagName extends PropertyKey> = Type[tag][TagName]
 */

// ✓ UnwrapTagged: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapTagged(TaggedType: Tag<PropertyKey, any>) {
  return RemoveAllTags(TaggedType)
}
/* compiles to:
 * export type UnwrapTagged<TaggedType extends Tag<PropertyKey, any>> = RemoveAllTags<TaggedType>
 */

// ✓ RemoveAllTags: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveAllTags__v1(T, ThisTag) {
  const m1 = matches<Tagged<Hole<"Type">, typeof ThisTag, (typeof T)[typeof tag][typeof ThisTag]>>(T)
  if (m1) {
    return RemoveAllTags(m1.Type)
  }
  return never
}
export function RemoveAllTags(T) {
  if (matches<Tag<PropertyKey, any>>(T)) {
    const out = emptyObject
    for (const ThisTag in keyof(T[t<typeof tag>()])) {
      out[ThisTag] = RemoveAllTags__v1(T, ThisTag)
    }
    return out[keyof(T[t<typeof tag>()])]
  }
  return T
}
/* compiles to:
 * export type RemoveAllTags__v1<T, ThisTag> =
 *   T extends Tagged<infer Type, ThisTag, T[tag][ThisTag]> ? RemoveAllTags<Type> : never
 * export type RemoveAllTags<T> =
 *   T extends Tag<PropertyKey, any>
 *     ? { [ThisTag in keyof T[tag]]: RemoveAllTags__v1<T, ThisTag> }[keyof T[tag]]
 *     : T
 */

// ✓ Opaque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Opaque(Type, Token = unknown) {
  return Type & TagContainer(Token)
}
/* compiles to:
 * export type Opaque<Type, Token = unknown> = Type & TagContainer<Token>
 */

// ✓ UnwrapOpaque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapOpaque(OpaqueType: TagContainer<unknown>) {
  if (matches<Tag<PropertyKey, any>>(OpaqueType)) {
    return RemoveAllTags(OpaqueType)
  }
  const m1 = matches<Opaque<Hole<"Type">, (typeof OpaqueType)[typeof tag]>>(OpaqueType)
  if (m1) {
    return m1.Type
  }
  return OpaqueType
}
/* compiles to:
 * export type UnwrapOpaque<OpaqueType extends TagContainer<unknown>> =
 *   OpaqueType extends Tag<PropertyKey, any> ? RemoveAllTags<OpaqueType>
 *   : OpaqueType extends Opaque<infer Type, OpaqueType[tag]> ? Type
 *   : OpaqueType
 */

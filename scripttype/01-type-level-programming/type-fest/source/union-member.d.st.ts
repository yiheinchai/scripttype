/**
 * ScriptType for 01-type-level-programming/type-fest/source/union-member.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const IsNever: any
declare const UnionToIntersection: any
type IsNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnionToIntersection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ UnionMember: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionMember(T) {
  if (matches<true>(IsNever(T))) {
    return never
  }
  const m1 = matches<() => (Hole<"R">)>(UnionToIntersection(matches<any>(T) ? fnType([], T) : never))
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type UnionMember<T> = IsNever<T> extends true ? never : UnionToIntersection<T extends any ? () => T : never> extends () => (infer R) ? R : never
 */

/**
 * ScriptType for 01-type-level-programming/type-fest/source/words.d.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { WordsOptions, _DefaultWordsOptions } from '../../../../01-type-level-programming/type-fest/source/words.d.js'
declare namespace m1 {
  export type FirstCharacter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type LeftSide<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type RemainingCharacters<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ApplyDefaultOptions: any
declare const AsciiPunctuation: any
declare const FirstCharacter: any
declare const IsLowercase: any
declare const IsNumeric: any
declare const IsUppercase: any
declare const WordSeparators: any
declare const WordsOptions: any
declare const _DefaultWordsOptions: any
type ApplyDefaultOptions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AsciiPunctuation<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FirstCharacter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsLowercase<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsNumeric<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsUppercase<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type WordSeparators<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ SkipEmptyWord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SkipEmptyWord(Word: string): any {
  if (matches<''>(Word)) {
    return []
  }
  return [Word]
}
/* compiles to:
 * export type SkipEmptyWord<Word extends string> = Word extends '' ? [] : [Word]
 */

// ✓ RemoveLastCharacter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveLastCharacter(Sentence: string, Character: string): any {
  const m1 = matches<`${Hole<"LeftSide">}${typeof Character}`>(Sentence)
  if (m1) {
    return SkipEmptyWord(m1.LeftSide)
  }
  return never
}
/* compiles to:
 * export type RemoveLastCharacter<Sentence extends string, Character extends string> =
 *   Sentence extends `${infer LeftSide}${Character}` ? SkipEmptyWord<LeftSide> : never
 */

// ✓ Words: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Words(Sentence: string, Options: WordsOptions = {}): any {
  return WordsImplementation(Sentence, ApplyDefaultOptions(WordsOptions, _DefaultWordsOptions, Options))
}
/* compiles to:
 * export type Words<Sentence extends string, Options extends WordsOptions = {}> = WordsImplementation<
 *   Sentence,
 *   ApplyDefaultOptions<WordsOptions, _DefaultWordsOptions, Options>
 * >
 */

// ✗ WordsImplementation: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function WordsImplementation(Sentence: string, Options: Required<WordsOptions>, LastCharacter: string = '', CurrentWord: string = ''): any {
  const m1 = matches<`${Hole<"FirstCharacter">}${Hole<"RemainingCharacters">}`>(Sentence)
  if (m1) {
    if (matches<WordSeparators | ((typeof Options)['splitOnPunctuation'] extends true ? AsciiPunctuation : never)>(m1.FirstCharacter)) {
      return [...SkipEmptyWord(CurrentWord), ...WordsImplementation(m1.RemainingCharacters, Options)]
    }
    if (matches<''>(LastCharacter)) {
      return WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, m1.FirstCharacter)
    }
    if (matches<[ IsNumeric<typeof LastCharacter>, IsNumeric<typeof m1.FirstCharacter> ]>([false, true])) {
      if (matches<true>(Options['splitOnNumbers'])) {
        return [...SkipEmptyWord(CurrentWord), ...WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, m1.FirstCharacter)]
      }
      return WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, `${CurrentWord}${m1.FirstCharacter}`)
    }
    if (matches<[ IsNumeric<typeof LastCharacter>, IsNumeric<typeof m1.FirstCharacter> ]>([true, false])) {
      if (matches<true>(Options['splitOnNumbers'])) {
        return [...SkipEmptyWord(CurrentWord), ...WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, m1.FirstCharacter)]
      }
      return WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, `${CurrentWord}${m1.FirstCharacter}`)
    }
    if (matches<[ IsNumeric<typeof LastCharacter>, IsNumeric<typeof m1.FirstCharacter> ]>([true, true])) {
      return WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, `${CurrentWord}${m1.FirstCharacter}`)
    }
    if (matches<[ IsLowercase<typeof LastCharacter>, IsUppercase<typeof m1.FirstCharacter> ]>([true, true])) {
      return [...SkipEmptyWord(CurrentWord), ...WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, m1.FirstCharacter)]
    }
    if (matches<[ IsUppercase<typeof LastCharacter>, IsLowercase<typeof m1.FirstCharacter> ]>([true, true])) {
      return [...RemoveLastCharacter(CurrentWord, LastCharacter), ...WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, `${LastCharacter}${m1.FirstCharacter}`)]
    }
    return WordsImplementation(m1.RemainingCharacters, Options, m1.FirstCharacter, `${CurrentWord}${m1.FirstCharacter}`)
  }
  return [...SkipEmptyWord(CurrentWord)]
}
/* compiles to:
 * export type WordsImplementation<
 *   Sentence extends string,
 *   Options extends Required<WordsOptions>,
 *   LastCharacter extends string = '',
 *   CurrentWord extends string = ''
 * > =
 *   Sentence extends `${infer FirstCharacter}${infer RemainingCharacters}`
 *     ? FirstCharacter extends WordSeparators | (Options)['splitOnPunctuation'] extends true ? AsciiPunctuation : never
 *       ? [...SkipEmptyWord<CurrentWord>, ...WordsImplementation<RemainingCharacters, Options>]
 *     : LastCharacter extends ''
 *       ? WordsImplementation<RemainingCharacters, Options, FirstCharacter, FirstCharacter>
 *     : [false, true] extends [IsNumeric<LastCharacter>, IsNumeric<FirstCharacter>]
 *       ? Options['splitOnNumbers'] extends true
 *         ? [
 *           ...SkipEmptyWord<CurrentWord>,
 *           ...WordsImplementation<RemainingCharacters, Options, FirstCharacter, FirstCharacter>
 *         ]
 *         : WordsImplementation<
 *           RemainingCharacters,
 *           Options,
 *           FirstCharacter,
 *           `${CurrentWord}${FirstCharacter}`
 *         >
 *     : [true, false] extends [IsNumeric<LastCharacter>, IsNumeric<FirstCharacter>]
 *       ? Options['splitOnNumbers'] extends true
 *         ? [
 *           ...SkipEmptyWord<CurrentWord>,
 *           ...WordsImplementation<RemainingCharacters, Options, FirstCharacter, FirstCharacter>
 *         ]
 *         : WordsImplementation<
 *           RemainingCharacters,
 *           Options,
 *           FirstCharacter,
 *           `${CurrentWord}${FirstCharacter}`
 *         >
 *     : [true, true] extends [IsNumeric<LastCharacter>, IsNumeric<FirstCharacter>]
 *       ? WordsImplementation<
 *         RemainingCharacters,
 *         Options,
 *         FirstCharacter,
 *         `${CurrentWord}${FirstCharacter}`
 *       >
 *     : [true, true] extends [IsLowercase<LastCharacter>, IsUppercase<FirstCharacter>]
 *       ? [
 *         ...SkipEmptyWord<CurrentWord>,
 *         ...WordsImplementation<RemainingCharacters, Options, FirstCharacter, FirstCharacter>
 *       ]
 *     : [true, true] extends [IsUppercase<LastCharacter>, IsLowercase<FirstCharacter>]
 *       ? [
 *         ...RemoveLastCharacter<CurrentWord, LastCharacter>,
 *         ...WordsImplementation<
 *           RemainingCharacters,
 *           Options,
 *           FirstCharacter,
 *           `${LastCharacter}${FirstCharacter}`
 *         >
 *       ]
 *     : WordsImplementation<
 *       RemainingCharacters,
 *       Options,
 *       FirstCharacter,
 *       `${CurrentWord}${FirstCharacter}`
 *     >
 *     : [...SkipEmptyWord<CurrentWord>]
 */

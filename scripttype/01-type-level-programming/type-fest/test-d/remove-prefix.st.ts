/**
 * ScriptType for 01-type-level-programming/type-fest/test-d/remove-prefix.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const RemovePrefix: any
type RemovePrefix<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Assignability: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Assignability(S: string) {
  return S
}
/* compiles to:
 * export type Assignability<S extends string> = S
 */

// ✓ Test1: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Test1(S: string, Prefix: string) {
  return Assignability(RemovePrefix(S, Prefix))
}
/* compiles to:
 * export type Test1<S extends string, Prefix extends string> = Assignability<RemovePrefix<S, Prefix>>
 */

// ✓ Test2: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Test2(S: Uppercase<string>, Prefix: '-' | '/' | '#') {
  return Assignability(RemovePrefix(S, Prefix))
}
/* compiles to:
 * export type Test2<S extends Uppercase<string>, Prefix extends '-' | '/' | '#'> = Assignability<RemovePrefix<S, Prefix>>
 */

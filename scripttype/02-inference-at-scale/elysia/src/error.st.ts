/**
 * ScriptType for 02-inference-at-scale/elysia/src/error.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ CheckExcessProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CheckExcessProps(T, U) {
  if (matches<1 & typeof T>(0)) {
    return T
  }
  if (matches<typeof U>(U)) {
    if (matches<never>(Exclude(keyof(T), keyof(U)))) {
      return T
    }
    const out = emptyObject
    for (const K in keyof(U)) {
      out[K] = U[K]
    }
    const out2 = emptyObject
    for (const K in keySet(Exclude(keyof(T), keyof(U)))) {
      out2[K] = never
    }
    return out & out2
  }
  return never
}
/* compiles to:
 * export type CheckExcessProps<T, U> = 0 extends 1 & T ? T : U extends U ? Exclude<keyof T, keyof U> extends never ? T : { [K in keyof U]: U[K] } & { [K1 in Exclude<keyof T, keyof U>]: never } : never
 */

// ✗ SelectiveStatus: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function SelectiveStatus(Res) {
  return raw('<const Code extends keyof Res | InvertedStatusMap[Extract<keyof InvertedStatusMap, keyof Res>], T extends Code extends keyof Res ? Res[Code] : Code extends keyof StatusMap ? Res[StatusMap[Code]] : never>(code: Code, response: CheckExcessProps<T, Code extends keyof Res ? Res[Code] : Code extends keyof StatusMap ? Res[StatusMap[Code]] : never>) => ElysiaCustomStatusResponse<Code, T>')
}

/**
 * ScriptType for 02-inference-at-scale/elysia/src/trace.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Context } from '../../../../02-inference-at-scale/elysia/src/context.js'
import type { TraceEvent, TraceListener } from '../../../../02-inference-at-scale/elysia/src/trace.js'
import type { Prettify, RouteSchema, SingletonBase } from '../../../../02-inference-at-scale/elysia/src/types.js'
declare const Context: any
declare const Prettify: any
declare const RouteSchema: any
declare const SingletonBase: any
declare const TraceEndDetail: any
declare const TraceEvent: any
declare const TraceListener: any
type TraceEndDetail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ TraceProcess: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TraceProcess(Type: 'begin' | 'end' = anyOf('begin', 'end'), WithChildren: boolean = true): any {
  if (matches<'begin'>(Type)) {
    return Prettify(merge({ name: string, begin: number, end: t<Promise<number>>(), error: t<Promise<Error | null>>(), onStop: methodType([optElem(fnType([TraceEndDetail], unknown))], t<Promise<void>>()) }, matches<true>(WithChildren) ? { total: number, onEvent: methodType([optElem(fnType([TraceProcess('begin', false)], unknown))], t<Promise<void>>()) } : { index: number }))
  }
  return number
}
/* compiles to:
 * export type TraceProcess<
 *   Type extends 'begin' | 'end' = 'begin' | 'end',
 *   WithChildren extends boolean = true
 * > =
 *   Type extends 'begin'
 *     ? Prettify<
 *       & {
 *           name: string
 *           begin: number
 *           end: Promise<number>
 *           error: Promise<Error | null>
 *           onStop(a0?: (a0: TraceEndDetail) => unknown): Promise<void>
 *         }
 *       & (
 *           WithChildren extends true
 *             ? {
 *               total: number
 *               onEvent(a0?: (a0: TraceProcess<'begin', false>) => unknown): Promise<void>
 *             }
 *             : { index: number }
 *         )
 *     >
 *     : number
 */

// ✓ TraceHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TraceHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }): any {
  const out = emptyObject
  for (const x in keySet(`on${Capitalize(TraceEvent)}`)) {
    out[x] = TraceListener
  }
  return { ...callSig([Prettify(merge({ id: number, context: Context(Route, Singleton), set: Context['set'], time: number, store: Singleton['store'], response: unknown }, out))], unknown) }
}
/* compiles to:
 * export type TraceHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} }
 * > =
 *   {
 *     (a0: Prettify<{ id: number; context: Context<Route, Singleton>; set: Context['set']; time: number; store: Singleton['store']; response: unknown } & { [X in `on${Capitalize<TraceEvent>}`]: TraceListener }>): unknown
 *   }
 */

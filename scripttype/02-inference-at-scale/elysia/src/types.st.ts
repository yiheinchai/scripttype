/**
 * ScriptType for 02-inference-at-scale/elysia/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 102 type-level aliases: 84 covered, 15 compile-error, 3 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Context, ErrorContext, PreContext } from '../../../../02-inference-at-scale/elysia/src/context.js'
import type { AnyElysia } from '../../../../02-inference-at-scale/elysia/src/index.js'
import type { AnySchema, BaseMacro, ContentType, DefinitionBase, DocumentDecoration, EmptyRouteSchema, EphemeralType, FastStandardSchemaV1Like, GuardSchemaType, InputSchema, LifeCycleType, Macro, PossibleResponse, RouteSchema, SingletonBase, UnwrapRoute } from '../../../../02-inference-at-scale/elysia/src/types.js'
declare namespace A {
  export type unshift<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Array {
  export type isArray<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type A<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type B<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Collision<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Current<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Fn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type OptionalParam<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Parameter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Params<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R200<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Rest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Start<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type Collision<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type Fn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Rest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m4 {
  export type A<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AnyElysia: any
declare const AnyElysiaCustomStatusResponse: any
declare const AnySchema: any
declare const AsyncGenerator: any
declare const BaseMacro: any
declare const Collision: any
declare const ContentType: any
declare const Context: any
declare const DefinitionBase: any
declare const DocumentDecoration: any
declare const Elysia: any
declare const ElysiaCustomStatusResponse: any
declare const EmptyRouteSchema: any
declare const EphemeralType: any
declare const ErrorContext: any
declare const FastStandardSchemaV1Like: any
declare const Generator: any
declare const GuardSchemaType: any
declare const InlineResponse: any
declare const InputSchema: any
declare const InternalServerError: any
declare const InvalidCookieSignature: any
declare const InvalidFileType: any
declare const InvertedStatusMap: any
declare const LifeCycleType: any
declare const Macro: any
declare const NotFoundError: any
declare const OptionalField: any
declare const ParseError: any
declare const PathParameterLike: any
declare const PossibleResponse: any
declare const PreContext: any
declare const R200: any
declare const Request: any
declare const Response: any
declare const RouteSchema: any
declare const RouteSchemaWithResolvedMacro: any
declare const SingletonBase: any
declare const TAnySchema: any
declare const TImport: any
declare const TModule: any
declare const TProperties: any
declare const TSchema: any
declare const UnwrapRoute: any
declare const ValidationError: any
type AfterHandler<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyElysiaCustomStatusResponse<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AsyncGenerator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Collision<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Elysia<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ElysiaCustomStatusResponse<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ErrorHandler<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Generator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InlineResponse<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InternalServerError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InvalidCookieSignature<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InvalidFileType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InvertedStatusMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MaybeArray<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MaybePromise<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NotFoundError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OptionalField<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OptionalHandler<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParseError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PathParameterLike<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type R200<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Request<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Response<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RouteSchemaWithResolvedMacro<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TAnySchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TImport<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TModule<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TProperties<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TSchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ValidationError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Equal: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equal(X, Y): any {
  if (matches<<T>() => T extends typeof Y ? 1 : 2>(genericFnType(['T'], [], matches<typeof X>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equal<X, Y> =
 *   (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? true : false
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✗ PickIfExists: does not compile yet
//   Type 'P' cannot be used to index type 'T'.
/* @scripttype preserveParamNames */
export function PickIfExists(T, K: string): any {
  if (matches<typeof T>({})) {
    return {}
  }
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P in T ? P : never] = T[P]
  }
  return out
}
/* compiles to:
 * export type PickIfExists<T, K extends string> =
 *   {} extends T ? {} : { [P in K as P extends keyof T ? P : never]: T[P] }
 */

// ✓ MaybeArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeArray(T): any {
  return T | arrayOf(T)
}
/* compiles to:
 * export type MaybeArray<T> = T | T[]
 */

// ✓ MaybeReadonlyArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeReadonlyArray(T): any {
  return T | readonlyArrayOf(T)
}
/* compiles to:
 * export type MaybeReadonlyArray<T> = T | readonly T[]
 */

// ✓ MaybePromise: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybePromise(T): any {
  return T | t<Promise<typeof T>>()
}
/* compiles to:
 * export type MaybePromise<T> = T | Promise<T>
 */

// ✓ ObjectValues: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectValues(T: object): any {
  return T[keyof(T)]
}
/* compiles to:
 * export type ObjectValues<T extends object> = T[keyof T]
 */

// ✓ IsPathParameter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPathParameter(Part: string): any {
  const m1 = matches<`:${Hole<"Parameter">}`>(Part)
  if (m1) {
    return m1.Parameter
  }
  if (matches<`*`>(Part)) {
    return '*'
  }
  return never
}
/* compiles to:
 * export type IsPathParameter<Part extends string> =
 *   Part extends `:${infer Parameter}` ? Parameter : Part extends `*` ? '*' : never
 */

// ✓ GetPathParameter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetPathParameter(Path: string): any {
  const m1 = matches<`${Hole<"A">}/${Hole<"B">}`>(Path)
  if (m1) {
    return IsPathParameter(m1.A) | GetPathParameter(m1.B)
  }
  return IsPathParameter(Path)
}
/* compiles to:
 * export type GetPathParameter<Path extends string> =
 *   Path extends `${infer A}/${infer B}`
 *     ? IsPathParameter<A> | GetPathParameter<B>
 *     : IsPathParameter<Path>
 */

// ✓ _ResolvePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _ResolvePath__v1(Param): any {
  const m1 = matches<`${Hole<"OptionalParam">}?`>(Param)
  if (m1) {
    return m1.OptionalParam
  }
  return never
}
export function _ResolvePath(Path: string): any {
  const out = emptyObject
  for (const Param in keySet(GetPathParameter(Path))) {
    out[matches<`${string}?`>(Param) ? never : Param] = string
  }
  const out2 = emptyObject
  for (const Param in keySet(GetPathParameter(Path))) {
    out2[_ResolvePath__v1(Param)] = optional(string)
  }
  return out & out2
}
/* compiles to:
 * export type _ResolvePath__v1<Param> =
 *   Param extends `${infer OptionalParam}?` ? OptionalParam : never
 * export type _ResolvePath<Path extends string> =
 *   & { [Param in GetPathParameter<Path> as Param extends `${string}?` ? never : Param]: string }
 *   & { [Param1 in GetPathParameter<Path> as _ResolvePath__v1<Param1>]?: string }
 */

// ✓ ResolvePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolvePath(Path: string): any {
  if (matches<''>(Path)) {
    return {}
  }
  if (matches<PathParameterLike>(Path)) {
    return _ResolvePath(Path)
  }
  return {}
}
/* compiles to:
 * export type ResolvePath<Path extends string> =
 *   Path extends '' ? {} : Path extends PathParameterLike ? _ResolvePath<Path> : {}
 */

// ✓ Or: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Or(T1: boolean, T2: boolean): any {
  if (matches<true>(T1)) {
    return true
  }
  if (matches<true>(T2)) {
    return true
  }
  return false
}
/* compiles to:
 * export type Or<T1 extends boolean, T2 extends boolean> =
 *   T1 extends true ? true : T2 extends true ? true : false
 */

// ✓ Prettify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prettify(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Prettify<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ NeverKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NeverKey(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = optional(never)
  }
  return merge(out, {})
}
/* compiles to:
 * export type NeverKey<T> = { [K in keyof T]?: never } & {}
 */

// ✓ IsBothObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsBothObject(A, B): any {
  if (matches<Record<keyof any, any>>(A)) {
    if (matches<Record<keyof any, any>>(B)) {
      if (matches<false>(IsClass(A))) {
        if (matches<false>(IsClass(B))) {
          return true
        }
        return false
      }
      return false
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsBothObject<A, B> =
 *   A extends Record<keyof any, any>
 *     ? B extends Record<keyof any, any>
 *       ? IsClass<A> extends false ? IsClass<B> extends false ? true : false : false
 *       : false
 *     : false
 */

// ✓ IsClass: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsClass(V): any {
  if (matches<abstract new (...args: any) => any>(V)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsClass<V> = V extends abstract new (...args: any) => any ? true : false
 */

// ✓ And: verified type-identical to the original
/* @scripttype preserveParamNames */
export function And(A, B): any {
  if (matches<true>(A)) {
    if (matches<true>(B)) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type And<A, B> = A extends true ? B extends true ? true : false : false
 */

// ✗ Reconcile: does not compile yet
//   Type 'Key1' cannot be used to index type 'A'.
/* @scripttype preserveParamNames */
/**
 * @param {Object} A
 * @param {Object} B
 */
export function Reconcile(A, B, Override: boolean = false, Stack: number[] = []): any {
  if (matches<16>(Stack['length'])) {
    return A
  }
  if (matches<true>(Override)) {
    const out = emptyObject
    for (const key in keyof(A)) {
      out[key in B ? never : key] = A[key]
    }
    const m1 = matches<Hole<"Collision">>(out)
    if (m1) {
      if (matches<typeof m1.Collision>({})) {
        const out2 = emptyObject
        for (const key in keyof(B)) {
          out2[key] = matches<true>(IsBothObject(A[key], B[key])) ? Reconcile(A[key], B[key], Override, [0, ...Stack]) : B[key]
        }
        return out2
      }
      const out3 = emptyObject
      for (const key in keyof(B)) {
        out3[key] = B[key]
      }
      return Prettify(merge(m1.Collision, out3))
    }
    return never
  }
  const out4 = emptyObject
  for (const key in keyof(B)) {
    out4[key in A ? never : key] = B[key]
  }
  const m2 = matches<Hole<"Collision">>(out4)
  if (m2) {
    if (matches<typeof m2.Collision>({})) {
      const out5 = emptyObject
      for (const key in keyof(A)) {
        out5[key] = matches<true>(IsBothObject(A[key], B[key])) ? Reconcile(A[key], B[key], Override, [0, ...Stack]) : A[key]
      }
      return out5
    }
    const out6 = emptyObject
    for (const key in keyof(A)) {
      out6[key] = A[key]
    }
    return Prettify(merge(out6, m2.Collision))
  }
  return never
}
/* compiles to:
 * export type Reconcile<
 *   A extends Object,
 *   B extends Object,
 *   Override extends boolean = false,
 *   Stack extends number[] = []
 * > =
 *   Stack['length'] extends 16 ? A
 *   : Override extends true
 *     ? { [Key in keyof A as Key extends keyof B ? never : Key]: A[Key] } extends infer Collision
 *       ? {} extends Collision
 *         ? {
 *           [Key1 in keyof B]: IsBothObject<A[Key1], B[Key1]> extends true
 *             ? Reconcile<A[Key1], B[Key1], Override, [0, ...Stack]>
 *             : B[Key1]
 *         }
 *         : Prettify<Collision & { [Key2 in keyof B]: B[Key2] }>
 *       : never
 *   : { [Key3 in keyof B as Key3 extends keyof A ? never : Key3]: B[Key3] } extends infer Collision
 *     ? {} extends Collision
 *       ? {
 *         [Key4 in keyof A]: IsBothObject<A[Key4], B[Key4]> extends true
 *           ? Reconcile<A[Key4], B[Key4], Override, [0, ...Stack]>
 *           : A[Key4]
 *       }
 *       : Prettify<{ [Key5 in keyof A]: A[Key5] } & Collision>
 *   : never
 */

// ✓ UnwrapSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapSchema(Schema: AnySchema | string | undefined, Definitions: DefinitionBase['typebox'] = {}): any {
  if (typeof Schema === 'undefined') {
    return unknown
  }
  if (matches<TSchema>(Schema)) {
    if (matches<OptionalField>(Schema)) {
      return Partial(TImport(merge(Definitions, { __elysia: readonlyProp(Schema) }), '__elysia')['static'])
    }
    return TImport(merge(Definitions, { __elysia: readonlyProp(Schema) }), '__elysia')['static']
  }
  if (matches<FastStandardSchemaV1Like>(Schema)) {
    return NonNullable(Schema['~standard']['types'])['output']
  }
  if (typeof Schema === 'string') {
    if (Schema in Definitions) {
      if (matches<TAnySchema>(Definitions[Schema])) {
        return TImport(Definitions, Schema)['static']
      }
      return NonNullable(Definitions[Schema]['~standard']['types'])['output']
    }
    return unknown
  }
  return unknown
}
/* compiles to:
 * export type UnwrapSchema<
 *   Schema extends AnySchema | string | undefined,
 *   Definitions extends DefinitionBase['typebox'] = {}
 * > =
 *   Schema extends undefined ? unknown
 *   : Schema extends TSchema
 *     ? Schema extends OptionalField
 *       ? Partial<TImport<Definitions & { readonly __elysia: Schema }, '__elysia'>['static']>
 *       : TImport<Definitions & { readonly __elysia: Schema }, '__elysia'>['static']
 *   : Schema extends FastStandardSchemaV1Like
 *     ? NonNullable<Schema['~standard']['types']>['output']
 *   : Schema extends string
 *     ? Schema extends keyof Definitions
 *       ? Definitions[Schema] extends TAnySchema
 *         ? TImport<Definitions, Schema>['static']
 *         : NonNullable<Definitions[Schema]['~standard']['types']>['output']
 *       : unknown
 *   : unknown
 */

// ✓ UnwrapBodySchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapBodySchema(Schema: AnySchema | string | undefined, Definitions: DefinitionBase['typebox'] = {}): any {
  if (matches<typeof Schema>(Undefined)) {
    return unknown
  }
  if (matches<TSchema>(Schema)) {
    if (matches<OptionalField>(Schema)) {
      return Partial(TImport(merge(Definitions, { __elysia: readonlyProp(Schema) }), '__elysia')['static']) | Null
    }
    return TImport(merge(Definitions, { __elysia: readonlyProp(Schema) }), '__elysia')['static']
  }
  if (matches<FastStandardSchemaV1Like>(Schema)) {
    return NonNullable(Schema['~standard']['types'])['output']
  }
  if (typeof Schema === 'string') {
    if (Schema in Definitions) {
      if (matches<TAnySchema>(Definitions[Schema])) {
        return TImport(Definitions, Schema)['static']
      }
      return NonNullable(Definitions[Schema]['~standard']['types'])['output']
    }
    return unknown
  }
  return unknown
}
/* compiles to:
 * export type UnwrapBodySchema<
 *   Schema extends AnySchema | string | undefined,
 *   Definitions extends DefinitionBase['typebox'] = {}
 * > =
 *   undefined extends Schema ? unknown
 *   : Schema extends TSchema
 *     ? Schema extends OptionalField
 *       ? | Partial<TImport<Definitions & { readonly __elysia: Schema }, '__elysia'>['static']>
 *       | null
 *       : TImport<Definitions & { readonly __elysia: Schema }, '__elysia'>['static']
 *   : Schema extends FastStandardSchemaV1Like
 *     ? NonNullable<Schema['~standard']['types']>['output']
 *   : Schema extends string
 *     ? Schema extends keyof Definitions
 *       ? Definitions[Schema] extends TAnySchema
 *         ? TImport<Definitions, Schema>['static']
 *         : NonNullable<Definitions[Schema]['~standard']['types']>['output']
 *       : unknown
 *   : unknown
 */

// ✓ HookContainer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HookContainer(T: Function = t<Function>()): any {
  return { checksum: optional(number), scope: optional(LifeCycleType), subType: optional(anyOf('derive', 'resolve', 'mapDerive', 'mapResolve', merge(string, {}))), fn: T, isAsync: optional(boolean), hasReturn: optional(boolean) }
}
/* compiles to:
 * export type HookContainer<T extends Function = Function> = {
 *   checksum?: number
 *   scope?: LifeCycleType
 *   subType?: 'derive' | 'resolve' | 'mapDerive' | 'mapResolve' | string & {}
 *   fn: T
 *   isAsync?: boolean
 *   hasReturn?: boolean
 * }
 */

// ✓ IntersectIfObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectIfObject(A, B): any {
  if (matches<Record<any, any>>(A)) {
    if (matches<Record<any, any>>(B)) {
      return A & B
    }
    return A
  }
  if (matches<Record<any, any>>(B)) {
    return B
  }
  return A
}
/* compiles to:
 * export type IntersectIfObject<A, B> =
 *   A extends Record<any, any> ? B extends Record<any, any> ? A & B : A
 *   : B extends Record<any, any> ? B
 *   : A
 */

// ✓ MergeSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeSchema(A: RouteSchema, B: RouteSchema, Path: string = ''): any {
  if (matches<typeof A>({})) {
    if (matches<PathParameterLike>(Path)) {
      return merge(Omit(B, 'params'), { params: ResolvePath(Path) })
    }
    return B
  }
  if (matches<typeof B>({})) {
    if (matches<PathParameterLike>(Path)) {
      return merge(Omit(A, 'params'), { params: ResolvePath(Path) })
    }
    return A
  }
  return { body: matches<(typeof A)['body']>(Undefined) ? B['body'] : A['body'], headers: matches<(typeof A)['headers']>(Undefined) ? B['headers'] : A['headers'], query: matches<(typeof A)['query']>(Undefined) ? B['query'] : A['query'], params: matches<true>(IsNever(keyof(A['params']))) ? (matches<true>(IsNever(keyof(B['params']))) ? ResolvePath(Path) : B['params']) : (matches<true>(IsNever(keyof(B['params']))) ? A['params'] : Prettify(merge(B['params'], Omit(A['params'], keyof(B['params']))))), cookie: matches<(typeof A)['cookie']>(Undefined) ? B['cookie'] : A['cookie'], response: matches<(typeof A)['response']>({}) ? (matches<(typeof B)['response']>({}) ? {} : B['response']) : (matches<(typeof B)['response']>({}) ? A['response'] : (merge(A['response'], Omit(B['response'], keyof(A['response']))))) }
}
/* compiles to:
 * export type MergeSchema<
 *   A extends RouteSchema,
 *   B extends RouteSchema,
 *   Path extends string = ''
 * > =
 *   {} extends A
 *     ? Path extends PathParameterLike ? Omit<B, 'params'> & { params: ResolvePath<Path> } : B
 *   : {} extends B
 *     ? Path extends PathParameterLike ? Omit<A, 'params'> & { params: ResolvePath<Path> } : A
 *   : {
 *     body: undefined extends A['body'] ? B['body'] : A['body']
 *     headers: undefined extends A['headers'] ? B['headers'] : A['headers']
 *     query: undefined extends A['query'] ? B['query'] : A['query']
 *     params: IsNever<keyof A['params']> extends true
 *       ? IsNever<keyof B['params']> extends true ? ResolvePath<Path> : B['params']
 *     : IsNever<keyof B['params']> extends true ? A['params']
 *     : Prettify<B['params'] & Omit<A['params'], keyof B['params']>>
 *     cookie: undefined extends A['cookie'] ? B['cookie'] : A['cookie']
 *     response: {} extends A['response'] ? {} extends B['response'] ? {} : B['response']
 *     : {} extends B['response'] ? A['response']
 *     : A['response'] & Omit<B['response'], keyof A['response']>
 *   }
 */

// ✓ Handler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Handler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([Context(Route, Singleton, Path)], MaybePromise(matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])]))
}
/* compiles to:
 * export type Handler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (a0: Context<Route, Singleton, Path>) => MaybePromise<
 *     {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']]
 *   >
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  if (matches<1 & typeof T>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = 0 extends 1 & T ? true : false
 */

// ✓ Replace: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Replace(Original, Target, With): any {
  if (matches<true>(IsAny(Target))) {
    return Original
  }
  if (matches<Record<string, unknown>>(Original)) {
    const out = emptyObject
    for (const K in keyof(Original)) {
      out[K] = matches<typeof Target>(Original[K]) ? With : Original[K]
    }
    return out
  }
  if (matches<typeof Target>(Original)) {
    return With
  }
  return Original
}
/* compiles to:
 * export type Replace<Original, Target, With> =
 *   IsAny<Target> extends true ? Original
 *   : Original extends Record<string, unknown>
 *     ? { [K in keyof Original]: Original[K] extends Target ? With : Original[K] }
 *   : Original extends Target ? With
 *   : Original
 */

// ✓ CoExist: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CoExist(Original, Target, With): any {
  if (matches<true>(IsAny(Target))) {
    return Original
  }
  if (matches<Record<string, unknown>>(Original)) {
    const out = emptyObject
    for (const K in keyof(Original)) {
      out[K] = matches<typeof Target>(Original[K]) ? (Original[K] | With) : Original[K]
    }
    return out
  }
  if (matches<typeof Target>(Original)) {
    return Original | With
  }
  return Original
}
/* compiles to:
 * export type CoExist<Original, Target, With> =
 *   IsAny<Target> extends true ? Original
 *   : Original extends Record<string, unknown>
 *     ? { [K in keyof Original]: Original[K] extends Target ? Original[K] | With : Original[K] }
 *   : Original extends Target ? Original | With
 *   : Original
 */

// ✓ ReturnTypeIfPossible: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReturnTypeIfPossible(T, Enabled = true): any {
  if (matches<typeof Enabled>(false)) {
    return {}
  }
  const m1 = matches<(...a: any) => Hole<"R">>(T)
  if (m1) {
    return m1.R
  }
  return T
}
/* compiles to:
 * export type ReturnTypeIfPossible<T, Enabled = true> =
 *   false extends Enabled ? {} : T extends (...a: any) => infer R ? R : T
 */

// ✗ FunctionArrayReturnType: does not compile yet
//   Type 'T' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function FunctionArrayReturnType(T): any {
  if (matches<typeof T>(arrayOf(any))) {
    return never
  }
  if (Array.isArray(T)) {
    return _FunctionArrayReturnType(T)
  }
  return Awaited(ReturnType(T))
}
/* compiles to:
 * export type FunctionArrayReturnType<T> =
 *   any[] extends T ? never
 *   : T extends any[] ? _FunctionArrayReturnType<T>
 *   : Awaited<ReturnType<T>>
 */

// ✗ _FunctionArrayReturnType: does not compile yet
//   Type 'Fn' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function _FunctionArrayReturnType(T, Carry = Undefined): any {
  const m3 = matches<[ Hole<"Fn">, ...Hole<"Rest"> ]>(T)
  if (m3) {
    const m4 = matches<Hole<"A">>(Awaited(ReturnType(m3.Fn)))
    return _FunctionArrayReturnType(m3.Rest, m4 ? (matches<true>(IsNever(m4.A)) ? Carry : (m4.A | Carry)) : Carry)
  }
  return Carry
}
/* compiles to:
 * export type _FunctionArrayReturnType<T, Carry = undefined> =
 *   T extends [infer Fn, ...infer Rest]
 *     ? _FunctionArrayReturnType<
 *       Rest,
 *       Awaited<ReturnType<Fn>> extends infer A
 *         ? IsNever<A> extends true ? Carry : A | Carry
 *         : Carry
 *     >
 *     : Carry
 */

// ✗ FunctionArrayReturnTypeNonNullable: does not compile yet
//   Type 'T' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function FunctionArrayReturnTypeNonNullable(T): any {
  if (matches<typeof T>(arrayOf(any))) {
    return never
  }
  if (Array.isArray(T)) {
    return _FunctionArrayReturnTypeNonNullable(T)
  }
  return NonNullable(Awaited(ReturnType(T)))
}
/* compiles to:
 * export type FunctionArrayReturnTypeNonNullable<T> =
 *   any[] extends T ? never
 *   : T extends any[] ? _FunctionArrayReturnTypeNonNullable<T>
 *   : NonNullable<Awaited<ReturnType<T>>>
 */

// ✗ _FunctionArrayReturnTypeNonNullable: does not compile yet
//   Type 'Fn' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function _FunctionArrayReturnTypeNonNullable(T, Carry = Undefined): any {
  const m3 = matches<[ Hole<"Fn">, ...Hole<"Rest"> ]>(T)
  if (m3) {
    const m4 = matches<Hole<"A">>(NonNullable(Awaited(ReturnType(m3.Fn))))
    return _FunctionArrayReturnTypeNonNullable(m3.Rest, m4 ? (matches<true>(IsNever(m4.A)) ? Carry : (m4.A | Carry)) : Carry)
  }
  return Carry
}
/* compiles to:
 * export type _FunctionArrayReturnTypeNonNullable<T, Carry = undefined> =
 *   T extends [infer Fn, ...infer Rest]
 *     ? _FunctionArrayReturnTypeNonNullable<
 *       Rest,
 *       NonNullable<Awaited<ReturnType<Fn>>> extends infer A
 *         ? IsNever<A> extends true ? Carry : A | Carry
 *         : Carry
 *     >
 *     : Carry
 */

// ✓ ExtractResolveFromMacro: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractResolveFromMacro(A): any {
  if (matches<true>(IsNever(A))) {
    return {}
  }
  if (matches<AnyElysiaCustomStatusResponse>(A)) {
    return A
  }
  const m1 = matches<Hole<"A">>(Exclude(A, AnyElysiaCustomStatusResponse))
  if (m1) {
    if (matches<true>(IsAny(m1.A))) {
      return {}
    }
    return m1.A
  }
  return {}
}
/* compiles to:
 * export type ExtractResolveFromMacro<A> =
 *   IsNever<A> extends true ? {}
 *   : A extends AnyElysiaCustomStatusResponse ? A
 *   : Exclude<A, AnyElysiaCustomStatusResponse> extends infer A ? IsAny<A> extends true ? {} : A
 *   : {}
 */

// ✓ ExtractOnlyResponseFromMacro: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractOnlyResponseFromMacro(A): any {
  if (matches<true>(IsNever(A))) {
    return {}
  }
  const m1 = matches<Hole<"A">>(Extract(A, AnyElysiaCustomStatusResponse))
  if (m1) {
    if (matches<true>(IsNever(m1.A))) {
      return {}
    }
    return { return: MergeResponseStatus(m1.A) }
  }
  return {}
}
/* compiles to:
 * export type ExtractOnlyResponseFromMacro<A> =
 *   IsNever<A> extends true ? {}
 *   : Extract<A, AnyElysiaCustomStatusResponse> extends infer A
 *     ? IsNever<A> extends true ? {} : { return: MergeResponseStatus<A> }
 *   : {}
 */

// ✗ MergeResponseStatus: the ScriptType does not itself typecheck as TypeScript
//   MergeResponseStatus.st.ts(14:47) TS2708: Cannot use namespace 'm1' as a value.
/* @scripttype preserveParamNames */
export function MergeResponseStatus__v1(A, status): any {
  const m1 = matches<Hole<"Value">>(Extract(A, { code: status })['response'])
  if (m1) {
    if (matches<true>(IsAny(m1.Value))) {
      return InvertedStatusMap[status]
    }
    return m1.Value
  }
  return never
}
export function MergeResponseStatus(A): any {
  const out = emptyObject
  for (const status in keyof(UnionToIntersect(m1 ? out2 : never))) {
    out[status] = MergeResponseStatus__v1(A, status)
  }
  return out
}
/* compiles to:
 * export type MergeResponseStatus__v1<A, status> =
 *   Extract<A, { code: status }>['response'] extends infer Value
 *     ? IsAny<Value> extends true ? InvertedStatusMap[status] : Value
 *     : never
 * export type MergeResponseStatus<A> = {
 *   [Status in keyof UnionToIntersect<m1 extends true ? out2 : never>]: MergeResponseStatus__v1<
 *     A,
 *     Status
 *   >
 * }
 */

// ✓ MergeAllStatus: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeAllStatus__v1(T, K): any {
  const m1 = matches<Record<typeof K, Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
export function MergeAllStatus(T): any {
  const out = emptyObject
  for (const K in keySet(matches<any>(T) ? keyof(T) : never)) {
    out[K] = MergeAllStatus__v1(T, K)
  }
  return out
}
/* compiles to:
 * export type MergeAllStatus__v1<T, K> = T extends Record<K, infer V> ? V : never
 * export type MergeAllStatus<T> = {
 *   [K in T extends any ? keyof T : never]: MergeAllStatus__v1<T, K>
 * }
 */

// ✗ ExtractAllResponseFromMacro: the ScriptType does not itself typecheck as TypeScript
//   ExtractAllResponseFromMacro.st.ts(7:45) TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
/* @scripttype preserveParamNames */
export function ExtractAllResponseFromMacro(A): any {
  if (matches<true>(IsNever(A))) {
    return {}
  }
  const m1 = matches<Hole<"A">>(Exclude(A, AnyElysiaCustomStatusResponse))
  return { return: MergeResponseStatus(A) & (m1 ? (matches<true>(IsAny(m1.A)) ? {} : (matches<true>(IsNever(m1.A)) ? {} : (matches<typeof m1.A>(NonNullable(voidType())) ? {} : (matches<typeof m1.A>(Undefined) ? {} : { 200: m1.A })))) : {}) }
}
/* compiles to:
 * export type ExtractAllResponseFromMacro<A> =
 *   IsNever<A> extends true
 *     ? {}
 *     : {
 *       return: & MergeResponseStatus<A>
 *       & (
 *           Exclude<A, AnyElysiaCustomStatusResponse> extends infer A
 *             ? IsAny<A> extends true ? {}
 *             : IsNever<A> extends true ? {}
 *             : NonNullable<void> extends A ? {}
 *             : undefined extends A ? {}
 *             : { '200': A }
 *             : {}
 *         )
 *     }
 */

// ✓ FlattenMacroResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FlattenMacroResponse(T): any {
  if (matches<object>(T)) {
    if ('_' in T) {
      return MergeFlattenMacroResponse(Omit(T, '_'), FlattenMacroResponse(MergeAllStatus(T['_'])))
    }
    return T
  }
  return T
}
/* compiles to:
 * export type FlattenMacroResponse<T> =
 *   T extends object
 *     ? '_' extends keyof T
 *       ? MergeFlattenMacroResponse<Omit<T, '_'>, FlattenMacroResponse<MergeAllStatus<T['_']>>>
 *       : T
 *     : T
 */

// ✓ MergeFlattenMacroResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeFlattenMacroResponse(A, B): any {
  const out = emptyObject
  for (const K in keySet(keyof(A) | keyof(B))) {
    out[K] = K in A ? (K in B ? (A[K] | B[K]) : A[K]) : (K in B ? B[K] : never)
  }
  return out
}
/* compiles to:
 * export type MergeFlattenMacroResponse<A, B> = {
 *   [K in keyof A | keyof B]: K extends keyof A ? K extends keyof B ? A[K] | B[K] : A[K]
 *   : K extends keyof B ? B[K]
 *   : never
 * }
 */

// ✗ UnionMacroContext: does not compile yet
//   Type '"return"' cannot be used to index type 'A'.
/* @scripttype preserveParamNames */
export function UnionMacroContext(A): any {
  const out = emptyObject
  for (const K in keySet(Exclude(keyof(A), 'return'))) {
    out[K] = A[K]
  }
  return merge(UnionToIntersect(out), { return: { _: A['return'] } })
}
/* compiles to:
 * export type UnionMacroContext<A> =
 *   UnionToIntersect<{ [K in Exclude<keyof A, 'return'>]: A[K] }> & { return: { _: A['return'] } }
 */

// ✗ MacroToContext: does not compile yet
//   Type '"return"' cannot be used to index type 'A'.
/* @scripttype preserveParamNames */
export function MacroToContext(MacroFn: Macro = {}, SelectedMacro: BaseMacro = {}, Definitions: DefinitionBase['typebox'] = {}, R: 1[] = []): any {
  const m1 = matches<Hole<"A">>(InnerMacroToContext(MacroFn, Pick(SelectedMacro, Extract(keyof(MacroFn), keyof(SelectedMacro))), Definitions, R))
  const out = emptyObject
  for (const K in keySet(Exclude(keyof(m1.A), 'return'))) {
    out[K] = UnionToIntersect(m1.A[K])
  }
  return Prettify(m1 ? (out & Prettify({ return: FlattenMacroResponse(m1.A['return']) })) : {})
}
/* compiles to:
 * export type MacroToContext<
 *   MacroFn extends Macro = {},
 *   SelectedMacro extends BaseMacro = {},
 *   Definitions extends DefinitionBase['typebox'] = {},
 *   R extends 1[] = []
 * > =
 *   Prettify<
 *     InnerMacroToContext<MacroFn, Pick<SelectedMacro, Extract<keyof MacroFn, keyof SelectedMacro>>, Definitions, R> extends infer A
 *       ? & { [K in Exclude<keyof A, 'return'>]: UnionToIntersect<A[K]> }
 *       & Prettify<{ return: FlattenMacroResponse<A['return']> }>
 *       : {}
 *   >
 */

// ✓ InnerMacroToContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InnerMacroToContext__v1(MacroFn, SelectedMacro, Definitions, R, key): any {
  const m1 = matches<Hole<"Value">>(ReturnTypeIfPossible(MacroFn[key], SelectedMacro[key]))
  if (m1) {
    return merge({ resolve: ExtractResolveFromMacro(Extract(Exclude(FunctionArrayReturnType(m1.Value['resolve']), AnyElysiaCustomStatusResponse), Record(any, unknown))) }, UnwrapMacroSchema(m1.Value, Definitions), ExtractAllResponseFromMacro(FunctionArrayReturnTypeNonNullable(m1.Value['beforeHandle'])), ExtractAllResponseFromMacro(FunctionArrayReturnTypeNonNullable(m1.Value['afterHandle'])), ExtractAllResponseFromMacro(FunctionArrayReturnType(m1.Value['error'])), ExtractOnlyResponseFromMacro(FunctionArrayReturnTypeNonNullable(m1.Value['resolve'])), InnerMacroToContext(MacroFn, Pick(m1.Value, Extract(keyof(MacroFn), keyof(m1.Value))), Definitions, [...R, 1]))
  }
  return {}
}
/**
 * @param {Macro} MacroFn
 * @param {BaseMacro} SelectedMacro
 */
export function InnerMacroToContext(MacroFn = {}, SelectedMacro = {}, Definitions: DefinitionBase['typebox'] = {}, R: 1[] = []): any {
  if (matches<typeof SelectedMacro>({})) {
    return {}
  }
  if (matches<15>(R['length'])) {
    return {}
  }
  const out = emptyObject
  for (const key in keyof(SelectedMacro)) {
    out[key] = InnerMacroToContext__v1(MacroFn, SelectedMacro, Definitions, R, key)
  }
  return UnionMacroContext(out[keyof(SelectedMacro)])
}
/* compiles to:
 * export type InnerMacroToContext__v1<MacroFn, SelectedMacro, Definitions, R, key> =
 *   ReturnTypeIfPossible<MacroFn[key], SelectedMacro[key]> extends infer Value
 *     ? & {
 *         resolve: ExtractResolveFromMacro<
 *           Extract<
 *             Exclude<FunctionArrayReturnType<Value['resolve']>, AnyElysiaCustomStatusResponse>,
 *             Record<any, unknown>
 *           >
 *         >
 *       }
 *     & UnwrapMacroSchema<Value, Definitions>
 *     & ExtractAllResponseFromMacro<FunctionArrayReturnTypeNonNullable<Value['beforeHandle']>>
 *     & ExtractAllResponseFromMacro<FunctionArrayReturnTypeNonNullable<Value['afterHandle']>>
 *     & ExtractAllResponseFromMacro<FunctionArrayReturnType<Value['error']>>
 *     & ExtractOnlyResponseFromMacro<FunctionArrayReturnTypeNonNullable<Value['resolve']>>
 *     & InnerMacroToContext<
 *         MacroFn,
 *         Pick<Value, Extract<keyof MacroFn, keyof Value>>,
 *         Definitions,
 *         [...R, 1]
 *       >
 *     : {}
 * export type InnerMacroToContext<
 *   MacroFn extends Macro = {},
 *   SelectedMacro extends BaseMacro = {},
 *   Definitions extends DefinitionBase['typebox'] = {},
 *   R extends 1[] = []
 * > =
 *   {} extends SelectedMacro ? {}
 *   : R['length'] extends 15 ? {}
 *   : UnionMacroContext<
 *     {
 *       [Key in keyof SelectedMacro]: InnerMacroToContext__v1<
 *         MacroFn,
 *         SelectedMacro,
 *         Definitions,
 *         R,
 *         Key
 *       >
 *     }[keyof SelectedMacro]
 *   >
 */

// ✓ UnwrapMacroSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapMacroSchema(T: Partial<InputSchema<any>>, Definitions: DefinitionBase['typebox'] = {}): any {
  return UnwrapRoute({ body: 'body' in T ? T['body'] : Undefined, headers: 'headers' in T ? T['headers'] : Undefined, query: 'query' in T ? T['query'] : Undefined, params: 'params' in T ? T['params'] : Undefined, cookie: 'cookie' in T ? T['cookie'] : Undefined, response: 'response' in T ? T['response'] : Undefined }, Definitions)
}
/* compiles to:
 * export type UnwrapMacroSchema<
 *   T extends Partial<InputSchema<any>>,
 *   Definitions extends DefinitionBase['typebox'] = {}
 * > =
 *   UnwrapRoute<
 *     {
 *       body: 'body' extends keyof T ? T['body'] : undefined
 *       headers: 'headers' extends keyof T ? T['headers'] : undefined
 *       query: 'query' extends keyof T ? T['query'] : undefined
 *       params: 'params' extends keyof T ? T['params'] : undefined
 *       cookie: 'cookie' extends keyof T ? T['cookie'] : undefined
 *       response: 'response' extends keyof T ? T['response'] : undefined
 *     },
 *     Definitions
 *   >
 */

// ✓ SimplifyToSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyToSchema(T: InputSchema<any>): any {
  if (matches<false>(IsUnknown(T['body']))) {
    return _SimplifyToSchema(T)
  }
  if (matches<false>(IsUnknown(T['headers']))) {
    return _SimplifyToSchema(T)
  }
  if (matches<false>(IsUnknown(T['query']))) {
    return _SimplifyToSchema(T)
  }
  if (matches<false>(IsUnknown(T['params']))) {
    return _SimplifyToSchema(T)
  }
  if (matches<false>(IsUnknown(T['cookie']))) {
    return _SimplifyToSchema(T)
  }
  if (matches<false>(IsUnknown(T['response']))) {
    return _SimplifyToSchema(T)
  }
  return {}
}
/* compiles to:
 * export type SimplifyToSchema<T extends InputSchema<any>> =
 *   IsUnknown<T['body']> extends false ? _SimplifyToSchema<T>
 *   : IsUnknown<T['headers']> extends false ? _SimplifyToSchema<T>
 *   : IsUnknown<T['query']> extends false ? _SimplifyToSchema<T>
 *   : IsUnknown<T['params']> extends false ? _SimplifyToSchema<T>
 *   : IsUnknown<T['cookie']> extends false ? _SimplifyToSchema<T>
 *   : IsUnknown<T['response']> extends false ? _SimplifyToSchema<T>
 *   : {}
 */

// ✓ _SimplifyToSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _SimplifyToSchema(T: InputSchema<any>): any {
  return Omit({ body: T['body'], headers: T['headers'], query: T['query'], params: T['params'], cookie: T['cookie'], response: T['response'] }, anyOf('body' in T ? never : 'body', 'headers' in T ? never : 'headers', 'query' in T ? never : 'query', 'params' in T ? never : 'params', 'cookie' in T ? never : 'cookie', 'response' in T ? never : 'response'))
}
/* compiles to:
 * export type _SimplifyToSchema<T extends InputSchema<any>> = Omit<
 *   {
 *     body: T['body']
 *     headers: T['headers']
 *     query: T['query']
 *     params: T['params']
 *     cookie: T['cookie']
 *     response: T['response']
 *   },
 *   | ('body' extends keyof T ? never : 'body')
 *   | ('headers' extends keyof T ? never : 'headers')
 *   | ('query' extends keyof T ? never : 'query')
 *   | ('params' extends keyof T ? never : 'params')
 *   | ('cookie' extends keyof T ? never : 'cookie')
 *   | ('response' extends keyof T ? never : 'response')
 * >
 */

// ✗ InlineHandlerResponse: does not compile yet
//   Type 'Status' does not satisfy the constraint 'number | "Continue" | "Switching Protocols" | "Processing" | "Early Hints" | "OK" | "Created" | "Accepted" | "Non
/* @scripttype preserveParamNames */
/**
 * @param {RouteSchema['response']} Route
 */
export function InlineHandlerResponse(Route): any {
  const out = emptyObject
  for (const Status in keyof(Route)) {
    out[Status] = ElysiaCustomStatusResponse(Status, Route[Status], Status)
  }
  return out[keyof(Route)]
}
/* compiles to:
 * export type InlineHandlerResponse<Route extends RouteSchema['response']> =
 *   { [Status in keyof Route]: ElysiaCustomStatusResponse<Status, Route[Status], Status> }[keyof Route]
 */

// ✓ LastOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LastOf(T): any {
  const m1 = matches<() => Hole<"R">>(UnionToIntersect(matches<any>(T) ? fnType([], T) : never))
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type LastOf<T> =
 *   UnionToIntersect<T extends any ? () => T : never> extends () => infer R ? R : never
 */

// ✓ Push: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Push(T: any[], V): any {
  return [...T, V]
}
/* compiles to:
 * export type Push<T extends any[], V> = [...T, V]
 */

// ✓ TuplifyUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TuplifyUnion(T, L = LastOf(T), N = matches<[ never ]>([T]) ? true : false): any {
  if (matches<typeof N>(true)) {
    return []
  }
  return Push(TuplifyUnion(Exclude(T, L)), L)
}
/* compiles to:
 * export type TuplifyUnion<T, L = LastOf<T>, N = [T] extends [never] ? true : false> =
 *   true extends N ? [] : Push<TuplifyUnion<Exclude<T, L>>, L>
 */

// ✓ Tuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tuple(T): any {
  let A: any[] = []
  while (true) {
    if (matches<(typeof A)['length']>(TuplifyUnion(T)['length'])) {
      break
    }
    A.unshift(T)
  }
  return [...A]
}
/* compiles to:
 * export type Tuple<T> = Tuple__loop<[], T>
 * type Tuple__loop<A extends any[], T> =
 *   TuplifyUnion<T>['length'] extends A['length'] ? [...A] : Tuple__loop<[T, ...A], T>
 */

// ✓ InlineHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InlineHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, MacroContext: { response: PossibleResponse; return: PossibleResponse; resolve: Record<string, unknown>; } = { response: {}, return: {}, resolve: {} }): any {
  return MaybePromise(InlineResponse) | fnType([Context(merge(Route, MacroContext), merge(Singleton, { resolve: MacroContext['resolve'] }))], MaybePromise(Response) | MaybePromise(matches<(typeof Route)['response']>({}) ? unknown : (anyOf(matches<{ 200: any; }>(Route['response']) ? (anyOf(Route['response'][200], ElysiaCustomStatusResponse(200, Route['response'][200], 200), t<Generator<(typeof Route)['response'][200]>>(), t<AsyncGenerator<(typeof Route)['response'][200]>>())) : unknown, Route['response'][keyof(Route['response'])], InlineHandlerResponse(merge(Route['response'], MacroContext['response']))))))
}
/* compiles to:
 * export type InlineHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   MacroContext extends { response: PossibleResponse; return: PossibleResponse; resolve: Record<string, unknown>; } = { response: {}; return: {}; resolve: {} }
 * > =
 *   | MaybePromise<InlineResponse>
 *   | (
 *       (a0: Context<Route & MacroContext, Singleton & { resolve: MacroContext['resolve'] }>) => | MaybePromise<Response>
 *       | MaybePromise<
 *           {} extends Route['response']
 *             ? unknown
 *             : | (
 *                 Route['response'] extends { 200: any; }
 *                   ? | Route['response'][200]
 *                   | ElysiaCustomStatusResponse<200, Route['response'][200], 200>
 *                   | Generator<Route['response'][200]>
 *                   | AsyncGenerator<Route['response'][200]>
 *                   : unknown
 *               )
 *             | Route['response'][keyof Route['response']]
 *             | InlineHandlerResponse<Route['response'] & MacroContext['response']>
 *         >
 *     )
 */

// ✓ InlineHandlerNonMacro: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InlineHandlerNonMacro(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }): any {
  return MaybePromise(InlineResponse) | fnType([Context(Route, Singleton)], MaybePromise(Response) | MaybePromise(matches<(typeof Route)['response']>({}) ? unknown : (anyOf(matches<{ 200: any; }>(Route['response']) ? (anyOf(Route['response'][200], ElysiaCustomStatusResponse(200, Route['response'][200], 200), t<Generator<(typeof Route)['response'][200]>>(), t<AsyncGenerator<(typeof Route)['response'][200]>>())) : unknown, Route['response'][keyof(Route['response'])], InlineHandlerResponse(Route['response'])))))
}
/* compiles to:
 * export type InlineHandlerNonMacro<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} }
 * > =
 *   | MaybePromise<InlineResponse>
 *   | (
 *       (a0: Context<Route, Singleton>) => | MaybePromise<Response>
 *       | MaybePromise<
 *           {} extends Route['response']
 *             ? unknown
 *             : | (
 *                 Route['response'] extends { 200: any; }
 *                   ? | Route['response'][200]
 *                   | ElysiaCustomStatusResponse<200, Route['response'][200], 200>
 *                   | Generator<Route['response'][200]>
 *                   | AsyncGenerator<Route['response'][200]>
 *                   : unknown
 *               )
 *             | Route['response'][keyof Route['response']]
 *             | InlineHandlerResponse<Route['response']>
 *         >
 *     )
 */

// ✓ OptionalHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionalHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([Context(Route, Singleton, Path)], MaybePromise(matches<(typeof Route)['response']>({}) ? unknown : (anyOf(Route['response'][keyof(Route['response'])], InlineHandlerResponse(Route['response']), voidType()))))
}
/* compiles to:
 * export type OptionalHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (a0: Context<Route, Singleton, Path>) => MaybePromise<
 *     {} extends Route['response']
 *       ? unknown
 *       : | Route['response'][keyof Route['response']]
 *       | InlineHandlerResponse<Route['response']>
 *       | void
 *   >
 */

// ✓ AfterHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AfterHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([merge(Context(Route, Singleton, Path), { responseValue: matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])], response: matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])] })], MaybePromise(matches<(typeof Route)['response']>({}) ? unknown : (anyOf(Route['response'][keyof(Route['response'])], InlineHandlerResponse(Route['response']), voidType()))))
}
/* compiles to:
 * export type AfterHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (
 *     a0: Context<Route, Singleton, Path> & { responseValue: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']]; response: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']] }
 *   ) => MaybePromise<
 *     {} extends Route['response']
 *       ? unknown
 *       : | Route['response'][keyof Route['response']]
 *       | InlineHandlerResponse<Route['response']>
 *       | void
 *   >
 */

// ✓ MapResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapResponse(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([merge(Context(Route, Singleton, Path), { responseValue: matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])], response: matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])] })], MaybePromise(anyOf(Response, voidType())))
}
/* compiles to:
 * export type MapResponse<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (
 *     a0: Context<Route, Singleton, Path> & { responseValue: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']]; response: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']] }
 *   ) => MaybePromise<Response | void>
 */

// ✓ VoidHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function VoidHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }): any {
  return fnType([Context(Route, Singleton)], MaybePromise(voidType()))
}
/* compiles to:
 * export type VoidHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} }
 * > =
 *   (a0: Context<Route, Singleton>) => MaybePromise<void>
 */

// ✓ TransformHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransformHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([Context(Route, merge(Omit(Singleton, 'resolve'), { resolve: {} }), Path)], MaybePromise(voidType()))
}
/* compiles to:
 * export type TransformHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (a0: Context<Route, Omit<Singleton, 'resolve'> & { resolve: {} }, Path>) => MaybePromise<void>
 */

// ✓ BodyHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BodyHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Path: string | undefined = Undefined): any {
  return fnType([Context(Route, merge(Singleton, { decorator: { contentType: string } }), Path), string], MaybePromise(any))
}
/* compiles to:
 * export type BodyHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Path extends string | undefined = undefined
 * > =
 *   (a0: Context<Route, Singleton & { decorator: { contentType: string } }, Path>, a1: string) => MaybePromise<
 *     any
 *   >
 */

// ✓ PreHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PreHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }): any {
  return fnType([PreContext(Singleton)], MaybePromise(anyOf(Route['response'], InlineHandlerResponse(Route['response']), voidType())))
}
/* compiles to:
 * export type PreHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} }
 * > =
 *   (a0: PreContext<Singleton>) => MaybePromise<
 *     Route['response'] | InlineHandlerResponse<Route['response']> | void
 *   >
 */

// ✓ AfterResponseHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AfterResponseHandler(Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }): any {
  return fnType([merge(Context(Route, Singleton), { responseValue: matches<(typeof Route)['response']>({}) ? unknown : Route['response'][keyof(Route['response'])], response: matches<(typeof Route)['response']>({}) ? unknown : (anyOf(Route['response'][keyof(Route['response'])], InlineHandlerResponse(Route['response']))) })], MaybePromise(unknown))
}
/* compiles to:
 * export type AfterResponseHandler<
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} }
 * > =
 *   (
 *     a0: Context<Route, Singleton> & { responseValue: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']]; response: {} extends Route['response'] ? unknown : Route['response'][keyof Route['response']] | InlineHandlerResponse<Route['response']> }
 *   ) => MaybePromise<unknown>
 */

// ✓ GracefulHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GracefulHandler(Instance: AnyElysia): any {
  return fnType([Instance], any)
}
/* compiles to:
 * export type GracefulHandler<Instance extends AnyElysia> = (a0: Instance) => any
 */

// ✓ ErrorHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<string, Error>} T
 */
export function ErrorHandler(T = {}, Route: RouteSchema = {}, Singleton: SingletonBase = { decorator: {}, store: {}, derive: {}, resolve: {} }, Ephemeral: EphemeralType = { derive: {}, resolve: {}, schema: {}, standaloneSchema: {}, response: {} }, Volatile: EphemeralType = { derive: {}, resolve: {}, schema: {}, standaloneSchema: {}, response: {} }): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = { request: Request, code: K, error: Readonly(T[K]), set: Context['set'] }
  }
  return fnType([merge(ErrorContext(Route, { store: Singleton['store'], decorator: Singleton['decorator'], derive: {}, resolve: {} }), Prettify(merge({ request: Request, code: 'UNKNOWN', error: Readonly(t<Error>()), set: Context['set'] }, Partial(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'VALIDATION', error: Readonly(ValidationError), set: Context['set'] }, Singleton['derive'], Ephemeral['derive'], Volatile['derive'], NeverKey(merge(Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'NOT_FOUND', error: Readonly(NotFoundError), set: Context['set'] }, NeverKey(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'PARSE', error: Readonly(ParseError), set: Context['set'] }, NeverKey(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'INTERNAL_SERVER_ERROR', error: Readonly(InternalServerError), set: Context['set'] }, Partial(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'INVALID_COOKIE_SIGNATURE', error: Readonly(InvalidCookieSignature), set: Context['set'] }, NeverKey(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: 'INVALID_FILE_TYPE', error: Readonly(InvalidFileType), set: Context['set'] }, Singleton['derive'], Ephemeral['derive'], Volatile['derive'], NeverKey(merge(Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge({ request: Request, code: number, error: Readonly(ElysiaCustomStatusResponse(number)), set: Context['set'] }, Partial(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))) | Prettify(merge(out[keyof(T)], Partial(merge(Singleton['derive'], Ephemeral['derive'], Volatile['derive'], Singleton['resolve'], Ephemeral['resolve'], Volatile['resolve'])))))], any | t<Promise<any>>())
}
/* compiles to:
 * export type ErrorHandler<
 *   T extends Record<string, Error> = {},
 *   Route extends RouteSchema = {},
 *   Singleton extends SingletonBase = { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   Ephemeral extends EphemeralType = { derive: {}; resolve: {}; schema: {}; standaloneSchema: {}; response: {} },
 *   Volatile extends EphemeralType = { derive: {}; resolve: {}; schema: {}; standaloneSchema: {}; response: {} }
 * > =
 *   (
 *     a0: ErrorContext<Route, { store: Singleton['store']; decorator: Singleton['decorator']; derive: {}; resolve: {} }> & (Prettify<{ request: Request; code: 'UNKNOWN'; error: Readonly<Error>; set: Context['set'] } & Partial<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'VALIDATION'; error: Readonly<ValidationError>; set: Context['set'] } & Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & NeverKey<Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'NOT_FOUND'; error: Readonly<NotFoundError>; set: Context['set'] } & NeverKey<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'PARSE'; error: Readonly<ParseError>; set: Context['set'] } & NeverKey<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'INTERNAL_SERVER_ERROR'; error: Readonly<InternalServerError>; set: Context['set'] } & Partial<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'INVALID_COOKIE_SIGNATURE'; error: Readonly<InvalidCookieSignature>; set: Context['set'] } & NeverKey<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: 'INVALID_FILE_TYPE'; error: Readonly<InvalidFileType>; set: Context['set'] } & Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & NeverKey<Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ request: Request; code: number; error: Readonly<ElysiaCustomStatusResponse<number>>; set: Context['set'] } & Partial<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>> | Prettify<{ [K in keyof T]: { request: Request; code: K; error: Readonly<T[K]>; set: Context['set'] } }[keyof T] & Partial<Singleton['derive'] & Ephemeral['derive'] & Volatile['derive'] & Singleton['resolve'] & Ephemeral['resolve'] & Volatile['resolve']>>)
 *   ) => any | Promise<any>
 */

// ✓ ResolveHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveHandler(Route: RouteSchema, Singleton: SingletonBase, Derivative: Record<string, unknown> | AnyElysiaCustomStatusResponse | void = Record(string, unknown) | AnyElysiaCustomStatusResponse | voidType()): any {
  return fnType([Context(Route, Singleton)], MaybePromise(Derivative))
}
/* compiles to:
 * export type ResolveHandler<
 *   Route extends RouteSchema,
 *   Singleton extends SingletonBase,
 *   Derivative extends Record<string, unknown> | AnyElysiaCustomStatusResponse | void = Record<string, unknown> | AnyElysiaCustomStatusResponse | void
 * > =
 *   (a0: Context<Route, Singleton>) => MaybePromise<Derivative>
 */

// ✗ ResolveReturnType: does not compile yet
//   Type 'T' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function ResolveReturnType(T: MaybeArray<unknown>): any {
  if (matches<typeof T>(arrayOf(any))) {
    return {}
  }
  if (Array.isArray(T)) {
    return _ResolveReturnTypeArray(T)
  }
  const m1 = matches<Hole<"Value", Record<any, unknown>>>(Exclude(Awaited(ReturnType(T)), AnyElysiaCustomStatusResponse))
  if (m1) {
    return m1.Value
  }
  return {}
}
/* compiles to:
 * export type ResolveReturnType<T extends MaybeArray<unknown>> =
 *   any[] extends T ? {}
 *   : T extends any[] ? _ResolveReturnTypeArray<T>
 *   : Exclude<Awaited<ReturnType<T>>, AnyElysiaCustomStatusResponse> extends (infer Value extends Record<any, unknown>)
 *     ? Value
 *   : {}
 */

// ✗ _ResolveReturnTypeArray: does not compile yet
//   Type 'Fn' does not satisfy the constraint '(...args: any) => any'.
/* @scripttype preserveParamNames */
export function _ResolveReturnTypeArray(T, Carry = {}): any {
  const m1 = matches<[ Hole<"Fn">, ...Hole<"Rest"> ]>(T)
  if (m1) {
    const m2 = matches<Hole<"Value", Record<any, unknown>>>(Exclude(Awaited(ReturnType(m1.Fn)), AnyElysiaCustomStatusResponse))
    if (m2) {
      return _ResolveReturnTypeArray(m1.Rest, merge(m2.Value, Carry))
    }
    return _ResolveReturnTypeArray(m1.Rest, merge(Carry, {}))
  }
  return Prettify(Carry)
}
/* compiles to:
 * export type _ResolveReturnTypeArray<T, Carry = {}> =
 *   T extends [infer Fn, ...infer Rest]
 *     ? Exclude<Awaited<ReturnType<Fn>>, AnyElysiaCustomStatusResponse> extends (infer Value extends Record<any, unknown>)
 *       ? _ResolveReturnTypeArray<Rest, Value & Carry>
 *       : _ResolveReturnTypeArray<Rest, Carry & {}>
 *     : Prettify<Carry>
 */

// ✓ CreateDecorator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateDecorator(Singleton: SingletonBase, Ephemeral: EphemeralType, Volatile: EphemeralType): any {
  if (matches<typeof Ephemeral>({})) {
    if (matches<typeof Volatile>({})) {
      return Singleton
    }
    return merge(Singleton, Volatile)
  }
  if (matches<typeof Volatile>({})) {
    return merge(Singleton, Ephemeral)
  }
  return merge(Singleton, Ephemeral, Volatile)
}
/* compiles to:
 * export type CreateDecorator<
 *   Singleton extends SingletonBase,
 *   Ephemeral extends EphemeralType,
 *   Volatile extends EphemeralType
 * > =
 *   {} extends Ephemeral ? {} extends Volatile ? Singleton : Singleton & Volatile
 *   : {} extends Volatile ? Singleton & Ephemeral
 *   : Singleton & Ephemeral & Volatile
 */

// ✗ LocalHook: does not compile yet
//   Property 'tags' does not exist on type 'DocumentDecoration'.
/* @scripttype preserveParamNames */
export function LocalHook(Input: BaseMacro, Schema: RouteSchemaWithResolvedMacro, Singleton: SingletonBase, Errors: { [key in string]: Error; }, Parser: keyof any = ''): any {
  return merge({ detail: optional(DocumentDecoration), parse: optional(MaybeArray(anyOf(BodyHandler(Schema, merge(Singleton, { resolve: Schema['resolve'] })), ContentType, Parser))), transform: optional(MaybeArray(TransformHandler(Schema, merge(Singleton, { resolve: Schema['resolve'] })))), beforeHandle: optional(MaybeArray(OptionalHandler(Schema, merge(Singleton, { resolve: Schema['resolve'] })))), afterHandle: optional(MaybeArray(AfterHandler(Schema, merge(Singleton, { resolve: Schema['resolve'] })))), mapResponse: optional(MaybeArray(MapResponse(Schema, merge(Singleton, { resolve: Schema['resolve'] })))), afterResponse: optional(MaybeArray(AfterResponseHandler(Schema, merge(Singleton, { resolve: Schema['resolve'] })))), error: optional(MaybeArray(ErrorHandler(Errors, Schema, merge(Singleton, { resolve: Schema['resolve'] })))), tags: optional(DocumentDecoration['tags']) }, matches<any>(Input) ? Input : Prettify(Input))
}
/* compiles to:
 * export type LocalHook<
 *   Input extends BaseMacro,
 *   Schema extends RouteSchemaWithResolvedMacro,
 *   Singleton extends SingletonBase,
 *   Errors extends { [key in string]: Error; },
 *   Parser extends keyof any = ''
 * > =
 *   & {
 *       detail?: DocumentDecoration
 *       parse?: MaybeArray<
 *         BodyHandler<Schema, Singleton & { resolve: Schema['resolve'] }> | ContentType | Parser
 *       >
 *       transform?: MaybeArray<
 *         TransformHandler<Schema, Singleton & { resolve: Schema['resolve'] }>
 *       >
 *       beforeHandle?: MaybeArray<
 *         OptionalHandler<Schema, Singleton & { resolve: Schema['resolve'] }>
 *       >
 *       afterHandle?: MaybeArray<AfterHandler<Schema, Singleton & { resolve: Schema['resolve'] }>>
 *       mapResponse?: MaybeArray<MapResponse<Schema, Singleton & { resolve: Schema['resolve'] }>>
 *       afterResponse?: MaybeArray<
 *         AfterResponseHandler<Schema, Singleton & { resolve: Schema['resolve'] }>
 *       >
 *       error?: MaybeArray<
 *         ErrorHandler<Errors, Schema, Singleton & { resolve: Schema['resolve'] }>
 *       >
 *       tags?: DocumentDecoration['tags']
 *     }
 *   & (Input extends any ? Input : Prettify<Input>)
 */

// ✗ GuardLocalHook: does not compile yet
//   Property 'tags' does not exist on type 'DocumentDecoration'.
/* @scripttype preserveParamNames */
export function GuardLocalHook(Input: BaseMacro | undefined, Schema: RouteSchema, Singleton: SingletonBase, Parser: keyof any, BeforeHandle: MaybeArray<OptionalHandler<any, any>>, AfterHandle: MaybeArray<AfterHandler<any, any>>, ErrorHandle: MaybeArray<ErrorHandler<any, any, any>>, GuardType: GuardSchemaType = 'standalone', AsType: LifeCycleType = 'local'): any {
  return merge(matches<any>(Input) ? Input : Prettify(Input), { as: optional(AsType), schema: optional(GuardType), detail: optional(DocumentDecoration), parse: optional(MaybeArray(anyOf(BodyHandler(Schema, Singleton), ContentType, Parser))), transform: optional(MaybeArray(TransformHandler(Schema, Singleton))), beforeHandle: optional(BeforeHandle), afterHandle: optional(AfterHandle), mapResponse: optional(MaybeArray(MapResponse(Schema, Singleton))), afterResponse: optional(MaybeArray(AfterResponseHandler(Schema, Singleton))), error: optional(ErrorHandle), tags: optional(DocumentDecoration['tags']) })
}
/* compiles to:
 * export type GuardLocalHook<
 *   Input extends BaseMacro | undefined,
 *   Schema extends RouteSchema,
 *   Singleton extends SingletonBase,
 *   Parser extends keyof any,
 *   BeforeHandle extends MaybeArray<OptionalHandler<any, any>>,
 *   AfterHandle extends MaybeArray<AfterHandler<any, any>>,
 *   ErrorHandle extends MaybeArray<ErrorHandler<any, any, any>>,
 *   GuardType extends GuardSchemaType = 'standalone',
 *   AsType extends LifeCycleType = 'local'
 * > =
 *   & (Input extends any ? Input : Prettify<Input>)
 *   & {
 *       as?: AsType
 *       schema?: GuardType
 *       detail?: DocumentDecoration
 *       parse?: MaybeArray<BodyHandler<Schema, Singleton> | ContentType | Parser>
 *       transform?: MaybeArray<TransformHandler<Schema, Singleton>>
 *       beforeHandle?: BeforeHandle
 *       afterHandle?: AfterHandle
 *       mapResponse?: MaybeArray<MapResponse<Schema, Singleton>>
 *       afterResponse?: MaybeArray<AfterResponseHandler<Schema, Singleton>>
 *       error?: ErrorHandle
 *       tags?: DocumentDecoration['tags']
 *     }
 */

// ✓ AddPrefix: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddPrefix(Prefix: string, T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[typeof Prefix === 'string' ? `${Prefix}${merge(K, string)}` : K] = T[K]
  }
  return out
}
/* compiles to:
 * export type AddPrefix<Prefix extends string, T> = {
 *   [K in keyof T as Prefix extends string ? `${Prefix}${K & string}` : K]: T[K]
 * }
 */

// ✓ AddPrefixCapitalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddPrefixCapitalize(Prefix: string, T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[`${Prefix}${Capitalize(merge(K, string))}`] = T[K]
  }
  return out
}
/* compiles to:
 * export type AddPrefixCapitalize<Prefix extends string, T> = {
 *   [K in keyof T as `${Prefix}${Capitalize<K & string>}`]: T[K]
 * }
 */

// ✓ AddSuffix: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddSuffix(Suffix: string, T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[`${merge(K, string)}${Suffix}`] = T[K]
  }
  return out
}
/* compiles to:
 * export type AddSuffix<Suffix extends string, T> = {
 *   [K in keyof T as `${K & string}${Suffix}`]: T[K]
 * }
 */

// ✓ AddSuffixCapitalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddSuffixCapitalize(Suffix: string, T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[`${merge(K, string)}${Capitalize(Suffix)}`] = T[K]
  }
  return out
}
/* compiles to:
 * export type AddSuffixCapitalize<Suffix extends string, T> = {
 *   [K in keyof T as `${K & string}${Capitalize<Suffix>}`]: T[K]
 * }
 */

// ✓ MaybeValueOrVoidFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeValueOrVoidFunction(T): any {
  return T | fnType([...any], voidType() | T)
}
/* compiles to:
 * export type MaybeValueOrVoidFunction<T> = T | ((...a0: any) => void | T)
 */

// ✓ MaybeFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeFunction(T): any {
  return T | fnType([...arrayOf(any)], T)
}
/* compiles to:
 * export type MaybeFunction<T> = T | ((...a0: any[]) => T)
 */

// ✓ MacroToProperty: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MacroToProperty__v1(T, K): any {
  const m1 = matches<(a: Hole<"Params">) => any>(T[K])
  if (m1) {
    return m1.Params
  }
  return boolean
}
/**
 * @param {Macro<any, any, any, any>} T
 */
export function MacroToProperty(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = matches<Function>(T[K]) ? (MacroToProperty__v1(T, K)) : boolean
  }
  return Prettify(out)
}
/* compiles to:
 * export type MacroToProperty__v1<T, K> = T[K] extends (a: infer Params) => any ? Params : boolean
 * export type MacroToProperty<T extends Macro<any, any, any, any>> = Prettify<
 *   { [K in keyof T]: T[K] extends Function ? MacroToProperty__v1<T, K> : boolean }
 * >
 */

// ✓ _CreateEden: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _CreateEden(Path: string, Property: Record<string, unknown> = {}): any {
  const m1 = matches<`${Hole<"Start">}/${Hole<"Rest">}`>(Path)
  if (m1) {
    const out = emptyObject
    for (const x in keySet(m1.Start)) {
      out[x] = _CreateEden(m1.Rest, Property)
    }
    return out
  }
  if (matches<''>(Path)) {
    return Property
  }
  const out2 = emptyObject
  for (const x in keySet(Path)) {
    out2[x] = Property
  }
  return out2
}
/* compiles to:
 * export type _CreateEden<Path extends string, Property extends Record<string, unknown> = {}> =
 *   Path extends `${infer Start}/${infer Rest}` ? { [X in Start]: _CreateEden<Rest, Property> }
 *   : Path extends '' ? Property
 *   : { [X1 in Path]: Property }
 */

// ✓ RemoveStartingSlash: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveStartingSlash(T): any {
  const m1 = matches<`/${Hole<"Rest">}`>(T)
  if (m1) {
    return m1.Rest
  }
  return T
}
/* compiles to:
 * export type RemoveStartingSlash<T> = T extends `/${infer Rest}` ? Rest : T
 */

// ✓ CreateEden: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateEden(Path: string, Property: Record<string, unknown> = {}): any {
  const m1 = matches<`/${Hole<"Rest">}`>(Path)
  if (m1) {
    return _CreateEden(m1.Rest, Property)
  }
  if (matches<'' | '/'>(Path)) {
    return Property
  }
  return _CreateEden(Path, Property)
}
/* compiles to:
 * export type CreateEden<Path extends string, Property extends Record<string, unknown> = {}> =
 *   Path extends `/${infer Rest}` ? _CreateEden<Rest, Property>
 *   : Path extends '' | '/' ? Property
 *   : _CreateEden<Path, Property>
 */

// ✓ Extract200: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Extract200(T): any {
  if (matches<AnyElysiaCustomStatusResponse>(T)) {
    return Exclude(T, AnyElysiaCustomStatusResponse) | Extract(T, ElysiaCustomStatusResponse(200, any, 200))['response']
  }
  return T
}
/* compiles to:
 * export type Extract200<T> =
 *   T extends AnyElysiaCustomStatusResponse
 *     ? | Exclude<T, AnyElysiaCustomStatusResponse>
 *     | Extract<T, ElysiaCustomStatusResponse<200, any, 200>>['response']
 *     : T
 */

// ✓ IsUnknown: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnknown(T): any {
  if (matches<[ typeof T ]>([unknown])) {
    if (matches<true>(IsAny(T))) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsUnknown<T> = [unknown] extends [T] ? IsAny<T> extends true ? false : true : false
 */

// ✗ ValueToResponseSchema: the ScriptType does not itself typecheck as TypeScript
//   ValueToResponseSchema.st.ts(4:42) TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
/* @scripttype preserveParamNames */
export function ValueToResponseSchema(Value): any {
  const m1 = matches<Hole<"R200">>(Extract200(Value))
  return ExtractErrorFromHandle(Value) & (m1 ? (matches<typeof m1.R200>(Undefined) ? {} : (matches<true>(IsNever(m1.R200)) ? {} : { 200: m1.R200 })) : {})
}
/* compiles to:
 * export type ValueToResponseSchema<Value> =
 *   & ExtractErrorFromHandle<Value>
 *   & (
 *       Extract200<Value> extends infer R200
 *         ? undefined extends R200 ? {} : IsNever<R200> extends true ? {} : { '200': R200 }
 *         : {}
 *     )
 */

// ✓ ValueOrFunctionToResponseSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValueOrFunctionToResponseSchema(T): any {
  const m1 = matches<(...a: any) => MaybePromise<Hole<"R">>>(T)
  if (m1) {
    return ValueToResponseSchema(m1.R)
  }
  return ValueToResponseSchema(T)
}
/* compiles to:
 * export type ValueOrFunctionToResponseSchema<T> =
 *   T extends (...a: any) => MaybePromise<infer R>
 *     ? ValueToResponseSchema<R>
 *     : ValueToResponseSchema<T>
 */

// ✓ ElysiaHandlerToResponseSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ElysiaHandlerToResponseSchema(Handle: Function): any {
  const m1 = matches<(...a: any) => MaybePromise<Hole<"R">>>(Handle)
  return Prettify(m1 ? ValueToResponseSchema(Exclude(m1.R, Undefined)) : {})
}
/* compiles to:
 * export type ElysiaHandlerToResponseSchema<Handle extends Function> = Prettify<
 *   Handle extends (...a: any) => MaybePromise<infer R>
 *     ? ValueToResponseSchema<Exclude<R, undefined>>
 *     : {}
 * >
 */

// ✓ ElysiaHandlerToResponseSchemas: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ElysiaHandlerToResponseSchemas(Handle: Function[]): any {
  let Carry = {}
  let handle = Handle
  while (true) {
    const m1 = matches<[ Hole<"Current">, ...Hole<"Rest"> ]>(handle)
    if (!m1) {
      break
    }
    handle = m1.Rest
    Carry = UnionResponseStatus(ElysiaHandlerToResponseSchema(m1.Current), Carry)
  }
  return Prettify(Carry)
}
/* compiles to:
 * export type ElysiaHandlerToResponseSchemas<Handle extends Function[]> = ElysiaHandlerToResponseSchemas__loop<
 *   Handle,
 *   {}
 * >
 * type ElysiaHandlerToResponseSchemas__loop<Handle extends Function[], Carry> =
 *   Handle extends [infer Current, ...infer Rest]
 *     ? ElysiaHandlerToResponseSchemas__loop<
 *       Rest,
 *       UnionResponseStatus<ElysiaHandlerToResponseSchema<Current>, Carry>
 *     >
 *     : Prettify<Carry>
 */

// ✓ ElysiaHandlerToResponseSchemaAmbiguous: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ElysiaHandlerToResponseSchemaAmbiguous(Schemas: MaybeArray<Function>): any {
  if (matches<typeof Schemas>(MaybeArray(fnType([...any], any)))) {
    return {}
  }
  if (matches<Function>(Schemas)) {
    return ElysiaHandlerToResponseSchema(Schemas)
  }
  if (matches<Function[]>(Schemas)) {
    return ElysiaHandlerToResponseSchemas(Schemas)
  }
  return {}
}
/* compiles to:
 * export type ElysiaHandlerToResponseSchemaAmbiguous<Schemas extends MaybeArray<Function>> =
 *   MaybeArray<(...a0: any) => any> extends Schemas ? {}
 *   : Schemas extends Function ? ElysiaHandlerToResponseSchema<Schemas>
 *   : Schemas extends Function[] ? ElysiaHandlerToResponseSchemas<Schemas>
 *   : {}
 */

// ✗ ReconcileStatus: does not compile yet
//   Type 'K' cannot be used to index type 'B'.
/* @scripttype preserveParamNames */
/**
 * @param {Record<number, unknown>} A
 * @param {Record<number, unknown>} B
 */
export function ReconcileStatus(A, B): any {
  const out = emptyObject
  for (const K in keySet(keyof(A) | keyof(B))) {
    out[K] = K in A ? A[K] : B[K]
  }
  return out
}
/* compiles to:
 * export type ReconcileStatus<
 *   A extends Record<number, unknown>,
 *   B extends Record<number, unknown>
 * > =
 *   { [K in keyof A | keyof B]: K extends keyof A ? A[K] : B[K] }
 */

// ✗ ComposeElysiaResponse: does not compile yet
//   Type 'Schema["response"]' does not satisfy the constraint 'Record<number, unknown>'.
/* @scripttype preserveParamNames */
export function ComposeElysiaResponse(Schema: RouteSchema, Handle, Possibility: PossibleResponse): any {
  return ReconcileStatus(Schema['response'], UnionResponseStatus(ValueOrFunctionToResponseSchema(Handle), merge(Possibility, matches<Pick<typeof Schema, keyof EmptyRouteSchema>>(EmptyRouteSchema) ? {} : { 422: { type: 'validation', on: string, summary: optional(string), message: optional(string), found: optional(unknown), property: optional(string), expected: optional(string) } })))
}
/* compiles to:
 * export type ComposeElysiaResponse<
 *   Schema extends RouteSchema,
 *   Handle,
 *   Possibility extends PossibleResponse
 * > =
 *   ReconcileStatus<
 *     Schema['response'],
 *     UnionResponseStatus<
 *       ValueOrFunctionToResponseSchema<Handle>,
 *       & Possibility
 *       & (
 *           EmptyRouteSchema extends Pick<Schema, keyof EmptyRouteSchema>
 *             ? {}
 *             : {
 *               '422': {
 *                 type: 'validation'
 *                 on: string
 *                 summary?: string
 *                 message?: string
 *                 found?: unknown
 *                 property?: string
 *                 expected?: string
 *               }
 *             }
 *         )
 *     >
 *   >
 */

// ✓ ExtractErrorFromHandle: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractErrorFromHandle(Handle): any {
  const out = emptyObject
  for (const ErrorResponse in keySet(Extract(Handle, AnyElysiaCustomStatusResponse))) {
    out[matches<AnyElysiaCustomStatusResponse>(ErrorResponse) ? ErrorResponse['code'] : never] = Prettify(ErrorResponse['response'])
  }
  return out
}
/* compiles to:
 * export type ExtractErrorFromHandle<Handle> = {
 *   [ErrorResponse in Extract<Handle, AnyElysiaCustomStatusResponse> as ErrorResponse extends AnyElysiaCustomStatusResponse ? ErrorResponse['code'] : never]: Prettify<
 *     ErrorResponse['response']
 *   >
 * }
 */

// ✓ MergeElysiaInstances: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeElysiaInstances(): any {
  let Instances: any[] = []
  let Prefix = ''
  let Singleton = { decorator: {}, store: {}, derive: {}, resolve: {} }
  let Definitions = { typebox: {}, error: {} }
  let Metadata = { schema: {}, standaloneSchema: {}, macro: {}, macroFn: {}, parser: {}, response: {} }
  let Ephemeral = { derive: {}, resolve: {}, schema: {}, standaloneSchema: {}, response: {} }
  let Volatile = { derive: {}, resolve: {}, schema: {}, standaloneSchema: {}, response: {} }
  let Routes = {}
  while (true) {
    const m1 = matches<[ Hole<"Current", AnyElysia>, ...Hole<"Rest", AnyElysia[]> ]>(Instances)
    if (!m1) {
      break
    }
    Instances = m1.Rest
    Singleton = merge(Singleton, m1.Current['~Singleton'])
    Definitions = merge(Definitions, m1.Current['~Definitions'])
    Metadata = merge(Metadata, m1.Current['~Metadata'])
    Volatile = merge(Volatile, m1.Current['~Ephemeral'])
    Routes = merge(Routes, matches<``>(Prefix) ? m1.Current['~Routes'] : CreateEden(Prefix, m1.Current['~Routes']))
  }
  return Elysia(Prefix, { decorator: Singleton['decorator'], store: Prettify(Singleton['store']), derive: Singleton['derive'], resolve: Singleton['resolve'] }, Definitions, Metadata, Routes, Ephemeral, Volatile)
}
/* compiles to:
 * export type MergeElysiaInstances = MergeElysiaInstances__loop<
 *   [],
 *   { decorator: {}; store: {}; derive: {}; resolve: {} },
 *   { typebox: {}; error: {} },
 *   { schema: {}; standaloneSchema: {}; macro: {}; macroFn: {}; parser: {}; response: {} },
 *   { derive: {}; resolve: {}; schema: {}; standaloneSchema: {}; response: {} },
 *   {},
 *   ''
 * >
 * type MergeElysiaInstances__loop<
 *   Instances extends any[],
 *   Singleton,
 *   Definitions,
 *   Metadata,
 *   Volatile,
 *   Routes,
 *   Prefix extends string
 * > =
 *   Instances extends [infer Current extends AnyElysia, ...infer Rest extends AnyElysia[]]
 *     ? MergeElysiaInstances__loop<
 *       Rest,
 *       Singleton & Current['~Singleton'],
 *       Definitions & Current['~Definitions'],
 *       Metadata & Current['~Metadata'],
 *       Volatile & Current['~Ephemeral'],
 *       Routes & (Prefix extends `` ? Current['~Routes'] : CreateEden<Prefix, Current['~Routes']>),
 *       Prefix
 *     >
 *     : Elysia<
 *       Prefix,
 *       {
 *         decorator: Singleton['decorator']
 *         store: Prettify<Singleton['store']>
 *         derive: Singleton['derive']
 *         resolve: Singleton['resolve']
 *       },
 *       Definitions,
 *       Metadata,
 *       Routes,
 *       Ephemeral,
 *       Volatile
 *     >
 */

// ✓ PartialIf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PartialIf(T, Condition: boolean): any {
  if (matches<true>(Condition)) {
    return Partial(T)
  }
  return T
}
/* compiles to:
 * export type PartialIf<T, Condition extends boolean> = Condition extends true ? Partial<T> : T
 */

// ✓ ExcludeElysiaResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeElysiaResponse(T): any {
  const m1 = matches<Hole<"A">>(Exclude(Awaited(T), AnyElysiaCustomStatusResponse))
  return PartialIf(m1 ? (matches<true>(IsNever(merge(m1.A, {}))) ? {} : (merge(m1.A, {}))) : {}, matches<Awaited<typeof T>>(Undefined) ? true : false)
}
/* compiles to:
 * export type ExcludeElysiaResponse<T> = PartialIf<
 *   Exclude<Awaited<T>, AnyElysiaCustomStatusResponse> extends infer A
 *     ? IsNever<A & {}> extends true ? {} : A & {}
 *     : {},
 *   undefined extends Awaited<T> ? true : false
 * >
 */

// ✓ InferContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferContext(T: AnyElysia, Path: string = T['~Prefix'], Schema: RouteSchema = T['~Metadata']['schema']): any {
  return Context(MergeSchema(Schema, T['~Metadata']['schema']), merge(T['~Singleton'], { derive: merge(T['~Ephemeral']['derive'], T['~Volatile']['derive']), resolve: merge(T['~Ephemeral']['resolve'], T['~Volatile']['resolve']) }), Path)
}
/* compiles to:
 * export type InferContext<
 *   T extends AnyElysia,
 *   Path extends string = T['~Prefix'],
 *   Schema extends RouteSchema = T['~Metadata']['schema']
 * > =
 *   Context<
 *     MergeSchema<Schema, T['~Metadata']['schema']>,
 *     & T['~Singleton']
 *     & {
 *         derive: T['~Ephemeral']['derive'] & T['~Volatile']['derive']
 *         resolve: T['~Ephemeral']['resolve'] & T['~Volatile']['resolve']
 *       },
 *     Path
 *   >
 */

// ✓ InferHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferHandler(T: AnyElysia, Path: string = T['~Prefix'], Schema: RouteSchema = T['~Metadata']['schema']): any {
  return InlineHandler(MergeSchema(Schema, T['~Metadata']['schema'], Path), merge(T['~Singleton'], { derive: merge(T['~Ephemeral']['derive'], T['~Volatile']['derive']), resolve: merge(T['~Ephemeral']['resolve'], T['~Volatile']['resolve']) }))
}
/* compiles to:
 * export type InferHandler<
 *   T extends AnyElysia,
 *   Path extends string = T['~Prefix'],
 *   Schema extends RouteSchema = T['~Metadata']['schema']
 * > =
 *   InlineHandler<
 *     MergeSchema<Schema, T['~Metadata']['schema'], Path>,
 *     & T['~Singleton']
 *     & {
 *         derive: T['~Ephemeral']['derive'] & T['~Volatile']['derive']
 *         resolve: T['~Ephemeral']['resolve'] & T['~Volatile']['resolve']
 *       }
 *   >
 */

// ✓ UnionToIntersect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersect(U): any {
  const m1 = matches<(arg: Hole<"I">) => 0>(matches<unknown>(U) ? fnType([U], 0) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersect<U> =
 *   (U extends unknown ? (a0: U) => 0 : never) extends (arg: infer I) => 0 ? I : never
 */

// ✓ HigherOrderFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HigherOrderFunction(T: (...arg: unknown[]) => Function = fnType([...arrayOf(unknown)], t<Function>())): any {
  return fnType([T, Request], ReturnType(T))
}
/* compiles to:
 * export type HigherOrderFunction<
 *   T extends (...arg: unknown[]) => Function = (...a0: unknown[]) => Function
 * > =
 *   (a0: T, a1: Request) => ReturnType<T>
 */

// ✓ JoinPath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function JoinPath(A: string, B: string): any {
  if (matches<`/${string}`>(B)) {
    return `${A}${B}`
  }
  return `${A}/${B}`
}
/* compiles to:
 * export type JoinPath<A extends string, B extends string> =
 *   B extends `/${string}` ? `${A}${B}` : `${A}/${B}`
 */

// ✓ UnwrapTypeModule: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapTypeModule(Module: TModule<any, any>): any {
  const m1 = matches<TModule<Hole<"Type", TProperties>, any>>(Module)
  if (m1) {
    return m1.Type
  }
  return {}
}
/* compiles to:
 * export type UnwrapTypeModule<Module extends TModule<any, any>> =
 *   Module extends TModule<infer Type extends TProperties, any> ? Type : {}
 */

// ✓ MergeTypeModule: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeTypeModule(A: TModule<any, any>, B: TModule<any, any>): any {
  return TModule(Prettify(merge(UnwrapTypeModule(A), UnwrapTypeModule(B))))
}
/* compiles to:
 * export type MergeTypeModule<A extends TModule<any, any>, B extends TModule<any, any>> = TModule<
 *   Prettify<UnwrapTypeModule<A> & UnwrapTypeModule<B>>
 * >
 */

// ✓ SSEPayload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SSEPayload(Data: unknown = unknown, Event: string | undefined = anyOf(string, Undefined)): any {
  return { id: optional(string | number | Null), event: optional(Event), retry: optional(number), data: optional(Data) }
}
/* compiles to:
 * export type SSEPayload<Data = unknown, Event extends string | undefined = string | undefined> = {
 *   id?: string | number | null
 *   event?: Event
 *   retry?: number
 *   data?: Data
 * }
 */

// ✓ UnionResponseStatus: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionResponseStatus(A, B): any {
  if (matches<typeof A>({})) {
    return B
  }
  if (matches<typeof B>({})) {
    return A
  }
  const out = emptyObject
  for (const key in keySet(keyof(A) | keyof(B))) {
    out[key] = key in A ? (key in B ? (A[key] | B[key]) : A[key]) : (key in B ? B[key] : never)
  }
  return out
}
/* compiles to:
 * export type UnionResponseStatus<A, B> =
 *   {} extends A ? B
 *   : {} extends B ? A
 *   : {
 *     [Key in keyof A | keyof B]: Key extends keyof A ? Key extends keyof B ? A[Key] | B[Key] : A[Key]
 *     : Key extends keyof B ? B[Key]
 *     : never
 *   }
 */

// ✓ CreateEdenResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateEdenResponse(Path: string, Schema: RouteSchema, MacroContext: RouteSchema, Res: PossibleResponse): any {
  if (matches<typeof MacroContext>(RouteSchema)) {
    return { body: Schema['body'], params: matches<true>(IsNever(keyof(Schema['params']))) ? ResolvePath(Path) : Schema['params'], query: Schema['query'], headers: Schema['headers'], response: Prettify(Res) }
  }
  return { body: Prettify(merge(Schema['body'], MacroContext['body'])), params: matches<true>(IsNever(keyof(merge(Schema['params'], MacroContext['params'])))) ? ResolvePath(Path) : Prettify(merge(Schema['params'], MacroContext['params'])), query: Prettify(merge(Schema['query'], MacroContext['query'])), headers: Prettify(merge(Schema['headers'], MacroContext['headers'])), response: Prettify(Res) }
}
/* compiles to:
 * export type CreateEdenResponse<
 *   Path extends string,
 *   Schema extends RouteSchema,
 *   MacroContext extends RouteSchema,
 *   Res extends PossibleResponse
 * > =
 *   RouteSchema extends MacroContext
 *     ? {
 *       body: Schema['body']
 *       params: IsNever<keyof Schema['params']> extends true
 *         ? ResolvePath<Path>
 *         : Schema['params']
 *       query: Schema['query']
 *       headers: Schema['headers']
 *       response: Prettify<Res>
 *     }
 *     : {
 *       body: Prettify<Schema['body'] & MacroContext['body']>
 *       params: IsNever<keyof (Schema['params'] & MacroContext['params'])> extends true
 *         ? ResolvePath<Path>
 *         : Prettify<Schema['params'] & MacroContext['params']>
 *       query: Prettify<Schema['query'] & MacroContext['query']>
 *       headers: Prettify<Schema['headers'] & MacroContext['headers']>
 *       response: Prettify<Res>
 *     }
 */

// ✓ ModelsToTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<keyof any, AnySchema>} T
 */
export function ModelsToTypes(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = UnwrapSchema(T[K])
  }
  return out
}
/* compiles to:
 * export type ModelsToTypes<T extends Record<keyof any, AnySchema>> = {
 *   [K in keyof T]: UnwrapSchema<T[K]>
 * }
 */

/**
 * ScriptType for 02-inference-at-scale/hono/src/client/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 32 type-level aliases: 29 covered, 2 compile-error, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BuildSearchParamsFn, ClientResponse, TypedURL } from '../../../../../02-inference-at-scale/hono/src/client/types.js'
import type { Endpoint, ExtractSchema, KnownResponseFormat, ResponseFormat, Schema } from '../../../../../02-inference-at-scale/hono/src/types.js'
import type { StatusCode } from '../../../../../02-inference-at-scale/hono/src/utils/http-status.js'
import type { HasRequiredKeys } from '../../../../../02-inference-at-scale/hono/src/utils/types.js'
declare namespace m1 {
  export type B<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type E<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type F<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Host<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type O<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Param<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Port<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Protocol<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Query<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type RC<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type RF<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type RT<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Rest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type S<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type F<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Hostname<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type K<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type O<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Param<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type S<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type Host<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Port<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m4 {
  export type Host<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Param<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Port<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m5 {
  export type Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BuildSearchParamsFn: any
declare const ClientResponse: any
declare const Endpoint: any
declare const ExtractSchema: any
declare const GlobalResponseDefinition: any
declare const HasRequiredKeys: any
declare const Hono: any
declare const HonoBase: any
declare const HonoRequest: any
declare const KnownResponseFormat: any
declare const MethodNameAll: any
declare const Param: any
declare const RequestInit: any
declare const ResponseFormat: any
declare const Schema: any
declare const StandardMethods: any
declare const StatusCode: any
declare const TypedURL: any
declare const URL: any
declare const WebSocket: any
declare const fetch: any
type ClientResponseOfEndpoint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ExpandAllMethod<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GlobalResponseDefinition<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Hono<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type HonoBase<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type HonoRequest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InferEndpointType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MethodNameAll<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Param<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RequestInit<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StandardMethods<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type URL<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type WebSocket<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type fetch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ExpandAllMethod: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExpandAllMethod(S): any {
  if (MethodNameAll in S) {
    const out = emptyObject
    for (const M in keySet(StandardMethods)) {
      out[M] = S[MethodNameAll]
    }
    return out & Omit(S, MethodNameAll)
  }
  return S
}
/* compiles to:
 * export type ExpandAllMethod<S> =
 *   MethodNameAll extends keyof S
 *     ? { [M in StandardMethods]: S[MethodNameAll] } & Omit<S, MethodNameAll>
 *     : S
 */

// ✗ ClientRequestOptions: does not compile yet
//   'fetch' refers to a value, but is being used as a type here. Did you mean 'typeof fetch'?
/* @scripttype preserveParamNames */
export function ClientRequestOptions(T = unknown): any {
  return merge({ fetch: optional(anyOf(t<typeof fetch>(), HonoRequest)), webSocket: optional(fnType([...ConstructorParameters(t<typeof WebSocket>())], WebSocket)), init: optional(RequestInit), buildSearchParams: optional(BuildSearchParamsFn) }, matches<never>(keyof(T)) ? { headers: optional(Record(string, string) | fnType([], Record(string, string) | t<Promise<Record<string, string>>>())) } : { headers: T | fnType([], T | t<Promise<typeof T>>()) })
}
/* compiles to:
 * export type ClientRequestOptions<T = unknown> =
 *   & {
 *       fetch?: fetch | HonoRequest
 *       webSocket?: (...a0: ConstructorParameters<WebSocket>) => WebSocket
 *       init?: RequestInit
 *       buildSearchParams?: BuildSearchParamsFn
 *     }
 *   & (
 *       keyof T extends never
 *         ? {
 *           headers?: | Record<string, string>
 *           | (() => Record<string, string> | Promise<Record<string, string>>)
 *         }
 *         : { headers: T | (() => T | Promise<T>) }
 *     )
 */

// ✗ ClientRequest: does not compile yet
//   Unterminated string literal.
/* @scripttype preserveParamNames */
export function ClientRequest__v1(S, M): any {
  const m1 = matches<Endpoint & { input: Hole<"R">; }>(ExpandAllMethod(S)[M])
  if (m1) {
    if (matches<object>(m1.R)) {
      if (matches<true>(HasRequiredKeys(m1.R))) {
        return fnType([m1.R, optElem(ClientRequestOptions)], t<Promise<ClientResponseOfEndpoint<ExpandAllMethod<typeof S>[typeof M]>>>())
      }
      return fnType([optElem(m1.R), optElem(ClientRequestOptions)], t<Promise<ClientResponseOfEndpoint<ExpandAllMethod<typeof S>[typeof M]>>>())
    }
    return never
  }
  return never
}
export function ClientRequest(Prefix: string, Path: string, S: Schema): any {
  const out = emptyObject
  for (const M in keyof(ExpandAllMethod(S))) {
    out[M] = ClientRequest__v1(S, M)
  }
  const m1 = matches<{ input: Hole<"I">; }>(S['$get'])
  return merge(out, { $url: genericFnType(['const Arg extends | (S[keyof S] extends { input: infer R } ? R extends { param: infer P } ? R extends { query: infer Q } ? { param: P; query: Q } : { param: P } : R extends { query: infer Q } ? { query: Q } : {} : {}) | undefined = undefined'], [optElem(Arg)], HonoURL(Prefix, Path, Arg)), $path: genericFnType(['const Arg extends | (S[keyof S] extends { input: infer R } ? R extends { param: infer P } ? R extends { query: infer Q } ? { param: P; query: Q } : { param: P } : R extends { query: infer Q } ? { query: Q } : {} : {}) | undefined = undefined'], [optElem(Arg)], BuildPath(Path, Arg)) }, matches<{ outputFormat: 'ws'; }>(S['$get']) ? (m1 ? { $ws: fnType([optElem(m1.I)], WebSocket) } : {}) : {})
}
/* compiles to:
 * export type ClientRequest__v1<S, M> =
 *   ExpandAllMethod<S>[M] extends Endpoint & { input: infer R; }
 *     ? R extends object
 *       ? HasRequiredKeys<R> extends true
 *         ? (a0: R, a1?: ClientRequestOptions) => Promise<
 *           ClientResponseOfEndpoint<ExpandAllMethod<S>[M]>
 *         >
 *         : (a0?: R, a1?: ClientRequestOptions) => Promise<
 *           ClientResponseOfEndpoint<ExpandAllMethod<S>[M]>
 *         >
 *       : never
 *     : never
 * export type ClientRequest<Prefix extends string, Path extends string, S extends Schema> =
 *   & { [M in keyof ExpandAllMethod<S>]: ClientRequest__v1<S, M> }
 *   & {
 *       $url: <const Arg extends | (S[keyof S] extends { input: infer R } ? R extends { param: infer P } ? R extends { query: infer Q } ? { param: P; query: Q } : { param: P } : R extends { query: infer Q } ? { query: Q } : {} : {}) | undefined = undefined>(
 *         a0?: Arg
 *       ) => HonoURL<Prefix, Path, Arg>
 *       $path: <const Arg extends | (S[keyof S] extends { input: infer R } ? R extends { param: infer P } ? R extends { query: infer Q } ? { param: P; query: Q } : { param: P } : R extends { query: infer Q } ? { query: Q } : {} : {}) | undefined = undefined>(
 *         a0?: Arg
 *       ) => BuildPath<Path, Arg>
 *     }
 *   & (
 *       S['$get'] extends { outputFormat: 'ws'; }
 *         ? S['$get'] extends { input: infer I; } ? { $ws: (a0?: I) => WebSocket } : {}
 *         : {}
 *     )
 */

// ✓ ClientResponseOfEndpoint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ClientResponseOfEndpoint(T: Endpoint = Endpoint): any {
  const m1 = matches<{ output: Hole<"O">; outputFormat: Hole<"F">; status: Hole<"S">; }>(T)
  if (m1) {
    return ClientResponse(m1.O, typeof m1.S === 'number' ? m1.S : never, matches<ResponseFormat>(m1.F) ? m1.F : never)
  }
  return never
}
/* compiles to:
 * export type ClientResponseOfEndpoint<T extends Endpoint = Endpoint> =
 *   T extends { output: infer O; outputFormat: infer F; status: infer S; }
 *     ? ClientResponse<O, S extends number ? S : never, F extends ResponseFormat ? F : never>
 *     : never
 */

// ✓ BuildSearch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuildSearch(Arg, Key: 'query'): any {
  const m1 = matches<{ [K in typeof Key]: Hole<"Query">; }>(Arg)
  if (m1) {
    if (matches<true>(IsEmptyObject(m1.Query))) {
      return ''
    }
    return `?${string}`
  }
  return ''
}
/* compiles to:
 * export type BuildSearch<Arg, Key extends 'query'> =
 *   Arg extends { [K in Key]: infer Query; }
 *     ? IsEmptyObject<Query> extends true ? '' : `?${string}`
 *     : ''
 */

// ✓ BuildPathname: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuildPathname(P: string, Arg): any {
  const m1 = matches<{ param: Hole<"Param">; }>(Arg)
  if (m1) {
    return `${ApplyParam(TrimStartSlash(P), m1.Param)}`
  }
  return `/${TrimStartSlash(P)}`
}
/* compiles to:
 * export type BuildPathname<P extends string, Arg> =
 *   Arg extends { param: infer Param; }
 *     ? `${ApplyParam<TrimStartSlash<P>, Param>}`
 *     : `/${TrimStartSlash<P>}`
 */

// ✓ BuildPath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuildPath(P: string, Arg): any {
  return `${BuildPathname(P, Arg)}${BuildSearch(Arg, 'query')}`
}
/* compiles to:
 * export type BuildPath<P extends string, Arg> = `${BuildPathname<P, Arg>}${BuildSearch<Arg, 'query'>}`
 */

// ✓ BuildTypedURL: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuildTypedURL(Protocol: string, Host: string, Port: string, P: string, Arg): any {
  return TypedURL(`${Protocol}:`, Host, Port, BuildPathname(P, Arg), BuildSearch(Arg, 'query'))
}
/* compiles to:
 * export type BuildTypedURL<
 *   Protocol extends string,
 *   Host extends string,
 *   Port extends string,
 *   P extends string,
 *   Arg
 * > =
 *   TypedURL<`${Protocol}:`, Host, Port, BuildPathname<P, Arg>, BuildSearch<Arg, 'query'>>
 */

// ✓ HonoURL: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HonoURL(Prefix: string, Path: string, Arg): any {
  if (matches<true>(IsLiteral(Prefix))) {
    const m1 = matches<`${Hole<"Protocol">}://${Hole<"Rest">}`>(TrimEndSlash(Prefix))
    if (m1) {
      const m2 = matches<`${Hole<"Hostname">}/${Hole<"P">}`>(m1.Rest)
      if (m2) {
        const m3 = matches<[ Hole<"Host", string>, Hole<"Port", string> ]>(ParseHostName(m2.Hostname))
        if (m3) {
          return BuildTypedURL(m1.Protocol, m3.Host, m3.Port, m2.P, Arg)
        }
        return never
      }
      const m4 = matches<[ Hole<"Host", string>, Hole<"Port", string> ]>(ParseHostName(m1.Rest))
      if (m4) {
        return BuildTypedURL(m1.Protocol, m4.Host, m4.Port, Path, Arg)
      }
      return never
    }
    return URL
  }
  return URL
}
/* compiles to:
 * export type HonoURL<Prefix extends string, Path extends string, Arg> =
 *   IsLiteral<Prefix> extends true
 *     ? TrimEndSlash<Prefix> extends `${infer Protocol}://${infer Rest}`
 *       ? Rest extends `${infer Hostname}/${infer P}`
 *         ? ParseHostName<Hostname> extends [infer Host extends string, infer Port extends string]
 *           ? BuildTypedURL<Protocol, Host, Port, P, Arg>
 *           : never
 *       : ParseHostName<Rest> extends [infer Host extends string, infer Port extends string]
 *         ? BuildTypedURL<Protocol, Host, Port, Path, Arg>
 *       : never
 *       : URL
 *     : URL
 */

// ✓ ParseHostName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseHostName(T: string): any {
  const m1 = matches<`${Hole<"Host">}:${Hole<"Port">}`>(T)
  if (m1) {
    return [m1.Host, m1.Port]
  }
  return [T, '']
}
/* compiles to:
 * export type ParseHostName<T extends string> =
 *   T extends `${infer Host}:${infer Port}` ? [Host, Port] : [T, '']
 */

// ✓ TrimStartSlash: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrimStartSlash(T: string): any {
  let t = T
  while (true) {
    const m1 = matches<`/${Hole<"R">}`>(t)
    if (!m1) {
      break
    }
    t = m1.R
  }
  return t
}
/* compiles to:
 * export type TrimStartSlash<T extends string> = TrimStartSlash__loop<T>
 * type TrimStartSlash__loop<T extends string> =
 *   T extends `/${infer R}` ? TrimStartSlash__loop<R> : T
 */

// ✓ TrimEndSlash: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrimEndSlash(T: string): any {
  let t = T
  while (true) {
    const m1 = matches<`${Hole<"R">}/`>(t)
    if (!m1) {
      break
    }
    t = m1.R
  }
  return t
}
/* compiles to:
 * export type TrimEndSlash<T extends string> = TrimEndSlash__loop<T>
 * type TrimEndSlash__loop<T extends string> = T extends `${infer R}/` ? TrimEndSlash__loop<R> : T
 */

// ✓ IsLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsLiteral(T: string): any {
  if (matches<[ never ]>([T])) {
    return false
  }
  if (matches<typeof T>(string)) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsLiteral<T extends string> =
 *   [T] extends [never] ? false : string extends T ? false : true
 */

// ✓ ApplyParam: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyParam(Path: string, P, Result: string = ''): any {
  const m1 = matches<`${Hole<"Head">}/${Hole<"Rest">}`>(Path)
  if (m1) {
    const m2 = matches<`:${Hole<"Param">}`>(m1.Head)
    if (m2) {
      const m3 = matches<Record<typeof m2.Param, Hole<"Value", string>>>(P)
      if (m3) {
        if (matches<true>(IsLiteral(m3.Value))) {
          return ApplyParam(m1.Rest, P, `${Result}/${m3.Value & string}`)
        }
        return ApplyParam(m1.Rest, P, `${Result}/${m1.Head}`)
      }
      return ApplyParam(m1.Rest, P, `${Result}/${m1.Head}`)
    }
    return ApplyParam(m1.Rest, P, `${Result}/${m1.Head}`)
  }
  const m4 = matches<`:${Hole<"Param">}`>(Path)
  if (m4) {
    const m5 = matches<Record<typeof m4.Param, Hole<"Value", string>>>(P)
    if (m5) {
      if (matches<true>(IsLiteral(m5.Value))) {
        return `${Result}/${m5.Value & string}`
      }
      return `${Result}/${Path}`
    }
    return `${Result}/${Path}`
  }
  return `${Result}/${Path}`
}
/* compiles to:
 * export type ApplyParam<Path extends string, P, Result extends string = ''> =
 *   Path extends `${infer Head}/${infer Rest}`
 *     ? Head extends `:${infer Param}`
 *       ? P extends Record<Param, infer Value extends string>
 *         ? IsLiteral<Value> extends true
 *           ? ApplyParam<Rest, P, `${Result}/${Value & string}`>
 *           : ApplyParam<Rest, P, `${Result}/${Head}`>
 *         : ApplyParam<Rest, P, `${Result}/${Head}`>
 *       : ApplyParam<Rest, P, `${Result}/${Head}`>
 *   : Path extends `:${infer Param}`
 *     ? P extends Record<Param, infer Value extends string>
 *       ? IsLiteral<Value> extends true ? `${Result}/${Value & string}` : `${Result}/${Path}`
 *       : `${Result}/${Path}`
 *   : `${Result}/${Path}`
 */

// ✓ IsEmptyObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsEmptyObject(T): any {
  if (matches<never>(keyof(T))) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsEmptyObject<T> = keyof T extends never ? true : false
 */

// ✓ Fetch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Fetch(T): any {
  return fnType([optElem(InferRequestType(T)), optElem(ClientRequestOptions)], t<Promise<ClientResponseOfEndpoint<InferEndpointType<typeof T>>>>())
}
/* compiles to:
 * export type Fetch<T> =
 *   (a0?: InferRequestType<T>, a1?: ClientRequestOptions) => Promise<
 *     ClientResponseOfEndpoint<InferEndpointType<T>>
 *   >
 */

// ✓ InferEndpointType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferEndpointType(T): any {
  const m1 = matches<(args: Hole<"R">, options: any | undefined) => Promise<Hole<"U">>>(T)
  if (m1) {
    const m2 = matches<ClientResponse<Hole<"O">, Hole<"S">, Hole<"F">>>(m1.U)
    if (m2) {
      if (matches<Endpoint>({ input: NonNullable(m1.R), output: m2.O, outputFormat: m2.F, status: m2.S })) {
        return { input: NonNullable(m1.R), output: m2.O, outputFormat: m2.F, status: m2.S }
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type InferEndpointType<T> =
 *   T extends (args: infer R, options: any | undefined) => Promise<infer U>
 *     ? U extends ClientResponse<infer O, infer S, infer F>
 *       ? { input: NonNullable<R>; output: O; outputFormat: F; status: S } extends Endpoint
 *         ? { input: NonNullable<R>; output: O; outputFormat: F; status: S }
 *         : never
 *       : never
 *     : never
 */

// ✓ InferResponseType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferResponseType(T, U: StatusCode = StatusCode): any {
  return InferResponseTypeFromEndpoint(InferEndpointType(T), U)
}
/* compiles to:
 * export type InferResponseType<T, U extends StatusCode = StatusCode> = InferResponseTypeFromEndpoint<
 *   InferEndpointType<T>,
 *   U
 * >
 */

// ✓ InferResponseTypeFromEndpoint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferResponseTypeFromEndpoint(T: Endpoint, U: StatusCode): any {
  const m1 = matches<{ output: Hole<"O">; status: Hole<"S">; }>(T)
  if (m1) {
    if (matches<typeof U>(m1.S)) {
      return m1.O
    }
    return never
  }
  return never
}
/* compiles to:
 * export type InferResponseTypeFromEndpoint<T extends Endpoint, U extends StatusCode> =
 *   T extends { output: infer O; status: infer S; } ? S extends U ? O : never : never
 */

// ✓ InferRequestType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferRequestType(T): any {
  const m1 = matches<(args: Hole<"R">, options: any | undefined) => Promise<ClientResponse<unknown>>>(T)
  if (m1) {
    return NonNullable(m1.R)
  }
  return never
}
/* compiles to:
 * export type InferRequestType<T> =
 *   T extends (args: infer R, options: any | undefined) => Promise<ClientResponse<unknown>>
 *     ? NonNullable<R>
 *     : never
 */

// ✓ InferRequestOptionsType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferRequestOptionsType(T): any {
  const m1 = matches<(args: any, options: Hole<"R">) => Promise<ClientResponse<unknown>>>(T)
  if (m1) {
    return NonNullable(m1.R)
  }
  return never
}
/* compiles to:
 * export type InferRequestOptionsType<T> =
 *   T extends (args: any, options: infer R) => Promise<ClientResponse<unknown>>
 *     ? NonNullable<R>
 *     : never
 */

// ✓ FilterClientResponseByStatusCode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FilterClientResponseByStatusCode(T: ClientResponse<any, any, any>, U: number = StatusCode): any {
  const m1 = matches<ClientResponse<Hole<"RT">, Hole<"RC">, Hole<"RF">>>(T)
  if (m1) {
    if (matches<typeof U>(m1.RC)) {
      return ClientResponse(m1.RT, m1.RC, m1.RF)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type FilterClientResponseByStatusCode<
 *   T extends ClientResponse<any, any, any>,
 *   U extends number = StatusCode
 * > =
 *   T extends ClientResponse<infer RT, infer RC, infer RF>
 *     ? RC extends U ? ClientResponse<RT, RC, RF> : never
 *     : never
 */

// ✗ PathToChain: the ScriptType does not itself typecheck as TypeScript
//   PathToChain.st.ts(16:43) TS2554: Expected 3 arguments, but got 4.
/* @scripttype preserveParamNames */
export function PathToChain(Prefix: string, Path: string, E: Schema): any {
  let Original = Path
  let path = Path
  while (true) {
    const m1 = matches<`/${Hole<"P">}`>(path)
    if (!m1) {
      break
    }
    path = m1.P
    Original = path
  }
  const m2 = matches<`${Hole<"P">}/${Hole<"R">}`>(path)
  const out = emptyObject
  for (const K in keySet(m2.P)) {
    out[K] = PathToChain(Prefix, m2.R, E, Original)
  }
  const out2 = emptyObject
  for (const K in keySet(matches<''>(path) ? 'index' : path)) {
    out2[K] = ClientRequest(Prefix, Original, matches<Record<string, unknown>>(E) ? E[Original] : never)
  }
  return m2 ? out : out2
}
/* compiles to:
 * export type PathToChain<Prefix extends string, Path extends string, E extends Schema> = PathToChain__loop<
 *   Path,
 *   Path
 * >
 * type PathToChain__loop<Path extends string, Original extends string> =
 *   Path extends `/${infer P}` ? PathToChain__loop<P, P>
 *   : Path extends `${infer P}/${infer R}` ? { [K in P]: PathToChain<Prefix, R, E, Original> }
 *   : {
 *     [K1 in Path extends '' ? 'index' : Path]: ClientRequest<
 *       Prefix,
 *       Original,
 *       E extends Record<string, unknown> ? E[Original] : never
 *     >
 *   }
 */

// ✓ Client: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Client(T, Prefix: string): any {
  const m1 = matches<HonoBase<any, Hole<"S">, any>>(T)
  if (m1) {
    const m2 = matches<Record<Hole<"K">, Schema>>(m1.S)
    if (m2) {
      if (typeof m2.K === 'string') {
        return PathToChain(Prefix, m2.K, m1.S)
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type Client<T, Prefix extends string> =
 *   T extends HonoBase<any, infer S, any>
 *     ? S extends Record<infer K, Schema>
 *       ? K extends string ? PathToChain<Prefix, K, S> : never
 *       : never
 *     : never
 */

// ✓ ObjectType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ObjectType(T = unknown): any {
  return indexRecord(string, T)
}
/* compiles to:
 * export type ObjectType<T = unknown> = { [key: string]: T }
 */

// ✓ ToEndpoints: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToEndpoints__v1(Def, R, S): any {
  const out = emptyObject
  for (const F in keySet(keyof(Def[S]) & KnownResponseFormat)) {
    out[F] = merge(Omit(R, anyOf('output', 'status', 'outputFormat')), { output: Def[S][F], status: S, outputFormat: F })
  }
  return out
}
/**
 * @param {GlobalResponseDefinition} Def
 */
export function ToEndpoints(Def, R): any {
  const out = emptyObject
  for (const S in keySet(keyof(Def) & StatusCode)) {
    out[S] = ToEndpoints__v1(Def, R, S)[keyof(Def[S]) & KnownResponseFormat]
  }
  return out[keyof(Def) & StatusCode]
}
/* compiles to:
 * export type ToEndpoints__v1<Def, R, S> = {
 *   [F in keyof Def[S] & KnownResponseFormat]: & Omit<R, 'output' | 'status' | 'outputFormat'>
 *   & { output: Def[S][F]; status: S; outputFormat: F }
 * }
 * export type ToEndpoints<Def extends GlobalResponseDefinition, R> =
 *   {
 *     [S in keyof Def & StatusCode]: ToEndpoints__v1<Def, R, S>[keyof Def[S] & KnownResponseFormat]
 *   }[keyof Def & StatusCode]
 */

// ✓ ModRoute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ModRoute(R, Def: GlobalResponseDefinition): any {
  if (matches<Endpoint>(R)) {
    return R | ToEndpoints(Def, R)
  }
  return R
}
/* compiles to:
 * export type ModRoute<R, Def extends GlobalResponseDefinition> =
 *   R extends Endpoint ? R | ToEndpoints<Def, R> : R
 */

// ✓ ModSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ModSchema__v1(D, Def, K): any {
  const out = emptyObject
  for (const M in keyof(D[K])) {
    out[M] = ModRoute(D[K][M], Def)
  }
  return out
}
export function ModSchema(D, Def: GlobalResponseDefinition): any {
  const out = emptyObject
  for (const K in keyof(D)) {
    out[K] = ModSchema__v1(D, Def, K)
  }
  return out
}
/* compiles to:
 * export type ModSchema__v1<D, Def, K> = { [M in keyof D[K]]: ModRoute<D[K][M], Def> }
 * export type ModSchema<D, Def extends GlobalResponseDefinition> = {
 *   [K in keyof D]: ModSchema__v1<D, Def, K>
 * }
 */

// ✓ ApplyGlobalResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyGlobalResponse(App, Def: GlobalResponseDefinition): any {
  const m1 = matches<HonoBase<Hole<"E">, Hole<"_", Schema>, Hole<"B">>>(App)
  if (m1) {
    const m2 = matches<Hole<"S", Schema>>(ModSchema(ExtractSchema(App), Def))
    if (m2) {
      return Hono(m1.E, m2.S, m1.B)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ApplyGlobalResponse<App, Def extends GlobalResponseDefinition> =
 *   App extends HonoBase<infer E, Schema, infer B>
 *     ? ModSchema<ExtractSchema<App>, Def> extends (infer S extends Schema)
 *       ? Hono<E, S, B>
 *       : never
 *     : never
 */

// ✓ PickRoute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PickRoute(R, U: StatusCode): any {
  if (matches<Endpoint>(R)) {
    if (matches<{ status: typeof U; }>(R)) {
      return R
    }
    return never
  }
  return R
}
/* compiles to:
 * export type PickRoute<R, U extends StatusCode> =
 *   R extends Endpoint ? R extends { status: U; } ? R : never : R
 */

// ✓ PickSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PickSchema__v1(D, U, K): any {
  const out = emptyObject
  for (const M in keyof(D[K])) {
    out[M] = PickRoute(D[K][M], U)
  }
  return out
}
export function PickSchema(D, U: StatusCode): any {
  const out = emptyObject
  for (const K in keyof(D)) {
    out[K] = PickSchema__v1(D, U, K)
  }
  return out
}
/* compiles to:
 * export type PickSchema__v1<D, U, K> = { [M in keyof D[K]]: PickRoute<D[K][M], U> }
 * export type PickSchema<D, U extends StatusCode> = { [K in keyof D]: PickSchema__v1<D, U, K> }
 */

// ✓ PickResponseByStatusCode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PickResponseByStatusCode(App, U: StatusCode): any {
  const m1 = matches<HonoBase<Hole<"E">, Hole<"_", Schema>, Hole<"B">>>(App)
  if (m1) {
    const m2 = matches<Hole<"S", Schema>>(PickSchema(ExtractSchema(App), U))
    if (m2) {
      return Hono(m1.E, m2.S, m1.B)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type PickResponseByStatusCode<App, U extends StatusCode> =
 *   App extends HonoBase<infer E, Schema, infer B>
 *     ? PickSchema<ExtractSchema<App>, U> extends (infer S extends Schema) ? Hono<E, S, B> : never
 *     : never
 */

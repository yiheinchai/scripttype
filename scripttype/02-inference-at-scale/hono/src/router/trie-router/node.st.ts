/**
 * ScriptType for 02-inference-at-scale/hono/src/router/trie-router/node.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ HandlerSet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerSet(T) {
  return { handler: T, possibleKeys: arrayOf(string), score: number }
}
/* compiles to:
 * export type HandlerSet<T> = { handler: T; possibleKeys: string[]; score: number }
 */

// ✓ HandlerParamsSet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerParamsSet(T) {
  return merge(HandlerSet(T), { params: Record(string, string) })
}
/* compiles to:
 * export type HandlerParamsSet<T> = HandlerSet<T> & { params: Record<string, string> }
 */

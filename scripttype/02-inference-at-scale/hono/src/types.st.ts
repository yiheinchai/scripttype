/**
 * ScriptType for 02-inference-at-scale/hono/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 32 type-level aliases: 22 covered, 5 typecheck-error, 5 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BlankInput: any
declare const Context: any
declare const CustomHeader: any
declare const Endpoint: any
declare const Env: any
declare const FormValue: any
declare const HTTPResponseError: any
declare const HonoBase: any
declare const IfAnyThenEmptyObject: any
declare const Input: any
declare const IsAny: any
declare const JSONValue: any
declare const Next: any
declare const NotFoundResponse: any
declare const ParsedFormValue: any
declare const RemoveBlankRecord: any
declare const RequestHeader: any
declare const Response: any
declare const ResponseFormat: any
declare const Schema: any
declare const StatusCode: any
declare const UnionToIntersection: any
type BlankInput<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Context<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type CustomHeader<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Endpoint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Env<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type FormValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HTTPResponseError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HandlerResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HonoBase<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IfAnyThenEmptyObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Input<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsAny<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type JSONValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Next<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NotFoundResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParsedFormValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RemoveBlankRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RequestHeader<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Response<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResponseFormat<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StatusCode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypedResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnionToIntersection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValidationTargets<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ExtractInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractInput(I: Input | Input['in']) {
  if (matches<Input>(I)) {
    if (matches<(typeof I)['in']>(unknown)) {
      return {}
    }
    return I['in']
  }
  return I
}
/* compiles to:
 * export type ExtractInput<I extends Input | Input['in']> = I extends Input ? unknown extends I['in'] ? {} : I['in'] : I
 */

// ✗ HandlerResponse: the ScriptType does not itself typecheck as TypeScript
//   HandlerResponse.st.ts(3:40) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function HandlerResponse(O) {
  return Response | TypedResponse(O) | Promise(Response | TypedResponse(O)) | Promise(voidType())
}
/* compiles to:
 * export type HandlerResponse<O> = Response | TypedResponse<O> | Promise<Response | TypedResponse<O>> | Promise<void>
 */

// ✓ Handler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Handler(E: Env = any, P: string = any, I: Input = BlankInput, R: HandlerResponse<any> = any) {
  return fnType([Context(E, P, I), Next], R)
}
/* compiles to:
 * export type Handler<E extends Env = any, P extends string = any, I extends Input = BlankInput, R extends HandlerResponse<any> = any> = (a0: Context<E, P, I>, a1: Next) => R
 */

// ✗ MiddlewareHandler: the ScriptType does not itself typecheck as TypeScript
//   MiddlewareHandler.st.ts(3:43) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function MiddlewareHandler(E: Env = any, P: string = string, I: Input = {}, R: HandlerResponse<any> = Response) {
  return fnType([Context(E, P, I), Next], Promise(anyOf(R, voidType())))
}
/* compiles to:
 * export type MiddlewareHandler<E extends Env = any, P extends string = string, I extends Input = {}, R extends HandlerResponse<any> = Response> = (a0: Context<E, P, I>, a1: Next) => Promise<R | void>
 */

// ✓ H: verified type-identical to the original
/* @scripttype preserveParamNames */
export function H(E: Env = any, P: string = any, I: Input = BlankInput, R: HandlerResponse<any> = any) {
  return Handler(E, P, I, R) | MiddlewareHandler(E, P, I, R)
}
/* compiles to:
 * export type H<E extends Env = any, P extends string = any, I extends Input = BlankInput, R extends HandlerResponse<any> = any> = Handler<E, P, I, R> | MiddlewareHandler<E, P, I, R>
 */

// ✗ NotFoundHandler: the ScriptType does not itself typecheck as TypeScript
//   NotFoundHandler.st.ts(3:89) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function NotFoundHandler(E: Env = any) {
  return fnType([Context(E)], matches<Response>(NotFoundResponse) ? (NotFoundResponse | Promise(NotFoundResponse)) : (Response | Promise(Response)))
}
/* compiles to:
 * export type NotFoundHandler<E extends Env = any> = (a0: Context<E>) => NotFoundResponse extends Response ? NotFoundResponse | Promise<NotFoundResponse> : Response | Promise<Response>
 */

// ✗ ErrorHandler: the ScriptType does not itself typecheck as TypeScript
//   ErrorHandler.st.ts(3:18) TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
/* @scripttype preserveParamNames */
export function ErrorHandler(E: Env = any) {
  return fnType([Error | HTTPResponseError, Context(E)], Response | Promise(Response))
}
/* compiles to:
 * export type ErrorHandler<E extends Env = any> = (a0: Error | HTTPResponseError, a1: Context<E>) => Response | Promise<Response>
 */

// ✓ ToSchemaOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToSchemaOutput(RorO, I: Input | Input['in']) {
  const m1 = matches<TypedResponse<Hole<"T">, Hole<"U">, Hole<"F">>>(RorO)
  if (m1) {
    return { output: matches<typeof m1.T>(unknown) ? {} : m1.T, outputFormat: matches<{ outputFormat: string; }>(I) ? I['outputFormat'] : m1.F, status: m1.U }
  }
  return { output: matches<typeof RorO>(unknown) ? {} : RorO, outputFormat: matches<typeof RorO>(unknown) ? 'json' : (matches<{ outputFormat: string; }>(I) ? I['outputFormat'] : 'json'), status: StatusCode }
}
/* compiles to:
 * export type ToSchemaOutput<RorO, I extends Input | Input['in']> = RorO extends TypedResponse<infer T, infer U, infer F> ? { output: unknown extends T ? {} : T; outputFormat: I extends { outputFormat: string; } ? I['outputFormat'] : F; status: U } : { output: unknown extends RorO ? {} : RorO; outputFormat: unknown extends RorO ? 'json' : I extends { outputFormat: string; } ? I['outputFormat'] : 'json'; status: StatusCode }
 */

// ✗ ToSchema: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function ToSchema(M: string, P: string, I: Input | Input['in'], RorO) {
  if (matches<true>(IsAny(RorO))) {
    const out = emptyObject
    for (const K in keySet(P)) {
      out[K] = out
    }
    return out
  }
  if (matches<[ never ]>([RorO])) {
    return {}
  }
  if (matches<[ Promise<void> ]>([RorO])) {
    return {}
  }
  const out = emptyObject
  for (const K in keySet(P)) {
    out[K] = out2
  }
  return out
}
/* compiles to:
 * export type ToSchema<M extends string, P extends string, I extends Input | Input['in'], RorO> = IsAny<RorO> extends true ? { [K in P]: {} } : [RorO] extends [never] ? {} : [RorO] extends [Promise<void>] ? {} : { [K1 in P]: out2 }
 */

// ✓ AddSchemaIfHasResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddSchemaIfHasResponse(Merged, S: Schema, M: string, P: string, I: Input | Input['in'], BasePath: string) {
  if (matches<[ Promise<void> ]>([Merged])) {
    return S
  }
  return merge(S, ToSchema(M, MergePath(BasePath, P), I, Merged))
}
/* compiles to:
 * export type AddSchemaIfHasResponse<Merged, S extends Schema, M extends string, P extends string, I extends Input | Input['in'], BasePath extends string> = [Merged] extends [Promise<void>] ? S : S & ToSchema<M, MergePath<BasePath, P>, I, Merged>
 */

// ✓ ExtractParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractParams(Path: string) {
  if (matches<typeof Path>(string)) {
    return Record(string, string)
  }
  const m1 = matches<`${Hole<"_Start">}:${Hole<"Param">}/${Hole<"Rest">}`>(Path)
  if (m1) {
    const out = emptyObject
    for (const K in keySet(m1.Param | keyof(ExtractParams(`/${m1.Rest}`)))) {
      out[K] = string
    }
    return out
  }
  const m2 = matches<`${Hole<"_Start">}:${Hole<"Param">}`>(Path)
  if (m2) {
    const out = emptyObject
    for (const K in keySet(m2.Param)) {
      out[K] = string
    }
    return out
  }
  return never
}
/* compiles to:
 * export type ExtractParams<Path extends string> = string extends Path ? Record<string, string> : Path extends `${string}:${infer Param}/${infer Rest}` ? { [K in Param | keyof ExtractParams<`/${Rest}`>]: string } : Path extends `${string}:${infer Param}` ? { [K1 in Param]: string } : never
 */

// ✓ FlattenIfIntersect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FlattenIfIntersect(T) {
  const m1 = matches<Hole<"O">>(T)
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.O)) {
      out[K] = m1.O[K]
    }
    return out
  }
  return never
}
/* compiles to:
 * export type FlattenIfIntersect<T> = T extends infer O ? { [K in keyof O]: O[K] } : never
 */

// ✗ MergeSchemaPath: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function MergeSchemaPath(OrigSchema: Schema, SubPath: string) {
  const out = emptyObject
  for (const P in keyof(OrigSchema)) {
    out[MergePath(SubPath, merge(P, string))] = matches<[ Record<string, Endpoint> ]>([OrigSchema[P]]) ? out : never
  }
  return out
}
/* compiles to:
 * export type MergeSchemaPath<OrigSchema extends Schema, SubPath extends string> = { [P in keyof OrigSchema as MergePath<SubPath, P & string>]: [OrigSchema[P]] extends [Record<string, Endpoint>] ? {} : never }
 */

// ✗ MergeEndpointParamsWithPath: the ScriptType does not itself typecheck as TypeScript
//   MergeEndpointParamsWithPath.st.ts(4:11) TS2451: Cannot redeclare block-scoped variable 'm1'.
/* @scripttype preserveParamNames */
export function MergeEndpointParamsWithPath(T: Endpoint, SubPath: string) {
  if (matches<unknown>(T)) {
    const m1 = matches<{ param: Hole<"_">; }>(T['input'])
    const m2 = matches<`${Hole<"Prefix">}{${Hole<"_">}}`>(K)
    const out = emptyObject
    for (const K in keyof(ExtractParams(SubPath))) {
      out[m2 ? m2.Prefix : K] = string
    }
    const m3 = matches<`${Hole<"Prefix">}{${Hole<"_">}}`>(K)
    const out2 = emptyObject
    for (const K in keyof(ExtractParams(SubPath))) {
      out2[m3 ? m3.Prefix : K] = string
    }
    return { input: m1 ? (matches<never>(ExtractParams(SubPath)) ? T['input'] : FlattenIfIntersect(merge(T['input'], { param: out }))) : (matches<never>(RemoveBlankRecord(ExtractParams(SubPath))) ? T['input'] : (merge(T['input'], { param: out2 }))), output: T['output'], outputFormat: T['outputFormat'], status: T['status'] }
  }
  return never
}
/* compiles to:
 * export type MergeEndpointParamsWithPath<T extends Endpoint, SubPath extends string> = T extends unknown ? { input: T['input'] extends { param: infer _; } ? ExtractParams<SubPath> extends never ? T['input'] : FlattenIfIntersect<T['input'] & { param: { [K in keyof ExtractParams<SubPath> as K extends `${infer Prefix}{${string}}` ? Prefix : K]: string } }> : RemoveBlankRecord<ExtractParams<SubPath>> extends never ? T['input'] : T['input'] & { param: { [K1 in keyof ExtractParams<SubPath> as K extends `${infer Prefix}{${string}}` ? Prefix : K1]: string } }; output: T['output']; outputFormat: T['outputFormat']; status: T['status'] } : never
 */

// ✓ AddParam: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddParam(I, P: string) {
  if (matches<never>(ParamKeys(P))) {
    return I
  }
  const m1 = matches<{ param: Hole<"_">; }>(I)
  if (m1) {
    return I
  }
  return merge(I, { param: UnionToIntersection(ParamKeyToRecord(ParamKeys(P))) })
}
/* compiles to:
 * export type AddParam<I, P extends string> = ParamKeys<P> extends never ? I : I extends { param: infer _; } ? I : I & { param: UnionToIntersection<ParamKeyToRecord<ParamKeys<P>>> }
 */

// ✓ AddDollar: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddDollar(T: string) {
  return `$${Lowercase(T)}`
}
/* compiles to:
 * export type AddDollar<T extends string> = `$${Lowercase<T>}`
 */

// ✓ MergePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergePath(A: string, B: string) {
  if (matches<''>(B)) {
    return MergePath(A, '/')
  }
  if (matches<''>(A)) {
    return B
  }
  if (matches<'/'>(A)) {
    return B
  }
  const m1 = matches<`${Hole<"P">}/`>(A)
  if (m1) {
    const m2 = matches<`/${Hole<"Q">}`>(B)
    if (m2) {
      return `${m1.P}/${m2.Q}`
    }
    return `${m1.P}/${B}`
  }
  const m3 = matches<`/${Hole<"Q">}`>(B)
  if (m3) {
    if (matches<''>(m3.Q)) {
      return A
    }
    return `${A}/${m3.Q}`
  }
  return `${A}/${B}`
}
/* compiles to:
 * export type MergePath<A extends string, B extends string> = B extends '' ? MergePath<A, '/'> : A extends '' ? B : A extends '/' ? B : A extends `${infer P}/` ? B extends `/${infer Q}` ? `${P}/${Q}` : `${P}/${B}` : B extends `/${infer Q}` ? Q extends '' ? A : `${A}/${Q}` : `${A}/${B}`
 */

// ✓ TypedResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TypedResponse(T = unknown, U: StatusCode = StatusCode, F: ResponseFormat = matches<string>(T) ? 'text' : (matches<JSONValue>(T) ? 'json' : ResponseFormat)) {
  return { _data: T, _status: U, _format: F }
}
/* compiles to:
 * export type TypedResponse<T = unknown, U extends StatusCode = StatusCode, F extends ResponseFormat = T extends string ? 'text' : T extends JSONValue ? 'json' : ResponseFormat> = { _data: T; _status: U; _format: F }
 */

// ✓ MergeTypedResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeTypedResponse(T) {
  if (matches<Promise<void>>(T)) {
    return T
  }
  const m1 = matches<Promise<Hole<"T2">>>(T)
  if (m1) {
    if (matches<TypedResponse>(m1.T2)) {
      return m1.T2
    }
    return TypedResponse
  }
  if (matches<TypedResponse>(T)) {
    return T
  }
  return TypedResponse
}
/* compiles to:
 * export type MergeTypedResponse<T> = T extends Promise<void> ? T : T extends Promise<infer T2> ? T2 extends TypedResponse ? T2 : TypedResponse : T extends TypedResponse ? T : TypedResponse
 */

// ✓ ExtractTypedResponseOnly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractTypedResponseOnly(T) {
  if (matches<TypedResponse>(T)) {
    return T
  }
  return never
}
/* compiles to:
 * export type ExtractTypedResponseOnly<T> = T extends TypedResponse ? T : never
 */

// ✓ MergeMiddlewareResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeMiddlewareResponse(T) {
  const m1 = matches<(c: any, next: any) => Promise<Hole<"R">>>(T)
  if (m1) {
    if (matches<never>(Exclude(m1.R, voidType()))) {
      return never
    }
    if (matches<Response | TypedResponse<any, any, any>>(Exclude(m1.R, voidType()))) {
      return ExtractTypedResponseOnly(Exclude(m1.R, voidType()))
    }
    return never
  }
  const m2 = matches<(c: any, next: any) => Hole<"R">>(T)
  if (m2) {
    if (matches<Response | TypedResponse<any, any, any>>(m2.R)) {
      return ExtractTypedResponseOnly(m2.R)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type MergeMiddlewareResponse<T> = T extends (c: any, next: any) => Promise<infer R> ? Exclude<R, void> extends never ? never : Exclude<R, void> extends Response | TypedResponse<any, any, any> ? ExtractTypedResponseOnly<Exclude<R, void>> : never : T extends (c: any, next: any) => infer R ? R extends Response | TypedResponse<any, any, any> ? ExtractTypedResponseOnly<R> : never : never
 */

// ✓ ValidationTargets: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidationTargets(T: FormValue = ParsedFormValue, P: string = string) {
  const m1 = matches<`${Hole<"_">}?`>(P)
  return { json: any, form: Record(string, anyOf(T, arrayOf(T))), query: Record(string, string | arrayOf(string)), param: Record(P, m1 ? (anyOf(string, Undefined)) : string), header: Record(RequestHeader | CustomHeader, string), cookie: Record(string, string) }
}
/* compiles to:
 * export type ValidationTargets<T extends FormValue = ParsedFormValue, P extends string = string> = { json: any; form: Record<string, T | T[]>; query: Record<string, string | string[]>; param: Record<P, P extends `${string}?` ? string | undefined : string>; header: Record<RequestHeader | CustomHeader, string>; cookie: Record<string, string> }
 */

// ✓ ParamKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParamKey(Component) {
  const m1 = matches<`:${Hole<"NameWithPattern">}`>(Component)
  if (m1) {
    const m2 = matches<`${Hole<"Name">}{${Hole<"Rest">}`>(m1.NameWithPattern)
    if (m2) {
      const m3 = matches<`${Hole<"_Pattern">}?`>(m2.Rest)
      if (m3) {
        return `${m2.Name}?`
      }
      return m2.Name
    }
    return m1.NameWithPattern
  }
  return never
}
/* compiles to:
 * export type ParamKey<Component> = Component extends `:${infer NameWithPattern}` ? NameWithPattern extends `${infer Name}{${infer Rest}` ? Rest extends `${string}?` ? `${Name}?` : Name : NameWithPattern : never
 */

// ✓ ParamKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParamKeys(Path) {
  const m1 = matches<`${Hole<"Component">}/${Hole<"Rest">}`>(Path)
  if (m1) {
    return ParamKey(m1.Component) | ParamKeys(m1.Rest)
  }
  return ParamKey(Path)
}
/* compiles to:
 * export type ParamKeys<Path> = Path extends `${infer Component}/${infer Rest}` ? ParamKey<Component> | ParamKeys<Rest> : ParamKey<Path>
 */

// ✓ ParamKeyToRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParamKeyToRecord(T: string) {
  const m1 = matches<`${Hole<"R">}?`>(T)
  if (m1) {
    return Record(m1.R, anyOf(string, Undefined))
  }
  const out = emptyObject
  for (const K in keySet(T)) {
    out[K] = string
  }
  return out
}
/* compiles to:
 * export type ParamKeyToRecord<T extends string> = T extends `${infer R}?` ? Record<R, string | undefined> : { [K in T]: string }
 */

// ✗ InputToDataByTarget: does not compile yet
//   'Target' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function InputToDataByTarget(T: Input['out'], Target: keyof ValidationTargets) {
  const m1 = matches<{ [K in typeof Target]: Hole<"R">; }>(T)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type InputToDataByTarget<T extends Input['out'], Target extends keyof ValidationTargets> = T extends { [K in typeof Target]: infer R; } ? R : never
 */

// ✓ RemoveQuestion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveQuestion(T) {
  const m1 = matches<`${Hole<"R">}?`>(T)
  if (m1) {
    return m1.R
  }
  return T
}
/* compiles to:
 * export type RemoveQuestion<T> = T extends `${infer R}?` ? R : T
 */

// ✗ ExtractSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Env'.
/* @scripttype preserveParamNames */
export function ExtractSchema(T) {
  const m1 = matches<HonoBase<Hole<"_">, Hole<"S">, any, any>>(T)
  return UnionToIntersection(m1 ? m1.S : never)
}
/* compiles to:
 * export type ExtractSchema<T> = UnionToIntersection<T extends HonoBase<unknown, infer S, any, any> ? S : never>
 */

// ✗ ExtractSchemaForStatusCode: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function ExtractSchemaForStatusCode(T, Status: number) {
  const out = emptyObject
  for (const Path in keyof(ExtractSchema(T))) {
    out[Path] = out
  }
  return out
}
/* compiles to:
 * export type ExtractSchemaForStatusCode<T, Status extends number> = { [Path in keyof ExtractSchema<T>]: {} }
 */

// ✓ ExtractHandlerResponse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractHandlerResponse(T) {
  const m1 = matches<(c: any, next: any) => Promise<Hole<"R">>>(T)
  if (m1) {
    if (matches<never>(Exclude(m1.R, voidType()))) {
      return never
    }
    if (matches<Response | TypedResponse<any, any, any>>(Exclude(m1.R, voidType()))) {
      return Exclude(m1.R, voidType())
    }
    return never
  }
  const m2 = matches<(c: any, next: any) => Hole<"R">>(T)
  if (m2) {
    if (matches<Response | TypedResponse<any, any, any>>(m2.R)) {
      return m2.R
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractHandlerResponse<T> = T extends (c: any, next: any) => Promise<infer R> ? Exclude<R, void> extends never ? never : Exclude<R, void> extends Response | TypedResponse<any, any, any> ? Exclude<R, void> : never : T extends (c: any, next: any) => infer R ? R extends Response | TypedResponse<any, any, any> ? R : never : never
 */

// ✓ ProcessHead: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ProcessHead(T) {
  return IfAnyThenEmptyObject(matches<Env>(T) ? (matches<typeof T>(Env) ? {} : T) : T)
}
/* compiles to:
 * export type ProcessHead<T> = IfAnyThenEmptyObject<T extends Env ? Env extends T ? {} : T : T>
 */

// ✓ IntersectNonAnyTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IntersectNonAnyTypes(T: any[]) {
  const m1 = matches<[ Hole<"Head">, ...Hole<"Rest"> ]>(T)
  if (m1) {
    return ProcessHead(m1.Head) & IntersectNonAnyTypes(m1.Rest)
  }
  return {}
}
/* compiles to:
 * export type IntersectNonAnyTypes<T extends any[]> = T extends [infer Head, ...infer Rest] ? ProcessHead<Head> & IntersectNonAnyTypes<Rest> : {}
 */

/**
 * ScriptType for 02-inference-at-scale/hono/src/utils/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 17 type-level aliases: 16 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { InvalidJSONValue, JSONPrimitive, JSONValue } from '../../../../../02-inference-at-scale/hono/src/utils/types.js'
declare namespace Array {
  export type isArray<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type J<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type K<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type A<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const InvalidJSONValue: any
declare const JSONPrimitive: any
declare const JSONValue: any
declare const toJSON: any
type Equal<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsInvalid<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type toJSON<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Expect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Expect(T: true): any {
  return T
}
/* compiles to:
 * export type Expect<T extends true> = T
 */

// ✓ Equal: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equal(X, Y): any {
  if (matches<<T>() => T extends typeof Y ? 1 : 2>(genericFnType(['T'], [], matches<typeof X>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equal<X, Y> =
 *   (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? true : false
 */

// ✓ NotEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotEqual(X, Y): any {
  if (matches<Equal<typeof X, typeof Y>>(true)) {
    return false
  }
  return true
}
/* compiles to:
 * export type NotEqual<X, Y> = true extends Equal<X, Y> ? false : true
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U): any {
  const m1 = matches<(k: Hole<"I">) => void>(matches<any>(U) ? fnType([U], voidType()) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> =
 *   (U extends any ? (a0: U) => void : never) extends (k: infer I) => void ? I : never
 */

// ✓ RemoveBlankRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveBlankRecord(T): any {
  const m1 = matches<Record<Hole<"K">, unknown>>(T)
  if (m1) {
    if (typeof m1.K === 'string') {
      return T
    }
    return never
  }
  return never
}
/* compiles to:
 * export type RemoveBlankRecord<T> =
 *   T extends Record<infer K, unknown> ? K extends string ? T : never : never
 */

// ✓ IfAnyThenEmptyObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IfAnyThenEmptyObject(T): any {
  if (matches<1 & typeof T>(0)) {
    return {}
  }
  return T
}
/* compiles to:
 * export type IfAnyThenEmptyObject<T> = 0 extends 1 & T ? {} : T
 */

// ✓ InvalidToNull: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvalidToNull(T): any {
  if (matches<InvalidJSONValue>(T)) {
    return Null
  }
  return T
}
/* compiles to:
 * export type InvalidToNull<T> = T extends InvalidJSONValue ? null : T
 */

// ✓ IsInvalid: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsInvalid(T): any {
  if (matches<InvalidJSONValue>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsInvalid<T> = T extends InvalidJSONValue ? true : false
 */

// ✓ OmitSymbolKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitSymbolKeys(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[typeof K === 'symbol' ? never : K] = T[K]
  }
  return out
}
/* compiles to:
 * export type OmitSymbolKeys<T> = { [K in keyof T as K extends symbol ? never : K]: T[K] }
 */

// ✓ JSONParsed: verified type-identical to the original
/* @scripttype preserveParamNames */
export function JSONParsed(T, TError = bigint | readonlyArrayOf(bigint)): any {
  const m1 = matches<{ toJSON(): Hole<"J">; }>(T)
  if (m1) {
    if (matches<() => JSONPrimitive>(fnType([], m1.J))) {
      return m1.J
    }
    if (matches<() => { toJSON(): unknown; }>(fnType([], m1.J))) {
      return {}
    }
    return JSONParsed(m1.J, TError)
  }
  if (matches<JSONPrimitive>(T)) {
    return T
  }
  if (matches<InvalidJSONValue>(T)) {
    return never
  }
  if (matches<ReadonlyArray<unknown>>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = JSONParsed(InvalidToNull(T[K]), TError)
    }
    const m2 = matches<Hole<"A">>(out)
    if (m2) {
      if (matches<ReadonlyArray<unknown>>(m2.A)) {
        return m2.A
      }
      return arrayOf(JSONParsed(InvalidToNull(T[number]), TError))
    }
    return never
  }
  if (matches<Set<unknown> | Map<unknown, unknown> | Record<string, never>>(T)) {
    return {}
  }
  if (matches<object>(T)) {
    if (matches<typeof TError>(T[keyof(T)])) {
      return never
    }
    const out2 = emptyObject
    for (const K in keyof(OmitSymbolKeys(T))) {
      out2[matches<true>(IsInvalid(T[K])) ? never : K] = matches<IsInvalid<(typeof T)[typeof K]>>(boolean) ? (anyOf(JSONParsed(T[K], TError), Undefined)) : JSONParsed(T[K], TError)
    }
    return out2
  }
  if (matches<unknown>(T)) {
    if (matches<typeof TError>(T)) {
      return never
    }
    return JSONValue
  }
  return never
}
/* compiles to:
 * export type JSONParsed<T, TError = bigint | readonly bigint[]> =
 *   T extends { toJSON(): infer J; }
 *     ? (() => J) extends () => JSONPrimitive ? J
 *     : (() => J) extends () => { toJSON(): unknown; } ? {}
 *     : JSONParsed<J, TError>
 *   : T extends JSONPrimitive ? T
 *   : T extends InvalidJSONValue ? never
 *   : T extends ReadonlyArray<unknown>
 *     ? { [K in keyof T]: JSONParsed<InvalidToNull<T[K]>, TError> } extends infer A
 *       ? A extends ReadonlyArray<unknown> ? A : JSONParsed<InvalidToNull<T[number]>, TError>[]
 *       : never
 *   : T extends Set<unknown> | Map<unknown, unknown> | Record<string, never> ? {}
 *   : T extends object
 *     ? T[keyof T] extends TError
 *       ? never
 *       : {
 *         [K1 in keyof OmitSymbolKeys<T> as IsInvalid<T[K1]> extends true ? never : K1]: boolean extends IsInvalid<T[K1]>
 *           ? JSONParsed<T[K1], TError> | undefined
 *           : JSONParsed<T[K1], TError>
 *       }
 *   : T extends unknown ? T extends TError ? never : JSONValue
 *   : never
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T): any {
  const out = emptyObject
  for (const KeyType in keyof(T)) {
    out[KeyType] = T[KeyType]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<T> = { [KeyType in keyof T]: T[KeyType] } & {}
 */

// ✓ SimplifyDeepArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyDeepArray(T): any {
  if (Array.isArray(T)) {
    const out = emptyObject
    for (const E in keyof(T)) {
      out[E] = SimplifyDeepArray(T[E])
    }
    return out
  }
  return Simplify(T)
}
/* compiles to:
 * export type SimplifyDeepArray<T> =
 *   T extends any[] ? { [E in keyof T]: SimplifyDeepArray<T[E]> } : Simplify<T>
 */

// ✓ InterfaceToType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InterfaceToType(T): any {
  if (matches<Function>(T)) {
    return T
  }
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = InterfaceToType(T[K])
  }
  return out
}
/* compiles to:
 * export type InterfaceToType<T> =
 *   T extends Function ? T : { [K in keyof T]: InterfaceToType<T[K]> }
 */

// ✓ RequiredKeysOf: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} BaseType
 */
export function RequiredKeysOf(BaseType): any {
  const out = emptyObject
  for (const Key in keyof(BaseType)) {
    out[Key] = matches<Record<typeof Key, (typeof BaseType)[typeof Key]>>(BaseType) ? Key : never
  }
  return Exclude(out[keyof(BaseType)], Undefined)
}
/* compiles to:
 * export type RequiredKeysOf<BaseType extends object> = Exclude<
 *   { [Key in keyof BaseType]: BaseType extends Record<Key, BaseType[Key]> ? Key : never }[keyof BaseType],
 *   undefined
 * >
 */

// ✓ HasRequiredKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HasRequiredKeys(BaseType: object): any {
  if (matches<never>(RequiredKeysOf(BaseType))) {
    return false
  }
  return true
}
/* compiles to:
 * export type HasRequiredKeys<BaseType extends object> =
 *   RequiredKeysOf<BaseType> extends never ? false : true
 */

// ✗ IsAny: does not compile yet
//   Cannot find name 'extends'.
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  if (matches<(typeof T extends never ? true : false)>(boolean)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = boolean extends T extends never ? true : false ? true : false
 */

// ✓ StringLiteralUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StringLiteralUnion(T): any {
  return anyOf(T, string & Record(never, never))
}
/* compiles to:
 * export type StringLiteralUnion<T> = T | string & Record<never, never>
 */

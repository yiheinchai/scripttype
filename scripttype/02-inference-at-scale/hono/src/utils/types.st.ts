/**
 * ScriptType for 02-inference-at-scale/hono/src/utils/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 17 type-level aliases: 14 covered, 1 raw, 1 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const InvalidJSONValue: any
declare const JSONPrimitive: any
declare const JSONValue: any
declare const toJSON: any
type Equal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InvalidJSONValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsInvalid<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type JSONPrimitive<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type JSONValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type toJSON<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Expect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Expect(T: true) {
  return T
}
/* compiles to:
 * export type Expect<T extends true> = T
 */

// ✗ Equal: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function Equal(X, Y) {
  if (matches<<T>() => T extends typeof Y ? 1 : 2>(raw('<T>() => T extends X ? 1 : 2'))) {
    return true
  }
  return false
}

// ✓ NotEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotEqual(X, Y) {
  if (matches<Equal<typeof X, typeof Y>>(true)) {
    return false
  }
  return true
}
/* compiles to:
 * export type NotEqual<X, Y> = true extends Equal<X, Y> ? false : true
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U) {
  const m1 = matches<(k: Hole<"I">) => void>(matches<any>(U) ? fnType([U], voidType()) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> = (U extends any ? (a0: U) => void : never) extends (k: infer I) => void ? I : never
 */

// ✓ RemoveBlankRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemoveBlankRecord(T) {
  const m1 = matches<Record<Hole<"K">, unknown>>(T)
  if (m1) {
    if (matches<string>(m1.K)) {
      return T
    }
    return never
  }
  return never
}
/* compiles to:
 * export type RemoveBlankRecord<T> = T extends Record<infer K, unknown> ? K extends string ? T : never : never
 */

// ✓ IfAnyThenEmptyObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IfAnyThenEmptyObject(T) {
  if (matches<1 & typeof T>(0)) {
    return {}
  }
  return T
}
/* compiles to:
 * export type IfAnyThenEmptyObject<T> = 0 extends 1 & T ? {} : T
 */

// ✓ InvalidToNull: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvalidToNull(T) {
  if (matches<InvalidJSONValue>(T)) {
    return Null
  }
  return T
}
/* compiles to:
 * export type InvalidToNull<T> = T extends InvalidJSONValue ? null : T
 */

// ✓ IsInvalid: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsInvalid(T) {
  if (matches<InvalidJSONValue>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsInvalid<T> = T extends InvalidJSONValue ? true : false
 */

// ✓ OmitSymbolKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitSymbolKeys(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[matches<symbol>(K) ? never : K] = T[K]
  }
  return out
}
/* compiles to:
 * export type OmitSymbolKeys<T> = { [K in keyof T as K extends symbol ? never : K]: T[K] }
 */

// ✓ JSONParsed: verified type-identical to the original
/* @scripttype preserveParamNames */
export function JSONParsed(T, TError = bigint | ReadonlyArray(bigint)) {
  const m1 = matches<{ toJSON(): Hole<"J">; }>(T)
  if (m1) {
    if (matches<() => JSONPrimitive>(fnType([], m1.J))) {
      return m1.J
    }
    if (matches<() => { toJSON(): unknown; }>(fnType([], m1.J))) {
      return {}
    }
    return JSONParsed(m1.J, TError)
  }
  if (matches<JSONPrimitive>(T)) {
    return T
  }
  if (matches<InvalidJSONValue>(T)) {
    return never
  }
  if (matches<ReadonlyArray<unknown>>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = JSONParsed(InvalidToNull(T[K]), TError)
    }
    const m2 = matches<Hole<"A">>(out)
    if (m2) {
      if (matches<ReadonlyArray<unknown>>(m2.A)) {
        return m2.A
      }
      return arrayOf(JSONParsed(InvalidToNull(T[number]), TError))
    }
    return never
  }
  if (matches<Set<unknown> | Map<unknown, unknown> | Record<string, never>>(T)) {
    return {}
  }
  if (matches<object>(T)) {
    if (matches<typeof TError>(T[keyof(T)])) {
      return never
    }
    const out = emptyObject
    for (const K in keyof(OmitSymbolKeys(T))) {
      out[matches<true>(IsInvalid(T[K])) ? never : K] = matches<IsInvalid<(typeof T)[typeof K]>>(boolean) ? (anyOf(JSONParsed(T[K], TError), Undefined)) : JSONParsed(T[K], TError)
    }
    return out
  }
  if (matches<unknown>(T)) {
    if (matches<typeof TError>(T)) {
      return never
    }
    return JSONValue
  }
  return never
}
/* compiles to:
 * export type JSONParsed<T, TError = bigint | ReadonlyArray<bigint>> = T extends { toJSON(): infer J; } ? (() => J) extends () => JSONPrimitive ? J : (() => J) extends () => { toJSON(): unknown; } ? {} : JSONParsed<J, TError> : T extends JSONPrimitive ? T : T extends InvalidJSONValue ? never : T extends ReadonlyArray<unknown> ? { [K in keyof T]: JSONParsed<InvalidToNull<T[K]>, TError> } extends infer A ? A extends ReadonlyArray<unknown> ? A : JSONParsed<InvalidToNull<T[number]>, TError>[] : never : T extends Set<unknown> | Map<unknown, unknown> | Record<string, never> ? {} : T extends object ? T[keyof T] extends TError ? never : { [K1 in keyof OmitSymbolKeys<T> as IsInvalid<T[K1]> extends true ? never : K1]: boolean extends IsInvalid<T[K1]> ? JSONParsed<T[K1], TError> | undefined : JSONParsed<T[K1], TError> } : T extends unknown ? T extends TError ? never : JSONValue : never
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T) {
  const out = emptyObject
  for (const KeyType in keyof(T)) {
    out[KeyType] = T[KeyType]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<T> = { [KeyType in keyof T]: T[KeyType] } & {}
 */

// ✓ SimplifyDeepArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyDeepArray(T) {
  if (matches<any[]>(T)) {
    const out = emptyObject
    for (const E in keyof(T)) {
      out[E] = SimplifyDeepArray(T[E])
    }
    return out
  }
  return Simplify(T)
}
/* compiles to:
 * export type SimplifyDeepArray<T> = T extends any[] ? { [E in keyof T]: SimplifyDeepArray<T[E]> } : Simplify<T>
 */

// ✓ InterfaceToType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InterfaceToType(T) {
  if (matches<Function>(T)) {
    return T
  }
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = InterfaceToType(T[K])
  }
  return out
}
/* compiles to:
 * export type InterfaceToType<T> = T extends Function ? T : { [K in keyof T]: InterfaceToType<T[K]> }
 */

// ✗ RequiredKeysOf: the ScriptType does not itself typecheck as TypeScript
//   RequiredKeysOf.st.ts(5:60) TS2537: Type '{}' has no matching index signature for type 'string'.
/* @scripttype preserveParamNames */
export function RequiredKeysOf(BaseType: object) {
  const out = emptyObject
  for (const Key in keyof(BaseType)) {
    out[Key] = matches<Record<typeof Key, (typeof BaseType)[typeof Key]>>(BaseType) ? Key : never
  }
  return Exclude(out[keyof(BaseType)], Undefined)
}
/* compiles to:
 * export type RequiredKeysOf<BaseType extends object> = Exclude<{ [Key in keyof BaseType]: BaseType extends Record<Key, BaseType[Key]> ? Key : never }[keyof BaseType], undefined>
 */

// ✓ HasRequiredKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HasRequiredKeys(BaseType: object) {
  if (matches<never>(RequiredKeysOf(BaseType))) {
    return false
  }
  return true
}
/* compiles to:
 * export type HasRequiredKeys<BaseType extends object> = RequiredKeysOf<BaseType> extends never ? false : true
 */

// ✗ IsAny: does not compile yet
//   'T' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function IsAny(T) {
  if (matches<(typeof T extends never ? true : false)>(boolean)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = boolean extends typeof T extends never ? true : false ? true : false
 */

// ✓ StringLiteralUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StringLiteralUnion(T) {
  return anyOf(T, string & Record(never, never))
}
/* compiles to:
 * export type StringLiteralUnion<T> = T | string & Record<never, never>
 */

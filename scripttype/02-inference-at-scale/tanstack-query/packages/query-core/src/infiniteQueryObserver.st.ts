/**
 * ScriptType for 02-inference-at-scale/tanstack-query/packages/query-core/src/infiniteQueryObserver.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { InfiniteQueryObserverResult } from '../../../../../../02-inference-at-scale/tanstack-query/packages/query-core/src/types.js'
declare const InfiniteQueryObserverResult: any
// ✓ InfiniteQueryObserverListener: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQueryObserverListener(TData, TError): any {
  return fnType([InfiniteQueryObserverResult(TData, TError)], voidType())
}
/* compiles to:
 * export type InfiniteQueryObserverListener<TData, TError> =
 *   (a0: InfiniteQueryObserverResult<TData, TError>) => void
 */

// ✓ ReplaceReturnType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReplaceReturnType(TFunction: (...args: Array<any>) => unknown, TReturn): any {
  return fnType([...Parameters(TFunction)], TReturn)
}
/* compiles to:
 * export type ReplaceReturnType<TFunction extends (...args: Array<any>) => unknown, TReturn> =
 *   (...a0: Parameters<TFunction>) => TReturn
 */

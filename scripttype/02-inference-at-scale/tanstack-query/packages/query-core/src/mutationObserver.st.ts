/**
 * ScriptType for 02-inference-at-scale/tanstack-query/packages/query-core/src/mutationObserver.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { MutationObserverResult } from '../../../../../../02-inference-at-scale/tanstack-query/packages/query-core/src/types.js'
declare const MutationObserverResult: any
// ✓ MutationObserverListener: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationObserverListener(TData, TError, TVariables, TOnMutateResult): any {
  return fnType([MutationObserverResult(TData, TError, TVariables, TOnMutateResult)], voidType())
}
/* compiles to:
 * export type MutationObserverListener<TData, TError, TVariables, TOnMutateResult> =
 *   (a0: MutationObserverResult<TData, TError, TVariables, TOnMutateResult>) => void
 */

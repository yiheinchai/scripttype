/**
 * ScriptType for 02-inference-at-scale/tanstack-query/packages/query-core/src/thenable.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Fulfilled: any
declare const Pending: any
declare const Rejected: any
type Fulfilled<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Pending<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Rejected<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ FulfilledThenable: the ScriptType does not itself typecheck as TypeScript
//   FulfilledThenable.st.ts(3:10) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function FulfilledThenable(T) {
  return Promise(T) & Fulfilled(T)
}
/* compiles to:
 * export type FulfilledThenable<T> = Promise<T> & Fulfilled<T>
 */

// ✗ RejectedThenable: the ScriptType does not itself typecheck as TypeScript
//   RejectedThenable.st.ts(3:10) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function RejectedThenable(T) {
  return Promise(T) & Rejected
}
/* compiles to:
 * export type RejectedThenable<T> = Promise<T> & Rejected
 */

// ✗ PendingThenable: the ScriptType does not itself typecheck as TypeScript
//   PendingThenable.st.ts(3:10) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function PendingThenable(T) {
  return Promise(T) & Pending(T)
}
/* compiles to:
 * export type PendingThenable<T> = Promise<T> & Pending<T>
 */

// ✓ Thenable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Thenable(T) {
  return FulfilledThenable(T) | RejectedThenable(T) | PendingThenable(T)
}
/* compiles to:
 * export type Thenable<T> = FulfilledThenable<T> | RejectedThenable<T> | PendingThenable<T>
 */

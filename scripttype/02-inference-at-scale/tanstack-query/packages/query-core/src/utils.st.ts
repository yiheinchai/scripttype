/**
 * ScriptType for 02-inference-at-scale/tanstack-query/packages/query-core/src/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ DropLast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DropLast(T: ReadonlyArray<unknown>) {
  const m1 = matches<readonly [ ...Hole<"R">, unknown ]>(T)
  if (m1) {
    return asReadonly([...m1.R])
  }
  return never
}
/* compiles to:
 * export type DropLast<T extends ReadonlyArray<unknown>> =
 *   T extends readonly [...infer R, unknown] ? readonly [...R] : never
 */

// ✓ TuplePrefixes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TuplePrefixes(T: ReadonlyArray<unknown>) {
  if (matches<readonly [ ]>(T)) {
    return asReadonly([])
  }
  return anyOf(TuplePrefixes(DropLast(T)), T)
}
/* compiles to:
 * export type TuplePrefixes<T extends ReadonlyArray<unknown>> =
 *   T extends readonly [] ? readonly [] : TuplePrefixes<DropLast<T>> | T
 */

// ✓ Updater: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Updater(TInput, TOutput) {
  return TOutput | fnType([TInput], TOutput)
}
/* compiles to:
 * export type Updater<TInput, TOutput> = TOutput | ((a0: TInput) => TOutput)
 */

/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/arktype-adapter/src/index.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ArkTypeLike } from '../../../../../../02-inference-at-scale/tanstack-router/packages/arktype-adapter/src/index.js'
import type { ValidatorAdapter } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/validators.js'
declare const ArkTypeLike: any
declare const ValidatorAdapter: any
// ✓ ArkTypeValidatorAdapter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArkTypeValidatorAdapter(TOptions: ArkTypeLike): any {
  return ValidatorAdapter(TOptions['inferIn'], TOptions['infer'])
}
/* compiles to:
 * export type ArkTypeValidatorAdapter<TOptions extends ArkTypeLike> = ValidatorAdapter<
 *   TOptions['inferIn'],
 *   TOptions['infer']
 * >
 */

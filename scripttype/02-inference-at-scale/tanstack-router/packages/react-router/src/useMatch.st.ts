/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/react-router/src/useMatch.tsx
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { StructuralSharingOption } from '../../../../../../02-inference-at-scale/tanstack-router/packages/react-router/src/structuralSharing.js'
import type { UseMatchBaseOptions } from '../../../../../../02-inference-at-scale/tanstack-router/packages/react-router/src/useMatch.js'
import type { MakeRouteMatch, MakeRouteMatchUnion } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/Matches.js'
import type { AnyRouter } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/router.js'
import type { StrictOrFrom } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/utils.js'
declare const AnyRouter: any
declare const MakeRouteMatch: any
declare const MakeRouteMatchUnion: any
declare const StrictOrFrom: any
declare const StructuralSharingOption: any
declare const UseMatchBaseOptions: any
// ✓ UseMatchRoute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseMatchRoute(TFrom): any {
  return genericFnType(['TRouter extends AnyRouter = RegisteredRouter', 'TSelected = unknown', 'TStructuralSharing extends boolean = boolean'], [optElem(UseMatchBaseOptions(TRouter, TFrom, true, true, TSelected, TStructuralSharing) & StructuralSharingOption(TRouter, TSelected, TStructuralSharing))], UseMatchResult(TRouter, TFrom, true, TSelected))
}
/* compiles to:
 * export type UseMatchRoute<TFrom> =
 *   <TRouter extends AnyRouter = RegisteredRouter, TSelected = unknown, TStructuralSharing extends boolean = boolean>(
 *     a0?: UseMatchBaseOptions<TRouter, TFrom, true, true, TSelected, TStructuralSharing> & StructuralSharingOption<TRouter, TSelected, TStructuralSharing>
 *   ) => UseMatchResult<TRouter, TFrom, true, TSelected>
 */

// ✓ UseMatchOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseMatchOptions(TRouter: AnyRouter, TFrom: string | undefined, TStrict: boolean, TThrow: boolean, TSelected, TStructuralSharing: boolean): any {
  return StrictOrFrom(TRouter, TFrom, TStrict) & UseMatchBaseOptions(TRouter, TFrom, TStrict, TThrow, TSelected, TStructuralSharing) & StructuralSharingOption(TRouter, TSelected, TStructuralSharing)
}
/* compiles to:
 * export type UseMatchOptions<
 *   TRouter extends AnyRouter,
 *   TFrom extends string | undefined,
 *   TStrict extends boolean,
 *   TThrow extends boolean,
 *   TSelected,
 *   TStructuralSharing extends boolean
 * > =
 *   & StrictOrFrom<TRouter, TFrom, TStrict>
 *   & UseMatchBaseOptions<TRouter, TFrom, TStrict, TThrow, TSelected, TStructuralSharing>
 *   & StructuralSharingOption<TRouter, TSelected, TStructuralSharing>
 */

// ✓ UseMatchResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseMatchResult(TRouter: AnyRouter, TFrom, TStrict: boolean, TSelected): any {
  if (matches<typeof TSelected>(unknown)) {
    if (matches<true>(TStrict)) {
      return MakeRouteMatch(TRouter['routeTree'], TFrom, TStrict)
    }
    return MakeRouteMatchUnion(TRouter)
  }
  return TSelected
}
/* compiles to:
 * export type UseMatchResult<
 *   TRouter extends AnyRouter,
 *   TFrom,
 *   TStrict extends boolean,
 *   TSelected
 * > =
 *   unknown extends TSelected
 *     ? TStrict extends true
 *       ? MakeRouteMatch<TRouter['routeTree'], TFrom, TStrict>
 *       : MakeRouteMatchUnion<TRouter>
 *     : TSelected
 */

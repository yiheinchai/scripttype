/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/react-start-rsc/src/ServerComponentTypes.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 16 type-level aliases: 10 covered, 5 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ComponentProps: any
declare const ComponentType: any
declare const CompositeComponent: any
declare const Constrain: any
declare const LooseAsyncReturnType: any
declare const LooseReturnType: any
declare const React: any
declare const ReactNode: any
declare const ReactSerializable: any
declare const RenderableServerComponentAttributes: any
declare const ValidateSerializable: any
type ComponentProps<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ComponentType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type CompositeComponent<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constrain<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LooseAsyncReturnType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LooseReturnType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type React<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReactNode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReactSerializable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RenderableServerComponentAttributes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValidateCompositeComponentResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValidateSerializable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ValidateCompositeComponent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponent(TComp) {
  return Constrain(TComp, fnType([ValidateCompositeComponentProps(TComp)], ValidateCompositeComponentReturnType(TComp)))
}
/* compiles to:
 * export type ValidateCompositeComponent<TComp> = Constrain<TComp, (a0: ValidateCompositeComponentProps<TComp>) => ValidateCompositeComponentReturnType<TComp>>
 */

// ✓ ValidateCompositeComponentProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentProps(TComp) {
  if (matches<typeof TComp>(unknown)) {
    return TComp
  }
  return ValidateCompositeComponentPropsObject(CompositeComponentProps(TComp))
}
/* compiles to:
 * export type ValidateCompositeComponentProps<TComp> = unknown extends TComp ? TComp : ValidateCompositeComponentPropsObject<CompositeComponentProps<TComp>>
 */

// ✓ ValidateCompositeComponentPropsObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentPropsObject(TProps) {
  if (matches<typeof TProps>(unknown)) {
    return TProps
  }
  const out = emptyObject
  for (const TKey in keyof(TProps)) {
    out[TKey] = ValidateCompositeComponentProp(TProps[TKey])
  }
  return out
}
/* compiles to:
 * export type ValidateCompositeComponentPropsObject<TProps> = unknown extends TProps ? TProps : { [TKey in keyof TProps]: ValidateCompositeComponentProp<TProps[TKey]> }
 */

// ✓ CompositeComponentProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CompositeComponentProps(TComp) {
  const m1 = matches<(props: Hole<"TProps">) => any>(TComp)
  if (m1) {
    return m1.TProps
  }
  return unknown
}
/* compiles to:
 * export type CompositeComponentProps<TComp> = TComp extends (props: infer TProps) => any ? TProps : unknown
 */

// ✗ ValidateCompositeComponentProp: the ScriptType does not itself typecheck as TypeScript
//   ValidateCompositeComponentProp.st.ts(9:15) TS2702: 'React' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentProp(TProp) {
  if (matches<(...args: Array<any>) => any>(TProp)) {
    return fnType([ValidateReactSerializable(Parameters(TProp))], React.ReactNode)
  }
  if (matches<ComponentType<any>>(TProp)) {
    return ComponentType(ValidateReactSerializable(ComponentProps(TProp)))
  }
  if (matches<React.ReactNode>(TProp)) {
    return TProp
  }
  return React.ReactNode
}
/* compiles to:
 * export type ValidateCompositeComponentProp<TProp> = TProp extends (...args: Array<any>) => any ? (a0: ValidateReactSerializable<Parameters<TProp>>) => React.ReactNode : TProp extends ComponentType<any> ? ComponentType<ValidateReactSerializable<ComponentProps<TProp>>> : TProp extends React.ReactNode ? TProp : React.ReactNode
 */

// ✓ ValidateReactSerializable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateReactSerializable(T) {
  return ValidateSerializable(T, ReactSerializable)
}
/* compiles to:
 * export type ValidateReactSerializable<T> = ValidateSerializable<T, ReactSerializable>
 */

// ✗ ValidateCompositeComponentReturnType: does not compile yet
//   Cannot find namespace 'React'.
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentReturnType(TComp) {
  if (matches<typeof TComp>(unknown)) {
    return React.ReactNode
  }
  return ValidateCompositeComponentResult(LooseReturnType(TComp))
}
/* compiles to:
 * export type ValidateCompositeComponentReturnType<TComp> = unknown extends TComp ? React.ReactNode : ValidateCompositeComponentResult<LooseReturnType<TComp>>
 */

// ✓ ValidateCompositeComponentResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentResult(TNode) {
  return ValidateServerComponentResult(TNode)
}
/* compiles to:
 * export type ValidateCompositeComponentResult<TNode> = ValidateServerComponentResult<TNode>
 */

// ✗ ValidateServerComponentResult: the ScriptType does not itself typecheck as TypeScript
//   ValidateServerComponentResult.st.ts(6:15) TS2702: 'React' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ValidateServerComponentResult(TNode) {
  if (matches<Promise<any>>(TNode)) {
    return ValidateCompositeComponentPromiseResult(TNode)
  }
  if (matches<React.ReactNode>(TNode)) {
    return TNode
  }
  if (matches<(...args: Array<any>) => any>(TNode)) {
    return React.ReactNode
  }
  if (matches<object>(TNode)) {
    return ValidateCompositeComponentObjectResult(TNode)
  }
  return React.ReactNode
}
/* compiles to:
 * export type ValidateServerComponentResult<TNode> = TNode extends Promise<any> ? ValidateCompositeComponentPromiseResult<TNode> : TNode extends React.ReactNode ? TNode : TNode extends (...args: Array<any>) => any ? React.ReactNode : TNode extends object ? ValidateCompositeComponentObjectResult<TNode> : React.ReactNode
 */

// ✓ ValidateCompositeComponentPromiseResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentPromiseResult(TPromise) {
  const m1 = matches<Promise<Hole<"T">>>(TPromise)
  if (m1) {
    return t<Promise<ValidateCompositeComponentResult<typeof m1.T>>>()
  }
  return never
}
/* compiles to:
 * export type ValidateCompositeComponentPromiseResult<TPromise> = TPromise extends Promise<infer T> ? Promise<ValidateCompositeComponentResult<T>> : never
 */

// ✓ ValidateCompositeComponentObjectResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateCompositeComponentObjectResult(TObject) {
  const out = emptyObject
  for (const TKey in keyof(TObject)) {
    out[TKey] = ValidateCompositeComponentResult(TObject[TKey])
  }
  return out
}
/* compiles to:
 * export type ValidateCompositeComponentObjectResult<TObject> = { [TKey in keyof TObject]: ValidateCompositeComponentResult<TObject[TKey]> }
 */

// ✓ CompositeComponentResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CompositeComponentResult(TComp) {
  return CompositeComponentBuilder(TComp, LooseAsyncReturnType(TComp))
}
/* compiles to:
 * export type CompositeComponentResult<TComp> = CompositeComponentBuilder<TComp, LooseAsyncReturnType<TComp>>
 */

// ✗ CompositeComponentBuilder: the ScriptType does not itself typecheck as TypeScript
//   CompositeComponentBuilder.st.ts(3:15) TS2702: 'React' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function CompositeComponentBuilder(TComp, TReturn) {
  if (matches<React.ReactNode>(TReturn)) {
    return CompositeComponent(TComp, TReturn)
  }
  const out = emptyObject
  for (const TKey in keyof(TReturn)) {
    out[TKey] = CompositeComponentBuilder(TComp, TReturn[TKey])
  }
  return out
}
/* compiles to:
 * export type CompositeComponentBuilder<TComp, TReturn> = TReturn extends React.ReactNode ? CompositeComponent<TComp, TReturn> : { [TKey in keyof TReturn]: CompositeComponentBuilder<TComp, TReturn[TKey]> }
 */

// ✓ ValidateRenderableServerComponent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateRenderableServerComponent(TNode) {
  return ValidateServerComponentResult(TNode)
}
/* compiles to:
 * export type ValidateRenderableServerComponent<TNode> = ValidateServerComponentResult<TNode>
 */

// ✗ RenderableServerComponentBuilder: the ScriptType does not itself typecheck as TypeScript
//   RenderableServerComponentBuilder.st.ts(3:15) TS2702: 'React' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function RenderableServerComponentBuilder(T) {
  if (matches<React.ReactNode>(T)) {
    return RenderableServerComponent(T)
  }
  const out = emptyObject
  for (const TKey in keyof(T)) {
    out[TKey] = RenderableServerComponentBuilder(T[TKey])
  }
  return out
}
/* compiles to:
 * export type RenderableServerComponentBuilder<T> = T extends React.ReactNode ? RenderableServerComponent<T> : { [TKey in keyof T]: RenderableServerComponentBuilder<T[TKey]> }
 */

// ✗ RenderableServerComponent: the ScriptType does not itself typecheck as TypeScript
//   RenderableServerComponent.st.ts(2:50) TS2702: 'React' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function RenderableServerComponent(TNode: React.ReactNode) {
  return merge(TNode, RenderableServerComponentAttributes(TNode))
}
/* compiles to:
 * export type RenderableServerComponent<TNode extends React.ReactNode> = TNode & RenderableServerComponentAttributes<TNode>
 */

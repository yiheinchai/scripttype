/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/router-core/src/defer.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TSR_DEFERRED_PROMISE: any
type TSR_DEFERRED_PROMISE<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ DeferredPromiseState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeferredPromiseState(T) {
  return anyOf({ status: 'pending', data: optional(T), error: optional(unknown) }, { status: 'success', data: T }, { status: 'error', data: optional(T), error: unknown })
}
/* compiles to:
 * export type DeferredPromiseState<T> = { status: 'pending'; data?: T; error?: unknown } | { status: 'success'; data: T } | { status: 'error'; data?: T; error: unknown }
 */

// ✓ DeferredPromise: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeferredPromise(T) {
  return merge(t<Promise<typeof T>>(), { [TSR_DEFERRED_PROMISE]: DeferredPromiseState(T) })
}
/* compiles to:
 * export type DeferredPromise<T> = Promise<T> & { [TSR_DEFERRED_PROMISE]: DeferredPromiseState<T> }
 */

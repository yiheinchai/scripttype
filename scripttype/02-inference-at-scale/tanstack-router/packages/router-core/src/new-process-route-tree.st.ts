/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/router-core/src/new-process-route-tree.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 4 covered, 3 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ExtendedSegmentKind: any
declare const LRUCache: any
declare const ParamExtractionState: any
declare const RouteLike: any
type DynamicSegmentNode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtendedSegmentKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LRUCache<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParamExtractionState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RouteLike<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StaticSegmentNode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ StaticSegmentNode: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function StaticSegmentNode(T: RouteLike) {
  return merge(SegmentNode(T), { kind: anyOf(raw('typeof SEGMENT_TYPE_PATHNAME'), raw('typeof SEGMENT_TYPE_PATHLESS'), raw('typeof SEGMENT_TYPE_INDEX')) })
}

// ✗ DynamicSegmentNode: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function DynamicSegmentNode(T: RouteLike) {
  return merge(SegmentNode(T), { kind: anyOf(raw('typeof SEGMENT_TYPE_PARAM'), raw('typeof SEGMENT_TYPE_WILDCARD'), raw('typeof SEGMENT_TYPE_OPTIONAL_PARAM')), prefix: optional(string), suffix: optional(string), caseSensitive: boolean })
}

// ✓ AnySegmentNode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnySegmentNode(T: RouteLike) {
  return StaticSegmentNode(T) | DynamicSegmentNode(T)
}
/* compiles to:
 * export type AnySegmentNode<T extends RouteLike> = StaticSegmentNode<T> | DynamicSegmentNode<T>
 */

// ✓ SegmentNode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SegmentNode(T: RouteLike) {
  return { kind: ExtendedSegmentKind, pathless: t<Array<StaticSegmentNode<typeof T>>>() | Null, index: StaticSegmentNode(T) | Null, static: t<Map<string, StaticSegmentNode<typeof T>>>() | Null, staticInsensitive: t<Map<string, StaticSegmentNode<typeof T>>>() | Null, dynamic: t<Array<DynamicSegmentNode<typeof T>>>() | Null, optional: t<Array<DynamicSegmentNode<typeof T>>>() | Null, wildcard: t<Array<DynamicSegmentNode<typeof T>>>() | Null, route: anyOf(T, Null), fullPath: string, parent: AnySegmentNode(T) | Null, depth: number, parse: Null | fnType([Record(string, string)], unknown), priority: number }
}
/* compiles to:
 * export type SegmentNode<T extends RouteLike> = { kind: ExtendedSegmentKind; pathless: Array<StaticSegmentNode<T>> | null; index: StaticSegmentNode<T> | null; static: Map<string, StaticSegmentNode<T>> | null; staticInsensitive: Map<string, StaticSegmentNode<T>> | null; dynamic: Array<DynamicSegmentNode<T>> | null; optional: Array<DynamicSegmentNode<T>> | null; wildcard: Array<DynamicSegmentNode<T>> | null; route: T | null; fullPath: string; parent: AnySegmentNode<T> | null; depth: number; parse: null | ((a0: Record<string, string>) => unknown); priority: number }
 */

// ✗ ProcessedTree: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function ProcessedTree(TTree: Extract<RouteLike, { fullPath: string; }>, TFlat: Extract<RouteLike, { from: string; }>, TSingle: Extract<RouteLike, { from: string; }>) {
  return { segmentTree: AnySegmentNode(TTree), masksTree: AnySegmentNode(TFlat) | Null, singleCache: LRUCache(string, AnySegmentNode(TSingle)), matchCache: LRUCache(string, RouteMatch(TTree) | Null), flatCache: LRUCache(string, ReturnType(raw('typeof findMatch<TFlat>'))) | Null }
}

// ✓ RouteMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RouteMatch(T: Extract<RouteLike, { fullPath: string; }>) {
  return { route: T, rawParams: Record(string, string), branch: t<ReadonlyArray<typeof T>>() }
}
/* compiles to:
 * export type RouteMatch<T extends Extract<RouteLike, { fullPath: string; }>> = { route: T; rawParams: Record<string, string>; branch: ReadonlyArray<T> }
 */

// ✓ MatchStackFrame: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MatchStackFrame(T: RouteLike) {
  return { node: AnySegmentNode(T), index: number, depth: number, skipped: number, statics: number, dynamics: number, optionals: number, extract: optional(ParamExtractionState), rawParams: optional(Record(string, string)) }
}
/* compiles to:
 * export type MatchStackFrame<T extends RouteLike> = { node: AnySegmentNode<T>; index: number; depth: number; skipped: number; statics: number; dynamics: number; optionals: number; extract?: ParamExtractionState; rawParams?: Record<string, string> }
 */

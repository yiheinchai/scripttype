/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/router-core/src/searchMiddleware.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { SearchMiddlewareMeta } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/route.js'
declare const SearchMiddlewareMeta: any
// ✓ SearchMiddlewareNextWithMeta: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SearchMiddlewareNextWithMeta(TSearchSchema): any {
  return fnType([TSearchSchema, true], { search: TSearchSchema, meta: SearchMiddlewareMeta })
}
/* compiles to:
 * export type SearchMiddlewareNextWithMeta<TSearchSchema> =
 *   (a0: TSearchSchema, a1: true) => { search: TSearchSchema; meta: SearchMiddlewareMeta }
 */

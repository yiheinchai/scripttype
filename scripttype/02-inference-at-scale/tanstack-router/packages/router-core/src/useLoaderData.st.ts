/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/router-core/src/useLoaderData.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AllLoaderData, RouteById } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/routeInfo.js'
import type { AnyRouter } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/router.js'
import type { Expand } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/utils.js'
declare const AllLoaderData: any
declare const AnyRouter: any
declare const Expand: any
declare const RouteById: any
// ✓ ResolveUseLoaderData: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveUseLoaderData(TRouter: AnyRouter, TFrom, TStrict: boolean): any {
  if (matches<false>(TStrict)) {
    return AllLoaderData(TRouter['routeTree'])
  }
  return Expand(RouteById(TRouter['routeTree'], TFrom)['types']['loaderData'])
}
/* compiles to:
 * export type ResolveUseLoaderData<TRouter extends AnyRouter, TFrom, TStrict extends boolean> =
 *   TStrict extends false
 *     ? AllLoaderData<TRouter['routeTree']>
 *     : Expand<RouteById<TRouter['routeTree'], TFrom>['types']['loaderData']>
 */

// ✓ UseLoaderDataResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseLoaderDataResult(TRouter: AnyRouter, TFrom, TStrict: boolean, TSelected): any {
  if (matches<typeof TSelected>(unknown)) {
    return ResolveUseLoaderData(TRouter, TFrom, TStrict)
  }
  return TSelected
}
/* compiles to:
 * export type UseLoaderDataResult<
 *   TRouter extends AnyRouter,
 *   TFrom,
 *   TStrict extends boolean,
 *   TSelected
 * > =
 *   unknown extends TSelected ? ResolveUseLoaderData<TRouter, TFrom, TStrict> : TSelected
 */

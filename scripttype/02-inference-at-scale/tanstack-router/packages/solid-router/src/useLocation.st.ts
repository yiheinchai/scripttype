/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/solid-router/src/useLocation.tsx
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyRouter, RouterState } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/router.js'
declare const AnyRouter: any
declare const RouterState: any
// ✓ UseLocationResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseLocationResult(TRouter: AnyRouter, TSelected): any {
  if (matches<typeof TSelected>(unknown)) {
    return RouterState(TRouter['routeTree'])['location']
  }
  return TSelected
}
/* compiles to:
 * export type UseLocationResult<TRouter extends AnyRouter, TSelected> =
 *   unknown extends TSelected ? RouterState<TRouter['routeTree']>['location'] : TSelected
 */

/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/start-client-core/src/createCsrfMiddleware.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Register } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/router.js'
import type { RequestServerOptions } from '../../../../../../02-inference-at-scale/tanstack-router/packages/start-client-core/src/createMiddleware.js'
declare const Register: any
declare const RequestServerOptions: any
// ✓ CsrfMatcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CsrfMatcher(TValue, TRegister = Register, TMiddlewares = unknown): any {
  return TValue | arrayOf(TValue) | fnType([anyOf(TValue, merge(string, {})), RequestServerOptions(TRegister, TMiddlewares)], boolean | t<Promise<boolean>>())
}
/* compiles to:
 * export type CsrfMatcher<TValue, TRegister = Register, TMiddlewares = unknown> =
 *   | TValue
 *   | TValue[]
 *   | (
 *       (a0: TValue | string & {}, a1: RequestServerOptions<TRegister, TMiddlewares>) => | boolean
 *       | Promise<boolean>
 *     )
 */

/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/start-client-core/src/createServerFn.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 14 type-level aliases: 12 covered, 1 mismatch, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyFunctionMiddleware: any
declare const AnyRequestMiddleware: any
declare const Constrain: any
declare const FormData: any
declare const IntersectAllValidatorInputs: any
declare const Method: any
declare const OptionalFetcher: any
declare const PromiseLike: any
declare const RegisteredSerializableInput: any
declare const RequiredFetcher: any
declare const ResolveValidatorInput: any
declare const Response: any
declare const ServerFnAfterValidator: any
declare const ServerFnBuilder: any
declare const ServerFnCtx: any
declare const ServerFnOptions: any
declare const ServerFnStrict: any
declare const U: any
declare const ValidateSerializable: any
declare const ValidateSerializableInput: any
declare const Validator: any
type AnyFunctionMiddleware<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyRequestMiddleware<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Constrain<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FormData<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IntersectAllValidatorInputs<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Method<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OptionalFetcher<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PromiseLike<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RegisteredSerializableInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RequiredFetcher<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ResolveValidatorInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Response<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnAfterValidator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnBuilder<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnCtx<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnOptions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnReturnType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ServerFnStrict<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ValidateSerializable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ValidateSerializableInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ValidateValidator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Validator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ServerFnStrictInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServerFnStrictInput(TStrict: ServerFnStrict) {
  if (matches<false>(TStrict)) {
    return false
  }
  const m1 = matches<{ input: Hole<"TInput", boolean>; }>(TStrict)
  if (m1) {
    return m1.TInput
  }
  return true
}
/* compiles to:
 * export type ServerFnStrictInput<TStrict extends ServerFnStrict> =
 *   TStrict extends false ? false
 *   : TStrict extends { input: infer TInput extends boolean; } ? TInput
 *   : true
 */

// ✓ ServerFnStrictOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServerFnStrictOutput(TStrict: ServerFnStrict) {
  if (matches<false>(TStrict)) {
    return false
  }
  const m1 = matches<{ output: Hole<"TOutput", boolean>; }>(TStrict)
  if (m1) {
    return m1.TOutput
  }
  return true
}
/* compiles to:
 * export type ServerFnStrictOutput<TStrict extends ServerFnStrict> =
 *   TStrict extends false ? false
 *   : TStrict extends { output: infer TOutput extends boolean; } ? TOutput
 *   : true
 */

// ✗ CreateServerFn: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function CreateServerFn(TRegister) {
  return genericFnType(['TMethod extends Method', 'TStrict extends ServerFnStrict = true', 'TResponse = unknown', 'TMiddlewares = undefined', 'TInputValidator = undefined'], [ServerFnOptions(TMethod, TStrict), ServerFnBaseOptions(TRegister, TMethod, TResponse, TMiddlewares, TInputValidator, TStrict)], ServerFnBuilder(TRegister, TMethod, TStrict))
}
/* compiles to:
 * export type CreateServerFn<TRegister> =
 *   <TMethod extends Method, TStrict extends ServerFnStrict = true, TResponse = unknown, TMiddlewares = undefined, TInputValidator = undefined>(
 *     a0: ServerFnOptions<TMethod, TStrict>,
 *     a1: ServerFnBaseOptions<TRegister, TMethod, TResponse, TMiddlewares, TInputValidator, TStrict>
 *   ) => ServerFnBuilder<TRegister, TMethod, TStrict>
 */

// ✓ Fetcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Fetcher(TMiddlewares, TInputValidator, TResponse) {
  if (matches<IntersectAllValidatorInputs<typeof TMiddlewares, typeof TInputValidator>>(Undefined)) {
    return OptionalFetcher(TMiddlewares, TInputValidator, TResponse)
  }
  return RequiredFetcher(TMiddlewares, TInputValidator, TResponse)
}
/* compiles to:
 * export type Fetcher<TMiddlewares, TInputValidator, TResponse> =
 *   undefined extends IntersectAllValidatorInputs<TMiddlewares, TInputValidator>
 *     ? OptionalFetcher<TMiddlewares, TInputValidator, TResponse>
 *     : RequiredFetcher<TMiddlewares, TInputValidator, TResponse>
 */

// ✓ RscStream: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RscStream(T) {
  return { __cacheState: T }
}
/* compiles to:
 * export type RscStream<T> = { __cacheState: T }
 */

// ✓ ServerFnReturnType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServerFnReturnType(TRegister, TResponse, TStrict: ServerFnStrict = true) {
  if (matches<false>(ServerFnStrictOutput(TStrict))) {
    return TResponse
  }
  const m1 = matches<PromiseLike<Hole<"U">>>(TResponse)
  if (m1) {
    return t<Promise<ServerFnReturnType<typeof TRegister, typeof m1.U, typeof TStrict>>>()
  }
  if (matches<Response>(TResponse)) {
    return TResponse
  }
  return ValidateSerializableInput(TRegister, TResponse)
}
/* compiles to:
 * export type ServerFnReturnType<TRegister, TResponse, TStrict extends ServerFnStrict = true> =
 *   ServerFnStrictOutput<TStrict> extends false ? TResponse
 *   : TResponse extends PromiseLike<infer U> ? Promise<ServerFnReturnType<TRegister, U, TStrict>>
 *   : TResponse extends Response ? TResponse
 *   : ValidateSerializableInput<TRegister, TResponse>
 */

// ✓ ServerFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServerFn(TRegister, TMethod, TMiddlewares, TInputValidator, TResponse, TStrict: ServerFnStrict = true) {
  return fnType([ServerFnCtx(TRegister, TMethod, TMiddlewares, TInputValidator)], ServerFnReturnType(TRegister, TResponse, TStrict))
}
/* compiles to:
 * export type ServerFn<
 *   TRegister,
 *   TMethod,
 *   TMiddlewares,
 *   TInputValidator,
 *   TResponse,
 *   TStrict extends ServerFnStrict = true
 * > =
 *   (a0: ServerFnCtx<TRegister, TMethod, TMiddlewares, TInputValidator>) => ServerFnReturnType<
 *     TRegister,
 *     TResponse,
 *     TStrict
 *   >
 */

// ✗ CompiledFetcherFn: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature
/* @scripttype preserveParamNames */
export function CompiledFetcherFn(TRegister, TResponse) {
  return raw('{ (opts: CompiledFetcherFnOptions & ServerFnBaseOptions<TRegister, Method>): Promise<TResponse>; url: string; serverFnMeta: ServerFnMeta; }')
}

// ✓ ServerFnBaseOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServerFnBaseOptions(TRegister, TMethod: Method = 'GET', TResponse = unknown, TMiddlewares = unknown, TInputValidator = unknown, TStrict: ServerFnStrict = true) {
  return { method: TMethod, strict: optional(TStrict), middleware: optional(Constrain(TMiddlewares, t<ReadonlyArray<AnyFunctionMiddleware | AnyRequestMiddleware>>())), validator: optional(ConstrainValidator(TRegister, TMethod, TInputValidator, TStrict)), inputValidator: optional(ConstrainValidator(TRegister, TMethod, TInputValidator, TStrict)), extractedFn: optional(CompiledFetcherFn(TRegister, TResponse)), serverFn: optional(ServerFn(TRegister, TMethod, TMiddlewares, TInputValidator, TResponse, TStrict)) }
}
/* compiles to:
 * export type ServerFnBaseOptions<
 *   TRegister,
 *   TMethod extends Method = 'GET',
 *   TResponse = unknown,
 *   TMiddlewares = unknown,
 *   TInputValidator = unknown,
 *   TStrict extends ServerFnStrict = true
 * > =
 *   {
 *     method: TMethod
 *     strict?: TStrict
 *     middleware?: Constrain<
 *       TMiddlewares,
 *       ReadonlyArray<AnyFunctionMiddleware | AnyRequestMiddleware>
 *     >
 *     validator?: ConstrainValidator<TRegister, TMethod, TInputValidator, TStrict>
 *     inputValidator?: ConstrainValidator<TRegister, TMethod, TInputValidator, TStrict>
 *     extractedFn?: CompiledFetcherFn<TRegister, TResponse>
 *     serverFn?: ServerFn<TRegister, TMethod, TMiddlewares, TInputValidator, TResponse, TStrict>
 *   }
 */

// ✓ ValidateValidatorInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateValidatorInput(TRegister, TMethod: Method, TInputValidator, TStrict: ServerFnStrict = true) {
  if (matches<false>(ServerFnStrictInput(TStrict))) {
    return ResolveValidatorInput(TInputValidator)
  }
  if (matches<'POST'>(TMethod)) {
    if (matches<FormData>(ResolveValidatorInput(TInputValidator))) {
      return ResolveValidatorInput(TInputValidator)
    }
    return ValidateSerializable(ResolveValidatorInput(TInputValidator), RegisteredSerializableInput(TRegister))
  }
  return ValidateSerializable(ResolveValidatorInput(TInputValidator), RegisteredSerializableInput(TRegister))
}
/* compiles to:
 * export type ValidateValidatorInput<
 *   TRegister,
 *   TMethod extends Method,
 *   TInputValidator,
 *   TStrict extends ServerFnStrict = true
 * > =
 *   ServerFnStrictInput<TStrict> extends false ? ResolveValidatorInput<TInputValidator>
 *   : TMethod extends 'POST'
 *     ? ResolveValidatorInput<TInputValidator> extends FormData
 *       ? ResolveValidatorInput<TInputValidator>
 *       : ValidateSerializable<
 *         ResolveValidatorInput<TInputValidator>,
 *         RegisteredSerializableInput<TRegister>
 *       >
 *   : ValidateSerializable<
 *     ResolveValidatorInput<TInputValidator>,
 *     RegisteredSerializableInput<TRegister>
 *   >
 */

// ✓ ValidateValidator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateValidator(TRegister, TMethod: Method, TInputValidator, TStrict: ServerFnStrict = true) {
  const m1 = matches<Hole<"TInput">>(ValidateValidatorInput(TRegister, TMethod, TInputValidator, TStrict))
  if (m1) {
    return Validator(m1.TInput, any)
  }
  return never
}
/* compiles to:
 * export type ValidateValidator<
 *   TRegister,
 *   TMethod extends Method,
 *   TInputValidator,
 *   TStrict extends ServerFnStrict = true
 * > =
 *   ValidateValidatorInput<TRegister, TMethod, TInputValidator, TStrict> extends infer TInput
 *     ? Validator<TInput, any>
 *     : never
 */

// ✓ ConstrainValidator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConstrainValidator(TRegister, TMethod: Method, TInputValidator, TStrict: ServerFnStrict = true) {
  return (matches<typeof TInputValidator>(unknown) ? TInputValidator : (matches<ValidateValidator<typeof TRegister, typeof TMethod, typeof TInputValidator, typeof TStrict>>(ResolveValidatorInput(TInputValidator)) ? TInputValidator : never)) | ValidateValidator(TRegister, TMethod, TInputValidator, TStrict)
}
/* compiles to:
 * export type ConstrainValidator<
 *   TRegister,
 *   TMethod extends Method,
 *   TInputValidator,
 *   TStrict extends ServerFnStrict = true
 * > =
 *   | (
 *       unknown extends TInputValidator ? TInputValidator
 *       : ResolveValidatorInput<TInputValidator> extends ValidateValidator<TRegister, TMethod, TInputValidator, TStrict>
 *         ? TInputValidator
 *       : never
 *     )
 *   | ValidateValidator<TRegister, TMethod, TInputValidator, TStrict>
 */

// ✓ AppendMiddlewares: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AppendMiddlewares(TMiddlewares, TNewMiddlewares) {
  if (matches<ReadonlyArray<any>>(TMiddlewares)) {
    if (matches<ReadonlyArray<any>>(TNewMiddlewares)) {
      return asReadonly([...TMiddlewares, ...TNewMiddlewares])
    }
    return TMiddlewares
  }
  return TNewMiddlewares
}
/* compiles to:
 * export type AppendMiddlewares<TMiddlewares, TNewMiddlewares> =
 *   TMiddlewares extends ReadonlyArray<any>
 *     ? TNewMiddlewares extends ReadonlyArray<any>
 *       ? readonly [...TMiddlewares, ...TNewMiddlewares]
 *       : TMiddlewares
 *     : TNewMiddlewares
 */

// ✓ ValidatorFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidatorFn(TRegister, TMethod: Method, TMiddlewares, TStrict: ServerFnStrict) {
  return genericFnType(['TInputValidator'], [ConstrainValidator(TRegister, TMethod, TInputValidator, TStrict)], ServerFnAfterValidator(TRegister, TMethod, TMiddlewares, TInputValidator, TStrict))
}
/* compiles to:
 * export type ValidatorFn<
 *   TRegister,
 *   TMethod extends Method,
 *   TMiddlewares,
 *   TStrict extends ServerFnStrict
 * > =
 *   <TInputValidator>(a0: ConstrainValidator<TRegister, TMethod, TInputValidator, TStrict>) => ServerFnAfterValidator<
 *     TRegister,
 *     TMethod,
 *     TMiddlewares,
 *     TInputValidator,
 *     TStrict
 *   >
 */

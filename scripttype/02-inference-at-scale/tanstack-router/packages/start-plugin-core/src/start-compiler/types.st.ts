/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/start-plugin-core/src/start-compiler/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { CompilationContext, RewriteCandidate } from '../../../../../../../02-inference-at-scale/tanstack-router/packages/start-plugin-core/src/start-compiler/types.js'
declare const CompilationContext: any
declare const RewriteCandidate: any
// ✓ BatchedPluginHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BatchedPluginHandler(TOpts = unknown): any {
  return fnType([arrayOf(RewriteCandidate), CompilationContext, TOpts], voidType())
}
/* compiles to:
 * export type BatchedPluginHandler<TOpts = unknown> =
 *   (a0: RewriteCandidate[], a1: CompilationContext, a2: TOpts) => void
 */

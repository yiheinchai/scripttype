/**
 * ScriptType for 02-inference-at-scale/tanstack-router/packages/vue-router-ssr-query/src/index.tsx
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyRouter } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-core/src/router.js'
import type { RouterSsrQueryOptions } from '../../../../../../02-inference-at-scale/tanstack-router/packages/router-ssr-query-core/src/index.js'
declare const AnyRouter: any
declare const RouterSsrQueryOptions: any
// ✓ Options: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Options(TRouter: AnyRouter): any {
  return merge(RouterSsrQueryOptions(TRouter), { wrapQueryClient: optional(boolean) })
}
/* compiles to:
 * export type Options<TRouter extends AnyRouter> =
 *   RouterSsrQueryOptions<TRouter> & { wrapQueryClient?: boolean }
 */

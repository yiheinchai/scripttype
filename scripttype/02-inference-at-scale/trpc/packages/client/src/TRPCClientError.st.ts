/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/TRPCClientError.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TRPCClientErrorBase } from '../../../../../../02-inference-at-scale/trpc/packages/client/src/TRPCClientError.js'
import type { InferrableClientTypes, inferClientTypes } from '../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inferrable.js'
declare const InferrableClientTypes: any
declare const TRPCClientErrorBase: any
declare const inferClientTypes: any
// ✓ inferErrorShape: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferErrorShape(TInferrable: InferrableClientTypes): any {
  return inferClientTypes(TInferrable)['errorShape']
}
/* compiles to:
 * export type inferErrorShape<TInferrable extends InferrableClientTypes> =
 *   inferClientTypes<TInferrable>['errorShape']
 */

// ✓ TRPCClientErrorLike: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TRPCClientErrorLike(TInferrable: InferrableClientTypes): any {
  return TRPCClientErrorBase(inferErrorShape(TInferrable))
}
/* compiles to:
 * export type TRPCClientErrorLike<TInferrable extends InferrableClientTypes> = TRPCClientErrorBase<
 *   inferErrorShape<TInferrable>
 * >
 */

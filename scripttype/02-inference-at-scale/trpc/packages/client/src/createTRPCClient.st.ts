/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/createTRPCClient.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 6 covered, 1 mismatch, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const $Next: any
declare const $Return: any
declare const $T: any
declare const AnyRouter: any
declare const AsyncGenerator: any
declare const AsyncIterable: any
declare const InferrableClientTypes: any
declare const ProcedureType: any
declare const RouterRecord: any
declare const TRPCClientError: any
declare const TRPCProcedureOptions: any
declare const TRPCResolverDef: any
declare const TRPCSubscriptionObserver: any
declare const TRPCUntypedClient: any
declare const Unsubscribable: any
declare const untypedClientSymbol: any
type $Next<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $Return<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $T<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyRouter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncGenerator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncIterable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InferrableClientTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ProcedureType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RouterRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRPCClientError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRPCProcedureOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRPCResolverDef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRPCSubscriptionObserver<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRPCUntypedClient<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Unsubscribable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type coerceAsyncGeneratorToIterable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type untypedClientSymbol<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ inferRouterClient: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRouterClient(TRouter: AnyRouter) {
  return TRPCClient(TRouter)
}
/* compiles to:
 * export type inferRouterClient<TRouter extends AnyRouter> = TRPCClient<TRouter>
 */

// ✓ CreateTRPCClient: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateTRPCClient(TRouter: AnyRouter) {
  return TRPCClient(TRouter)
}
/* compiles to:
 * export type CreateTRPCClient<TRouter extends AnyRouter> = TRPCClient<TRouter>
 */

// ✓ TRPCClient: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TRPCClient(TRouter: AnyRouter) {
  return merge(DecoratedProcedureRecord({ transformer: TRouter['_def']['_config']['$types']['transformer'], errorShape: TRouter['_def']['_config']['$types']['errorShape'] }, TRouter['_def']['record']), { [untypedClientSymbol]: TRPCUntypedClient(TRouter) })
}
/* compiles to:
 * export type TRPCClient<TRouter extends AnyRouter> = DecoratedProcedureRecord<{ transformer: TRouter['_def']['_config']['$types']['transformer']; errorShape: TRouter['_def']['_config']['$types']['errorShape'] }, TRouter['_def']['record']> & { [untypedClientSymbol]: TRPCUntypedClient<TRouter> }
 */

// ✓ coerceAsyncGeneratorToIterable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function coerceAsyncGeneratorToIterable(T) {
  const m1 = matches<AsyncGenerator<Hole<"$T">, Hole<"$Return">, Hole<"$Next">>>(T)
  if (m1) {
    return t<AsyncIterable<typeof m1.$T, typeof m1.$Return, typeof m1.$Next>>()
  }
  return T
}
/* compiles to:
 * export type coerceAsyncGeneratorToIterable<T> = T extends AsyncGenerator<infer $T, infer $Return, infer $Next> ? AsyncIterable<$T, $Return, $Next> : T
 */

// ✗ Resolver: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Resolver(TDef: TRPCResolverDef) {
  return fnType([TDef['input'], TRPCProcedureOptions], t<Promise<coerceAsyncGeneratorToIterable<(typeof TDef)['output']>>>())
}
/* compiles to:
 * export type Resolver<TDef extends TRPCResolverDef> = (a0: TDef['input'], a1: TRPCProcedureOptions) => Promise<coerceAsyncGeneratorToIterable<TDef['output']>>
 */

// ✓ SubscriptionResolver: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SubscriptionResolver(TDef: TRPCResolverDef) {
  return fnType([TDef['input'], Partial(TRPCSubscriptionObserver(TDef['output'], TRPCClientError(TDef))) & TRPCProcedureOptions], Unsubscribable)
}
/* compiles to:
 * export type SubscriptionResolver<TDef extends TRPCResolverDef> = (a0: TDef['input'], a1: Partial<TRPCSubscriptionObserver<TDef['output'], TRPCClientError<TDef>>> & TRPCProcedureOptions) => Unsubscribable
 */

// ✓ DecorateProcedure: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateProcedure(TType: ProcedureType, TDef: TRPCResolverDef) {
  if (matches<'query'>(TType)) {
    return { query: Resolver(TDef) }
  }
  if (matches<'mutation'>(TType)) {
    return { mutate: Resolver(TDef) }
  }
  if (matches<'subscription'>(TType)) {
    return { subscribe: SubscriptionResolver(TDef) }
  }
  return never
}
/* compiles to:
 * export type DecorateProcedure<TType extends ProcedureType, TDef extends TRPCResolverDef> = TType extends 'query' ? { query: Resolver<TDef> } : TType extends 'mutation' ? { mutate: Resolver<TDef> } : TType extends 'subscription' ? { subscribe: SubscriptionResolver<TDef> } : never
 */

// ✗ DecoratedProcedureRecord: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function DecoratedProcedureRecord(TRoot: InferrableClientTypes, TRecord: RouterRecord) {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = raw('TRecord[TKey] extends infer $Value ? $Value extends AnyProcedure ? DecorateProcedure<$Value[\'_def\'][\'type\'], { input: inferProcedureInput<$Value>; output: inferTransformedProcedureOutput<inferClientTypes<TRoot>, $Value>; errorShape: inferClientTypes<TRoot>[\'errorShape\']; transformer: inferClientTypes<TRoot>[\'transformer\']; }> : $Value extends RouterRecord ? DecoratedProcedureRecord<TRoot, $Value> : never : never')
  }
  return out
}

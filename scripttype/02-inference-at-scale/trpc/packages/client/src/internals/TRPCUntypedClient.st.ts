/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/internals/TRPCUntypedClient.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TRPCLink } from '../../../../../../../02-inference-at-scale/trpc/packages/client/src/links/types.js'
import type { InferrableClientTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inferrable.js'
import type { TypeError } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/types.js'
declare const InferrableClientTypes: any
declare const TRPCLink: any
declare const TypeError: any
// ✓ CreateTRPCClientOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateTRPCClientOptions(TRouter: InferrableClientTypes): any {
  return { links: arrayOf(TRPCLink(TRouter)), transformer: optional(TypeError('The transformer property has moved to httpLink/httpBatchLink/wsLink')) }
}
/* compiles to:
 * export type CreateTRPCClientOptions<TRouter extends InferrableClientTypes> = {
 *   links: TRPCLink<TRouter>[]
 *   transformer?: TypeError<'The transformer property has moved to httpLink/httpBatchLink/wsLink'>
 * }
 */

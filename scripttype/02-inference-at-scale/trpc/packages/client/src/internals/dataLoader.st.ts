/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/internals/dataLoader.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ BatchItem: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BatchItem(TKey, TValue) {
  return { aborted: boolean, key: TKey, resolve: fnType([TValue], voidType()) | Null, reject: fnType([Error], voidType()) | Null, batch: Batch(TKey, TValue) | Null }
}
/* compiles to:
 * export type BatchItem<TKey, TValue> = { aborted: boolean; key: TKey; resolve: ((a0: TValue) => void) | null; reject: ((a0: Error) => void) | null; batch: Batch<TKey, TValue> | null }
 */

// ✓ Batch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Batch(TKey, TValue) {
  return { items: arrayOf(BatchItem(TKey, TValue)) }
}
/* compiles to:
 * export type Batch<TKey, TValue> = { items: BatchItem<TKey, TValue>[] }
 */

// ✗ BatchLoader: the ScriptType does not itself typecheck as TypeScript
//   BatchLoader.st.ts(3:87) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function BatchLoader(TKey, TValue) {
  return { validate: fnType([arrayOf(TKey)], boolean), fetch: fnType([arrayOf(TKey)], Promise(arrayOf(TValue) | arrayOf(Promise(TValue)))) }
}
/* compiles to:
 * export type BatchLoader<TKey, TValue> = { validate: (a0: TKey[]) => boolean; fetch: (a0: TKey[]) => Promise<TValue[] | Promise<TValue>[]> }
 */

/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/links/httpBatchStreamLink.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { HTTPBatchLinkOptions } from '../../../../../../../02-inference-at-scale/trpc/packages/client/src/links/HTTPBatchLinkOptions.js'
import type { AnyClientTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inferrable.js'
declare const AnyClientTypes: any
declare const HTTPBatchLinkOptions: any
// ✓ HTTPBatchStreamLinkOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HTTPBatchStreamLinkOptions(TRoot: AnyClientTypes): any {
  return merge(HTTPBatchLinkOptions(TRoot), { streamHeader: optional(anyOf('trpc-accept', 'accept')) })
}
/* compiles to:
 * export type HTTPBatchStreamLinkOptions<TRoot extends AnyClientTypes> =
 *   HTTPBatchLinkOptions<TRoot> & { streamHeader?: 'trpc-accept' | 'accept' }
 */

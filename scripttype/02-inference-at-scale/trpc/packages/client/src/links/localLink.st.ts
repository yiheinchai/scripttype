/**
 * ScriptType for 02-inference-at-scale/trpc/packages/client/src/links/localLink.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TransformerOptions } from '../../../../../../../02-inference-at-scale/trpc/packages/client/src/internals/transformer.js'
import type { inferClientTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inferrable.js'
import type { ErrorHandlerOptions } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/procedure.js'
import type { AnyRouter, inferRouterContext } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
declare const AnyRouter: any
declare const ErrorHandlerOptions: any
declare const TransformerOptions: any
declare const inferClientTypes: any
declare const inferRouterContext: any
// ✓ LocalLinkOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LocalLinkOptions(TRouter: AnyRouter): any {
  return merge({ router: TRouter, createContext: fnType([], t<Promise<inferRouterContext<typeof TRouter>>>()), onError: optional(fnType([ErrorHandlerOptions(inferRouterContext(TRouter))], voidType())) }, TransformerOptions(inferClientTypes(TRouter)))
}
/* compiles to:
 * export type LocalLinkOptions<TRouter extends AnyRouter> =
 *   & {
 *       router: TRouter
 *       createContext: () => Promise<inferRouterContext<TRouter>>
 *       onError?: (a0: ErrorHandlerOptions<inferRouterContext<TRouter>>) => void
 *     }
 *   & TransformerOptions<inferClientTypes<TRouter>>
 */

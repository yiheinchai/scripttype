/**
 * ScriptType for 02-inference-at-scale/trpc/packages/next/src/app-dir/shared.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 5 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Resolver } from '../../../../../../../02-inference-at-scale/trpc/packages/client/src/createTRPCClient.js'
import type { inferTransformedProcedureOutput } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inference.js'
import type { AnyClientTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inferrable.js'
import type { AnyProcedure, inferProcedureInput, inferProcedureOutput } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/procedure.js'
import type { AnyRootTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/rootConfig.js'
import type { AnyRouter, RouterRecord } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
import type { ProtectedIntersection } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/types.js'
declare namespace m1 {
  export type $Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AnyClientTypes: any
declare const AnyProcedure: any
declare const AnyQueryProcedure: any
declare const AnyRootTypes: any
declare const AnyRouter: any
declare const ProtectedIntersection: any
declare const Resolver: any
declare const RouterRecord: any
declare const TData: any
declare const inferProcedureInput: any
declare const inferProcedureOutput: any
declare const inferTransformedProcedureOutput: any
type AnyQueryProcedure<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TData<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ UseProcedureRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseProcedureRecord__v1(TRoot, TRecord, TKey): any {
  const m1 = matches<Hole<"$Value">>(TRecord[TKey])
  if (m1) {
    if (matches<AnyQueryProcedure>(m1.$Value)) {
      return Resolver({ input: inferProcedureInput(m1.$Value), output: inferTransformedProcedureOutput(TRoot, m1.$Value), errorShape: TRoot['errorShape'], transformer: TRoot['transformer'] })
    }
    if (matches<RouterRecord>(m1.$Value)) {
      return UseProcedureRecord(TRoot, m1.$Value)
    }
    return never
  }
  return never
}
/**
 * @param {RouterRecord} TRecord
 */
export function UseProcedureRecord(TRoot: AnyRootTypes, TRecord): any {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = UseProcedureRecord__v1(TRoot, TRecord, TKey)
  }
  return out
}
/* compiles to:
 * export type UseProcedureRecord__v1<TRoot, TRecord, TKey> =
 *   TRecord[TKey] extends infer $Value
 *     ? $Value extends AnyQueryProcedure
 *       ? Resolver<
 *         {
 *           input: inferProcedureInput<$Value>
 *           output: inferTransformedProcedureOutput<TRoot, $Value>
 *           errorShape: TRoot['errorShape']
 *           transformer: TRoot['transformer']
 *         }
 *       >
 *     : $Value extends RouterRecord ? UseProcedureRecord<TRoot, $Value>
 *     : never
 *     : never
 * export type UseProcedureRecord<TRoot extends AnyRootTypes, TRecord extends RouterRecord> = {
 *   [TKey in keyof TRecord]: UseProcedureRecord__v1<TRoot, TRecord, TKey>
 * }
 */

// ✓ NextAppRouterUse: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NextAppRouterUse(TRouter: AnyRouter): any {
  const out = emptyObject
  for (const TKey in keyof(TData)) {
    out[TKey] = Awaited(TData[TKey])
  }
  return { ...genericCallSig(['TData extends Promise<unknown>[]'], [fnType([UseProcedureRecord(TRouter['_def']['_config']['$types'], TRouter['_def']['record'])], [...TData])], out), ...genericCallSig(['TData extends Promise<unknown>'], [fnType([UseProcedureRecord(TRouter['_def']['_config']['$types'], TRouter['_def']['record'])], TData)], Awaited(TData)) }
}
/* compiles to:
 * export type NextAppRouterUse<TRouter extends AnyRouter> = {
 *   <TData extends Promise<unknown>[]>(a0: (a0: UseProcedureRecord<TRouter['_def']['_config']['$types'], TRouter['_def']['record']>) => [...TData]): {
 *     [TKey in keyof TData]: Awaited<TData[TKey]>
 *   }
 *   <TData extends Promise<unknown>>(a0: (a0: UseProcedureRecord<TRouter['_def']['_config']['$types'], TRouter['_def']['record']>) => TData): Awaited<
 *     TData
 *   >
 * }
 */

// ✓ CreateTRPCNextAppRouterBase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateTRPCNextAppRouterBase(TRouter: AnyRouter): any {
  return { use: NextAppRouterUse(TRouter) }
}
/* compiles to:
 * export type CreateTRPCNextAppRouterBase<TRouter extends AnyRouter> = {
 *   use: NextAppRouterUse<TRouter>
 * }
 */

// ✓ CreateTRPCNextAppRouter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CreateTRPCNextAppRouter(TRouter: AnyRouter): any {
  return ProtectedIntersection(CreateTRPCNextAppRouterBase(TRouter), UseProcedureRecord(TRouter['_def']['_config']['$types'], TRouter['_def']['record']))
}
/* compiles to:
 * export type CreateTRPCNextAppRouter<TRouter extends AnyRouter> = ProtectedIntersection<
 *   CreateTRPCNextAppRouterBase<TRouter>,
 *   UseProcedureRecord<TRouter['_def']['_config']['$types'], TRouter['_def']['record']>
 * >
 */

// ✓ inferActionDef: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferActionDef(TRoot: AnyClientTypes, TProc: AnyProcedure): any {
  return { input: inferProcedureInput(TProc), output: inferProcedureOutput(TProc), errorShape: TRoot['errorShape'] }
}
/* compiles to:
 * export type inferActionDef<TRoot extends AnyClientTypes, TProc extends AnyProcedure> = {
 *   input: inferProcedureInput<TProc>
 *   output: inferProcedureOutput<TProc>
 *   errorShape: TRoot['errorShape']
 * }
 */

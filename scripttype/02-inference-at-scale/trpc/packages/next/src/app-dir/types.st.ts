/**
 * ScriptType for 02-inference-at-scale/trpc/packages/next/src/app-dir/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Resolver } from '../../../../../../../02-inference-at-scale/trpc/packages/client/src/createTRPCClient.js'
import type { inferTransformedProcedureOutput } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inference.js'
import type { AnyProcedure, ProcedureType, inferProcedureInput } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/procedure.js'
import type { AnyRootTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/rootConfig.js'
import type { RouterRecord } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
import type { ResolverDef } from '../../../../../../../02-inference-at-scale/trpc/packages/tanstack-react-query/src/internals/types.js'
declare namespace m1 {
  export type $Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AnyProcedure: any
declare const AnyRootTypes: any
declare const ProcedureType: any
declare const Resolver: any
declare const ResolverDef: any
declare const RouterRecord: any
declare const inferProcedureInput: any
declare const inferTransformedProcedureOutput: any
// ✓ DecorateProcedureServer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateProcedureServer(TType: ProcedureType, TDef: ResolverDef): any {
  if (matches<'query'>(TType)) {
    return { query: Resolver(TDef), revalidate: fnType([optElem(TDef['input'])], t<Promise<{ revalidated: false; error: string; } | { revalidated: true; }>>()) }
  }
  if (matches<'mutation'>(TType)) {
    return { mutate: Resolver(TDef) }
  }
  if (matches<'subscription'>(TType)) {
    return { subscribe: Resolver(TDef) }
  }
  return never
}
/* compiles to:
 * export type DecorateProcedureServer<TType extends ProcedureType, TDef extends ResolverDef> =
 *   TType extends 'query'
 *     ? {
 *       query: Resolver<TDef>
 *       revalidate: (a0?: TDef['input']) => Promise<
 *         { revalidated: false; error: string; } | { revalidated: true; }
 *       >
 *     }
 *   : TType extends 'mutation' ? { mutate: Resolver<TDef> }
 *   : TType extends 'subscription' ? { subscribe: Resolver<TDef> }
 *   : never
 */

// ✓ NextAppDirDecorateRouterRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NextAppDirDecorateRouterRecord__v1(TRoot, TRecord, TKey): any {
  const m1 = matches<Hole<"$Value">>(TRecord[TKey])
  if (m1) {
    if (matches<AnyProcedure>(m1.$Value)) {
      return DecorateProcedureServer(m1.$Value['_def']['type'], { input: inferProcedureInput(m1.$Value), output: inferTransformedProcedureOutput(TRoot, m1.$Value), errorShape: TRoot['errorShape'], transformer: TRoot['transformer'] })
    }
    if (matches<RouterRecord>(m1.$Value)) {
      return NextAppDirDecorateRouterRecord(TRoot, m1.$Value)
    }
    return never
  }
  return never
}
/**
 * @param {RouterRecord} TRecord
 */
export function NextAppDirDecorateRouterRecord(TRoot: AnyRootTypes, TRecord): any {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = NextAppDirDecorateRouterRecord__v1(TRoot, TRecord, TKey)
  }
  return out
}
/* compiles to:
 * export type NextAppDirDecorateRouterRecord__v1<TRoot, TRecord, TKey> =
 *   TRecord[TKey] extends infer $Value
 *     ? $Value extends AnyProcedure
 *       ? DecorateProcedureServer<
 *         $Value['_def']['type'],
 *         {
 *           input: inferProcedureInput<$Value>
 *           output: inferTransformedProcedureOutput<TRoot, $Value>
 *           errorShape: TRoot['errorShape']
 *           transformer: TRoot['transformer']
 *         }
 *       >
 *     : $Value extends RouterRecord ? NextAppDirDecorateRouterRecord<TRoot, $Value>
 *     : never
 *     : never
 * export type NextAppDirDecorateRouterRecord<
 *   TRoot extends AnyRootTypes,
 *   TRecord extends RouterRecord
 * > =
 *   { [TKey in keyof TRecord]: NextAppDirDecorateRouterRecord__v1<TRoot, TRecord, TKey> }
 */

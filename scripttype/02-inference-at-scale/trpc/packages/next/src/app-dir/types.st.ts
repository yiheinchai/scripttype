/**
 * ScriptType for 02-inference-at-scale/trpc/packages/next/src/app-dir/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyRootTypes: any
declare const ProcedureType: any
declare const Resolver: any
declare const ResolverDef: any
declare const RouterRecord: any
type AnyRootTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ProcedureType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Resolver<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResolverDef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RouterRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ DecorateProcedureServer: the ScriptType does not itself typecheck as TypeScript
//   DecorateProcedureServer.st.ts(4:73) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function DecorateProcedureServer(TType: ProcedureType, TDef: ResolverDef) {
  if (matches<'query'>(TType)) {
    return { query: Resolver(TDef), revalidate: fnType([TDef['input']], Promise(anyOf({ revalidated: false, error: string }, { revalidated: true }))) }
  }
  if (matches<'mutation'>(TType)) {
    return { mutate: Resolver(TDef) }
  }
  if (matches<'subscription'>(TType)) {
    return { subscribe: Resolver(TDef) }
  }
  return never
}
/* compiles to:
 * export type DecorateProcedureServer<TType extends ProcedureType, TDef extends ResolverDef> = TType extends 'query' ? { query: Resolver<TDef>; revalidate: (a0: TDef['input']) => Promise<{ revalidated: false; error: string } | { revalidated: true }> } : TType extends 'mutation' ? { mutate: Resolver<TDef> } : TType extends 'subscription' ? { subscribe: Resolver<TDef> } : never
 */

// ✗ NextAppDirDecorateRouterRecord: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function NextAppDirDecorateRouterRecord(TRoot: AnyRootTypes, TRecord: RouterRecord) {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = raw('TRecord[TKey] extends infer $Value ? $Value extends AnyProcedure ? DecorateProcedureServer<$Value[\'_def\'][\'type\'], { input: inferProcedureInput<$Value>; output: inferTransformedProcedureOutput<TRoot, $Value>; errorShape: TRoot[\'errorShape\']; transformer: TRoot[\'transformer\']; }> : $Value extends RouterRecord ? NextAppDirDecorateRouterRecord<TRoot, $Value> : never : never')
  }
  return out
}

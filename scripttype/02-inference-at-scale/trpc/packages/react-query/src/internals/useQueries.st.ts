/**
 * ScriptType for 02-inference-at-scale/trpc/packages/react-query/src/internals/useQueries.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 12 type-level aliases: 8 covered, 4 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyRouter: any
declare const DistributiveOmit: any
declare const QueryKey: any
declare const UseQueryOptions: any
declare const UseSuspenseQueryOptions: any
declare const UseTRPCQueryOptions: any
declare const UseTRPCSuspenseQueryOptions: any
type AnyRouter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DistributiveOmit<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type QueryKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseQueryOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseQueryOptionsForUseQueries<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseQueryOptionsForUseSuspenseQueries<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseSuspenseQueryOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseTRPCQueryOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UseTRPCSuspenseQueryOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ UseQueryOptionsForUseQueries: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseQueryOptionsForUseQueries(TQueryFnData = unknown, TError = unknown, TData = TQueryFnData, TQueryKey: QueryKey = QueryKey) {
  return DistributiveOmit(UseQueryOptions(TQueryFnData, TError, TData, TQueryKey), 'queryKey')
}
/* compiles to:
 * export type UseQueryOptionsForUseQueries<TQueryFnData = unknown, TError = unknown, TData = TQueryFnData, TQueryKey extends QueryKey = QueryKey> = DistributiveOmit<UseQueryOptions<TQueryFnData, TError, TData, TQueryKey>, 'queryKey'>
 */

// ✓ UseQueryOptionsForUseSuspenseQueries: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UseQueryOptionsForUseSuspenseQueries(TQueryFnData = unknown, TError = unknown, TData = TQueryFnData, TQueryKey: QueryKey = QueryKey) {
  return DistributiveOmit(UseSuspenseQueryOptions(TQueryFnData, TError, TData, TQueryKey), 'queryKey')
}
/* compiles to:
 * export type UseQueryOptionsForUseSuspenseQueries<TQueryFnData = unknown, TError = unknown, TData = TQueryFnData, TQueryKey extends QueryKey = QueryKey> = DistributiveOmit<UseSuspenseQueryOptions<TQueryFnData, TError, TData, TQueryKey>, 'queryKey'>
 */

// ✓ TrpcQueryOptionsForUseQueries: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrpcQueryOptionsForUseQueries(TOutput, TData, TError) {
  return DistributiveOmit(UseTRPCQueryOptions(TOutput, TData, TError), 'queryKey')
}
/* compiles to:
 * export type TrpcQueryOptionsForUseQueries<TOutput, TData, TError> = DistributiveOmit<UseTRPCQueryOptions<TOutput, TData, TError>, 'queryKey'>
 */

// ✓ TrpcQueryOptionsForUseSuspenseQueries: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TrpcQueryOptionsForUseSuspenseQueries(TOutput, TData, TError) {
  return DistributiveOmit(UseTRPCSuspenseQueryOptions(TOutput, TData, TError), 'queryKey')
}
/* compiles to:
 * export type TrpcQueryOptionsForUseSuspenseQueries<TOutput, TData, TError> = DistributiveOmit<UseTRPCSuspenseQueryOptions<TOutput, TData, TError>, 'queryKey'>
 */

// ✗ QueriesResults: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function QueriesResults(TQueriesOptions: UseQueryOptionsForUseQueries<any, any, any, any>[]) {
  const out = emptyObject
  for (const TKey in keyof(TQueriesOptions)) {
    out[TKey] = raw('TQueriesOptions[TKey] extends UseQueryOptionsForUseQueries<infer TQueryFnData, infer TError, infer TData, any> ? UseTRPCQueryResult<unknown extends TData ? TQueryFnData : TData, TError> : never')
  }
  return out
}

// ✗ SuspenseQueriesResults: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function SuspenseQueriesResults(TQueriesOptions: UseQueryOptionsForUseSuspenseQueries<any, any, any, any>[]) {
  const out = emptyObject
  for (const TKey in keyof(TQueriesOptions)) {
    out[TKey] = raw('TQueriesOptions[TKey] extends UseQueryOptionsForUseSuspenseQueries<infer TQueryFnData, any, infer TData, any> ? unknown extends TData ? TQueryFnData : TData : never')
  }
  const out2 = emptyObject
  for (const TKey in keyof(TQueriesOptions)) {
    out2[TKey] = raw('TQueriesOptions[TKey] extends UseQueryOptionsForUseSuspenseQueries<infer TQueryFnData, infer TError, infer TData, any> ? UseSuspenseQueryResult<unknown extends TData ? TQueryFnData : TData, TError> : never')
  }
  return [out, out2]
}

// ✓ GetOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetOptions(TQueryOptions) {
  if (matches<UseQueryOptionsForUseQueries<any, any, any, any>>(TQueryOptions)) {
    return TQueryOptions
  }
  return never
}
/* compiles to:
 * export type GetOptions<TQueryOptions> = TQueryOptions extends UseQueryOptionsForUseQueries<any, any, any, any> ? TQueryOptions : never
 */

// ✓ QueriesOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueriesOptions(TQueriesOptions: any[], TResult: any[] = []) {
  if (matches<[ ]>(TQueriesOptions)) {
    return []
  }
  const m1 = matches<[ Hole<"Head"> ]>(TQueriesOptions)
  if (m1) {
    return [...TResult, GetOptions(m1.Head)]
  }
  const m2 = matches<[ Hole<"Head">, ...Hole<"Tail"> ]>(TQueriesOptions)
  if (m2) {
    return QueriesOptions(m2.Tail, [...TResult, GetOptions(m2.Head)])
  }
  if (matches<typeof TQueriesOptions>(arrayOf(unknown))) {
    return TQueriesOptions
  }
  const m3 = matches<UseQueryOptionsForUseQueries<Hole<"TQueryFnData">, Hole<"TError">, Hole<"TData">, Hole<"TQueryKey">>[]>(TQueriesOptions)
  if (m3) {
    return arrayOf(UseQueryOptionsForUseQueries(m3.TQueryFnData, m3.TError, m3.TData, m3.TQueryKey))
  }
  return arrayOf(UseQueryOptionsForUseQueries)
}
/* compiles to:
 * export type QueriesOptions<TQueriesOptions extends any[], TResult extends any[] = []> = TQueriesOptions extends [] ? [] : TQueriesOptions extends [infer Head] ? [...TResult, GetOptions<Head>] : TQueriesOptions extends [infer Head, ...infer Tail] ? QueriesOptions<Tail, [...TResult, GetOptions<Head>]> : unknown[] extends TQueriesOptions ? TQueriesOptions : TQueriesOptions extends UseQueryOptionsForUseQueries<infer TQueryFnData, infer TError, infer TData, infer TQueryKey>[] ? UseQueryOptionsForUseQueries<TQueryFnData, TError, TData, TQueryKey>[] : UseQueryOptionsForUseQueries[]
 */

// ✓ GetSuspenseOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetSuspenseOptions(TQueryOptions) {
  if (matches<UseQueryOptionsForUseSuspenseQueries<any, any, any, any>>(TQueryOptions)) {
    return TQueryOptions
  }
  return never
}
/* compiles to:
 * export type GetSuspenseOptions<TQueryOptions> = TQueryOptions extends UseQueryOptionsForUseSuspenseQueries<any, any, any, any> ? TQueryOptions : never
 */

// ✓ SuspenseQueriesOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuspenseQueriesOptions(TQueriesOptions: any[], TResult: any[] = []) {
  if (matches<[ ]>(TQueriesOptions)) {
    return []
  }
  const m1 = matches<[ Hole<"Head"> ]>(TQueriesOptions)
  if (m1) {
    return [...TResult, GetSuspenseOptions(m1.Head)]
  }
  const m2 = matches<[ Hole<"Head">, ...Hole<"Tail"> ]>(TQueriesOptions)
  if (m2) {
    return SuspenseQueriesOptions(m2.Tail, [...TResult, GetSuspenseOptions(m2.Head)])
  }
  if (matches<typeof TQueriesOptions>(arrayOf(unknown))) {
    return TQueriesOptions
  }
  const m3 = matches<UseQueryOptionsForUseSuspenseQueries<Hole<"TQueryFnData">, Hole<"TError">, Hole<"TData">, Hole<"TQueryKey">>[]>(TQueriesOptions)
  if (m3) {
    return arrayOf(UseQueryOptionsForUseSuspenseQueries(m3.TQueryFnData, m3.TError, m3.TData, m3.TQueryKey))
  }
  return arrayOf(UseQueryOptionsForUseSuspenseQueries)
}
/* compiles to:
 * export type SuspenseQueriesOptions<TQueriesOptions extends any[], TResult extends any[] = []> = TQueriesOptions extends [] ? [] : TQueriesOptions extends [infer Head] ? [...TResult, GetSuspenseOptions<Head>] : TQueriesOptions extends [infer Head, ...infer Tail] ? SuspenseQueriesOptions<Tail, [...TResult, GetSuspenseOptions<Head>]> : unknown[] extends TQueriesOptions ? TQueriesOptions : TQueriesOptions extends UseQueryOptionsForUseSuspenseQueries<infer TQueryFnData, infer TError, infer TData, infer TQueryKey>[] ? UseQueryOptionsForUseSuspenseQueries<TQueryFnData, TError, TData, TQueryKey>[] : UseQueryOptionsForUseSuspenseQueries[]
 */

// ✗ TRPCUseQueries: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function TRPCUseQueries(TRouter: AnyRouter) {
  return raw('<TQueryOptions extends UseQueryOptionsForUseQueries<any, any, any, any>[], TCombinedResult = QueriesResults<TQueryOptions>>(queriesCallback: (t: UseQueriesProcedureRecord<TRouter[\'_def\'][\'_config\'][\'$types\'], TRouter[\'_def\'][\'record\']>) => readonly [ ...QueriesOptions<TQueryOptions> ], options?: { combine?: (results: QueriesResults<TQueryOptions>) => TCombinedResult; }) => TCombinedResult')
}

// ✗ TRPCUseSuspenseQueries: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function TRPCUseSuspenseQueries(TRouter: AnyRouter) {
  return raw('<TQueryOptions extends UseQueryOptionsForUseSuspenseQueries<any, any, any, any>[]>(queriesCallback: (t: UseSuspenseQueriesProcedureRecord<TRouter[\'_def\'][\'_config\'][\'$types\'], TRouter[\'_def\'][\'record\']>) => readonly [ ...SuspenseQueriesOptions<TQueryOptions> ]) => SuspenseQueriesResults<TQueryOptions>')
}

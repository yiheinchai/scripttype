/**
 * ScriptType for 02-inference-at-scale/trpc/packages/react-query/src/rsc.tsx
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TRPCFetchInfiniteQueryOptions, TRPCFetchQueryOptions } from '../../../../../../02-inference-at-scale/trpc/packages/react-query/src/internals/context.js'
import type { inferTransformedProcedureOutput } from '../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/clientish/inference.js'
import type { AnyProcedure, inferProcedureInput, inferProcedureOutput } from '../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/procedure.js'
import type { AnyRootTypes } from '../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/rootConfig.js'
import type { AnyRouter, RouterCaller, RouterRecord, inferRouterRootTypes } from '../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
declare namespace m1 {
  export type $Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AnyProcedure: any
declare const AnyRootTypes: any
declare const AnyRouter: any
declare const RouterCaller: any
declare const RouterRecord: any
declare const TRPCClientError: any
declare const TRPCFetchInfiniteQueryOptions: any
declare const TRPCFetchQueryOptions: any
declare const inferProcedureInput: any
declare const inferProcedureOutput: any
declare const inferRouterRootTypes: any
declare const inferTransformedProcedureOutput: any
type TRPCClientError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ DecorateProcedure: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateProcedure(TRoot: AnyRootTypes, TProcedure: AnyProcedure): any {
  return { ...callSig([inferProcedureInput(TProcedure)], t<Promise<inferProcedureOutput<typeof TProcedure>>>()), prefetch: fnType([inferProcedureInput(TProcedure), optElem(TRPCFetchQueryOptions(inferTransformedProcedureOutput(TRoot, TProcedure), TRPCClientError(TRoot)))], t<Promise<void>>()), prefetchInfinite: fnType([inferProcedureInput(TProcedure), optElem(TRPCFetchInfiniteQueryOptions(inferProcedureInput(TProcedure), inferTransformedProcedureOutput(TRoot, TProcedure), TRPCClientError(TRoot)))], t<Promise<void>>()) }
}
/* compiles to:
 * export type DecorateProcedure<TRoot extends AnyRootTypes, TProcedure extends AnyProcedure> = {
 *   (a0: inferProcedureInput<TProcedure>): Promise<inferProcedureOutput<TProcedure>>
 *   prefetch: (
 *     a0: inferProcedureInput<TProcedure>,
 *     a1?: TRPCFetchQueryOptions<inferTransformedProcedureOutput<TRoot, TProcedure>, TRPCClientError<TRoot>>
 *   ) => Promise<void>
 *   prefetchInfinite: (
 *     a0: inferProcedureInput<TProcedure>,
 *     a1?: TRPCFetchInfiniteQueryOptions<inferProcedureInput<TProcedure>, inferTransformedProcedureOutput<TRoot, TProcedure>, TRPCClientError<TRoot>>
 *   ) => Promise<void>
 * }
 */

// ✓ DecorateRouterRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateRouterRecord__v1(TRoot, TRecord, TKey): any {
  const m1 = matches<Hole<"$Value">>(TRecord[TKey])
  if (m1) {
    if (matches<AnyProcedure>(m1.$Value)) {
      return DecorateProcedure(TRoot, m1.$Value)
    }
    if (matches<RouterRecord>(m1.$Value)) {
      return DecorateRouterRecord(TRoot, m1.$Value)
    }
    return never
  }
  return never
}
/**
 * @param {RouterRecord} TRecord
 */
export function DecorateRouterRecord(TRoot: AnyRootTypes, TRecord): any {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = DecorateRouterRecord__v1(TRoot, TRecord, TKey)
  }
  return out
}
/* compiles to:
 * export type DecorateRouterRecord__v1<TRoot, TRecord, TKey> =
 *   TRecord[TKey] extends infer $Value
 *     ? $Value extends AnyProcedure ? DecorateProcedure<TRoot, $Value>
 *     : $Value extends RouterRecord ? DecorateRouterRecord<TRoot, $Value>
 *     : never
 *     : never
 * export type DecorateRouterRecord<TRoot extends AnyRootTypes, TRecord extends RouterRecord> = {
 *   [TKey in keyof TRecord]: DecorateRouterRecord__v1<TRoot, TRecord, TKey>
 * }
 */

// ✓ Caller: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Caller(TRouter: AnyRouter): any {
  return ReturnType(RouterCaller(inferRouterRootTypes(TRouter), TRouter['_def']['record']))
}
/* compiles to:
 * export type Caller<TRouter extends AnyRouter> = ReturnType<
 *   RouterCaller<inferRouterRootTypes<TRouter>, TRouter['_def']['record']>
 * >
 */

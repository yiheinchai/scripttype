/**
 * ScriptType for 02-inference-at-scale/trpc/packages/server/src/adapters/node-http/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 6 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { NodeHTTPRequest, NodeHTTPResponse } from '../../../../../../../../02-inference-at-scale/trpc/packages/server/src/adapters/node-http/types.js'
import type { HTTPBaseHandlerOptions, TRPCRequestInfo } from '../../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/http/types.js'
import type { CreateContextCallback } from '../../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/rootConfig.js'
import type { AnyRouter, inferRouterContext } from '../../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
import type { MaybePromise } from '../../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/types.js'
declare const AnyRouter: any
declare const CreateContextCallback: any
declare const HTTPBaseHandlerOptions: any
declare const MaybePromise: any
declare const NodeHTTPRequest: any
declare const NodeHTTPResponse: any
declare const TRPCRequestInfo: any
declare const inferRouterContext: any
// ✓ NodeHTTPCreateContextOption: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NodeHTTPCreateContextOption(TRouter: AnyRouter, TRequest, TResponse): any {
  return CreateContextCallback(inferRouterContext(TRouter), NodeHTTPCreateContextFn(TRouter, TRequest, TResponse))
}
/* compiles to:
 * export type NodeHTTPCreateContextOption<TRouter extends AnyRouter, TRequest, TResponse> = CreateContextCallback<
 *   inferRouterContext<TRouter>,
 *   NodeHTTPCreateContextFn<TRouter, TRequest, TResponse>
 * >
 */

// ✓ ConnectMiddleware: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConnectMiddleware(TRequest: NodeHTTPRequest = NodeHTTPRequest, TResponse: NodeHTTPResponse = NodeHTTPResponse): any {
  return fnType([TRequest, TResponse, fnType([optElem(any)], any)], voidType())
}
/* compiles to:
 * export type ConnectMiddleware<
 *   TRequest extends NodeHTTPRequest = NodeHTTPRequest,
 *   TResponse extends NodeHTTPResponse = NodeHTTPResponse
 * > =
 *   (a0: TRequest, a1: TResponse, a2: (a0?: any) => any) => void
 */

// ✓ NodeHTTPHandlerOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NodeHTTPHandlerOptions(TRouter: AnyRouter, TRequest: NodeHTTPRequest, TResponse: NodeHTTPResponse): any {
  return merge(HTTPBaseHandlerOptions(TRouter, TRequest), NodeHTTPCreateContextOption(TRouter, TRequest, TResponse), { middleware: optional(ConnectMiddleware(TRequest, TResponse)), maxBodySize: optional(number) })
}
/* compiles to:
 * export type NodeHTTPHandlerOptions<
 *   TRouter extends AnyRouter,
 *   TRequest extends NodeHTTPRequest,
 *   TResponse extends NodeHTTPResponse
 * > =
 *   & HTTPBaseHandlerOptions<TRouter, TRequest>
 *   & NodeHTTPCreateContextOption<TRouter, TRequest, TResponse>
 *   & { middleware?: ConnectMiddleware<TRequest, TResponse>; maxBodySize?: number }
 */

// ✓ NodeHTTPRequestHandlerOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NodeHTTPRequestHandlerOptions(TRouter: AnyRouter, TRequest: NodeHTTPRequest, TResponse: NodeHTTPResponse): any {
  return merge(NodeHTTPHandlerOptions(TRouter, TRequest, TResponse), { req: TRequest, res: TResponse, path: string })
}
/* compiles to:
 * export type NodeHTTPRequestHandlerOptions<
 *   TRouter extends AnyRouter,
 *   TRequest extends NodeHTTPRequest,
 *   TResponse extends NodeHTTPResponse
 * > =
 *   & NodeHTTPHandlerOptions<TRouter, TRequest, TResponse>
 *   & { req: TRequest; res: TResponse; path: string }
 */

// ✓ NodeHTTPCreateContextFnOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NodeHTTPCreateContextFnOptions(TRequest, TResponse): any {
  return { req: TRequest, res: TResponse, info: TRPCRequestInfo }
}
/* compiles to:
 * export type NodeHTTPCreateContextFnOptions<TRequest, TResponse> = {
 *   req: TRequest
 *   res: TResponse
 *   info: TRPCRequestInfo
 * }
 */

// ✓ NodeHTTPCreateContextFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NodeHTTPCreateContextFn(TRouter: AnyRouter, TRequest, TResponse): any {
  return fnType([NodeHTTPCreateContextFnOptions(TRequest, TResponse)], MaybePromise(inferRouterContext(TRouter)))
}
/* compiles to:
 * export type NodeHTTPCreateContextFn<TRouter extends AnyRouter, TRequest, TResponse> =
 *   (a0: NodeHTTPCreateContextFnOptions<TRequest, TResponse>) => MaybePromise<
 *     inferRouterContext<TRouter>
 *   >
 */

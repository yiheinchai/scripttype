/**
 * ScriptType for 02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/parser.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 13 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Parser } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/parser.js'
import type { StandardSchemaV1 } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/vendor/standard-schema-v1/spec.js'
declare namespace m1 {
  export type $TIn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type $TOut<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type $TIn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type $TOut<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type $InOut<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Parser: any
declare const StandardSchemaV1: any
type ParserStandardSchemaEsque<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParserWithInputOutput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParserWithoutInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ParserZodEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserZodEsque(TInput, TParsedInput): any {
  return { _input: TInput, _output: TParsedInput }
}
/* compiles to:
 * export type ParserZodEsque<TInput, TParsedInput> = { _input: TInput; _output: TParsedInput }
 */

// ✓ ParserValibotEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserValibotEsque(TInput, TParsedInput): any {
  return { schema: { _types: optional({ input: TInput, output: TParsedInput }) } }
}
/* compiles to:
 * export type ParserValibotEsque<TInput, TParsedInput> = {
 *   schema: { _types?: { input: TInput; output: TParsedInput } }
 * }
 */

// ✓ ParserArkTypeEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserArkTypeEsque(TInput, TParsedInput): any {
  return { inferIn: TInput, infer: TParsedInput }
}
/* compiles to:
 * export type ParserArkTypeEsque<TInput, TParsedInput> = { inferIn: TInput; infer: TParsedInput }
 */

// ✓ ParserStandardSchemaEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserStandardSchemaEsque(TInput, TParsedInput): any {
  return StandardSchemaV1(TInput, TParsedInput)
}
/* compiles to:
 * export type ParserStandardSchemaEsque<TInput, TParsedInput> = StandardSchemaV1<
 *   TInput,
 *   TParsedInput
 * >
 */

// ✓ ParserMyZodEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserMyZodEsque(TInput): any {
  return { parse: fnType([any], TInput) }
}
/* compiles to:
 * export type ParserMyZodEsque<TInput> = { parse: (a0: any) => TInput }
 */

// ✓ ParserSuperstructEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserSuperstructEsque(TInput): any {
  return { create: fnType([unknown], TInput) }
}
/* compiles to:
 * export type ParserSuperstructEsque<TInput> = { create: (a0: unknown) => TInput }
 */

// ✓ ParserCustomValidatorEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserCustomValidatorEsque(TInput): any {
  return fnType([unknown], t<Promise<typeof TInput>>() | TInput)
}
/* compiles to:
 * export type ParserCustomValidatorEsque<TInput> = (a0: unknown) => Promise<TInput> | TInput
 */

// ✓ ParserYupEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserYupEsque(TInput): any {
  return { validateSync: fnType([unknown], TInput) }
}
/* compiles to:
 * export type ParserYupEsque<TInput> = { validateSync: (a0: unknown) => TInput }
 */

// ✓ ParserScaleEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserScaleEsque(TInput): any {
  return { assert: methodType([unknown], paramAsserts(0, TInput)) }
}
/* compiles to:
 * export type ParserScaleEsque<TInput> = { assert(a0: unknown): asserts a0 is TInput }
 */

// ✓ ParserWithoutInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserWithoutInput(TInput): any {
  return ParserCustomValidatorEsque(TInput) | ParserMyZodEsque(TInput) | ParserScaleEsque(TInput) | ParserSuperstructEsque(TInput) | ParserYupEsque(TInput)
}
/* compiles to:
 * export type ParserWithoutInput<TInput> =
 *   | ParserCustomValidatorEsque<TInput>
 *   | ParserMyZodEsque<TInput>
 *   | ParserScaleEsque<TInput>
 *   | ParserSuperstructEsque<TInput>
 *   | ParserYupEsque<TInput>
 */

// ✓ ParserWithInputOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserWithInputOutput(TInput, TParsedInput): any {
  return ParserZodEsque(TInput, TParsedInput) | ParserValibotEsque(TInput, TParsedInput) | ParserArkTypeEsque(TInput, TParsedInput) | ParserStandardSchemaEsque(TInput, TParsedInput)
}
/* compiles to:
 * export type ParserWithInputOutput<TInput, TParsedInput> =
 *   | ParserZodEsque<TInput, TParsedInput>
 *   | ParserValibotEsque<TInput, TParsedInput>
 *   | ParserArkTypeEsque<TInput, TParsedInput>
 *   | ParserStandardSchemaEsque<TInput, TParsedInput>
 */

// ✓ inferParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferParser(TParser: Parser): any {
  const m1 = matches<ParserStandardSchemaEsque<Hole<"$TIn">, Hole<"$TOut">>>(TParser)
  if (m1) {
    return { in: m1.$TIn, out: m1.$TOut }
  }
  const m2 = matches<ParserWithInputOutput<Hole<"$TIn">, Hole<"$TOut">>>(TParser)
  if (m2) {
    return { in: m2.$TIn, out: m2.$TOut }
  }
  const m3 = matches<ParserWithoutInput<Hole<"$InOut">>>(TParser)
  if (m3) {
    return { in: m3.$InOut, out: m3.$InOut }
  }
  return never
}
/* compiles to:
 * export type inferParser<TParser extends Parser> =
 *   TParser extends ParserStandardSchemaEsque<infer $TIn, infer $TOut> ? { in: $TIn; out: $TOut }
 *   : TParser extends ParserWithInputOutput<infer $TIn, infer $TOut> ? { in: $TIn; out: $TOut }
 *   : TParser extends ParserWithoutInput<infer $InOut> ? { in: $InOut; out: $InOut }
 *   : never
 */

// ✓ ParseFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseFn(TType): any {
  return fnType([unknown], t<Promise<typeof TType>>() | TType)
}
/* compiles to:
 * export type ParseFn<TType> = (a0: unknown) => Promise<TType> | TType
 */

/**
 * ScriptType for 02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/parser.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 10 covered, 2 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Parser: any
declare const StandardSchemaV1: any
type Parser<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParserStandardSchemaEsque<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParserWithInputOutput<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParserWithoutInput<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StandardSchemaV1<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ParserZodEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserZodEsque(TInput, TParsedInput) {
  return { _input: TInput, _output: TParsedInput }
}
/* compiles to:
 * export type ParserZodEsque<TInput, TParsedInput> = { _input: TInput; _output: TParsedInput }
 */

// ✓ ParserValibotEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserValibotEsque(TInput, TParsedInput) {
  return { schema: { _types: optional({ input: TInput, output: TParsedInput }) } }
}
/* compiles to:
 * export type ParserValibotEsque<TInput, TParsedInput> = { schema: { _types?: { input: TInput; output: TParsedInput } } }
 */

// ✓ ParserArkTypeEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserArkTypeEsque(TInput, TParsedInput) {
  return { inferIn: TInput, infer: TParsedInput }
}
/* compiles to:
 * export type ParserArkTypeEsque<TInput, TParsedInput> = { inferIn: TInput; infer: TParsedInput }
 */

// ✓ ParserStandardSchemaEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserStandardSchemaEsque(TInput, TParsedInput) {
  return StandardSchemaV1(TInput, TParsedInput)
}
/* compiles to:
 * export type ParserStandardSchemaEsque<TInput, TParsedInput> = StandardSchemaV1<TInput, TParsedInput>
 */

// ✓ ParserMyZodEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserMyZodEsque(TInput) {
  return { parse: fnType([any], TInput) }
}
/* compiles to:
 * export type ParserMyZodEsque<TInput> = { parse: (a0: any) => TInput }
 */

// ✓ ParserSuperstructEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserSuperstructEsque(TInput) {
  return { create: fnType([unknown], TInput) }
}
/* compiles to:
 * export type ParserSuperstructEsque<TInput> = { create: (a0: unknown) => TInput }
 */

// ✗ ParserCustomValidatorEsque: the ScriptType does not itself typecheck as TypeScript
//   ParserCustomValidatorEsque.st.ts(3:28) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function ParserCustomValidatorEsque(TInput) {
  return fnType([unknown], Promise(TInput) | TInput)
}
/* compiles to:
 * export type ParserCustomValidatorEsque<TInput> = (a0: unknown) => Promise<TInput> | TInput
 */

// ✓ ParserYupEsque: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserYupEsque(TInput) {
  return { validateSync: fnType([unknown], TInput) }
}
/* compiles to:
 * export type ParserYupEsque<TInput> = { validateSync: (a0: unknown) => TInput }
 */

// ✗ ParserScaleEsque: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function ParserScaleEsque(TInput) {
  return raw('{ assert(value: unknown): asserts value is TInput; }')
}

// ✓ ParserWithoutInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserWithoutInput(TInput) {
  return ParserCustomValidatorEsque(TInput) | ParserMyZodEsque(TInput) | ParserScaleEsque(TInput) | ParserSuperstructEsque(TInput) | ParserYupEsque(TInput)
}
/* compiles to:
 * export type ParserWithoutInput<TInput> = ParserCustomValidatorEsque<TInput> | ParserMyZodEsque<TInput> | ParserScaleEsque<TInput> | ParserSuperstructEsque<TInput> | ParserYupEsque<TInput>
 */

// ✓ ParserWithInputOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParserWithInputOutput(TInput, TParsedInput) {
  return ParserZodEsque(TInput, TParsedInput) | ParserValibotEsque(TInput, TParsedInput) | ParserArkTypeEsque(TInput, TParsedInput) | ParserStandardSchemaEsque(TInput, TParsedInput)
}
/* compiles to:
 * export type ParserWithInputOutput<TInput, TParsedInput> = ParserZodEsque<TInput, TParsedInput> | ParserValibotEsque<TInput, TParsedInput> | ParserArkTypeEsque<TInput, TParsedInput> | ParserStandardSchemaEsque<TInput, TParsedInput>
 */

// ✓ inferParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferParser(TParser: Parser) {
  const m1 = matches<ParserStandardSchemaEsque<Hole<"$TIn">, Hole<"$TOut">>>(TParser)
  if (m1) {
    return { in: m1.$TIn, out: m1.$TOut }
  }
  const m2 = matches<ParserWithInputOutput<Hole<"$TIn">, Hole<"$TOut">>>(TParser)
  if (m2) {
    return { in: m2.$TIn, out: m2.$TOut }
  }
  const m3 = matches<ParserWithoutInput<Hole<"$InOut">>>(TParser)
  if (m3) {
    return { in: m3.$InOut, out: m3.$InOut }
  }
  return never
}
/* compiles to:
 * export type inferParser<TParser extends Parser> = TParser extends ParserStandardSchemaEsque<infer $TIn, infer $TOut> ? { in: $TIn; out: $TOut } : TParser extends ParserWithInputOutput<infer $TIn, infer $TOut> ? { in: $TIn; out: $TOut } : TParser extends ParserWithoutInput<infer $InOut> ? { in: $InOut; out: $InOut } : never
 */

// ✗ ParseFn: the ScriptType does not itself typecheck as TypeScript
//   ParseFn.st.ts(3:28) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function ParseFn(TType) {
  return fnType([unknown], Promise(TType) | TType)
}
/* compiles to:
 * export type ParseFn<TType> = (a0: unknown) => Promise<TType> | TType
 */

/**
 * ScriptType for 02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 13 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AbortSignal } from '../../../../../../../02-inference-at-scale/trpc/packages/openapi/test/routers/nodeModulesJsDocRouter-heyapi/types.gen.js'
import type { Observable } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/observable/types.js'
import type { AnyProcedure, ErrorHandlerOptions, inferProcedureInput, inferProcedureOutput } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/procedure.js'
import type { AnyRootTypes } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/rootConfig.js'
import type { AnyRouter, CreateRouterOptions, Router, RouterRecord } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/router.js'
import type { MaybePromise } from '../../../../../../../02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/types.js'
declare namespace m1 {
  export type $Value<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type TRecord<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type TRecord<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AbortSignal: any
declare const AnyProcedure: any
declare const AnyRootTypes: any
declare const AnyRouter: any
declare const CreateRouterOptions: any
declare const ErrorHandlerOptions: any
declare const LegacyObservableSubscriptionProcedure: any
declare const MaybePromise: any
declare const Observable: any
declare const Router: any
declare const RouterRecord: any
declare const TRPCError: any
declare const inferProcedureInput: any
declare const inferProcedureOutput: any
declare const lazyMarker: any
type Lazy<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type LegacyObservableSubscriptionProcedure<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TRPCError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type lazyMarker<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ DecorateProcedure: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateProcedure(TProcedure: AnyProcedure): any {
  return fnType([inferProcedureInput(TProcedure)], t<Promise<(typeof TProcedure)['_def']['type'] extends 'subscription' ? typeof TProcedure extends LegacyObservableSubscriptionProcedure<any> ? Observable<inferProcedureOutput<typeof TProcedure>, TRPCError> : inferProcedureOutput<typeof TProcedure> : inferProcedureOutput<typeof TProcedure>>>())
}
/* compiles to:
 * export type DecorateProcedure<TProcedure extends AnyProcedure> =
 *   (a0: inferProcedureInput<TProcedure>) => Promise<
 *     (TProcedure)['_def']['type'] extends 'subscription' ? TProcedure extends LegacyObservableSubscriptionProcedure<any> ? Observable<inferProcedureOutput<TProcedure>, TRPCError> : inferProcedureOutput<TProcedure> : inferProcedureOutput<TProcedure>
 *   >
 */

// ✓ DecorateRouterRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateRouterRecord__v1(TRecord, TKey): any {
  const m1 = matches<Hole<"$Value">>(TRecord[TKey])
  if (m1) {
    if (matches<AnyProcedure>(m1.$Value)) {
      return DecorateProcedure(m1.$Value)
    }
    if (matches<RouterRecord>(m1.$Value)) {
      return DecorateRouterRecord(m1.$Value)
    }
    return never
  }
  return never
}
/**
 * @param {RouterRecord} TRecord
 */
export function DecorateRouterRecord(TRecord): any {
  const out = emptyObject
  for (const TKey in keyof(TRecord)) {
    out[TKey] = DecorateRouterRecord__v1(TRecord, TKey)
  }
  return out
}
/* compiles to:
 * export type DecorateRouterRecord__v1<TRecord, TKey> =
 *   TRecord[TKey] extends infer $Value
 *     ? $Value extends AnyProcedure ? DecorateProcedure<$Value>
 *     : $Value extends RouterRecord ? DecorateRouterRecord<$Value>
 *     : never
 *     : never
 * export type DecorateRouterRecord<TRecord extends RouterRecord> = {
 *   [TKey in keyof TRecord]: DecorateRouterRecord__v1<TRecord, TKey>
 * }
 */

// ✓ RouterCallerErrorHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RouterCallerErrorHandler(TContext): any {
  return fnType([ErrorHandlerOptions(TContext)], voidType())
}
/* compiles to:
 * export type RouterCallerErrorHandler<TContext> = (a0: ErrorHandlerOptions<TContext>) => void
 */

// ✓ RouterCaller: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RouterCaller(TRoot: AnyRootTypes, TRecord: RouterRecord): any {
  return fnType([anyOf(TRoot['ctx'], fnType([], MaybePromise(TRoot['ctx']))), optElem({ onError: optional(RouterCallerErrorHandler(TRoot['ctx'])), signal: optional(AbortSignal) })], DecorateRouterRecord(TRecord))
}
/* compiles to:
 * export type RouterCaller<TRoot extends AnyRootTypes, TRecord extends RouterRecord> =
 *   (
 *     a0: TRoot['ctx'] | (() => MaybePromise<TRoot['ctx']>),
 *     a1?: { onError?: RouterCallerErrorHandler<TRoot['ctx']>; signal?: AbortSignal }
 *   ) => DecorateRouterRecord<TRecord>
 */

// ✓ Lazy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Lazy(TAny): any {
  return merge(fnType([], t<Promise<typeof TAny>>()), { [lazyMarker]: true })
}
/* compiles to:
 * export type Lazy<TAny> = (() => Promise<TAny>) & { [lazyMarker]: true }
 */

// ✓ LazyLoader: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LazyLoader(TAny): any {
  return { load: fnType([], t<Promise<void>>()), ref: Lazy(TAny) }
}
/* compiles to:
 * export type LazyLoader<TAny> = { load: () => Promise<void>; ref: Lazy<TAny> }
 */

// ✓ BuiltRouter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BuiltRouter(TRoot: AnyRootTypes, TRecord: RouterRecord): any {
  return merge(Router(TRoot, TRecord), TRecord)
}
/* compiles to:
 * export type BuiltRouter<TRoot extends AnyRootTypes, TRecord extends RouterRecord> =
 *   Router<TRoot, TRecord> & TRecord
 */

// ✓ inferRouterRootTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRouterRootTypes(TRouter: AnyRouter): any {
  return TRouter['_def']['_config']['$types']
}
/* compiles to:
 * export type inferRouterRootTypes<TRouter extends AnyRouter> = TRouter['_def']['_config']['$types']
 */

// ✓ inferRouterContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRouterContext(TRouter: AnyRouter): any {
  return inferRouterRootTypes(TRouter)['ctx']
}
/* compiles to:
 * export type inferRouterContext<TRouter extends AnyRouter> = inferRouterRootTypes<TRouter>['ctx']
 */

// ✓ inferRouterError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRouterError(TRouter: AnyRouter): any {
  return inferRouterRootTypes(TRouter)['errorShape']
}
/* compiles to:
 * export type inferRouterError<TRouter extends AnyRouter> = inferRouterRootTypes<TRouter>['errorShape']
 */

// ✓ inferRouterMeta: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRouterMeta(TRouter: AnyRouter): any {
  return inferRouterRootTypes(TRouter)['meta']
}
/* compiles to:
 * export type inferRouterMeta<TRouter extends AnyRouter> = inferRouterRootTypes<TRouter>['meta']
 */

// ✓ DecorateCreateRouterOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DecorateCreateRouterOptions__v1(TRouterOptions, K): any {
  const m1 = matches<Hole<"$Value">>(TRouterOptions[K])
  if (m1) {
    if (matches<AnyProcedure>(m1.$Value)) {
      return m1.$Value
    }
    const m2 = matches<Router<any, Hole<"TRecord">>>(m1.$Value)
    if (m2) {
      return m2.TRecord
    }
    const m3 = matches<Lazy<Router<any, Hole<"TRecord">>>>(m1.$Value)
    if (m3) {
      return m3.TRecord
    }
    if (matches<CreateRouterOptions>(m1.$Value)) {
      return DecorateCreateRouterOptions(m1.$Value)
    }
    return never
  }
  return never
}
/**
 * @param {CreateRouterOptions} TRouterOptions
 */
export function DecorateCreateRouterOptions(TRouterOptions): any {
  const out = emptyObject
  for (const K in keyof(TRouterOptions)) {
    out[K] = DecorateCreateRouterOptions__v1(TRouterOptions, K)
  }
  return out
}
/* compiles to:
 * export type DecorateCreateRouterOptions__v1<TRouterOptions, K> =
 *   TRouterOptions[K] extends infer $Value
 *     ? $Value extends AnyProcedure ? $Value
 *     : $Value extends Router<any, infer TRecord> ? TRecord
 *     : $Value extends Lazy<Router<any, infer TRecord>> ? TRecord
 *     : $Value extends CreateRouterOptions ? DecorateCreateRouterOptions<$Value>
 *     : never
 *     : never
 * export type DecorateCreateRouterOptions<TRouterOptions extends CreateRouterOptions> = {
 *   [K in keyof TRouterOptions]: DecorateCreateRouterOptions__v1<TRouterOptions, K>
 * }
 */

// ✓ MergeRouters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeRouters(TRouters: AnyRouter[]): any {
  let TRoot = TRouters[0]['_def']['_config']['$types']
  let TRecord = {}
  let tRouters = TRouters
  while (true) {
    const m1 = matches<[ Hole<"Head", AnyRouter>, ...Hole<"Tail", AnyRouter[]> ]>(tRouters)
    if (!m1) {
      break
    }
    tRouters = m1.Tail
    TRecord = merge(m1.Head['_def']['record'], TRecord)
  }
  return BuiltRouter(TRoot, TRecord)
}
/* compiles to:
 * export type MergeRouters<TRouters extends AnyRouter[]> = MergeRouters__loop<TRouters, {}>
 * type MergeRouters__loop<TRouters extends AnyRouter[], TRecord> =
 *   TRouters extends [infer Head extends AnyRouter, ...infer Tail extends AnyRouter[]]
 *     ? MergeRouters__loop<Tail, Head['_def']['record'] & TRecord>
 *     : BuiltRouter<TRoot, TRecord>
 */

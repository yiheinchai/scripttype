/**
 * ScriptType for 02-inference-at-scale/trpc/packages/server/src/unstable-core-do-not-import/stream/tracked.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 raw, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TrackedData: any
declare const TrackedId: any
type TrackedData<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TrackedEnvelope<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TrackedId<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TrackedEnvelope: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TrackedEnvelope(TData) {
  return [TrackedId, TData, raw('typeof trackedSymbol')]
}

// ✓ inferTrackedOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferTrackedOutput(TData) {
  const m1 = matches<TrackedEnvelope<Hole<"$Data">>>(TData)
  if (m1) {
    return TrackedData(m1.$Data)
  }
  return TData
}
/* compiles to:
 * export type inferTrackedOutput<TData> = TData extends TrackedEnvelope<infer $Data> ? TrackedData<$Data> : TData
 */

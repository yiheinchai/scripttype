/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/formatAbi.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Abi } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/abi.js'
import type { FormatAbiItem } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/formatAbiItem.js'
declare const Abi: any
declare const FormatAbiItem: any
// ✓ FormatAbi: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Abi | readonly unknown[]} abi
 */
export function FormatAbi(abi): any {
  if (matches<typeof abi>(Abi)) {
    return readonlyArrayOf(string)
  }
  if (matches<readonly [ ]>(abi)) {
    return never
  }
  if (matches<Abi>(abi)) {
    const out = emptyObject
    for (const key in keyof(abi)) {
      out[key] = FormatAbiItem(abi[key])
    }
    return out
  }
  return readonlyArrayOf(string)
}
/* compiles to:
 * export type FormatAbi<abi extends Abi | readonly unknown[]> =
 *   Abi extends abi ? readonly string[]
 *   : abi extends readonly [] ? never
 *   : abi extends Abi ? { [Key in keyof abi]: FormatAbiItem<abi[Key]> }
 *   : readonly string[]
 */

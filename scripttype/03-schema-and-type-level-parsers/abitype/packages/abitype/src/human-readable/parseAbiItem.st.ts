/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/parseAbiItem.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Abi } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/abi.js'
import type { Signature, Signatures } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/signatures.js'
import type { ParseStructs } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/structs.js'
import type { ParseSignature } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/utils.js'
import type { Filter } from '../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/types.js'
declare namespace m1 {
  export type structs<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type mapped<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type result<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Abi: any
declare const Filter: any
declare const ParseSignature: any
declare const ParseStructs: any
declare const Signature: any
declare const Signatures: any
// ✓ ParseAbiItem: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {string | readonly string[] | readonly unknown[]} signature
 */
export function ParseAbiItem(signature): any {
  const m1 = matches<Hole<"structs">>(ParseStructs(signature))
  const out = emptyObject
  for (const key in keyof(signature)) {
    out[key] = ParseSignature(typeof signature[key] === 'string' ? signature[key] : never, m1.structs)
  }
  const m2 = matches<Hole<"mapped", readonly unknown[]>>(out)
  const m3 = matches<Hole<"result">>(Filter(m2.mapped, never)[0])
  return (typeof signature === 'string' ? (matches<typeof signature>(string) ? Abi[number] : (matches<Signature<typeof signature>>(signature) ? ParseSignature(signature) : never)) : never) | (matches<readonly string[]>(signature) ? (matches<typeof signature>(arrayOf(string)) ? Abi[number] : (matches<Signatures<typeof signature>>(signature) ? (m1 ? (m2 ? (m3 ? (typeof m3.result === 'undefined' ? never : m3.result) : never) : never) : never) : never)) : never)
}
/* compiles to:
 * export type ParseAbiItem<signature extends string | readonly string[] | readonly unknown[]> =
 *   | (
 *       signature extends string
 *         ? string extends signature ? Abi[number]
 *         : signature extends Signature<signature> ? ParseSignature<signature>
 *         : never
 *         : never
 *     )
 *   | (
 *       signature extends readonly string[]
 *         ? string[] extends signature ? Abi[number]
 *         : signature extends Signatures<signature>
 *           ? ParseStructs<signature> extends infer structs
 *             ? { [Key in keyof signature]: ParseSignature<signature[Key] extends string ? signature[Key] : never, structs> } extends (infer mapped extends readonly unknown[])
 *               ? Filter<mapped, never>[0] extends infer result
 *                 ? result extends undefined ? never : result
 *                 : never
 *               : never
 *             : never
 *         : never
 *         : never
 *     )
 */

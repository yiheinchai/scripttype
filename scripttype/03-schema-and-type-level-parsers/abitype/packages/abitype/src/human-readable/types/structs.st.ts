/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/structs.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AbiParameter } from '../../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/abi.js'
import type { StructSignature } from '../../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/signatures.js'
import type { StructLookup } from '../../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/structs.js'
import type { Trim } from '../../../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/types.js'
declare namespace m1 {
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type name<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type properties<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type struct<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type structs<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace result {
  export type push<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AbiParameter: any
declare const ParseAbiParameter: any
declare const StructLookup: any
declare const StructSignature: any
declare const Trim: any
type ParseAbiParameter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ParseStructs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStructs__v1(signature): any {
  const m1 = matches<Hole<"struct", { name: string; }>>(ParseStruct(signature))
  if (m1) {
    return m1.struct['name']
  }
  return never
}
export function ParseStructs(signatures: readonly string[]): any {
  const out = emptyObject
  for (const signature in keySet(signatures[number])) {
    out[ParseStructs__v1(signature)] = ParseStruct(signature)['components']
  }
  const m1 = matches<Hole<"structs", Record<string, readonly (AbiParameter & { type: string; })[]>>>(out)
  if (m1) {
    const out2 = emptyObject
    for (const structName in keyof(m1.structs)) {
      out2[structName] = ResolveStructs(m1.structs[structName], m1.structs)
    }
    return out2
  }
  return never
}
/* compiles to:
 * export type ParseStructs__v1<signature> =
 *   ParseStruct<signature> extends (infer struct extends { name: string; })
 *     ? struct['name']
 *     : never
 * export type ParseStructs<signatures extends readonly string[]> =
 *   { [Signature in signatures[number] as ParseStructs__v1<Signature>]: ParseStruct<Signature>['components'] } extends (infer structs extends Record<string, readonly (AbiParameter & { type: string; })[]>)
 *     ? { [StructName in keyof structs]: ResolveStructs<structs[StructName], structs> }
 *     : never
 */

// ✓ ParseStruct: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStruct(signature: string, structs: StructLookup | unknown = unknown): any {
  const m1 = matches<StructSignature<Hole<"name">, Hole<"properties">>>(signature)
  if (m1) {
    return { name: readonlyProp(Trim(m1.name)), components: readonlyProp(ParseStructProperties(m1.properties, structs)) }
  }
  return never
}
/* compiles to:
 * export type ParseStruct<
 *   signature extends string,
 *   structs extends StructLookup | unknown = unknown
 * > =
 *   signature extends StructSignature<infer name, infer properties>
 *     ? {
 *       readonly name: Trim<name>
 *       readonly components: ParseStructProperties<properties, structs>
 *     }
 *     : never
 */

// ✗ ResolveStructs: the ScriptType does not itself typecheck as TypeScript
//   ResolveStructs.st.ts(6:16) TS2315: Type 'Error' is not generic.
/* @scripttype preserveParamNames */
export function ResolveStructs__v1(abiParameters, structs, keyReferences, key): any {
  const m1 = matches<`${Hole<"head", string & keyof typeof structs>}[${Hole<"tail">}]`>(abiParameters[key]['type'])
  if (m1) {
    if (m1.head in keyReferences) {
      return t<Error<`Circular reference detected. Struct "${(typeof abiParameters)[typeof key]['type']}" is a circular reference.`>>()
    }
    const out = emptyObject
    for (const _ in keySet(m1.head)) {
      out[_] = true
    }
    return { name: readonlyProp(abiParameters[key]['name']), type: readonlyProp(`tuple[${m1.tail}]`), components: readonlyProp(ResolveStructs(structs[m1.head], structs, merge(keyReferences, out))) }
  }
  if (abiParameters[key]['type'] in structs) {
    if (abiParameters[key]['type'] in keyReferences) {
      return t<Error<`Circular reference detected. Struct "${(typeof abiParameters)[typeof key]['type']}" is a circular reference.`>>()
    }
    const out2 = emptyObject
    for (const _ in keySet(abiParameters[key]['type'])) {
      out2[_] = true
    }
    return { name: readonlyProp(abiParameters[key]['name']), type: readonlyProp('tuple'), components: readonlyProp(ResolveStructs(structs[abiParameters[key]['type']], structs, merge(keyReferences, out2))) }
  }
  return abiParameters[key]
}
/**
 * @param {readonly (AbiParameter & { type: string; })[]} abiParameters
 * @param {Record<string, readonly (AbiParameter & { type: string; })[]>} structs
 */
export function ResolveStructs(abiParameters, structs, keyReferences: { [_: string]: unknown; } | unknown = unknown): any {
  const out = emptyObject
  for (const key in keyof(abiParameters)) {
    out[key] = ResolveStructs__v1(abiParameters, structs, keyReferences, key)
  }
  return asReadonly([...out])
}
/* compiles to:
 * export type ResolveStructs__v1<abiParameters, structs, keyReferences, key> =
 *   abiParameters[key]['type'] extends `${infer head extends string & keyof structs}[${infer tail}]`
 *     ? head extends keyof keyReferences
 *       ? Error<
 *         `Circular reference detected. Struct "${abiParameters[key]['type']}" is a circular reference.`
 *       >
 *       : {
 *         readonly name: abiParameters[key]['name']
 *         readonly type: `tuple[${tail}]`
 *         readonly components: ResolveStructs<
 *           structs[head],
 *           structs,
 *           keyReferences & { [_ in head]: true }
 *         >
 *       }
 *   : abiParameters[key]['type'] extends keyof structs
 *     ? abiParameters[key]['type'] extends keyof keyReferences
 *       ? Error<
 *         `Circular reference detected. Struct "${abiParameters[key]['type']}" is a circular reference.`
 *       >
 *       : {
 *         readonly name: abiParameters[key]['name']
 *         readonly type: 'tuple'
 *         readonly components: ResolveStructs<
 *           structs[abiParameters[key]['type']],
 *           structs,
 *           keyReferences & { [_1 in abiParameters[key]['type']]: true }
 *         >
 *       }
 *   : abiParameters[key]
 * export type ResolveStructs<
 *   abiParameters extends readonly (AbiParameter & { type: string; })[],
 *   structs extends Record<string, readonly (AbiParameter & { type: string; })[]>,
 *   keyReferences extends { [_: string]: unknown; } | unknown = unknown
 * > =
 *   readonly [
 *     ...{
 *       [Key in keyof abiParameters]: ResolveStructs__v1<
 *         abiParameters,
 *         structs,
 *         keyReferences,
 *         Key
 *       >
 *     }
 *   ]
 */

// ✓ ParseStructProperties: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStructProperties(signature: string): any {
  let structs = unknown
  let result: any[] = []
  let signature_ = signature
  while (true) {
    const m1 = matches<`${Hole<"head">};${Hole<"tail">}`>(Trim(signature_))
    if (!m1) {
      break
    }
    signature_ = m1.tail
    result.push(ParseAbiParameter(m1.head, { structs: structs }))
  }
  return result
}
/* compiles to:
 * export type ParseStructProperties<signature extends string> = ParseStructProperties__loop<
 *   signature,
 *   [],
 *   unknown
 * >
 * type ParseStructProperties__loop<Signature_ extends string, Result extends any[], Structs> =
 *   Trim<Signature_> extends `${infer head};${infer tail}`
 *     ? ParseStructProperties__loop<
 *       tail,
 *       [...Result, ParseAbiParameter<head, { structs: Structs }>],
 *       Structs
 *     >
 *     : Result
 */

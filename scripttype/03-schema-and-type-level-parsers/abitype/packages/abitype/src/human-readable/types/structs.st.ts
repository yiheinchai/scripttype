/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/structs.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 typecheck-error, 1 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AbiParameter: any
declare const ParseAbiParameter: any
declare const StructLookup: any
declare const StructSignature: any
declare const Trim: any
type AbiParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParseAbiParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StructLookup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StructSignature<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Trim<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ ParseStructs: the ScriptType does not itself typecheck as TypeScript
//   ParseStructs.st.ts(3:9) TS2451: Cannot redeclare block-scoped variable 'm1'.
/* @scripttype preserveParamNames */
export function ParseStructs(signatures: readonly string[]) {
  const m1 = matches<Hole<"struct", { name: string; }>>(ParseStruct(signature))
  const out = emptyObject
  for (const signature in keySet(signatures[number])) {
    out[m1 ? m1.struct['name'] : never] = ParseStruct(signature)['components']
  }
  const m2 = matches<Hole<"structs", Record<string, readonly (AbiParameter & { type: string; })[]>>>(out)
  if (m2) {
    const out = emptyObject
    for (const structName in keyof(m2.structs)) {
      out[structName] = ResolveStructs(m2.structs[structName], m2.structs)
    }
    return out
  }
  return never
}
/* compiles to:
 * export type ParseStructs<signatures extends readonly string[]> = { [Signature in signatures[number] as ParseStruct<signature> extends (infer struct extends { name: string; }) ? struct['name'] : never]: ParseStruct<Signature>['components'] } extends (infer structs extends Record<string, readonly (AbiParameter & { type: string; })[]>) ? { [StructName in keyof structs]: ResolveStructs<structs[StructName], structs> } : never
 */

// ✓ ParseStruct: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStruct(signature: string, structs: StructLookup | unknown = unknown) {
  const m1 = matches<StructSignature<Hole<"name">, Hole<"properties">>>(signature)
  if (m1) {
    return { name: readonlyProp(Trim(m1.name)), components: readonlyProp(ParseStructProperties(m1.properties, structs)) }
  }
  return never
}
/* compiles to:
 * export type ParseStruct<signature extends string, structs extends StructLookup | unknown = unknown> = signature extends StructSignature<infer name, infer properties> ? { readonly name: Trim<name>; readonly components: ParseStructProperties<properties, structs> } : never
 */

// ✗ ResolveStructs: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ResolveStructs(abiParameters: readonly (AbiParameter & { type: string; })[], structs: Record<string, readonly (AbiParameter & { type: string; })[]>, keyReferences: { [_: string]: unknown; } | unknown = unknown) {
  const out = emptyObject
  for (const key in keyof(abiParameters)) {
    out[key] = raw('abiParameters[key][\'type\'] extends `${infer head extends string & keyof structs}[${infer tail}]` ? head extends keyof keyReferences ? Error<`Circular reference detected. Struct "${abiParameters[key][\'type\']}" is a circular reference.`> : { readonly name: abiParameters[key][\'name\']; readonly type: `tuple[${tail}]`; readonly components: ResolveStructs<structs[head], structs, keyReferences & { [_ in head]: true; }>; } : abiParameters[key][\'type\'] extends keyof structs ? abiParameters[key][\'type\'] extends keyof keyReferences ? Error<`Circular reference detected. Struct "${abiParameters[key][\'type\']}" is a circular reference.`> : { readonly name: abiParameters[key][\'name\']; readonly type: \'tuple\'; readonly components: ResolveStructs<structs[abiParameters[key][\'type\']], structs, keyReferences & { [_ in abiParameters[key][\'type\']]: true; }>; } : abiParameters[key]')
  }
  return asReadonly([...out])
}

// ✗ ParseStructProperties: the ScriptType does not itself typecheck as TypeScript
//   ParseStructProperties.st.ts(12:17) TS2345: Argument of type 'any' is not assignable to parameter of type 'never'.
/* @scripttype preserveParamNames */
export function ParseStructProperties(signature: string) {
  let structs = unknown
  let result = []
  let signature_ = signature
  while (true) {
    const m1 = matches<`${Hole<"head">};${Hole<"tail">}`>(Trim(signature_))
    if (!m1) {
      break
    }
    signature_ = m1.tail
    result.push(ParseAbiParameter(m1.head, { structs: structs }))
  }
  return result
}
/* compiles to:
 * export type ParseStructProperties<signature extends string> = ParseStructProperties__loop<signature, [], unknown>
 * type ParseStructProperties__loop<Signature_ extends string, Result extends readonly unknown[], Structs> = Trim<Signature_> extends `${infer head};${infer tail}` ? ParseStructProperties__loop<tail, [...Result, ParseAbiParameter<head, { structs: Structs }>], Structs> : Result
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/human-readable/types/structs.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 typecheck-error, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AbiParameter: any
declare const ParseAbiParameter: any
declare const StructLookup: any
declare const StructSignature: any
declare const Trim: any
type AbiParameter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParseAbiParameter<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StructLookup<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StructSignature<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Trim<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ ParseStructs: the ScriptType does not itself typecheck as TypeScript
//   ParseStructs.st.ts(3:69) TS2552: Cannot find name 'signature'. Did you mean 'signatures'?
/* @scripttype preserveParamNames */
export function ParseStructs(signatures: readonly string[]) {
  const m1 = matches<Hole<"struct", { name: string; }>>(ParseStruct(signature))
  const out = emptyObject
  for (const signature in keySet(signatures[number])) {
    out[m1 ? m1.struct['name'] : never] = ParseStruct(signature)['components']
  }
  const m2 = matches<Hole<"structs", Record<string, readonly (AbiParameter & { type: string; })[]>>>(out)
  if (m2) {
    const out2 = emptyObject
    for (const structName in keyof(m2.structs)) {
      out2[structName] = ResolveStructs(m2.structs[structName], m2.structs)
    }
    return out2
  }
  return never
}
/* compiles to:
 * export type ParseStructs<signatures extends readonly string[]> =
 *   { [Signature in signatures[number] as ParseStruct<signature> extends (infer struct extends { name: string; }) ? struct['name'] : never]: ParseStruct<Signature>['components'] } extends (infer structs extends Record<string, readonly (AbiParameter & { type: string; })[]>)
 *     ? { [StructName in keyof structs]: ResolveStructs<structs[StructName], structs> }
 *     : never
 */

// ✓ ParseStruct: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStruct(signature: string, structs: StructLookup | unknown = unknown) {
  const m1 = matches<StructSignature<Hole<"name">, Hole<"properties">>>(signature)
  if (m1) {
    return { name: readonlyProp(Trim(m1.name)), components: readonlyProp(ParseStructProperties(m1.properties, structs)) }
  }
  return never
}
/* compiles to:
 * export type ParseStruct<
 *   signature extends string,
 *   structs extends StructLookup | unknown = unknown
 * > =
 *   signature extends StructSignature<infer name, infer properties>
 *     ? {
 *       readonly name: Trim<name>
 *       readonly components: ParseStructProperties<properties, structs>
 *     }
 *     : never
 */

// ✗ ResolveStructs: the ScriptType does not itself typecheck as TypeScript
//   ResolveStructs.st.ts(3:53) TS2749: 'structs' refers to a value, but is being used as a type here. Did you mean 'typeof structs'?
/* @scripttype preserveParamNames */
export function ResolveStructs__v1(abiParameters, structs, keyReferences, key) {
  const m1 = matches<`${Hole<"head", string & keyof structs>}[${Hole<"tail">}]`>(abiParameters[key]['type'])
  if (m1) {
    if (m1.head in keyReferences) {
      return t<Error<`Circular reference detected. Struct "${(typeof abiParameters)[typeof key]['type']}" is a circular reference.`>>()
    }
    const out = emptyObject
    for (const _ in keySet(m1.head)) {
      out[_] = true
    }
    return { name: readonlyProp(abiParameters[key]['name']), type: readonlyProp(`tuple[${m1.tail}]`), components: readonlyProp(ResolveStructs(structs[m1.head], structs, keyReferences & out)) }
  }
  if (abiParameters[key]['type'] in structs) {
    if (abiParameters[key]['type'] in keyReferences) {
      return t<Error<`Circular reference detected. Struct "${(typeof abiParameters)[typeof key]['type']}" is a circular reference.`>>()
    }
    const out2 = emptyObject
    for (const _ in keySet(abiParameters[key]['type'])) {
      out2[_] = true
    }
    return { name: readonlyProp(abiParameters[key]['name']), type: readonlyProp('tuple'), components: readonlyProp(ResolveStructs(structs[abiParameters[key]['type']], structs, keyReferences & out2)) }
  }
  return abiParameters[key]
}
export function ResolveStructs(abiParameters: readonly (AbiParameter & { type: string; })[], structs: Record<string, readonly (AbiParameter & { type: string; })[]>, keyReferences: { [_: string]: unknown; } | unknown = unknown) {
  const out = emptyObject
  for (const key in keyof(abiParameters)) {
    out[key] = ResolveStructs__v1(abiParameters, structs, keyReferences, key)
  }
  return asReadonly([...out])
}
/* compiles to:
 * export type ResolveStructs__v1<abiParameters, structs, keyReferences, key> =
 *   abiParameters[key]['type'] extends `${infer head extends string & keyof structs}[${infer tail}]`
 *     ? head extends keyof keyReferences
 *       ? Error<
 *         `Circular reference detected. Struct "${abiParameters[key]['type']}" is a circular reference.`
 *       >
 *       : {
 *         readonly name: abiParameters[key]['name']
 *         readonly type: `tuple[${tail}]`
 *         readonly components: ResolveStructs<
 *           structs[head],
 *           structs,
 *           keyReferences & { [_ in head]: true }
 *         >
 *       }
 *   : abiParameters[key]['type'] extends keyof structs
 *     ? abiParameters[key]['type'] extends keyof keyReferences
 *       ? Error<
 *         `Circular reference detected. Struct "${abiParameters[key]['type']}" is a circular reference.`
 *       >
 *       : {
 *         readonly name: abiParameters[key]['name']
 *         readonly type: 'tuple'
 *         readonly components: ResolveStructs<
 *           structs[abiParameters[key]['type']],
 *           structs,
 *           keyReferences & { [_1 in abiParameters[key]['type']]: true }
 *         >
 *       }
 *   : abiParameters[key]
 * export type ResolveStructs<
 *   abiParameters extends readonly (AbiParameter & { type: string; })[],
 *   structs extends Record<string, readonly (AbiParameter & { type: string; })[]>,
 *   keyReferences extends { [_: string]: unknown; } | unknown = unknown
 * > =
 *   readonly [
 *     ...{
 *       [Key in keyof abiParameters]: ResolveStructs__v1<
 *         abiParameters,
 *         structs,
 *         keyReferences,
 *         Key
 *       >
 *     }
 *   ]
 */

// ✓ ParseStructProperties: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParseStructProperties(signature: string) {
  let structs = unknown
  let result: any[] = []
  let signature_ = signature
  while (true) {
    const m1 = matches<`${Hole<"head">};${Hole<"tail">}`>(Trim(signature_))
    if (!m1) {
      break
    }
    signature_ = m1.tail
    result.push(ParseAbiParameter(m1.head, { structs: structs }))
  }
  return result
}
/* compiles to:
 * export type ParseStructProperties<signature extends string> = ParseStructProperties__loop<
 *   signature,
 *   [],
 *   unknown
 * >
 * type ParseStructProperties__loop<Signature_ extends string, Result extends any[], Structs> =
 *   Trim<Signature_> extends `${infer head};${infer tail}`
 *     ? ParseStructProperties__loop<
 *       tail,
 *       [...Result, ParseAbiParameter<head, { structs: Structs }>],
 *       Structs
 *     >
 *     : Result
 */

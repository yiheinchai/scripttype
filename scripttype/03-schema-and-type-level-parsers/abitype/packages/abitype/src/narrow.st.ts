/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/narrow.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Narrow: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Narrow(type): any {
  const out = emptyObject
  for (const K in keyof(type)) {
    out[K] = Narrow(type[K])
  }
  return (matches<typeof type>(unknown) ? unknown : never) | (matches<Function>(type) ? type : never) | (matches<bigint | boolean | number | string>(type) ? type : never) | (matches<[ ]>(type) ? [] : never) | out
}
/* compiles to:
 * export type Narrow<type> =
 *   | (unknown extends type ? unknown : never)
 *   | (type extends Function ? type : never)
 *   | (type extends bigint | boolean | number | string ? type : never)
 *   | (type extends [] ? [] : never)
 *   | { [K in keyof type]: Narrow<type[K]> }
 */

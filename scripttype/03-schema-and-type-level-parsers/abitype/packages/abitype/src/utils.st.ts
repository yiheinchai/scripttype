/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/packages/abitype/src/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 23 type-level aliases: 19 covered, 2 typecheck-error, 2 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Abi: any
declare const AbiBasicType: any
declare const AbiParameter: any
declare const AbiParameterKind: any
declare const AbiParameterTupleNameLookup: any
declare const AbiStateMutability: any
declare const AbiType: any
declare const Merge: any
declare const Pretty: any
declare const PrimitiveTypeLookup: any
declare const ResolvedRegister: any
declare const SolidityBytes: any
declare const SolidityFixedArrayRange: any
declare const SolidityFixedArraySizeLookup: any
declare const SolidityTuple: any
declare const Tuple: any
declare const TypedData: any
declare const TypedDataParameter: any
declare const TypedDataType: any
type Abi<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiBasicType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiParameterKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiParameterTupleNameLookup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiStateMutability<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractAbiErrorNames<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractAbiEventNames<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractAbiFunctionNames<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Merge<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Pretty<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PrimitiveTypeLookup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResolvedRegister<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SolidityBytes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SolidityFixedArrayRange<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SolidityFixedArraySizeLookup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SolidityTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Tuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypedData<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypedDataParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypedDataType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ AbiTypeToPrimitiveType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiTypeToPrimitiveType(abiType: AbiType, abiParameterKind: AbiParameterKind = AbiParameterKind) {
  if (matches<SolidityBytes>(abiType)) {
    return PrimitiveTypeLookup[abiType][abiParameterKind]
  }
  return PrimitiveTypeLookup[abiType]
}
/* compiles to:
 * export type AbiTypeToPrimitiveType<abiType extends AbiType, abiParameterKind extends AbiParameterKind = AbiParameterKind> = abiType extends SolidityBytes ? PrimitiveTypeLookup[abiType][abiParameterKind] : PrimitiveTypeLookup[abiType]
 */

// ✗ AbiParameterToPrimitiveType: the ScriptType does not itself typecheck as TypeScript
//   AbiParameterToPrimitiveType.st.ts(17:29) TS2315: Type 'Error' is not generic.
/* @scripttype preserveParamNames */
export function AbiParameterToPrimitiveType(abiParameter: AbiParameter | { name: string; type: unknown; }, abiParameterKind: AbiParameterKind = AbiParameterKind) {
  if (matches<AbiBasicType>(abiParameter['type'])) {
    return AbiTypeToPrimitiveType(abiParameter['type'], abiParameterKind)
  }
  const m1 = matches<{ type: SolidityTuple; components: Hole<"components", readonly AbiParameter[]>; }>(abiParameter)
  if (m1) {
    return AbiComponentsToPrimitiveType(m1.components, abiParameterKind)
  }
  const m2 = matches<[ Hole<"head", string>, Hole<"size"> ]>(MaybeExtractArrayParameterType(abiParameter['type']))
  if (m2) {
    return AbiArrayToPrimitiveType(abiParameter, abiParameterKind, m2.head, m2.size)
  }
  if (matches<true>(ResolvedRegister['strictAbiType'])) {
    return Error(`Unknown type '${abiParameter['type'] & string}'.`)
  }
  if (matches<{ components: Error<string>; }>(abiParameter)) {
    return abiParameter['components']
  }
  return unknown
}
/* compiles to:
 * export type AbiParameterToPrimitiveType<abiParameter extends AbiParameter | { name: string; type: unknown; }, abiParameterKind extends AbiParameterKind = AbiParameterKind> = abiParameter['type'] extends AbiBasicType ? AbiTypeToPrimitiveType<abiParameter['type'], abiParameterKind> : abiParameter extends { type: SolidityTuple; components: infer components extends readonly AbiParameter[]; } ? AbiComponentsToPrimitiveType<components, abiParameterKind> : MaybeExtractArrayParameterType<abiParameter['type']> extends [infer head extends string, infer size] ? AbiArrayToPrimitiveType<abiParameter, abiParameterKind, head, size> : ResolvedRegister['strictAbiType'] extends true ? Error<`Unknown type '${abiParameter['type'] & string}'.`> : abiParameter extends { components: Error<string>; } ? abiParameter['components'] : unknown
 */

// ✓ AbiComponentsToPrimitiveType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiComponentsToPrimitiveType(components: readonly AbiParameter[], abiParameterKind: AbiParameterKind) {
  if (matches<readonly [ ]>(components)) {
    return []
  }
  if (matches<Exclude<(typeof components)[number]['name'] & string, undefined | ''>>(components[number]['name'])) {
    const out = emptyObject
    for (const component in keySet(components[number])) {
      out[merge(component['name'], {})] = AbiParameterToPrimitiveType(component, abiParameterKind)
    }
    return out
  }
  const out = emptyObject
  for (const key in keyof(components)) {
    out[key] = AbiParameterToPrimitiveType(components[key], abiParameterKind)
  }
  return out
}
/* compiles to:
 * export type AbiComponentsToPrimitiveType<components extends readonly AbiParameter[], abiParameterKind extends AbiParameterKind> = components extends readonly [] ? [] : components[number]['name'] extends Exclude<components[number]['name'] & string, undefined | ''> ? { [Component in components[number] as Component['name'] & {}]: AbiParameterToPrimitiveType<Component, abiParameterKind> } : { [Key in keyof components]: AbiParameterToPrimitiveType<components[Key], abiParameterKind> }
 */

// ✓ MaybeExtractArrayParameterType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeExtractArrayParameterType(type) {
  const m1 = matches<`${Hole<"head">}[${'' | `${SolidityFixedArrayRange}`}]`>(type)
  if (m1) {
    const m2 = matches<`${typeof m1.head}[${Hole<"size">}]`>(type)
    if (m2) {
      return [m1.head, m2.size]
    }
    return Undefined
  }
  return Undefined
}
/* compiles to:
 * export type MaybeExtractArrayParameterType<type> = type extends `${infer head}[${'' | `${SolidityFixedArrayRange}`}]` ? type extends `${head}[${infer size}]` ? [head, size] : undefined : undefined
 */

// ✓ AbiArrayToPrimitiveType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiArrayToPrimitiveType(abiParameter: AbiParameter | { name: string; type: unknown; }, abiParameterKind: AbiParameterKind, head: string, size) {
  if (matches<keyof SolidityFixedArraySizeLookup>(size)) {
    return Tuple(AbiParameterToPrimitiveType(Merge(abiParameter, { type: head }), abiParameterKind), SolidityFixedArraySizeLookup[size])
  }
  return readonlyArrayOf(AbiParameterToPrimitiveType(Merge(abiParameter, { type: head }), abiParameterKind))
}
/* compiles to:
 * export type AbiArrayToPrimitiveType<abiParameter extends AbiParameter | { name: string; type: unknown; }, abiParameterKind extends AbiParameterKind, head extends string, size> = size extends keyof SolidityFixedArraySizeLookup ? Tuple<AbiParameterToPrimitiveType<Merge<abiParameter, { type: head }>, abiParameterKind>, SolidityFixedArraySizeLookup[size]> : readonly AbiParameterToPrimitiveType<Merge<abiParameter, { type: head }>, abiParameterKind>[]
 */

// ✓ AbiParametersToPrimitiveTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiParametersToPrimitiveTypes(abiParameters: readonly AbiParameter[], abiParameterKind: AbiParameterKind = AbiParameterKind, experimental_namedTuples: boolean = ResolvedRegister['experimental_namedTuples']) {
  if (matches<true>(experimental_namedTuples)) {
    return AbiParametersToPrimitiveTypes_named(abiParameters, abiParameterKind)
  }
  return AbiParametersToPrimitiveTypes_mapped(abiParameters, abiParameterKind)
}
/* compiles to:
 * export type AbiParametersToPrimitiveTypes<abiParameters extends readonly AbiParameter[], abiParameterKind extends AbiParameterKind = AbiParameterKind, experimental_namedTuples extends boolean = ResolvedRegister['experimental_namedTuples']> = experimental_namedTuples extends true ? AbiParametersToPrimitiveTypes_named<abiParameters, abiParameterKind> : AbiParametersToPrimitiveTypes_mapped<abiParameters, abiParameterKind>
 */

// ✓ AbiParametersToPrimitiveTypes_mapped: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiParametersToPrimitiveTypes_mapped(abiParameters: readonly AbiParameter[], abiParameterKind: AbiParameterKind = AbiParameterKind) {
  const out = emptyObject
  for (const key in keyof(abiParameters)) {
    out[key] = AbiParameterToPrimitiveType(abiParameters[key], abiParameterKind)
  }
  return Pretty(out)
}
/* compiles to:
 * export type AbiParametersToPrimitiveTypes_mapped<abiParameters extends readonly AbiParameter[], abiParameterKind extends AbiParameterKind = AbiParameterKind> = Pretty<{ [Key in keyof abiParameters]: AbiParameterToPrimitiveType<abiParameters[Key], abiParameterKind> }>
 */

// ✓ AbiParametersToPrimitiveTypes_named: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AbiParametersToPrimitiveTypes_named(abiParameters: readonly AbiParameter[], abiParameterKind: AbiParameterKind = AbiParameterKind, acc: readonly unknown[] = [], depth: readonly number[] = []) {
  if (matches<15>(depth['length'])) {
    return readonlyArrayOf(unknown)
  }
  const m1 = matches<readonly [ Hole<"head1", AbiParameter>, Hole<"head2", AbiParameter>, Hole<"head3", AbiParameter>, Hole<"head4", AbiParameter>, Hole<"head5", AbiParameter>, Hole<"head6", AbiParameter>, ...Hole<"tail", readonly AbiParameter[]> ]>(abiParameters)
  if (m1) {
    return AbiParametersToPrimitiveTypes_named(m1.tail, abiParameterKind, asReadonly([...acc, ...ToNamedTuple(m1.head1, abiParameterKind), ...ToNamedTuple(m1.head2, abiParameterKind), ...ToNamedTuple(m1.head3, abiParameterKind), ...ToNamedTuple(m1.head4, abiParameterKind), ...ToNamedTuple(m1.head5, abiParameterKind), ...ToNamedTuple(m1.head6, abiParameterKind)]), [...depth, 1])
  }
  const m2 = matches<readonly [ Hole<"head1", AbiParameter>, Hole<"head2", AbiParameter>, Hole<"head3", AbiParameter>, Hole<"head4", AbiParameter>, Hole<"head5", AbiParameter> ]>(abiParameters)
  if (m2) {
    return asReadonly([...acc, ...ToNamedTuple(m2.head1, abiParameterKind), ...ToNamedTuple(m2.head2, abiParameterKind), ...ToNamedTuple(m2.head3, abiParameterKind), ...ToNamedTuple(m2.head4, abiParameterKind), ...ToNamedTuple(m2.head5, abiParameterKind)])
  }
  const m3 = matches<readonly [ Hole<"head1", AbiParameter>, Hole<"head2", AbiParameter>, Hole<"head3", AbiParameter>, Hole<"head4", AbiParameter> ]>(abiParameters)
  if (m3) {
    return asReadonly([...acc, ...ToNamedTuple(m3.head1, abiParameterKind), ...ToNamedTuple(m3.head2, abiParameterKind), ...ToNamedTuple(m3.head3, abiParameterKind), ...ToNamedTuple(m3.head4, abiParameterKind)])
  }
  const m4 = matches<readonly [ Hole<"head1", AbiParameter>, Hole<"head2", AbiParameter>, Hole<"head3", AbiParameter> ]>(abiParameters)
  if (m4) {
    return asReadonly([...acc, ...ToNamedTuple(m4.head1, abiParameterKind), ...ToNamedTuple(m4.head2, abiParameterKind), ...ToNamedTuple(m4.head3, abiParameterKind)])
  }
  const m5 = matches<readonly [ Hole<"head1", AbiParameter>, Hole<"head2", AbiParameter> ]>(abiParameters)
  if (m5) {
    return asReadonly([...acc, ...ToNamedTuple(m5.head1, abiParameterKind), ...ToNamedTuple(m5.head2, abiParameterKind)])
  }
  const m6 = matches<readonly [ Hole<"head", AbiParameter> ]>(abiParameters)
  if (m6) {
    return asReadonly([...acc, ...ToNamedTuple(m6.head, abiParameterKind)])
  }
  if (matches<readonly [ ]>(acc)) {
    if (matches<readonly [ ]>(abiParameters)) {
      return asReadonly([])
    }
    return readonlyArrayOf(unknown)
  }
  return acc
}
/* compiles to:
 * export type AbiParametersToPrimitiveTypes_named<abiParameters extends readonly AbiParameter[], abiParameterKind extends AbiParameterKind = AbiParameterKind, acc extends readonly unknown[] = [], depth extends readonly number[] = []> = depth['length'] extends 15 ? readonly unknown[] : abiParameters extends readonly [infer head1 extends AbiParameter, infer head2 extends AbiParameter, infer head3 extends AbiParameter, infer head4 extends AbiParameter, infer head5 extends AbiParameter, infer head6 extends AbiParameter, ...(infer tail extends readonly AbiParameter[])] ? AbiParametersToPrimitiveTypes_named<tail, abiParameterKind, readonly [...acc, ...ToNamedTuple<head1, abiParameterKind>, ...ToNamedTuple<head2, abiParameterKind>, ...ToNamedTuple<head3, abiParameterKind>, ...ToNamedTuple<head4, abiParameterKind>, ...ToNamedTuple<head5, abiParameterKind>, ...ToNamedTuple<head6, abiParameterKind>], [...depth, 1]> : abiParameters extends readonly [infer head1 extends AbiParameter, infer head2 extends AbiParameter, infer head3 extends AbiParameter, infer head4 extends AbiParameter, infer head5 extends AbiParameter] ? readonly [...acc, ...ToNamedTuple<head1, abiParameterKind>, ...ToNamedTuple<head2, abiParameterKind>, ...ToNamedTuple<head3, abiParameterKind>, ...ToNamedTuple<head4, abiParameterKind>, ...ToNamedTuple<head5, abiParameterKind>] : abiParameters extends readonly [infer head1 extends AbiParameter, infer head2 extends AbiParameter, infer head3 extends AbiParameter, infer head4 extends AbiParameter] ? readonly [...acc, ...ToNamedTuple<head1, abiParameterKind>, ...ToNamedTuple<head2, abiParameterKind>, ...ToNamedTuple<head3, abiParameterKind>, ...ToNamedTuple<head4, abiParameterKind>] : abiParameters extends readonly [infer head1 extends AbiParameter, infer head2 extends AbiParameter, infer head3 extends AbiParameter] ? readonly [...acc, ...ToNamedTuple<head1, abiParameterKind>, ...ToNamedTuple<head2, abiParameterKind>, ...ToNamedTuple<head3, abiParameterKind>] : abiParameters extends readonly [infer head1 extends AbiParameter, infer head2 extends AbiParameter] ? readonly [...acc, ...ToNamedTuple<head1, abiParameterKind>, ...ToNamedTuple<head2, abiParameterKind>] : abiParameters extends readonly [infer head extends AbiParameter] ? readonly [...acc, ...ToNamedTuple<head, abiParameterKind>] : acc extends readonly [] ? abiParameters extends readonly [] ? readonly [] : readonly unknown[] : acc
 */

// ✓ ToNamedTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToNamedTuple(abiParameter: AbiParameter, abiParameterKind: AbiParameterKind) {
  return unwrapName(AbiParameterToPrimitiveType(abiParameter, abiParameterKind), abiParameter['name'])
}
/* compiles to:
 * export type ToNamedTuple<abiParameter extends AbiParameter, abiParameterKind extends AbiParameterKind> = unwrapName<AbiParameterToPrimitiveType<abiParameter, abiParameterKind>, abiParameter['name']>
 */

// ✓ unwrapName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unwrapName(type, name) {
  if (matches<string>(name)) {
    return AbiParameterTupleNameLookup(type)[name]
  }
  return [type]
}
/* compiles to:
 * export type unwrapName<type, name> = name extends string ? AbiParameterTupleNameLookup<type>[name] : [type]
 */

// ✓ IsAbi: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAbi(abi) {
  if (matches<Abi>(abi)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAbi<abi> = abi extends Abi ? true : false
 */

// ✓ ExtractAbiFunctions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiFunctions(abi: Abi, abiStateMutability: AbiStateMutability = AbiStateMutability) {
  return Extract(abi[number], { type: 'function', stateMutability: abiStateMutability })
}
/* compiles to:
 * export type ExtractAbiFunctions<abi extends Abi, abiStateMutability extends AbiStateMutability = AbiStateMutability> = Extract<abi[number], { type: 'function'; stateMutability: abiStateMutability }>
 */

// ✓ ExtractAbiFunctionNames: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiFunctionNames(abi: Abi, abiStateMutability: AbiStateMutability = AbiStateMutability) {
  return ExtractAbiFunctions(abi, abiStateMutability)['name']
}
/* compiles to:
 * export type ExtractAbiFunctionNames<abi extends Abi, abiStateMutability extends AbiStateMutability = AbiStateMutability> = ExtractAbiFunctions<abi, abiStateMutability>['name']
 */

// ✓ ExtractAbiFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiFunction(abi: Abi, functionName: ExtractAbiFunctionNames<typeof abi>, abiStateMutability: AbiStateMutability = AbiStateMutability) {
  return Extract(ExtractAbiFunctions(abi, abiStateMutability), { name: functionName })
}
/* compiles to:
 * export type ExtractAbiFunction<abi extends Abi, functionName extends ExtractAbiFunctionNames<abi>, abiStateMutability extends AbiStateMutability = AbiStateMutability> = Extract<ExtractAbiFunctions<abi, abiStateMutability>, { name: functionName }>
 */

// ✓ ExtractAbiEvents: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiEvents(abi: Abi) {
  return Extract(abi[number], { type: 'event' })
}
/* compiles to:
 * export type ExtractAbiEvents<abi extends Abi> = Extract<abi[number], { type: 'event' }>
 */

// ✓ ExtractAbiEventNames: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiEventNames(abi: Abi) {
  return ExtractAbiEvents(abi)['name']
}
/* compiles to:
 * export type ExtractAbiEventNames<abi extends Abi> = ExtractAbiEvents<abi>['name']
 */

// ✓ ExtractAbiEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiEvent(abi: Abi, eventName: ExtractAbiEventNames<typeof abi>) {
  return Extract(ExtractAbiEvents(abi), { name: eventName })
}
/* compiles to:
 * export type ExtractAbiEvent<abi extends Abi, eventName extends ExtractAbiEventNames<abi>> = Extract<ExtractAbiEvents<abi>, { name: eventName }>
 */

// ✓ ExtractAbiErrors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiErrors(abi: Abi) {
  return Extract(abi[number], { type: 'error' })
}
/* compiles to:
 * export type ExtractAbiErrors<abi extends Abi> = Extract<abi[number], { type: 'error' }>
 */

// ✓ ExtractAbiErrorNames: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiErrorNames(abi: Abi) {
  return ExtractAbiErrors(abi)['name']
}
/* compiles to:
 * export type ExtractAbiErrorNames<abi extends Abi> = ExtractAbiErrors<abi>['name']
 */

// ✓ ExtractAbiError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAbiError(abi: Abi, errorName: ExtractAbiErrorNames<typeof abi>) {
  return Extract(ExtractAbiErrors(abi), { name: errorName })
}
/* compiles to:
 * export type ExtractAbiError<abi extends Abi, errorName extends ExtractAbiErrorNames<abi>> = Extract<ExtractAbiErrors<abi>, { name: errorName }>
 */

// ✗ IsTypedData: the ScriptType does not itself typecheck as TypeScript
//   IsTypedData.st.ts(5:41) TS2552: Cannot find name 'key'. Did you mean 'key2'?
/* @scripttype preserveParamNames */
export function IsTypedData(typedData) {
  if (matches<TypedData>(typedData)) {
    const out2 = emptyObject
    for (const key2 in keySet(typedData[key][number])) {
      out2[matches<keyof typeof typedData>(key2['type']) ? never : (matches<`${keyof typeof typedData & string}[${string}]`>(key2['type']) ? never : (matches<TypedDataType>(key2['type']) ? never : key2['name']))] = false
    }
    const out = emptyObject
    for (const key in keyof(typedData)) {
      out[key] = out2
    }
    if (matches<{ [key in keyof typeof typedData]: Record<string, never>; }>(out)) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsTypedData<typedData> = typedData extends TypedData ? { [Key in keyof typedData]: { [Key2 in typedData[key][number] as Key2['type'] extends keyof typedData ? never : Key2['type'] extends `${keyof typedData & string}[${string}]` ? never : Key2['type'] extends TypedDataType ? never : Key2['name']]: false } } extends { [key in keyof typeof typedData]: Record<string, never>; } ? true : false : false
 */

// ✗ TypedDataToPrimitiveTypes: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function TypedDataToPrimitiveTypes(typedData: TypedData, abiParameterKind: AbiParameterKind = AbiParameterKind, keyReferences: { [_: string]: unknown; } | unknown = unknown) {
  const out3 = emptyObject
  for (const _ in keySet(anyOf(key2['type'], key))) {
    out3[_] = true
  }
  const out2 = emptyObject
  for (const key2 in keySet(typedData[key][number])) {
    out2[key2['name']] = matches<typeof key>(key2['type']) ? Error(`Cannot convert self-referencing struct '${key2['type']}' to primitive type.`) : (matches<keyof typeof typedData>(key2['type']) ? (matches<keyof typeof keyReferences>(key2['type']) ? Error(`Circular reference detected. '${key2['type']}' is a circular reference.`) : TypedDataToPrimitiveTypes(Exclude(typedData, key), abiParameterKind, merge(keyReferences, out3))[key2['type']]) : (raw('key2[\'type\'] extends `${infer type extends keyof typedData & string}[${infer tail}]` ? AbiParameterToPrimitiveType<{ name: key2[\'name\']; type: `tuple[${tail}]`; components: _TypedDataParametersToAbiParameters<typedData[type], typedData, keyReferences & { [_ in type | key]: true; }>; }, abiParameterKind> : key2[\'type\'] extends TypedDataType ? AbiParameterToPrimitiveType<key2, abiParameterKind> : Error<`Cannot convert unknown type \'${key2[\'type\']}\' to primitive type.`>')))
  }
  const out = emptyObject
  for (const key in keyof(typedData)) {
    out[key] = out2
  }
  return out & unknown
}

// ✗ _TypedDataParametersToAbiParameters: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function _TypedDataParametersToAbiParameters(typedDataParameters: readonly TypedDataParameter[], typedData: TypedData, keyReferences: { [_: string]: unknown; } | unknown = unknown) {
  const out = emptyObject
  for (const key in keyof(typedDataParameters)) {
    out[key] = raw('typedDataParameters[key] extends infer typedDataParameter extends { name: string; type: unknown; } ? typedDataParameter[\'type\'] extends keyof typedData & string ? { name: typedDataParameter[\'name\']; type: \'tuple\'; components: typedDataParameter[\'type\'] extends keyof keyReferences ? Error<`Circular reference detected. \'${typedDataParameter[\'type\']}\' is a circular reference.`> : _TypedDataParametersToAbiParameters<typedData[typedDataParameter[\'type\']], typedData, keyReferences & { [_ in typedDataParameter[\'type\']]: true; }>; } : typedDataParameter[\'type\'] extends `${infer type extends keyof typedData & string}[${infer tail}]` ? { name: typedDataParameter[\'name\']; type: `tuple[${tail}]`; components: type extends keyof keyReferences ? Error<`Circular reference detected. \'${typedDataParameter[\'type\']}\' is a circular reference.`> : _TypedDataParametersToAbiParameters<typedData[type], typedData, keyReferences & { [_ in type]: true; }>; } : typedDataParameter : never')
  }
  return out
}

/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/playgrounds/functions/src/read.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Abi } from '../../../../../../03-schema-and-type-level-parsers/abitype/packages/abitype/src/abi.js'
import type { ContractParameters, ContractReturnType } from '../../../../../../03-schema-and-type-level-parsers/abitype/playgrounds/functions/src/types.js'
declare const Abi: any
declare const ContractParameters: any
declare const ContractReturnType: any
// ✓ ReadParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadParameters(abi: Abi | readonly unknown[], functionName: string, args: readonly unknown[] | undefined = anyOf(readonlyArrayOf(unknown), Undefined)): any {
  return merge({ abi: abi }, ContractParameters(abi, functionName, anyOf('pure', 'view'), args))
}
/* compiles to:
 * export type ReadParameters<
 *   abi extends Abi | readonly unknown[],
 *   functionName extends string,
 *   args extends readonly unknown[] | undefined = readonly unknown[] | undefined
 * > =
 *   { abi: abi } & ContractParameters<abi, functionName, 'pure' | 'view', args>
 */

// ✓ ReadReturnType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadReturnType(abi: Abi | readonly unknown[], functionName: string, args: readonly unknown[] | undefined): any {
  return ContractReturnType(abi, functionName, args)
}
/* compiles to:
 * export type ReadReturnType<
 *   abi extends Abi | readonly unknown[],
 *   functionName extends string,
 *   args extends readonly unknown[] | undefined
 * > =
 *   ContractReturnType<abi, functionName, args>
 */

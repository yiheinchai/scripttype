/**
 * ScriptType for 03-schema-and-type-level-parsers/abitype/playgrounds/functions/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 12 type-level aliases: 11 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Abi: any
declare const AbiFunction: any
declare const AbiParameter: any
declare const AbiParametersToPrimitiveTypes: any
declare const AbiStateMutability: any
declare const Address: any
declare const ExtractAbiFunction: any
declare const ExtractAbiFunctionNames: any
declare const ResolvedRegister: any
declare const m2: any
type Abi<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiParametersToPrimitiveTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AbiStateMutability<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Address<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractAbiFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractAbiFunctionNames<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResolvedRegister<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ContractParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ContractParameters(abi: Abi | readonly unknown[] = Abi, functionName: string = string, abiStateMutability: AbiStateMutability = AbiStateMutability, args: readonly unknown[] | undefined = asReadonly([]), functionNames: string = matches<Abi>(abi) ? ExtractAbiFunctionNames(abi, abiStateMutability) : string) {
  return merge({ functionName: anyOf(functionNames, matches<typeof functionNames>(functionName) ? functionName : never, matches<typeof abi>(Abi) ? string : never) }, GetArgs(abi, functionName, args))
}
/* compiles to:
 * export type ContractParameters<abi extends Abi | readonly unknown[] = Abi, functionName extends string = string, abiStateMutability extends AbiStateMutability = AbiStateMutability, args extends readonly unknown[] | undefined = readonly [], functionNames extends string = abi extends Abi ? ExtractAbiFunctionNames<abi, abiStateMutability> : string> = { functionName: functionNames | (functionName extends functionNames ? functionName : never) | (Abi extends abi ? string : never) } & GetArgs<abi, functionName, args>
 */

// ✓ GetArgs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetArgs(abi: Abi | readonly unknown[] = Abi, functionName: string = string, args: readonly unknown[] | undefined = asReadonly([]), abiFunction: AbiFunction = matches<Abi>(abi) ? ExtractAbiFunction(abi, functionName) : AbiFunction, primitiveTypes = AbiParametersToPrimitiveTypes(abiFunction['inputs'], 'inputs', true), args_ = primitiveTypes | (matches<Abi>(abi) ? (matches<typeof primitiveTypes>(args) ? (matches<typeof args>(primitiveTypes) ? ReadonlyWiden(args) : never) : never) : never) | (matches<typeof abi>(Abi) ? readonlyArrayOf(unknown) : never)) {
  return MaybePartialBy({ args: args_ }, matches<typeof primitiveTypes>(asReadonly([])) ? 'args' : (matches<typeof abi>(Abi) ? 'args' : string))
}
/* compiles to:
 * export type GetArgs<abi extends Abi | readonly unknown[] = Abi, functionName extends string = string, args extends readonly unknown[] | undefined = readonly [], abiFunction extends AbiFunction = abi extends Abi ? ExtractAbiFunction<abi, functionName> : AbiFunction, primitiveTypes = AbiParametersToPrimitiveTypes<abiFunction['inputs'], 'inputs', true>, args_ = primitiveTypes | (abi extends Abi ? args extends primitiveTypes ? primitiveTypes extends args ? ReadonlyWiden<args> : never : never : never) | (Abi extends abi ? readonly unknown[] : never)> = MaybePartialBy<{ args: args_ }, readonly [] extends primitiveTypes ? 'args' : Abi extends abi ? 'args' : string>
 */

// ✗ ContractReturnType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function ContractReturnType(abi: Abi | readonly unknown[] = Abi, functionName: string = string, args: readonly unknown[] | undefined = anyOf(readonlyArrayOf(unknown), Undefined), abiFunction: AbiFunction = m1 ? (matches<true>(IsUnion(m1.abiFunction_)) ? (m2 ? out[number] : never) : m1.abiFunction_) : never, outputs: readonly AbiParameter[] = abiFunction['outputs'], primitiveTypes: readonly unknown[] = AbiParametersToPrimitiveTypes(outputs, 'outputs', true)) {
  if (matches<[ never ]>([abiFunction])) {
    return unknown
  }
  if (matches<typeof primitiveTypes>(readonlyArrayOf(unknown))) {
    return unknown
  }
  if (matches<readonly [ ]>(primitiveTypes)) {
    return voidType()
  }
  const m3 = matches<readonly [ Hole<"primitiveType"> ]>(primitiveTypes)
  if (m3) {
    return m3.primitiveType
  }
  return primitiveTypes
}
/* compiles to:
 * export type ContractReturnType<abi extends Abi | readonly unknown[] = Abi, functionName extends string = string, args extends readonly unknown[] | undefined = readonly unknown[] | undefined, abiFunction extends AbiFunction = m1 extends true ? IsUnion<m1.abiFunction_> extends true ? m2 extends true ? out[number] : never : m1.abiFunction_ : never, outputs extends readonly AbiParameter[] = abiFunction['outputs'], primitiveTypes extends readonly unknown[] = AbiParametersToPrimitiveTypes<outputs, 'outputs', true>> = [abiFunction] extends [never] ? unknown : readonly unknown[] extends primitiveTypes ? unknown : primitiveTypes extends readonly [] ? void : primitiveTypes extends readonly [infer primitiveType] ? primitiveType : primitiveTypes
 */

// ✓ IsUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnion(T, C = T) {
  if (matches<typeof C>(T)) {
    if (matches<[ typeof T ]>([C])) {
      return false
    }
    return true
  }
  return never
}
/* compiles to:
 * export type IsUnion<T, C = T> = T extends C ? [C] extends [T] ? false : true : never
 */

// ✓ UnionToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToTuple(U, Last = LastInUnion(U)) {
  if (matches<[ never ]>([U])) {
    return []
  }
  return [...UnionToTuple(Exclude(U, Last)), Last]
}
/* compiles to:
 * export type UnionToTuple<U, Last = LastInUnion<U>> = [U] extends [never] ? [] : [...UnionToTuple<Exclude<U, Last>>, Last]
 */

// ✓ LastInUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LastInUnion(U) {
  const m1 = matches<(x: Hole<"L">) => 0>(UnionToIntersection(matches<unknown>(U) ? fnType([U], 0) : never))
  if (m1) {
    return m1.L
  }
  return never
}
/* compiles to:
 * export type LastInUnion<U> = UnionToIntersection<U extends unknown ? (a0: U) => 0 : never> extends (x: infer L) => 0 ? L : never
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U) {
  const m1 = matches<(arg: Hole<"I">) => 0>(matches<unknown>(U) ? fnType([U], 0) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> = (U extends unknown ? (a0: U) => 0 : never) extends (arg: infer I) => 0 ? I : never
 */

// ✓ PartialBy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PartialBy(TType, TKeys: keyof typeof TType) {
  return ExactPartial(Pick(TType, TKeys)) & Omit(TType, TKeys)
}
/* compiles to:
 * export type PartialBy<TType, TKeys extends keyof TType> = ExactPartial<Pick<TType, TKeys>> & Omit<TType, TKeys>
 */

// ✓ ExactPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExactPartial(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = optional(anyOf(T[K], Undefined))
  }
  return out
}
/* compiles to:
 * export type ExactPartial<T> = { [K in keyof T]?: T[K] | undefined }
 */

// ✓ MaybePartialBy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybePartialBy(TType, TKeys: string) {
  if (matches<keyof typeof TType>(TKeys)) {
    return PartialBy(TType, TKeys)
  }
  return TType
}
/* compiles to:
 * export type MaybePartialBy<TType, TKeys extends string> = TKeys extends keyof TType ? PartialBy<TType, TKeys> : TType
 */

// ✓ ReadonlyWiden: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyWiden(TType) {
  const out = emptyObject
  for (const K in keyof(TType)) {
    out[K] = ReadonlyWiden(TType[K])
  }
  const out2 = emptyObject
  for (const K in keyof(TType)) {
    out2[K] = ReadonlyWiden(TType[K])
  }
  const m1 = matches<Hole<"Val", unknown[]>>(out2)
  return (matches<Function>(TType) ? TType : never) | (matches<ResolvedRegister['bigIntType']>(TType) ? bigint : never) | (matches<boolean>(TType) ? boolean : never) | (matches<ResolvedRegister['intType']>(TType) ? number : never) | (matches<string>(TType) ? (matches<Address>(TType) ? Address : (matches<ResolvedRegister['bytesType']['inputs']>(TType) ? ResolvedRegister['bytesType'] : string)) : never) | (matches<readonly [ ]>(TType) ? asReadonly([]) : never) | (matches<Record<string, unknown>>(TType) ? out : never) | (matches<{ length: number; }>(TType) ? (m1 ? asReadonly([...m1.Val]) : never) : never)
}
/* compiles to:
 * export type ReadonlyWiden<TType> = (TType extends Function ? TType : never) | (TType extends ResolvedRegister['bigIntType'] ? bigint : never) | (TType extends boolean ? boolean : never) | (TType extends ResolvedRegister['intType'] ? number : never) | (TType extends string ? TType extends Address ? Address : TType extends ResolvedRegister['bytesType']['inputs'] ? ResolvedRegister['bytesType'] : string : never) | (TType extends readonly [] ? readonly [] : never) | (TType extends Record<string, unknown> ? { [K in keyof TType]: ReadonlyWiden<TType[K]> } : never) | (TType extends { length: number; } ? { [K1 in keyof TType]: ReadonlyWiden<TType[K1]> } extends (infer Val extends unknown[]) ? readonly [...Val] : never : never)
 */

// ✓ DeepPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepPartial(T, MaxDepth: number, Depth: readonly number[] = []) {
  if (matches<typeof MaxDepth>(Depth['length'])) {
    return T
  }
  if (matches<object>(T)) {
    const out = emptyObject
    for (const P in keyof(T)) {
      out[P] = optional(anyOf(DeepPartial(T[P], MaxDepth, [...Depth, 1]), Undefined))
    }
    return out
  }
  return T
}
/* compiles to:
 * export type DeepPartial<T, MaxDepth extends number, Depth extends readonly number[] = []> = Depth['length'] extends MaxDepth ? T : T extends object ? { [P in keyof T]?: DeepPartial<T[P], MaxDepth, [...Depth, 1]> | undefined } : T
 */

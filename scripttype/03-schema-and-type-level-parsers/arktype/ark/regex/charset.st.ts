/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/regex/charset.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 3 typecheck-error, 1 compile-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Backslash: any
declare const ErrorMessage: any
declare const Scanner: any
declare const State: any
declare const StringDigit: any
declare const UnescapedDashMarker: any
declare const emptyCharacterSetMessage: any
declare const parseEscapedChar: any
declare const shift: any
declare const shiftResult: any
declare const writeUnclosedGroupMessage: any
type Backslash<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Scanner<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type State<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StringDigit<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnescapedDashMarker<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type emptyCharacterSetMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type parseEscapedChar<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shift<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shiftResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type writeUnclosedGroupMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ parseCharset: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseCharset(s: State, unscanned: string) {
  const m1 = matches<(Scanner.shiftResult<Hole<"scanned">, Hole<"nextUnscanned">>)>(Scanner.shiftUntilEscapable(unscanned, ']', Backslash))
  if (m1) {
    const m2 = matches<`]${Hole<"remaining">}`>(m1.nextUnscanned)
    if (m2) {
      if (matches<Scanner.shift<"^", string>>(m1.scanned)) {
        return s.shiftQuantifiable(s, string, m2.remaining)
      }
      const m3 = matches<(Hole<"result", string>)>(parseNonNegatedCharset(m1.scanned, never, Null))
      if (m3) {
        if (matches<[ never ]>([m3.result])) {
          return s.error(emptyCharacterSetMessage)
        }
        return s.shiftQuantifiable(s, m3.result, m2.remaining)
      }
      return never
    }
    return s.error(writeUnclosedGroupMessage(']'))
  }
  return never
}

// ✗ parseNonNegatedCharset: the ScriptType does not itself typecheck as TypeScript
//   parseNonNegatedCharset.st.ts(3:22) TS2702: 'Scanner' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function parseNonNegatedCharset(chars: string, set: string, lastChar: string | null) {
  const m1 = matches<Scanner.shiftResult<Hole<"result">, Hole<"unscanned">>>(parseChar(chars))
  if (m1) {
    if (matches<UnescapedDashMarker>(m1.result)) {
      return parseDash(m1.unscanned, set, lastChar)
    }
    if (matches<ErrorMessage>(m1.result)) {
      return m1.result
    }
    return parseNonNegatedCharset(m1.unscanned, anyOf(set, m1.result), m1.result)
  }
  return set
}
/* compiles to:
 * export type parseNonNegatedCharset<chars extends string, set extends string, lastChar extends string | null> = parseChar<chars> extends Scanner.shiftResult<infer result, infer unscanned> ? result extends UnescapedDashMarker ? parseDash<unscanned, set, lastChar> : result extends ErrorMessage ? result : parseNonNegatedCharset<unscanned, set | result, result> : set
 */

// ✗ parseDash: the ScriptType does not itself typecheck as TypeScript
//   parseDash.st.ts(4:25) TS2702: 'Scanner' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function parseDash(unscanned: string, set: string, lastChar: string | null) {
  if (matches<string>(lastChar)) {
    const m1 = matches<(Scanner.shiftResult<Hole<"rangeEnd">, Hole<"next">>)>(parseChar(unscanned))
    if (m1) {
      return parseNonNegatedCharset(m1.next, anyOf(set, inferRange(lastChar, m1.rangeEnd)), Null)
    }
    return anyOf(set, '-')
  }
  return parseNonNegatedCharset(unscanned, anyOf(set, '-'), '-')
}
/* compiles to:
 * export type parseDash<unscanned extends string, set extends string, lastChar extends string | null> = lastChar extends string ? parseChar<unscanned> extends Scanner.shiftResult<infer rangeEnd, infer next> ? parseNonNegatedCharset<next, set | inferRange<lastChar, rangeEnd>, null> : set | '-' : parseNonNegatedCharset<unscanned, set | '-', '-'>
 */

// ✓ inferRange: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferRange(start: string, end: string) {
  if (matches<StringDigit>(anyOf(start, end))) {
    return `${number}`
  }
  return string
}
/* compiles to:
 * export type inferRange<start extends string, end extends string> = start | end extends StringDigit ? `${number}` : string
 */

// ✗ parseChar: the ScriptType does not itself typecheck as TypeScript
//   parseChar.st.ts(3:22) TS2702: 'Scanner' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function parseChar(unscanned: string) {
  const m1 = matches<Scanner.shift<Hole<"lookahead">, Hole<"next">>>(unscanned)
  if (m1) {
    if (matches<Backslash>(m1.lookahead)) {
      const m2 = matches<Scanner.shift<Hole<"escaped">, Hole<"postEscaped">>>(m1.next)
      if (m2) {
        return Scanner.shiftResult(parseEscapedChar(m2.escaped), m2.postEscaped)
      }
      return never
    }
    return Scanner.shiftResult(matches<"-">(m1.lookahead) ? UnescapedDashMarker : m1.lookahead, m1.next)
  }
  return Null
}
/* compiles to:
 * export type parseChar<unscanned extends string> = unscanned extends Scanner.shift<infer lookahead, infer next> ? lookahead extends Backslash ? next extends Scanner.shift<infer escaped, infer postEscaped> ? Scanner.shiftResult<parseEscapedChar<escaped>, postEscaped> : never : Scanner.shiftResult<lookahead extends '-' ? UnescapedDashMarker : lookahead, next> : null
 */

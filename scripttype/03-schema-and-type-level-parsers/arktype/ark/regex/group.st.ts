/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/regex/group.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 6 covered, 5 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace Scanner {
  export type shift<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type shiftResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type shiftUntil<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace State {
  export type UnnamedCaptureKind<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ErrorMessage: any
declare const LookaroundChar: any
declare const ModifiableFlag: any
declare const Scanner: any
declare const State: any
declare const missingNegatedModifierMessage: any
declare const multipleModifierDashesMessage: any
declare const shift: any
declare const shiftResult: any
declare const unescapedLiteralQuestionMarkMessage: any
declare const unnamedCaptureGroupMessage: any
declare const writeUnclosedGroupMessage: any
type ErrorMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type LookaroundChar<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ModifiableFlag<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParsedModifiers<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Scanner<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ShiftedModifiers<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type State<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type missingNegatedModifierMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type multipleModifierDashesMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type shift<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type shiftResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type unescapedLiteralQuestionMarkMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type unnamedCaptureGroupMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnclosedGroupMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ parseGroup: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseGroup(s: State, unscanned: string) {
  const m1 = matches<Scanner.shift<Hole<"lookahead">, Hole<"next">>>(unscanned)
  if (m1) {
    if (matches<"?">(m1.lookahead)) {
      return parseNonCapturingGroup(s, m1.next)
    }
    return s.pushGroup(s, State.UnnamedCaptureKind.indexed, unscanned, Undefined)
  }
  return s.error(writeUnclosedGroupMessage(')'))
}

// ✗ parseNonCapturingGroup: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseNonCapturingGroup(s: State, unscanned: string) {
  const m1 = matches<Scanner.shift<Hole<"lookahead">, Hole<"next">>>(unscanned)
  if (m1) {
    if (matches<":">(m1.lookahead)) {
      return s.pushGroup(s, State.UnnamedCaptureKind.noncapturing, m1.next, Undefined)
    }
    if (matches<LookaroundChar>(m1.lookahead)) {
      return s.pushGroup(s, State.UnnamedCaptureKind.lookaround, m1.next, Undefined)
    }
    if (matches<"<">(m1.lookahead)) {
      return parseNamedGroupOrLookbehind(s, m1.next)
    }
    const m2 = matches<(ShiftedModifiers<Hole<"flags">, Hole<"negated">, Hole<"following">>)>(shiftModifiers(unscanned))
    if (m2) {
      const m3 = matches<ErrorMessage<Hole<"message">>>(m2.following)
      if (m3) {
        return s.error(m3.message)
      }
      return s.pushGroup(s, State.UnnamedCaptureKind.noncapturing, m2.following, matches<typeof m2.flags>('i') ? true : (matches<typeof m2.negated>('i') ? false : Undefined))
    }
    return never
  }
  return s.error(writeUnclosedGroupMessage(')'))
}

// ✓ ShiftedModifiers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShiftedModifiers(flags: ModifiableFlag = ModifiableFlag, negated: ModifiableFlag = ModifiableFlag, unscanned: string = string) {
  return [ParsedModifiers(flags, negated), unscanned]
}
/* compiles to:
 * export type ShiftedModifiers<
 *   flags extends ModifiableFlag = ModifiableFlag,
 *   negated extends ModifiableFlag = ModifiableFlag,
 *   unscanned extends string = string
 * > =
 *   [ParsedModifiers<flags, negated>, unscanned]
 */

// ✓ ParsedModifiers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParsedModifiers(flags: ModifiableFlag = ModifiableFlag, negated: ModifiableFlag = ModifiableFlag) {
  return { flags: flags, negated: negated }
}
/* compiles to:
 * export type ParsedModifiers<
 *   flags extends ModifiableFlag = ModifiableFlag,
 *   negated extends ModifiableFlag = ModifiableFlag
 * > =
 *   { flags: flags; negated: negated }
 */

// ✗ shiftModifiers: does not compile yet
//   Type 'scanned' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function shiftModifiers(unscanned: string) {
  const m1 = matches<(Scanner.shiftResult<Hole<"scanned">, Hole<"next">>)>(Scanner.shiftUntil(unscanned, anyOf(':', ')')))
  if (m1) {
    const m2 = matches<Scanner.shift<Hole<"terminator">, Hole<"following">>>(m1.next)
    if (m2) {
      if (matches<":">(m2.terminator)) {
        const m3 = matches<(ParsedModifiers<Hole<"flags">, Hole<"negated">>)>(parseModifiers(m1.scanned))
        if (m3) {
          return ShiftedModifiers(m3.flags, m3.negated, m2.following)
        }
        return ShiftedModifiers(never, never, ErrorMessage(parseModifiers(m1.scanned) & string))
      }
      return ShiftedModifiers(never, never, ErrorMessage(unescapedLiteralQuestionMarkMessage))
    }
    return ShiftedModifiers(never, never, ErrorMessage(writeUnclosedGroupMessage(')')))
  }
  return never
}
/* compiles to:
 * export type shiftModifiers<unscanned extends string> =
 *   Scanner.shiftUntil<unscanned, ':' | ')'> extends Scanner.shiftResult<infer scanned, infer next>
 *     ? next extends Scanner.shift<infer terminator, infer following>
 *       ? terminator extends ':'
 *         ? parseModifiers<scanned> extends ParsedModifiers<infer flags, infer negated>
 *           ? ShiftedModifiers<flags, negated, following>
 *           : ShiftedModifiers<never, never, ErrorMessage<parseModifiers<scanned> & string>>
 *         : ShiftedModifiers<never, never, ErrorMessage<unescapedLiteralQuestionMarkMessage>>
 *       : ShiftedModifiers<never, never, ErrorMessage<writeUnclosedGroupMessage<')'>>>
 *     : never
 */

// ✓ parseModifiers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseModifiers(unscanned: string) {
  return _parseModifiers(unscanned, never, never)
}
/* compiles to:
 * export type parseModifiers<unscanned extends string> = _parseModifiers<unscanned, never, never>
 */

// ✗ _parseModifiers: does not compile yet
//   Type 'next' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function _parseModifiers(unscanned: string, flags: ModifiableFlag, negated: ModifiableFlag) {
  const m1 = matches<Scanner.shift<Hole<"lookahead">, Hole<"next">>>(unscanned)
  if (m1) {
    if (matches<"-">(m1.lookahead)) {
      if (matches<[ never ]>([negated])) {
        const m2 = matches<Scanner.shift<Hole<"modifier">, Hole<"next">>>(m1.next)
        if (m2) {
          if (matches<ModifiableFlag>(m2.modifier)) {
            if (matches<typeof flags | typeof negated>(m2.modifier)) {
              return writeDuplicateModifierMessage(m2.modifier)
            }
            return _parseModifiers(m2.next, flags, anyOf(negated, m2.modifier))
          }
          return writeInvalidModifierMessage(m2.modifier)
        }
        return missingNegatedModifierMessage
      }
      return multipleModifierDashesMessage
    }
    if (matches<ModifiableFlag>(m1.lookahead)) {
      if (matches<typeof flags | typeof negated>(m1.lookahead)) {
        return writeDuplicateModifierMessage(m1.lookahead)
      }
      if (matches<[ never ]>([negated])) {
        return _parseModifiers(m1.next, anyOf(flags, m1.lookahead), negated)
      }
      return _parseModifiers(m1.next, flags, anyOf(negated, m1.lookahead))
    }
    return writeInvalidModifierMessage(m1.lookahead)
  }
  return ParsedModifiers(flags, negated)
}
/* compiles to:
 * export type _parseModifiers<
 *   unscanned extends string,
 *   flags extends ModifiableFlag,
 *   negated extends ModifiableFlag
 * > =
 *   unscanned extends Scanner.shift<infer lookahead, infer next>
 *     ? lookahead extends '-'
 *       ? [negated] extends [never]
 *         ? next extends Scanner.shift<infer modifier, infer next>
 *           ? modifier extends ModifiableFlag
 *             ? modifier extends flags | negated
 *               ? writeDuplicateModifierMessage<modifier>
 *               : _parseModifiers<next, flags, negated | modifier>
 *             : writeInvalidModifierMessage<modifier>
 *           : missingNegatedModifierMessage
 *         : multipleModifierDashesMessage
 *     : lookahead extends ModifiableFlag
 *       ? lookahead extends flags | negated ? writeDuplicateModifierMessage<lookahead>
 *       : [negated] extends [never] ? _parseModifiers<next, flags | lookahead, negated>
 *       : _parseModifiers<next, flags, negated | lookahead>
 *     : writeInvalidModifierMessage<lookahead>
 *     : ParsedModifiers<flags, negated>
 */

// ✓ writeDuplicateModifierMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeDuplicateModifierMessage(modifier: ModifiableFlag) {
  return `Modifier ${modifier} cannot appear multiple times in a single group`
}
/* compiles to:
 * export type writeDuplicateModifierMessage<modifier extends ModifiableFlag> = `Modifier ${modifier} cannot appear multiple times in a single group`
 */

// ✓ writeInvalidModifierMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeInvalidModifierMessage(char: string) {
  return `Modifier flag ${char} must be 'i', 'm' or 's'`
}
/* compiles to:
 * export type writeInvalidModifierMessage<char extends string> = `Modifier flag ${char} must be 'i', 'm' or 's'`
 */

// ✗ parseNamedGroupOrLookbehind: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseNamedGroupOrLookbehind(s: State, unscanned: string) {
  const m1 = matches<Scanner.shift<LookaroundChar, Hole<"next">>>(unscanned)
  if (m1) {
    return s.pushGroup(s, State.UnnamedCaptureKind.lookaround, m1.next, Undefined)
  }
  const m2 = matches<(Scanner.shiftResult<Hole<"name">, Hole<"following">>)>(shiftNamedGroup(unscanned))
  if (m2) {
    return s.pushGroup(s, m2.name, m2.following, Undefined)
  }
  return s.error(writeUnclosedGroupMessage(')'))
}

// ✓ shiftNamedGroup: verified type-identical to the original
/* @scripttype preserveParamNames */
export function shiftNamedGroup(unscanned: string) {
  const m1 = matches<`${Hole<"name">}>${Hole<"next">}`>(unscanned)
  if (m1) {
    if (matches<"">(m1.name)) {
      return Scanner.shiftResult('', ErrorMessage(unnamedCaptureGroupMessage))
    }
    return Scanner.shiftResult(m1.name, m1.next)
  }
  return Scanner.shiftResult('', ErrorMessage(writeUnclosedGroupMessage('>')))
}
/* compiles to:
 * export type shiftNamedGroup<unscanned extends string> =
 *   unscanned extends `${infer name}>${infer next}`
 *     ? name extends ''
 *       ? Scanner.shiftResult<'', ErrorMessage<unnamedCaptureGroupMessage>>
 *       : Scanner.shiftResult<name, next>
 *     : Scanner.shiftResult<'', ErrorMessage<writeUnclosedGroupMessage<'>'>>>
 */

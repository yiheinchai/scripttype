/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/regex/state.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 55 type-level aliases: 50 covered, 4 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { quantify } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/regex/quantify.js'
import type { Flags, IndexedCaptures, NamedCaptures, Regex } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/regex/regex.js'
import type { Anchor, CapturedGroupKind, EmptyCaptures, EndAnchorMarker, IncompleteCaptureGroup, IndexedCaptureOffset, RegexAst, StartAnchorMarker } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/regex/state.js'
import type { setIndex } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
import type { ErrorMessage } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/errors.js'
import type { NumberLiteral } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/numbers.js'
import type { unionKeyOf } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/records.js'
import type { contains } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/strings.js'
declare namespace FinalizationContext {
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace FinalizationResult {
  export type error<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace FinalizedBranch {
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace GroupTree {
  export type finalize<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace QuantifierTree {
  export type finalize<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace ReferenceNode {
  export type finalize<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace SequenceTree {
  export type Empty<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type finalize<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace State {
  export type Group<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type UnnamedCaptureKind<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace UnionTree {
  export type finalize<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type branches<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type elements<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type firstCaptureBranch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type init<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type last<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type pattern<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type r<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type rest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type startStripped<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type bothStripped<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type headBranches<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type r<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type endStripped<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace s {
  export type error<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Anchor: any
declare const CapturedGroupKind: any
declare const Empty: any
declare const EmptyCaptures: any
declare const EndAnchorMarker: any
declare const ErrorMessage: any
declare const FinalizationContext: any
declare const FinalizationResult: any
declare const FinalizedBranch: any
declare const Flags: any
declare const Group: any
declare const GroupTree: any
declare const IncompleteCaptureGroup: any
declare const IndexedCaptureOffset: any
declare const IndexedCaptures: any
declare const NamedCaptures: any
declare const NumberLiteral: any
declare const QuantifierTree: any
declare const ReferenceNode: any
declare const Regex: any
declare const RegexAst: any
declare const SequenceTree: any
declare const StartAnchorMarker: any
declare const State: any
declare const UnionTree: any
declare const UnnamedCaptureKind: any
declare const ZeroWidthSpace: any
declare const contains: any
declare const indexed: any
declare const lookaround: any
declare const quantify: any
declare const setIndex: any
declare const unionKeyOf: any
declare const writeUnclosedGroupMessage: any
declare const writeUnmatchedGroupCloseMessage: any
declare const writeUnresolvableBackreferenceMessage: any
type AnchorMarker<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Empty<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FinalizationContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FinalizationResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FinalizedBranch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Group<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GroupTree<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type QuantifierTree<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ReferenceNode<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type SequenceTree<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type State<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnionTree<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnnamedCaptureKind<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ZeroWidthSpace<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type anchorsAway<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type finalizeBranchCaptures<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type finalizeNamedCapture<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type finalizeUnnamedCapture<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type indexed<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type inferReference<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type lookaround<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type pushQuantifiable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeIncompleteReferenceError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnclosedGroupMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnmatchedGroupCloseMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnresolvableBackreferenceMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type zeroQuantifiedNames<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(s: State): any {
  return s
}
/* compiles to:
 * export type from<s extends State> = s
 */

// ✓ initialize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function initialize(source: string, flags: Flags): any {
  return from({ unscanned: source, groups: [], capture: never, branches: [], sequence: t<SequenceTree.Empty>(), root: '', caseInsensitive: contains(flags, 'i'), flags: flags })
}
/* compiles to:
 * export type initialize<source extends string, flags extends Flags> = from<
 *   {
 *     unscanned: source
 *     groups: []
 *     capture: never
 *     branches: []
 *     sequence: SequenceTree.Empty
 *     root: ''
 *     caseInsensitive: contains<flags, 'i'>
 *     flags: flags
 *   }
 * >
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(g: Group): any {
  return g
}
/* compiles to:
 * export type from<g extends Group> = g
 */

// ✓ pop: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pop(init: Group, last: Group[]): any {
  return [...last, init]
}
/* compiles to:
 * export type pop<init extends Group, last extends Group[]> = [...last, init]
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(g: Group): any {
  if (matches<[ ]>(g['branches'])) {
    return pushQuantifiable(g['sequence'], g['root'])
  }
  const m1 = matches<(Hole<"branches", RegexAst[]>)>([...g['branches'], pushQuantifiable(g['sequence'], g['root'])])
  if (m1) {
    return finalizeUnion(m1.branches, [])
  }
  return never
}
/* compiles to:
 * export type finalize<g extends Group> =
 *   g['branches'] extends [] ? pushQuantifiable<g['sequence'], g['root']>
 *   : [...g['branches'], pushQuantifiable<g['sequence'], g['root']>] extends (infer branches extends RegexAst[])
 *     ? finalizeUnion<branches, []>
 *   : never
 */

// ✓ finalizeUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeUnion(remaining: RegexAst[], flattened: RegexAst[]): any {
  const m1 = matches<([ Hole<"head", RegexAst>, ...Hole<"tail", RegexAst[]> ])>(remaining)
  if (m1) {
    const m2 = matches<UnionTree<Hole<"headBranches">>>(m1.head)
    if (m2) {
      return finalizeUnion(m1.tail, [...flattened, ...m2.headBranches])
    }
    return finalizeUnion(m1.tail, [...flattened, m1.head])
  }
  return UnionTree(flattened)
}
/* compiles to:
 * export type finalizeUnion<remaining extends RegexAst[], flattened extends RegexAst[]> =
 *   remaining extends [infer head extends RegexAst, ...infer tail extends RegexAst[]]
 *     ? head extends UnionTree<infer headBranches>
 *       ? finalizeUnion<tail, [...flattened, ...headBranches]>
 *       : finalizeUnion<tail, [...flattened, head]>
 *     : UnionTree<flattened>
 */

// ✓ AnchorMarker: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnchorMarker(inner: Anchor = Anchor): any {
  return `<${ZeroWidthSpace}${inner}${ZeroWidthSpace}>`
}
/* compiles to:
 * export type AnchorMarker<inner extends Anchor = Anchor> = `<${ZeroWidthSpace}${inner}${ZeroWidthSpace}>`
 */

// ✓ error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function error(message: string): any {
  return t<State.from<{ unscanned: ErrorMessage<typeof message>; groups: [ ]; capture: never; branches: [ ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: false; flags: ""; }>>()
}
/* compiles to:
 * export type error<message extends string> = State.from<
 *   { unscanned: ErrorMessage<message>; groups: [ ]; capture: never; branches: [ ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: false; flags: ""; }
 * >
 */

// ✓ shiftQuantifiable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function shiftQuantifiable(s: State, root: RegexAst, unscanned: string): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: (typeof s)["groups"]; capture: (typeof s)["capture"]; branches: (typeof s)["branches"]; sequence: pushQuantifiable<(typeof s)["sequence"], (typeof s)["root"]>; root: typeof root; caseInsensitive: (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type shiftQuantifiable<
 *   s extends State,
 *   root extends RegexAst,
 *   unscanned extends string
 * > =
 *   State.from<
 *     { unscanned: unscanned; groups: (s)["groups"]; capture: (s)["capture"]; branches: (s)["branches"]; sequence: pushQuantifiable<(s)["sequence"], (s)["root"]>; root: root; caseInsensitive: (s)["caseInsensitive"]; flags: (s)["flags"]; }
 *   >
 */

// ✓ pushQuantified: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pushQuantified(s: State, quantified: RegexAst, unscanned: string): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: (typeof s)["groups"]; capture: (typeof s)["capture"]; branches: (typeof s)["branches"]; sequence: pushQuantifiable<(typeof s)["sequence"], typeof quantified>; root: ""; caseInsensitive: (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type pushQuantified<
 *   s extends State,
 *   quantified extends RegexAst,
 *   unscanned extends string
 * > =
 *   State.from<
 *     { unscanned: unscanned; groups: (s)["groups"]; capture: (s)["capture"]; branches: (s)["branches"]; sequence: pushQuantifiable<(s)["sequence"], quantified>; root: ""; caseInsensitive: (s)["caseInsensitive"]; flags: (s)["flags"]; }
 *   >
 */

// ✓ pushQuantifier: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pushQuantifier(s: State, min: number, max: number | null, unscanned: string): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: (typeof s)["groups"]; capture: (typeof s)["capture"]; branches: (typeof s)["branches"]; sequence: pushQuantifiable<(typeof s)["sequence"], { kind: "quantifier"; ast: (typeof s)["root"]; min: typeof min; max: typeof max; }>; root: ""; caseInsensitive: (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type pushQuantifier<
 *   s extends State,
 *   min extends number,
 *   max extends number | null,
 *   unscanned extends string
 * > =
 *   State.from<
 *     { unscanned: unscanned; groups: (s)["groups"]; capture: (s)["capture"]; branches: (s)["branches"]; sequence: pushQuantifiable<(s)["sequence"], { kind: "quantifier"; ast: (s)["root"]; min: min; max: max; }>; root: ""; caseInsensitive: (s)["caseInsensitive"]; flags: (s)["flags"]; }
 *   >
 */

// ✓ finalizeBranch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeBranch(s: State, unscanned: string): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: (typeof s)["groups"]; capture: (typeof s)["capture"]; branches: [ ...(typeof s)["branches"], pushQuantifiable<(typeof s)["sequence"], (typeof s)["root"]> ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type finalizeBranch<s extends State, unscanned extends string> = State.from<
 *   { unscanned: unscanned; groups: (s)["groups"]; capture: (s)["capture"]; branches: [ ...(s)["branches"], pushQuantifiable<(s)["sequence"], (s)["root"]> ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: (s)["caseInsensitive"]; flags: (s)["flags"]; }
 * >
 */

// ✓ anchor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function anchor(s: State, a: AnchorMarker, unscanned: string): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: (typeof s)["groups"]; capture: (typeof s)["capture"]; branches: (typeof s)["branches"]; sequence: pushQuantifiable<(typeof s)["sequence"], pushQuantifiable<(typeof s)["root"], typeof a>>; root: ""; caseInsensitive: (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type anchor<s extends State, a extends AnchorMarker, unscanned extends string> = State.from<
 *   { unscanned: unscanned; groups: (s)["groups"]; capture: (s)["capture"]; branches: (s)["branches"]; sequence: pushQuantifiable<(s)["sequence"], pushQuantifiable<(s)["root"], a>>; root: ""; caseInsensitive: (s)["caseInsensitive"]; flags: (s)["flags"]; }
 * >
 */

// ✓ pushGroup: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pushGroup(s: State, capture: string | State.UnnamedCaptureKind, unscanned: string, caseInsensitive: boolean | undefined): any {
  return t<State.from<{ unscanned: typeof unscanned; groups: [ ...(typeof s)["groups"], typeof s ]; capture: typeof capture; branches: [ ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: typeof caseInsensitive extends boolean ? typeof caseInsensitive : (typeof s)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
}
/* compiles to:
 * export type pushGroup<
 *   s extends State,
 *   capture extends string | State.UnnamedCaptureKind,
 *   unscanned extends string,
 *   caseInsensitive extends boolean | undefined
 * > =
 *   State.from<
 *     { unscanned: unscanned; groups: [ ...(s)["groups"], s ]; capture: capture; branches: [ ]; sequence: SequenceTree.Empty; root: ""; caseInsensitive: caseInsensitive extends boolean ? caseInsensitive : (s)["caseInsensitive"]; flags: (s)["flags"]; }
 *   >
 */

// ✗ popGroup: the ScriptType does not itself typecheck as TypeScript
//   popGroup.st.ts(3:34) TS2713: Cannot access 'Group.pop' because 'Group' is a type, but not a namespace. Did you mean to retrieve the type of the property 'pop' i
/* @scripttype preserveParamNames */
export function popGroup(s: State, unscanned: string): any {
  const m1 = matches<State.Group.pop<Hole<"last">, Hole<"init">>>(s['groups'])
  if (m1) {
    return t<State.from<{ unscanned: typeof unscanned; groups: typeof m1.init; capture: (typeof m1.last)["capture"]; branches: (typeof m1.last)["branches"]; sequence: pushQuantifiable<(typeof m1.last)["sequence"], (typeof m1.last)["root"]>; root: (typeof s)["capture"] extends CapturedGroupKind ? GroupTree<State.Group.finalize<typeof s>, (typeof s)["capture"]> : (typeof s)["capture"] extends State.UnnamedCaptureKind.lookaround ? "" : State.Group.finalize<typeof s>; caseInsensitive: (typeof m1.last)["caseInsensitive"]; flags: (typeof s)["flags"]; }>>()
  }
  return t<s.error<writeUnmatchedGroupCloseMessage<")", typeof unscanned>>>()
}
/* compiles to:
 * export type popGroup<s extends State, unscanned extends string> =
 *   s['groups'] extends State.Group.pop<any, any>
 *     ? State.from<
 *       { unscanned: unscanned; groups: typeof m1.init; capture: (typeof m1.last)["capture"]; branches: (typeof m1.last)["branches"]; sequence: pushQuantifiable<(typeof m1.last)["sequence"], (typeof m1.last)["root"]>; root: (s)["capture"] extends CapturedGroupKind ? GroupTree<State.Group.finalize<s>, (s)["capture"]> : (s)["capture"] extends State.UnnamedCaptureKind.lookaround ? "" : State.Group.finalize<s>; caseInsensitive: (typeof m1.last)["caseInsensitive"]; flags: (s)["flags"]; }
 *     >
 *     : s.error<writeUnmatchedGroupCloseMessage<')', unscanned>>
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(s: State): any {
  if (matches<[ unknown, ...unknown[] ]>(s['groups'])) {
    return ErrorMessage(writeUnclosedGroupMessage(')'))
  }
  return finalizeRegexOrError(finalizeTree(t<State.Group.finalize<typeof s>>(), { captures: EmptyCaptures, names: {}, flags: s['flags'], errors: [] }))
}
/* compiles to:
 * export type finalize<s extends State> =
 *   s['groups'] extends [unknown, ...unknown[]]
 *     ? ErrorMessage<writeUnclosedGroupMessage<')'>>
 *     : finalizeRegexOrError<
 *       finalizeTree<
 *         State.Group.finalize<s>,
 *         { captures: EmptyCaptures; names: {}; flags: s['flags']; errors: [] }
 *       >
 *     >
 */

// ✓ finalizeRegexOrError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeRegexOrError(r: FinalizationResult): any {
  if (matches<[ ]>(r['ctx']['errors'])) {
    const m1 = matches<Hole<"pattern", string>>(applyAnchors(r['pattern']))
    if (m1) {
      if (matches<false>(contains(m1.pattern, StartAnchorMarker))) {
        if (matches<false>(contains(m1.pattern, EndAnchorMarker))) {
          return Regex(m1.pattern, finalizeContext(r['ctx']))
        }
        return ErrorMessage(writeMidAnchorError('$'))
      }
      return ErrorMessage(writeMidAnchorError('^'))
    }
    return never
  }
  return r['ctx']['errors'][0]
}
/* compiles to:
 * export type finalizeRegexOrError<r extends FinalizationResult> =
 *   r['ctx']['errors'] extends []
 *     ? applyAnchors<r['pattern']> extends (infer pattern extends string)
 *       ? contains<pattern, StartAnchorMarker> extends false
 *         ? contains<pattern, EndAnchorMarker> extends false
 *           ? Regex<pattern, finalizeContext<r['ctx']>>
 *           : ErrorMessage<writeMidAnchorError<'$'>>
 *         : ErrorMessage<writeMidAnchorError<'^'>>
 *       : never
 *     : r['ctx']['errors'][0]
 */

// ✓ finalizeContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeContext(ctx: FinalizationContext): any {
  if (matches<EmptyCaptures>(ctx['captures'])) {
    return finalizeContextWithoutCaptures(ctx)
  }
  const m1 = matches<([ IndexedCaptureOffset, ...Hole<"rest", IndexedCaptures> ])>(ctx['captures'])
  return finalizeContextWithCaptures({ captures: m1 ? m1.rest : never, names: ctx['names'], flags: ctx['flags'], errors: ctx['errors'] })
}
/* compiles to:
 * export type finalizeContext<ctx extends FinalizationContext> =
 *   ctx['captures'] extends EmptyCaptures
 *     ? finalizeContextWithoutCaptures<ctx>
 *     : finalizeContextWithCaptures<
 *       {
 *         captures: ctx['captures'] extends [IndexedCaptureOffset, ...infer rest extends IndexedCaptures]
 *           ? rest
 *           : never
 *         names: ctx['names']
 *         flags: ctx['flags']
 *         errors: ctx['errors']
 *       }
 *     >
 */

// ✓ finalizeContextWithoutCaptures: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeContextWithoutCaptures(ctx: FinalizationContext): any {
  if (matches<"">(ctx['flags'])) {
    return {}
  }
  return { flags: ctx['flags'] }
}
/* compiles to:
 * export type finalizeContextWithoutCaptures<ctx extends FinalizationContext> =
 *   ctx['flags'] extends '' ? {} : { flags: ctx['flags'] }
 */

// ✓ finalizeContextWithCaptures: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeContextWithCaptures(ctx: FinalizationContext): any {
  if (matches<never>(keyof(ctx['names']))) {
    if (matches<"">(ctx['flags'])) {
      return { captures: ctx['captures'] }
    }
    return { captures: ctx['captures'], flags: ctx['flags'] }
  }
  if (matches<"">(ctx['flags'])) {
    return { captures: ctx['captures'], names: ctx['names'] }
  }
  return { captures: ctx['captures'], names: ctx['names'], flags: ctx['flags'] }
}
/* compiles to:
 * export type finalizeContextWithCaptures<ctx extends FinalizationContext> =
 *   keyof ctx['names'] extends never
 *     ? ctx['flags'] extends ''
 *       ? { captures: ctx['captures'] }
 *       : { captures: ctx['captures']; flags: ctx['flags'] }
 *   : ctx['flags'] extends '' ? { captures: ctx['captures']; names: ctx['names'] }
 *   : { captures: ctx['captures']; names: ctx['names']; flags: ctx['flags'] }
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(self: ReferenceNode, ctx: FinalizationContext, to: string = self['to']): any {
  if (matches<NumberLiteral & keyof (typeof ctx)["captures"]>(to)) {
    if (matches<IncompleteCaptureGroup>(ctx['captures'][to])) {
      return t<FinalizationResult.error<typeof ctx, writeIncompleteReferenceError<typeof to>>>()
    }
    return t<FinalizationResult.from<{ pattern: inferReference<(typeof ctx)["captures"][typeof to]>; ctx: typeof ctx; }>>()
  }
  if (to in ctx['names']) {
    if (matches<IncompleteCaptureGroup>(ctx['names'][to])) {
      return t<FinalizationResult.error<typeof ctx, writeIncompleteReferenceError<typeof to>>>()
    }
    return t<FinalizationResult.from<{ pattern: inferReference<(typeof ctx)["names"][typeof to]>; ctx: typeof ctx; }>>()
  }
  return t<FinalizationResult.error<typeof ctx, writeUnresolvableBackreferenceMessage<typeof to>>>()
}
/* compiles to:
 * export type finalize<
 *   self extends ReferenceNode,
 *   ctx extends FinalizationContext,
 *   to extends string = self['to']
 * > =
 *   to extends NumberLiteral & keyof ctx['captures']
 *     ? ctx['captures'][to] extends IncompleteCaptureGroup
 *       ? FinalizationResult.error<ctx, writeIncompleteReferenceError<to>>
 *       : FinalizationResult.from<{ pattern: inferReference<(ctx)["captures"][to]>; ctx: ctx; }>
 *   : to extends keyof ctx['names']
 *     ? ctx['names'][to] extends IncompleteCaptureGroup
 *       ? FinalizationResult.error<ctx, writeIncompleteReferenceError<to>>
 *       : FinalizationResult.from<{ pattern: inferReference<(ctx)["names"][to]>; ctx: ctx; }>
 *   : FinalizationResult.error<ctx, writeUnresolvableBackreferenceMessage<to>>
 */

// ✓ inferReference: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferReference(to: string | undefined): any {
  if (typeof to === 'string') {
    return to
  }
  return ''
}
/* compiles to:
 * export type inferReference<to extends string | undefined> = to extends string ? to : ''
 */

// ✓ writeIncompleteReferenceError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeIncompleteReferenceError(ref: string): any {
  return `Reference to incomplete group '${ref}' has no effect`
}
/* compiles to:
 * export type writeIncompleteReferenceError<ref extends string> = `Reference to incomplete group '${ref}' has no effect`
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(self: SequenceTree, ctx: FinalizationContext): any {
  return _finalize(self['ast'], '', ctx)
}
/* compiles to:
 * export type finalize<self extends SequenceTree, ctx extends FinalizationContext> = _finalize<
 *   self['ast'],
 *   '',
 *   ctx
 * >
 */

// ✓ _finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _finalize(tree: unknown[], pattern: string, ctx: FinalizationContext): any {
  const m1 = matches<[ Hole<"head">, ...Hole<"tail"> ]>(tree)
  if (m1) {
    const m2 = matches<Hole<"r">>(finalizeTree(m1.head, ctx))
    if (m2) {
      if (matches<FinalizationResult>(m2.r)) {
        return _finalize(m1.tail, appendNonRedundant(pattern, m2.r['pattern']), m2.r['ctx'])
      }
      return never
    }
    return never
  }
  return t<FinalizationResult.from<{ pattern: typeof pattern; ctx: typeof ctx; }>>()
}
/* compiles to:
 * export type _finalize<
 *   tree extends unknown[],
 *   pattern extends string,
 *   ctx extends FinalizationContext
 * > =
 *   tree extends [infer head, ...infer tail]
 *     ? finalizeTree<head, ctx> extends infer r
 *       ? r extends FinalizationResult
 *         ? _finalize<tail, appendNonRedundant<pattern, r['pattern']>, r['ctx']>
 *         : never
 *       : never
 *     : FinalizationResult.from<{ pattern: pattern; ctx: ctx; }>
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(self: UnionTree, ctx: FinalizationContext): any {
  return _finalize(self['ast'], [], ctx)
}
/* compiles to:
 * export type finalize<self extends UnionTree, ctx extends FinalizationContext> = _finalize<
 *   self['ast'],
 *   [],
 *   ctx
 * >
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(b: FinalizedBranch): any {
  return b
}
/* compiles to:
 * export type from<b extends FinalizedBranch> = b
 */

// ✓ _finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _finalize(astBranches: unknown[], acc: FinalizedBranch[], ctx: FinalizationContext): any {
  const m1 = matches<[ Hole<"head">, ...Hole<"tail"> ]>(astBranches)
  if (m1) {
    const m2 = matches<Hole<"r">>(finalizeTree(m1.head, ctx))
    if (m2) {
      if (matches<FinalizationResult>(m2.r)) {
        return _finalize(m1.tail, finalizeBranch(acc, ctx, m2.r), ctx)
      }
      return never
    }
    return never
  }
  return finalizeBranches(keyof(acc), acc, ctx)
}
/* compiles to:
 * export type _finalize<
 *   astBranches extends unknown[],
 *   acc extends FinalizedBranch[],
 *   ctx extends FinalizationContext
 * > =
 *   astBranches extends [infer head, ...infer tail]
 *     ? finalizeTree<head, ctx> extends infer r
 *       ? r extends FinalizationResult ? _finalize<tail, finalizeBranch<acc, ctx, r>, ctx> : never
 *       : never
 *     : finalizeBranches<keyof acc, acc, ctx>
 */

// ✓ finalizeBranch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeBranch(acc: FinalizedBranch[], ctx: FinalizationContext, r: FinalizationResult): any {
  return [...acc, t<FinalizedBranch.from<{ pattern: (typeof r)["pattern"]; captures: finalizeBranchCaptures<typeof acc, typeof ctx, typeof r>; names: (typeof r)["ctx"]["names"]; }>>()]
}
/* compiles to:
 * export type finalizeBranch<
 *   acc extends FinalizedBranch[],
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult
 * > =
 *   [
 *     ...acc,
 *     FinalizedBranch.from<
 *       { pattern: (r)["pattern"]; captures: finalizeBranchCaptures<acc, ctx, r>; names: (r)["ctx"]["names"]; }
 *     >
 *   ]
 */

// ✓ finalizeBranchCaptures: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeBranchCaptures(acc: FinalizedBranch[], ctx: FinalizationContext, r: FinalizationResult, branchCaptures: IndexedCaptures = extractNewCaptures(ctx['captures'], r['ctx']['captures'])): any {
  if (matches<[ ]>(acc)) {
    return branchCaptures
  }
  const m1 = matches<(Hole<"firstCaptureBranch", IndexedCaptures>)>(acc[0]['captures'])
  if (m1) {
    if (matches<[ ]>(branchCaptures)) {
      const out = emptyObject
      for (const i in keyof(m1.firstCaptureBranch)) {
        out[i] = Undefined
      }
      return out
    }
    const out2 = emptyObject
    for (const i in keyof(m1.firstCaptureBranch)) {
      out2[i] = Undefined
    }
    return [...out2, ...branchCaptures]
  }
  return never
}
/* compiles to:
 * export type finalizeBranchCaptures<
 *   acc extends FinalizedBranch[],
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult,
 *   branchCaptures extends IndexedCaptures = extractNewCaptures<ctx['captures'], r['ctx']['captures']>
 * > =
 *   acc extends [] ? branchCaptures
 *   : acc[0]['captures'] extends (infer firstCaptureBranch extends IndexedCaptures)
 *     ? branchCaptures extends []
 *       ? { [I in keyof firstCaptureBranch]: undefined }
 *       : [...{ [I1 in keyof firstCaptureBranch]: undefined }, ...branchCaptures]
 *   : never
 */

// ✗ finalizeBranches: does not compile yet
//   Type '{ pattern: acc[i]["pattern"]; ctx: { flags: ctx["flags"]; captures: [...ctx["captures"], ...acc[i]["captures"]]; names: { [x: string]: unionKeyOf<acc[numb
/* @scripttype preserveParamNames */
export function finalizeBranches(i, acc: FinalizedBranch[], ctx: FinalizationContext): any {
  if (matches<keyof typeof acc & NumberLiteral>(i)) {
    return t<FinalizationResult.from<{ pattern: (typeof acc)[typeof i]["pattern"]; ctx: { flags: (typeof ctx)["flags"]; captures: [ ...(typeof ctx)["captures"], ...(typeof acc)[typeof i]["captures"] ]; names: { [k in unionKeyOf<(typeof acc)[number]["names"]>]: k extends (keyof (typeof acc)[typeof i]["names"]) ? (typeof acc)[typeof i]["names"][k] : undefined; }; errors: (typeof ctx)["errors"]; }; }>>()
  }
  return never
}
/* compiles to:
 * export type finalizeBranches<
 *   i,
 *   acc extends FinalizedBranch[],
 *   ctx extends FinalizationContext
 * > =
 *   i extends keyof acc & NumberLiteral
 *     ? FinalizationResult.from<
 *       { pattern: (acc)[i]["pattern"]; ctx: { flags: (ctx)["flags"]; captures: [ ...(ctx)["captures"], ...(acc)[i]["captures"] ]; names: { [k in unionKeyOf<(acc)[number]["names"]>]: k extends (keyof (acc)[i]["names"]) ? (acc)[i]["names"][k] : undefined; }; errors: (ctx)["errors"]; }; }
 *     >
 *     : never
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(self: GroupTree, ctx: FinalizationContext): any {
  const m1 = matches<Hole<"r">>(finalizeGroupAst(self, ctx))
  if (m1) {
    if (matches<FinalizationResult>(m1.r)) {
      return finalizeGroupResult(self, ctx, m1.r)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type finalize<self extends GroupTree, ctx extends FinalizationContext> =
 *   finalizeGroupAst<self, ctx> extends infer r
 *     ? r extends FinalizationResult ? finalizeGroupResult<self, ctx, r> : never
 *     : never
 */

// ✗ finalizeGroupAst: the ScriptType does not itself typecheck as TypeScript
//   finalizeGroupAst.st.ts(7:246) TS2713: Cannot access 'UnnamedCaptureKind.indexed' because 'UnnamedCaptureKind' is a type, but not a namespace. Did you mean to re
/* @scripttype preserveParamNames */
export function finalizeGroupAst(self: GroupTree, ctx: FinalizationContext): any {
  const out = emptyObject
  for (const _ in keySet(self['capture'])) {
    out[_] = IncompleteCaptureGroup
  }
  return finalizeTree(self['ast'], typeof self['capture'] === 'string' ? { captures: [...ctx['captures'], IncompleteCaptureGroup], names: merge(ctx['names'], out), flags: ctx['flags'], errors: ctx['errors'] } : (matches<State.UnnamedCaptureKind.indexed>(self['capture']) ? { captures: [...ctx['captures'], IncompleteCaptureGroup], names: ctx['names'], flags: ctx['flags'], errors: ctx['errors'] } : ctx))
}
/* compiles to:
 * export type finalizeGroupAst<self extends GroupTree, ctx extends FinalizationContext> = finalizeTree<
 *   self['ast'],
 *   self['capture'] extends string
 *     ? {
 *       captures: [...ctx['captures'], IncompleteCaptureGroup]
 *       names: ctx['names'] & { [_ in self['capture']]: IncompleteCaptureGroup }
 *       flags: ctx['flags']
 *       errors: ctx['errors']
 *     }
 *   : self['capture'] extends State.UnnamedCaptureKind.indexed
 *     ? {
 *       captures: [...ctx['captures'], IncompleteCaptureGroup]
 *       names: ctx['names']
 *       flags: ctx['flags']
 *       errors: ctx['errors']
 *     }
 *   : ctx
 * >
 */

// ✗ finalizeGroupResult: the ScriptType does not itself typecheck as TypeScript
//   finalizeGroupResult.st.ts(3:302) TS2713: Cannot access 'UnnamedCaptureKind.indexed' because 'UnnamedCaptureKind' is a type, but not a namespace. Did you mean to
/* @scripttype preserveParamNames */
export function finalizeGroupResult(self: GroupTree, ctx: FinalizationContext, r: FinalizationResult): any {
  return t<FinalizationResult.from<{ pattern: (typeof r)["pattern"]; ctx: (typeof self)["capture"] extends string ? finalizeNamedCapture<(typeof self)["capture"], (typeof ctx)["captures"]["length"], (typeof r)["pattern"], (typeof r)["ctx"]> : (typeof self)["capture"] extends State.UnnamedCaptureKind.indexed ? finalizeUnnamedCapture<(typeof ctx)["captures"]["length"], (typeof r)["pattern"], (typeof r)["ctx"]> : (typeof r)["ctx"]; }>>()
}
/* compiles to:
 * export type finalizeGroupResult<
 *   self extends GroupTree,
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult
 * > =
 *   FinalizationResult.from<
 *     { pattern: (r)["pattern"]; ctx: (self)["capture"] extends string ? finalizeNamedCapture<(self)["capture"], (ctx)["captures"]["length"], (r)["pattern"], (r)["ctx"]> : (self)["capture"] extends State.UnnamedCaptureKind.indexed ? finalizeUnnamedCapture<(ctx)["captures"]["length"], (r)["pattern"], (r)["ctx"]> : (r)["ctx"]; }
 *   >
 */

// ✓ finalizeNamedCapture: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeNamedCapture(name: string, index: number, pattern: string, ctx: FinalizationContext): any {
  return t<FinalizationContext.from<{ captures: setIndex<(typeof ctx)["captures"], typeof index, anchorsAway<typeof pattern>>; names: { [k in keyof (typeof ctx)["names"]]: k extends typeof name ? anchorsAway<typeof pattern> : (typeof ctx)["names"][k]; }; flags: (typeof ctx)["flags"]; errors: (typeof ctx)["errors"]; }>>()
}
/* compiles to:
 * export type finalizeNamedCapture<
 *   name extends string,
 *   index extends number,
 *   pattern extends string,
 *   ctx extends FinalizationContext
 * > =
 *   FinalizationContext.from<
 *     { captures: setIndex<(ctx)["captures"], index, anchorsAway<pattern>>; names: { [k in keyof (ctx)["names"]]: k extends name ? anchorsAway<pattern> : (ctx)["names"][k]; }; flags: (ctx)["flags"]; errors: (ctx)["errors"]; }
 *   >
 */

// ✓ finalizeUnnamedCapture: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeUnnamedCapture(index: number, pattern: string, ctx: FinalizationContext): any {
  return t<FinalizationContext.from<{ captures: setIndex<(typeof ctx)["captures"], typeof index, anchorsAway<typeof pattern>>; names: (typeof ctx)["names"]; flags: (typeof ctx)["flags"]; errors: (typeof ctx)["errors"]; }>>()
}
/* compiles to:
 * export type finalizeUnnamedCapture<
 *   index extends number,
 *   pattern extends string,
 *   ctx extends FinalizationContext
 * > =
 *   FinalizationContext.from<
 *     { captures: setIndex<(ctx)["captures"], index, anchorsAway<pattern>>; names: (ctx)["names"]; flags: (ctx)["flags"]; errors: (ctx)["errors"]; }
 *   >
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(self: QuantifierTree, ctx: FinalizationContext): any {
  const m1 = matches<Hole<"r", FinalizationResult>>(finalizeTree(self['ast'], ctx))
  if (m1) {
    return finalizeQuantifierResult(self, ctx, m1.r)
  }
  return never
}
/* compiles to:
 * export type finalize<self extends QuantifierTree, ctx extends FinalizationContext> =
 *   finalizeTree<self['ast'], ctx> extends (infer r extends FinalizationResult)
 *     ? finalizeQuantifierResult<self, ctx, r>
 *     : never
 */

// ✓ finalizeQuantifierResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeQuantifierResult(self: QuantifierTree, ctx: FinalizationContext, r: FinalizationResult, quantifiedCaptures: IndexedCaptures = extractNewCaptures(ctx['captures'], r['ctx']['captures'])): any {
  if (matches<0>(self['min'])) {
    if (matches<[ ]>(quantifiedCaptures)) {
      return finalizeNonZeroMinQuantified(self, r)
    }
    return finalizeZeroMinQuantifiedWithCaptures(self, ctx, r, quantifiedCaptures)
  }
  return finalizeNonZeroMinQuantified(self, r)
}
/* compiles to:
 * export type finalizeQuantifierResult<
 *   self extends QuantifierTree,
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult,
 *   quantifiedCaptures extends IndexedCaptures = extractNewCaptures<ctx['captures'], r['ctx']['captures']>
 * > =
 *   self['min'] extends 0
 *     ? quantifiedCaptures extends []
 *       ? finalizeNonZeroMinQuantified<self, r>
 *       : finalizeZeroMinQuantifiedWithCaptures<self, ctx, r, quantifiedCaptures>
 *     : finalizeNonZeroMinQuantified<self, r>
 */

// ✓ finalizeNonZeroMinQuantified: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeNonZeroMinQuantified(self: QuantifierTree, r: FinalizationResult): any {
  return t<FinalizationResult.from<{ pattern: quantify<(typeof r)["pattern"], (typeof self)["min"], (typeof self)["max"]>; ctx: (typeof r)["ctx"]; }>>()
}
/* compiles to:
 * export type finalizeNonZeroMinQuantified<
 *   self extends QuantifierTree,
 *   r extends FinalizationResult
 * > =
 *   FinalizationResult.from<
 *     { pattern: quantify<(r)["pattern"], (self)["min"], (self)["max"]>; ctx: (r)["ctx"]; }
 *   >
 */

// ✓ finalizeZeroMinQuantifiedWithCaptures: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeZeroMinQuantifiedWithCaptures(self: QuantifierTree, ctx: FinalizationContext, r: FinalizationResult, quantifiedCaptures: IndexedCaptures): any {
  return finalizeZeroQuantified(ctx, r, quantifiedCaptures) | finalizeOnePlusQuantified(self['max'], r)
}
/* compiles to:
 * export type finalizeZeroMinQuantifiedWithCaptures<
 *   self extends QuantifierTree,
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult,
 *   quantifiedCaptures extends IndexedCaptures
 * > =
 *   finalizeZeroQuantified<ctx, r, quantifiedCaptures> | finalizeOnePlusQuantified<self['max'], r>
 */

// ✗ finalizeZeroQuantified: the ScriptType does not itself typecheck as TypeScript
//   finalizeZeroQuantified.st.ts(3:99) TS2574: A rest element type must be an array type.
/* @scripttype preserveParamNames */
export function finalizeZeroQuantified(ctx: FinalizationContext, r: FinalizationResult, quantifiedCaptures: IndexedCaptures): any {
  return t<FinalizationResult.from<{ pattern: ""; ctx: { captures: [ ...(typeof ctx)["captures"], ...{ [i in keyof typeof quantifiedCaptures]: undefined; } ]; flags: (typeof r)["ctx"]["flags"]; names: zeroQuantifiedNames<(typeof ctx)["names"], (typeof r)["ctx"]["names"]>; errors: (typeof r)["ctx"]["errors"]; }; }>>()
}
/* compiles to:
 * export type finalizeZeroQuantified<
 *   ctx extends FinalizationContext,
 *   r extends FinalizationResult,
 *   quantifiedCaptures extends IndexedCaptures
 * > =
 *   FinalizationResult.from<
 *     { pattern: ""; ctx: { captures: [ ...(ctx)["captures"], ...{ [i in keyof quantifiedCaptures]: undefined; } ]; flags: (r)["ctx"]["flags"]; names: zeroQuantifiedNames<(ctx)["names"], (r)["ctx"]["names"]>; errors: (r)["ctx"]["errors"]; }; }
 *   >
 */

// ✓ zeroQuantifiedNames: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {NamedCaptures} result
 */
export function zeroQuantifiedNames(base: NamedCaptures, result): any {
  const out = emptyObject
  for (const k in keyof(result)) {
    out[k] = k in base ? result[k] : Undefined
  }
  return out & unknown
}
/* compiles to:
 * export type zeroQuantifiedNames<base extends NamedCaptures, result extends NamedCaptures> =
 *   { [K in keyof result]: K extends keyof base ? result[K] : undefined } & unknown
 */

// ✓ finalizeOnePlusQuantified: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeOnePlusQuantified(max: number | null, r: FinalizationResult): any {
  if (matches<1>(max)) {
    return r
  }
  return t<FinalizationResult.from<{ pattern: quantify<(typeof r)["pattern"], 1, typeof max>; ctx: (typeof r)["ctx"]; }>>()
}
/* compiles to:
 * export type finalizeOnePlusQuantified<
 *   max extends number | null,
 *   r extends FinalizationResult
 * > =
 *   max extends 1
 *     ? r
 *     : FinalizationResult.from<{ pattern: quantify<(r)["pattern"], 1, max>; ctx: (r)["ctx"]; }>
 */

// ✓ pushQuantifiable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pushQuantifiable(sequence: RegexAst, root: RegexAst): any {
  if (matches<"">(root)) {
    return sequence
  }
  if (typeof sequence === 'string') {
    if (matches<"">(sequence)) {
      return root
    }
    if (typeof root === 'string') {
      return appendNonRedundant(sequence, root)
    }
    return SequenceTree([sequence, root])
  }
  if (matches<SequenceTree>(sequence)) {
    return pushToSequence(sequence, root)
  }
  return SequenceTree([sequence, root])
}
/* compiles to:
 * export type pushQuantifiable<sequence extends RegexAst, root extends RegexAst> =
 *   root extends '' ? sequence
 *   : sequence extends string
 *     ? sequence extends '' ? root
 *     : root extends string ? appendNonRedundant<sequence, root>
 *     : SequenceTree<[sequence, root]>
 *   : sequence extends SequenceTree ? pushToSequence<sequence, root>
 *   : SequenceTree<[sequence, root]>
 */

// ✓ pushToSequence: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pushToSequence(sequence: SequenceTree, root: RegexAst): any {
  if (matches<SequenceTree.Empty>(sequence)) {
    return root
  }
  if (matches<SequenceTree>(root)) {
    return SequenceTree([...sequence['ast'], ...root['ast']])
  }
  return SequenceTree([...sequence['ast'], root])
}
/* compiles to:
 * export type pushToSequence<sequence extends SequenceTree, root extends RegexAst> =
 *   sequence extends SequenceTree.Empty ? root
 *   : root extends SequenceTree ? SequenceTree<[...sequence['ast'], ...root['ast']]>
 *   : SequenceTree<[...sequence['ast'], root]>
 */

// ✓ extractNewCaptures: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractNewCaptures(base: IndexedCaptures, result: IndexedCaptures): any {
  const m1 = matches<readonly [ ...typeof base, ...Hole<"elements", IndexedCaptures> ]>(result)
  if (m1) {
    return m1.elements
  }
  return []
}
/* compiles to:
 * export type extractNewCaptures<base extends IndexedCaptures, result extends IndexedCaptures> =
 *   result extends readonly [...base, ...infer elements extends IndexedCaptures] ? elements : []
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(ctx: FinalizationContext): any {
  return ctx
}
/* compiles to:
 * export type from<ctx extends FinalizationContext> = ctx
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(r: FinalizationResult): any {
  return r
}
/* compiles to:
 * export type from<r extends FinalizationResult> = r
 */

// ✓ error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function error(ctx: FinalizationContext, message: string): any {
  return from({ pattern: string, ctx: { captures: ctx['captures'], names: ctx['names'], flags: ctx['flags'], errors: [...ctx['errors'], ErrorMessage(message)] } })
}
/* compiles to:
 * export type error<ctx extends FinalizationContext, message extends string> = from<
 *   {
 *     pattern: string
 *     ctx: {
 *       captures: ctx['captures']
 *       names: ctx['names']
 *       flags: ctx['flags']
 *       errors: [...ctx['errors'], ErrorMessage<message>]
 *     }
 *   }
 * >
 */

// ✓ finalizeTree: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeTree(tree, ctx: FinalizationContext): any {
  if (typeof tree === 'string') {
    return t<FinalizationResult.from<{ pattern: typeof tree; ctx: typeof ctx; }>>()
  }
  if (matches<SequenceTree>(tree)) {
    return t<SequenceTree.finalize<typeof tree, typeof ctx>>()
  }
  if (matches<UnionTree>(tree)) {
    return t<UnionTree.finalize<typeof tree, typeof ctx>>()
  }
  if (matches<GroupTree>(tree)) {
    return t<GroupTree.finalize<typeof tree, typeof ctx>>()
  }
  if (matches<QuantifierTree>(tree)) {
    return t<QuantifierTree.finalize<typeof tree, typeof ctx>>()
  }
  if (matches<ReferenceNode>(tree)) {
    return t<ReferenceNode.finalize<typeof tree, typeof ctx>>()
  }
  return never
}
/* compiles to:
 * export type finalizeTree<tree, ctx extends FinalizationContext> =
 *   tree extends string ? FinalizationResult.from<{ pattern: tree; ctx: ctx; }>
 *   : tree extends SequenceTree ? SequenceTree.finalize<tree, ctx>
 *   : tree extends UnionTree ? UnionTree.finalize<tree, ctx>
 *   : tree extends GroupTree ? GroupTree.finalize<tree, ctx>
 *   : tree extends QuantifierTree ? QuantifierTree.finalize<tree, ctx>
 *   : tree extends ReferenceNode ? ReferenceNode.finalize<tree, ctx>
 *   : never
 */

// ✓ applyAnchors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyAnchors(pattern: string): any {
  const m1 = matches<`${StartAnchorMarker}${Hole<"startStripped">}`>(pattern)
  if (m1) {
    const m2 = matches<`${Hole<"bothStripped">}${EndAnchorMarker}`>(m1.startStripped)
    if (m2) {
      return m2.bothStripped
    }
    return appendNonRedundant(m1.startStripped, string)
  }
  const m3 = matches<`${Hole<"endStripped">}${EndAnchorMarker}`>(pattern)
  if (m3) {
    return prependNonRedundant(m3.endStripped, string)
  }
  return prependNonRedundant(appendNonRedundant(pattern, string), string)
}
/* compiles to:
 * export type applyAnchors<pattern extends string> =
 *   pattern extends `${StartAnchorMarker}${infer startStripped}`
 *     ? startStripped extends `${infer bothStripped}${EndAnchorMarker}`
 *       ? bothStripped
 *       : appendNonRedundant<startStripped, string>
 *   : pattern extends `${infer endStripped}${EndAnchorMarker}`
 *     ? prependNonRedundant<endStripped, string>
 *   : prependNonRedundant<appendNonRedundant<pattern, string>, string>
 */

// ✓ anchorsAway: verified type-identical to the original
/* @scripttype preserveParamNames */
export function anchorsAway(pattern: string): any {
  const m1 = matches<`${StartAnchorMarker}${Hole<"startStripped">}`>(pattern)
  if (m1) {
    const m2 = matches<`${Hole<"bothStripped">}${EndAnchorMarker}`>(m1.startStripped)
    if (m2) {
      return m2.bothStripped
    }
    return m1.startStripped
  }
  const m3 = matches<`${Hole<"endStripped">}${EndAnchorMarker}`>(pattern)
  if (m3) {
    return m3.endStripped
  }
  return pattern
}
/* compiles to:
 * export type anchorsAway<pattern extends string> =
 *   pattern extends `${StartAnchorMarker}${infer startStripped}`
 *     ? startStripped extends `${infer bothStripped}${EndAnchorMarker}`
 *       ? bothStripped
 *       : startStripped
 *   : pattern extends `${infer endStripped}${EndAnchorMarker}` ? endStripped
 *   : pattern
 */

// ✓ appendNonRedundant: verified type-identical to the original
/* @scripttype preserveParamNames */
export function appendNonRedundant(base: string, suffix: string): any {
  if (matches<typeof base>(string)) {
    if (matches<typeof suffix>(string)) {
      return string
    }
    return `${base}${suffix}`
  }
  if (matches<typeof base>(`${number}`)) {
    if (matches<typeof suffix>(`${number}`)) {
      return `${number}`
    }
    return `${base}${suffix}`
  }
  return `${base}${suffix}`
}
/* compiles to:
 * export type appendNonRedundant<base extends string, suffix extends string> =
 *   string extends base ? string extends suffix ? string : `${base}${suffix}`
 *   : `${number}` extends base ? `${number}` extends suffix ? `${number}` : `${base}${suffix}`
 *   : `${base}${suffix}`
 */

// ✓ prependNonRedundant: verified type-identical to the original
/* @scripttype preserveParamNames */
export function prependNonRedundant(base: string, prefix: string): any {
  if (matches<typeof base>(string)) {
    if (matches<typeof prefix>(string)) {
      return string
    }
    return `${prefix}${base}`
  }
  if (matches<typeof base>(`${number}`)) {
    if (matches<typeof prefix>(`${number}`)) {
      return `${number}`
    }
    return `${prefix}${base}`
  }
  return `${prefix}${base}`
}
/* compiles to:
 * export type prependNonRedundant<base extends string, prefix extends string> =
 *   string extends base ? string extends prefix ? string : `${prefix}${base}`
 *   : `${number}` extends base ? `${number}` extends prefix ? `${number}` : `${prefix}${base}`
 *   : `${prefix}${base}`
 */

// ✓ writeMidAnchorError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeMidAnchorError(anchor: Anchor): any {
  return `Anchor ${anchor} may not appear mid-pattern`
}
/* compiles to:
 * export type writeMidAnchorError<anchor extends Anchor> = `Anchor ${anchor} may not appear mid-pattern`
 */

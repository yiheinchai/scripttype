/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/generic.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 9 type-level aliases: 9 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { RootSchema } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/kinds.js'
import type { array } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
declare namespace m1 {
  export type name<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BaseRoot: any
declare const GenericRoot: any
declare const Hkt: any
declare const LazyGenericBody: any
declare const RootSchema: any
declare const array: any
declare const body: any
type BaseRoot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GenericParamDef<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GenericRoot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Hkt<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type LazyGenericBody<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type body<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ GenericParamAst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericParamAst(name: string = string, constraint = unknown): any {
  return [name, constraint]
}
/* compiles to:
 * export type GenericParamAst<name extends string = string, constraint = unknown> = [
 *   name,
 *   constraint
 * ]
 */

// ✓ GenericParamDef: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericParamDef(name: string = string): any {
  return anyOf(name, asReadonly([name, unknown]))
}
/* compiles to:
 * export type GenericParamDef<name extends string = string> = name | readonly [name, unknown]
 */

// ✓ genericParamNames: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {array<GenericParamAst>} params
 */
export function genericParamNames(params): any {
  const out = emptyObject
  for (const i in keyof(params)) {
    out[i] = params[i][0]
  }
  return out
}
/* compiles to:
 * export type genericParamNames<params extends array<GenericParamAst>> = {
 *   [I in keyof params]: params[I][0]
 * }
 */

// ✓ genericParamConstraints: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {array<GenericParamAst>} params
 */
export function genericParamConstraints(params): any {
  const out = emptyObject
  for (const i in keyof(params)) {
    out[i] = params[i][1]
  }
  return out
}
/* compiles to:
 * export type genericParamConstraints<params extends array<GenericParamAst>> = {
 *   [I in keyof params]: params[I][1]
 * }
 */

// ✓ GenericArgResolutions: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {array<GenericParamAst>} params
 */
export function GenericArgResolutions(params = array(GenericParamAst)): any {
  const out = emptyObject
  for (const i in keyof(params)) {
    out[params[merge(i, `${number}`)][0]] = BaseRoot
  }
  return out
}
/* compiles to:
 * export type GenericArgResolutions<
 *   params extends array<GenericParamAst> = array<GenericParamAst>
 * > =
 *   { [I in keyof params as params[I & `${number}`][0]]: BaseRoot }
 */

// ✓ genericParamSchemasToAst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function genericParamSchemasToAst__v1(schemas, i): any {
  const m1 = matches<GenericParamDef<Hole<"name">>>(schemas[i])
  if (m1) {
    return [m1.name, unknown]
  }
  return never
}
/**
 * @param {readonly GenericParamDef[]} schemas
 */
export function genericParamSchemasToAst(schemas): any {
  const out = emptyObject
  for (const i in keyof(schemas)) {
    out[i] = genericParamSchemasToAst__v1(schemas, i)
  }
  return out
}
/* compiles to:
 * export type genericParamSchemasToAst__v1<schemas, i> =
 *   schemas[i] extends GenericParamDef<infer name> ? [name, unknown] : never
 * export type genericParamSchemasToAst<schemas extends readonly GenericParamDef[]> = {
 *   [I in keyof schemas]: genericParamSchemasToAst__v1<schemas, I>
 * }
 */

// ✓ genericHktToConstraints: verified type-identical to the original
/* @scripttype preserveParamNames */
export function genericHktToConstraints(hkt: abstract new () => Hkt): any {
  return InstanceType(hkt)['constraints']
}
/* compiles to:
 * export type genericHktToConstraints<hkt extends abstract new () => Hkt> = InstanceType<hkt>['constraints']
 */

// ✓ GenericRootBodyParser: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {array<GenericParamAst>} params
 */
export function GenericRootBodyParser(params): any {
  const out = emptyObject
  for (const i in keyof(params)) {
    out[i] = [params[i][0], genericHktToConstraints(hkt)[i]]
  }
  return { ...genericCallSig(['const body'], [RootSchema], GenericRoot(params, body)), ...genericCallSig(['hkt extends Hkt.constructor'], [LazyGenericBody(GenericArgResolutions(params)), hkt], GenericRoot(out, InstanceType(hkt))) }
}
/* compiles to:
 * export type GenericRootBodyParser<params extends array<GenericParamAst>> = {
 *   <const body>(a0: RootSchema): GenericRoot<params, body>
 *   <hkt extends Hkt.constructor>(a0: LazyGenericBody<GenericArgResolutions<params>>, a1: hkt): GenericRoot<
 *     { [I in keyof params]: [params[I][0], genericHktToConstraints<hkt>[I]] },
 *     InstanceType<hkt>
 *   >
 * }
 */

// ✓ writeUnsatisfiedParameterConstraintMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeUnsatisfiedParameterConstraintMessage(name: string, constraint: string, arg: string): any {
  return `${name} must be assignable to ${constraint} (was ${arg})`
}
/* compiles to:
 * export type writeUnsatisfiedParameterConstraintMessage<
 *   name extends string,
 *   constraint extends string,
 *   arg extends string
 * > =
 *   `${name} must be assignable to ${constraint} (was ${arg})`
 */

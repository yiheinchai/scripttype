/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/module.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BaseRoot: any
declare const InternalResolution: any
declare const anyOrNever: any
type BaseRoot<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InternalResolution<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type anyOrNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ exportSchemaScope: verified type-identical to the original
/* @scripttype preserveParamNames */
export function exportSchemaScope($) {
  const out = emptyObject
  for (const k in keyof($)) {
    out[k] = instantiateRoot($[k])
  }
  return out
}
/* compiles to:
 * export type exportSchemaScope<$> = { [K in keyof $]: instantiateRoot<$[K]> }
 */

// ✓ instantiateRoot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function instantiateRoot(t) {
  if (matches<InternalResolution>(t)) {
    if (matches<[ anyOrNever ]>([t])) {
      return BaseRoot
    }
    return t
  }
  return BaseRoot
}
/* compiles to:
 * export type instantiateRoot<t> = t extends InternalResolution ? [t] extends [anyOrNever] ? BaseRoot : t : BaseRoot
 */

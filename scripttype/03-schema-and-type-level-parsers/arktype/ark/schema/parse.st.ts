/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/parse.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ContextualArgs, NodeId } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/parse.js'
declare const ContextualArgs: any
declare const NodeId: any
// ✓ BaseParseOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseParseOptions(prereduced: boolean = boolean): any {
  return { alias: optional(string), prereduced: optional(prereduced), args: optional(ContextualArgs), id: optional(NodeId) }
}
/* compiles to:
 * export type BaseParseOptions<prereduced extends boolean = boolean> = {
 *   alias?: string
 *   prereduced?: prereduced
 *   args?: ContextualArgs
 *   id?: NodeId
 * }
 */

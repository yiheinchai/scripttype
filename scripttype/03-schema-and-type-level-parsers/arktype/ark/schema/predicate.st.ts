/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/predicate.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 compile-error, 1 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const NormalizedSchema: any
declare const Traversal: any
type NormalizedSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Predicate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Traversal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Schema: does not compile yet
//   Cannot find name 'NormalizedSchema'. Did you mean 'BaseNormalizedSchema'?
/* @scripttype preserveParamNames */
export function Schema(predicate: Predicate = Predicate) {
  return anyOf(NormalizedSchema(predicate), predicate)
}
/* compiles to:
 * export type Schema<predicate extends Predicate = Predicate> = NormalizedSchema<predicate> | predicate
 */

// ✓ Predicate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Predicate(data = any) {
  return fnType([data, Traversal], boolean)
}
/* compiles to:
 * export type Predicate<data = any> = (a0: data, a1: Traversal) => boolean
 */

// ✗ Casted: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function Casted(input = never, narrowed: typeof input = input) {
  return fnType([input, Traversal], raw('input is narrowed'))
}

// ✗ Castable: does not compile yet
//   Cannot find name 'Casted'.
/* @scripttype preserveParamNames */
export function Castable(input = never, narrowed: typeof input = input) {
  return Predicate(input) | Casted(input, narrowed)
}
/* compiles to:
 * export type Castable<input = never, narrowed extends input = input> = Predicate<input> | Casted<input, narrowed>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/roots/intersection.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 4 covered, 3 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BaseNormalizedSchema: any
declare const ConditionalIntersectionKey: any
declare const ConditionalTerminalIntersectionKey: any
declare const ConditionalTerminalIntersectionRoot: any
declare const ConstraintKind: any
declare const Domain: any
declare const FlattenedChildKind: any
declare const Intersection: any
declare const NodeSchema: any
declare const OpenNodeKind: any
declare const Prerequisite: any
declare const Proto: any
declare const listable: any
declare const nodeOfKind: any
declare const show: any
type BaseNormalizedSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConditionalIntersectionKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConditionalTerminalIntersectionKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConditionalTerminalIntersectionRoot<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConstraintKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Domain<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type FlattenedChildKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Intersection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NodeSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OpenNodeKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Prerequisite<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Proto<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type listable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type nodeOfKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type show<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ConstraintsSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConstraintsSchema(inferredBasis = any) {
  return show(merge(BaseNormalizedSchema, { domain: optional(Domain.Schema), proto: optional(Proto.Schema) }, conditionalRootOf(inferredBasis)))
}
/* compiles to:
 * export type ConstraintsSchema<inferredBasis = any> = show<BaseNormalizedSchema & { domain?: Domain.Schema; proto?: Proto.Schema } & conditionalRootOf<inferredBasis>>
 */

// ✗ Schema: does not compile yet
//   Cannot find name 'ConstraintsSchema'. Did you mean 'ConstraintsSchema__st0'?
/* @scripttype preserveParamNames */
export function Schema(inferredBasis = any) {
  return ConstraintsSchema(inferredBasis)
}
/* compiles to:
 * export type Schema<inferredBasis = any> = ConstraintsSchema<inferredBasis>
 */

// ✓ constraintKindOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function constraintKindOf(t) {
  const out = emptyObject
  for (const k in keySet(ConstraintKind)) {
    out[k] = matches<Prerequisite<typeof k>>(t) ? k : never
  }
  return out[ConstraintKind]
}
/* compiles to:
 * export type constraintKindOf<t> = { [K in ConstraintKind]: t extends Prerequisite<K> ? K : never }[ConstraintKind]
 */

// ✓ conditionalIntersectionKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function conditionalIntersectionKeyOf(t) {
  return constraintKindOf(t) | (matches<object>(t) ? 'undeclared' : never)
}
/* compiles to:
 * export type conditionalIntersectionKeyOf<t> = constraintKindOf<t> | (t extends object ? 'undeclared' : never)
 */

// ✗ intersectionChildSchemaValueOf: the ScriptType does not itself typecheck as TypeScript
//   intersectionChildSchemaValueOf.st.ts(2:51) TS2702: 'Intersection' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function intersectionChildSchemaValueOf(k: Intersection.FlattenedChildKind) {
  if (matches<OpenNodeKind>(k)) {
    return listable(NodeSchema(k))
  }
  return NodeSchema(k)
}
/* compiles to:
 * export type intersectionChildSchemaValueOf<k extends Intersection.FlattenedChildKind> = k extends OpenNodeKind ? listable<NodeSchema<k>> : NodeSchema<k>
 */

// ✗ conditionalSchemaValueOfKey: the ScriptType does not itself typecheck as TypeScript
//   conditionalSchemaValueOfKey.st.ts(3:15) TS2702: 'Intersection' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function conditionalSchemaValueOfKey(k: ConditionalIntersectionKey) {
  if (matches<Intersection.FlattenedChildKind>(k)) {
    return intersectionChildSchemaValueOf(k)
  }
  return ConditionalTerminalIntersectionRoot[merge(k, ConditionalTerminalIntersectionKey)]
}
/* compiles to:
 * export type conditionalSchemaValueOfKey<k extends ConditionalIntersectionKey> = k extends Intersection.FlattenedChildKind ? intersectionChildSchemaValueOf<k> : ConditionalTerminalIntersectionRoot[k & ConditionalTerminalIntersectionKey]
 */

// ✗ intersectionChildInnerValueOf: the ScriptType does not itself typecheck as TypeScript
//   intersectionChildInnerValueOf.st.ts(2:50) TS2702: 'Intersection' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function intersectionChildInnerValueOf(k: Intersection.FlattenedChildKind) {
  if (matches<OpenNodeKind>(k)) {
    return readonlyArrayOf(nodeOfKind(k))
  }
  return nodeOfKind(k)
}
/* compiles to:
 * export type intersectionChildInnerValueOf<k extends Intersection.FlattenedChildKind> = k extends OpenNodeKind ? readonly nodeOfKind<k>[] : nodeOfKind<k>
 */

// ✓ conditionalRootOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function conditionalRootOf(t) {
  const out = emptyObject
  for (const k in keySet(conditionalIntersectionKeyOf(t))) {
    out[k] = optional(conditionalSchemaValueOfKey(k))
  }
  return out
}
/* compiles to:
 * export type conditionalRootOf<t> = { [K in conditionalIntersectionKeyOf<t>]?: conditionalSchemaValueOfKey<K> }
 */

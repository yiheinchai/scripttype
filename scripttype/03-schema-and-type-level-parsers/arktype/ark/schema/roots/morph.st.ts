/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/roots/morph.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 mismatch, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Traversal: any
type Morph<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Traversal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ In: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function In(morph: Morph) {
  const m1 = matches<Morph<Hole<"i">>>(morph)
  if (m1) {
    return m1.i
  }
  return never
}
/* compiles to:
 * export type In<morph extends Morph> = morph extends Morph<infer i> ? i : never
 */

// ✗ Out: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Out(morph: Morph) {
  const m1 = matches<Morph<never, Hole<"o">>>(morph)
  if (m1) {
    return m1.o
  }
  return never
}
/* compiles to:
 * export type Out<morph extends Morph> = morph extends Morph<never, infer o> ? o : never
 */

// ✗ ContextFree: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function ContextFree(i = never, o = unknown) {
  return fnType([i], o)
}
/* compiles to:
 * export type ContextFree<i = never, o = unknown> = (a0: i) => o
 */

// ✓ Morph: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Morph(i = never, o = unknown) {
  return fnType([i, Traversal], o)
}
/* compiles to:
 * export type Morph<i = never, o = unknown> = (a0: i, a1: Traversal) => o
 */

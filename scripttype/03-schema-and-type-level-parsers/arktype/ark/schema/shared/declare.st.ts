/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/shared/declare.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 9 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { nodeOfKind, reducibleKindOf } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/kinds.js'
import type { BaseErrorContext, BaseNodeDeclaration, TypeMeta } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/shared/declare.js'
import type { NarrowedAttachments } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/shared/implement.js'
import type { show } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
declare const BaseErrorContext: any
declare const BaseNodeDeclaration: any
declare const DeclarationInput: any
declare const Disjoint: any
declare const NarrowedAttachments: any
declare const TypeMeta: any
declare const nodeOfKind: any
declare const reducibleKindOf: any
declare const show: any
type DeclarationInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Disjoint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ withMetaPrefixedKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function withMetaPrefixedKeys(o): any {
  const out = emptyObject
  for (const k in keyof(o)) {
    out[typeof k === 'string' ? `meta.${k}` : never] = o[k]
  }
  return out
}
/* compiles to:
 * export type withMetaPrefixedKeys<o> = {
 *   [K in keyof o as K extends string ? `meta.${K}` : never]: o[K]
 * }
 */

// ✓ Collapsible: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Collapsible(meta: TypeMeta = TypeMeta): any {
  return anyOf(meta, string)
}
/* compiles to:
 * export type Collapsible<meta extends TypeMeta = TypeMeta> = meta | string
 */

// ✓ Mapper: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mapper(meta: TypeMeta = TypeMeta): any {
  return fnType([Readonly(meta)], meta)
}
/* compiles to:
 * export type Mapper<meta extends TypeMeta = TypeMeta> = (a0: Readonly<meta>) => meta
 */

// ✓ MappableInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MappableInput(meta: TypeMeta = TypeMeta): any {
  return Collapsible(meta) | Mapper(meta)
}
/* compiles to:
 * export type MappableInput<meta extends TypeMeta = TypeMeta> = Collapsible<meta> | Mapper<meta>
 */

// ✓ defaultErrorContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function defaultErrorContext(d: DeclarationInput): any {
  return show(merge(BaseErrorContext(d['kind']), d['inner']))
}
/* compiles to:
 * export type defaultErrorContext<d extends DeclarationInput> = show<
 *   BaseErrorContext<d['kind']> & d['inner']
 * >
 */

// ✗ declareNode: does not compile yet
//   'd' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
/**
 * @param {{ [k in keyof typeof d]: k extends keyof DeclarationInput ? DeclarationInput[k] : never; } & DeclarationInput} d
 */
export function declareNode(d): any {
  return merge({ intersectionIsOpen: false, prerequisite: prerequisiteOf(d), childKind: never, reducibleTo: d['kind'], errorContext: Null }, d)
}
/* compiles to:
 * export type declareNode<
 *   d extends { [k in keyof typeof d]: k extends keyof DeclarationInput ? DeclarationInput[k] : never; } & DeclarationInput
 * > =
 *   & {
 *       intersectionIsOpen: false
 *       prerequisite: prerequisiteOf<d>
 *       childKind: never
 *       reducibleTo: d['kind']
 *       errorContext: null
 *     }
 *   & d
 */

// ✓ prerequisiteOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function prerequisiteOf(d: DeclarationInput): any {
  if ('prerequisite' in d) {
    return d['prerequisite']
  }
  return unknown
}
/* compiles to:
 * export type prerequisiteOf<d extends DeclarationInput> =
 *   'prerequisite' extends keyof d ? d['prerequisite'] : unknown
 */

// ✓ attachmentsOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function attachmentsOf(d: BaseNodeDeclaration): any {
  return NarrowedAttachments(d) & attachedInner(d)
}
/* compiles to:
 * export type attachmentsOf<d extends BaseNodeDeclaration> =
 *   NarrowedAttachments<d> & attachedInner<d>
 */

// ✓ attachedInner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function attachedInner(d: BaseNodeDeclaration): any {
  if (matches<never>(merge('intersection', d['kind']))) {
    return d['inner']
  }
  return {}
}
/* compiles to:
 * export type attachedInner<d extends BaseNodeDeclaration> =
 *   'intersection' & d['kind'] extends never ? d['inner'] : {}
 */

// ✓ ownIntersectionResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ownIntersectionResult(d: BaseNodeDeclaration): any {
  return nodeOfKind(reducibleKindOf(d['kind'])) | Disjoint
}
/* compiles to:
 * export type ownIntersectionResult<d extends BaseNodeDeclaration> =
 *   nodeOfKind<reducibleKindOf<d['kind']>> | Disjoint
 */

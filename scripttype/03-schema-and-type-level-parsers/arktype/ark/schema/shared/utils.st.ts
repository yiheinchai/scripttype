/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/shared/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { array } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
import type { Thunk } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/functions.js'
declare namespace m1 {
  export type args<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type returnValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Thunk: any
declare const array: any
// ✓ makeRootAndArrayPropertiesMutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function makeRootAndArrayPropertiesMutable(inner): any {
  const out = emptyObject
  for (const k in keyof(inner)) {
    out[k] = mutable(matches<array | undefined>(inner[k]) ? mutable(inner[k]) : inner[k])
  }
  return out & unknown
}
/* compiles to:
 * export type makeRootAndArrayPropertiesMutable<inner> =
 *   & {
 *       -readonly [K in keyof inner]: inner[K] extends array | undefined
 *         ? mutable<inner[K]>
 *         : inner[K]
 *     }
 *   & unknown
 */

// ✓ internalImplementationOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function internalImplementationOf__v1(external, k): any {
  const m1 = matches<((...args: Hole<"args">) => unknown)>(external[k])
  if (m1) {
    const out = emptyObject
    for (const i in keyof(m1.args)) {
      out[i] = never
    }
    return fnType([...out], unknown)
  }
  return unknown
}
/**
 * @param {keyof typeof external} typeOnlyKey
 */
export function internalImplementationOf(external, typeOnlyKey = never): any {
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(external), typeOnlyKey))) {
    out[k] = internalImplementationOf__v1(external, k)
  }
  return out
}
/* compiles to:
 * export type internalImplementationOf__v1<external, k> =
 *   external[k] extends (...args: infer args) => unknown
 *     ? (...a0: { [I in keyof args]: never }) => unknown
 *     : unknown
 * export type internalImplementationOf<external, typeOnlyKey extends keyof external = never> = {
 *   [K in Exclude<keyof external, typeOnlyKey>]: internalImplementationOf__v1<external, K>
 * }
 */

// ✓ unwrapDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unwrapDefault(thunkableValue): any {
  const m1 = matches<Thunk<Hole<"returnValue">>>(thunkableValue)
  if (m1) {
    return m1.returnValue
  }
  return thunkableValue
}
/* compiles to:
 * export type unwrapDefault<thunkableValue> =
 *   thunkableValue extends Thunk<infer returnValue> ? returnValue : thunkableValue
 */

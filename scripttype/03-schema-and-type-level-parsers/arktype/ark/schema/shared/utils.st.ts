/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/schema/shared/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Thunk: any
declare const array: any
type Thunk<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ makeRootAndArrayPropertiesMutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function makeRootAndArrayPropertiesMutable(inner) {
  const out = emptyObject
  for (const k in keyof(inner)) {
    out[k] = mutable(matches<array | undefined>(inner[k]) ? mutable(inner[k]) : inner[k])
  }
  return out & unknown
}
/* compiles to:
 * export type makeRootAndArrayPropertiesMutable<inner> = { -readonly [K in keyof inner]: inner[K] extends array | undefined ? mutable<inner[K]> : inner[K] } & unknown
 */

// ✗ internalImplementationOf: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function internalImplementationOf(external, typeOnlyKey: keyof typeof external = never) {
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(external), typeOnlyKey))) {
    out[k] = raw('external[k] extends ((...args: infer args) => unknown) ? (...args: { [i in keyof args]: never; }) => unknown : unknown')
  }
  return out
}

// ✓ unwrapDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unwrapDefault(thunkableValue) {
  const m1 = matches<Thunk<Hole<"returnValue">>>(thunkableValue)
  if (m1) {
    return m1.returnValue
  }
  return thunkableValue
}
/* compiles to:
 * export type unwrapDefault<thunkableValue> = thunkableValue extends Thunk<infer returnValue> ? returnValue : thunkableValue
 */

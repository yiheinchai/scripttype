/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/attributes.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 40 type-level aliases: 31 covered, 9 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace distill {
  export type In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Side<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace type {
  export type cast<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ArkError: any
declare const ArkErrors: any
declare const Brand: any
declare const Morph: any
declare const MorphableIntersection: any
declare const Primitive: any
declare const PropertyKey: any
declare const Side: any
declare const TerminallyInferredObject: any
declare const To: any
declare const Type: any
declare const anyOrNever: any
declare const array: any
declare const cast: any
declare const defaultsToKey: any
declare const intersectArrays: any
declare const isSafelyMappable: any
declare const optionalKeyOf: any
declare const show: any
declare const type: any
declare const unionKeyOf: any
declare const unionToTuple: any
type ArkError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ArkErrors<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Brand<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DateLiteral<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Default<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InferredMorph<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Morph<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MorphableIntersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Out<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Primitive<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Side<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TerminallyInferredObject<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type To<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type anyOrNever<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type array<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type cast<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type defaultsToKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type distill<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type inferredDefaultKeyOf<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type intersectArrays<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type isSafelyMappable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type optionalKeyOf<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type show<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type unionKeyOf<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type unionToTuple<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ RegexLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RegexLiteral(source: string = string) {
  return `/${source}/`
}
/* compiles to:
 * export type RegexLiteral<source extends string = string> = `/${source}/`
 */

// ✓ DateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DateLiteral(source: string = string) {
  return anyOf(`d"${source}"`, `d'${source}'`)
}
/* compiles to:
 * export type DateLiteral<source extends string = string> = `d"${source}"` | `d'${source}'`
 */

// ✓ normalizeLimit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function normalizeLimit(limit) {
  const m1 = matches<DateLiteral<Hole<"source">>>(limit)
  if (m1) {
    return m1.source
  }
  if (matches<number | string>(limit)) {
    return limit
  }
  return never
}
/* compiles to:
 * export type normalizeLimit<limit> =
 *   limit extends DateLiteral<infer source> ? source
 *   : limit extends number | string ? limit
 *   : never
 */

// ✗ distill: does not compile yet
//   'distill__st3' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function distill(t, side: distill.Side) {
  return finalizeDistillation(t, _distill(t, side))
}
/* compiles to:
 * export type distill<t, side extends distill.Side> = finalizeDistillation<t, _distill<t, side>>
 */

// ✓ In: verified type-identical to the original
/* @scripttype preserveParamNames */
export function In(t) {
  return distill(t, 'in')
}
/* compiles to:
 * export type In<t> = distill<t, 'in'>
 */

// ✓ Out: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Out(t) {
  return distill(t, 'out')
}
/* compiles to:
 * export type Out<t> = distill<t, 'out'>
 */

// ✓ Out: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Out(t) {
  return distill(t, 'introspectableOut')
}
/* compiles to:
 * export type Out<t> = distill<t, 'introspectableOut'>
 */

// ✓ finalizeDistillation: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeDistillation(t, distilled) {
  if (matches<true>(equals(t, distilled))) {
    return t
  }
  return distilled
}
/* compiles to:
 * export type finalizeDistillation<t, distilled> =
 *   Equals<t, distilled> extends true ? t : distilled
 */

// ✗ _distill: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function _distill(t, side: distill.Side) {
  if (typeof t === 'undefined') {
    return t
  }
  if (matches<[ anyOrNever ]>([t])) {
    return t
  }
  if (matches<typeof t>(unknown)) {
    return unknown
  }
  const m1 = matches<Brand<Hole<"base">>>(t)
  if (m1) {
    if (matches<"in">(side)) {
      return m1.base
    }
    return t
  }
  if (matches<TerminallyInferredObject | Primitive>(t)) {
    return t
  }
  if (matches<Function>(t)) {
    if (matches<(...args: never) => anyOrNever>(t)) {
      return t
    }
    const m2 = matches<InferredMorph<Hole<"i">, Hole<"o">>>(t)
    if (m2) {
      return distillIo(m2.i, m2.o, side)
    }
    return t
  }
  const m3 = matches<Default<Hole<"constraint">>>(t)
  if (m3) {
    return _distill(m3.constraint, side)
  }
  if (matches<array>(t)) {
    return distillArray(t, side)
  }
  if (matches<true>(isSafelyMappable(t))) {
    return distillMappable(t, side)
  }
  return t
}
/* compiles to:
 * export type _distill<t, side extends distill.Side> =
 *   t extends undefined ? t
 *   : [t] extends [anyOrNever] ? t
 *   : unknown extends t ? unknown
 *   : t extends Brand<infer base> ? side extends 'in' ? base : t
 *   : t extends TerminallyInferredObject | Primitive ? t
 *   : t extends Function
 *     ? t extends (...args: never) => anyOrNever ? t
 *     : t extends InferredMorph<infer i, infer o> ? distillIo<i, o, side>
 *     : t
 *   : t extends Default<infer constraint> ? _distill<constraint, side>
 *   : t extends array ? distillArray<t, side>
 *   : isSafelyMappable<t> extends true ? distillMappable<t, side>
 *   : t
 */

// ✗ distillMappable: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillMappable(o, side: distill.Side) {
  if (matches<"in">(side)) {
    const out = emptyObject
    for (const k in keyof(o)) {
      out[matches<inferredDefaultKeyOf<typeof o>>(k) ? never : k] = _distill(o[k], side)
    }
    const out2 = emptyObject
    for (const k in keySet(inferredDefaultKeyOf(o))) {
      out2[k] = optional(_distill(o[k], side))
    }
    return show(out & out2)
  }
  const out3 = emptyObject
  for (const k in keyof(o)) {
    out3[k] = _distill(o[k], side)
  }
  return out3
}
/* compiles to:
 * export type distillMappable<o, side extends distill.Side> =
 *   side extends 'in'
 *     ? show<
 *       & {
 *           [K in keyof o as K extends inferredDefaultKeyOf<o> ? never : K]: _distill<o[K], side>
 *         }
 *       & { [K1 in inferredDefaultKeyOf<o>]?: _distill<o[K1], side> }
 *     >
 *     : { [K2 in keyof o]: _distill<o[K2], side> }
 */

// ✗ distillIo: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillIo(i, o: Out, side: distill.Side) {
  if (matches<"out">(side)) {
    return _distill(o['t'], side)
  }
  if (matches<"in">(side)) {
    return _distill(i, side)
  }
  const m1 = matches<To<Hole<"validatedOut">>>(o)
  if (m1) {
    return _distill(m1.validatedOut, side)
  }
  return unknown
}
/* compiles to:
 * export type distillIo<i, o extends Out, side extends distill.Side> =
 *   side extends 'out' ? _distill<o['t'], side>
 *   : side extends 'in' ? _distill<i, side>
 *   : o extends To<infer validatedOut> ? _distill<validatedOut, side>
 *   : unknown
 */

// ✓ unwrapInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unwrapInput(t) {
  const m1 = matches<InferredMorph<Hole<"i">>>(t)
  if (m1) {
    if (matches<anyOrNever>(t)) {
      return t
    }
    return m1.i
  }
  return t
}
/* compiles to:
 * export type unwrapInput<t> = t extends InferredMorph<infer i> ? t extends anyOrNever ? t : i : t
 */

// ✓ inferredDefaultKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferredDefaultKeyOf(o) {
  const m1 = matches<Hole<"k">>(keyof(o))
  if (m1) {
    if (m1.k in o) {
      const m2 = matches<Default<Hole<"t">>>(unwrapInput(o[m1.k]))
      if (m2) {
        if (matches<[ anyOrNever ]>([m2.t])) {
          return never
        }
        return m1.k
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type inferredDefaultKeyOf<o> =
 *   keyof o extends infer k
 *     ? k extends keyof o
 *       ? unwrapInput<o[k]> extends Default<infer t>
 *         ? [t] extends [anyOrNever] ? never : k
 *         : never
 *       : never
 *     : never
 */

// ✗ distillArray: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillArray(t: array, side: distill.Side) {
  if (matches<typeof t>(arrayOf(t[number]))) {
    return alignReadonly(arrayOf(_distill(t[number], side)), t)
  }
  return distillNonArraykeys(t, alignReadonly(distillArrayFromPrefix([...t], side, []), t), side)
}
/* compiles to:
 * export type distillArray<t extends array, side extends distill.Side> =
 *   t[number][] extends t
 *     ? alignReadonly<_distill<t[number], side>[], t>
 *     : distillNonArraykeys<t, alignReadonly<distillArrayFromPrefix<[...t], side, []>, t>, side>
 */

// ✓ alignReadonly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function alignReadonly(result: unknown[], original: array) {
  if (matches<unknown[]>(original)) {
    return result
  }
  return Readonly(result)
}
/* compiles to:
 * export type alignReadonly<result extends unknown[], original extends array> =
 *   original extends unknown[] ? result : Readonly<result>
 */

// ✗ distillNonArraykeys: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillNonArraykeys(originalArray: array, distilledArray, side: distill.Side) {
  if (keyof(originalArray) in distilledArray) {
    return distilledArray
  }
  const out = emptyObject
  for (const k in keyof(originalArray)) {
    out[k in distilledArray ? never : k] = originalArray[k]
  }
  return distilledArray & _distill(out, side)
}
/* compiles to:
 * export type distillNonArraykeys<
 *   originalArray extends array,
 *   distilledArray,
 *   side extends distill.Side
 * > =
 *   keyof originalArray extends keyof distilledArray
 *     ? distilledArray
 *     : & distilledArray
 *     & _distill<
 *         {
 *           [K in keyof originalArray as K extends keyof distilledArray ? never : K]: originalArray[K]
 *         },
 *         side
 *       >
 */

// ✗ distillArrayFromPrefix: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillArrayFromPrefix(t: array, side: distill.Side, prefix: array) {
  let t_ = t
  let prefix_ = prefix
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t_)
    if (!m1) {
      break
    }
    t_ = m1.tail
    prefix_ = matches<[ "in", Default ]>([side, m1.head]) ? [...prefix_, optElem(_distill(m1.head, side))] : [...prefix_, _distill(m1.head, side)]
  }
  return [...prefix_, ...distillArrayFromPostfix(t_, side, [])]
}
/* compiles to:
 * export type distillArrayFromPrefix<
 *   t extends array,
 *   side extends distill.Side,
 *   prefix extends array
 * > =
 *   DistillArrayFromPrefix__loop<t, prefix, side>
 * type DistillArrayFromPrefix__loop<
 *   T_ extends array,
 *   Prefix_ extends array,
 *   Side extends distill.Side
 * > =
 *   T_ extends readonly [infer head, ...infer tail]
 *     ? DistillArrayFromPrefix__loop<
 *       tail,
 *       [Side, head] extends ['in', Default]
 *         ? [...Prefix_, _distill<head, Side>?]
 *         : [...Prefix_, _distill<head, Side>],
 *       Side
 *     >
 *     : [...Prefix_, ...distillArrayFromPostfix<T_, Side, []>]
 */

// ✗ distillArrayFromPostfix: does not compile yet
//   Namespace 'distill' has no exported member 'Side'.
/* @scripttype preserveParamNames */
export function distillArrayFromPostfix(t: array, side: distill.Side, postfix: array) {
  let t_ = t
  let postfix_ = postfix
  while (true) {
    const m1 = matches<readonly [ ...Hole<"init">, Hole<"last"> ]>(t_)
    if (!m1) {
      break
    }
    t_ = m1.init
    postfix_.unshift(_distill(m1.last, side))
  }
  const out = emptyObject
  for (const i in keyof(t_)) {
    out[i] = _distill(t_[i], side)
  }
  return [...out, ...postfix_]
}
/* compiles to:
 * export type distillArrayFromPostfix<
 *   t extends array,
 *   side extends distill.Side,
 *   postfix extends array
 * > =
 *   DistillArrayFromPostfix__loop<t, postfix, side>
 * type DistillArrayFromPostfix__loop<
 *   T_ extends array,
 *   Postfix_ extends array,
 *   Side extends distill.Side
 * > =
 *   T_ extends readonly [...infer init, infer last]
 *     ? DistillArrayFromPostfix__loop<init, [_distill<last, Side>, ...Postfix_], Side>
 *     : [...{ [I in keyof T_]: _distill<T_[I], Side> }, ...Postfix_]
 */

// ✓ inferPredicate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferPredicate(t, predicate) {
  const m1 = matches<(data: any, ...args: any[]) => data is Hole<"narrowed">>(predicate)
  if (m1) {
    return m1.narrowed
  }
  return t
}
/* compiles to:
 * export type inferPredicate<t, predicate> =
 *   predicate extends (data: any, ...args: any[]) => data is infer narrowed ? narrowed : t
 */

// ✓ inferNaryPipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferNaryPipe(morphs: readonly Morph[]) {
  return _inferNaryPipe(morphs, unknown)
}
/* compiles to:
 * export type inferNaryPipe<morphs extends readonly Morph[]> = _inferNaryPipe<morphs, unknown>
 */

// ✓ _inferNaryPipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferNaryPipe(remaining: readonly unknown[], result) {
  let remaining_ = remaining
  let result_ = result
  while (true) {
    const m1 = matches<(readonly [ Hole<"head", Morph>, ...Hole<"tail", Morph[]> ])>(remaining_)
    if (!m1) {
      break
    }
    remaining_ = m1.tail
    result_ = inferMorph(result_, m1.head)
  }
  return result_
}
/* compiles to:
 * export type _inferNaryPipe<remaining extends readonly unknown[], result> = _inferNaryPipe__loop<
 *   remaining,
 *   result
 * >
 * type _inferNaryPipe__loop<Remaining_ extends readonly unknown[], Result_> =
 *   Remaining_ extends readonly [infer head extends Morph, ...infer tail extends Morph[]]
 *     ? _inferNaryPipe__loop<tail, inferMorph<Result_, head>>
 *     : Result_
 */

// ✓ inferNaryIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferNaryIntersection(types: readonly unknown[]) {
  if (matches<(typeof types)["length"]>(number)) {
    return _inferNaryIntersection(unionToTuple(types[number]), unknown)
  }
  return _inferNaryIntersection(types, unknown)
}
/* compiles to:
 * export type inferNaryIntersection<types extends readonly unknown[]> =
 *   number extends types['length']
 *     ? _inferNaryIntersection<unionToTuple<types[number]>, unknown>
 *     : _inferNaryIntersection<types, unknown>
 */

// ✓ _inferNaryIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferNaryIntersection(remaining: readonly unknown[], result) {
  let remaining_ = remaining
  let result_ = result
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(remaining_)
    if (!m1) {
      break
    }
    remaining_ = m1.tail
    result_ = inferIntersection(result_, m1.head)
  }
  return result_
}
/* compiles to:
 * export type _inferNaryIntersection<remaining extends readonly unknown[], result> = _inferNaryIntersection__loop<
 *   remaining,
 *   result
 * >
 * type _inferNaryIntersection__loop<Remaining_ extends readonly unknown[], Result_> =
 *   Remaining_ extends readonly [infer head, ...infer tail]
 *     ? _inferNaryIntersection__loop<tail, inferIntersection<Result_, head>>
 *     : Result_
 */

// ✓ inferNaryMerge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferNaryMerge(types: readonly unknown[]) {
  if (matches<(typeof types)["length"]>(number)) {
    return _inferUnorderedMerge(types)
  }
  return _inferNaryMerge(types, {})
}
/* compiles to:
 * export type inferNaryMerge<types extends readonly unknown[]> =
 *   number extends types['length'] ? _inferUnorderedMerge<types> : _inferNaryMerge<types, {}>
 */

// ✓ _inferUnorderedMerge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferUnorderedMerge__v1(types, k) {
  const m1 = matches<Hole<"v">>(types[number])
  if (m1) {
    if (matches<unknown>(m1.v)) {
      if (k in m1.v) {
        return m1.v[k]
      }
      return never
    }
    return never
  }
  return never
}

export function _inferUnorderedMerge__v2(types, k) {
  const m1 = matches<Hole<"v">>(types[number])
  if (m1) {
    if (matches<unknown>(m1.v)) {
      if (k in m1.v) {
        return m1.v[k]
      }
      return never
    }
    return never
  }
  return never
}
export function _inferUnorderedMerge(types: readonly unknown[], optionalKey: PropertyKey = optionalAtLeastOnceUnionKeyOf(types[number]), requiredKey: PropertyKey = Exclude(unionKeyOf(types[number]), optionalKey)) {
  const out = emptyObject
  for (const k in keySet(requiredKey)) {
    out[k] = _inferUnorderedMerge__v1(types, k)
  }
  const out2 = emptyObject
  for (const k in keySet(optionalKey)) {
    out2[k] = optional(_inferUnorderedMerge__v2(types, k))
  }
  return show(out & out2)
}
/* compiles to:
 * export type _inferUnorderedMerge__v1<types, k> =
 *   types[number] extends infer v
 *     ? v extends unknown ? k extends keyof v ? v[k] : never : never
 *     : never
 * export type _inferUnorderedMerge__v2<types, k> =
 *   types[number] extends infer v
 *     ? v extends unknown ? k extends keyof v ? v[k] : never : never
 *     : never
 * export type _inferUnorderedMerge<
 *   types extends readonly unknown[],
 *   optionalKey extends PropertyKey = optionalAtLeastOnceUnionKeyOf<types[number]>,
 *   requiredKey extends PropertyKey = Exclude<unionKeyOf<types[number]>, optionalKey>
 * > =
 *   show<
 *     & { [K in requiredKey]: _inferUnorderedMerge__v1<types, K> }
 *     & { [K1 in optionalKey]?: _inferUnorderedMerge__v2<types, K1> }
 *   >
 */

// ✓ optionalAtLeastOnceUnionKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function optionalAtLeastOnceUnionKeyOf(t) {
  if (matches<unknown>(t)) {
    return optionalKeyOf(t)
  }
  return never
}
/* compiles to:
 * export type optionalAtLeastOnceUnionKeyOf<t> = t extends unknown ? optionalKeyOf<t> : never
 */

// ✓ _inferNaryMerge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferNaryMerge(remaining: readonly unknown[], result) {
  let remaining_ = remaining
  let result_ = result
  while (true) {
    const m1 = matches<(readonly [ Hole<"head">, ...Hole<"tail", readonly unknown[]> ])>(remaining_)
    if (!m1) {
      break
    }
    remaining_ = m1.tail
    result_ = merge(result_, m1.head)
  }
  return result_
}
/* compiles to:
 * export type _inferNaryMerge<remaining extends readonly unknown[], result> = _inferNaryMerge__loop<
 *   remaining,
 *   result
 * >
 * type _inferNaryMerge__loop<Remaining_ extends readonly unknown[], Result_> =
 *   Remaining_ extends readonly [infer head, ...infer tail extends readonly unknown[]]
 *     ? _inferNaryMerge__loop<tail, Result_ & head>
 *     : Result_
 */

// ✓ inferMorphOut: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferMorphOut(morph: Morph) {
  return Exclude(ReturnType(morph), ArkError | ArkErrors)
}
/* compiles to:
 * export type inferMorphOut<morph extends Morph> = Exclude<
 *   ReturnType<morph>,
 *   ArkError | ArkErrors
 * >
 */

// ✓ InferredMorph: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferredMorph(i = never, o: Out = Out) {
  return fnType([i], o)
}
/* compiles to:
 * export type InferredMorph<i = never, o extends Out = Out> = (a0: i) => o
 */

// ✓ Default: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Default(t = unknown, v = unknown) {
  return { [defaultsToKey]: [t, v] }
}
/* compiles to:
 * export type Default<t = unknown, v = unknown> = { [defaultsToKey]: [t, v] }
 */

// ✓ withDefault: verified type-identical to the original
/* @scripttype preserveParamNames */
export function withDefault(t, v, undistributed = t) {
  if (matches<InferredMorph>(t)) {
    return addDefaultToMorph(t, v)
  }
  return Default(Exclude(undistributed, InferredMorph), v)
}
/* compiles to:
 * export type withDefault<t, v, undistributed = t> =
 *   t extends InferredMorph
 *     ? addDefaultToMorph<t, v>
 *     : Default<Exclude<undistributed, InferredMorph>, v>
 */

// ✓ addDefaultToMorph: verified type-identical to the original
/* @scripttype preserveParamNames */
export function addDefaultToMorph(t: InferredMorph, v) {
  const m1 = matches<[ InferredMorph<Hole<"i">, Hole<"o">> ]>([normalizeMorphDistribution(t)])
  if (m1) {
    return fnType([Default(m1.i, v)], m1.o)
  }
  return never
}
/* compiles to:
 * export type addDefaultToMorph<t extends InferredMorph, v> =
 *   [normalizeMorphDistribution<t>] extends [InferredMorph<infer i, infer o>]
 *     ? (a0: Default<i, v>) => o
 *     : never
 */

// ✓ normalizeMorphDistribution: verified type-identical to the original
/* @scripttype preserveParamNames */
export function normalizeMorphDistribution__v1(t) {
  const m1 = matches<InferredMorph<Hole<"i">>>(t)
  if (m1) {
    return m1.i
  }
  return never
}

export function normalizeMorphDistribution__v2(t) {
  const m1 = matches<InferredMorph<never, Hole<"o">>>(t)
  if (m1) {
    const m2 = matches<[ To<Hole<"unwrappedOut">> ]>([m1.o])
    if (m2) {
      return To(m2.unwrappedOut)
    }
    return m1.o
  }
  return never
}
export function normalizeMorphDistribution(t, undistributedIn = normalizeMorphDistribution__v1(t), undistributedOut: Out = normalizeMorphDistribution__v2(t)) {
  const m1 = matches<InferredMorph<Hole<"i">, Hole<"o">>>(Extract(t, InferredMorph))
  const m2 = matches<Hole<"_">>((matches<anyOrNever>(Extract(t, InferredMorph)) ? never : (m1 ? (matches<[ typeof m1.o ]>([undistributedOut]) ? fnType([undistributedIn], undistributedOut) : (matches<[ typeof m1.i ]>([undistributedIn]) ? fnType([undistributedIn], undistributedOut) : t)) : never)) | Exclude(t, InferredMorph))
  if (m2) {
    return m2._
  }
  return never
}
/* compiles to:
 * export type normalizeMorphDistribution__v1<t> = t extends InferredMorph<infer i> ? i : never
 * export type normalizeMorphDistribution__v2<t> =
 *   t extends InferredMorph<never, infer o>
 *     ? [o] extends [To<infer unwrappedOut>] ? To<unwrappedOut> : o
 *     : never
 * export type normalizeMorphDistribution<
 *   t,
 *   undistributedIn = normalizeMorphDistribution__v1<t>,
 *   undistributedOut extends Out = normalizeMorphDistribution__v2<t>
 * > =
 *   (Extract<t, InferredMorph> extends anyOrNever ? never : Extract<t, InferredMorph> extends InferredMorph<infer i, infer o> ? [undistributedOut] extends [o] ? (a0: undistributedIn) => undistributedOut : [undistributedIn] extends [i] ? (a0: undistributedIn) => undistributedOut : t : never) | Exclude<t, InferredMorph> extends infer _
 *     ? _
 *     : never
 */

// ✓ defaultFor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function defaultFor(t = unknown) {
  return (matches<typeof t>(Primitive) ? Primitive : (matches<Primitive>(t) ? t : never)) | fnType([], t)
}
/* compiles to:
 * export type defaultFor<t = unknown> =
 *   (Primitive extends t ? Primitive : t extends Primitive ? t : never) | (() => t)
 */

// ✓ termOrType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function termOrType(t) {
  return t | Type(t, any)
}
/* compiles to:
 * export type termOrType<t> = t | Type<t, any>
 */

// ✓ inferIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferIntersection(l, r) {
  return normalizeMorphDistribution(_inferIntersection(l, r, false))
}
/* compiles to:
 * export type inferIntersection<l, r> = normalizeMorphDistribution<
 *   _inferIntersection<l, r, false>
 * >
 */

// ✗ inferMorph: does not compile yet
//   Namespace 'distill' has no exported member 'In'.
/* @scripttype preserveParamNames */
export function inferMorph(t, morph: Morph) {
  const m1 = matches<type.cast<Hole<"tMorph">>>(morph)
  if (m1) {
    return inferPipe(t, m1.tMorph)
  }
  const m2 = matches<Hole<"out">>(inferMorphOut(morph))
  if (m2) {
    return fnType([distill.In(t)], Out(m2.out))
  }
  return never
}
/* compiles to:
 * export type inferMorph<t, morph extends Morph> =
 *   morph extends type.cast<infer tMorph> ? inferPipe<t, tMorph>
 *   : inferMorphOut<morph> extends infer out ? (a0: distill.In<t>) => Out<out>
 *   : never
 */

// ✓ inferPipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferPipe(l, r) {
  return normalizeMorphDistribution(_inferIntersection(l, r, true))
}
/* compiles to:
 * export type inferPipe<l, r> = normalizeMorphDistribution<_inferIntersection<l, r, true>>
 */

// ✓ _inferIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferIntersection(l, r, piped: boolean) {
  const m1 = matches<[ Hole<"t", anyOrNever> ]>([l & r])
  if (m1) {
    return m1.t
  }
  const m2 = matches<InferredMorph<Hole<"lIn">, Hole<"lOut">>>(l)
  if (m2) {
    const m3 = matches<InferredMorph<never, Hole<"rOut">>>(r)
    if (m3) {
      if (matches<true>(piped)) {
        return fnType([m2.lIn], m3.rOut)
      }
      return never
    }
    if (matches<true>(piped)) {
      return fnType([m2.lIn], To(r))
    }
    return fnType([_inferIntersection(m2.lIn, r, false)], m2.lOut)
  }
  const m4 = matches<InferredMorph<Hole<"rIn">, Hole<"rOut">>>(r)
  if (m4) {
    return fnType([_inferIntersection(m4.rIn, l, false)], m4.rOut)
  }
  if (matches<[ object, object ]>([l, r])) {
    const m5 = matches<Hole<"result">>(intersectObjects(l, r, piped))
    if (m5) {
      return m5.result
    }
    return never
  }
  return l & r
}
/* compiles to:
 * export type _inferIntersection<l, r, piped extends boolean> =
 *   [l & r] extends [infer t extends anyOrNever] ? t
 *   : l extends InferredMorph<infer lIn, infer lOut>
 *     ? r extends InferredMorph<never, infer rOut> ? piped extends true ? (a0: lIn) => rOut : never
 *     : piped extends true ? (a0: lIn) => To<r>
 *     : (a0: _inferIntersection<lIn, r, false>) => lOut
 *   : r extends InferredMorph<infer rIn, infer rOut>
 *     ? (a0: _inferIntersection<rIn, l, false>) => rOut
 *   : [l, r] extends [object, object]
 *     ? intersectObjects<l, r, piped> extends infer result ? result : never
 *   : l & r
 */

// ✓ intersectObjects: verified type-identical to the original
/* @scripttype preserveParamNames */
export function intersectObjects(l, r, piped: boolean) {
  if (matches<array>(l)) {
    if (matches<array>(r)) {
      return intersectArrays(l, r, MorphableIntersection(piped))
    }
    return l & r
  }
  if (matches<array>(r)) {
    return l & r
  }
  if (matches<never>(keyof(l) & keyof(r))) {
    return show(l & r)
  }
  const out = emptyObject
  for (const k in keyof(l)) {
    out[k] = k in r ? _inferIntersection(l[k], r[k], piped) : l[k]
  }
  const out2 = emptyObject
  for (const k in keyof(r)) {
    out2[k] = k in l ? _inferIntersection(l[k], r[k], piped) : r[k]
  }
  return show(out & out2)
}
/* compiles to:
 * export type intersectObjects<l, r, piped extends boolean> =
 *   l extends array
 *     ? r extends array ? intersectArrays<l, r, MorphableIntersection<piped>> : l & r
 *   : r extends array ? l & r
 *   : keyof l & keyof r extends never ? show<l & r>
 *   : show<
 *     & { [K in keyof l]: K extends keyof r ? _inferIntersection<l[K], r[K], piped> : l[K] }
 *     & { [K1 in keyof r]: K1 extends keyof l ? _inferIntersection<l[K1], r[K1], piped> : r[K1] }
 *   >
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/fn.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 1 raw, 1 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const type: any
declare const validateInnerDefinition: any
declare const validateTupleLiteral: any
type type<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateInnerDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateTupleLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ BaseFnParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function BaseFnParser($ = {}) {
  return raw('<const args extends readonly unknown[], paramsT extends readonly unknown[] = inferTupleLiteral<args extends readonly [ ...infer params, ":", unknown ] ? params : args, $, {}>, returnT = args extends readonly [ ...unknown[], ":", infer returnDef ] ? type.infer<returnDef, $> : unknown>(...args: { [i in keyof args]: conform<args[i], get<validateFnArgs<args, $>, i>>; }) => <internalSignature extends (...args: distill.Out<paramsT>) => distill.In<returnT>, externalSignature extends Fn = (...args: applyElementLabels<distill.In<paramsT>, Parameters<internalSignature>>) => args extends readonly [ ...unknown[], ":", unknown ] ? distill.Out<returnT> : ReturnType<internalSignature>>(implementation: internalSignature) => TypedFn<externalSignature, $, args extends readonly [ ...unknown[], ":", unknown ] ? Return.introspectable : {}>')
}

// ✓ validateFnArgs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateFnArgs(args, $) {
  if (matches<readonly unknown[]>(args)) {
    const m1 = matches<readonly [ ...Hole<"paramDefs">, ":", Hole<"returnDef"> ]>(args)
    if (m1) {
      return asReadonly([...validateFnParamDefs(m1.paramDefs, $), ':', type.validate(m1.returnDef, $)])
    }
    return validateFnParamDefs(args, $)
  }
  return never
}
/* compiles to:
 * export type validateFnArgs<args, $> = args extends readonly unknown[] ? args extends readonly [...infer paramDefs, ':', infer returnDef] ? readonly [...validateFnParamDefs<paramDefs, $>, ':', type.validate<returnDef, $>] : validateFnParamDefs<args, $> : never
 */

// ✗ validateFnParamDefs: does not compile yet
//   'paramDefs' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function validateFnParamDefs(paramDefs: readonly unknown[], $) {
  if (matches<validateTupleLiteral<typeof paramDefs, typeof $, {}>>(paramDefs)) {
    return paramDefs
  }
  if (matches<{ [i in keyof typeof paramDefs]: (typeof paramDefs)[i] extends "..." ? (typeof paramDefs)[i] : validateInnerDefinition<(typeof paramDefs)[i], typeof $, {}>; }>(paramDefs)) {
    return validateTupleLiteral(paramDefs, $, {})
  }
  const out = emptyObject
  for (const i in keyof(paramDefs)) {
    out[i] = validateInnerDefinition(paramDefs[i], $, {})
  }
  return out
}
/* compiles to:
 * export type validateFnParamDefs<paramDefs extends readonly unknown[], $> = paramDefs extends validateTupleLiteral<paramDefs, $, {}> ? paramDefs : paramDefs extends { [i in keyof typeof paramDefs]: (typeof paramDefs)[i] extends "..." ? (typeof paramDefs)[i] : validateInnerDefinition<(typeof paramDefs)[i], typeof $, {}>; } ? validateTupleLiteral<paramDefs, $, {}> : { [I in keyof paramDefs]: validateInnerDefinition<paramDefs[I], $, {}> }
 */

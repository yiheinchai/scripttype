/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/generic.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 19 type-level aliases: 16 covered, 1 raw, 1 compile-error, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace Hkt {
  export type apply<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Scanner {
  export type skipWhitespace<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace type {
  export type infer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type validate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ErrorMessage: any
declare const ErrorType: any
declare const Generic: any
declare const GenericBodyParser: any
declare const GenericParamAst: any
declare const GenericParamDef: any
declare const Hkt: any
declare const ParamsTerminator: any
declare const Scanner: any
declare const StaticState: any
declare const Type: any
declare const WhitespaceChar: any
declare const array: any
declare const conform: any
declare const emptyGenericParameterMessage: any
declare const extendsToken: any
declare const inferAstRoot: any
declare const inferDefinition: any
declare const paramsDef: any
declare const parseUntilFinalizer: any
declare const type: any
declare const validateAst: any
type ErrorMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ErrorType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Generic<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GenericBodyParser<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GenericParamAst<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type GenericParamDef<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Hkt<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParameterString<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ParamsTerminator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Scanner<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StaticState<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type WhitespaceChar<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type array<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type conform<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type emptyGenericParameterMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type extendsToken<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type inferAstRoot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type inferDefinition<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type paramsDef<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type parseUntilFinalizer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type validateAst<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ParameterString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParameterString(params: string = string) {
  return `<${params}>`
}
/* compiles to:
 * export type ParameterString<params extends string = string> = `<${params}>`
 */

// ✓ extractParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractParams(s: ParameterString) {
  const m1 = matches<ParameterString<Hole<"params">>>(s)
  if (m1) {
    return m1.params
  }
  return never
}
/* compiles to:
 * export type extractParams<s extends ParameterString> =
 *   s extends ParameterString<infer params> ? params : never
 */

// ✓ validateParameterString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateParameterString(s: ParameterString, $) {
  const m1 = matches<Hole<"e", ErrorMessage>>(parseGenericParams(extractParams(s), $))
  if (m1) {
    return m1.e
  }
  return s
}
/* compiles to:
 * export type validateParameterString<s extends ParameterString, $> =
 *   parseGenericParams<extractParams<s>, $> extends (infer e extends ErrorMessage) ? e : s
 */

// ✓ validateGenericArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateGenericArg(arg, param: GenericParamAst, $) {
  if (matches<(typeof param)[1]>(type.infer(arg, $))) {
    return unknown
  }
  return ErrorType([`Invalid argument for ${param[0]}`, param[1]])
}
/* compiles to:
 * export type validateGenericArg<arg, param extends GenericParamAst, $> =
 *   type.infer<arg, $> extends param[1]
 *     ? unknown
 *     : ErrorType<[`Invalid argument for ${param[0]}`, param[1]]>
 */

// ✗ GenericInstantiator: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature; literal type FirstTemplateToken
/* @scripttype preserveParamNames */
export function GenericInstantiator(params: array<GenericParamAst>, def, $, args$) {
  if (matches<1>(params['length'])) {
    return raw('{ <const a, r = instantiateGeneric<def, params, [ a ], $, args$>>(a: type.validate<a, args$> & validateGenericArg<a, params[0], args$>): r extends infer _ ? _ : never; }')
  }
  if (matches<2>(params['length'])) {
    return raw('{ <const a, const b, r = instantiateGeneric<def, params, [ a, b ], $, args$>>(...args: [ type.validate<a, args$> & validateGenericArg<a, params[0], args$>, type.validate<b, args$> & validateGenericArg<b, params[1], args$> ]): r extends infer _ ? _ : never; }')
  }
  if (matches<3>(params['length'])) {
    return raw('{ <const a, const b, const c, r = instantiateGeneric<def, params, [ a, b, c ], $, args$>>(...args: [ type.validate<a, args$> & validateGenericArg<a, params[0], args$>, type.validate<b, args$> & validateGenericArg<b, params[1], args$>, type.validate<c, args$> & validateGenericArg<c, params[2], args$> ]): r extends infer _ ? _ : never; }')
  }
  if (matches<4>(params['length'])) {
    return raw('{ <const a, const b, const c, const d, r = instantiateGeneric<def, params, [ a, b, c, d ], $, args$>>(...args: [ type.validate<a, args$> & validateGenericArg<a, params[0], args$>, type.validate<b, args$> & validateGenericArg<b, params[1], args$>, type.validate<c, args$> & validateGenericArg<c, params[2], args$>, type.validate<d, args$> & validateGenericArg<d, params[3], args$> ]): r extends infer _ ? _ : never; }')
  }
  if (matches<5>(params['length'])) {
    return raw('{ <const a, const b, const c, const d, const e, r = instantiateGeneric<def, params, [ a, b, c, d, e ], $, args$>>(...args: [ type.validate<a, args$> & validateGenericArg<a, params[0], args$>, type.validate<b, args$> & validateGenericArg<b, params[1], args$>, type.validate<c, args$> & validateGenericArg<c, params[2], args$>, type.validate<d, args$> & validateGenericArg<d, params[3], args$>, type.validate<e, args$> & validateGenericArg<e, params[4], args$> ]): r extends infer _ ? _ : never; }')
  }
  if (matches<6>(params['length'])) {
    return raw('{ <const a, const b, const c, const d, const e, const f, r = instantiateGeneric<def, params, [ a, b, c, d, e, f ], $, args$>>(...args: [ type.validate<a, args$> & validateGenericArg<a, params[0], args$>, type.validate<b, args$> & validateGenericArg<b, params[1], args$>, type.validate<c, args$> & validateGenericArg<c, params[2], args$>, type.validate<d, args$> & validateGenericArg<d, params[3], args$>, type.validate<e, args$> & validateGenericArg<e, params[4], args$>, type.validate<f, args$> & validateGenericArg<f, params[5], args$> ]): r extends infer _ ? _ : never; }')
  }
  return fnType([ErrorMessage(raw('`You may not define more than 6 positional generic parameters`'))], never)
}

// ✓ instantiateGeneric: verified type-identical to the original
/* @scripttype preserveParamNames */
export function instantiateGeneric(def, params: array<GenericParamAst>, args, $, args$) {
  const out = emptyObject
  for (const i in keyof(args)) {
    out[i] = type.infer(args[i], args$)
  }
  return Type(matches<[ Hkt ]>([def]) ? Hkt.apply(def, out) : inferDefinition(def, $, bindGenericArgs(params, args$, args)), args$)
}
/* compiles to:
 * export type instantiateGeneric<def, params extends array<GenericParamAst>, args, $, args$> = Type<
 *   [def] extends [Hkt]
 *     ? Hkt.apply<def, { [I in keyof args]: type.infer<args[I], args$> }>
 *     : inferDefinition<def, $, bindGenericArgs<params, args$, args>>,
 *   args$
 * >
 */

// ✓ bindGenericArgs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function bindGenericArgs(params: array<GenericParamAst>, $, args) {
  const out = emptyObject
  for (const i in keySet(merge(keyof(params), `${number}`))) {
    out[params[i][0]] = type.infer(args[merge(i, keyof(args))], $)
  }
  return out
}
/* compiles to:
 * export type bindGenericArgs<params extends array<GenericParamAst>, $, args> = {
 *   [I in keyof params & `${number}` as params[I][0]]: type.infer<args[I & keyof args], $>
 * }
 */

// ✓ baseGenericResolutions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function baseGenericResolutions(params: array<GenericParamAst>, $) {
  const m1 = matches<Hole<"baseConstraints">>(baseGenericConstraints(params))
  if (m1) {
    const out = emptyObject
    for (const k in keyof(m1.baseConstraints)) {
      out[k] = Type(m1.baseConstraints[k], $)
    }
    return out
  }
  return never
}
/* compiles to:
 * export type baseGenericResolutions<params extends array<GenericParamAst>, $> =
 *   baseGenericConstraints<params> extends infer baseConstraints
 *     ? { [K in keyof baseConstraints]: Type<baseConstraints[K], $> }
 *     : never
 */

// ✓ baseGenericConstraints: verified type-identical to the original
/* @scripttype preserveParamNames */
export function baseGenericConstraints(params: array<GenericParamAst>) {
  const out = emptyObject
  for (const i in keySet(merge(keyof(params), `${number}`))) {
    out[params[i][0]] = params[i][1]
  }
  return out
}
/* compiles to:
 * export type baseGenericConstraints<params extends array<GenericParamAst>> = {
 *   [I in keyof params & `${number}` as params[I][0]]: params[I][1]
 * }
 */

// ✓ GenericConstructor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericConstructor(params: array<GenericParamAst> = array(GenericParamAst), bodyDef = unknown, $ = {}, arg$ = {}) {
  return ctorType([], Generic(params, bodyDef, $, arg$))
}
/* compiles to:
 * export type GenericConstructor<
 *   params extends array<GenericParamAst> = array<GenericParamAst>,
 *   bodyDef = unknown,
 *   $ = {},
 *   arg$ = {}
 * > =
 *   new () => Generic<params, bodyDef, $, arg$>
 */

// ✓ GenericDeclaration: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericDeclaration(name: string = string, params: ParameterString = ParameterString) {
  return `${name}${params}`
}
/* compiles to:
 * export type GenericDeclaration<
 *   name extends string = string,
 *   params extends ParameterString = ParameterString
 * > =
 *   `${name}${params}`
 */

// ✓ parseValidGenericParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseValidGenericParams(def: ParameterString, $) {
  return conform(parseGenericParams(extractParams(def), $), array(GenericParamAst))
}
/* compiles to:
 * export type parseValidGenericParams<def extends ParameterString, $> = conform<
 *   parseGenericParams<extractParams<def>, $>,
 *   array<GenericParamAst>
 * >
 */

// ✓ parseGenericParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseGenericParams(def: string, $) {
  return parseNextNameChar(Scanner.skipWhitespace(def), '', [], $)
}
/* compiles to:
 * export type parseGenericParams<def extends string, $> = parseNextNameChar<
 *   Scanner.skipWhitespace<def>,
 *   '',
 *   [],
 *   $
 * >
 */

// ✓ parseName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseName(unscanned: string, result: array<GenericParamAst>, $) {
  return parseNextNameChar(Scanner.skipWhitespace(unscanned), '', result, $)
}
/* compiles to:
 * export type parseName<unscanned extends string, result extends array<GenericParamAst>, $> = parseNextNameChar<
 *   Scanner.skipWhitespace<unscanned>,
 *   '',
 *   result,
 *   $
 * >
 */

// ✓ parseNextNameChar: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseNextNameChar(unscanned: string, name: string, result: array<GenericParamAst>, $) {
  const m1 = matches<`${Hole<"lookahead">}${Hole<"nextUnscanned">}`>(unscanned)
  if (m1) {
    if (matches<ParamsTerminator>(m1.lookahead)) {
      if (matches<"">(name)) {
        return ErrorMessage(emptyGenericParameterMessage)
      }
      if (matches<",">(m1.lookahead)) {
        return parseName(m1.nextUnscanned, [...result, [name, unknown]], $)
      }
      if (matches<WhitespaceChar>(m1.lookahead)) {
        return _parseOptionalConstraint(m1.nextUnscanned, name, result, $)
      }
      return never
    }
    return parseNextNameChar(m1.nextUnscanned, `${name}${m1.lookahead}`, result, $)
  }
  if (matches<"">(name)) {
    return result
  }
  return [...result, [name, unknown]]
}
/* compiles to:
 * export type parseNextNameChar<
 *   unscanned extends string,
 *   name extends string,
 *   result extends array<GenericParamAst>,
 *   $
 * > =
 *   unscanned extends `${infer lookahead}${infer nextUnscanned}`
 *     ? lookahead extends ParamsTerminator
 *       ? name extends '' ? ErrorMessage<emptyGenericParameterMessage>
 *       : lookahead extends ',' ? parseName<nextUnscanned, [...result, [name, unknown]], $>
 *       : lookahead extends WhitespaceChar
 *         ? _parseOptionalConstraint<nextUnscanned, name, result, $>
 *       : never
 *       : parseNextNameChar<nextUnscanned, `${name}${lookahead}`, result, $>
 *   : name extends '' ? result
 *   : [...result, [name, unknown]]
 */

// ✓ _parseOptionalConstraint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _parseOptionalConstraint(unscanned: string, name: string, result: array<GenericParamAst>, $) {
  const m1 = matches<(`${extendsToken}${Hole<"nextUnscanned">}`)>(Scanner.skipWhitespace(unscanned))
  if (m1) {
    const m2 = matches<(Hole<"finalArgState", StaticState>)>(parseUntilFinalizer(s.initialize(m1.nextUnscanned), $, {}))
    if (m2) {
      const m3 = matches<(Hole<"e", ErrorMessage>)>(validateAst(m2.finalArgState['root'], $, {}))
      if (m3) {
        return m3.e
      }
      return parseName(m2.finalArgState['unscanned'], [...result, [name, inferAstRoot(m2.finalArgState['root'], $, {})]], $)
    }
    return never
  }
  const m4 = matches<`,${Hole<"nextUnscanned">}`>(Scanner.skipWhitespace(unscanned))
  return parseName(m4 ? m4.nextUnscanned : unscanned, [...result, [name, unknown]], $)
}
/* compiles to:
 * export type _parseOptionalConstraint<
 *   unscanned extends string,
 *   name extends string,
 *   result extends array<GenericParamAst>,
 *   $
 * > =
 *   Scanner.skipWhitespace<unscanned> extends `${extendsToken}${infer nextUnscanned}`
 *     ? parseUntilFinalizer<s.initialize<nextUnscanned>, $, {}> extends (infer finalArgState extends StaticState)
 *       ? validateAst<finalArgState['root'], $, {}> extends (infer e extends ErrorMessage)
 *         ? e
 *         : parseName<
 *           finalArgState['unscanned'],
 *           [...result, [name, inferAstRoot<finalArgState['root'], $, {}>]],
 *           $
 *         >
 *       : never
 *     : parseName<
 *       Scanner.skipWhitespace<unscanned> extends `,${infer nextUnscanned}`
 *         ? nextUnscanned
 *         : unscanned,
 *       [...result, [name, unknown]],
 *       $
 *     >
 */

// ✓ genericParamDefToAst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function genericParamDefToAst(schema: GenericParamDef, $) {
  if (typeof schema === 'string') {
    return [schema, unknown]
  }
  const m1 = matches<readonly [ Hole<"name">, Hole<"def"> ]>(schema)
  if (m1) {
    return [m1.name, type.infer(m1.def, $)]
  }
  return never
}
/* compiles to:
 * export type genericParamDefToAst<schema extends GenericParamDef, $> =
 *   schema extends string ? [schema, unknown]
 *   : schema extends readonly [infer name, infer def] ? [name, type.infer<def, $>]
 *   : never
 */

// ✗ genericParamDefsToAst: does not compile yet
//   A rest element type must be an array type.
/* @scripttype preserveParamNames */
export function genericParamDefsToAst(defs: array<GenericParamDef>, $) {
  const out = emptyObject
  for (const i in keyof(defs)) {
    out[i] = genericParamDefToAst(defs[i], $)
  }
  return [...out]
}
/* compiles to:
 * export type genericParamDefsToAst<defs extends array<GenericParamDef>, $> = [
 *   ...{ [I in keyof defs]: genericParamDefToAst<defs[I], $> }
 * ]
 */

// ✗ GenericParser: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function GenericParser__v1($, i) {
  const m1 = matches<(readonly [ Hole<"name">, Hole<"def"> ])>(paramsDef[i])
  if (m1) {
    return asReadonly([m1.name, type.validate(m1.def, $)])
  }
  return paramsDef[i]
}
export function GenericParser($ = {}) {
  const out = emptyObject
  for (const i in keyof(paramsDef)) {
    out[i] = GenericParser__v1($, i)
  }
  return genericFnType(['const paramsDef extends array<GenericParamDef>'], [out], GenericBodyParser(genericParamDefsToAst(paramsDef, $), $))
}
/* compiles to:
 * export type GenericParser__v1<$, i> =
 *   paramsDef[i] extends readonly [infer name, infer def]
 *     ? readonly [name, type.validate<def, $>]
 *     : paramsDef[i]
 * export type GenericParser<$ = {}> =
 *   <const paramsDef extends array<GenericParamDef>>(
 *     a0: { [I in keyof paramsDef]: GenericParser__v1<$, I> }
 *   ) => GenericBodyParser<genericParamDefsToAst<paramsDef, $>, $>
 */

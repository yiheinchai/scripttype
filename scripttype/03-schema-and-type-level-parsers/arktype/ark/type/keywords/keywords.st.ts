/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/keywords/keywords.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 3 covered, 3 compile-error, 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Brand: any
declare const InferredMorph: any
declare const To: any
declare const Type: any
declare const bindThis: any
declare const distill: any
declare const inferDefinition: any
declare const instantiateType: any
declare const validateDefinition: any
type Brand<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InferredMorph<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type To<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Type<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type bindThis<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type distill<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type instantiateType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ validate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validate(def, $ = {}, args = bindThis(def)) {
  return validateDefinition(def, $, args)
}
/* compiles to:
 * export type validate<def, $ = {}, args = bindThis<def>> = validateDefinition<def, $, args>
 */

// ✗ instantiate: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function instantiate(def, $ = {}, args = bindThis(def)) {
  return instantiateType(inferDefinition(def, $, args), $)
}
/* compiles to:
 * export type instantiate<def, $ = {}, args = bindThis<def>> = instantiateType<inferDefinition<def, $, args>, $>
 */

// ✓ infer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function infer(def, $ = {}, args = bindThis(def)) {
  return inferDefinition(def, $, args)
}
/* compiles to:
 * export type infer<def, $ = {}, args = bindThis<def>> = inferDefinition<def, $, args>
 */

// ✗ In: does not compile yet
//   Namespace '"/Users/yihein.chai/Documents/learn/ScriptType/03-schema-and-type-level-parsers/arktype/ark/type/attributes".distill' has no exported member 'In__st3
/* @scripttype preserveParamNames */
export function In(def, $ = {}, args = {}) {
  return distill.In(inferDefinition(def, $, args))
}
/* compiles to:
 * export type In<def, $ = {}, args = {}> = distill.In<inferDefinition<def, $, args>>
 */

// ✗ Out: does not compile yet
//   Namespace '"/Users/yihein.chai/Documents/learn/ScriptType/03-schema-and-type-level-parsers/arktype/ark/type/attributes".distill.introspectable' has no exported 
/* @scripttype preserveParamNames */
export function Out(def, $ = {}, args = {}) {
  return distill.Out(inferDefinition(def, $, args))
}
/* compiles to:
 * export type Out<def, $ = {}, args = {}> = distill.Out<inferDefinition<def, $, args>>
 */

// ✗ Out: does not compile yet
//   Namespace '"/Users/yihein.chai/Documents/learn/ScriptType/03-schema-and-type-level-parsers/arktype/ark/type/attributes".distill.introspectable' has no exported 
/* @scripttype preserveParamNames */
export function Out(def, $ = {}, args = {}) {
  return distill.introspectable.Out(inferDefinition(def, $, args))
}
/* compiles to:
 * export type Out<def, $ = {}, args = {}> = distill.introspectable.Out<inferDefinition<def, $, args>>
 */

// ✓ brand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function brand(t, id) {
  const m1 = matches<InferredMorph<Hole<"i">, Hole<"o">>>(t)
  if (m1) {
    if (matches<true>(m1.o['introspectable'])) {
      return fnType([m1.i], To(Brand(m1.o['t'], id)))
    }
    return fnType([m1.i], Out(Brand(m1.o['t'], id)))
  }
  return Brand(t, id)
}
/* compiles to:
 * export type brand<t, id> = t extends InferredMorph<infer i, infer o> ? o['introspectable'] extends true ? (a0: i) => To<Brand<o['t'], id>> : (a0: i) => Out<Brand<o['t'], id>> : Brand<t, id>
 */

// ✗ type: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function type(t = unknown, $ = {}) {
  return Type(t, $)
}
/* compiles to:
 * export type type<t = unknown, $ = {}> = Type<t, $>
 */

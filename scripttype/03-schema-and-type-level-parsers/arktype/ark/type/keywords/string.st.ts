/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/keywords/string.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const DayDelimiter: any
declare const DayPart: any
declare const DayPatterns: any
type DayDelimiter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DayPart<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DayPatterns<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ fragment: verified type-identical to the original
/* @scripttype preserveParamNames */
export function fragment(part: DayPart, delimiter: DayDelimiter) {
  return anyOf(`${delimiter}${part}`, '')
}
/* compiles to:
 * export type fragment<part extends DayPart, delimiter extends DayDelimiter> = `${delimiter}${part}` | ''
 */

// ✗ DayPattern: the ScriptType does not itself typecheck as TypeScript
//   DayPattern.st.ts(5:57) TS2304: Cannot find name 'k1'.
/* @scripttype preserveParamNames */
export function DayPattern(delimiter: DayDelimiter = DayDelimiter) {
  if (matches<unknown>(delimiter)) {
    const out2 = emptyObject
    for (const k2 in keySet(Exclude(keyof(DayPatterns), k1))) {
      out2[k2] = `${DayPatterns[k1]}${fragment(DayPatterns[k2], delimiter)}${fragment(DayPatterns[Exclude(keyof(DayPatterns), anyOf(k1, k2))], delimiter)}`
    }
    const out = emptyObject
    for (const k1 in keyof(DayPatterns)) {
      out[k1] = out2[Exclude(keyof(DayPatterns), k1)]
    }
    return out[keyof(DayPatterns)]
  }
  return never
}
/* compiles to:
 * export type DayPattern<delimiter extends DayDelimiter = DayDelimiter> = delimiter extends unknown ? { [K1 in keyof DayPatterns]: { [K2 in Exclude<keyof DayPatterns, k1>]: `${DayPatterns[k1]}${fragment<DayPatterns[K2], delimiter>}${fragment<DayPatterns[Exclude<keyof DayPatterns, k1 | K2>], delimiter>}` }[Exclude<keyof DayPatterns, K1>] }[keyof DayPatterns] : never
 */

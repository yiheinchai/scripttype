/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/match.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 21 type-level aliases: 17 covered, 2 typecheck-error, 2 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace Morph {
  export type In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace type {
  export type infer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type validate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ArkErrors: any
declare const BaseCompletions: any
declare const CaseKeyKind: any
declare const ChainableMatchParser: any
declare const DefaultCaseKeyword: any
declare const ErrorMessage: any
declare const ErrorType: any
declare const Key: any
declare const Match: any
declare const Morph: any
declare const PropertyKey: any
declare const chainedAtMessage: any
declare const conform: any
declare const distill: any
declare const doubleAtMessage: any
declare const propValueOf: any
declare const result: any
declare const ret: any
declare const show: any
declare const type: any
declare const unionToTuple: any
declare const validate: any
type ArkErrors<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type BaseCompletions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type CaseKeyKind<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ChainableMatchParser<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DefaultCase<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DefaultCaseKeyword<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ErrorMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ErrorType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Key<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Match<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MatchParserContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Morph<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type chainedAtMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type conform<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type distill<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type doubleAtMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type propValueOf<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type result<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ret<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type show<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type stringValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type unionToTuple<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type validate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type validateCases<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ MatchParserContext: the ScriptType does not itself typecheck as TypeScript
//   MatchParserContext.st.ts(3:84) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function MatchParserContext(input = unknown) {
  return { cases: arrayOf(Morph), $: unknown, input: input, checked: boolean, key: PropertyKey | Null }
}
/* compiles to:
 * export type MatchParserContext<input = unknown> = {
 *   cases: Morph[]
 *   $: unknown
 *   input: input
 *   checked: boolean
 *   key: PropertyKey | null
 * }
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(ctx: MatchParserContext) {
  return ctx
}
/* compiles to:
 * export type from<ctx extends MatchParserContext> = ctx
 */

// ✓ init: verified type-identical to the original
/* @scripttype preserveParamNames */
export function init($, input = unknown, checked: boolean = false) {
  return from({ cases: [], $: $, input: input, checked: checked, key: Null })
}
/* compiles to:
 * export type init<$, input = unknown, checked extends boolean = false> = from<
 *   { cases: []; $: $; input: input; checked: checked; key: null }
 * >
 */

// ✓ atKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function atKey(ctx: MatchParserContext, key: string) {
  return from({ cases: ctx['cases'], $: ctx['$'], input: ctx['input'], checked: ctx['checked'], key: key })
}
/* compiles to:
 * export type atKey<ctx extends MatchParserContext, key extends string> = from<
 *   { cases: ctx['cases']; $: ctx['$']; input: ctx['input']; checked: ctx['checked']; key: key }
 * >
 */

// ✗ addCasesToContext: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function addCasesToContext(ctx: MatchParserContext, cases: unknown[]) {
  if (matches<Morph[]>(cases)) {
    return ctx.from({ $: ctx['$'], input: ctx['input'], cases: [...ctx['cases'], ...cases], checked: ctx['checked'], key: ctx['key'] })
  }
  return never
}

// ✗ addDefaultToContext: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function addDefaultToContext(ctx: MatchParserContext, defaultCase: DefaultCase<typeof ctx>) {
  return ctx.from({ $: ctx['$'], input: matches<"never">(defaultCase) ? Morph.In(ctx['cases'][number]) : ctx['input'], cases: matches<"never" | "assert">(defaultCase) ? ctx['cases'] : (matches<Morph>(defaultCase) ? (matches<true>(ctx['checked']) ? [fnType([unknown], ArkErrors), ...ctx['cases'], defaultCase] : [...ctx['cases'], defaultCase]) : [...ctx['cases'], fnType([ctx['input']], ArkErrors)]), checked: ctx['checked'], key: ctx['key'] })
}

// ✓ casesToMorphTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function casesToMorphTuple__v1(cases, ctx, kind, def) {
  const m1 = matches<(Morph<never, Hole<"o">>)>(cases[def])
  if (m1) {
    if (matches<"def">(kind)) {
      return fnType([inferCaseArg(typeof def === 'number' ? `${number}` : def, ctx, 'in')], m1.o)
    }
    return fnType([maybeLiftToKey(def, ctx)], m1.o)
  }
  return never
}
export function casesToMorphTuple(cases, ctx: MatchParserContext, kind: CaseKeyKind) {
  const out = emptyObject
  for (const def in keySet(Exclude(keyof(cases), 'default'))) {
    out[def] = casesToMorphTuple__v1(cases, ctx, kind, def)
  }
  return unionToTuple(propValueOf(out))
}
/* compiles to:
 * export type casesToMorphTuple__v1<cases, ctx, kind, def> =
 *   cases[def] extends Morph<never, infer o>
 *     ? kind extends 'def'
 *       ? (a0: inferCaseArg<def extends number ? `${number}` : def, ctx, 'in'>) => o
 *       : (a0: maybeLiftToKey<def, ctx>) => o
 *     : never
 * export type casesToMorphTuple<
 *   cases,
 *   ctx extends MatchParserContext,
 *   kind extends CaseKeyKind
 * > =
 *   unionToTuple<
 *     propValueOf<
 *       { [Def in Exclude<keyof cases, 'default'>]: casesToMorphTuple__v1<cases, ctx, kind, Def> }
 *     >
 *   >
 */

// ✗ addCasesToParser: the ScriptType does not itself typecheck as TypeScript
//   addCasesToParser.st.ts(3:64) TS2749: 'ctx' refers to a value, but is being used as a type here. Did you mean 'typeof ctx'?
/* @scripttype preserveParamNames */
export function addCasesToParser(cases, ctx: MatchParserContext, kind: CaseKeyKind) {
  const m1 = matches<{ default: Hole<"defaultDef", DefaultCase<ctx>>; }>(cases)
  if (m1) {
    return finalizeMatchParser(addCasesToContext(ctx, casesToMorphTuple(cases, ctx, kind)), m1.defaultDef)
  }
  return ChainableMatchParser(addCasesToContext(ctx, casesToMorphTuple(cases, ctx, kind)))
}
/* compiles to:
 * export type addCasesToParser<cases, ctx extends MatchParserContext, kind extends CaseKeyKind> =
 *   cases extends { default: infer defaultDef extends DefaultCase<ctx>; }
 *     ? finalizeMatchParser<
 *       addCasesToContext<ctx, casesToMorphTuple<cases, ctx, kind>>,
 *       defaultDef
 *     >
 *     : ChainableMatchParser<addCasesToContext<ctx, casesToMorphTuple<cases, ctx, kind>>>
 */

// ✓ inferCaseArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferCaseArg(def, ctx: MatchParserContext, endpoint: "in" | "out") {
  return _finalizeCaseArg(maybeLiftToKey(type.infer(def, ctx['$']), ctx), ctx, endpoint)
}
/* compiles to:
 * export type inferCaseArg<def, ctx extends MatchParserContext, endpoint extends 'in' | 'out'> = _finalizeCaseArg<
 *   maybeLiftToKey<type.infer<def, ctx['$']>, ctx>,
 *   ctx,
 *   endpoint
 * >
 */

// ✓ maybeLiftToKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function maybeLiftToKey(t, ctx: MatchParserContext) {
  if (matches<PropertyKey>(ctx['key'])) {
    const out = emptyObject
    for (const k in keySet(ctx['key'])) {
      out[k] = t
    }
    return out
  }
  return t
}
/* compiles to:
 * export type maybeLiftToKey<t, ctx extends MatchParserContext> =
 *   ctx['key'] extends PropertyKey ? { [K in ctx['key']]: t } : t
 */

// ✓ _finalizeCaseArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _finalizeCaseArg(t, ctx: MatchParserContext, endpoint: "in" | "out", ctxInput = ctx['input']) {
  if (matches<unknown>(ctxInput)) {
    if (matches<unknown>(t)) {
      const m1 = matches<Hole<"result">>(distill(t, endpoint))
      if (m1) {
        if (matches<typeof m1.result>(ctxInput)) {
          return ctxInput
        }
        return show(ctxInput & m1.result)
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type _finalizeCaseArg<
 *   t,
 *   ctx extends MatchParserContext,
 *   endpoint extends 'in' | 'out',
 *   ctxInput = ctx['input']
 * > =
 *   ctxInput extends unknown
 *     ? t extends unknown
 *       ? distill<t, endpoint> extends infer result
 *         ? ctxInput extends result ? ctxInput : show<ctxInput & result>
 *         : never
 *       : never
 *     : never
 */

// ✓ CaseParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseParser(ctx: MatchParserContext) {
  return genericFnType(['const def', 'ret'], [type.validate(def, ctx['$']), fnType([inferCaseArg(def, ctx, 'out')], ret)], ChainableMatchParser(addCasesToContext(ctx, [fnType([inferCaseArg(def, ctx, 'in')], ret)])))
}
/* compiles to:
 * export type CaseParser<ctx extends MatchParserContext> =
 *   <const def, ret>(
 *     a0: type.validate<def, ctx['$']>,
 *     a1: (a0: inferCaseArg<def, ctx, 'out'>) => ret
 *   ) => ChainableMatchParser<addCasesToContext<ctx, [(a0: inferCaseArg<def, ctx, 'in'>) => ret]>>
 */

// ✓ validateKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateKey(key: Key, ctx: MatchParserContext) {
  if (matches<Key>(ctx['key'])) {
    return ErrorMessage(doubleAtMessage)
  }
  if (matches<0>(ctx['cases']['length'])) {
    if (matches<never>(keyof(ctx['input']))) {
      return key
    }
    return conform(key, keyof(ctx['input']))
  }
  return ErrorMessage(chainedAtMessage)
}
/* compiles to:
 * export type validateKey<key extends Key, ctx extends MatchParserContext> =
 *   ctx['key'] extends Key ? ErrorMessage<doubleAtMessage>
 *   : ctx['cases']['length'] extends 0
 *     ? keyof ctx['input'] extends never ? key : conform<key, keyof ctx['input']>
 *   : ErrorMessage<chainedAtMessage>
 */

// ✓ validateStringCases: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateStringCases(cases, ctx: MatchParserContext) {
  if (matches<(typeof ctx)["input"]>(unknown)) {
    const out = emptyObject
    for (const k in keyof(cases)) {
      out[k] = optional(matches<"default">(k) ? DefaultCase(ctx) : fnType([_finalizeCaseArg(maybeLiftToKey(k, ctx), ctx, 'out')], unknown))
    }
    return merge(out, { default: optional(DefaultCase(ctx)) })
  }
  const out2 = emptyObject
  for (const k in keyof(cases)) {
    out2[k] = optional(matches<"default">(k) ? DefaultCase(ctx) : (matches<stringValue<typeof ctx>>(k) ? fnType([_finalizeCaseArg(maybeLiftToKey(k, ctx), ctx, 'out')], unknown) : ErrorType(`${merge(k, string)} must be a possible string value`)))
  }
  const out3 = emptyObject
  for (const k in keySet(stringValue(ctx))) {
    out3[k] = optional(unknown)
  }
  return merge(out2, out3, { default: optional(DefaultCase(ctx)) })
}
/* compiles to:
 * export type validateStringCases<cases, ctx extends MatchParserContext> =
 *   unknown extends ctx['input']
 *     ? & {
 *         [K in keyof cases]?: K extends 'default'
 *           ? DefaultCase<ctx>
 *           : (a0: _finalizeCaseArg<maybeLiftToKey<K, ctx>, ctx, 'out'>) => unknown
 *       }
 *     & { default?: DefaultCase<ctx> }
 *     : & {
 *         [K1 in keyof cases]?: K1 extends 'default' ? DefaultCase<ctx>
 *         : K1 extends stringValue<ctx>
 *           ? (a0: _finalizeCaseArg<maybeLiftToKey<K1, ctx>, ctx, 'out'>) => unknown
 *         : ErrorType<`${K1 & string} must be a possible string value`>
 *       }
 *     & { [K2 in stringValue<ctx>]?: unknown }
 *     & { default?: DefaultCase<ctx> }
 */

// ✓ stringValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function stringValue(ctx: MatchParserContext) {
  if (typeof ctx['input'] === 'string') {
    return ctx['input']
  }
  if (ctx['key'] in ctx['input']) {
    const m1 = matches<Hole<"s", string>>(ctx['input'][ctx['key']])
    if (m1) {
      return m1.s
    }
    return never
  }
  return never
}
/* compiles to:
 * export type stringValue<ctx extends MatchParserContext> =
 *   ctx['input'] extends string ? ctx['input']
 *   : ctx['key'] extends keyof ctx['input']
 *     ? ctx['input'][ctx['key']] extends (infer s extends string) ? s : never
 *   : never
 */

// ✓ DefaultCase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DefaultCase(ctx: MatchParserContext = MatchParserContext(any)) {
  return DefaultCaseKeyword | Morph(ctx['input'])
}
/* compiles to:
 * export type DefaultCase<ctx extends MatchParserContext = MatchParserContext<any>> =
 *   DefaultCaseKeyword | Morph<ctx['input']>
 */

// ✓ DefaultMethod: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DefaultMethod(ctx: MatchParserContext) {
  return genericFnType(['const def extends DefaultCase<ctx>'], [def], finalizeMatchParser(ctx, def))
}
/* compiles to:
 * export type DefaultMethod<ctx extends MatchParserContext> =
 *   <const def extends DefaultCase<ctx>>(a0: def) => finalizeMatchParser<ctx, def>
 */

// ✓ validateCases: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateCases(cases, ctx: MatchParserContext) {
  const out = emptyObject
  for (const def in keySet(keyof(cases) | BaseCompletions(ctx['$'], {}, 'default'))) {
    out[def] = optional(matches<"default">(def) ? DefaultCase(ctx) : (typeof def === 'number' ? fnType([inferCaseArg(`${def}`, ctx, 'out')], unknown) : (matches<type.validate<typeof def, (typeof ctx)["$"]>>(def) ? fnType([inferCaseArg(def, ctx, 'out')], unknown) : type.validate(def, ctx['$']))))
  }
  return out
}
/* compiles to:
 * export type validateCases<cases, ctx extends MatchParserContext> = {
 *   [Def in keyof cases | BaseCompletions<ctx['$'], {}, 'default'>]?: Def extends 'default' ? DefaultCase<ctx>
 *   : Def extends number ? (a0: inferCaseArg<`${Def}`, ctx, 'out'>) => unknown
 *   : Def extends type.validate<Def, ctx['$']> ? (a0: inferCaseArg<Def, ctx, 'out'>) => unknown
 *   : type.validate<Def, ctx['$']>
 * }
 */

// ✓ errorCases: verified type-identical to the original
/* @scripttype preserveParamNames */
export function errorCases(cases, ctx: MatchParserContext) {
  const out = emptyObject
  for (const def in keyof(cases)) {
    out[def] = optional(matches<"default">(def) ? DefaultCase(ctx) : (typeof def === 'number' ? fnType([inferCaseArg(`${def}`, ctx, 'out')], unknown) : (matches<type.validate<typeof def, (typeof ctx)["$"]>>(def) ? fnType([inferCaseArg(def, ctx, 'out')], unknown) : ErrorType(type.validate(def, ctx['$'])))))
  }
  const out2 = emptyObject
  for (const k in keySet(BaseCompletions(ctx['$'], {}))) {
    out2[k] = optional(fnType([inferCaseArg(k, ctx, 'out')], unknown))
  }
  return merge(out, out2, { default: optional(DefaultCase(ctx)) })
}
/* compiles to:
 * export type errorCases<cases, ctx extends MatchParserContext> =
 *   & {
 *       [Def in keyof cases]?: Def extends 'default' ? DefaultCase<ctx>
 *       : Def extends number ? (a0: inferCaseArg<`${Def}`, ctx, 'out'>) => unknown
 *       : Def extends type.validate<Def, ctx['$']>
 *         ? (a0: inferCaseArg<Def, ctx, 'out'>) => unknown
 *       : ErrorType<type.validate<Def, ctx['$']>>
 *     }
 *   & { [K in BaseCompletions<ctx['$'], {}>]?: (a0: inferCaseArg<K, ctx, 'out'>) => unknown }
 *   & { default?: DefaultCase<ctx> }
 */

// ✓ CaseMatchParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseMatchParser(ctx: MatchParserContext) {
  return genericFnType(['const cases'], [matches<validateCases<cases, typeof ctx>>(cases) ? cases : errorCases(cases, ctx)], addCasesToParser(cases, ctx, 'def'))
}
/* compiles to:
 * export type CaseMatchParser<ctx extends MatchParserContext> =
 *   <const cases>(
 *     a0: cases extends validateCases<cases, ctx> ? cases : errorCases<cases, ctx>
 *   ) => addCasesToParser<cases, ctx, 'def'>
 */

// ✓ finalizeMatchParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeMatchParser(ctx: MatchParserContext, defaultCase: DefaultCase<typeof ctx>) {
  const m1 = matches<(Hole<"ctx", MatchParserContext>)>(addDefaultToContext(ctx, defaultCase))
  if (m1) {
    return Match(m1.ctx['input'], m1.ctx['cases'])
  }
  return never
}
/* compiles to:
 * export type finalizeMatchParser<
 *   ctx extends MatchParserContext,
 *   defaultCase extends DefaultCase<ctx>
 * > =
 *   addDefaultToContext<ctx, defaultCase> extends (infer ctx extends MatchParserContext)
 *     ? Match<ctx['input'], ctx['cases']>
 *     : never
 */

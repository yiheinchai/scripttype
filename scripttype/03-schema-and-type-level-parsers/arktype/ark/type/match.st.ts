/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/match.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 21 type-level aliases: 11 covered, 4 typecheck-error, 4 raw, 2 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ArkErrors: any
declare const BaseCompletions: any
declare const CaseKeyKind: any
declare const ChainableMatchParser: any
declare const DefaultCaseKeyword: any
declare const ErrorMessage: any
declare const ErrorType: any
declare const Key: any
declare const Match: any
declare const Morph: any
declare const PropertyKey: any
declare const chainedAtMessage: any
declare const conform: any
declare const distill: any
declare const doubleAtMessage: any
declare const propValueOf: any
declare const result: any
declare const show: any
declare const type: any
declare const unionToTuple: any
declare const validate: any
type ArkErrors<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BaseCompletions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type CaseKeyKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ChainableMatchParser<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DefaultCase<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DefaultCaseKeyword<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Key<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Match<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MatchParserContext<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Morph<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type chainedAtMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type conform<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type distill<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type doubleAtMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type propValueOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type result<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type show<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type stringValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type type<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type unionToTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ MatchParserContext: the ScriptType does not itself typecheck as TypeScript
//   MatchParserContext.st.ts(3:84) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function MatchParserContext(input = unknown) {
  return { cases: arrayOf(Morph), $: unknown, input: input, checked: boolean, key: PropertyKey | Null }
}
/* compiles to:
 * export type MatchParserContext<input = unknown> = { cases: Morph[]; $: unknown; input: input; checked: boolean; key: PropertyKey | null }
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(ctx: MatchParserContext) {
  return ctx
}
/* compiles to:
 * export type from<ctx extends MatchParserContext> = ctx
 */

// ✓ init: verified type-identical to the original
/* @scripttype preserveParamNames */
export function init($, input = unknown, checked: boolean = false) {
  return from({ cases: [], $: $, input: input, checked: checked, key: Null })
}
/* compiles to:
 * export type init<$, input = unknown, checked extends boolean = false> = from<{ cases: []; $: $; input: input; checked: checked; key: null }>
 */

// ✓ atKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function atKey(ctx: MatchParserContext, key: string) {
  return from({ cases: ctx['cases'], $: ctx['$'], input: ctx['input'], checked: ctx['checked'], key: key })
}
/* compiles to:
 * export type atKey<ctx extends MatchParserContext, key extends string> = from<{ cases: ctx['cases']; $: ctx['$']; input: ctx['input']; checked: ctx['checked']; key: key }>
 */

// ✗ addCasesToContext: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function addCasesToContext(ctx: MatchParserContext, cases: unknown[]) {
  if (matches<Morph[]>(cases)) {
    return ctx.from({ $: ctx['$'], input: ctx['input'], cases: [...ctx['cases'], ...cases], checked: ctx['checked'], key: ctx['key'] })
  }
  return never
}

// ✗ addDefaultToContext: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function addDefaultToContext(ctx: MatchParserContext, defaultCase: DefaultCase<typeof ctx>) {
  return ctx.from({ $: ctx['$'], input: matches<"never">(defaultCase) ? Morph.In(ctx['cases'][number]) : ctx['input'], cases: matches<"never" | "assert">(defaultCase) ? ctx['cases'] : (matches<Morph>(defaultCase) ? (matches<true>(ctx['checked']) ? [fnType([unknown], ArkErrors), ...ctx['cases'], defaultCase] : [...ctx['cases'], defaultCase]) : [...ctx['cases'], fnType([ctx['input']], ArkErrors)]), checked: ctx['checked'], key: ctx['key'] })
}

// ✗ casesToMorphTuple: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function casesToMorphTuple(cases, ctx: MatchParserContext, kind: CaseKeyKind) {
  const out = emptyObject
  for (const def in keySet(Exclude(keyof(cases), 'default'))) {
    out[def] = raw('cases[def] extends (Morph<never, infer o>) ? kind extends "def" ? (In: inferCaseArg<def extends number ? `${number}` : def, ctx, "in">) => o : (In: maybeLiftToKey<def, ctx>) => o : never')
  }
  return unionToTuple(propValueOf(out))
}

// ✗ addCasesToParser: the ScriptType does not itself typecheck as TypeScript
//   addCasesToParser.st.ts(3:64) TS2749: 'ctx' refers to a value, but is being used as a type here. Did you mean 'typeof ctx'?
/* @scripttype preserveParamNames */
export function addCasesToParser(cases, ctx: MatchParserContext, kind: CaseKeyKind) {
  const m1 = matches<{ default: Hole<"defaultDef", DefaultCase<ctx>>; }>(cases)
  if (m1) {
    return finalizeMatchParser(addCasesToContext(ctx, casesToMorphTuple(cases, ctx, kind)), m1.defaultDef)
  }
  return ChainableMatchParser(addCasesToContext(ctx, casesToMorphTuple(cases, ctx, kind)))
}
/* compiles to:
 * export type addCasesToParser<cases, ctx extends MatchParserContext, kind extends CaseKeyKind> = cases extends { default: infer defaultDef extends DefaultCase<ctx>; } ? finalizeMatchParser<addCasesToContext<ctx, casesToMorphTuple<cases, ctx, kind>>, defaultDef> : ChainableMatchParser<addCasesToContext<ctx, casesToMorphTuple<cases, ctx, kind>>>
 */

// ✓ inferCaseArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferCaseArg(def, ctx: MatchParserContext, endpoint: "in" | "out") {
  return _finalizeCaseArg(maybeLiftToKey(type.infer(def, ctx['$']), ctx), ctx, endpoint)
}
/* compiles to:
 * export type inferCaseArg<def, ctx extends MatchParserContext, endpoint extends 'in' | 'out'> = _finalizeCaseArg<maybeLiftToKey<type.infer<def, ctx['$']>, ctx>, ctx, endpoint>
 */

// ✓ maybeLiftToKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function maybeLiftToKey(t, ctx: MatchParserContext) {
  if (matches<PropertyKey>(ctx['key'])) {
    const out = emptyObject
    for (const k in keySet(ctx['key'])) {
      out[k] = t
    }
    return out
  }
  return t
}
/* compiles to:
 * export type maybeLiftToKey<t, ctx extends MatchParserContext> = ctx['key'] extends PropertyKey ? { [K in ctx['key']]: t } : t
 */

// ✓ _finalizeCaseArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _finalizeCaseArg(t, ctx: MatchParserContext, endpoint: "in" | "out", ctxInput = ctx['input']) {
  if (matches<unknown>(ctxInput)) {
    if (matches<unknown>(t)) {
      const m1 = matches<Hole<"result">>(distill(t, endpoint))
      if (m1) {
        if (matches<typeof m1.result>(ctxInput)) {
          return ctxInput
        }
        return show(ctxInput & m1.result)
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type _finalizeCaseArg<t, ctx extends MatchParserContext, endpoint extends 'in' | 'out', ctxInput = ctx['input']> = ctxInput extends unknown ? t extends unknown ? distill<t, endpoint> extends infer result ? ctxInput extends result ? ctxInput : show<ctxInput & result> : never : never : never
 */

// ✗ CaseParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function CaseParser(ctx: MatchParserContext) {
  return raw('<const def, ret>(def: type.validate<def, ctx["$"]>, resolve: (In: inferCaseArg<def, ctx, "out">) => ret) => ChainableMatchParser<addCasesToContext<ctx, [ (In: inferCaseArg<def, ctx, "in">) => ret ]>>')
}

// ✓ validateKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateKey(key: Key, ctx: MatchParserContext) {
  if (matches<Key>(ctx['key'])) {
    return ErrorMessage(doubleAtMessage)
  }
  if (matches<0>(ctx['cases']['length'])) {
    if (matches<never>(keyof(ctx['input']))) {
      return key
    }
    return conform(key, keyof(ctx['input']))
  }
  return ErrorMessage(chainedAtMessage)
}
/* compiles to:
 * export type validateKey<key extends Key, ctx extends MatchParserContext> = ctx['key'] extends Key ? ErrorMessage<doubleAtMessage> : ctx['cases']['length'] extends 0 ? keyof ctx['input'] extends never ? key : conform<key, keyof ctx['input']> : ErrorMessage<chainedAtMessage>
 */

// ✓ validateStringCases: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateStringCases(cases, ctx: MatchParserContext) {
  if (matches<(typeof ctx)["input"]>(unknown)) {
    const out = emptyObject
    for (const k in keyof(cases)) {
      out[k] = optional(matches<"default">(k) ? DefaultCase(ctx) : fnType([_finalizeCaseArg(maybeLiftToKey(k, ctx), ctx, 'out')], unknown))
    }
    return merge(out, { default: optional(DefaultCase(ctx)) })
  }
  const out2 = emptyObject
  for (const k in keyof(cases)) {
    out2[k] = optional(matches<"default">(k) ? DefaultCase(ctx) : (matches<stringValue<typeof ctx>>(k) ? fnType([_finalizeCaseArg(maybeLiftToKey(k, ctx), ctx, 'out')], unknown) : ErrorType(`${merge(k, string)} must be a possible string value`)))
  }
  const out3 = emptyObject
  for (const k in keySet(stringValue(ctx))) {
    out3[k] = optional(unknown)
  }
  return merge(out2, out3, { default: optional(DefaultCase(ctx)) })
}
/* compiles to:
 * export type validateStringCases<cases, ctx extends MatchParserContext> = unknown extends ctx['input'] ? { [K in keyof cases]?: K extends 'default' ? DefaultCase<ctx> : (a0: _finalizeCaseArg<maybeLiftToKey<K, ctx>, ctx, 'out'>) => unknown } & { default?: DefaultCase<ctx> } : { [K1 in keyof cases]?: K1 extends 'default' ? DefaultCase<ctx> : K1 extends stringValue<ctx> ? (a0: _finalizeCaseArg<maybeLiftToKey<K1, ctx>, ctx, 'out'>) => unknown : ErrorType<`${K1 & string} must be a possible string value`> } & { [K2 in stringValue<ctx>]?: unknown } & { default?: DefaultCase<ctx> }
 */

// ✓ stringValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function stringValue(ctx: MatchParserContext) {
  if (matches<string>(ctx['input'])) {
    return ctx['input']
  }
  if (matches<keyof (typeof ctx)["input"]>(ctx['key'])) {
    const m1 = matches<Hole<"s", string>>(ctx['input'][ctx['key']])
    if (m1) {
      return m1.s
    }
    return never
  }
  return never
}
/* compiles to:
 * export type stringValue<ctx extends MatchParserContext> = ctx['input'] extends string ? ctx['input'] : ctx['key'] extends keyof ctx['input'] ? ctx['input'][ctx['key']] extends (infer s extends string) ? s : never : never
 */

// ✓ DefaultCase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DefaultCase(ctx: MatchParserContext = MatchParserContext(any)) {
  return DefaultCaseKeyword | Morph(ctx['input'])
}
/* compiles to:
 * export type DefaultCase<ctx extends MatchParserContext = MatchParserContext<any>> = DefaultCaseKeyword | Morph<ctx['input']>
 */

// ✗ DefaultMethod: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function DefaultMethod(ctx: MatchParserContext) {
  return raw('<const def extends DefaultCase<ctx>>(def: def) => finalizeMatchParser<ctx, def>')
}

// ✗ validateCases: the ScriptType does not itself typecheck as TypeScript
//   validateCases.st.ts(5:159) TS2702: 'type' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function validateCases(cases, ctx: MatchParserContext) {
  const out = emptyObject
  for (const def in keySet(keyof(cases) | BaseCompletions(ctx['$'], {}, 'default'))) {
    out[def] = optional(matches<"default">(def) ? DefaultCase(ctx) : (matches<number>(def) ? fnType([inferCaseArg(`${def}`, ctx, 'out')], unknown) : (matches<type.validate<typeof def, (typeof ctx)["$"]>>(def) ? fnType([inferCaseArg(def, ctx, 'out')], unknown) : type.validate(def, ctx['$']))))
  }
  return out
}
/* compiles to:
 * export type validateCases<cases, ctx extends MatchParserContext> = { [Def in keyof cases | BaseCompletions<ctx['$'], {}, 'default'>]?: Def extends 'default' ? DefaultCase<ctx> : Def extends number ? (a0: inferCaseArg<`${Def}`, ctx, 'out'>) => unknown : Def extends type.validate<Def, ctx['$']> ? (a0: inferCaseArg<Def, ctx, 'out'>) => unknown : type.validate<Def, ctx['$']> }
 */

// ✗ errorCases: the ScriptType does not itself typecheck as TypeScript
//   errorCases.st.ts(5:158) TS2702: 'type' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function errorCases(cases, ctx: MatchParserContext) {
  const out = emptyObject
  for (const def in keyof(cases)) {
    out[def] = optional(matches<"default">(def) ? DefaultCase(ctx) : (matches<number>(def) ? fnType([inferCaseArg(`${def}`, ctx, 'out')], unknown) : (matches<type.validate<typeof def, (typeof ctx)["$"]>>(def) ? fnType([inferCaseArg(def, ctx, 'out')], unknown) : ErrorType(type.validate(def, ctx['$'])))))
  }
  const out2 = emptyObject
  for (const k in keySet(BaseCompletions(ctx['$'], {}))) {
    out2[k] = optional(fnType([inferCaseArg(k, ctx, 'out')], unknown))
  }
  return merge(out, out2, { default: optional(DefaultCase(ctx)) })
}
/* compiles to:
 * export type errorCases<cases, ctx extends MatchParserContext> = { [Def in keyof cases]?: Def extends 'default' ? DefaultCase<ctx> : Def extends number ? (a0: inferCaseArg<`${Def}`, ctx, 'out'>) => unknown : Def extends type.validate<Def, ctx['$']> ? (a0: inferCaseArg<Def, ctx, 'out'>) => unknown : ErrorType<type.validate<Def, ctx['$']>> } & { [K in BaseCompletions<ctx['$'], {}>]?: (a0: inferCaseArg<K, ctx, 'out'>) => unknown } & { default?: DefaultCase<ctx> }
 */

// ✗ CaseMatchParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function CaseMatchParser(ctx: MatchParserContext) {
  return raw('<const cases>(def: cases extends validateCases<cases, ctx> ? cases : errorCases<cases, ctx>) => addCasesToParser<cases, ctx, "def">')
}

// ✓ finalizeMatchParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeMatchParser(ctx: MatchParserContext, defaultCase: DefaultCase<typeof ctx>) {
  const m1 = matches<(Hole<"ctx", MatchParserContext>)>(addDefaultToContext(ctx, defaultCase))
  if (m1) {
    return Match(m1.ctx['input'], m1.ctx['cases'])
  }
  return never
}
/* compiles to:
 * export type finalizeMatchParser<ctx extends MatchParserContext, defaultCase extends DefaultCase<ctx>> = addDefaultToContext<ctx, defaultCase> extends (infer ctx extends MatchParserContext) ? Match<ctx['input'], ctx['cases']> : never
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/definition.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 6 covered, 1 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BadDefinitionType: any
declare const BaseCompletions: any
declare const ErrorMessage: any
declare const OptionalPropertyDefinition: any
declare const Out: any
declare const StandardSchemaV1: any
declare const TerminalObjectDefinition: any
declare const TupleExpression: any
declare const anyOrNever: any
declare const array: any
declare const cast: any
declare const ifEmptyObjectLiteral: any
declare const inferObjectLiteral: any
declare const inferString: any
declare const inferTupleExpression: any
declare const inferTupleLiteral: any
declare const isDefaultable: any
declare const maybeValidateTupleExpression: any
declare const objectKindOrDomainOf: any
declare const shallowDefaultableMessage: any
declare const shallowOptionalMessage: any
declare const type: any
declare const validateObjectLiteral: any
declare const validateString: any
declare const validateTupleLiteral: any
type BadDefinitionType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BaseCompletions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OptionalPropertyDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Out<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StandardSchemaV1<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TerminalObjectDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ThunkCast<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TupleExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type anyOrNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type cast<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ifEmptyObjectLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferObjectLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferTupleExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferTupleLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type isDefaultable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type maybeValidateTupleExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type objectKindOrDomainOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shallowDefaultableMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shallowOptionalMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type type<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateObjectLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateTupleLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ inferDefinition: the ScriptType does not itself typecheck as TypeScript
//   inferDefinition.st.ts(6:22) TS2702: 'type' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function inferDefinition(def, $, args) {
  if (matches<[ anyOrNever ]>([def])) {
    return def
  }
  const m1 = matches<type.cast<Hole<"t">>>(def)
  if (m1) {
    return ifEmptyObjectLiteral(def, object, m1.t)
  }
  const m2 = matches<ThunkCast<Hole<"t">>>(def)
  if (m2) {
    return m2.t
  }
  if (matches<string>(def)) {
    return inferString(def, $, args)
  }
  if (matches<array>(def)) {
    return inferTuple(def, $, args)
  }
  if (matches<RegExp>(def)) {
    return string
  }
  if (matches<StandardSchemaV1>(def)) {
    return inferStandardSchema(def)
  }
  if (matches<object>(def)) {
    return inferObjectLiteral(def, $, args)
  }
  return never
}
/* compiles to:
 * export type inferDefinition<def, $, args> = [def] extends [anyOrNever] ? def : def extends type.cast<infer t> ? ifEmptyObjectLiteral<def, object, t> : def extends ThunkCast<infer t> ? t : def extends string ? inferString<def, $, args> : def extends array ? inferTuple<def, $, args> : def extends RegExp ? string : def extends StandardSchemaV1 ? inferStandardSchema<def> : def extends object ? inferObjectLiteral<def, $, args> : never
 */

// ✓ inferStandardSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferStandardSchema(schema: StandardSchemaV1, i = StandardSchemaV1.InferInput(schema), o = StandardSchemaV1.InferOutput(schema)) {
  if (matches<[ typeof o, typeof i ]>([i, o])) {
    return i
  }
  return fnType([i], Out(o))
}
/* compiles to:
 * export type inferStandardSchema<schema extends StandardSchemaV1, i = StandardSchemaV1.InferInput<schema>, o = StandardSchemaV1.InferOutput<schema>> = [i, o] extends [o, i] ? i : (a0: i) => Out<o>
 */

// ✗ validateDefinition: uses raw() — language gap, does not count as covered
//   gap: literal type FirstTemplateToken
/* @scripttype preserveParamNames */
export function validateDefinition(def, $, args) {
  if (matches<undefined>(Null)) {
    return ErrorMessage(raw('`\'strict\' or \'strictNullChecks\' must be set to true in your tsconfig\'s \'compilerOptions\'`'))
  }
  if (matches<[ anyOrNever ]>([def])) {
    return def
  }
  if (matches<OptionalPropertyDefinition>(def)) {
    return ErrorMessage(shallowOptionalMessage)
  }
  if (matches<true>(isDefaultable(def, $, args))) {
    return ErrorMessage(shallowDefaultableMessage)
  }
  return validateInnerDefinition(def, $, args)
}

// ✓ validateInnerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateInnerDefinition(def, $, args) {
  if (matches<[ TerminalObjectDefinition ]>([def])) {
    return def
  }
  if (matches<string>(def)) {
    return validateString(def, $, args)
  }
  if (matches<typeof def>(unknown)) {
    return anyOf(BaseCompletions($, args), {})
  }
  if (matches<readonly unknown[]>(def)) {
    return validateTuple(def, $, args)
  }
  if (matches<BadDefinitionType>(def)) {
    return ErrorMessage(writeBadDefinitionTypeMessage(objectKindOrDomainOf(def)))
  }
  return validateObjectLiteral(def, $, args)
}
/* compiles to:
 * export type validateInnerDefinition<def, $, args> = [def] extends [TerminalObjectDefinition] ? def : def extends string ? validateString<def, $, args> : unknown extends def ? BaseCompletions<$, args> | {} : def extends readonly unknown[] ? validateTuple<def, $, args> : def extends BadDefinitionType ? ErrorMessage<writeBadDefinitionTypeMessage<objectKindOrDomainOf<def>>> : validateObjectLiteral<def, $, args>
 */

// ✓ validateTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateTuple(def: array, $, args) {
  const m1 = matches<Hole<"result">>(maybeValidateTupleExpression(def, $, args))
  if (m1) {
    if (matches<null>(m1.result)) {
      return validateTupleLiteral(def, $, args)
    }
    return m1.result
  }
  return never
}
/* compiles to:
 * export type validateTuple<def extends array, $, args> = maybeValidateTupleExpression<def, $, args> extends infer result ? result extends null ? validateTupleLiteral<def, $, args> : result : never
 */

// ✓ inferTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferTuple(def: array, $, args) {
  if (matches<TupleExpression>(def)) {
    return inferTupleExpression(def, $, args)
  }
  return inferTupleLiteral(def, $, args)
}
/* compiles to:
 * export type inferTuple<def extends array, $, args> = def extends TupleExpression ? inferTupleExpression<def, $, args> : inferTupleLiteral<def, $, args>
 */

// ✓ ThunkCast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ThunkCast(t = unknown) {
  return fnType([], type.cast(t))
}
/* compiles to:
 * export type ThunkCast<t = unknown> = () => type.cast<t>
 */

// ✓ writeBadDefinitionTypeMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeBadDefinitionTypeMessage(actual: string) {
  return `Type definitions must be strings or objects (was ${actual})`
}
/* compiles to:
 * export type writeBadDefinitionTypeMessage<actual extends string> = `Type definitions must be strings or objects (was ${actual})`
 */

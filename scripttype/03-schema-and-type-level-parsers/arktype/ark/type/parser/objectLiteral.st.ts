/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/objectLiteral.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 12 type-level aliases: 11 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { validateString } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/ast/validate.js'
import type { inferDefinition } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/definition.js'
import type { EntryKeyKind, MetaKey, SpecialKeyKind } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/objectLiteral.js'
import type { OptionalPropertyDefinition, validateProperty } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/property.js'
import type { ErrorMessage, ErrorType } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/errors.js'
import type { anyOrNever, show } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
import type { Key } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/keys.js'
declare namespace PreparsedKey {
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type baseDef<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type inner<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type parsedKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type baseName<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type message<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type escapedMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m4 {
  export type def<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Backslash: any
declare const EntryKeyKind: any
declare const ErrorMessage: any
declare const ErrorType: any
declare const Key: any
declare const MetaKey: any
declare const OptionalPropertyDefinition: any
declare const PreparsedKey: any
declare const PropertyKey: any
declare const SpecialKeyKind: any
declare const anyOrNever: any
declare const baseName: any
declare const escapedIndexKey: any
declare const escapedMeta: any
declare const inferDefinition: any
declare const inner: any
declare const show: any
declare const validateProperty: any
declare const validateString: any
declare const writeInvalidPropertyKeyMessage: any
type Backslash<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IndexKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PreparsedEntryKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PreparsedKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type baseName<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type escapedIndexKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type escapedMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type inner<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeInvalidPropertyKeyMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ inferObjectLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferObjectLiteral(def: object, $, args): any {
  return show('...' in def ? merge(inferDefinition(def['...'], $, args), _inferObjectLiteral(def, $, args)) : _inferObjectLiteral(def, $, args))
}
/* compiles to:
 * export type inferObjectLiteral<def extends object, $, args> = show<
 *   '...' extends keyof def
 *     ? inferDefinition<def['...'], $, args> & _inferObjectLiteral<def, $, args>
 *     : _inferObjectLiteral<def, $, args>
 * >
 */

// ✓ _inferObjectLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _inferObjectLiteral__v1(def, $, args, k): any {
  const m1 = matches<OptionalPropertyDefinition<Hole<"baseDef">>>(def[k])
  if (m1) {
    return inferDefinition(m1.baseDef, $, args)
  }
  return inferDefinition(def[k], $, args)
}
/**
 * @param {object} def
 */
export function _inferObjectLiteral(def, $, args): any {
  const out = emptyObject
  for (const k in keyof(def)) {
    out[nonOptionalKeyFromEntry(k, def[k], $, args)] = mutable(inferDefinition(def[k], $, args))
  }
  const out2 = emptyObject
  for (const k in keyof(def)) {
    out2[optionalKeyFromEntry(k, def[k])] = mutable(optional(_inferObjectLiteral__v1(def, $, args, k)))
  }
  return out & out2
}
/* compiles to:
 * export type _inferObjectLiteral__v1<def, $, args, k> =
 *   def[k] extends OptionalPropertyDefinition<infer baseDef>
 *     ? inferDefinition<baseDef, $, args>
 *     : inferDefinition<def[k], $, args>
 * export type _inferObjectLiteral<def extends object, $, args> =
 *   & {
 *       -readonly [K in keyof def as nonOptionalKeyFromEntry<K, def[K], $, args>]: inferDefinition<
 *         def[K],
 *         $,
 *         args
 *       >
 *     }
 *   & {
 *       -readonly [K1 in keyof def as optionalKeyFromEntry<K1, def[K1]>]?: _inferObjectLiteral__v1<
 *         def,
 *         $,
 *         args,
 *         K1
 *       >
 *     }
 */

// ✓ validateObjectLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateObjectLiteral__v1(def, $, args, k): any {
  const m1 = matches<(Hole<"parsedKey", PreparsedKey>)>(preparseKey(k))
  if (m1) {
    if (matches<PreparsedEntryKey<"index">>(m1.parsedKey)) {
      const m2 = matches<(ErrorMessage<Hole<"message">>)>(validateString(m1.parsedKey['normalized'], $, args))
      if (m2) {
        return ErrorType(m2.message)
      }
      if (matches<Key>(inferDefinition(m1.parsedKey['normalized'], $, args))) {
        return validateProperty(def[k], m1.parsedKey['kind'], $, args)
      }
      return ErrorMessage(writeInvalidPropertyKeyMessage(m1.parsedKey['normalized']))
    }
    return validateProperty(def[k], m1.parsedKey['kind'], $, args)
  }
  return never
}
export function validateObjectLiteral(def, $, args): any {
  const out = emptyObject
  for (const k in keyof(def)) {
    out[k] = validateObjectLiteral__v1(def, $, args, k)
  }
  return out
}
/* compiles to:
 * export type validateObjectLiteral__v1<def, $, args, k> =
 *   preparseKey<k> extends (infer parsedKey extends PreparsedKey)
 *     ? parsedKey extends PreparsedEntryKey<'index'>
 *       ? validateString<parsedKey['normalized'], $, args> extends ErrorMessage<infer message>
 *         ? ErrorType<message>
 *       : inferDefinition<parsedKey['normalized'], $, args> extends Key
 *         ? validateProperty<def[k], parsedKey['kind'], $, args>
 *       : ErrorMessage<writeInvalidPropertyKeyMessage<parsedKey['normalized']>>
 *       : validateProperty<def[k], parsedKey['kind'], $, args>
 *     : never
 * export type validateObjectLiteral<def, $, args> = {
 *   [K in keyof def]: validateObjectLiteral__v1<def, $, args, K>
 * }
 */

// ✓ nonOptionalKeyFromEntry: verified type-identical to the original
/* @scripttype preserveParamNames */
export function nonOptionalKeyFromEntry(k: PropertyKey, v, $, args): any {
  const m1 = matches<Hole<"parsedKey">>(preparseKey(k))
  if (m1) {
    if (matches<PreparsedEntryKey<"required">>(m1.parsedKey)) {
      if (matches<[ OptionalPropertyDefinition ]>([v])) {
        if (matches<[ anyOrNever ]>([v])) {
          return m1.parsedKey['normalized']
        }
        return never
      }
      return m1.parsedKey['normalized']
    }
    if (matches<PreparsedEntryKey<"index">>(m1.parsedKey)) {
      return inferDefinition(m1.parsedKey['normalized'], $, args) & Key
    }
    return never
  }
  return never
}
/* compiles to:
 * export type nonOptionalKeyFromEntry<k extends PropertyKey, v, $, args> =
 *   preparseKey<k> extends infer parsedKey
 *     ? parsedKey extends PreparsedEntryKey<'required'>
 *       ? [v] extends [OptionalPropertyDefinition]
 *         ? [v] extends [anyOrNever] ? parsedKey['normalized'] : never
 *         : parsedKey['normalized']
 *     : parsedKey extends PreparsedEntryKey<'index'>
 *       ? inferDefinition<parsedKey['normalized'], $, args> & Key
 *     : never
 *     : never
 */

// ✓ optionalKeyFromEntry: verified type-identical to the original
/* @scripttype preserveParamNames */
export function optionalKeyFromEntry(k: PropertyKey, v): any {
  const m1 = matches<Hole<"parsedKey">>(preparseKey(k))
  if (m1) {
    if (matches<PreparsedEntryKey<"optional">>(m1.parsedKey)) {
      return m1.parsedKey['normalized']
    }
    if (matches<OptionalPropertyDefinition>(v)) {
      return k
    }
    return never
  }
  return never
}
/* compiles to:
 * export type optionalKeyFromEntry<k extends PropertyKey, v> =
 *   preparseKey<k> extends infer parsedKey
 *     ? parsedKey extends PreparsedEntryKey<'optional'> ? parsedKey['normalized']
 *     : v extends OptionalPropertyDefinition ? k
 *     : never
 *     : never
 */

// ✓ normalizedKeyKind: verified type-identical to the original
/* @scripttype preserveParamNames */
export function normalizedKeyKind(kind: EntryKeyKind): any {
  if (matches<"index">(kind)) {
    return string
  }
  return Key
}
/* compiles to:
 * export type normalizedKeyKind<kind extends EntryKeyKind> = kind extends 'index' ? string : Key
 */

// ✓ PreparsedEntryKey: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EntryKeyKind} kind
 * @param {normalizedKeyKind<typeof kind>} normalized
 */
export function PreparsedEntryKey(kind = EntryKeyKind, normalized = normalizedKeyKind(kind)): any {
  return { kind: kind, normalized: normalized }
}
/* compiles to:
 * export type PreparsedEntryKey<
 *   kind extends EntryKeyKind = EntryKeyKind,
 *   normalized extends normalizedKeyKind<kind> = normalizedKeyKind<kind>
 * > =
 *   { kind: kind; normalized: normalized }
 */

// ✓ PreparsedSpecialKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PreparsedSpecialKey(kind: SpecialKeyKind = SpecialKeyKind): any {
  return { kind: kind }
}
/* compiles to:
 * export type PreparsedSpecialKey<kind extends SpecialKeyKind = SpecialKeyKind> = { kind: kind }
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(t: PreparsedKey): any {
  return t
}
/* compiles to:
 * export type from<t extends PreparsedKey> = t
 */

// ✓ IndexKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexKey(def: string = string): any {
  return `[${def}]`
}
/* compiles to:
 * export type IndexKey<def extends string = string> = `[${def}]`
 */

// ✗ preparseKey: does not compile yet
//   Cannot find name 'm2'.
/* @scripttype preserveParamNames */
export function preparseKey(k): any {
  if (typeof k === 'symbol') {
    return t<PreparsedKey.from<{ kind: "required"; normalized: typeof k; }>>()
  }
  const m1 = matches<`${Hole<"inner">}?`>(k)
  if (m1) {
    const m2 = matches<`${Hole<"baseName">}${Backslash}`>(m1.inner)
    if (m2) {
      return t<PreparsedKey.from<{ kind: "required"; normalized: `${typeof m2.baseName}?`; }>>()
    }
    return t<PreparsedKey.from<{ kind: "optional"; normalized: typeof m1.inner; }>>()
  }
  if (matches<"+">(k)) {
    return { kind: 'undeclared' }
  }
  if (matches<"...">(k)) {
    return { kind: 'spread' }
  }
  const m3 = matches<`${Backslash}${Hole<"escapedMeta", MetaKey>}`>(k)
  if (m3) {
    return t<PreparsedKey.from<{ kind: "required"; normalized: typeof m3.escapedMeta; }>>()
  }
  const m4 = matches<IndexKey<Hole<"def">>>(k)
  if (m4) {
    return t<PreparsedKey.from<{ kind: "index"; normalized: typeof m4.def; }>>()
  }
  return t<PreparsedKey.from<{ kind: "required"; normalized: typeof k extends (`${Backslash}${Hole<"escapedIndexKey", IndexKey>}`) ? escapedIndexKey : typeof k extends Key ? typeof k : `${typeof k & number}`; }>>()
}
/* compiles to:
 * export type preparseKey<k> =
 *   k extends symbol ? PreparsedKey.from<{ kind: "required"; normalized: k; }>
 *   : k extends `${infer inner}?`
 *     ? inner extends `${string}${Backslash}`
 *       ? PreparsedKey.from<{ kind: "required"; normalized: `${typeof m2.baseName}?`; }>
 *       : PreparsedKey.from<{ kind: "optional"; normalized: typeof m1.inner; }>
 *   : k extends '+' ? { kind: 'undeclared' }
 *   : k extends '...' ? { kind: 'spread' }
 *   : k extends `${Backslash}${MetaKey}`
 *     ? PreparsedKey.from<{ kind: "required"; normalized: typeof m3.escapedMeta; }>
 *   : k extends IndexKey<any> ? PreparsedKey.from<{ kind: "index"; normalized: typeof m4.def; }>
 *   : PreparsedKey.from<
 *     { kind: "required"; normalized: k extends (`${Backslash}${infer escapedIndexKey extends IndexKey}`) ? escapedIndexKey : k extends Key ? k : `${k & number}`; }
 *   >
 */

// ✓ writeInvalidSpreadTypeMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeInvalidSpreadTypeMessage(def: string): any {
  return `Spread operand must resolve to an object literal type (was ${def})`
}
/* compiles to:
 * export type writeInvalidSpreadTypeMessage<def extends string> = `Spread operand must resolve to an object literal type (was ${def})`
 */

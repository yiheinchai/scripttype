/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/reduce/static.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 23 type-level aliases: 23 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { LimitLiteral } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/attributes.js'
import type { BranchOperator, InvertedComparators, MaxComparator, MinComparator, StringifiablePrefixOperator } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/reduce/shared.js'
import type { StaticState } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/reduce/static.js'
import type { FinalizingLookahead } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/shift/tokens.js'
import type { Completion, ErrorMessage } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/errors.js'
import type { defined } from '../../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
declare namespace m1 {
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type justScanned<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type stack<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type top<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace s {
  export type error<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type from<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BranchOperator: any
declare const BranchState: any
declare const Comparator: any
declare const Completion: any
declare const ErrorMessage: any
declare const FinalizingLookahead: any
declare const InvertedComparators: any
declare const LimitLiteral: any
declare const MaxComparator: any
declare const MinComparator: any
declare const StaticState: any
declare const StringifiablePrefixOperator: any
declare const defined: any
declare const initialBranches: any
declare const writeMultipleLeftBoundsMessage: any
declare const writeOpenRangeMessage: any
declare const writeUnclosedGroupMessage: any
declare const writeUnmatchedGroupCloseMessage: any
declare const writeUnpairableComparatorMessage: any
type BranchState<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Comparator<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type initialBranches<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type popGroup<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type updateScanned<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeMultipleLeftBoundsMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeOpenRangeMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnclosedGroupMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnmatchedGroupCloseMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeUnpairableComparatorMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ initialize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function initialize(def: string): any {
  return from({ root: Undefined, branches: initialBranches, groups: [], finalizer: Undefined, scanned: '', unscanned: def })
}
/* compiles to:
 * export type initialize<def extends string> = from<
 *   {
 *     root: undefined
 *     branches: initialBranches
 *     groups: []
 *     finalizer: undefined
 *     scanned: ''
 *     unscanned: def
 *   }
 * >
 */

// ✓ error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function error(message: string): any {
  return from({ root: ErrorMessage(message), branches: initialBranches, groups: [], finalizer: ErrorMessage(message), scanned: '', unscanned: '' })
}
/* compiles to:
 * export type error<message extends string> = from<
 *   {
 *     root: ErrorMessage<message>
 *     branches: initialBranches
 *     groups: []
 *     finalizer: ErrorMessage<message>
 *     scanned: ''
 *     unscanned: ''
 *   }
 * >
 */

// ✓ completion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function completion(text: string): any {
  return from({ root: Completion(text), branches: initialBranches, groups: [], finalizer: Completion(text), scanned: '', unscanned: '' })
}
/* compiles to:
 * export type completion<text extends string> = from<
 *   {
 *     root: Completion<text>
 *     branches: initialBranches
 *     groups: []
 *     finalizer: Completion<text>
 *     scanned: ''
 *     unscanned: ''
 *   }
 * >
 */

// ✓ updateScanned: verified type-identical to the original
/* @scripttype preserveParamNames */
export function updateScanned(previousScanned: string, previousUnscanned: string, updatedUnscanned: string): any {
  const m1 = matches<`${Hole<"justScanned">}${typeof updatedUnscanned}`>(previousUnscanned)
  if (m1) {
    return `${previousScanned}${m1.justScanned}`
  }
  return previousScanned
}
/* compiles to:
 * export type updateScanned<
 *   previousScanned extends string,
 *   previousUnscanned extends string,
 *   updatedUnscanned extends string
 * > =
 *   previousUnscanned extends `${infer justScanned}${updatedUnscanned}`
 *     ? `${previousScanned}${justScanned}`
 *     : previousScanned
 */

// ✓ setRoot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function setRoot(s: StaticState, root, unscanned: string = s['unscanned']): any {
  return from({ root: root, branches: s['branches'], groups: s['groups'], finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
}
/* compiles to:
 * export type setRoot<s extends StaticState, root, unscanned extends string = s['unscanned']> = from<
 *   {
 *     root: root
 *     branches: s['branches']
 *     groups: s['groups']
 *     finalizer: s['finalizer']
 *     scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *     unscanned: unscanned
 *   }
 * >
 */

// ✓ addPrefix: verified type-identical to the original
/* @scripttype preserveParamNames */
export function addPrefix(s: StaticState, prefix: StringifiablePrefixOperator, unscanned: string = s['unscanned']): any {
  return from({ root: s['root'], branches: { prefixes: [...s['branches']['prefixes'], prefix], leftBound: s['branches']['leftBound'], intersection: s['branches']['intersection'], pipe: s['branches']['pipe'], union: s['branches']['union'] }, groups: s['groups'], finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
}
/* compiles to:
 * export type addPrefix<
 *   s extends StaticState,
 *   prefix extends StringifiablePrefixOperator,
 *   unscanned extends string = s['unscanned']
 * > =
 *   from<
 *     {
 *       root: s['root']
 *       branches: {
 *         prefixes: [...s['branches']['prefixes'], prefix]
 *         leftBound: s['branches']['leftBound']
 *         intersection: s['branches']['intersection']
 *         pipe: s['branches']['pipe']
 *         union: s['branches']['union']
 *       }
 *       groups: s['groups']
 *       finalizer: s['finalizer']
 *       scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *       unscanned: unscanned
 *     }
 *   >
 */

// ✓ reduceBranch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function reduceBranch(s: StaticState, token: BranchOperator, unscanned: string): any {
  if (matches<{}>(s['branches']['leftBound'])) {
    return openRangeError(s['branches']['leftBound'])
  }
  return from({ root: Undefined, branches: { prefixes: [], leftBound: Undefined, intersection: matches<"&">(token) ? mergeToIntersection(s) : Undefined, union: matches<"|">(token) ? mergeToUnion(s) : (matches<"|>">(token) ? Undefined : s['branches']['union']), pipe: matches<"|>">(token) ? mergeToPipe(s) : s['branches']['pipe'] }, groups: s['groups'], finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
}
/* compiles to:
 * export type reduceBranch<
 *   s extends StaticState,
 *   token extends BranchOperator,
 *   unscanned extends string
 * > =
 *   s['branches']['leftBound'] extends {}
 *     ? openRangeError<s['branches']['leftBound']>
 *     : from<
 *       {
 *         root: undefined
 *         branches: {
 *           prefixes: []
 *           leftBound: undefined
 *           intersection: token extends '&' ? mergeToIntersection<s> : undefined
 *           union: token extends '|' ? mergeToUnion<s>
 *           : token extends '|>' ? undefined
 *           : s['branches']['union']
 *           pipe: token extends '|>' ? mergeToPipe<s> : s['branches']['pipe']
 *         }
 *         groups: s['groups']
 *         finalizer: s['finalizer']
 *         scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *         unscanned: unscanned
 *       }
 *     >
 */

// ✓ reduceLeftBound: verified type-identical to the original
/* @scripttype preserveParamNames */
export function reduceLeftBound(s: StaticState, limit: LimitLiteral, comparator: Comparator, unscanned: string): any {
  if (matches<"<" | "<=">(comparator)) {
    if (matches<{}>(s['branches']['leftBound'])) {
      return t<s.error<writeMultipleLeftBoundsMessage<(typeof s)["branches"]["leftBound"]["limit"], (typeof s)["branches"]["leftBound"]["comparator"], typeof limit, InvertedComparators[typeof comparator]>>>()
    }
    return from({ root: Undefined, branches: { prefixes: s['branches']['prefixes'], leftBound: { limit: limit, comparator: InvertedComparators[comparator] }, intersection: s['branches']['intersection'], pipe: s['branches']['pipe'], union: s['branches']['union'] }, groups: s['groups'], finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
  }
  return t<s.error<writeUnpairableComparatorMessage<typeof comparator>>>()
}
/* compiles to:
 * export type reduceLeftBound<
 *   s extends StaticState,
 *   limit extends LimitLiteral,
 *   comparator extends Comparator,
 *   unscanned extends string
 * > =
 *   comparator extends '<' | '<='
 *     ? s['branches']['leftBound'] extends {}
 *       ? s.error<
 *         writeMultipleLeftBoundsMessage<
 *           s['branches']['leftBound']['limit'],
 *           s['branches']['leftBound']['comparator'],
 *           limit,
 *           InvertedComparators[comparator]
 *         >
 *       >
 *       : from<
 *         {
 *           root: undefined
 *           branches: {
 *             prefixes: s['branches']['prefixes']
 *             leftBound: { limit: limit; comparator: InvertedComparators[comparator] }
 *             intersection: s['branches']['intersection']
 *             pipe: s['branches']['pipe']
 *             union: s['branches']['union']
 *           }
 *           groups: s['groups']
 *           finalizer: s['finalizer']
 *           scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *           unscanned: unscanned
 *         }
 *       >
 *     : s.error<writeUnpairableComparatorMessage<comparator>>
 */

// ✓ reduceRange: verified type-identical to the original
/* @scripttype preserveParamNames */
export function reduceRange(s: StaticState, minLimit: LimitLiteral, minComparator: MinComparator, maxComparator: MaxComparator, maxLimit: LimitLiteral, unscanned: string): any {
  return t<s.from<{ root: [ typeof minLimit, typeof minComparator, [ (typeof s)["root"], typeof maxComparator, typeof maxLimit ] ]; branches: { prefixes: (typeof s)["branches"]["prefixes"]; leftBound: undefined; intersection: (typeof s)["branches"]["intersection"]; pipe: (typeof s)["branches"]["pipe"]; union: (typeof s)["branches"]["union"]; }; groups: (typeof s)["groups"]; finalizer: (typeof s)["finalizer"]; scanned: updateScanned<(typeof s)["scanned"], (typeof s)["unscanned"], typeof unscanned>; unscanned: typeof unscanned; }>>()
}
/* compiles to:
 * export type reduceRange<
 *   s extends StaticState,
 *   minLimit extends LimitLiteral,
 *   minComparator extends MinComparator,
 *   maxComparator extends MaxComparator,
 *   maxLimit extends LimitLiteral,
 *   unscanned extends string
 * > =
 *   s.from<
 *     { root: [ minLimit, minComparator, [ (s)["root"], maxComparator, maxLimit ] ]; branches: { prefixes: (s)["branches"]["prefixes"]; leftBound: undefined; intersection: (s)["branches"]["intersection"]; pipe: (s)["branches"]["pipe"]; union: (s)["branches"]["union"]; }; groups: (s)["groups"]; finalizer: (s)["finalizer"]; scanned: updateScanned<(s)["scanned"], (s)["unscanned"], unscanned>; unscanned: unscanned; }
 *   >
 */

// ✓ reduceSingleBound: verified type-identical to the original
/* @scripttype preserveParamNames */
export function reduceSingleBound(s: StaticState, comparator: Comparator, limit: number | string, unscanned: string): any {
  return t<s.from<{ root: [ (typeof s)["root"], typeof comparator, typeof limit ]; branches: { prefixes: (typeof s)["branches"]["prefixes"]; leftBound: undefined; intersection: (typeof s)["branches"]["intersection"]; pipe: (typeof s)["branches"]["pipe"]; union: (typeof s)["branches"]["union"]; }; groups: (typeof s)["groups"]; finalizer: (typeof s)["finalizer"]; scanned: updateScanned<(typeof s)["scanned"], (typeof s)["unscanned"], typeof unscanned>; unscanned: typeof unscanned; }>>()
}
/* compiles to:
 * export type reduceSingleBound<
 *   s extends StaticState,
 *   comparator extends Comparator,
 *   limit extends number | string,
 *   unscanned extends string
 * > =
 *   s.from<
 *     { root: [ (s)["root"], comparator, limit ]; branches: { prefixes: (s)["branches"]["prefixes"]; leftBound: undefined; intersection: (s)["branches"]["intersection"]; pipe: (s)["branches"]["pipe"]; union: (s)["branches"]["union"]; }; groups: (s)["groups"]; finalizer: (s)["finalizer"]; scanned: updateScanned<(s)["scanned"], (s)["unscanned"], unscanned>; unscanned: unscanned; }
 *   >
 */

// ✓ mergeToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mergeToIntersection(s: StaticState): any {
  if (typeof s['branches']['intersection'] === 'undefined') {
    return mergePrefixes(s)
  }
  return [s['branches']['intersection'], '&', mergePrefixes(s)]
}
/* compiles to:
 * export type mergeToIntersection<s extends StaticState> =
 *   s['branches']['intersection'] extends undefined
 *     ? mergePrefixes<s>
 *     : [s['branches']['intersection'], '&', mergePrefixes<s>]
 */

// ✓ mergeToUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mergeToUnion(s: StaticState): any {
  if (typeof s['branches']['union'] === 'undefined') {
    return mergeToIntersection(s)
  }
  return [s['branches']['union'], '|', mergeToIntersection(s)]
}
/* compiles to:
 * export type mergeToUnion<s extends StaticState> =
 *   s['branches']['union'] extends undefined
 *     ? mergeToIntersection<s>
 *     : [s['branches']['union'], '|', mergeToIntersection<s>]
 */

// ✓ mergeToPipe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mergeToPipe(s: StaticState): any {
  if (typeof s['branches']['pipe'] === 'undefined') {
    return mergeToUnion(s)
  }
  return [s['branches']['pipe'], '|>', mergeToUnion(s)]
}
/* compiles to:
 * export type mergeToPipe<s extends StaticState> =
 *   s['branches']['pipe'] extends undefined
 *     ? mergeToUnion<s>
 *     : [s['branches']['pipe'], '|>', mergeToUnion<s>]
 */

// ✓ mergePrefixes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mergePrefixes(s: StaticState, remaining: unknown[] = s['branches']['prefixes']): any {
  const m1 = matches<[ Hole<"head">, ...Hole<"tail"> ]>(remaining)
  if (m1) {
    return [m1.head, mergePrefixes(s, m1.tail)]
  }
  return s['root']
}
/* compiles to:
 * export type mergePrefixes<
 *   s extends StaticState,
 *   remaining extends unknown[] = s['branches']['prefixes']
 * > =
 *   remaining extends [infer head, ...infer tail] ? [head, mergePrefixes<s, tail>] : s['root']
 */

// ✓ popGroup: verified type-identical to the original
/* @scripttype preserveParamNames */
export function popGroup(stack: BranchState[], top: BranchState): any {
  return [...stack, top]
}
/* compiles to:
 * export type popGroup<stack extends BranchState[], top extends BranchState> = [...stack, top]
 */

// ✓ finalizeGroup: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalizeGroup(s: StaticState, unscanned: string): any {
  if (matches<{}>(s['branches']['leftBound'])) {
    return openRangeError(s['branches']['leftBound'])
  }
  const m1 = matches<popGroup<Hole<"stack">, Hole<"top">>>(s['groups'])
  if (m1) {
    return from({ groups: m1.stack, branches: m1.top, root: mergeToPipe(s), finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
  }
  return t<s.error<writeUnmatchedGroupCloseMessage<")", typeof unscanned>>>()
}
/* compiles to:
 * export type finalizeGroup<s extends StaticState, unscanned extends string> =
 *   s['branches']['leftBound'] extends {} ? openRangeError<s['branches']['leftBound']>
 *   : s['groups'] extends popGroup<infer stack, infer top>
 *     ? from<
 *       {
 *         groups: stack
 *         branches: top
 *         root: mergeToPipe<s>
 *         finalizer: s['finalizer']
 *         scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *         unscanned: unscanned
 *       }
 *     >
 *   : s.error<writeUnmatchedGroupCloseMessage<')', unscanned>>
 */

// ✓ reduceGroupOpen: verified type-identical to the original
/* @scripttype preserveParamNames */
export function reduceGroupOpen(s: StaticState, unscanned: string): any {
  return from({ groups: [...s['groups'], s['branches']], branches: initialBranches, root: Undefined, finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
}
/* compiles to:
 * export type reduceGroupOpen<s extends StaticState, unscanned extends string> = from<
 *   {
 *     groups: [...s['groups'], s['branches']]
 *     branches: initialBranches
 *     root: undefined
 *     finalizer: s['finalizer']
 *     scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *     unscanned: unscanned
 *   }
 * >
 */

// ✓ finalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function finalize(s: StaticState, finalizer: FinalizingLookahead): any {
  if (matches<[ ]>(s['groups'])) {
    if (matches<{}>(s['branches']['leftBound'])) {
      return openRangeError(s['branches']['leftBound'])
    }
    return from({ root: mergeToPipe(s), groups: s['groups'], branches: initialBranches, finalizer: finalizer, scanned: s['scanned'], unscanned: s['unscanned'] })
  }
  return t<s.error<writeUnclosedGroupMessage<")">>>()
}
/* compiles to:
 * export type finalize<s extends StaticState, finalizer extends FinalizingLookahead> =
 *   s['groups'] extends []
 *     ? s['branches']['leftBound'] extends {}
 *       ? openRangeError<s['branches']['leftBound']>
 *       : from<
 *         {
 *           root: mergeToPipe<s>
 *           groups: s['groups']
 *           branches: initialBranches
 *           finalizer: finalizer
 *           scanned: s['scanned']
 *           unscanned: s['unscanned']
 *         }
 *       >
 *     : s.error<writeUnclosedGroupMessage<')'>>
 */

// ✓ openRangeError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function openRangeError(range: defined<BranchState["leftBound"]>): any {
  return t<s.error<writeOpenRangeMessage<(typeof range)["limit"], (typeof range)["comparator"]>>>()
}
/* compiles to:
 * export type openRangeError<range extends defined<BranchState['leftBound']>> = s.error<
 *   writeOpenRangeMessage<range['limit'], range['comparator']>
 * >
 */

// ✓ previousOperator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function previousOperator(s: StaticState): any {
  if (matches<{}>(s['branches']['leftBound'])) {
    return s['branches']['leftBound']['comparator']
  }
  const m1 = matches<([ ...unknown[], Hole<"tail", string> ])>(s['branches']['prefixes'])
  if (m1) {
    return m1.tail
  }
  if (matches<{}>(s['branches']['intersection'])) {
    return '&'
  }
  if (matches<{}>(s['branches']['union'])) {
    return '|'
  }
  return Undefined
}
/* compiles to:
 * export type previousOperator<s extends StaticState> =
 *   s['branches']['leftBound'] extends {} ? s['branches']['leftBound']['comparator']
 *   : s['branches']['prefixes'] extends [...unknown[], infer tail extends string] ? tail
 *   : s['branches']['intersection'] extends {} ? '&'
 *   : s['branches']['union'] extends {} ? '|'
 *   : undefined
 */

// ✓ scanTo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function scanTo(s: StaticState, unscanned: string): any {
  return from({ root: s['root'], branches: s['branches'], groups: s['groups'], finalizer: s['finalizer'], scanned: updateScanned(s['scanned'], s['unscanned'], unscanned), unscanned: unscanned })
}
/* compiles to:
 * export type scanTo<s extends StaticState, unscanned extends string> = from<
 *   {
 *     root: s['root']
 *     branches: s['branches']
 *     groups: s['groups']
 *     finalizer: s['finalizer']
 *     scanned: updateScanned<s['scanned'], s['unscanned'], unscanned>
 *     unscanned: unscanned
 *   }
 * >
 */

// ✓ from: verified type-identical to the original
/* @scripttype preserveParamNames */
export function from(s: StaticState): any {
  return s
}
/* compiles to:
 * export type from<s extends StaticState> = s
 */

// ✓ branchesFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function branchesFrom(b: BranchState): any {
  return b
}
/* compiles to:
 * export type branchesFrom<b extends BranchState> = b
 */

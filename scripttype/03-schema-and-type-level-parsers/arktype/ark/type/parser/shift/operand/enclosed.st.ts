/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/shift/operand/enclosed.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 4 covered, 1 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const EnclosingQuote: any
declare const EnclosingRegexToken: any
declare const EnclosingStartToken: any
declare const EnclosingTokens: any
declare const ErrorMessage: any
declare const InferredAst: any
declare const Out: any
declare const Regex: any
declare const Scanner: any
declare const StaticState: any
declare const enclosingCharDescriptions: any
declare const regex: any
declare const shift: any
declare const shiftResult: any
type EnclosingQuote<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnclosingRegexToken<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnclosingStartToken<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnclosingTokens<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InferredAst<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Out<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Regex<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Scanner<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StaticState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type enclosingCharDescriptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type regex<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shift<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shiftResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ StringLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StringLiteral(contents: string = string) {
  return DoubleQuotedStringLiteral(contents) | SingleQuotedStringLiteral(contents)
}
/* compiles to:
 * export type StringLiteral<contents extends string = string> = DoubleQuotedStringLiteral<contents> | SingleQuotedStringLiteral<contents>
 */

// ✓ DoubleQuotedStringLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DoubleQuotedStringLiteral(contents: string = string) {
  return `"${contents}"`
}
/* compiles to:
 * export type DoubleQuotedStringLiteral<contents extends string = string> = `"${contents}"`
 */

// ✓ SingleQuotedStringLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SingleQuotedStringLiteral(contents: string = string) {
  return `'${contents}'`
}
/* compiles to:
 * export type SingleQuotedStringLiteral<contents extends string = string> = `'${contents}'`
 */

// ✗ parseEnclosed: the ScriptType does not itself typecheck as TypeScript
//   parseEnclosed.st.ts(3:22) TS2702: 'Scanner' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function parseEnclosed(s: StaticState, enclosingStart: EnclosingStartToken, unscanned: string) {
  const m1 = matches<Scanner.shiftResult<Hole<"scanned">, Hole<"nextUnscanned">>>(Scanner.shiftUntilEscapable(unscanned, EnclosingTokens[enclosingStart], ''))
  if (m1) {
    return _parseEnclosed(s, enclosingStart, m1.scanned, m1.nextUnscanned)
  }
  return never
}
/* compiles to:
 * export type parseEnclosed<s extends StaticState, enclosingStart extends EnclosingStartToken, unscanned extends string> = Scanner.shiftUntilEscapable<unscanned, EnclosingTokens[enclosingStart], ''> extends Scanner.shiftResult<infer scanned, infer nextUnscanned> ? _parseEnclosed<s, enclosingStart, scanned, nextUnscanned> : never
 */

// ✗ _parseEnclosed: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function _parseEnclosed(s: StaticState, enclosingStart: EnclosingStartToken, scanned: string, nextUnscanned: string, def: string = `${enclosingStart}${scanned}${EnclosingTokens[enclosingStart]}`) {
  if (matches<"">(nextUnscanned)) {
    return s.error(writeUnterminatedEnclosedMessage(scanned, enclosingStart))
  }
  if (matches<EnclosingQuote>(enclosingStart)) {
    const m1 = matches<Scanner.shift<string, Hole<"unscanned">>>(nextUnscanned)
    return s.setRoot(s, InferredAst(scanned, def), m1 ? m1.unscanned : '')
  }
  if (matches<EnclosingRegexToken>(enclosingStart)) {
    const m2 = matches<Hole<"r">>(regex.parse(scanned))
    if (m2) {
      if (matches<Regex>(m2.r)) {
        const m3 = matches<Scanner.shift<string, Hole<"unscanned">>>(nextUnscanned)
        return s.setRoot(s, InferredAst(matches<"/">(enclosingStart) ? m2.r['infer'] : fnType([m2.r['infer']], Out(m2.r['inferExecArray'])), def), m3 ? m3.unscanned : '')
      }
      const m4 = matches<ErrorMessage<Hole<"e">>>(m2.r)
      if (m4) {
        return s.error(m4.e)
      }
      return never
    }
    return never
  }
  const m5 = matches<Scanner.shift<string, Hole<"unscanned">>>(nextUnscanned)
  return s.setRoot(s, InferredAst(t<Date>(), def), m5 ? m5.unscanned : '')
}

// ✓ writeUnterminatedEnclosedMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeUnterminatedEnclosedMessage(fragment: string, enclosingStart: EnclosingStartToken) {
  return `${enclosingStart}${fragment} requires a closing ${enclosingCharDescriptions[EnclosingTokens[enclosingStart]]}`
}
/* compiles to:
 * export type writeUnterminatedEnclosedMessage<fragment extends string, enclosingStart extends EnclosingStartToken> = `${enclosingStart}${fragment} requires a closing ${enclosingCharDescriptions[EnclosingTokens[enclosingStart]]}`
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/shift/operator/bounds.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 compile-error, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BoundExpressionKind: any
declare const Comparator: any
declare const ComparatorStartChar: any
declare const DateLiteral: any
declare const InferredAst: any
declare const InvertedComparators: any
declare const MaxComparator: any
declare const OneCharComparator: any
declare const Scanner: any
declare const StaticState: any
declare const astToString: any
declare const parseOperand: any
declare const shiftResult: any
declare const writeUnpairableComparatorMessage: any
type BoundExpressionKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Comparator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ComparatorStartChar<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DateLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InferredAst<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InvertedComparators<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MaxComparator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OneCharComparator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Scanner<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StaticState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type astToString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type parseOperand<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shiftResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type writeUnpairableComparatorMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ parseBound: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseBound(s: StaticState, start: ComparatorStartChar, unscanned: string, $, args) {
  const m1 = matches<Hole<"shiftResultOrError">>(shiftComparator(start, unscanned))
  if (m1) {
    const m2 = matches<(Scanner.shiftResult<Hole<"comparator", Comparator>, Hole<"nextUnscanned">>)>(m1.shiftResultOrError)
    if (m2) {
      const m3 = matches<(InferredAst<Date | number, `${Hole<"limit", number | DateLiteral>}`>)>(s['root'])
      if (m3) {
        return s.reduceLeftBound(s, m3.limit, m2.comparator, m2.nextUnscanned)
      }
      return parseRightBound(s.scanTo(s, m2.nextUnscanned), m2.comparator, $, args)
    }
    return m1.shiftResultOrError
  }
  return never
}

// ✓ shiftComparator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function shiftComparator(start: ComparatorStartChar, unscanned: string) {
  const m1 = matches<`=${Hole<"nextUnscanned">}`>(unscanned)
  if (m1) {
    return [`${start}=`, m1.nextUnscanned]
  }
  return [merge(start, OneCharComparator), unscanned]
}
/* compiles to:
 * export type shiftComparator<start extends ComparatorStartChar, unscanned extends string> = unscanned extends `=${infer nextUnscanned}` ? [`${start}=`, nextUnscanned] : [start & OneCharComparator, unscanned]
 */

// ✗ parseRightBound: does not compile yet
//   only direct calls are supported
/* @scripttype preserveParamNames */
export function parseRightBound(s: StaticState, comparator: Comparator, $, args) {
  const m1 = matches<Hole<"nextState", StaticState>>(parseOperand(s, $, args))
  if (m1) {
    const m2 = matches<(InferredAst<unknown, `${Hole<"limit", number | DateLiteral>}`>)>(m1.nextState['root'])
    if (m2) {
      if (matches<{}>(s['branches']['leftBound'])) {
        if (matches<MaxComparator>(comparator)) {
          return s.reduceRange(s, s['branches']['leftBound']['limit'], s['branches']['leftBound']['comparator'], comparator, m2.limit, m1.nextState['unscanned'])
        }
        return s.error(writeUnpairableComparatorMessage(comparator))
      }
      return s.reduceSingleBound(s, comparator, m2.limit, m1.nextState['unscanned'])
    }
    return s.error(writeInvalidLimitMessage(comparator, astToString(m1.nextState['root']), 'right'))
  }
  return never
}

// ✓ writeInvalidLimitMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeInvalidLimitMessage(comparator: Comparator, limit: string | number, boundKind: BoundExpressionKind) {
  return `Comparator ${matches<"left">(boundKind) ? InvertedComparators[comparator] : comparator} must be ${matches<"left">(boundKind) ? 'preceded' : 'followed'} by a corresponding literal (was ${limit})`
}
/* compiles to:
 * export type writeInvalidLimitMessage<comparator extends Comparator, limit extends string | number, boundKind extends BoundExpressionKind> = `Comparator ${boundKind extends 'left' ? InvertedComparators[comparator] : comparator} must be ${boundKind extends 'left' ? 'preceded' : 'followed'} by a corresponding literal (was ${limit})`
 */

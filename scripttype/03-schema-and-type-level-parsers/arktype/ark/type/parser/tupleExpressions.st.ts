/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/tupleExpressions.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 10 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BaseCompletions: any
declare const BaseParseContext: any
declare const BaseRoot: any
declare const Constructor: any
declare const IndexOneOperator: any
declare const IndexZeroOperator: any
declare const Morph: any
declare const Out: any
declare const Predicate: any
declare const TupleExpression: any
declare const TuplePostfixOperator: any
declare const TypeMeta: any
declare const array: any
declare const defaultFor: any
declare const distill: any
declare const inferDefinition: any
declare const inferIntersection: any
declare const inferMorphOut: any
declare const inferPipe: any
declare const inferPredicate: any
declare const show: any
declare const type: any
declare const unwrapDefault: any
declare const validateDefinition: any
declare const withDefault: any
declare const writeMissingRightOperandMessage: any
type BaseCompletions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BaseParseContext<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BaseRoot<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constructor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexOneExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexOneOperator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexZeroExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexZeroOperator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Morph<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Out<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Predicate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TupleExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TuplePostfixOperator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypeMeta<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type defaultFor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type distill<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferIntersection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferMorphOut<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferPipe<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type inferPredicate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type show<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type type<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type unwrapDefault<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type validateDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type withDefault<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type writeMissingRightOperandMessage<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ maybeValidateTupleExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function maybeValidateTupleExpression(def: array, $, args) {
  if (matches<IndexZeroExpression>(def)) {
    return validatePrefixExpression(def, $, args)
  }
  if (matches<IndexOneExpression>(def)) {
    return validateIndexOneExpression(def, $, args)
  }
  if (matches<(readonly [ "", ...unknown[] ] | readonly [ unknown, "", ...unknown[] ])>(def)) {
    return asReadonly([matches<"">(def[0]) ? BaseCompletions($, args, anyOf(IndexZeroOperator, '...')) : def[0], matches<"">(def[1]) ? BaseCompletions($, args, anyOf(IndexOneOperator, '...')) : def[1]])
  }
  return Null
}
/* compiles to:
 * export type maybeValidateTupleExpression<def extends array, $, args> = def extends IndexZeroExpression ? validatePrefixExpression<def, $, args> : def extends IndexOneExpression ? validateIndexOneExpression<def, $, args> : def extends readonly ['', ...unknown[]] | readonly [unknown, '', ...unknown[]] ? readonly [def[0] extends '' ? BaseCompletions<$, args, IndexZeroOperator | '...'> : def[0], def[1] extends '' ? BaseCompletions<$, args, IndexOneOperator | '...'> : def[1]] : null
 */

// ✓ inferTupleExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferTupleExpression(def: TupleExpression, $, args) {
  if (matches<"[]">(def[1])) {
    return arrayOf(inferDefinition(def[0], $, args))
  }
  if (matches<"?">(def[1])) {
    return inferDefinition(def[0], $, args)
  }
  if (matches<"&">(def[1])) {
    return inferIntersection(inferDefinition(def[0], $, args), inferDefinition(def[2], $, args))
  }
  if (matches<"|">(def[1])) {
    return inferDefinition(def[0], $, args) | inferDefinition(def[2], $, args)
  }
  if (matches<":">(def[1])) {
    return inferPredicate(inferDefinition(def[0], $, args), def[2])
  }
  if (matches<"=>">(def[1])) {
    return parseMorph(def[0], def[2], $, args)
  }
  if (matches<"|>">(def[1])) {
    return parseTo(def[0], def[2], $, args)
  }
  if (matches<"=">(def[1])) {
    return withDefault(inferDefinition(def[0], $, args), unwrapDefault(def[2]))
  }
  if (matches<"@">(def[1])) {
    return inferDefinition(def[0], $, args)
  }
  const m1 = matches<readonly [ "===", ...Hole<"values"> ]>(def)
  if (m1) {
    return m1.values[number]
  }
  const m2 = matches<(readonly [ "instanceof", ...Hole<"constructors", Constructor[]> ])>(def)
  if (m2) {
    return InstanceType(m2.constructors[number])
  }
  if (matches<"keyof">(def[0])) {
    return inferKeyOfExpression(def[1], $, args)
  }
  return never
}
/* compiles to:
 * export type inferTupleExpression<def extends TupleExpression, $, args> = def[1] extends '[]' ? inferDefinition<def[0], $, args>[] : def[1] extends '?' ? inferDefinition<def[0], $, args> : def[1] extends '&' ? inferIntersection<inferDefinition<def[0], $, args>, inferDefinition<def[2], $, args>> : def[1] extends '|' ? inferDefinition<def[0], $, args> | inferDefinition<def[2], $, args> : def[1] extends ':' ? inferPredicate<inferDefinition<def[0], $, args>, def[2]> : def[1] extends '=>' ? parseMorph<def[0], def[2], $, args> : def[1] extends '|>' ? parseTo<def[0], def[2], $, args> : def[1] extends '=' ? withDefault<inferDefinition<def[0], $, args>, unwrapDefault<def[2]>> : def[1] extends '@' ? inferDefinition<def[0], $, args> : def extends readonly ['===', ...infer values] ? values[number] : def extends readonly ['instanceof', ...(infer constructors extends Constructor[])] ? InstanceType<constructors[number]> : def[0] extends 'keyof' ? inferKeyOfExpression<def[1], $, args> : never
 */

// ✓ validatePrefixExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validatePrefixExpression(def: IndexZeroExpression, $, args) {
  if (matches<1>(def['length'])) {
    return asReadonly([writeMissingRightOperandMessage(def[0])])
  }
  if (matches<"keyof">(def[0])) {
    return asReadonly([def[0], validateDefinition(def[1], $, args)])
  }
  if (matches<"===">(def[0])) {
    return asReadonly([def[0], ...arrayOf(unknown)])
  }
  if (matches<"instanceof">(def[0])) {
    return asReadonly([def[0], ...arrayOf(Constructor)])
  }
  return never
}
/* compiles to:
 * export type validatePrefixExpression<def extends IndexZeroExpression, $, args> = def['length'] extends 1 ? readonly [writeMissingRightOperandMessage<def[0]>] : def[0] extends 'keyof' ? readonly [def[0], validateDefinition<def[1], $, args>] : def[0] extends '===' ? readonly [def[0], ...unknown[]] : def[0] extends 'instanceof' ? readonly [def[0], ...Constructor[]] : never
 */

// ✗ validateIndexOneExpression: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function validateIndexOneExpression(def: IndexOneExpression, $, args) {
  if (matches<TuplePostfixOperator>(def[1])) {
    return asReadonly([validateDefinition(def[0], $, args), def[1]])
  }
  return asReadonly([validateDefinition(def[0], $, args), matches<2>(def['length']) ? writeMissingRightOperandMessage(def[1]) : def[1], matches<"|">(def[1]) ? validateDefinition(def[2], $, args) : (matches<"&">(def[1]) ? validateDefinition(def[2], $, args) : (matches<":">(def[1]) ? Predicate(type.infer.Out(def[0], $, args)) : (matches<"=>">(def[1]) ? Morph(type.infer.Out(def[0], $, args)) : (matches<"|>">(def[1]) ? validateDefinition(def[2], $, args) : (matches<"=">(def[1]) ? defaultFor(type.infer.In(def[0], $, args)) : (matches<"@">(def[1]) ? TypeMeta.MappableInput : validateDefinition(def[2], $, args))))))), ...matches<"@">(def[1]) ? [raw('NodeSelector?')] : []])
}

// ✓ inferKeyOfExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferKeyOfExpression(operandDef, $, args) {
  return show(keyof(inferDefinition(operandDef, $, args)))
}
/* compiles to:
 * export type inferKeyOfExpression<operandDef, $, args> = show<keyof inferDefinition<operandDef, $, args>>
 */

// ✓ parseTo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseTo(inDef, outDef, $, args) {
  return inferPipe(inferDefinition(inDef, $, args), inferDefinition(outDef, $, args))
}
/* compiles to:
 * export type parseTo<inDef, outDef, $, args> = inferPipe<inferDefinition<inDef, $, args>, inferDefinition<outDef, $, args>>
 */

// ✓ parseMorph: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseMorph(inDef, morph, $, args) {
  if (matches<Morph>(morph)) {
    const m1 = matches<Hole<"out">>(inferMorphOut(morph))
    if (m1) {
      return fnType([distill.In(inferDefinition(inDef, $, args))], Out(m1.out))
    }
    return never
  }
  return never
}
/* compiles to:
 * export type parseMorph<inDef, morph, $, args> = morph extends Morph ? inferMorphOut<morph> extends infer out ? (a0: distill.In<inferDefinition<inDef, $, args>>) => Out<out> : never : never
 */

// ✓ IndexOneExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexOneExpression(token: string = IndexOneOperator) {
  return asReadonly([unknown, token, ...arrayOf(unknown)])
}
/* compiles to:
 * export type IndexOneExpression<token extends string = IndexOneOperator> = readonly [unknown, token, ...unknown[]]
 */

// ✓ IndexOneParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexOneParser(token: string) {
  return fnType([IndexOneExpression(token), BaseParseContext], BaseRoot)
}
/* compiles to:
 * export type IndexOneParser<token extends string> = (a0: IndexOneExpression<token>, a1: BaseParseContext) => BaseRoot
 */

// ✓ IndexZeroParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexZeroParser(token: string) {
  return fnType([IndexZeroExpression(token), BaseParseContext], BaseRoot)
}
/* compiles to:
 * export type IndexZeroParser<token extends string> = (a0: IndexZeroExpression<token>, a1: BaseParseContext) => BaseRoot
 */

// ✓ IndexZeroExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexZeroExpression(token: string = IndexZeroOperator) {
  return asReadonly([token, ...arrayOf(unknown)])
}
/* compiles to:
 * export type IndexZeroExpression<token extends string = IndexZeroOperator> = readonly [token, ...unknown[]]
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/parser/tupleExpressions.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 10 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { NodeSelector } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/node.js'
import type { BaseParseContext } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/parse.js'
import type { unwrapDefault } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/shared/utils.js'
import type { Out, defaultFor, inferIntersection, inferMorphOut, inferPipe, inferPredicate, withDefault } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/attributes.js'
import type { inferDefinition, validateDefinition } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/definition.js'
import type { BaseCompletions } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/string.js'
import type { IndexOneOperator, IndexZeroOperator, TupleExpression, TuplePostfixOperator } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/tupleExpressions.js'
import type { array } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
import type { show } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
import type { Constructor } from '../../../../../../03-schema-and-type-level-parsers/arktype/ark/util/objectKinds.js'
declare namespace TypeMeta {
  export type MappableInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace distill {
  export type In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type out<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type values<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type constructors<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace type {
  export type infer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BaseCompletions: any
declare const BaseParseContext: any
declare const BaseRoot: any
declare const Constructor: any
declare const In: any
declare const IndexOneOperator: any
declare const IndexZeroOperator: any
declare const MappableInput: any
declare const Morph: any
declare const NodeSelector: any
declare const Out: any
declare const Predicate: any
declare const TupleExpression: any
declare const TuplePostfixOperator: any
declare const TypeMeta: any
declare const array: any
declare const defaultFor: any
declare const distill: any
declare const infer: any
declare const inferDefinition: any
declare const inferIntersection: any
declare const inferMorphOut: any
declare const inferPipe: any
declare const inferPredicate: any
declare const show: any
declare const type: any
declare const unwrapDefault: any
declare const validateDefinition: any
declare const withDefault: any
declare const writeMissingRightOperandMessage: any
type BaseRoot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IndexOneExpression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IndexZeroExpression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MappableInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Morph<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Predicate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TypeMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type distill<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type infer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeMissingRightOperandMessage<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ maybeValidateTupleExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function maybeValidateTupleExpression(def: array, $, args): any {
  if (matches<IndexZeroExpression>(def)) {
    return validatePrefixExpression(def, $, args)
  }
  if (matches<IndexOneExpression>(def)) {
    return validateIndexOneExpression(def, $, args)
  }
  if (matches<(readonly [ "", ...unknown[] ] | readonly [ unknown, "", ...unknown[] ])>(def)) {
    return asReadonly([matches<"">(def[0]) ? BaseCompletions($, args, anyOf(IndexZeroOperator, '...')) : def[0], matches<"">(def[1]) ? BaseCompletions($, args, anyOf(IndexOneOperator, '...')) : def[1]])
  }
  return Null
}
/* compiles to:
 * export type maybeValidateTupleExpression<def extends array, $, args> =
 *   def extends IndexZeroExpression ? validatePrefixExpression<def, $, args>
 *   : def extends IndexOneExpression ? validateIndexOneExpression<def, $, args>
 *   : def extends readonly ['', ...unknown[]] | readonly [unknown, '', ...unknown[]]
 *     ? readonly [
 *       def[0] extends '' ? BaseCompletions<$, args, IndexZeroOperator | '...'> : def[0],
 *       def[1] extends '' ? BaseCompletions<$, args, IndexOneOperator | '...'> : def[1]
 *     ]
 *   : null
 */

// ✓ inferTupleExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferTupleExpression(def: TupleExpression, $, args): any {
  if (matches<"[]">(def[1])) {
    return arrayOf(inferDefinition(def[0], $, args))
  }
  if (matches<"?">(def[1])) {
    return inferDefinition(def[0], $, args)
  }
  if (matches<"&">(def[1])) {
    return inferIntersection(inferDefinition(def[0], $, args), inferDefinition(def[2], $, args))
  }
  if (matches<"|">(def[1])) {
    return inferDefinition(def[0], $, args) | inferDefinition(def[2], $, args)
  }
  if (matches<":">(def[1])) {
    return inferPredicate(inferDefinition(def[0], $, args), def[2])
  }
  if (matches<"=>">(def[1])) {
    return parseMorph(def[0], def[2], $, args)
  }
  if (matches<"|>">(def[1])) {
    return parseTo(def[0], def[2], $, args)
  }
  if (matches<"=">(def[1])) {
    return withDefault(inferDefinition(def[0], $, args), unwrapDefault(def[2]))
  }
  if (matches<"@">(def[1])) {
    return inferDefinition(def[0], $, args)
  }
  const m1 = matches<readonly [ "===", ...Hole<"values"> ]>(def)
  if (m1) {
    return m1.values[number]
  }
  const m2 = matches<(readonly [ "instanceof", ...Hole<"constructors", Constructor[]> ])>(def)
  if (m2) {
    return InstanceType(m2.constructors[number])
  }
  if (matches<"keyof">(def[0])) {
    return inferKeyOfExpression(def[1], $, args)
  }
  return never
}
/* compiles to:
 * export type inferTupleExpression<def extends TupleExpression, $, args> =
 *   def[1] extends '[]' ? inferDefinition<def[0], $, args>[]
 *   : def[1] extends '?' ? inferDefinition<def[0], $, args>
 *   : def[1] extends '&'
 *     ? inferIntersection<inferDefinition<def[0], $, args>, inferDefinition<def[2], $, args>>
 *   : def[1] extends '|' ? inferDefinition<def[0], $, args> | inferDefinition<def[2], $, args>
 *   : def[1] extends ':' ? inferPredicate<inferDefinition<def[0], $, args>, def[2]>
 *   : def[1] extends '=>' ? parseMorph<def[0], def[2], $, args>
 *   : def[1] extends '|>' ? parseTo<def[0], def[2], $, args>
 *   : def[1] extends '=' ? withDefault<inferDefinition<def[0], $, args>, unwrapDefault<def[2]>>
 *   : def[1] extends '@' ? inferDefinition<def[0], $, args>
 *   : def extends readonly ['===', ...infer values] ? values[number]
 *   : def extends readonly ['instanceof', ...infer constructors extends Constructor[]]
 *     ? InstanceType<constructors[number]>
 *   : def[0] extends 'keyof' ? inferKeyOfExpression<def[1], $, args>
 *   : never
 */

// ✓ validatePrefixExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validatePrefixExpression(def: IndexZeroExpression, $, args): any {
  if (matches<1>(def['length'])) {
    return asReadonly([writeMissingRightOperandMessage(def[0])])
  }
  if (matches<"keyof">(def[0])) {
    return asReadonly([def[0], validateDefinition(def[1], $, args)])
  }
  if (matches<"===">(def[0])) {
    return asReadonly([def[0], ...arrayOf(unknown)])
  }
  if (matches<"instanceof">(def[0])) {
    return asReadonly([def[0], ...arrayOf(Constructor)])
  }
  return never
}
/* compiles to:
 * export type validatePrefixExpression<def extends IndexZeroExpression, $, args> =
 *   def['length'] extends 1 ? readonly [writeMissingRightOperandMessage<def[0]>]
 *   : def[0] extends 'keyof' ? readonly [def[0], validateDefinition<def[1], $, args>]
 *   : def[0] extends '===' ? readonly [def[0], ...unknown[]]
 *   : def[0] extends 'instanceof' ? readonly [def[0], ...Constructor[]]
 *   : never
 */

// ✗ validateIndexOneExpression: the ScriptType does not itself typecheck as TypeScript
//   validateIndexOneExpression.st.ts(6:307) TS2713: Cannot access 'infer.Out' because 'infer' is a type, but not a namespace. Did you mean to retrieve the type of t
/* @scripttype preserveParamNames */
export function validateIndexOneExpression(def: IndexOneExpression, $, args): any {
  if (matches<TuplePostfixOperator>(def[1])) {
    return asReadonly([validateDefinition(def[0], $, args), def[1]])
  }
  return asReadonly([validateDefinition(def[0], $, args), matches<2>(def['length']) ? writeMissingRightOperandMessage(def[1]) : def[1], matches<"|">(def[1]) ? validateDefinition(def[2], $, args) : (matches<"&">(def[1]) ? validateDefinition(def[2], $, args) : (matches<":">(def[1]) ? Predicate(t<type.infer.Out<(typeof def)[0], typeof $, typeof args>>()) : (matches<"=>">(def[1]) ? Morph(t<type.infer.Out<(typeof def)[0], typeof $, typeof args>>()) : (matches<"|>">(def[1]) ? validateDefinition(def[2], $, args) : (matches<"=">(def[1]) ? defaultFor(t<type.infer.In<(typeof def)[0], typeof $, typeof args>>()) : (matches<"@">(def[1]) ? t<TypeMeta.MappableInput>() : validateDefinition(def[2], $, args))))))), ...matches<"@">(def[1]) ? [optElem(NodeSelector)] : []])
}
/* compiles to:
 * export type validateIndexOneExpression<def extends IndexOneExpression, $, args> =
 *   def[1] extends TuplePostfixOperator
 *     ? readonly [validateDefinition<def[0], $, args>, def[1]]
 *     : readonly [
 *       validateDefinition<def[0], $, args>,
 *       def['length'] extends 2 ? writeMissingRightOperandMessage<def[1]> : def[1],
 *       def[1] extends '|' ? validateDefinition<def[2], $, args>
 *       : def[1] extends '&' ? validateDefinition<def[2], $, args>
 *       : def[1] extends ':' ? Predicate<type.infer.Out<def[0], $, args>>
 *       : def[1] extends '=>' ? Morph<type.infer.Out<def[0], $, args>>
 *       : def[1] extends '|>' ? validateDefinition<def[2], $, args>
 *       : def[1] extends '=' ? defaultFor<type.infer.In<def[0], $, args>>
 *       : def[1] extends '@' ? TypeMeta.MappableInput
 *       : validateDefinition<def[2], $, args>,
 *       ...(def[1] extends '@' ? [NodeSelector?] : [])
 *     ]
 */

// ✓ inferKeyOfExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferKeyOfExpression(operandDef, $, args): any {
  return show(keyof(inferDefinition(operandDef, $, args)))
}
/* compiles to:
 * export type inferKeyOfExpression<operandDef, $, args> = show<
 *   keyof inferDefinition<operandDef, $, args>
 * >
 */

// ✓ parseTo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseTo(inDef, outDef, $, args): any {
  return inferPipe(inferDefinition(inDef, $, args), inferDefinition(outDef, $, args))
}
/* compiles to:
 * export type parseTo<inDef, outDef, $, args> = inferPipe<
 *   inferDefinition<inDef, $, args>,
 *   inferDefinition<outDef, $, args>
 * >
 */

// ✓ parseMorph: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseMorph(inDef, morph, $, args): any {
  if (matches<Morph>(morph)) {
    const m1 = matches<Hole<"out">>(inferMorphOut(morph))
    if (m1) {
      return fnType([t<distill.In<inferDefinition<typeof inDef, typeof $, typeof args>>>()], Out(m1.out))
    }
    return never
  }
  return never
}
/* compiles to:
 * export type parseMorph<inDef, morph, $, args> =
 *   morph extends Morph
 *     ? inferMorphOut<morph> extends infer out
 *       ? (a0: distill.In<inferDefinition<inDef, $, args>>) => Out<out>
 *       : never
 *     : never
 */

// ✓ IndexOneExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexOneExpression(token: string = IndexOneOperator): any {
  return asReadonly([unknown, token, ...arrayOf(unknown)])
}
/* compiles to:
 * export type IndexOneExpression<token extends string = IndexOneOperator> =
 *   readonly [unknown, token, ...unknown[]]
 */

// ✓ IndexOneParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexOneParser(token: string): any {
  return fnType([IndexOneExpression(token), BaseParseContext], BaseRoot)
}
/* compiles to:
 * export type IndexOneParser<token extends string> =
 *   (a0: IndexOneExpression<token>, a1: BaseParseContext) => BaseRoot
 */

// ✓ IndexZeroParser: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexZeroParser(token: string): any {
  return fnType([IndexZeroExpression(token), BaseParseContext], BaseRoot)
}
/* compiles to:
 * export type IndexZeroParser<token extends string> =
 *   (a0: IndexZeroExpression<token>, a1: BaseParseContext) => BaseRoot
 */

// ✓ IndexZeroExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexZeroExpression(token: string = IndexZeroOperator): any {
  return asReadonly([token, ...arrayOf(unknown)])
}
/* compiles to:
 * export type IndexZeroExpression<token extends string = IndexZeroOperator> =
 *   readonly [token, ...unknown[]]
 */

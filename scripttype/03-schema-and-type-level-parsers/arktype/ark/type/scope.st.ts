/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/scope.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 12 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { GenericAst, GenericParamAst } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/generic.js'
import type { PrivateDeclaration } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/schema/scope.js'
import type { GenericDeclaration, ParameterString, baseGenericConstraints, parseGenericParams, parseValidGenericParams } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/type/generic.js'
import type { Submodule } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/type/module.js'
import type { DefAst, InferredAst } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/ast/infer.js'
import type { inferDefinition } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/type/parser/definition.js'
import type { UnparsedScope } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/type/scope.js'
import type { array } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
import type { ErrorType } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/errors.js'
import type { Brand, anyOrNever } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
declare namespace m1 {
  export type def<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type g<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type name<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type params<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type thunkReturn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type $<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type g<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type name<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type root<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type $<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace type {
  export type Any<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type validate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Any: any
declare const BoundModule: any
declare const Brand: any
declare const DefAst: any
declare const ErrorType: any
declare const GenericAst: any
declare const GenericDeclaration: any
declare const GenericParamAst: any
declare const InferredAst: any
declare const Module: any
declare const ParameterString: any
declare const PreparsedResolution: any
declare const PrivateDeclaration: any
declare const Submodule: any
declare const UnparsedScope: any
declare const anyOrNever: any
declare const arkKind: any
declare const array: any
declare const baseGenericConstraints: any
declare const inferDefinition: any
declare const noSuggest: any
declare const parseGenericParams: any
declare const parseValidGenericParams: any
declare const type: any
declare const writeDuplicateAliasError: any
type Any<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type BoundModule<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Def<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Module<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PreparsedResolution<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type arkKind<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type bootstrapAliases<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type noSuggest<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type type<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type writeDuplicateAliasError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ bindThis: verified type-identical to the original
/* @scripttype preserveParamNames */
export function bindThis(def): any {
  return { this: Def(def) }
}
/* compiles to:
 * export type bindThis<def> = { this: Def<def> }
 */

// ✓ Def: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Def(def = {}): any {
  return Brand(def, 'unparsed')
}
/* compiles to:
 * export type Def<def = {}> = Brand<def, 'unparsed'>
 */

// ✗ bootstrapAliases: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function bootstrapAliases__v1(def, k): any {
  const m1 = matches<{ t: Hole<"g", GenericAst>; }>(def[k])
  if (m1) {
    return m1.g
  }
  const m2 = matches<Module<Hole<"$">> | BoundModule<Hole<"$">, any>>(def[k])
  if (m2) {
    return Submodule(m2.$)
  }
  return def[k]
}

export function bootstrapAliases__v2(def, k): any {
  const m1 = matches<(() => Hole<"thunkReturn", PreparsedResolution>)>(def[k])
  if (m1) {
    const m2 = matches<{ t: Hole<"g", GenericAst>; }>(m1.thunkReturn)
    if (m2) {
      return m2.g
    }
    const m3 = matches<Module<Hole<"$">> | BoundModule<Hole<"$">, any>>(m1.thunkReturn)
    if (m3) {
      return Submodule(m3.$)
    }
    return m1.thunkReturn
  }
  return Def(def[k])
}
export function bootstrapAliases(def): any {
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(def), GenericDeclaration))) {
    out[k] = matches<(PreparsedResolution)>(def[k]) ? (bootstrapAliases__v1(def, k)) : (bootstrapAliases__v2(def, k))
  }
  const out2 = emptyObject
  for (const k in keySet(keyof(def) & GenericDeclaration)) {
    out2[extractGenericName(k)] = GenericAst(parseValidGenericParams(extractGenericParameters(k), bootstrapAliases(def)), def[k], UnparsedScope)
  }
  return out & out2
}
/* compiles to:
 * export type bootstrapAliases__v1<def, k> =
 *   def[k] extends { t: infer g extends GenericAst; } ? g
 *   : def[k] extends Module<infer $> | BoundModule<infer $, any> ? Submodule<$>
 *   : def[k]
 * export type bootstrapAliases__v2<def, k> =
 *   def[k] extends () => infer thunkReturn extends PreparsedResolution
 *     ? thunkReturn extends { t: infer g extends GenericAst; } ? g
 *     : thunkReturn extends Module<infer $> | BoundModule<infer $, any> ? Submodule<$>
 *     : thunkReturn
 *     : Def<def[k]>
 * export type bootstrapAliases<def> =
 *   & {
 *       [K in Exclude<keyof def, GenericDeclaration>]: def[K] extends PreparsedResolution
 *         ? bootstrapAliases__v1<def, K>
 *         : bootstrapAliases__v2<def, K>
 *     }
 *   & {
 *       [K1 in keyof def & GenericDeclaration as extractGenericName<K1>]: GenericAst<
 *         parseValidGenericParams<extractGenericParameters<K1>, bootstrapAliases<def>>,
 *         def[K1],
 *         UnparsedScope
 *       >
 *     }
 */

// ✓ inferBootstrapped: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferBootstrapped__v1($, name): any {
  const m1 = matches<Def<Hole<"def">>>($[name])
  if (m1) {
    return inferDefinition(m1.def, $, {})
  }
  const m2 = matches<{ t: Hole<"g", GenericAst>; }>($[name])
  if (m2) {
    return bindGenericToScope(m2.g, $)
  }
  return $[name]
}
export function inferBootstrapped($): any {
  const out = emptyObject
  for (const name in keyof($)) {
    out[name] = inferBootstrapped__v1($, name)
  }
  return out & unknown
}
/* compiles to:
 * export type inferBootstrapped__v1<$, name> =
 *   $[name] extends Def<infer def> ? inferDefinition<def, $, {}>
 *   : $[name] extends { t: infer g extends GenericAst; } ? bindGenericToScope<g, $>
 *   : $[name]
 * export type inferBootstrapped<$> =
 *   { [Name in keyof $]: inferBootstrapped__v1<$, Name> } & unknown
 */

// ✓ bindGenericToScope: verified type-identical to the original
/* @scripttype preserveParamNames */
export function bindGenericToScope(g: GenericAst, $): any {
  return GenericAst(g['paramsAst'], g['bodyDef'], matches<UnparsedScope>(g['$']) ? $ : g['$'], $)
}
/* compiles to:
 * export type bindGenericToScope<g extends GenericAst, $> = GenericAst<
 *   g['paramsAst'],
 *   g['bodyDef'],
 *   g['$'] extends UnparsedScope ? $ : g['$'],
 *   $
 * >
 */

// ✓ extractGenericName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractGenericName(k): any {
  const m1 = matches<GenericDeclaration<Hole<"name">>>(k)
  if (m1) {
    return m1.name
  }
  return never
}
/* compiles to:
 * export type extractGenericName<k> = k extends GenericDeclaration<infer name> ? name : never
 */

// ✓ extractGenericParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractGenericParameters(k): any {
  const m1 = matches<`${string}<${Hole<"params">}>`>(k)
  if (m1) {
    return ParameterString(m1.params)
  }
  return never
}
/* compiles to:
 * export type extractGenericParameters<k> =
 *   k extends `${string}<${infer params}>` ? ParameterString<params> : never
 */

// ✓ resolutionToAst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function resolutionToAst(alias: string, resolution): any {
  if (matches<[ anyOrNever ]>([resolution])) {
    return InferredAst(resolution, alias)
  }
  const m1 = matches<Def<Hole<"def">>>(resolution)
  if (m1) {
    return DefAst(m1.def, alias)
  }
  const m2 = matches<{ [arkKind]: "module"; root: Hole<"root">; }>(resolution)
  if (m2) {
    return InferredAst(m2.root, alias)
  }
  if (matches<GenericAst>(resolution)) {
    return resolution
  }
  return InferredAst(resolution, alias)
}
/* compiles to:
 * export type resolutionToAst<alias extends string, resolution> =
 *   [resolution] extends [anyOrNever] ? InferredAst<resolution, alias>
 *   : resolution extends Def<infer def> ? DefAst<def, alias>
 *   : resolution extends { [arkKind]: "module"; root: infer root; } ? InferredAst<root, alias>
 *   : resolution extends GenericAst ? resolution
 *   : InferredAst<resolution, alias>
 */

// ✓ moduleKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function moduleKeyOf($): any {
  const out = emptyObject
  for (const k in keyof($)) {
    out[k] = matches<{ [arkKind]: "module"; }>($[k]) ? (matches<[ anyOrNever ]>([$[k]]) ? never : (merge(k, string))) : never
  }
  return out[keyof($)]
}
/* compiles to:
 * export type moduleKeyOf<$> =
 *   {
 *     [K in keyof $]: $[K] extends { [arkKind]: "module"; }
 *       ? [$[K]] extends [anyOrNever] ? never : K & string
 *       : never
 *   }[keyof $]
 */

// ✓ validate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validate__v1(def, k): any {
  const m1 = matches<Hole<"params">>(parseScopeKey(k, def)['params'])
  if (m1) {
    if (matches<array<GenericParamAst>>(m1.params)) {
      if (matches<0>(m1.params['length'])) {
        if (matches<type.Any | PreparsedResolution>(def[k])) {
          return def[k]
        }
        const m2 = matches<(PrivateDeclaration<Hole<"name", keyof typeof def & string>>)>(k)
        if (m2) {
          return ErrorType(writeDuplicateAliasError(m2.name))
        }
        return t<type.validate<(typeof def)[typeof k], bootstrapAliases<typeof def>, {}>>()
      }
      return t<type.validate<(typeof def)[typeof k], bootstrapAliases<typeof def>, baseGenericConstraints<typeof m1.params>>>()
    }
    return m1.params
  }
  return never
}
export function validate(def): any {
  const out = emptyObject
  for (const k in keyof(def)) {
    out[k] = matches<noSuggest>(k) ? unknown : (validate__v1(def, k))
  }
  return out
}
/* compiles to:
 * export type validate__v1<def, k> =
 *   parseScopeKey<k, def>['params'] extends infer params
 *     ? params extends array<GenericParamAst>
 *       ? params['length'] extends 0
 *         ? def[k] extends type.Any | PreparsedResolution ? def[k]
 *         : k extends PrivateDeclaration<infer name extends keyof def & string>
 *           ? ErrorType<writeDuplicateAliasError<name>>
 *         : type.validate<def[k], bootstrapAliases<def>, {}>
 *         : type.validate<def[k], bootstrapAliases<def>, baseGenericConstraints<params>>
 *       : params
 *     : never
 * export type validate<def> = {
 *   [K in keyof def]: K extends noSuggest ? unknown : validate__v1<def, K>
 * }
 */

// ✓ infer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function infer(def): any {
  return inferBootstrapped(bootstrapAliases(def))
}
/* compiles to:
 * export type infer<def> = inferBootstrapped<bootstrapAliases<def>>
 */

// ✓ parseScopeKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseScopeKey(k, def): any {
  const m1 = matches<`${Hole<"name">}<${Hole<"params">}>`>(k)
  if (m1) {
    return parseGenericScopeKey(m1.name, m1.params, def)
  }
  return { name: k, params: [] }
}
/* compiles to:
 * export type parseScopeKey<k, def> =
 *   k extends `${infer name}<${infer params}>`
 *     ? parseGenericScopeKey<name, params, def>
 *     : { name: k; params: [] }
 */

// ✓ parseGenericScopeKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseGenericScopeKey(name: string, params: string, def): any {
  return { name: name, params: parseGenericParams(params, bootstrapAliases(def)) }
}
/* compiles to:
 * export type parseGenericScopeKey<name extends string, params extends string, def> = {
 *   name: name
 *   params: parseGenericParams<params, bootstrapAliases<def>>
 * }
 */

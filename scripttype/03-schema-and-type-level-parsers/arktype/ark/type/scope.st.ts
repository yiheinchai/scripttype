/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/scope.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 9 covered, 3 raw, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Brand: any
declare const DefAst: any
declare const GenericAst: any
declare const GenericDeclaration: any
declare const InferredAst: any
declare const ParameterString: any
declare const PreparsedResolution: any
declare const UnparsedScope: any
declare const anyOrNever: any
declare const arkKind: any
declare const noSuggest: any
declare const parseGenericParams: any
declare const parseValidGenericParams: any
type Brand<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Def<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DefAst<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GenericAst<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GenericDeclaration<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InferredAst<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParameterString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PreparsedResolution<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnparsedScope<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type anyOrNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type arkKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type noSuggest<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type parseGenericParams<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type parseValidGenericParams<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ bindThis: verified type-identical to the original
/* @scripttype preserveParamNames */
export function bindThis(def) {
  return { this: Def(def) }
}
/* compiles to:
 * export type bindThis<def> = { this: Def<def> }
 */

// ✓ Def: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Def(def = {}) {
  return Brand(def, 'unparsed')
}
/* compiles to:
 * export type Def<def = {}> = Brand<def, 'unparsed'>
 */

// ✗ bootstrapAliases: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function bootstrapAliases(def) {
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(def), GenericDeclaration))) {
    out[k] = matches<(PreparsedResolution)>(def[k]) ? (raw('def[k] extends { t: infer g extends GenericAst; } ? g : def[k] extends Module<infer $> | BoundModule<infer $, any> ? Submodule<$> : def[k]')) : (raw('def[k] extends (() => infer thunkReturn extends PreparsedResolution) ? thunkReturn extends { t: infer g extends GenericAst; } ? g : thunkReturn extends Module<infer $> | BoundModule<infer $, any> ? Submodule<$> : thunkReturn : Def<def[k]>'))
  }
  const out2 = emptyObject
  for (const k in keySet(keyof(def) & GenericDeclaration)) {
    out2[extractGenericName(k)] = GenericAst(parseValidGenericParams(extractGenericParameters(k), bootstrapAliases(def)), def[k], UnparsedScope)
  }
  return out & out2
}

// ✗ inferBootstrapped: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function inferBootstrapped($) {
  const out = emptyObject
  for (const name in keyof($)) {
    out[name] = raw('$[name] extends Def<infer def> ? inferDefinition<def, $, {}> : $[name] extends { t: infer g extends GenericAst; } ? bindGenericToScope<g, $> : $[name]')
  }
  return out & unknown
}

// ✓ bindGenericToScope: verified type-identical to the original
/* @scripttype preserveParamNames */
export function bindGenericToScope(g: GenericAst, $) {
  return GenericAst(g['paramsAst'], g['bodyDef'], matches<UnparsedScope>(g['$']) ? $ : g['$'], $)
}
/* compiles to:
 * export type bindGenericToScope<g extends GenericAst, $> = GenericAst<g['paramsAst'], g['bodyDef'], g['$'] extends UnparsedScope ? $ : g['$'], $>
 */

// ✓ extractGenericName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractGenericName(k) {
  const m1 = matches<GenericDeclaration<Hole<"name">>>(k)
  if (m1) {
    return m1.name
  }
  return never
}
/* compiles to:
 * export type extractGenericName<k> = k extends GenericDeclaration<infer name> ? name : never
 */

// ✓ extractGenericParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function extractGenericParameters(k) {
  const m1 = matches<`${string}<${Hole<"params">}>`>(k)
  if (m1) {
    return ParameterString(m1.params)
  }
  return never
}
/* compiles to:
 * export type extractGenericParameters<k> = k extends `${string}<${infer params}>` ? ParameterString<params> : never
 */

// ✓ resolutionToAst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function resolutionToAst(alias: string, resolution) {
  if (matches<[ anyOrNever ]>([resolution])) {
    return InferredAst(resolution, alias)
  }
  const m1 = matches<Def<Hole<"def">>>(resolution)
  if (m1) {
    return DefAst(m1.def, alias)
  }
  const m2 = matches<{ [arkKind]: "module"; root: Hole<"root">; }>(resolution)
  if (m2) {
    return InferredAst(m2.root, alias)
  }
  if (matches<GenericAst>(resolution)) {
    return resolution
  }
  return InferredAst(resolution, alias)
}
/* compiles to:
 * export type resolutionToAst<alias extends string, resolution> = [resolution] extends [anyOrNever] ? InferredAst<resolution, alias> : resolution extends Def<infer def> ? DefAst<def, alias> : resolution extends { [arkKind]: "module"; root: infer root; } ? InferredAst<root, alias> : resolution extends GenericAst ? resolution : InferredAst<resolution, alias>
 */

// ✓ moduleKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function moduleKeyOf($) {
  const out = emptyObject
  for (const k in keyof($)) {
    out[k] = matches<{ [arkKind]: "module"; }>($[k]) ? (matches<[ anyOrNever ]>([$[k]]) ? never : (merge(k, string))) : never
  }
  return out[keyof($)]
}
/* compiles to:
 * export type moduleKeyOf<$> = { [K in keyof $]: $[K] extends { [arkKind]: "module"; } ? [$[K]] extends [anyOrNever] ? never : K & string : never }[keyof $]
 */

// ✗ validate: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function validate(def) {
  const out = emptyObject
  for (const k in keyof(def)) {
    out[k] = matches<noSuggest>(k) ? unknown : (raw('parseScopeKey<k, def>["params"] extends infer params ? params extends array<GenericParamAst> ? params["length"] extends 0 ? def[k] extends type.Any | PreparsedResolution ? def[k] : k extends (PrivateDeclaration<infer name extends keyof def & string>) ? ErrorType<writeDuplicateAliasError<name>> : type.validate<def[k], bootstrapAliases<def>, {}> : type.validate<def[k], bootstrapAliases<def>, baseGenericConstraints<params>> : params : never'))
  }
  return out
}

// ✗ infer: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function infer(def) {
  return inferBootstrapped(bootstrapAliases(def))
}
/* compiles to:
 * export type infer<def> = inferBootstrapped<bootstrapAliases<def>>
 */

// ✓ parseScopeKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseScopeKey(k, def) {
  const m1 = matches<`${Hole<"name">}<${Hole<"params">}>`>(k)
  if (m1) {
    return parseGenericScopeKey(m1.name, m1.params, def)
  }
  return { name: k, params: [] }
}
/* compiles to:
 * export type parseScopeKey<k, def> = k extends `${infer name}<${infer params}>` ? parseGenericScopeKey<name, params, def> : { name: k; params: [] }
 */

// ✓ parseGenericScopeKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function parseGenericScopeKey(name: string, params: string, def) {
  return { name: name, params: parseGenericParams(params, bootstrapAliases(def)) }
}
/* compiles to:
 * export type parseGenericScopeKey<name extends string, params extends string, def> = { name: name; params: parseGenericParams<params, bootstrapAliases<def>> }
 */

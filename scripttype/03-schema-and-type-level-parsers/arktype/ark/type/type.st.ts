/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/type/type.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 6 raw, 1 mismatch, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BaseParseOptions: any
declare const RootSchema: any
declare const instantiateType: any
type BaseParseOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RootSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type instantiateType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ UnitTypeParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function UnitTypeParser($) {
  return raw('<const t>(value: t) => Type<t, $>')
}

// ✗ InstanceOfTypeParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function InstanceOfTypeParser($) {
  return raw('<const t extends object>(ctor: Constructor<t>) => Type<t, $>')
}

// ✗ EnumeratedTypeParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function EnumeratedTypeParser($) {
  return raw('<const values extends readonly unknown[]>(...values: values) => Type<values[number], $>')
}

// ✗ ValueOfTypeParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function ValueOfTypeParser($) {
  return raw('<const o extends object>(o: o) => Type<o[keyof o], $>')
}

// ✗ DefinitionParser: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function DefinitionParser($) {
  return raw('<const def>(def: type.validate<def, $>) => def')
}

// ✗ SchemaParser: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function SchemaParser($) {
  return fnType([RootSchema, BaseParseOptions], Type(unknown, $))
}
/* compiles to:
 * export type SchemaParser<$> = (a0: RootSchema, a1: BaseParseOptions) => Type<unknown, $>
 */

// ✓ Type: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Type(t = unknown, $ = {}) {
  return instantiateType(t, $)
}
/* compiles to:
 * export type Type<t = unknown, $ = {}> = instantiateType<t, $>
 */

// ✗ TypeConstructor: uses raw() — language gap, does not count as covered
//   gap: type node ConstructorType
/* @scripttype preserveParamNames */
export function TypeConstructor(t = unknown, $ = {}) {
  return raw('new (def: unknown, $: Scope<$>) => Type<t, $>')
}

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/arrays.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 36 type-level aliases: 27 covered, 4 compile-error, 3 mismatch, 2 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const PropertyKey: any
declare const anyOrNever: any
declare const conform: any
declare const exponentials: any
declare const isDisjoint: any
declare const parseNonNegativeInteger: any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type anyOrNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type conform<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type exponentials<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type groupableKeyOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type isDisjoint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type parseNonNegativeInteger<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ DuplicateData: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DuplicateData(val = unknown) {
  return { element: val, indices: arrayOf(number) }
}
/* compiles to:
 * export type DuplicateData<val = unknown> = { element: val; indices: number[] }
 */

// ✓ pathToString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pathToString(segments: string[], delimiter: string = '/') {
  if (matches<[ ]>(segments)) {
    return '/'
  }
  return join(segments, delimiter)
}
/* compiles to:
 * export type pathToString<segments extends string[], delimiter extends string = '/'> = segments extends [] ? '/' : Join<segments, delimiter>
 */

// ✓ join: verified type-identical to the original
/* @scripttype preserveParamNames */
export function join(segments: array<string>, delimiter: string) {
  let result = ''
  let segments_ = segments
  while (true) {
    const m1 = matches<(readonly [ Hole<"head", string>, ...Hole<"tail", string[]> ])>(segments_)
    if (!m1) {
      break
    }
    segments_ = m1.tail
    result = matches<"">(result) ? m1.head : `${result}${delimiter}${m1.head}`
  }
  return result
}
/* compiles to:
 * export type join<segments extends array<string>, delimiter extends string> = Join__loop<segments, '', delimiter>
 * type Join__loop<Segments_ extends array<string>, Result extends string, Delimiter extends string> = Segments_ extends readonly [infer head extends string, ...(infer tail extends string[])] ? Join__loop<tail, Result extends '' ? head : `${Result}${Delimiter}${head}`, Delimiter> : Result
 */

// ✗ filter: the ScriptType does not itself typecheck as TypeScript
//   filter.st.ts(11:5) TS2322: Type 'any[]' is not assignable to type 'never[]'.   Type 'any' is not assignable to type 'never'.
/* @scripttype preserveParamNames */
export function filter(t: array, constraint) {
  let result = []
  let t_ = t
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t_)
    if (!m1) {
      break
    }
    t_ = m1.tail
    result = matches<typeof constraint>(m1.head) ? [...result, m1.head] : result
  }
  return result
}
/* compiles to:
 * export type filter<t extends array, constraint> = Filter__loop<t, [], constraint>
 * type Filter__loop<T_ extends array, Result extends readonly unknown[], Constraint> = T_ extends readonly [infer head, ...infer tail] ? Filter__loop<tail, head extends Constraint ? [...Result, head] : Result, Constraint> : Result
 */

// ✓ array: verified type-identical to the original
/* @scripttype preserveParamNames */
export function array(t = unknown) {
  return readonlyArrayOf(t)
}
/* compiles to:
 * export type array<t = unknown> = readonly t[]
 */

// ✗ multiply: does not compile yet
//   Cannot find name '_multiply'.
/* @scripttype preserveParamNames */
export function multiply(t: array, count: number) {
  return _multiply(t, [], count, [])
}
/* compiles to:
 * export type multiply<t extends array, count extends number> = _multiply<t, [], count, []>
 */

// ✗ _multiply: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function _multiply(base: array, result: array, count: number, i: 1[]) {
  let result_ = result
  let i_ = i
  while (true) {
    if (matches<typeof count>(i_['length'])) {
      break
    }
    result_ = [...result_, ...base]
    i_.push(1)
  }
  return result_
}
/* compiles to:
 * export type _multiply<base extends array, result extends array, count extends number, i extends 1[]> = _multiply__loop<result, i, count, base>
 * type _multiply__loop<Result_ extends array, I_ extends 1[], Count extends number, Base extends array> = I_['length'] extends Count ? Result_ : _multiply__loop<[...Result_, ...Base], [...I_, 1], Count, Base>
 */

// ✗ repeat: does not compile yet
//   Cannot find name 'buildFromSegments'. Did you mean 'buildFromSegments__st8'?
/* @scripttype preserveParamNames */
export function repeat(element, count: number) {
  return buildFromSegments(element, [], exponentials.max(count), count)
}
/* compiles to:
 * export type repeat<element, count extends number> = buildFromSegments<element, [], exponentials.max<count>, count>
 */

// ✗ buildFromSegments: does not compile yet
//   Cannot find name 'nextSegments'. Did you mean 'segments'?
/* @scripttype preserveParamNames */
export function buildFromSegments(element, result: 1[], segments: 1[][], count: number, next: 1[] = [...result, ...segments[0]]) {
  if (matches<typeof count>(next['length'])) {
    const out = emptyObject
    for (const i in keyof(next)) {
      out[i] = element
    }
    return out
  }
  if (matches<keyof typeof next>(`${count}`)) {
    return buildFromSegments(element, result, nextSegments(segments), count)
  }
  return buildFromSegments(element, next, nextSegments(segments), count)
}
/* compiles to:
 * export type buildFromSegments<element, result extends 1[], segments extends 1[][], count extends number, next extends 1[] = [...result, ...segments[0]]> = next['length'] extends count ? { [I in keyof next]: element } : `${count}` extends keyof next ? buildFromSegments<element, result, nextSegments<segments>, count> : buildFromSegments<element, next, nextSegments<segments>, count>
 */

// ✗ nextSegments: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function nextSegments(segments: 1[][]) {
  const m1 = matches<[ unknown, ...Hole<"nextSegments", 1[][]> ]>(segments)
  if (m1) {
    return m1.nextSegments
  }
  return never
}
/* compiles to:
 * export type nextSegments<segments extends 1[][]> = segments extends [unknown, ...(infer nextSegments extends 1[][])] ? nextSegments : never
 */

// ✗ minLength: does not compile yet
//   Cannot find name 'multiply'.
/* @scripttype preserveParamNames */
export function minLength(element, minLength: number) {
  return asReadonly([...multiply([element], minLength), ...arrayOf(element)])
}
/* compiles to:
 * export type minLength<element, minLength extends number> = readonly [...multiply<[element], minLength>, ...element[]]
 */

// ✓ listable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function listable(t) {
  return t | readonlyArrayOf(t)
}
/* compiles to:
 * export type listable<t> = t | readonly t[]
 */

// ✓ flattenListable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function flattenListable(t) {
  const m1 = matches<array<Hole<"element">>>(t)
  if (m1) {
    return m1.element
  }
  return t
}
/* compiles to:
 * export type flattenListable<t> = t extends array<infer element> ? element : t
 */

// ✓ longerThan: verified type-identical to the original
/* @scripttype preserveParamNames */
export function longerThan(t: array, n: number) {
  if (matches<keyof typeof t>(`${n}`)) {
    return true
  }
  return false
}
/* compiles to:
 * export type longerThan<t extends array, n extends number> = `${n}` extends keyof t ? true : false
 */

// ✓ CollapsingList: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CollapsingList(t = unknown) {
  return asReadonly([]) | t | asReadonly([t, t, ...arrayOf(t)])
}
/* compiles to:
 * export type CollapsingList<t = unknown> = readonly [] | t | readonly [t, t, ...t[]]
 */

// ✓ headOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function headOf(t: array) {
  return t[0]
}
/* compiles to:
 * export type headOf<t extends array> = t[0]
 */

// ✓ tailOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function tailOf(t: array) {
  const m1 = matches<readonly [ unknown, ...Hole<"tail"> ]>(t)
  if (m1) {
    return m1.tail
  }
  return never
}
/* compiles to:
 * export type tailOf<t extends array> = t extends readonly [unknown, ...infer tail] ? tail : never
 */

// ✓ lastIndexOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function lastIndexOf(t: array) {
  return tailOf(t)['length']
}
/* compiles to:
 * export type lastIndexOf<t extends array> = tailOf<t>['length']
 */

// ✓ lastOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function lastOf(t: array) {
  return t[lastIndexOf(t)]
}
/* compiles to:
 * export type lastOf<t extends array> = t[lastIndexOf<t>]
 */

// ✓ initOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function initOf(t: array) {
  const m1 = matches<readonly [ ...Hole<"init">, unknown ]>(t)
  if (m1) {
    return m1.init
  }
  return never
}
/* compiles to:
 * export type initOf<t extends array> = t extends readonly [...infer init, unknown] ? init : never
 */

// ✓ numericStringKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function numericStringKeyOf(t: array) {
  return Extract(keyof(t), `${number}`)
}
/* compiles to:
 * export type numericStringKeyOf<t extends array> = Extract<keyof t, `${number}`>
 */

// ✓ arrayIndexOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arrayIndexOf(a: array) {
  const m1 = matches<Hole<"k">>(keyof(a))
  if (m1) {
    return parseNonNegativeInteger(m1.k & string)
  }
  return never
}
/* compiles to:
 * export type arrayIndexOf<a extends array> = keyof a extends infer k ? parseNonNegativeInteger<k & string> : never
 */

// ✓ liftArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function liftArray(t) {
  if (matches<array>(t)) {
    if (matches<[ anyOrNever ]>([t])) {
      return arrayOf(t)
    }
    return t
  }
  return arrayOf(t)
}
/* compiles to:
 * export type liftArray<t> = t extends array ? [t] extends [anyOrNever] ? t[] : t : t[]
 */

// ✓ appendableValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function appendableValue(to: array | undefined) {
  const m1 = matches<array<Hole<"element">>>(to)
  if (m1) {
    if (matches<array>(m1.element)) {
      return array(m1.element)
    }
    return listable(m1.element)
  }
  return never
}
/* compiles to:
 * export type appendableValue<to extends array | undefined> = to extends array<infer element> ? element extends array ? array<element> : listable<element> : never
 */

// ✓ groupableKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function groupableKeyOf(o) {
  const m1 = matches<Hole<"k">>(keyof(o))
  if (m1) {
    if (matches<keyof typeof o>(m1.k)) {
      if (matches<PropertyKey>(o[m1.k])) {
        return m1.k
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type groupableKeyOf<o> = keyof o extends infer k ? k extends keyof o ? o[k] extends PropertyKey ? k : never : never : never
 */

// ✗ groupBy: the ScriptType does not itself typecheck as TypeScript
//   groupBy.st.ts(4:50) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function groupBy(element, discriminant: groupableKeyOf<typeof element>) {
  const out = emptyObject
  for (const k in keySet(element[discriminant] & PropertyKey)) {
    out[k] = optional(arrayOf(matches<unknown>(element) ? (matches<true>(isDisjoint(element[discriminant], k)) ? never : element) : never))
  }
  return out & unknown
}
/* compiles to:
 * export type groupBy<element, discriminant extends groupableKeyOf<element>> = { [K in element[discriminant] & PropertyKey]?: (element extends unknown ? isDisjoint<element[discriminant], K> extends true ? never : element : never)[] } & unknown
 */

// ✓ validateExhaustiveKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateExhaustiveKeys(keys: readonly PropertyKey[], expectedKey: PropertyKey) {
  const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail", PropertyKey[]> ]>(keys)
  if (m1) {
    return asReadonly([conform(m1.head, expectedKey), ...validateExhaustiveKeys(m1.tail, Exclude(expectedKey, m1.head))])
  }
  if (matches<[ never ]>([expectedKey])) {
    return []
  }
  return [expectedKey]
}
/* compiles to:
 * export type validateExhaustiveKeys<keys extends readonly PropertyKey[], expectedKey extends PropertyKey> = keys extends readonly [infer head, ...(infer tail extends PropertyKey[])] ? readonly [conform<head, expectedKey>, ...validateExhaustiveKeys<tail, Exclude<expectedKey, head>>] : [expectedKey] extends [never] ? [] : [expectedKey]
 */

// ✓ applyElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyElementLabels(t: readonly unknown[], labels: readonly unknown[]) {
  const m1 = matches<[ unknown, ...Hole<"labelsTail"> ]>(labels)
  if (m1) {
    const m2 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t)
    if (m2) {
      return asReadonly([...labelElement(m2.head, labels), ...applyElementLabels(m2.tail, m1.labelsTail)])
    }
    return applyOptionalElementLabels(Required(t), labels)
  }
  return t
}
/* compiles to:
 * export type applyElementLabels<t extends readonly unknown[], labels extends readonly unknown[]> = labels extends [unknown, ...infer labelsTail] ? t extends readonly [infer head, ...infer tail] ? readonly [...labelElement<head, labels>, ...applyElementLabels<tail, labelsTail>] : applyOptionalElementLabels<Required<t>, labels> : t
 */

// ✓ applyOptionalElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyOptionalElementLabels(t: readonly unknown[], labels: readonly unknown[]) {
  const m1 = matches<readonly [ unknown, ...Hole<"labelsTail"> ]>(labels)
  if (m1) {
    const m2 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t)
    if (m2) {
      return [...labelOptionalElement(m2.head, labels), ...applyOptionalElementLabels(m2.tail, m1.labelsTail)]
    }
    return applyRestElementLabels(t, labels)
  }
  return t
}
/* compiles to:
 * export type applyOptionalElementLabels<t extends readonly unknown[], labels extends readonly unknown[]> = labels extends readonly [unknown, ...infer labelsTail] ? t extends readonly [infer head, ...infer tail] ? [...labelOptionalElement<head, labels>, ...applyOptionalElementLabels<tail, labelsTail>] : applyRestElementLabels<t, labels> : t
 */

// ✓ applyRestElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyRestElementLabels(t: readonly unknown[], labels: readonly unknown[]) {
  if (matches<readonly [ ]>(t)) {
    return []
  }
  const m1 = matches<readonly [ unknown, ...Hole<"tail"> ]>(labels)
  if (m1) {
    return [...labelOptionalElement(t[0], labels), ...applyRestElementLabels(t, m1.tail)]
  }
  return t
}
/* compiles to:
 * export type applyRestElementLabels<t extends readonly unknown[], labels extends readonly unknown[]> = t extends readonly [] ? [] : labels extends readonly [unknown, ...infer tail] ? [...labelOptionalElement<t[0], labels>, ...applyRestElementLabels<t, tail>] : t
 */

// ✓ labelElement: verified type-identical to the original
/* @scripttype preserveParamNames */
export function labelElement(element, labels: readonly unknown[]) {
  if (matches<readonly [ unknown ]>(labels)) {
    const out = emptyObject
    for (const K in keyof(labels)) {
      out[K] = element
    }
    return out
  }
  const m1 = matches<readonly [ ...Hole<"head">, unknown ]>(labels)
  if (m1) {
    return labelElement(element, m1.head)
  }
  return [element]
}
/* compiles to:
 * export type labelElement<element, labels extends readonly unknown[]> = labels extends readonly [unknown] ? { [K in keyof labels]: element } : labels extends readonly [...infer head, unknown] ? labelElement<element, head> : [element]
 */

// ✗ labelOptionalElement: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function labelOptionalElement(element, label: readonly unknown[]) {
  if (matches<readonly [ unknown ]>(label)) {
    const out = emptyObject
    for (const K in keyof(label)) {
      out[K] = optional(element)
    }
    return out
  }
  const m1 = matches<readonly [ ...Hole<"head">, unknown ]>(label)
  if (m1) {
    return labelOptionalElement(element, m1.head)
  }
  return [element]
}
/* compiles to:
 * export type labelOptionalElement<element, label extends readonly unknown[]> = label extends readonly [unknown] ? { [K in keyof label]?: element } : label extends readonly [...infer head, unknown] ? labelOptionalElement<element, head> : [element]
 */

// ✓ setIndex: verified type-identical to the original
/* @scripttype preserveParamNames */
export function setIndex(arr: readonly unknown[], i: number, to: (typeof arr)[number]) {
  if (matches<(typeof arr)[number][]>(arr)) {
    return _setIndex(arr, i, to, [])
  }
  return Readonly(_setIndex(arr, i, to, []))
}
/* compiles to:
 * export type setIndex<arr extends readonly unknown[], i extends number, to extends arr[number]> = arr extends arr[number][] ? _setIndex<arr, i, to, []> : Readonly<_setIndex<arr, i, to, []>>
 */

// ✓ _setIndex: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _setIndex(arr: readonly unknown[], i: number, to: (typeof arr)[number], result: (typeof arr)[number][]) {
  let arr_ = arr
  let result_ = result
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(arr_)
    if (!m1) {
      break
    }
    arr_ = m1.tail
    result_.push(matches<typeof i>(result_['length']) ? to : m1.head)
  }
  return result_
}
/* compiles to:
 * export type _setIndex<arr extends readonly unknown[], i extends number, to extends arr[number], result extends arr[number][]> = _setIndex__loop<arr, result, i, to>
 * type _setIndex__loop<Arr_ extends readonly unknown[], Result_ extends arr[number][], I extends number, To extends arr[number]> = Arr_ extends readonly [infer head, ...infer tail] ? _setIndex__loop<tail, [...Result_, Result_['length'] extends I ? To : head], I, To> : Result_
 */

// ✓ max: verified type-identical to the original
/* @scripttype preserveParamNames */
export function max(n: number) {
  return _max(n, exponentials)
}
/* compiles to:
 * export type max<n extends number> = _max<n, exponentials>
 */

// ✓ _max: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _max(n: number, filtered: unknown[]) {
  if (matches<keyof (typeof filtered)[0]>(`${n}`)) {
    return _max(n, tailOf(filtered))
  }
  return filtered
}
/* compiles to:
 * export type _max<n extends number, filtered extends unknown[]> = `${n}` extends keyof filtered[0] ? _max<n, tailOf<filtered>> : filtered
 */

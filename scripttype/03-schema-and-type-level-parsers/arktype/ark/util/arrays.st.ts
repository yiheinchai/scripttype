/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/arrays.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 36 type-level aliases: 33 covered, 2 typecheck-error, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { anyOrNever, conform } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
import type { isDisjoint } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/intersections.js'
import type { parseNonNegativeInteger } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/numbers.js'
declare namespace exponentials {
  export type max<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace i_ {
  export type push<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type element<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type init<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type k<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type labelsTail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type nextSegments<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace result_ {
  export type push<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const PropertyKey: any
declare const anyOrNever: any
declare const conform: any
declare const exponentials: any
declare const isDisjoint: any
declare const parseNonNegativeInteger: any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type array<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type exponentials<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ DuplicateData: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DuplicateData(val = unknown): any {
  return { element: val, indices: arrayOf(number) }
}
/* compiles to:
 * export type DuplicateData<val = unknown> = { element: val; indices: number[] }
 */

// ✓ pathToString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function pathToString(segments: string[], delimiter: string = '/'): any {
  if (matches<[ ]>(segments)) {
    return '/'
  }
  return join(segments, delimiter)
}
/* compiles to:
 * export type pathToString<segments extends string[], delimiter extends string = '/'> =
 *   segments extends [] ? '/' : Join<segments, delimiter>
 */

// ✓ join: verified type-identical to the original
/* @scripttype preserveParamNames */
export function join(segments: array<string>, delimiter: string): any {
  let result = ''
  let segments_ = segments
  while (true) {
    const m1 = matches<(readonly [ Hole<"head", string>, ...Hole<"tail", string[]> ])>(segments_)
    if (!m1) {
      break
    }
    segments_ = m1.tail
    result = matches<"">(result) ? m1.head : `${result}${delimiter}${m1.head}`
  }
  return result
}
/* compiles to:
 * export type join<segments extends array<string>, delimiter extends string> = Join__loop<
 *   segments,
 *   '',
 *   delimiter
 * >
 * type Join__loop<
 *   Segments_ extends array<string>,
 *   Result extends string,
 *   Delimiter extends string
 * > =
 *   Segments_ extends readonly [infer head extends string, ...infer tail extends string[]]
 *     ? Join__loop<tail, Result extends '' ? head : `${Result}${Delimiter}${head}`, Delimiter>
 *     : Result
 */

// ✓ filter: verified type-identical to the original
/* @scripttype preserveParamNames */
export function filter(t: array, constraint): any {
  let result: any[] = []
  let t_ = t
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t_)
    if (!m1) {
      break
    }
    t_ = m1.tail
    result = matches<typeof constraint>(m1.head) ? [...result, m1.head] : result
  }
  return result
}
/* compiles to:
 * export type filter<t extends array, constraint> = Filter__loop<t, [], constraint>
 * type Filter__loop<T_ extends array, Result extends any[], Constraint> =
 *   T_ extends readonly [infer head, ...infer tail]
 *     ? Filter__loop<tail, head extends Constraint ? [...Result, head] : Result, Constraint>
 *     : Result
 */

// ✓ array: verified type-identical to the original
/* @scripttype preserveParamNames */
export function array(t = unknown): any {
  return readonlyArrayOf(t)
}
/* compiles to:
 * export type array<t = unknown> = readonly t[]
 */

// ✓ multiply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function multiply(t: array, count: number): any {
  return _multiply(t, [], count, [])
}
/* compiles to:
 * export type multiply<t extends array, count extends number> = _multiply<t, [], count, []>
 */

// ✓ _multiply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _multiply(base: array, result: array, count: number, i: 1[]): any {
  let result_ = result
  let i_ = i
  while (true) {
    if (matches<typeof count>(i_['length'])) {
      break
    }
    result_ = [...result_, ...base]
    i_.push(1)
  }
  return result_
}
/* compiles to:
 * export type _multiply<
 *   base extends array,
 *   result extends array,
 *   count extends number,
 *   i extends 1[]
 * > =
 *   _multiply__loop<result, i, count, base>
 * type _multiply__loop<
 *   Result_ extends array,
 *   I_ extends 1[],
 *   Count extends number,
 *   Base extends array
 * > =
 *   I_['length'] extends Count
 *     ? Result_
 *     : _multiply__loop<[...Result_, ...Base], [...I_, 1], Count, Base>
 */

// ✓ repeat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function repeat(element, count: number): any {
  return buildFromSegments(element, [], t<exponentials.max<typeof count>>(), count)
}
/* compiles to:
 * export type repeat<element, count extends number> = buildFromSegments<
 *   element,
 *   [],
 *   exponentials.max<count>,
 *   count
 * >
 */

// ✓ buildFromSegments: verified type-identical to the original
/* @scripttype preserveParamNames */
export function buildFromSegments(element, result: 1[], segments: 1[][], count: number, next: 1[] = [...result, ...segments[0]]): any {
  if (matches<typeof count>(next['length'])) {
    const out = emptyObject
    for (const i in keyof(next)) {
      out[i] = element
    }
    return out
  }
  if (`${count}` in next) {
    return buildFromSegments(element, result, nextSegments(segments), count)
  }
  return buildFromSegments(element, next, nextSegments(segments), count)
}
/* compiles to:
 * export type buildFromSegments<
 *   element,
 *   result extends 1[],
 *   segments extends 1[][],
 *   count extends number,
 *   next extends 1[] = [...result, ...segments[0]]
 * > =
 *   next['length'] extends count ? { [I in keyof next]: element }
 *   : `${count}` extends keyof next
 *     ? buildFromSegments<element, result, nextSegments<segments>, count>
 *   : buildFromSegments<element, next, nextSegments<segments>, count>
 */

// ✓ nextSegments: verified type-identical to the original
/* @scripttype preserveParamNames */
export function nextSegments(segments: 1[][]): any {
  const m1 = matches<[ unknown, ...Hole<"nextSegments", 1[][]> ]>(segments)
  if (m1) {
    return m1.nextSegments
  }
  return never
}
/* compiles to:
 * export type nextSegments<segments extends 1[][]> =
 *   segments extends [unknown, ...infer nextSegments extends 1[][]] ? nextSegments : never
 */

// ✓ minLength: verified type-identical to the original
/* @scripttype preserveParamNames */
export function minLength(element, minLength: number): any {
  return asReadonly([...multiply([element], minLength), ...arrayOf(element)])
}
/* compiles to:
 * export type minLength<element, minLength extends number> =
 *   readonly [...multiply<[element], minLength>, ...element[]]
 */

// ✓ listable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function listable(t): any {
  return t | readonlyArrayOf(t)
}
/* compiles to:
 * export type listable<t> = t | readonly t[]
 */

// ✓ flattenListable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function flattenListable(t): any {
  const m1 = matches<array<Hole<"element">>>(t)
  if (m1) {
    return m1.element
  }
  return t
}
/* compiles to:
 * export type flattenListable<t> = t extends array<infer element> ? element : t
 */

// ✓ longerThan: verified type-identical to the original
/* @scripttype preserveParamNames */
export function longerThan(t: array, n: number): any {
  if (`${n}` in t) {
    return true
  }
  return false
}
/* compiles to:
 * export type longerThan<t extends array, n extends number> =
 *   `${n}` extends keyof t ? true : false
 */

// ✓ CollapsingList: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CollapsingList(t = unknown): any {
  return asReadonly([]) | t | asReadonly([t, t, ...arrayOf(t)])
}
/* compiles to:
 * export type CollapsingList<t = unknown> = readonly [] | t | readonly [t, t, ...t[]]
 */

// ✓ headOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function headOf(t: array): any {
  return t[0]
}
/* compiles to:
 * export type headOf<t extends array> = t[0]
 */

// ✓ tailOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function tailOf(t: array): any {
  const m1 = matches<readonly [ unknown, ...Hole<"tail"> ]>(t)
  if (m1) {
    return m1.tail
  }
  return never
}
/* compiles to:
 * export type tailOf<t extends array> = t extends readonly [unknown, ...infer tail] ? tail : never
 */

// ✓ lastIndexOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function lastIndexOf(t: array): any {
  return tailOf(t)['length']
}
/* compiles to:
 * export type lastIndexOf<t extends array> = tailOf<t>['length']
 */

// ✓ lastOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function lastOf(t: array): any {
  return t[lastIndexOf(t)]
}
/* compiles to:
 * export type lastOf<t extends array> = t[lastIndexOf<t>]
 */

// ✓ initOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function initOf(t: array): any {
  const m1 = matches<readonly [ ...Hole<"init">, unknown ]>(t)
  if (m1) {
    return m1.init
  }
  return never
}
/* compiles to:
 * export type initOf<t extends array> = t extends readonly [...infer init, unknown] ? init : never
 */

// ✓ numericStringKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function numericStringKeyOf(t: array): any {
  return Extract(keyof(t), `${number}`)
}
/* compiles to:
 * export type numericStringKeyOf<t extends array> = Extract<keyof t, `${number}`>
 */

// ✓ arrayIndexOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arrayIndexOf(a: array): any {
  const m1 = matches<Hole<"k">>(keyof(a))
  if (m1) {
    return parseNonNegativeInteger(merge(m1.k, string))
  }
  return never
}
/* compiles to:
 * export type arrayIndexOf<a extends array> =
 *   keyof a extends infer k ? parseNonNegativeInteger<k & string> : never
 */

// ✓ liftArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function liftArray(t): any {
  if (matches<array>(t)) {
    if (matches<[ anyOrNever ]>([t])) {
      return arrayOf(t)
    }
    return t
  }
  return arrayOf(t)
}
/* compiles to:
 * export type liftArray<t> = t extends array ? [t] extends [anyOrNever] ? t[] : t : t[]
 */

// ✓ appendableValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function appendableValue(to: array | undefined): any {
  const m1 = matches<array<Hole<"element">>>(to)
  if (m1) {
    if (matches<array>(m1.element)) {
      return array(m1.element)
    }
    return listable(m1.element)
  }
  return never
}
/* compiles to:
 * export type appendableValue<to extends array | undefined> =
 *   to extends array<infer element>
 *     ? element extends array ? array<element> : listable<element>
 *     : never
 */

// ✓ groupableKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function groupableKeyOf(o): any {
  const m1 = matches<Hole<"k">>(keyof(o))
  if (m1) {
    if (m1.k in o) {
      if (matches<PropertyKey>(o[m1.k])) {
        return m1.k
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type groupableKeyOf<o> =
 *   keyof o extends infer k
 *     ? k extends keyof o ? o[k] extends PropertyKey ? k : never : never
 *     : never
 */

// ✗ groupBy: the ScriptType does not itself typecheck as TypeScript
//   groupBy.st.ts(7:50) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
/**
 * @param {groupableKeyOf<typeof element>} discriminant
 */
export function groupBy(element, discriminant): any {
  const out = emptyObject
  for (const k in keySet(element[discriminant] & PropertyKey)) {
    out[k] = optional(arrayOf(matches<unknown>(element) ? (matches<true>(isDisjoint(element[discriminant], k)) ? never : element) : never))
  }
  return out & unknown
}
/* compiles to:
 * export type groupBy<element, discriminant extends groupableKeyOf<element>> =
 *   & {
 *       [K in element[discriminant] & PropertyKey]?: (
 *         element extends unknown
 *           ? isDisjoint<element[discriminant], K> extends true ? never : element
 *           : never
 *       )[]
 *     }
 *   & unknown
 */

// ✓ validateExhaustiveKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validateExhaustiveKeys(keys: readonly PropertyKey[], expectedKey: PropertyKey): any {
  const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail", PropertyKey[]> ]>(keys)
  if (m1) {
    return asReadonly([conform(m1.head, expectedKey), ...validateExhaustiveKeys(m1.tail, Exclude(expectedKey, m1.head))])
  }
  if (matches<[ never ]>([expectedKey])) {
    return []
  }
  return [expectedKey]
}
/* compiles to:
 * export type validateExhaustiveKeys<
 *   keys extends readonly PropertyKey[],
 *   expectedKey extends PropertyKey
 * > =
 *   keys extends readonly [infer head, ...infer tail extends PropertyKey[]]
 *     ? readonly [
 *       conform<head, expectedKey>,
 *       ...validateExhaustiveKeys<tail, Exclude<expectedKey, head>>
 *     ]
 *   : [expectedKey] extends [never] ? []
 *   : [expectedKey]
 */

// ✓ applyElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyElementLabels(t: readonly unknown[], labels: readonly unknown[]): any {
  const m1 = matches<[ unknown, ...Hole<"labelsTail"> ]>(labels)
  if (m1) {
    const m2 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t)
    if (m2) {
      return asReadonly([...labelElement(m2.head, labels), ...applyElementLabels(m2.tail, m1.labelsTail)])
    }
    return applyOptionalElementLabels(Required(t), labels)
  }
  return t
}
/* compiles to:
 * export type applyElementLabels<
 *   t extends readonly unknown[],
 *   labels extends readonly unknown[]
 * > =
 *   labels extends [unknown, ...infer labelsTail]
 *     ? t extends readonly [infer head, ...infer tail]
 *       ? readonly [...labelElement<head, labels>, ...applyElementLabels<tail, labelsTail>]
 *       : applyOptionalElementLabels<Required<t>, labels>
 *     : t
 */

// ✓ applyOptionalElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyOptionalElementLabels(t: readonly unknown[], labels: readonly unknown[]): any {
  const m1 = matches<readonly [ unknown, ...Hole<"labelsTail"> ]>(labels)
  if (m1) {
    const m2 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t)
    if (m2) {
      return [...labelOptionalElement(m2.head, labels), ...applyOptionalElementLabels(m2.tail, m1.labelsTail)]
    }
    return applyRestElementLabels(t, labels)
  }
  return t
}
/* compiles to:
 * export type applyOptionalElementLabels<
 *   t extends readonly unknown[],
 *   labels extends readonly unknown[]
 * > =
 *   labels extends readonly [unknown, ...infer labelsTail]
 *     ? t extends readonly [infer head, ...infer tail]
 *       ? [...labelOptionalElement<head, labels>, ...applyOptionalElementLabels<tail, labelsTail>]
 *       : applyRestElementLabels<t, labels>
 *     : t
 */

// ✓ applyRestElementLabels: verified type-identical to the original
/* @scripttype preserveParamNames */
export function applyRestElementLabels(t: readonly unknown[], labels: readonly unknown[]): any {
  if (matches<readonly [ ]>(t)) {
    return []
  }
  const m1 = matches<readonly [ unknown, ...Hole<"tail"> ]>(labels)
  if (m1) {
    return [...labelOptionalElement(t[0], labels), ...applyRestElementLabels(t, m1.tail)]
  }
  return t
}
/* compiles to:
 * export type applyRestElementLabels<
 *   t extends readonly unknown[],
 *   labels extends readonly unknown[]
 * > =
 *   t extends readonly [] ? []
 *   : labels extends readonly [unknown, ...infer tail]
 *     ? [...labelOptionalElement<t[0], labels>, ...applyRestElementLabels<t, tail>]
 *   : t
 */

// ✓ labelElement: verified type-identical to the original
/* @scripttype preserveParamNames */
export function labelElement(element, labels: readonly unknown[]): any {
  if (matches<readonly [ unknown ]>(labels)) {
    const out = emptyObject
    for (const K in keyof(labels)) {
      out[K] = element
    }
    return out
  }
  const m1 = matches<readonly [ ...Hole<"head">, unknown ]>(labels)
  if (m1) {
    return labelElement(element, m1.head)
  }
  return [element]
}
/* compiles to:
 * export type labelElement<element, labels extends readonly unknown[]> =
 *   labels extends readonly [unknown] ? { [K in keyof labels]: element }
 *   : labels extends readonly [...infer head, unknown] ? labelElement<element, head>
 *   : [element]
 */

// ✗ labelOptionalElement: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function labelOptionalElement(element, label: readonly unknown[]): any {
  if (matches<readonly [ unknown ]>(label)) {
    const out = emptyObject
    for (const K in keyof(label)) {
      out[K] = optional(element)
    }
    return out
  }
  const m1 = matches<readonly [ ...Hole<"head">, unknown ]>(label)
  if (m1) {
    return labelOptionalElement(element, m1.head)
  }
  return [element]
}
/* compiles to:
 * export type labelOptionalElement<element, label extends readonly unknown[]> =
 *   label extends readonly [unknown] ? { [K in keyof label]?: element }
 *   : label extends readonly [...infer head, unknown] ? labelOptionalElement<element, head>
 *   : [element]
 */

// ✓ setIndex: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {readonly unknown[]} arr
 * @param {(typeof arr)[number]} to
 */
export function setIndex(arr, i: number, to): any {
  if (matches<(typeof arr)[number][]>(arr)) {
    return _setIndex(arr, i, to, [])
  }
  return Readonly(_setIndex(arr, i, to, []))
}
/* compiles to:
 * export type setIndex<
 *   arr extends readonly unknown[],
 *   i extends number,
 *   to extends arr[number]
 * > =
 *   arr extends arr[number][] ? _setIndex<arr, i, to, []> : Readonly<_setIndex<arr, i, to, []>>
 */

// ✓ _setIndex: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {readonly unknown[]} arr
 * @param {(typeof arr)[number]} to
 * @param {(typeof arr)[number][]} result
 */
export function _setIndex(arr, i: number, to, result): any {
  let arr_ = arr
  let result_ = result
  while (true) {
    const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(arr_)
    if (!m1) {
      break
    }
    arr_ = m1.tail
    result_.push(matches<typeof i>(result_['length']) ? to : m1.head)
  }
  return result_
}
/* compiles to:
 * export type _setIndex<
 *   arr extends readonly unknown[],
 *   i extends number,
 *   to extends arr[number],
 *   result extends arr[number][]
 * > =
 *   _setIndex__loop<arr, result, i, to>
 * type _setIndex__loop<
 *   Arr_ extends readonly unknown[],
 *   Result_ extends arr[number][],
 *   I extends number,
 *   To extends arr[number]
 * > =
 *   Arr_ extends readonly [infer head, ...infer tail]
 *     ? _setIndex__loop<tail, [...Result_, Result_['length'] extends I ? To : head], I, To>
 *     : Result_
 */

// ✓ max: verified type-identical to the original
/* @scripttype preserveParamNames */
export function max(n: number): any {
  return _max(n, exponentials)
}
/* compiles to:
 * export type max<n extends number> = _max<n, exponentials>
 */

// ✗ _max: the ScriptType does not itself typecheck as TypeScript
//   _max.st.ts(3:17) TS2571: Object is of type 'unknown'.
/* @scripttype preserveParamNames */
export function _max(n: number, filtered: unknown[]): any {
  if (`${n}` in filtered[0]) {
    return _max(n, tailOf(filtered))
  }
  return filtered
}
/* compiles to:
 * export type _max<n extends number, filtered extends unknown[]> =
 *   `${n}` extends keyof filtered[0] ? _max<n, tailOf<filtered>> : filtered
 */

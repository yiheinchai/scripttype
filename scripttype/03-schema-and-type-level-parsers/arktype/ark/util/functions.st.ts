/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/functions.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 3 covered, 2 raw, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✗ Fn: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Fn(args: readonly any[] = readonlyArrayOf(any), returns = unknown) {
  return fnType([args], returns)
}
/* compiles to:
 * export type Fn<args extends readonly any[] = readonly any[], returns = unknown> = (a0: args) => returns
 */

// ✓ Thunk: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Thunk(ret = unknown) {
  return fnType([], ret)
}
/* compiles to:
 * export type Thunk<ret = unknown> = () => ret
 */

// ✓ thunkable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function thunkable(t) {
  return t | Thunk(t)
}
/* compiles to:
 * export type thunkable<t> = t | Thunk<t>
 */

// ✓ CallableOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallableOptions(attachments: object) {
  return { attach: optional(attachments), bind: optional(object) }
}
/* compiles to:
 * export type CallableOptions<attachments extends object> = {
 *   attach?: attachments
 *   bind?: object
 * }
 */

// ✗ GuardablePredicate: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function GuardablePredicate(input = unknown, narrowed: typeof input = input) {
  return fnType([input], raw('In is narrowed')) | fnType([input], boolean)
}

// ✗ TypeGuard: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function TypeGuard(input = unknown, narrowed: typeof input = input) {
  return fnType([input], raw('In is narrowed'))
}

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/functions.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 6 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Fn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Fn(args: readonly any[] = readonlyArrayOf(any), returns = unknown): any {
  return fnType([...args], returns)
}
/* compiles to:
 * export type Fn<args extends readonly any[] = readonly any[], returns = unknown> =
 *   (...a0: args) => returns
 */

// ✓ Thunk: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Thunk(ret = unknown): any {
  return fnType([], ret)
}
/* compiles to:
 * export type Thunk<ret = unknown> = () => ret
 */

// ✓ thunkable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function thunkable(t): any {
  return t | Thunk(t)
}
/* compiles to:
 * export type thunkable<t> = t | Thunk<t>
 */

// ✓ CallableOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallableOptions(attachments: object): any {
  return { attach: optional(attachments), bind: optional(object) }
}
/* compiles to:
 * export type CallableOptions<attachments extends object> = {
 *   attach?: attachments
 *   bind?: object
 * }
 */

// ✓ GuardablePredicate: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {typeof input} narrowed
 */
export function GuardablePredicate(input = unknown, narrowed = input): any {
  return fnType([input], paramIs(0, narrowed)) | fnType([input], boolean)
}
/* compiles to:
 * export type GuardablePredicate<input = unknown, narrowed extends input = input> =
 *   ((a0: input) => a0 is narrowed) | ((a0: input) => boolean)
 */

// ✓ TypeGuard: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {typeof input} narrowed
 */
export function TypeGuard(input = unknown, narrowed = input): any {
  return fnType([input], paramIs(0, narrowed))
}
/* compiles to:
 * export type TypeGuard<input = unknown, narrowed extends input = input> = (a0: input) => a0 is narrowed
 */

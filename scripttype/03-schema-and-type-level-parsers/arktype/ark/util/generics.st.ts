/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/generics.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 20 type-level aliases: 19 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Primitive } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/domain.js'
import type { ErrorMessage } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/errors.js'
import type { unionToTuple } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/unionToTuple.js'
declare namespace m1 {
  export type base<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ErrorMessage: any
declare const Primitive: any
declare const PropertyKey: any
declare const _: any
declare const brand: any
declare const head: any
declare const unionToTuple: any
type Brand<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type _<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type brand<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ show: verified type-identical to the original
/* @scripttype preserveParamNames */
export function show(t): any {
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = t[k]
  }
  return out & unknown
}
/* compiles to:
 * export type show<t> = { [K in keyof t]: t[K] } & unknown
 */

// ✓ evaluate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function evaluate(t): any {
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = t[k]
  }
  return out & unknown
}
/* compiles to:
 * export type evaluate<t> = { [K in keyof t]: t[K] } & unknown
 */

// ✓ get: verified type-identical to the original
/* @scripttype preserveParamNames */
export function get(t, k: PropertyKey): any {
  return t[merge(k, keyof(t))]
}
/* compiles to:
 * export type get<t, k extends PropertyKey> = t[k & keyof t]
 */

// ✓ exact: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} t
 * @param {object} u
 */
export function exact(t, u): any {
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = k in u ? conform(t[k], u[k]) : never
  }
  return out
}
/* compiles to:
 * export type exact<t extends object, u extends object> = {
 *   [K in keyof t]: K extends keyof u ? conform<t[K], u[K]> : never
 * }
 */

// ✓ exactMessageOnError: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} t
 * @param {object} u
 */
export function exactMessageOnError(t, u): any {
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = k in u ? conform(t[k], u[k]) : ErrorMessage(`'${merge(k, string)}' is not a valid key`)
  }
  return out & u
}
/* compiles to:
 * export type exactMessageOnError<t extends object, u extends object> =
 *   & {
 *       [K in keyof t]: K extends keyof u
 *         ? conform<t[K], u[K]>
 *         : ErrorMessage<`'${K & string}' is not a valid key`>
 *     }
 *   & u
 */

// ✗ promisable: the ScriptType does not itself typecheck as TypeScript
//   promisable.st.ts(3:14) TS2347: Untyped function calls may not accept type arguments.
/* @scripttype preserveParamNames */
export function promisable(t): any {
  return t | t<Promise<typeof t>>()
}
/* compiles to:
 * export type promisable<t> = t | Promise<t>
 */

// ✓ leftIfEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function leftIfEqual(l, r): any {
  if (matches<[ typeof r, typeof l ]>([l, r])) {
    return l
  }
  return r
}
/* compiles to:
 * export type leftIfEqual<l, r> = [l, r] extends [r, l] ? l : r
 */

// ✓ andPreserveUnknown: verified type-identical to the original
/* @scripttype preserveParamNames */
export function andPreserveUnknown(l, r): any {
  if (matches<typeof l & typeof r>(unknown)) {
    return unknown
  }
  return show(merge(l, r))
}
/* compiles to:
 * export type andPreserveUnknown<l, r> = unknown extends l & r ? unknown : show<l & r>
 */

// ✓ conform: verified type-identical to the original
/* @scripttype preserveParamNames */
export function conform(t, base): any {
  if (matches<typeof base>(t)) {
    return t
  }
  return base
}
/* compiles to:
 * export type conform<t, base> = t extends base ? t : base
 */

// ✓ equals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function equals(l, r): any {
  if (matches<[ typeof r, typeof l ]>([l, r])) {
    return true
  }
  return false
}
/* compiles to:
 * export type equals<l, r> = [l, r] extends [r, l] ? true : false
 */

// ✓ exactEquals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function exactEquals(l, r): any {
  if (matches<<_>() => _ extends typeof r ? 1 : 2>(genericFnType(['_'], [], matches<typeof l>(_) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type exactEquals<l, r> =
 *   (<_>() => _ extends l ? 1 : 2) extends <_>() => _ extends r ? 1 : 2 ? true : false
 */

// ✓ Brand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Brand(t = unknown, id = unknown): any {
  return merge(t, { [brand]: readonlyProp([t, id]) })
}
/* compiles to:
 * export type Brand<t = unknown, id = unknown> = t & { readonly [brand]: [t, id] }
 */

// ✓ unbrand: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unbrand(t): any {
  const m1 = matches<Brand<Hole<"base">, string>>(t)
  if (m1) {
    return m1.base
  }
  return never
}
/* compiles to:
 * export type unbrand<t> = t extends Brand<infer base, string> ? base : never
 */

// ✓ satisfy: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {typeof base} t
 */
export function satisfy(base, t): any {
  return t
}
/* compiles to:
 * export type satisfy<base, t extends base> = t
 */

// ✓ defined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function defined(t): any {
  return merge(t, anyOf({}, Null))
}
/* compiles to:
 * export type defined<t> = t & ({} | null)
 */

// ✓ autocomplete: verified type-identical to the original
/* @scripttype preserveParamNames */
export function autocomplete(suggestions: string): any {
  return anyOf(suggestions, merge(string, {}))
}
/* compiles to:
 * export type autocomplete<suggestions extends string> = suggestions | string & {}
 */

// ✓ widen: verified type-identical to the original
/* @scripttype preserveParamNames */
export function widen(t, supertypes): any {
  return collectWidenedType(t, unionToTuple(supertypes))
}
/* compiles to:
 * export type widen<t, supertypes> = collectWidenedType<t, unionToTuple<supertypes>>
 */

// ✓ collectWidenedType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function collectWidenedType(t, remaining: unknown[]): any {
  let result = never
  let remaining_ = remaining
  while (true) {
    const m1 = matches<[ Hole<"head">, ...Hole<"tail"> ]>(remaining_)
    if (!m1) {
      break
    }
    remaining_ = m1.tail
    result = matches<typeof m1.head>(t) ? (result | m1.head) : result
  }
  return result
}
/* compiles to:
 * export type collectWidenedType<t, remaining extends unknown[]> = CollectWidenedType__loop<
 *   remaining,
 *   never,
 *   t
 * >
 * type CollectWidenedType__loop<Remaining_ extends unknown[], Result, T> =
 *   Remaining_ extends [infer head, ...infer tail]
 *     ? CollectWidenedType__loop<tail, T extends head ? Result | head : Result, T>
 *     : Result
 */

// ✓ narrowTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function narrowTuple(t: readonly unknown[]): any {
  const m1 = matches<readonly [ Hole<"head">, ...Hole<"tail"> ]>(t)
  if (m1) {
    return asReadonly([m1.head, ...narrowTuple(m1.tail)])
  }
  return []
}
/* compiles to:
 * export type narrowTuple<t extends readonly unknown[]> =
 *   t extends readonly [infer head, ...infer tail] ? readonly [head, ...narrowTuple<tail>] : []
 */

// ✓ narrow: verified type-identical to the original
/* @scripttype preserveParamNames */
export function narrow(t): any {
  if (matches<Primitive>(t)) {
    return t
  }
  if (matches<readonly unknown[]>(t)) {
    return narrowTuple(t)
  }
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = narrow(t[k])
  }
  return out
}
/* compiles to:
 * export type narrow<t> =
 *   t extends Primitive ? t
 *   : t extends readonly unknown[] ? narrowTuple<t>
 *   : { [K in keyof t]: narrow<t[K]> }
 */

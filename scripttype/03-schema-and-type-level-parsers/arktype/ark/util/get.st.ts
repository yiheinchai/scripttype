/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/get.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { keyOf } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/records.js'
declare namespace m1 {
  export type head<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type n<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type tail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const keyOf: any
// ✓ getKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function getKey(o, k): any {
  if (k in o) {
    return o[k]
  }
  const m1 = matches<`${Hole<"n", number & keyof typeof o>}`>(k)
  if (m1) {
    return o[m1.n]
  }
  return never
}
/* compiles to:
 * export type getKey<o, k> =
 *   k extends keyof o ? o[k] : k extends `${infer n extends number & keyof o}` ? o[n] : never
 */

// ✓ getPath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function getPath(o, path: string): any {
  let o_ = o
  let path_ = path
  while (true) {
    const m1 = matches<`${Hole<"head">}.${Hole<"tail">}`>(path_)
    if (!m1) {
      break
    }
    o_ = getKey(o_, m1.head)
    path_ = m1.tail
  }
  return getKey(o_, path_)
}
/* compiles to:
 * export type getPath<o, path extends string> = GetPath__loop<o, path>
 * type GetPath__loop<O_, Path_ extends string> =
 *   Path_ extends `${infer head}.${infer tail}`
 *     ? GetPath__loop<getKey<O_, head>, tail>
 *     : getKey<O_, Path_>
 */

// ✓ validatePath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function validatePath(o, path: string, prefix: string = ''): any {
  const m1 = matches<`${Hole<"head">}.${Hole<"tail">}`>(path)
  if (m1) {
    if (matches<keyOf<typeof o>>(m1.head)) {
      return validatePath(getKey(o, m1.head), m1.tail, `${prefix}${m1.head}.`)
    }
    return `Key '${m1.head}' is not valid following '${prefix}'`
  }
  const out = emptyObject
  for (const k in keySet(keyOf(o))) {
    out[k] = matches<`${typeof path}${string}`>(k) ? `${prefix}${k}` : never
  }
  return out[keyOf(o)]
}
/* compiles to:
 * export type validatePath<o, path extends string, prefix extends string = ''> =
 *   path extends `${infer head}.${infer tail}`
 *     ? head extends keyOf<o>
 *       ? validatePath<getKey<o, head>, tail, `${prefix}${head}.`>
 *       : `Key '${head}' is not valid following '${prefix}'`
 *     : { [K in keyOf<o>]: K extends `${path}${string}` ? `${prefix}${K}` : never }[keyOf<o>]
 */

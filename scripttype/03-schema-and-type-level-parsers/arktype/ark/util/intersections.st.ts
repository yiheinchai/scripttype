/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/intersections.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 3 covered, 1 raw, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AndPreserveUnknown: any
declare const Hkt: any
declare const SequenceIntersectionKind: any
declare const array: any
declare const domainOf: any
declare const k: any
declare const propValueOf: any
declare const requiredKeyOf: any
type AndPreserveUnknown<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Hkt<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SequenceIntersectionKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type domainOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type k<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type overlaps<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type propValueOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type requiredKeyOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ intersectArrays: verified type-identical to the original
/* @scripttype preserveParamNames */
export function intersectArrays(l: array, r: array, operator: Hkt = AndPreserveUnknown) {
  return intersectSequences(l, r, [], [], operator, 'array')
}
/* compiles to:
 * export type intersectArrays<l extends array, r extends array, operator extends Hkt = AndPreserveUnknown> = intersectSequences<l, r, [], [], operator, 'array'>
 */

// ✓ intersectParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function intersectParameters(l: array, r: array, operator: Hkt = AndPreserveUnknown) {
  return intersectSequences(l, r, [], [], operator, 'parameters')
}
/* compiles to:
 * export type intersectParameters<l extends array, r extends array, operator extends Hkt = AndPreserveUnknown> = intersectSequences<l, r, [], [], operator, 'parameters'>
 */

// ✗ intersectSequences: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function intersectSequences(l: array, r: array, acc: array, postfix: array, operation: Hkt, kind: SequenceIntersectionKind) {
  if (matches<readonly [ ]>(l)) {
    if (matches<"array">(kind)) {
      if (matches<typeof r>([])) {
        return [...acc, ...postfix]
      }
      return never
    }
    return [...acc, ...r, ...postfix]
  }
  if (matches<readonly [ ]>(r)) {
    if (matches<"array">(kind)) {
      if (matches<typeof l>([])) {
        return [...acc, ...postfix]
      }
      return never
    }
    return [...acc, ...l, ...postfix]
  }
  const m1 = matches<([ readonly [ (Hole<"lHead">)?, ...Hole<"lTail"> ], readonly [ (Hole<"rHead">)?, ...Hole<"rTail"> ] ])>([l, r])
  if (m1) {
    if (matches<[ keyof typeof l | keyof typeof r, (typeof l)[0], (typeof r)[0] ]>(['0', m1.lHead, m1.rHead])) {
      return intersectSequences(m1.lTail, m1.rTail, matches<[ typeof l, typeof r ]>([[], []]) ? [...acc, raw('Hkt.apply<operation, [ lHead, rHead ]>?')] : [...acc, Hkt.apply(operation, [m1.lHead, m1.rHead])], postfix, operation, kind)
    }
    const m2 = matches<readonly [ ...Hole<"lInit">, Hole<"lLast"> ]>(l)
    if (m2) {
      const m3 = matches<readonly [ ...Hole<"rInit">, Hole<"rLast"> ]>(r)
      if (m3) {
        return intersectSequences(m2.lInit, m3.rInit, acc, [Hkt.apply(operation, [m2.lLast, m3.rLast]), ...postfix], operation, kind)
      }
      return intersectSequences(m2.lInit, r, acc, [Hkt.apply(operation, [m2.lLast, r[number]]), ...postfix], operation, kind)
    }
    const m4 = matches<readonly [ ...Hole<"rInit">, Hole<"rLast"> ]>(r)
    if (m4) {
      return intersectSequences(l, m4.rInit, acc, [Hkt.apply(operation, [l[number], m4.rLast]), ...postfix], operation, kind)
    }
    return [...acc, ...arrayOf(Hkt.apply(operation, [m1.lHead, m1.rHead])), ...postfix]
  }
  return never
}

// ✓ isDisjoint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function isDisjoint(l, r) {
  if (matches<true>(overlaps(l, r))) {
    return false
  }
  return true
}
/* compiles to:
 * export type isDisjoint<l, r> = overlaps<l, r> extends true ? false : true
 */

// ✗ overlaps: does not compile yet
//   'l' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function overlaps(l, r) {
  if (matches<never>(l & r)) {
    return false
  }
  if (matches<never>(domainOf(l) & domainOf(r))) {
    return false
  }
  if (matches<[ object, object ]>([l, r])) {
    if (matches<(propValueOf<{ [k in Extract<keyof typeof l & keyof typeof r, requiredKeyOf<typeof l> | requiredKeyOf<typeof r>>]: overlaps<(typeof l)[k], (typeof r)[k]>; }>)>(false)) {
      return false
    }
    return true
  }
  return true
}
/* compiles to:
 * export type overlaps<l, r> = l & r extends never ? false : domainOf<l> & domainOf<r> extends never ? false : [l, r] extends [object, object] ? false extends propValueOf<{ [k in Extract<keyof typeof l & keyof typeof r, requiredKeyOf<typeof l> | requiredKeyOf<typeof r>>]: overlaps<(typeof l)[k], (typeof r)[k]>; }> ? false : true : true
 */

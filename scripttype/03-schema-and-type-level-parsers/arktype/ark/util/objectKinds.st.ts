/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/objectKinds.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 6 covered, 2 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltinObjectKind: any
declare const DescribeOptions: any
declare const Fn: any
declare const GlobalName: any
declare const builtinConstructors: any
declare const domainDescriptions: any
declare const domainOf: any
declare const objectKindDescriptions: any
type BuiltinObjectKind<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constructor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DescribeOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Fn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GlobalName<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type builtinConstructors<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type domainDescriptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type domainOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type objectKindDescriptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ instantiateConstructors: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function instantiateConstructors(kind: BuiltinObjectKind) {
  const out = emptyObject
  for (const k in keySet(kind)) {
    out[k] = matches<GlobalName>(k) ? InstanceType(raw('typeof globalThis')[k]) : (matches<GlobalName>(`${k}Constructor`) ? InstanceType(raw('typeof globalThis')[`${k}Constructor`]) : never)
  }
  return out
}

// ✓ objectKindOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function objectKindOf(data: object) {
  if (matches<typeof data>(object)) {
    return anyOf(keyof(builtinConstructors), Undefined)
  }
  if (matches<Fn>(data)) {
    return 'Function'
  }
  if (matches<never>(instantiableObjectKind(data))) {
    return Undefined
  }
  return instantiableObjectKind(data)
}
/* compiles to:
 * export type objectKindOf<data extends object> = object extends data ? keyof builtinConstructors | undefined : data extends Fn ? 'Function' : instantiableObjectKind<data> extends never ? undefined : instantiableObjectKind<data>
 */

// ✓ describeObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function describeObject(o: object, opts: DescribeOptions = {}) {
  if (matches<string>(objectKindOf(o))) {
    if (matches<[ true ]>([opts['includeArticles']])) {
      return objectKindDescriptions[objectKindOf(o)]
    }
    return objectKindOf(o)
  }
  if (matches<[ true ]>([opts['includeArticles']])) {
    return domainDescriptions['object']
  }
  return 'object'
}
/* compiles to:
 * export type describeObject<o extends object, opts extends DescribeOptions = {}> = objectKindOf<o> extends string ? [opts['includeArticles']] extends [true] ? objectKindDescriptions[objectKindOf<o>] : objectKindOf<o> : [opts['includeArticles']] extends [true] ? domainDescriptions['object'] : 'object'
 */

// ✓ instantiableObjectKind: verified type-identical to the original
/* @scripttype preserveParamNames */
export function instantiableObjectKind(data: object) {
  const out = emptyObject
  for (const kind in keyof(builtinConstructors)) {
    out[kind] = matches<(InstanceType<builtinConstructors[typeof kind]>)>(data) ? kind : never
  }
  return out[keyof(builtinConstructors)]
}
/* compiles to:
 * export type instantiableObjectKind<data extends object> = { [Kind in keyof builtinConstructors]: data extends InstanceType<builtinConstructors[Kind]> ? Kind : never }[keyof builtinConstructors]
 */

// ✓ objectKindOrDomainOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function objectKindOrDomainOf(data) {
  if (matches<object>(data)) {
    if (matches<undefined>(objectKindOf(data))) {
      return 'object'
    }
    return objectKindOf(data)
  }
  return domainOf(data)
}
/* compiles to:
 * export type objectKindOrDomainOf<data> = data extends object ? objectKindOf<data> extends undefined ? 'object' : objectKindOf<data> : domainOf<data>
 */

// ✗ Constructor: uses raw() — language gap, does not count as covered
//   gap: type node ConstructorType
/* @scripttype preserveParamNames */
export function Constructor(instance = {}) {
  return raw('abstract new (...args: never[]) => instance')
}

// ✓ instanceOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function instanceOf(constructor) {
  const m1 = matches<Constructor<Hole<"instance">>>(constructor)
  if (m1) {
    return m1.instance
  }
  return never
}
/* compiles to:
 * export type instanceOf<constructor> = constructor extends Constructor<infer instance> ? instance : never
 */

// ✓ normalizedKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function normalizedKeyOf(t) {
  const m1 = matches<Hole<"k">>(keyof(t))
  if (m1) {
    if (matches<number>(m1.k)) {
      return `${m1.k}`
    }
    return m1.k
  }
  return never
}
/* compiles to:
 * export type normalizedKeyOf<t> = keyof t extends infer k ? k extends number ? `${k}` : k : never
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/primitive.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { SerializablePrimitive } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/primitive.js'
declare const SerializablePrimitive: any
// ✓ SerializedString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SerializedString(value: string = string): any {
  return `"${value}"`
}
/* compiles to:
 * export type SerializedString<value extends string = string> = `"${value}"`
 */

// ✓ serializePrimitive: verified type-identical to the original
/* @scripttype preserveParamNames */
export function serializePrimitive(value: SerializablePrimitive): any {
  if (typeof value === 'string') {
    return `"${value}"`
  }
  if (typeof value === 'bigint') {
    return `${value}n`
  }
  return `${value}`
}
/* compiles to:
 * export type serializePrimitive<value extends SerializablePrimitive> =
 *   value extends string ? `"${value}"` : value extends bigint ? `${value}n` : `${value}`
 */

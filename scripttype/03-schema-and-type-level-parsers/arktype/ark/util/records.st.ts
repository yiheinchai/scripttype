/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/records.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 34 type-level aliases: 29 covered, 3 typecheck-error, 1 compile-error, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { array } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/arrays.js'
import type { Primitive } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/domain.js'
import type { Fn } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/functions.js'
import type { defined, show } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/generics.js'
import type { intersectUnion } from '../../../../../03-schema-and-type-level-parsers/arktype/ark/util/unionToTuple.js'
declare namespace m1 {
  export type distributed<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type k<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Fn: any
declare const Primitive: any
declare const PropertyKey: any
declare const TypedPropertyDescriptor: any
declare const array: any
declare const defined: any
declare const intersectUnion: any
declare const show: any
type Entry<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TypedPropertyDescriptor<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Dict: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Dict(k: string = string, v = unknown): any {
  const out = emptyObject
  for (const _ in keySet(k)) {
    out[_] = readonlyProp(v)
  }
  return out
}
/* compiles to:
 * export type Dict<k extends string = string, v = unknown> = { readonly [_ in k]: v }
 */

// ✓ dict: verified type-identical to the original
/* @scripttype preserveParamNames */
export function dict(v = unknown, k: string = string): any {
  const out = emptyObject
  for (const _ in keySet(k)) {
    out[_] = v
  }
  return out
}
/* compiles to:
 * export type dict<v = unknown, k extends string = string> = { [_ in k]: v }
 */

// ✓ propwiseXor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function propwiseXor(a, b): any {
  const out = emptyObject
  for (const k in keyof(b)) {
    out[k] = optional(Undefined)
  }
  const out2 = emptyObject
  for (const k in keyof(a)) {
    out2[k] = optional(Undefined)
  }
  return show(merge(a, out)) | show(merge(b, out2))
}
/* compiles to:
 * export type propwiseXor<a, b> =
 *   show<a & { [K in keyof b]?: undefined }> | show<b & { [K1 in keyof a]?: undefined }>
 */

// ✓ unionToPropwiseXor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unionToPropwiseXor(props: object, branchKey: PropertyKey = keyof(intersectUnion(props))): any {
  const m1 = matches<Hole<"distributed">>(props)
  if (m1) {
    const out = emptyObject
    for (const k in keySet(branchKey)) {
      out[k] = optional(k in m1.distributed ? unknown : Undefined)
    }
    return show(merge(m1.distributed, out))
  }
  return never
}
/* compiles to:
 * export type unionToPropwiseXor<
 *   props extends object,
 *   branchKey extends PropertyKey = keyof intersectUnion<props>
 * > =
 *   props extends infer distributed
 *     ? show<
 *       distributed & { [K in branchKey]?: K extends keyof distributed ? unknown : undefined }
 *     >
 *     : never
 */

// ✓ requireKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof o} key
 */
export function requireKeys(o, key): any {
  const out = emptyObject
  for (const requiredKey in keySet(key)) {
    out[requiredKey] = required(defined(o[requiredKey]))
  }
  return o & out
}
/* compiles to:
 * export type requireKeys<o, key extends keyof o> =
 *   o & { [RequiredKey in key]-?: defined<o[RequiredKey]> }
 */

// ✓ require: verified type-identical to the original
/* @scripttype preserveParamNames */
export function require(o, maxDepth: number = 1): any {
  return _require(o, [], maxDepth)
}
/* compiles to:
 * export type require<o, maxDepth extends number = 1> = _require<o, [], maxDepth>
 */

// ✓ _require: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _require(o, depth: 1[], maxDepth: number): any {
  if (matches<typeof maxDepth>(depth['length'])) {
    return o
  }
  if (matches<object>(o)) {
    if (matches<Fn>(o)) {
      return o
    }
    const out = emptyObject
    for (const k in keyof(o)) {
      out[k] = required(_require(o[k], [...depth, 1], maxDepth))
    }
    return out
  }
  return o
}
/* compiles to:
 * export type _require<o, depth extends 1[], maxDepth extends number> =
 *   depth['length'] extends maxDepth ? o
 *   : o extends object
 *     ? o extends Fn ? o : { [K in keyof o]-?: _require<o[K], [...depth, 1], maxDepth> }
 *   : o
 */

// ✗ PartialRecord: the ScriptType does not itself typecheck as TypeScript
//   PartialRecord.st.ts(2:48) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function PartialRecord(k: PropertyKey = PropertyKey, v = unknown): any {
  const out = emptyObject
  for (const _ in keySet(k)) {
    out[_] = optional(v)
  }
  return out
}
/* compiles to:
 * export type PartialRecord<k extends PropertyKey = PropertyKey, v = unknown> = { [_ in k]?: v }
 */

// ✓ isSafelyMappable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function isSafelyMappable(t): any {
  const out = emptyObject
  for (const k in keyof(t)) {
    out[k] = t[k]
  }
  if (matches<typeof t>(out)) {
    return true
  }
  return false
}
/* compiles to:
 * export type isSafelyMappable<t> = { [K in keyof t]: t[K] } extends t ? true : false
 */

// ✓ KeySet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KeySet(key: string = string): any {
  const out = emptyObject
  for (const _ in keySet(key)) {
    out[_] = readonlyProp(optional(1))
  }
  return out
}
/* compiles to:
 * export type KeySet<key extends string = string> = { readonly [_ in key]?: 1 }
 */

// ✓ keySetOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function keySetOf(o: object): any {
  return KeySet(Extract(keyof(o), string))
}
/* compiles to:
 * export type keySetOf<o extends object> = KeySet<Extract<keyof o, string>>
 */

// ✓ mutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mutable(o, maxDepth: number = 1): any {
  return _mutable(o, [], maxDepth)
}
/* compiles to:
 * export type mutable<o, maxDepth extends number = 1> = _mutable<o, [], maxDepth>
 */

// ✓ _mutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _mutable(o, depth: 1[], maxDepth: number): any {
  if (matches<typeof maxDepth>(depth['length'])) {
    return o
  }
  if (matches<Primitive>(o)) {
    return o
  }
  if (matches<Fn>(o)) {
    return o
  }
  const out = emptyObject
  for (const k in keyof(o)) {
    out[k] = mutable(_mutable(o[k], [...depth, 1], maxDepth))
  }
  return out
}
/* compiles to:
 * export type _mutable<o, depth extends 1[], maxDepth extends number> =
 *   depth['length'] extends maxDepth ? o
 *   : o extends Primitive ? o
 *   : o extends Fn ? o
 *   : { -readonly [K in keyof o]: _mutable<o[K], [...depth, 1], maxDepth> }
 */

// ✓ entryOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function entryOf(o): any {
  const out = emptyObject
  for (const k in keyof(o)) {
    out[k] = required([k, merge(o[k], anyOf({}, Null))])
  }
  return out[matches<readonly unknown[]>(o) ? (keyof(o) & number) : keyof(o)] & unknown
}
/* compiles to:
 * export type entryOf<o> =
 *   & { [K in keyof o]-?: [K, o[K] & ({} | null)] }[o extends readonly unknown[] ? keyof o & number : keyof o]
 *   & unknown
 */

// ✓ entriesOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function entriesOf(o: object): any {
  return arrayOf(entryOf(o))
}
/* compiles to:
 * export type entriesOf<o extends object> = entryOf<o>[]
 */

// ✗ Entry: the ScriptType does not itself typecheck as TypeScript
//   Entry.st.ts(2:42) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function Entry(key: PropertyKey = PropertyKey, value = unknown): any {
  return asReadonly([key, value])
}
/* compiles to:
 * export type Entry<key extends PropertyKey = PropertyKey, value = unknown> =
 *   readonly [key, value]
 */

// ✓ fromEntries: verified type-identical to the original
/* @scripttype preserveParamNames */
export function fromEntries(entries: readonly Entry[]): any {
  const out = emptyObject
  for (const entry in keySet(entries[number])) {
    out[entry[0]] = entry[1]
  }
  return show(out)
}
/* compiles to:
 * export type fromEntries<entries extends readonly Entry[]> = show<
 *   { [Entry in entries[number] as Entry[0]]: Entry[1] }
 * >
 */

// ✓ keyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function keyOf(o): any {
  if (matches<array>(o)) {
    if (matches<(typeof o)["length"]>(number)) {
      return `${number}`
    }
    return merge(keyof(o), `${number}`)
  }
  const m1 = matches<Hole<"k">>(keyof(o))
  if (m1) {
    if (typeof m1.k === 'string') {
      return m1.k
    }
    if (typeof m1.k === 'number') {
      return `${m1.k}`
    }
    return never
  }
  return never
}
/* compiles to:
 * export type keyOf<o> =
 *   o extends array ? number extends o['length'] ? `${number}` : keyof o & `${number}`
 *   : keyof o extends infer k ? k extends string ? k : k extends number ? `${k}` : never
 *   : never
 */

// ✓ unionKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function unionKeyOf(t): any {
  if (matches<unknown>(t)) {
    return keyof(t)
  }
  return never
}
/* compiles to:
 * export type unionKeyOf<t> = t extends unknown ? keyof t : never
 */

// ✓ extractKeyed: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} o
 * @param {unionKeyOf<typeof o>} k
 */
export function extractKeyed(o, k): any {
  const out = emptyObject
  for (const _ in keySet(k)) {
    out[_] = optional(unknown)
  }
  return Extract(o, out)
}
/* compiles to:
 * export type extractKeyed<o extends object, k extends unionKeyOf<o>> = Extract<
 *   o,
 *   { [_ in k]?: unknown }
 * >
 */

// ✓ extractDefinedKey: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} o
 * @param {unionKeyOf<typeof o>} k
 */
export function extractDefinedKey(o, k): any {
  const out = emptyObject
  for (const _ in keySet(k)) {
    out[_] = anyOf({}, Null)
  }
  return show(merge(extractKeyed(o, k), out))
}
/* compiles to:
 * export type extractDefinedKey<o extends object, k extends unionKeyOf<o>> = show<
 *   extractKeyed<o, k> & { [_ in k]: {} | null }
 * >
 */

// ✗ requiredKeyOf: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function requiredKeyOf(o): any {
  const m1 = matches<Hole<"k">>(keyof(o))
  if (m1) {
    if (m1.k in o) {
      if (matches<{ [_ in typeof m1.k]-?: (typeof o)[typeof m1.k]; }>(o)) {
        return m1.k
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type requiredKeyOf<o> =
 *   keyof o extends infer k
 *     ? k extends keyof o
 *       ? o extends { [_ in typeof m1.k]-?: (o)[typeof m1.k]; } ? k : never
 *       : never
 *     : never
 */

// ✓ optionalKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function optionalKeyOf(o): any {
  return Exclude(keyof(o), requiredKeyOf(o))
}
/* compiles to:
 * export type optionalKeyOf<o> = Exclude<keyof o, requiredKeyOf<o>>
 */

// ✓ merge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function merge(base, props): any {
  if (matches<unknown>(base)) {
    if (matches<unknown>(props)) {
      if (matches<never>(keyof(base) & keyof(props))) {
        return show(merge(base, props))
      }
      return show(merge(omit(base, merge(keyof(props), keyof(base))), props))
    }
    return never
  }
  return never
}
/* compiles to:
 * export type merge<base, props> =
 *   base extends unknown
 *     ? props extends unknown
 *       ? keyof base & keyof props extends never
 *         ? show<base & props>
 *         : show<Omit<base, keyof props & keyof base> & props>
 *       : never
 *     : never
 */

// ✗ override: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
/**
 * @param {{ [k in keyof typeof base]?: unknown; }} merged
 */
export function override(base, merged): any {
  return merge(base, merged)
}
/* compiles to:
 * export type override<base, merged extends { [k in keyof base]?: unknown; }> = base & merged
 */

// ✓ propValueOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function propValueOf(o): any {
  return o[keyof(o)]
}
/* compiles to:
 * export type propValueOf<o> = o[keyof o]
 */

// ✓ pick: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof o} key
 */
export function pick(o, key): any {
  if (matches<unknown>(o)) {
    const out = emptyObject
    for (const k in keyof(o)) {
      out[matches<typeof key>(k) ? k : never] = o[k]
    }
    return out
  }
  return never
}
/* compiles to:
 * export type pick<o, key extends keyof o> =
 *   o extends unknown ? { [K in keyof o as K extends key ? K : never]: o[K] } : never
 */

// ✓ omit: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof o} key
 */
export function omit(o, key): any {
  const out = emptyObject
  for (const k in keyof(o)) {
    out[matches<typeof key>(k) ? never : k] = o[k]
  }
  return out
}
/* compiles to:
 * export type omit<o, key extends keyof o> = { [K in keyof o as K extends key ? never : K]: o[K] }
 */

// ✓ ifEmptyObjectLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ifEmptyObjectLiteral(t, onTrue = true, onFalse = false): any {
  if (matches<[ typeof t | null | undefined, never ]>([unknown, merge(t, anyOf(Null, Undefined))])) {
    return onTrue
  }
  return onFalse
}
/* compiles to:
 * export type ifEmptyObjectLiteral<t, onTrue = true, onFalse = false> =
 *   [unknown, t & (null | undefined)] extends [t | null | undefined, never] ? onTrue : onFalse
 */

// ✓ invert: verified type-identical to the original
/* @scripttype preserveParamNames */
export function invert__v1(t, k): any {
  const out = emptyObject
  for (const k2 in keyof(t)) {
    out[k2] = matches<typeof k>(t[k2]) ? k2 : never
  }
  return out
}
/**
 * @param {Record<PropertyKey, PropertyKey>} t
 */
export function invert(t): any {
  const out = emptyObject
  for (const k in keySet(t[keyof(t)])) {
    out[k] = invert__v1(t, k)[keyof(t)]
  }
  return out & unknown
}
/* compiles to:
 * export type invert__v1<t, k> = { [K2 in keyof t]: t[K2] extends k ? K2 : never }
 * export type invert<t extends Record<PropertyKey, PropertyKey>> =
 *   { [K in t[keyof t]]: invert__v1<t, K>[keyof t] } & unknown
 */

// ✓ withJsDoc: verified type-identical to the original
/* @scripttype preserveParamNames */
export function withJsDoc(o, jsDocSource): any {
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(o), keyof(jsDocSource)))) {
    out[k] = o[k]
  }
  return show(keyof(o) in jsDocSource ? (keyof(jsDocSource) in o ? _withJsDoc(o, jsDocSource) : Pick(_withJsDoc(o, jsDocSource), merge(keyof(o), keyof(jsDocSource)))) : (Pick(_withJsDoc(o, jsDocSource), merge(keyof(o), keyof(jsDocSource))) & out))
}
/* compiles to:
 * export type withJsDoc<o, jsDocSource> = show<
 *   keyof o extends keyof jsDocSource
 *     ? keyof jsDocSource extends keyof o
 *       ? _withJsDoc<o, jsDocSource>
 *       : Pick<_withJsDoc<o, jsDocSource>, keyof o & keyof jsDocSource>
 *     : & Pick<_withJsDoc<o, jsDocSource>, keyof o & keyof jsDocSource>
 *     & { [K in Exclude<keyof o, keyof jsDocSource>]: o[K] }
 * >
 */

// ✓ _withJsDoc: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _withJsDoc(o, jsDocSource): any {
  const out = emptyObject
  for (const k in keyof(jsDocSource)) {
    out[k] = required(o[merge(k, keyof(o))])
  }
  return out
}
/* compiles to:
 * export type _withJsDoc<o, jsDocSource> = { [K in keyof jsDocSource]-?: o[K & keyof o] }
 */

// ✗ propertyDescriptorsOf: the ScriptType does not itself typecheck as TypeScript
//   propertyDescriptorsOf.st.ts(8:14) TS2693: 'TypedPropertyDescriptor' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
/**
 * @param {object} o
 */
export function propertyDescriptorsOf(o): any {
  const out = emptyObject
  for (const k in keyof(o)) {
    out[k] = TypedPropertyDescriptor(o[k])
  }
  return out
}
/* compiles to:
 * export type propertyDescriptorsOf<o extends object> = {
 *   [K in keyof o]: TypedPropertyDescriptor<o[K]>
 * }
 */

// ✓ keyWithValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function keyWithValue(t, constraint): any {
  const m1 = matches<Hole<"k">>(keyof(t))
  if (m1) {
    if (m1.k in t) {
      if (matches<typeof constraint>(t[m1.k])) {
        return m1.k
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type keyWithValue<t, constraint> =
 *   keyof t extends infer k
 *     ? k extends keyof t ? t[k] extends constraint ? k : never : never
 *     : never
 */

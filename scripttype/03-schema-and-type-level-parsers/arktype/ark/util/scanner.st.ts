/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/scanner.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 4 compile-error, 2 mismatch, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Backslash: any
declare const WhitespaceChar: any
type Backslash<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WhitespaceChar<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type shift<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ shift: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function shift(lookahead: string, unscanned: string) {
  return `${lookahead}${unscanned}`
}
/* compiles to:
 * export type shift<lookahead extends string, unscanned extends string> = `${lookahead}${unscanned}`
 */

// ✗ shiftUntil: does not compile yet
//   Cannot find name 'shift'.
/* @scripttype preserveParamNames */
export function shiftUntil(unscanned: string, terminator: string, appendTo: string = '') {
  const m1 = matches<shift<Hole<"lookahead">, Hole<"nextUnscanned">>>(unscanned)
  if (m1) {
    if (matches<typeof terminator>(m1.lookahead)) {
      return [appendTo, unscanned]
    }
    return shiftUntil(m1.nextUnscanned, terminator, `${appendTo}${m1.lookahead}`)
  }
  return [appendTo, '']
}
/* compiles to:
 * export type shiftUntil<unscanned extends string, terminator extends string, appendTo extends string = ''> = unscanned extends shift<infer lookahead, infer nextUnscanned> ? lookahead extends terminator ? [appendTo, unscanned] : shiftUntil<nextUnscanned, terminator, `${appendTo}${lookahead}`> : [appendTo, '']
 */

// ✗ shiftUntilEscapable: does not compile yet
//   Cannot find name 'shift'.
/* @scripttype preserveParamNames */
export function shiftUntilEscapable(unscanned: string, terminator: string, escapeEscape: Backslash | "", appendTo: string = '') {
  const m1 = matches<shift<Hole<"lookahead">, Hole<"nextUnscanned">>>(unscanned)
  if (m1) {
    if (matches<typeof terminator>(m1.lookahead)) {
      return [appendTo, unscanned]
    }
    if (matches<Backslash>(m1.lookahead)) {
      const m2 = matches<(shift<Hole<"nextLookahead">, Hole<"postEscapedUnscanned">>)>(m1.nextUnscanned)
      if (m2) {
        return shiftUntilEscapable(m2.postEscapedUnscanned, terminator, escapeEscape, `${appendTo}${matches<typeof terminator>(m2.nextLookahead) ? '' : (matches<Backslash>(m2.nextLookahead) ? escapeEscape : Backslash)}${m2.nextLookahead}`)
      }
      return [`${appendTo}${Backslash}`, '']
    }
    return shiftUntilEscapable(m1.nextUnscanned, terminator, escapeEscape, `${appendTo}${m1.lookahead}`)
  }
  return [appendTo, '']
}
/* compiles to:
 * export type shiftUntilEscapable<unscanned extends string, terminator extends string, escapeEscape extends Backslash | '', appendTo extends string = ''> = unscanned extends shift<infer lookahead, infer nextUnscanned> ? lookahead extends terminator ? [appendTo, unscanned] : lookahead extends Backslash ? nextUnscanned extends shift<infer nextLookahead, infer postEscapedUnscanned> ? shiftUntilEscapable<postEscapedUnscanned, terminator, escapeEscape, `${appendTo}${nextLookahead extends terminator ? '' : nextLookahead extends Backslash ? escapeEscape : Backslash}${nextLookahead}`> : [`${appendTo}${Backslash}`, ''] : shiftUntilEscapable<nextUnscanned, terminator, escapeEscape, `${appendTo}${lookahead}`> : [appendTo, '']
 */

// ✗ shiftUntilNot: does not compile yet
//   Cannot find name 'shift'.
/* @scripttype preserveParamNames */
export function shiftUntilNot(unscanned: string, nonTerminator: string, appendTo: string = '') {
  const m1 = matches<shift<Hole<"lookahead">, Hole<"nextUnscanned">>>(unscanned)
  if (m1) {
    if (matches<typeof nonTerminator>(m1.lookahead)) {
      return shiftUntilNot(m1.nextUnscanned, nonTerminator, `${appendTo}${m1.lookahead}`)
    }
    return [appendTo, unscanned]
  }
  return [appendTo, '']
}
/* compiles to:
 * export type shiftUntilNot<unscanned extends string, nonTerminator extends string, appendTo extends string = ''> = unscanned extends shift<infer lookahead, infer nextUnscanned> ? lookahead extends nonTerminator ? shiftUntilNot<nextUnscanned, nonTerminator, `${appendTo}${lookahead}`> : [appendTo, unscanned] : [appendTo, '']
 */

// ✗ skipWhitespace: does not compile yet
//   Cannot find name 'shiftUntilNot'.
/* @scripttype preserveParamNames */
export function skipWhitespace(unscanned: string) {
  return shiftUntilNot(unscanned, WhitespaceChar)[1]
}
/* compiles to:
 * export type skipWhitespace<unscanned extends string> = shiftUntilNot<unscanned, WhitespaceChar>[1]
 */

// ✗ shiftResult: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function shiftResult(scanned: string, unscanned: string) {
  return [scanned, unscanned]
}
/* compiles to:
 * export type shiftResult<scanned extends string, unscanned extends string> = [scanned, unscanned]
 */

// ✓ writeUnmatchedGroupCloseMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeUnmatchedGroupCloseMessage(char: string, unscanned: string) {
  return `Unmatched ${char}${matches<"">(unscanned) ? '' : ` before ${unscanned}`}`
}
/* compiles to:
 * export type writeUnmatchedGroupCloseMessage<char extends string, unscanned extends string> = `Unmatched ${char}${unscanned extends '' ? '' : ` before ${unscanned}`}`
 */

// ✓ writeUnclosedGroupMessage: verified type-identical to the original
/* @scripttype preserveParamNames */
export function writeUnclosedGroupMessage(missingChar: string) {
  return `Missing ${missingChar}`
}
/* compiles to:
 * export type writeUnclosedGroupMessage<missingChar extends string> = `Missing ${missingChar}`
 */

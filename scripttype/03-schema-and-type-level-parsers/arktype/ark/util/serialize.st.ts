/**
 * ScriptType for 03-schema-and-type-level-parsers/arktype/ark/util/serialize.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Primitive: any
declare const array: any
type Primitive<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ snapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function snapshot(t, depth: 1[] = []) {
  if (matches<typeof t>(unknown)) {
    return unknown
  }
  if (matches<Primitive>(t)) {
    return snapshotPrimitive(t)
  }
  const m1 = matches<{ toJSON: () => Hole<"serialized">; }>(t)
  if (m1) {
    return m1.serialized
  }
  if (matches<Function>(t)) {
    return `Function(${string})`
  }
  if (matches<Date>(t)) {
    return string
  }
  if (matches<10>(depth['length'])) {
    return unknown
  }
  const m2 = matches<array<Hole<"item">>>(t)
  if (m2) {
    return array(snapshot(m2.item, [...depth, 1]))
  }
  const out = emptyObject
  for (const k in keyof(t)) {
    out[snapshotPrimitive(k)] = snapshot(t[k], [...depth, 1])
  }
  return out
}
/* compiles to:
 * export type snapshot<t, depth extends 1[] = []> = unknown extends t ? unknown : t extends Primitive ? snapshotPrimitive<t> : t extends { toJSON: () => infer serialized; } ? serialized : t extends Function ? `Function(${string})` : t extends Date ? string : depth['length'] extends 10 ? unknown : t extends array<infer item> ? array<snapshot<item, [...depth, 1]>> : { [K in keyof t as snapshotPrimitive<K>]: snapshot<t[K], [...depth, 1]> }
 */

// ✓ snapshotPrimitive: verified type-identical to the original
/* @scripttype preserveParamNames */
export function snapshotPrimitive(t) {
  if (matches<symbol>(t)) {
    return `Symbol(${string})`
  }
  return t
}
/* compiles to:
 * export type snapshotPrimitive<t> = t extends symbol ? `Symbol(${string})` : t
 */

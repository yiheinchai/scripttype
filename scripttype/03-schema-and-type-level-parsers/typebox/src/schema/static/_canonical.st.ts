/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/schema/static/_canonical.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const PropertyKey: any
declare const out: any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type out<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ XCanonicalTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XCanonicalTuple(Value: readonly unknown[]) {
  const m1 = matches<[ Hole<"Left">, ...Hole<"Right", unknown[]> ]>(Value)
  if (m1) {
    return [XCanonical(m1.Left), ...XCanonicalTuple(m1.Right)]
  }
  return []
}
/* compiles to:
 * export type XCanonicalTuple<Value extends readonly unknown[]> = Value extends [infer Left, ...(infer Right extends unknown[])] ? [XCanonical<Left>, ...XCanonicalTuple<Right>] : []
 */

// ✓ XCanonicalArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XCanonicalArray(Value: unknown, Result: unknown[] = arrayOf(XCanonical(Value))) {
  return Result
}
/* compiles to:
 * export type XCanonicalArray<Value, Result extends unknown[] = XCanonical<Value>[]> = Result
 */

// ✗ XCanonicalObject: does not compile yet
//   Cannot find name 'out'.
/* @scripttype preserveParamNames */
export function XCanonicalObject(Value: object, Result: Record<PropertyKey, unknown> = out) {
  return Result
}
/* compiles to:
 * export type XCanonicalObject<Value extends object, Result extends Record<PropertyKey, unknown> = out> = Result
 */

// ✓ XCanonical: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XCanonical(Schema: unknown) {
  const m1 = matches<readonly [ ...Hole<"Schemas", unknown[]> ]>(Schema)
  if (m1) {
    return XCanonicalTuple(m1.Schemas)
  }
  const m2 = matches<readonly (Hole<"Schema">)[]>(Schema)
  if (m2) {
    return XCanonicalArray(m2.Schema)
  }
  if (matches<object>(Schema)) {
    return XCanonicalObject(Schema)
  }
  return Schema
}
/* compiles to:
 * export type XCanonical<Schema> = Schema extends readonly [...(infer Schemas extends unknown[])] ? XCanonicalTuple<Schemas> : Schema extends readonly (infer Schema)[] ? XCanonicalArray<Schema> : Schema extends object ? XCanonicalObject<Schema> : Schema
 */

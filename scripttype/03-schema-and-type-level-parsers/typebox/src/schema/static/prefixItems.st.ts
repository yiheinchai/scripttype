/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/schema/static/prefixItems.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { XStaticElements } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/static/_elements.js'
import type { XSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/types/schema.js'
declare const XSchema: any
declare const XStaticElements: any
// ✓ XStaticPrefixItems: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XStaticPrefixItems(Stack: string[], Root: XSchema, Schema: XSchema, PrefixItems: XSchema[], Result: unknown[] = XStaticElements(Stack, Root, Schema, PrefixItems)): any {
  return Result
}
/* compiles to:
 * export type XStaticPrefixItems<
 *   Stack extends string[],
 *   Root extends XSchema,
 *   Schema extends XSchema,
 *   PrefixItems extends XSchema[],
 *   Result extends unknown[] = XStaticElements<Stack, Root, Schema, PrefixItems>
 * > =
 *   Result
 */

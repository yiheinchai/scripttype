/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/schema/static/ref.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const XPointerGet: any
declare const XSchema: any
declare const XStaticSchema: any
type XPointerGet<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type XSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type XStaticSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ XCyclicCheck: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XCyclicCheck(Stack: unknown[], MaxLength: number, Buffer: unknown[] = []) {
  const m1 = matches<[ Hole<"Left">, ...Hole<"Right"> ]>(Stack)
  if (m1) {
    if (matches<typeof MaxLength>(Buffer['length'])) {
      return false
    }
    return XCyclicCheck(m1.Right, MaxLength, [...Buffer, m1.Left])
  }
  return true
}
/* compiles to:
 * export type XCyclicCheck<Stack extends unknown[], MaxLength extends number, Buffer extends unknown[] = []> = Stack extends [infer Left, ...infer Right] ? Buffer['length'] extends MaxLength ? false : XCyclicCheck<Right, MaxLength, [...Buffer, Left]> : true
 */

// ✓ XCyclicGuard: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XCyclicGuard(Stack: unknown[], Ref: string) {
  if (matches<(typeof Stack)[number]>(Ref)) {
    return XCyclicCheck(Stack, 2)
  }
  return true
}
/* compiles to:
 * export type XCyclicGuard<Stack extends unknown[], Ref extends string> = Ref extends Stack[number] ? XCyclicCheck<Stack, 2> : true
 */

// ✗ XNormal: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function XNormal(Pointer: string, Result: string = m1 ? m1.Rest : Pointer) {
  return Result
}
/* compiles to:
 * export type XNormal<Pointer extends string, Result extends string = m1 extends true ? m1.Rest : Pointer> = Result
 */

// ✓ XStaticRef: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XStaticRef(Stack: string[], Root: XSchema, Ref: string, Normal: string = XNormal(Ref), Target: unknown = XPointerGet(Root, Normal), Schema: XSchema = matches<XSchema>(Target) ? Target : {}, Result: unknown = matches<true>(XCyclicGuard(Stack, Ref)) ? XStaticSchema([...Stack, Ref], Root, Schema) : any) {
  return Result
}
/* compiles to:
 * export type XStaticRef<Stack extends string[], Root extends XSchema, Ref extends string, Normal extends string = XNormal<Ref>, Target = XPointerGet<Root, Normal>, Schema extends XSchema = Target extends XSchema ? Target : {}, Result = XCyclicGuard<Stack, Ref> extends true ? XStaticSchema<[...Stack, Ref], Root, Schema> : any> = Result
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/schema/static/static.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { XCanonical } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/static/_canonical.js'
import type { XStaticSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/static/schema.js'
import type { XSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/types/schema.js'
declare const XCanonical: any
declare const XSchema: any
declare const XStaticSchema: any
// ✓ XStatic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XStatic(Value: unknown, Schema: XSchema = matches<XSchema>(Value) ? Value : {}, Canonical: XSchema = XCanonical(Schema), Result: unknown = XStaticSchema([], Canonical, Canonical)): any {
  return Result
}
/* compiles to:
 * export type XStatic<
 *   Value,
 *   Schema extends XSchema = Value extends XSchema ? Value : {},
 *   Canonical extends XSchema = XCanonical<Schema>,
 *   Result = XStaticSchema<[], Canonical, Canonical>
 * > =
 *   Result
 */

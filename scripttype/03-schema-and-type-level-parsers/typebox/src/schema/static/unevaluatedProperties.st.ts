/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/schema/static/unevaluatedProperties.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { XStaticSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/static/schema.js'
import type { XSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/schema/types/schema.js'
declare const PropertyKey: any
declare const XSchema: any
declare const XStaticSchema: any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ XStaticUnevaluatedProperties: verified type-identical to the original
/* @scripttype preserveParamNames */
export function XStaticUnevaluatedProperties(Stack: string[], Root: XSchema, Schema: XSchema, Result: Record<PropertyKey, unknown> = matches<true>(Schema) ? indexRecord(string, unknown) : (matches<false>(Schema) ? {} : indexRecord(string, XStaticSchema(Stack, Root, Schema)))): any {
  return Result
}
/* compiles to:
 * export type XStaticUnevaluatedProperties<
 *   Stack extends string[],
 *   Root extends XSchema,
 *   Schema extends XSchema,
 *   Result extends Record<PropertyKey, unknown> = Schema extends true ? { [key: string]: unknown } : Schema extends false ? {} : { [key: string]: XStaticSchema<Stack, Root, Schema> }
 * > =
 *   Result
 */

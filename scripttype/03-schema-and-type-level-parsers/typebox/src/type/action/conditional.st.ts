/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/conditional.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TConditionalAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/conditional/instantiate.js'
import type { TState } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TConditionalAction: any
declare const TDeferred: any
declare const TSchema: any
declare const TState: any
// ✓ TConditionalDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TConditionalDeferred(Left: TSchema, Right: TSchema, True: TSchema, False: TSchema): any {
  return TDeferred('Conditional', [Left, Right, True, False])
}
/* compiles to:
 * export type TConditionalDeferred<
 *   Left extends TSchema,
 *   Right extends TSchema,
 *   True extends TSchema,
 *   False extends TSchema
 * > =
 *   TDeferred<'Conditional', [Left, Right, True, False]>
 */

// ✓ TConditional: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TConditional(Left: TSchema, Right: TSchema, True: TSchema, False: TSchema): any {
  return TConditionalAction({}, TState([], []), Left, Right, True, False)
}
/* compiles to:
 * export type TConditional<
 *   Left extends TSchema,
 *   Right extends TSchema,
 *   True extends TSchema,
 *   False extends TSchema
 * > =
 *   TConditionalAction<{}, TState<[], []>, Left, Right, True, False>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/extract.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TExtractAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/extract/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TDeferred: any
declare const TExtractAction: any
declare const TSchema: any
// ✓ TExtractDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExtractDeferred(Left: TSchema, Right: TSchema): any {
  return TDeferred('Extract', [Left, Right])
}
/* compiles to:
 * export type TExtractDeferred<Left extends TSchema, Right extends TSchema> = TDeferred<
 *   'Extract',
 *   [Left, Right]
 * >
 */

// ✓ TExtract: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExtract(Left: TSchema, Right: TSchema): any {
  return TExtractAction(Left, Right)
}
/* compiles to:
 * export type TExtract<Left extends TSchema, Right extends TSchema> = TExtractAction<Left, Right>
 */

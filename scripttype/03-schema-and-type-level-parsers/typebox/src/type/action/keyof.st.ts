/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/keyof.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TKeyOfAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/keyof/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TDeferred: any
declare const TKeyOfAction: any
declare const TSchema: any
// ✓ TKeyOfDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeyOfDeferred(Type: TSchema): any {
  return TDeferred('KeyOf', [Type])
}
/* compiles to:
 * export type TKeyOfDeferred<Type extends TSchema> = TDeferred<'KeyOf', [Type]>
 */

// ✓ TKeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeyOf(Type: TSchema): any {
  return TKeyOfAction(Type)
}
/* compiles to:
 * export type TKeyOf<Type extends TSchema> = TKeyOfAction<Type>
 */

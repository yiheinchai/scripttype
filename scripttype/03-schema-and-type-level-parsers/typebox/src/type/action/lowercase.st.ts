/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/lowercase.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TLowercaseAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/intrinsics/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TDeferred: any
declare const TLowercaseAction: any
declare const TSchema: any
// ✓ TLowercaseDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TLowercaseDeferred(Type: TSchema): any {
  return TDeferred('Lowercase', [Type])
}
/* compiles to:
 * export type TLowercaseDeferred<Type extends TSchema> = TDeferred<'Lowercase', [Type]>
 */

// ✓ TLowercase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TLowercase(Type: TSchema): any {
  return TLowercaseAction(Type)
}
/* compiles to:
 * export type TLowercase<Type extends TSchema> = TLowercaseAction<Type>
 */

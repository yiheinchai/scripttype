/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/parameters.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TParametersAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/parameters/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TDeferred: any
declare const TParametersAction: any
declare const TSchema: any
// ✓ TParametersDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TParametersDeferred(Type: TSchema): any {
  return TDeferred('Parameters', [Type])
}
/* compiles to:
 * export type TParametersDeferred<Type extends TSchema> = TDeferred<'Parameters', [Type]>
 */

// ✓ TParameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TParameters(Type: TSchema): any {
  return TParametersAction(Type)
}
/* compiles to:
 * export type TParameters<Type extends TSchema> = TParametersAction<Type>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/action/pick.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TPickAction } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/pick/instantiate.js'
import type { TDeferred } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/deferred.js'
import type { TSchema } from '../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TDeferred: any
declare const TPickAction: any
declare const TSchema: any
// ✓ TPickDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPickDeferred(Type: TSchema, Indexer: TSchema): any {
  return TDeferred('Pick', [Type, Indexer])
}
/* compiles to:
 * export type TPickDeferred<Type extends TSchema, Indexer extends TSchema> = TDeferred<
 *   'Pick',
 *   [Type, Indexer]
 * >
 */

// ✓ TPick: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPick(Type: TSchema, Indexer: TSchema): any {
  return TPickAction(Type, Indexer)
}
/* compiles to:
 * export type TPick<Type extends TSchema, Indexer extends TSchema> = TPickAction<Type, Indexer>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/call/instantiate.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 4 covered, 2 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TCallConstruct: any
declare const TDistributeArguments: any
declare const TEvaluateUnion: any
declare const TInstantiateType: any
declare const TInstantiateTypes: any
declare const TParameter: any
declare const TProperties: any
declare const TRef: any
declare const TResolveArgumentsContext: any
declare const TResolveTarget: any
declare const TSchema: any
declare const TState: any
type TCallConstruct<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TDistributeArguments<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEvaluateUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInstantiateType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInstantiateTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TResolveArgumentsContext<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TResolveTarget<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TPeek: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TPeek(State: TState, Result: string = m1 ? m1.Top : '') {
  return Result
}
/* compiles to:
 * export type TPeek<State extends TState, Result extends string = m1 extends true ? m1.Top : ''> = Result
 */

// ✓ TIsTailCall: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TIsTailCall(State: TState, Name: string, Result: boolean = matches<typeof Name>(TPeek(State)) ? true : false) {
  return Result
}
/* compiles to:
 * export type TIsTailCall<State extends TState, Name extends string, Result extends boolean = TPeek<State> extends Name ? true : false> = Result
 */

// ✓ TCallDispatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TCallDispatch(Context: TProperties, State: TState, Target: TRef, Parameters: TParameter[], Expression: TSchema, Arguments: TSchema[], ArgumentsContext: TProperties = TResolveArgumentsContext(Context, State, Parameters, Arguments), ReturnType: TSchema = TInstantiateType(ArgumentsContext, TState([...State['callstack'], Target['$ref']], State['visited']), Expression)) {
  return TInstantiateType(ArgumentsContext, TState([], []), ReturnType)
}
/* compiles to:
 * export type TCallDispatch<Context extends TProperties, State extends TState, Target extends TRef, Parameters extends TParameter[], Expression extends TSchema, Arguments extends TSchema[], ArgumentsContext extends TProperties = TResolveArgumentsContext<Context, State, Parameters, Arguments>, ReturnType extends TSchema = TInstantiateType<ArgumentsContext, TState<[...State['callstack'], Target['$ref']], State['visited']>, Expression>> = TInstantiateType<ArgumentsContext, TState<[], []>, ReturnType>
 */

// ✓ TCallDistributed: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TCallDistributed(Context: TProperties, State: TState, Target: TRef, Parameters: TParameter[], Expression: TSchema, DistributedArguments: TSchema[][], Result: TSchema[] = []) {
  const m1 = matches<[ Hole<"Arguments", TSchema[]>, ...Hole<"DistributedArguments", TSchema[][]> ]>(DistributedArguments)
  if (m1) {
    const m2 = matches<Hole<"ReturnType", TSchema>>(TCallDispatch(Context, State, Target, Parameters, Expression, m1.Arguments))
    if (m2) {
      return TCallDistributed(Context, State, Target, Parameters, Expression, m1.DistributedArguments, [...Result, m2.ReturnType])
    }
    return never
  }
  return Result
}
/* compiles to:
 * export type TCallDistributed<Context extends TProperties, State extends TState, Target extends TRef, Parameters extends TParameter[], Expression extends TSchema, DistributedArguments extends TSchema[][], Result extends TSchema[] = []> = DistributedArguments extends [infer Arguments extends TSchema[], ...(infer DistributedArguments extends TSchema[][])] ? TCallDispatch<Context, State, Target, Parameters, Expression, Arguments> extends (infer ReturnType extends TSchema) ? TCallDistributed<Context, State, Target, Parameters, Expression, DistributedArguments, [...Result, ReturnType]> : never : Result
 */

// ✓ TCallImmediate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TCallImmediate(Context: TProperties, State: TState, Target: TRef, Parameters: TParameter[], Expression: TSchema, InstantiatedArguments: TSchema[], DistributedArguments: TSchema[][] = TDistributeArguments(Parameters, InstantiatedArguments, Expression), ReturnTypes: TSchema[] = TCallDistributed(Context, State, Target, Parameters, Expression, DistributedArguments), Result: TSchema = matches<1>(ReturnTypes['length']) ? ReturnTypes[0] : TEvaluateUnion(ReturnTypes)) {
  return Result
}
/* compiles to:
 * export type TCallImmediate<Context extends TProperties, State extends TState, Target extends TRef, Parameters extends TParameter[], Expression extends TSchema, InstantiatedArguments extends TSchema[], DistributedArguments extends TSchema[][] = TDistributeArguments<Parameters, InstantiatedArguments, Expression>, ReturnTypes extends TSchema[] = TCallDistributed<Context, State, Target, Parameters, Expression, DistributedArguments>, Result extends TSchema = ReturnTypes['length'] extends 1 ? ReturnTypes[0] : TEvaluateUnion<ReturnTypes>> = Result
 */

// ✗ TCallInstantiate: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TCallInstantiate(Context: TProperties, State: TState, Target: TSchema, Arguments: TSchema[], InstantiatedArguments: TSchema[] = TInstantiateTypes(Context, State, Arguments), Resolved: [ string, TSchema ] = TResolveTarget(Context, Target, Arguments), Name: string = Resolved[0], Type: TSchema = Resolved[1], Result: TSchema = m1 ? (matches<true>(TIsTailCall(State, Name)) ? TCallConstruct(TRef(Name), InstantiatedArguments) : TCallImmediate(Context, State, TRef(Name), m1.Parameters, m1.Expression, InstantiatedArguments)) : TCallConstruct(Target, InstantiatedArguments)) {
  return Result
}
/* compiles to:
 * export type TCallInstantiate<Context extends TProperties, State extends TState, Target extends TSchema, Arguments extends TSchema[], InstantiatedArguments extends TSchema[] = TInstantiateTypes<Context, State, Arguments>, Resolved extends [string, TSchema] = TResolveTarget<Context, Target, Arguments>, Name extends string = Resolved[0], Type extends TSchema = Resolved[1], Result extends TSchema = m1 extends true ? TIsTailCall<State, Name> extends true ? TCallConstruct<TRef<Name>, InstantiatedArguments> : TCallImmediate<Context, State, TRef<Name>, m1.Parameters, m1.Expression, InstantiatedArguments> : TCallConstruct<Target, InstantiatedArguments>> = Result
 */

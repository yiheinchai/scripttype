/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/evaluate/evaluate.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 4 covered, 2 compile-error, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TBroaden: any
declare const TDistribute: any
declare const TEnumValue: any
declare const TExcludeOperation: any
declare const TIntersect: any
declare const TLiteral: any
declare const TNever: any
declare const TSchema: any
declare const TTemplateLiteralDecode: any
declare const TUnion: any
declare const m2: any
declare const m3: any
declare const m4: any
declare const m5: any
type TBroaden<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TDistribute<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEnumValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TExcludeOperation<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TIntersect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTemplateLiteralDecode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m3<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m4<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m5<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TEvaluateDependent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEvaluateDependent(If: TSchema, Then: TSchema, Else: TSchema, Intersect: TSchema = TIntersect([If, Then]), Excluded: TSchema = TExcludeOperation(Else, If), Result: TSchema = TEvaluateUnion([Intersect, Excluded])) {
  return Result
}
/* compiles to:
 * export type TEvaluateDependent<If extends TSchema, Then extends TSchema, Else extends TSchema, Intersect extends TSchema = TIntersect<[If, Then]>, Excluded extends TSchema = TExcludeOperation<Else, If>, Result extends TSchema = TEvaluateUnion<[Intersect, Excluded]>> = Result
 */

// ✗ TEvaluateEnum: the ScriptType does not itself typecheck as TypeScript
//   TEvaluateEnum.st.ts(11:17) TS2345: Argument of type 'any' is not assignable to parameter of type 'never'.
/* @scripttype preserveParamNames */
export function TEvaluateEnum(Values: TEnumValue[]) {
  let Result = []
  let values = Values
  while (true) {
    const m1 = matches<[ Hole<"Left", TEnumValue>, ...Hole<"Right", TEnumValue[]> ]>(values)
    if (!m1) {
      break
    }
    values = m1.Right
    Result.push(TLiteral(m1.Left))
  }
  return TEvaluateUnion(Result)
}
/* compiles to:
 * export type TEvaluateEnum<Values extends TEnumValue[]> = TEvaluateEnum__loop<Values, []>
 * type TEvaluateEnum__loop<Values extends TEnumValue[], Result extends readonly unknown[]> = Values extends [infer Left extends TEnumValue, ...(infer Right extends TEnumValue[])] ? TEvaluateEnum__loop<Right, [...Result, TLiteral<Left>]> : TEvaluateUnion<Result>
 */

// ✓ TEvaluateIntersect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEvaluateIntersect(Types: TSchema[], Distribution: TSchema[] = TDistribute(Types), Broadend: TSchema[] = TBroaden(Distribution), Result: TSchema = TEvaluateUnionFast(Broadend)) {
  return Result
}
/* compiles to:
 * export type TEvaluateIntersect<Types extends TSchema[], Distribution extends TSchema[] = TDistribute<Types>, Broadend extends TSchema[] = TBroaden<Distribution>, Result extends TSchema = TEvaluateUnionFast<Broadend>> = Result
 */

// ✓ TEvaluateTemplateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEvaluateTemplateLiteral(Pattern: string, Evaluated: TSchema = TTemplateLiteralDecode(Pattern), Result: TSchema = TEvaluateType(Evaluated)) {
  return Result
}
/* compiles to:
 * export type TEvaluateTemplateLiteral<Pattern extends string, Evaluated extends TSchema = TTemplateLiteralDecode<Pattern>, Result extends TSchema = TEvaluateType<Evaluated>> = Result
 */

// ✓ TEvaluateUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEvaluateUnion(Types: TSchema[], Broadend: TSchema[] = TBroaden(Types), Result: TSchema = TEvaluateUnionFast(Broadend)) {
  return Result
}
/* compiles to:
 * export type TEvaluateUnion<Types extends TSchema[], Broadend extends TSchema[] = TBroaden<Types>, Result extends TSchema = TEvaluateUnionFast<Broadend>> = Result
 */

// ✗ TEvaluateType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TEvaluateType(Type: TSchema, Result: TSchema = m1 ? TEvaluateDependent(m1.If, m1.Then, m1.Else) : (m2 ? TEvaluateEnum(m2.Values) : (m3 ? TEvaluateIntersect(m3.Types) : (m4 ? TEvaluateTemplateLiteral(m4.Pattern) : (m5 ? TEvaluateUnion(m5.Types) : Type))))) {
  return Result
}
/* compiles to:
 * export type TEvaluateType<Type extends TSchema, Result extends TSchema = m1 extends true ? TEvaluateDependent<m1.If, m1.Then, m1.Else> : m2 extends true ? TEvaluateEnum<m2.Values> : m3 extends true ? TEvaluateIntersect<m3.Types> : m4 extends true ? TEvaluateTemplateLiteral<m4.Pattern> : m5 extends true ? TEvaluateUnion<m5.Types> : Type> = Result
 */

// ✗ TEvaluateUnionFast: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TEvaluateUnionFast(Types: TSchema[], Result: TSchema = m1 ? m1.Type : (matches<[ ]>(Types) ? TNever : TUnion(Types))) {
  return Result
}
/* compiles to:
 * export type TEvaluateUnionFast<Types extends TSchema[], Result extends TSchema = m1 extends true ? m1.Type : Types extends [] ? TNever : TUnion<Types>> = Result
 */

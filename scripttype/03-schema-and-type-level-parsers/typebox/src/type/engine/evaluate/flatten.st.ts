/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/evaluate/flatten.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 compile-error, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TSchema: any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TFlattenType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TFlattenType(Type: TSchema, Result: TSchema[] = m1 ? TFlatten(m1.Types) : [Type]) {
  return Result
}
/* compiles to:
 * export type TFlattenType<Type extends TSchema, Result extends TSchema[] = m1 extends true ? TFlatten<m1.Types> : [Type]> = Result
 */

// ✗ TFlatten: the ScriptType does not itself typecheck as TypeScript
//   TFlatten.st.ts(11:5) TS2322: Type 'any[]' is not assignable to type 'never[]'.   Type 'any' is not assignable to type 'never'.
/* @scripttype preserveParamNames */
export function TFlatten(Types: TSchema[]) {
  let Result = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = [...Result, ...TFlattenType(m1.Left)]
  }
  return Result
}
/* compiles to:
 * export type TFlatten<Types extends TSchema[]> = TFlatten__loop<Types, []>
 * type TFlatten__loop<Types extends TSchema[], Result extends readonly unknown[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFlatten__loop<Right, [...Result, ...TFlattenType<Left>]> : Result
 */

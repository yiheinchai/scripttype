/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/helpers/keys.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TLiteralValue: any
type TLiteralValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ TConvertToIntegerKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TConvertToIntegerKey__v1(Value, Normal) {
  const m1 = matches<` ${Hole<"_", number>}`>(Normal)
  if (m1) {
    return Normal
  }
  const m2 = matches<`${Hole<"_", number>} `>(Normal)
  if (m2) {
    return Normal
  }
  const m3 = matches<`0${Hole<"_", number>}`>(Normal)
  if (m3) {
    return Normal
  }
  const m4 = matches<`-${Hole<"_", number>}`>(Normal)
  if (m4) {
    return Normal
  }
  const m5 = matches<`${Hole<"Value", number>}`>(Normal)
  if (m5) {
    return m5.Value
  }
  return Value
}
export function TConvertToIntegerKey(Value: TLiteralValue, Normal: string = `${Value}`, Result: TLiteralValue = TConvertToIntegerKey__v1(Value, Normal)) {
  return Result
}
/* compiles to:
 * export type TConvertToIntegerKey__v1<Value, Normal> =
 *   Normal extends ` ${number}` ? Normal
 *   : Normal extends `${number} ` ? Normal
 *   : Normal extends `0${number}` ? Normal
 *   : Normal extends `-${number}` ? Normal
 *   : Normal extends `${infer Value extends number}` ? Value
 *   : Value
 * export type TConvertToIntegerKey<
 *   Value extends TLiteralValue,
 *   Normal extends string = `${Value}`,
 *   Result extends TLiteralValue = TConvertToIntegerKey__v1<Value, Normal>
 * > =
 *   Result
 */

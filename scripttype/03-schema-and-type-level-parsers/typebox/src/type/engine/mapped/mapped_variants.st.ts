/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/mapped/mapped_variants.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 5 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TEnumValue: any
declare const TEvaluateEnum: any
declare const TEvaluateTemplateLiteral: any
declare const TLiteral: any
declare const TLiteralValue: any
declare const TSchema: any
declare const m2: any
declare const m3: any
declare const m4: any
type TEnumValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEvaluateEnum<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEvaluateTemplateLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m3<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m4<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TFromTemplateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromTemplateLiteral(Pattern: string, Evaluated: TSchema = TEvaluateTemplateLiteral(Pattern), Result: TSchema[] = TFromType(Evaluated)) {
  return Result
}
/* compiles to:
 * export type TFromTemplateLiteral<Pattern extends string, Evaluated extends TSchema = TEvaluateTemplateLiteral<Pattern>, Result extends TSchema[] = TFromType<Evaluated>> = Result
 */

// ✓ TFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnion(Types: TSchema[]) {
  let Result: any[] = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = [...Result, ...TFromType(m1.Left)]
  }
  return Result
}
/* compiles to:
 * export type TFromUnion<Types extends TSchema[]> = TFromUnion__loop<Types, []>
 * type TFromUnion__loop<Types extends TSchema[], Result extends any[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFromUnion__loop<Right, [...Result, ...TFromType<Left>]> : Result
 */

// ✓ TFromEnum: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromEnum(Values: TEnumValue[], Evaluated: TSchema = TEvaluateEnum(Values), Result: TSchema[] = TFromType(Evaluated)) {
  return Result
}
/* compiles to:
 * export type TFromEnum<Values extends TEnumValue[], Evaluated extends TSchema = TEvaluateEnum<Values>, Result extends TSchema[] = TFromType<Evaluated>> = Result
 */

// ✓ TFromLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromLiteral(Value: TLiteralValue, Result: TSchema[] = matches<number>(Value) ? [TLiteral(`${Value}`)] : [TLiteral(Value)]) {
  return Result
}
/* compiles to:
 * export type TFromLiteral<Value extends TLiteralValue, Result extends TSchema[] = Value extends number ? [TLiteral<`${Value}`>] : [TLiteral<Value>]> = Result
 */

// ✗ TFromType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TFromType(Type: TSchema, Result: TSchema[] = m1 ? TFromEnum(m1.Values) : (m2 ? TFromLiteral(m2.Value) : (m3 ? TFromTemplateLiteral(m3.Pattern) : (m4 ? TFromUnion(m4.Types) : [Type])))) {
  return Result
}
/* compiles to:
 * export type TFromType<Type extends TSchema, Result extends TSchema[] = m1 extends true ? TFromEnum<m1.Values> : m2 extends true ? TFromLiteral<m2.Value> : m3 extends true ? TFromTemplateLiteral<m3.Pattern> : m4 extends true ? TFromUnion<m4.Types> : [Type]> = Result
 */

// ✓ TMappedVariants: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TMappedVariants(Type: TSchema, Result: TSchema[] = TFromType(Type)) {
  return Result
}
/* compiles to:
 * export type TMappedVariants<Type extends TSchema, Result extends TSchema[] = TFromType<Type>> = Result
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/object/from_union.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const PropertyKey: any
declare const TFromType: any
declare const TProperties: any
declare const TSchema: any
declare const TUnreachable: any
declare const out: any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TFromType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnreachable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type out<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TCollapseUnionProperties: does not compile yet
//   Cannot find name 'out'.
/* @scripttype preserveParamNames */
export function TCollapseUnionProperties(Left: TProperties, Right: TProperties, SharedKeys: PropertyKey = keyof(Left) & keyof(Right), Result: TProperties = out) {
  return Result
}
/* compiles to:
 * export type TCollapseUnionProperties<Left extends TProperties, Right extends TProperties, SharedKeys extends PropertyKey = keyof Left & keyof Right, Result extends TProperties = out> = Result
 */

// ✓ TReduceVariants: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TReduceVariants(Types: TSchema[], Result: TProperties) {
  let types = Types
  let result = Result
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    result = TCollapseUnionProperties(result, TFromType(m1.Left))
  }
  return result
}
/* compiles to:
 * export type TReduceVariants<Types extends TSchema[], Result extends TProperties> = TReduceVariants__loop<Types, Result>
 * type TReduceVariants__loop<Types extends TSchema[], Result extends TProperties> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TReduceVariants__loop<Right, TCollapseUnionProperties<Result, TFromType<Left>>> : Result
 */

// ✓ TFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnion(Types: TSchema[]) {
  const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(Types)
  if (m1) {
    return TReduceVariants(m1.Right, TFromType(m1.Left))
  }
  return TUnreachable
}
/* compiles to:
 * export type TFromUnion<Types extends TSchema[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TReduceVariants<Right, TFromType<Left>> : TUnreachable
 */

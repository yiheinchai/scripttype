/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/priority/priority.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 4 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TCompare, TCompareResult } from '../../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/evaluate/compare.js'
import type { TSchema } from '../../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare namespace m1 {
  export type Left<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Right<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const TCompare: any
declare const TCompareResult: any
declare const TSchema: any
// ✓ TComparer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TComparer(Left: TSchema, Right: TSchema, CompareResult: TCompareResult = TCompare(Left, Right), Result: 0 | 1 = matches<'right-inside'>(CompareResult) ? 1 : (matches<'disjoint'>(CompareResult) ? 1 : 0)): any {
  return Result
}
/* compiles to:
 * export type TComparer<
 *   Left extends TSchema,
 *   Right extends TSchema,
 *   CompareResult extends TCompareResult = TCompare<Left, Right>,
 *   Result extends 0 | 1 = CompareResult extends 'right-inside' ? 1 : CompareResult extends 'disjoint' ? 1 : 0
 * > =
 *   Result
 */

// ✓ TInsert: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TInsert(Type: TSchema, Types: TSchema[], Result: TSchema[] = []): any {
  const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(Types)
  if (m1) {
    if (matches<1>(TComparer(Type, m1.Left))) {
      return TInsert(Type, m1.Right, [...Result, m1.Left])
    }
    return [...Result, Type, ...Types]
  }
  return [...Result, Type]
}
/* compiles to:
 * export type TInsert<
 *   Type extends TSchema,
 *   Types extends TSchema[],
 *   Result extends TSchema[] = []
 * > =
 *   Types extends [infer Left extends TSchema, ...infer Right extends TSchema[]]
 *     ? TComparer<Type, Left> extends 1
 *       ? TInsert<Type, Right, [...Result, Left]>
 *       : [...Result, Type, ...Types]
 *     : [...Result, Type]
 */

// ✓ TSort: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TSort(Types: TSchema[]): any {
  let Result: any[] = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = TInsert(m1.Left, Result)
  }
  return Result
}
/* compiles to:
 * export type TSort<Types extends TSchema[]> = TSort__loop<Types, []>
 * type TSort__loop<Types extends TSchema[], Result extends any[]> =
 *   Types extends [infer Left extends TSchema, ...infer Right extends TSchema[]]
 *     ? TSort__loop<Right, TInsert<Left, Result>>
 *     : Result
 */

// ✓ TPriority: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPriority(Types: TSchema[], Result: TSchema[] = TSort(Types)): any {
  return Result
}
/* compiles to:
 * export type TPriority<Types extends TSchema[], Result extends TSchema[] = TSort<Types>> = Result
 */

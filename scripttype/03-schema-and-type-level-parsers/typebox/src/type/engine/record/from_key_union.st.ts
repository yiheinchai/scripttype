/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/record/from_key_union.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 4 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TFlatten: any
declare const TInteger: any
declare const TLiteral: any
declare const TNumber: any
declare const TObject: any
declare const TProperties: any
declare const TRecord: any
declare const TSchema: any
declare const TString: any
type TFlatten<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInteger<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNumber<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TStringOrNumberCheck: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TStringOrNumberCheck(Types: TSchema[]) {
  const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(Types)
  if (m1) {
    if (matches<TString | TNumber | TInteger>(m1.Left)) {
      return true
    }
    return TStringOrNumberCheck(m1.Right)
  }
  return false
}
/* compiles to:
 * export type TStringOrNumberCheck<Types extends TSchema[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? Left extends TString | TNumber | TInteger ? true : TStringOrNumberCheck<Right> : false
 */

// ✗ TTryBuildRecord: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TTryBuildRecord(Types: TSchema[], Value: TSchema, Result: TSchema | undefined = matches<true>(TStringOrNumberCheck(Types)) ? TRecord(raw('typeof StringKey'), Value) : Undefined) {
  return Result
}

// ✓ TCreateProperties: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TCreateProperties(Variants: TSchema[], Value: TSchema, Result: TProperties = {}) {
  const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(Variants)
  if (m1) {
    if (matches<TLiteral<string | number>>(m1.Left)) {
      const out = emptyObject
      for (const _ in keySet(m1.Left['const'])) {
        out[_] = Value
      }
      return TCreateProperties(m1.Right, Value, merge(Result, out))
    }
    return TCreateProperties(m1.Right, Value, Result)
  }
  const out = emptyObject
  for (const Key in keyof(Result)) {
    out[Key] = Result[Key]
  }
  return out
}
/* compiles to:
 * export type TCreateProperties<Variants extends TSchema[], Value extends TSchema, Result extends TProperties = {}> = Variants extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? Left extends TLiteral<string | number> ? TCreateProperties<Right, Value, Result & { [_ in Left['const']]: Value }> : TCreateProperties<Right, Value, Result> : { [Key in keyof Result]: Result[Key] }
 */

// ✓ TCreateObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TCreateObject(Variants: TSchema[], Value: TSchema, Properties: TProperties = TCreateProperties(Variants, Value), Result: TSchema = TObject(Properties)) {
  return Result
}
/* compiles to:
 * export type TCreateObject<Variants extends TSchema[], Value extends TSchema, Properties extends TProperties = TCreateProperties<Variants, Value>, Result extends TSchema = TObject<Properties>> = Result
 */

// ✓ TFromUnionKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnionKey(Types: TSchema[], Value: TSchema, Flattened: TSchema[] = TFlatten(Types), Record: TSchema | undefined = TTryBuildRecord(Flattened, Value), Result: TSchema = matches<TSchema>(Record) ? Record : TCreateObject(Flattened, Value)) {
  return Result
}
/* compiles to:
 * export type TFromUnionKey<Types extends TSchema[], Value extends TSchema, Flattened extends TSchema[] = TFlatten<Types>, Record extends TSchema | undefined = TTryBuildRecord<Flattened, Value>, Result extends TSchema = Record extends TSchema ? Record : TCreateObject<Flattened, Value>> = Result
 */

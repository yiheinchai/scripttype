/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/rest/spread.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 compile-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TInfer: any
declare const TNever: any
declare const TRef: any
declare const TSchema: any
declare const m2: any
type TInfer<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TSpreadElement: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TSpreadElement(Type: TSchema, Result: TSchema[] = m1 ? (m2 ? TRestSpread(m2.Elements) : (matches<TInfer<string, TSchema>>(m1.Rest) ? [Type] : (matches<TRef<string>>(m1.Rest) ? [Type] : [TNever]))) : [Type]) {
  return Result
}
/* compiles to:
 * export type TSpreadElement<Type extends TSchema, Result extends TSchema[] = m1 extends true ? m2 extends true ? TRestSpread<m2.Elements> : m1.Rest extends TInfer<string, TSchema> ? [Type] : m1.Rest extends TRef<string> ? [Type] : [TNever] : [Type]> = Result
 */

// ✓ TRestSpread: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TRestSpread(Types: TSchema[]) {
  let Result: any[] = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = [...Result, ...TSpreadElement(m1.Left)]
  }
  return Result
}
/* compiles to:
 * export type TRestSpread<Types extends TSchema[]> = TRestSpread__loop<Types, []>
 * type TRestSpread__loop<Types extends TSchema[], Result extends any[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TRestSpread__loop<Right, [...Result, ...TSpreadElement<Left>]> : Result
 */

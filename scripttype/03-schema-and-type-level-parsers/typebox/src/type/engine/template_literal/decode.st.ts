/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/template_literal/decode.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 8 covered, 2 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TIsTemplateLiteralFinite: any
declare const TLiteral: any
declare const TLiteralValue: any
declare const TParsePatternIntoTypes: any
declare const TSchema: any
declare const TString: any
declare const TTemplateLiteral: any
declare const TUnion: any
declare const TUnreachable: any
declare const m2: any
type TIsTemplateLiteralFinite<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TParsePatternIntoTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTemplateLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnreachable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TFromLiteralPush: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromLiteralPush(Variants: string[], Value: TLiteralValue) {
  let Result: any[] = []
  let variants = Variants
  while (true) {
    const m1 = matches<[ Hole<"Left", string>, ...Hole<"Right", string[]> ]>(variants)
    if (!m1) {
      break
    }
    variants = m1.Right
    Result.push(`${m1.Left}${Value}`)
  }
  return Result
}
/* compiles to:
 * export type TFromLiteralPush<Variants extends string[], Value extends TLiteralValue> = TFromLiteralPush__loop<Variants, [], Value>
 * type TFromLiteralPush__loop<Variants extends string[], Result extends any[], Value extends TLiteralValue> = Variants extends [infer Left extends string, ...(infer Right extends string[])] ? TFromLiteralPush__loop<Right, [...Result, `${Left}${Value}`], Value> : Result
 */

// ✓ TFromLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromLiteral(Variants: string[], Value: TLiteralValue) {
  if (matches<[ ]>(Variants)) {
    return [`${Value}`]
  }
  return TFromLiteralPush(Variants, Value)
}
/* compiles to:
 * export type TFromLiteral<Variants extends string[], Value extends TLiteralValue> = Variants extends [] ? [`${Value}`] : TFromLiteralPush<Variants, Value>
 */

// ✓ TFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnion(Variants: string[], Types: TSchema[]) {
  let Result: any[] = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = [...Result, ...TFromType(Variants, m1.Left)]
  }
  return Result
}
/* compiles to:
 * export type TFromUnion<Variants extends string[], Types extends TSchema[]> = TFromUnion__loop<Types, [], Variants>
 * type TFromUnion__loop<Types extends TSchema[], Result extends any[], Variants extends string[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFromUnion__loop<Right, [...Result, ...TFromType<Variants, Left>], Variants> : Result
 */

// ✗ TFromType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TFromType(Variants: string[], Type: TSchema, Result: string[] = m1 ? TFromUnion(Variants, m1.Types) : (m2 ? TFromLiteral(Variants, m2.Value) : TUnreachable)) {
  return Result
}
/* compiles to:
 * export type TFromType<Variants extends string[], Type extends TSchema, Result extends string[] = m1 extends true ? TFromUnion<Variants, m1.Types> : m2 extends true ? TFromLiteral<Variants, m2.Value> : TUnreachable> = Result
 */

// ✓ TDecodeFromSpan: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TDecodeFromSpan(Variants: string[], Types: TSchema[]) {
  let variants = Variants
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    variants = TFromType(variants, m1.Left)
    types = m1.Right
  }
  return variants
}
/* compiles to:
 * export type TDecodeFromSpan<Variants extends string[], Types extends TSchema[]> = TDecodeFromSpan__loop<Variants, Types>
 * type TDecodeFromSpan__loop<Variants extends string[], Types extends TSchema[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TDecodeFromSpan__loop<TFromType<Variants, Left>, Right> : Variants
 */

// ✓ TVariantsToLiterals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TVariantsToLiterals(Variants: string[]) {
  let Result: any[] = []
  let variants = Variants
  while (true) {
    const m1 = matches<[ Hole<"Left", string>, ...Hole<"Right", string[]> ]>(variants)
    if (!m1) {
      break
    }
    variants = m1.Right
    Result.push(TLiteral(m1.Left))
  }
  return Result
}
/* compiles to:
 * export type TVariantsToLiterals<Variants extends string[]> = TVariantsToLiterals__loop<Variants, []>
 * type TVariantsToLiterals__loop<Variants extends string[], Result extends any[]> = Variants extends [infer Left extends string, ...(infer Right extends string[])] ? TVariantsToLiterals__loop<Right, [...Result, TLiteral<Left>]> : Result
 */

// ✓ TDecodeTypesAsUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TDecodeTypesAsUnion(Types: TSchema[], Variants: string[] = TDecodeFromSpan([], Types), Literals: TSchema[] = TVariantsToLiterals(Variants), Result: TSchema = TUnion(Literals)) {
  return Result
}
/* compiles to:
 * export type TDecodeTypesAsUnion<Types extends TSchema[], Variants extends string[] = TDecodeFromSpan<[], Types>, Literals extends TSchema[] = TVariantsToLiterals<Variants>, Result extends TSchema = TUnion<Literals>> = Result
 */

// ✗ TDecodeTypes: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TDecodeTypes(Types: TSchema[], Result: TSchema = matches<[ ]>(Types) ? TUnreachable : (m1 ? m1.Type : TDecodeTypesAsUnion(Types))) {
  return Result
}
/* compiles to:
 * export type TDecodeTypes<Types extends TSchema[], Result extends TSchema = Types extends [] ? TUnreachable : m1 extends true ? m1.Type : TDecodeTypesAsUnion<Types>> = Result
 */

// ✓ TTemplateLiteralDecodeUnsafe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTemplateLiteralDecodeUnsafe(Pattern: string, Types: TSchema[] = TParsePatternIntoTypes(Pattern), Result: TSchema = matches<[ ]>(Types) ? TString : (matches<true>(TIsTemplateLiteralFinite(Types)) ? TDecodeTypes(Types) : TTemplateLiteral(Pattern))) {
  return Result
}
/* compiles to:
 * export type TTemplateLiteralDecodeUnsafe<Pattern extends string, Types extends TSchema[] = TParsePatternIntoTypes<Pattern>, Result extends TSchema = Types extends [] ? TString : TIsTemplateLiteralFinite<Types> extends true ? TDecodeTypes<Types> : TTemplateLiteral<Pattern>> = Result
 */

// ✓ TTemplateLiteralDecode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTemplateLiteralDecode(Pattern: string, Decoded: TSchema = TTemplateLiteralDecodeUnsafe(Pattern), Result: TSchema = matches<TTemplateLiteral>(Decoded) ? TString : Decoded) {
  return Result
}
/* compiles to:
 * export type TTemplateLiteralDecode<Pattern extends string, Decoded extends TSchema = TTemplateLiteralDecodeUnsafe<Pattern>, Result extends TSchema = Decoded extends TTemplateLiteral ? TString : Decoded> = Result
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/template_literal/encode.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 16 type-level aliases: 11 covered, 5 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TBigInt: any
declare const TBoolean: any
declare const TEnum: any
declare const TEnumValue: any
declare const TEvaluateEnum: any
declare const TInteger: any
declare const TLiteral: any
declare const TLiteralValue: any
declare const TNumber: any
declare const TSchema: any
declare const TString: any
declare const TTemplateLiteral: any
declare const TTemplateLiteralAction: any
declare const TTemplateLiteralDeferred: any
declare const TUnion: any
type TBigInt<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TBoolean<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEnum<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEnumValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TEvaluateEnum<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInteger<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNumber<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTemplateLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTemplateLiteralAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTemplateLiteralDeferred<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TJoinString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TJoinString(Input: string[], Result: string = '') {
  const m1 = matches<[ Hole<"Left", string>, ...Hole<"Right", string[]> ]>(Input)
  if (m1) {
    if (matches<''>(Result)) {
      return TJoinString(m1.Right, m1.Left)
    }
    return TJoinString(m1.Right, `${Result}|${m1.Left}`)
  }
  return Result
}
/* compiles to:
 * export type TJoinString<Input extends string[], Result extends string = ''> = Input extends [infer Left extends string, ...(infer Right extends string[])] ? Result extends '' ? TJoinString<Right, Left> : TJoinString<Right, `${Result}|${Left}`> : Result
 */

// ✓ TUnwrapTemplateLiteralPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TUnwrapTemplateLiteralPattern(Pattern: string) {
  const m1 = matches<`^${Hole<"Pattern", string>}$`>(Pattern)
  if (m1) {
    return m1.Pattern
  }
  return never
}
/* compiles to:
 * export type TUnwrapTemplateLiteralPattern<Pattern extends string> = Pattern extends `^${infer Pattern extends string}$` ? Pattern : never
 */

// ✓ TEncodeLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeLiteral(Value: TLiteralValue, Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${Value}`)
}
/* compiles to:
 * export type TEncodeLiteral<Value extends TLiteralValue, Right extends TSchema[], Pattern extends string> = TEncodeTypes<Right, `${Pattern}${Value}`>
 */

// ✗ TEncodeBigInt: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TEncodeBigInt(Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${raw('typeof BigIntPattern')}`)
}

// ✗ TEncodeInteger: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TEncodeInteger(Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${raw('typeof IntegerPattern')}`)
}

// ✗ TEncodeNumber: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TEncodeNumber(Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${raw('typeof NumberPattern')}`)
}

// ✓ TEncodeBoolean: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeBoolean(Right: TSchema[], Pattern: string) {
  return TEncodeType(TUnion([TLiteral('false'), TLiteral('true')]), Right, Pattern)
}
/* compiles to:
 * export type TEncodeBoolean<Right extends TSchema[], Pattern extends string> = TEncodeType<TUnion<[TLiteral<'false'>, TLiteral<'true'>]>, Right, Pattern>
 */

// ✗ TEncodeString: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TEncodeString(Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${raw('typeof StringPattern')}`)
}

// ✓ TEncodeTemplateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeTemplateLiteral(TemplatePattern: string, Right: TSchema[], Pattern: string) {
  return TEncodeTypes(Right, `${Pattern}${TUnwrapTemplateLiteralPattern(TemplatePattern)}`)
}
/* compiles to:
 * export type TEncodeTemplateLiteral<TemplatePattern extends string, Right extends TSchema[], Pattern extends string> = TEncodeTypes<Right, `${Pattern}${TUnwrapTemplateLiteralPattern<TemplatePattern>}`>
 */

// ✓ TEncodeTemplateLiteralDeferred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeTemplateLiteralDeferred(Types: TSchema[], Right: TSchema[], Pattern: string, TemplateLiteral: TSchema = TTemplateLiteralAction(Types), Result: TSchema = TEncodeType(TemplateLiteral, Right, Pattern)) {
  return Result
}
/* compiles to:
 * export type TEncodeTemplateLiteralDeferred<Types extends TSchema[], Right extends TSchema[], Pattern extends string, TemplateLiteral extends TSchema = TTemplateLiteralAction<Types>, Result extends TSchema = TEncodeType<TemplateLiteral, Right, Pattern>> = Result
 */

// ✓ TEncodeEnum: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeEnum(Values: TEnumValue[], Right: TSchema[], Pattern: string, Evaluated: TSchema = TEvaluateEnum(Values)) {
  return TEncodeType(Evaluated, Right, Pattern)
}
/* compiles to:
 * export type TEncodeEnum<Values extends TEnumValue[], Right extends TSchema[], Pattern extends string, Evaluated extends TSchema = TEvaluateEnum<Values>> = TEncodeType<Evaluated, Right, Pattern>
 */

// ✓ TEncodeUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeUnion(Types: TSchema[], Right: TSchema[], Pattern: string) {
  let Result: any[] = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Head", TSchema>, ...Hole<"Tail", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Tail
    Result.push(TEncodeType(m1.Head, [], ''))
  }
  return TEncodeTypes(Right, `${Pattern}(${TJoinString(Result)})`)
}
/* compiles to:
 * export type TEncodeUnion<Types extends TSchema[], Right extends TSchema[], Pattern extends string> = TEncodeUnion__loop<Types, []>
 * type TEncodeUnion__loop<Types extends TSchema[], Result extends any[]> = Types extends [infer Head extends TSchema, ...(infer Tail extends TSchema[])] ? TEncodeUnion__loop<Tail, [...Result, TEncodeType<Head, [], ''>]> : TEncodeTypes<Right, `${Pattern}(${TJoinString<Result>})`>
 */

// ✗ TEncodeType: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function TEncodeType(Type: TSchema, Right: TSchema[], Pattern: string) {
  const m1 = matches<TEnum<Hole<"Values", TEnumValue[]>>>(Type)
  if (m1) {
    return TEncodeEnum(m1.Values, Right, Pattern)
  }
  if (matches<TInteger>(Type)) {
    return TEncodeInteger(Right, Pattern)
  }
  const m2 = matches<TLiteral<Hole<"Value", TLiteralValue>>>(Type)
  if (m2) {
    return TEncodeLiteral(m2.Value, Right, Pattern)
  }
  if (matches<TBigInt>(Type)) {
    return TEncodeBigInt(Right, Pattern)
  }
  if (matches<TBoolean>(Type)) {
    return TEncodeBoolean(Right, Pattern)
  }
  if (matches<TNumber>(Type)) {
    return TEncodeNumber(Right, Pattern)
  }
  if (matches<TString>(Type)) {
    return TEncodeString(Right, Pattern)
  }
  const m3 = matches<TTemplateLiteral<Hole<"TemplatePattern", string>>>(Type)
  if (m3) {
    return TEncodeTemplateLiteral(m3.TemplatePattern, Right, Pattern)
  }
  const m4 = matches<TTemplateLiteralDeferred<Hole<"Types", TSchema[]>>>(Type)
  if (m4) {
    return TEncodeTemplateLiteralDeferred(m4.Types, Right, Pattern)
  }
  const m5 = matches<TUnion<Hole<"Types", TSchema[]>>>(Type)
  if (m5) {
    return TEncodeUnion(m5.Types, Right, Pattern)
  }
  return raw('typeof NeverPattern')
}

// ✓ TEncodeTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodeTypes(Types: TSchema[], Pattern: string) {
  const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(Types)
  if (m1) {
    return TEncodeType(m1.Left, m1.Right, Pattern)
  }
  return Pattern
}
/* compiles to:
 * export type TEncodeTypes<Types extends TSchema[], Pattern extends string> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TEncodeType<Left, Right, Pattern> : Pattern
 */

// ✓ TEncodePattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TEncodePattern(Types: TSchema[], Encoded: string = TEncodeTypes(Types, ''), Result: string = `^${Encoded}$`) {
  return Result
}
/* compiles to:
 * export type TEncodePattern<Types extends TSchema[], Encoded extends string = TEncodeTypes<Types, ''>, Result extends string = `^${Encoded}$`> = Result
 */

// ✓ TTemplateLiteralEncode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTemplateLiteralEncode(Types: TSchema[], Pattern: string = TEncodePattern(Types), Result: TSchema = TTemplateLiteral(Pattern)) {
  return Result
}
/* compiles to:
 * export type TTemplateLiteralEncode<Types extends TSchema[], Pattern extends string = TEncodePattern<Types>, Result extends TSchema = TTemplateLiteral<Pattern>> = Result
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/template_literal/is_pattern.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TParsePatternIntoTypes } from '../../../../../../../03-schema-and-type-level-parsers/typebox/src/type/engine/patterns/pattern.js'
import type { TSchema } from '../../../../../../../03-schema-and-type-level-parsers/typebox/src/type/types/schema.js'
declare const TParsePatternIntoTypes: any
declare const TSchema: any
// ✓ TIsTemplateLiteralPattern: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TIsTemplateLiteralPattern(Pattern: string, Types: TSchema[] = TParsePatternIntoTypes(Pattern), Result: boolean = matches<[ ]>(Types) ? false : true): any {
  return Result
}
/* compiles to:
 * export type TIsTemplateLiteralPattern<
 *   Pattern extends string,
 *   Types extends TSchema[] = TParsePatternIntoTypes<Pattern>,
 *   Result extends boolean = Types extends [] ? false : true
 * > =
 *   Result
 */

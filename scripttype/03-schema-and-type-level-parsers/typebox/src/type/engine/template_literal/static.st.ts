/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/template_literal/static.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 9 type-level aliases: 8 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TBigInt: any
declare const TInteger: any
declare const TLiteralValue: any
declare const TNumber: any
declare const TParsePatternIntoTypes: any
declare const TSchema: any
declare const TString: any
declare const m2: any
type TBigInt<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInteger<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNumber<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TParsePatternIntoTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TFromLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromLiteral(Template: string, Value: TLiteralValue) {
  return `${Template}${Value}`
}
/* compiles to:
 * export type TFromLiteral<Template extends string, Value extends TLiteralValue> = `${Template}${Value}`
 */

// ✓ TFromBigInt: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromBigInt(Template: string) {
  return `${Template}${bigint}`
}
/* compiles to:
 * export type TFromBigInt<Template extends string> = `${Template}${bigint}`
 */

// ✓ TFromString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromString(Template: string) {
  return `${Template}${string}`
}
/* compiles to:
 * export type TFromString<Template extends string> = `${Template}${string}`
 */

// ✓ TFromNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromNumber(Template: string) {
  return `${Template}${number}`
}
/* compiles to:
 * export type TFromNumber<Template extends string> = `${Template}${number}`
 */

// ✓ TFromInteger: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromInteger(Template: string) {
  return `${Template}${number}`
}
/* compiles to:
 * export type TFromInteger<Template extends string> = `${Template}${number}`
 */

// ✓ TFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnion(Template: string, Types: TSchema[]) {
  let Result = never
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = anyOf(Result, TFromType('', m1.Left))
  }
  return `${Template}${Result}`
}
/* compiles to:
 * export type TFromUnion<Template extends string, Types extends TSchema[]> = TFromUnion__loop<Types, never>
 * type TFromUnion__loop<Types extends TSchema[], Result> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFromUnion__loop<Right, Result | TFromType<'', Left>> : `${Template}${Result}`
 */

// ✗ TFromType: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TFromType(Template: string, Type: TSchema, Result: string = m1 ? TFromUnion(Template, m1.Types) : (m2 ? TFromLiteral(Template, m2.Value) : (matches<TBigInt>(Type) ? TFromBigInt(Template) : (matches<TString>(Type) ? TFromString(Template) : (matches<TNumber>(Type) ? TFromNumber(Template) : (matches<TInteger>(Type) ? TFromInteger(Template) : never)))))) {
  return Result
}
/* compiles to:
 * export type TFromType<Template extends string, Type extends TSchema, Result extends string = m1 extends true ? TFromUnion<Template, m1.Types> : m2 extends true ? TFromLiteral<Template, m2.Value> : Type extends TBigInt ? TFromBigInt<Template> : Type extends TString ? TFromString<Template> : Type extends TNumber ? TFromNumber<Template> : Type extends TInteger ? TFromInteger<Template> : never> = Result
 */

// ✓ TFromSpan: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromSpan(Template: string, Types: TSchema[]) {
  let template = Template
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    template = TFromType(template, m1.Left)
    types = m1.Right
  }
  return template
}
/* compiles to:
 * export type TFromSpan<Template extends string, Types extends TSchema[]> = TFromSpan__loop<Template, Types>
 * type TFromSpan__loop<Template extends string, Types extends TSchema[]> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFromSpan__loop<TFromType<Template, Left>, Right> : Template
 */

// ✓ TTemplateLiteralStatic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTemplateLiteralStatic(Pattern: string, Types: TSchema[] = TParsePatternIntoTypes(Pattern)) {
  return TFromSpan('', Types)
}
/* compiles to:
 * export type TTemplateLiteralStatic<Pattern extends string, Types extends TSchema[] = TParsePatternIntoTypes<Pattern>> = TFromSpan<'', Types>
 */

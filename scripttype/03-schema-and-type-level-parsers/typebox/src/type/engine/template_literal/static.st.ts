/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/template_literal/static.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 9 type-level aliases: 9 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TBigInt: any
declare const TInteger: any
declare const TLiteral: any
declare const TLiteralValue: any
declare const TNumber: any
declare const TParsePatternIntoTypes: any
declare const TSchema: any
declare const TString: any
declare const TUnion: any
type TBigInt<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TInteger<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TLiteral<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TLiteralValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TNumber<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TParsePatternIntoTypes<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TSchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TString<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TUnion<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ TFromLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromLiteral(Template: string, Value: TLiteralValue) {
  return `${Template}${Value}`
}
/* compiles to:
 * export type TFromLiteral<Template extends string, Value extends TLiteralValue> = `${Template}${Value}`
 */

// ✓ TFromBigInt: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromBigInt(Template: string) {
  return `${Template}${bigint}`
}
/* compiles to:
 * export type TFromBigInt<Template extends string> = `${Template}${bigint}`
 */

// ✓ TFromString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromString(Template: string) {
  return `${Template}${string}`
}
/* compiles to:
 * export type TFromString<Template extends string> = `${Template}${string}`
 */

// ✓ TFromNumber: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromNumber(Template: string) {
  return `${Template}${number}`
}
/* compiles to:
 * export type TFromNumber<Template extends string> = `${Template}${number}`
 */

// ✓ TFromInteger: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromInteger(Template: string) {
  return `${Template}${number}`
}
/* compiles to:
 * export type TFromInteger<Template extends string> = `${Template}${number}`
 */

// ✓ TFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromUnion(Template: string, Types: TSchema[]) {
  let Result = never
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result = anyOf(Result, TFromType('', m1.Left))
  }
  return `${Template}${Result}`
}
/* compiles to:
 * export type TFromUnion<Template extends string, Types extends TSchema[]> = TFromUnion__loop<
 *   Types,
 *   never
 * >
 * type TFromUnion__loop<Types extends TSchema[], Result> =
 *   Types extends [infer Left extends TSchema, ...infer Right extends TSchema[]]
 *     ? TFromUnion__loop<Right, Result | TFromType<'', Left>>
 *     : `${Template}${Result}`
 */

// ✓ TFromType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromType__v1(Template, Type) {
  const m1 = matches<TUnion<Hole<"Types", TSchema[]>>>(Type)
  if (m1) {
    return TFromUnion(Template, m1.Types)
  }
  const m2 = matches<TLiteral<Hole<"Value", TLiteralValue>>>(Type)
  if (m2) {
    return TFromLiteral(Template, m2.Value)
  }
  if (matches<TBigInt>(Type)) {
    return TFromBigInt(Template)
  }
  if (matches<TString>(Type)) {
    return TFromString(Template)
  }
  if (matches<TNumber>(Type)) {
    return TFromNumber(Template)
  }
  if (matches<TInteger>(Type)) {
    return TFromInteger(Template)
  }
  return never
}
export function TFromType(Template: string, Type: TSchema, Result: string = TFromType__v1(Template, Type)) {
  return Result
}
/* compiles to:
 * export type TFromType__v1<Template, Type> =
 *   Type extends TUnion<infer Types extends TSchema[]> ? TFromUnion<Template, Types>
 *   : Type extends TLiteral<infer Value extends TLiteralValue> ? TFromLiteral<Template, Value>
 *   : Type extends TBigInt ? TFromBigInt<Template>
 *   : Type extends TString ? TFromString<Template>
 *   : Type extends TNumber ? TFromNumber<Template>
 *   : Type extends TInteger ? TFromInteger<Template>
 *   : never
 * export type TFromType<
 *   Template extends string,
 *   Type extends TSchema,
 *   Result extends string = TFromType__v1<Template, Type>
 * > =
 *   Result
 */

// ✓ TFromSpan: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromSpan(Template: string, Types: TSchema[]) {
  let template = Template
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    template = TFromType(template, m1.Left)
    types = m1.Right
  }
  return template
}
/* compiles to:
 * export type TFromSpan<Template extends string, Types extends TSchema[]> = TFromSpan__loop<
 *   Template,
 *   Types
 * >
 * type TFromSpan__loop<Template extends string, Types extends TSchema[]> =
 *   Types extends [infer Left extends TSchema, ...infer Right extends TSchema[]]
 *     ? TFromSpan__loop<TFromType<Template, Left>, Right>
 *     : Template
 */

// ✓ TTemplateLiteralStatic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTemplateLiteralStatic(Pattern: string, Types: TSchema[] = TParsePatternIntoTypes(Pattern)) {
  return TFromSpan('', Types)
}
/* compiles to:
 * export type TTemplateLiteralStatic<
 *   Pattern extends string,
 *   Types extends TSchema[] = TParsePatternIntoTypes<Pattern>
 * > =
 *   TFromSpan<'', Types>
 */

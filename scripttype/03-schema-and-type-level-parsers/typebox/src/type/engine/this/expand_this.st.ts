/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/engine/this/expand_this.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TArray: any
declare const TConstructor: any
declare const TFunction: any
declare const TIntersect: any
declare const TObject: any
declare const TProperties: any
declare const TSchema: any
declare const TThis: any
declare const TTuple: any
declare const TUnion: any
type TArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TConstructor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TIntersect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TThis<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TFromTypes: the ScriptType does not itself typecheck as TypeScript
//   TFromTypes.st.ts(11:17) TS2345: Argument of type 'any' is not assignable to parameter of type 'never'.
/* @scripttype preserveParamNames */
export function TFromTypes(Properties: TProperties, Types: TSchema[]) {
  let Result = []
  let types = Types
  while (true) {
    const m1 = matches<[ Hole<"Left", TSchema>, ...Hole<"Right", TSchema[]> ]>(types)
    if (!m1) {
      break
    }
    types = m1.Right
    Result.push(TFromType(Properties, m1.Left))
  }
  return Result
}
/* compiles to:
 * export type TFromTypes<Properties extends TProperties, Types extends TSchema[]> = TFromTypes__loop<Types, [], Properties>
 * type TFromTypes__loop<Types extends TSchema[], Result extends readonly unknown[], Properties extends TProperties> = Types extends [infer Left extends TSchema, ...(infer Right extends TSchema[])] ? TFromTypes__loop<Right, [...Result, TFromType<Properties, Left>], Properties> : Result
 */

// ✓ TFromType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TFromType(Properties: TProperties, Type: TSchema) {
  const m1 = matches<TArray<Hole<"Type", TSchema>>>(Type)
  if (m1) {
    return TArray(TFromType(Properties, m1.Type))
  }
  const m2 = matches<TConstructor<Hole<"Parameters", TSchema[]>, Hole<"InstanceType", TSchema>>>(Type)
  if (m2) {
    return TConstructor(TFromTypes(Properties, m2.Parameters), TFromType(Properties, m2.InstanceType))
  }
  const m3 = matches<TFunction<Hole<"Parameters", TSchema[]>, Hole<"ReturnType", TSchema>>>(Type)
  if (m3) {
    return TFunction(TFromTypes(Properties, m3.Parameters), TFromType(Properties, m3.ReturnType))
  }
  const m4 = matches<TTuple<Hole<"Types", TSchema[]>>>(Type)
  if (m4) {
    return TTuple(TFromTypes(Properties, m4.Types))
  }
  const m5 = matches<TUnion<Hole<"Types", TSchema[]>>>(Type)
  if (m5) {
    return TUnion(TFromTypes(Properties, m5.Types))
  }
  const m6 = matches<TIntersect<Hole<"Types", TSchema[]>>>(Type)
  if (m6) {
    return TIntersect(TFromTypes(Properties, m6.Types))
  }
  if (matches<TThis>(Type)) {
    return TObject(Properties)
  }
  return Type
}
/* compiles to:
 * export type TFromType<Properties extends TProperties, Type extends TSchema> = Type extends TArray<infer Type extends TSchema> ? TArray<TFromType<Properties, Type>> : Type extends TConstructor<infer Parameters extends TSchema[], infer InstanceType extends TSchema> ? TConstructor<TFromTypes<Properties, Parameters>, TFromType<Properties, InstanceType>> : Type extends TFunction<infer Parameters extends TSchema[], infer ReturnType extends TSchema> ? TFunction<TFromTypes<Properties, Parameters>, TFromType<Properties, ReturnType>> : Type extends TTuple<infer Types extends TSchema[]> ? TTuple<TFromTypes<Properties, Types>> : Type extends TUnion<infer Types extends TSchema[]> ? TUnion<TFromTypes<Properties, Types>> : Type extends TIntersect<infer Types extends TSchema[]> ? TIntersect<TFromTypes<Properties, Types>> : Type extends TThis ? TObject<Properties> : Type
 */

// ✓ TExpandThis: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExpandThis(Properties: TProperties, Type: TSchema, Result: TSchema = TFromType(Properties, Type)) {
  return Result
}
/* compiles to:
 * export type TExpandThis<Properties extends TProperties, Type extends TSchema, Result extends TSchema = TFromType<Properties, Type>> = Result
 */

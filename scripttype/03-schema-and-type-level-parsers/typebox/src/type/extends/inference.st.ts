/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/extends/inference.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 2 compile-error, 2 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Memory: any
declare const TExtendsLeft: any
declare const TExtendsTrueLike: any
declare const TInferable: any
declare const TProperties: any
declare const TSchema: any
declare const TTuple: any
declare const TUnion: any
declare const TUnknown: any
declare const TUnreachable: any
declare const m2: any
declare const m3: any
type Memory<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TExtendsLeft<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TExtendsTrueLike<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInferable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnknown<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnreachable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m2<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type m3<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TTryRestInferable: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TTryRestInferable(Type: TSchema, Result: TInferable | undefined = m1 ? (m2 ? (m3 ? TInferable(m2.Name, m3.Type) : (matches<TUnknown>(m2.Type) ? TInferable(m2.Name, m2.Type) : Undefined)) : TUnreachable) : Undefined) {
  return Result
}
/* compiles to:
 * export type TTryRestInferable<Type extends TSchema, Result extends TInferable | undefined = m1 extends true ? m2 extends true ? m3 extends true ? TInferable<m2.Name, m3.Type> : m2.Type extends TUnknown ? TInferable<m2.Name, m2.Type> : undefined : TUnreachable : undefined> = Result
 */

// ✗ TTryInferable: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TTryInferable(Type: TSchema, Result: TInferable | undefined = m1 ? TInferable(m1.Name, m1.Type) : Undefined) {
  return Result
}
/* compiles to:
 * export type TTryInferable<Type extends TSchema, Result extends TInferable | undefined = m1 extends true ? TInferable<m1.Name, m1.Type> : undefined> = Result
 */

// ✗ TryInferResults: the ScriptType does not itself typecheck as TypeScript
//   TryInferResults.st.ts(5:17) TS2503: Cannot find namespace 'Result'.
/* @scripttype preserveParamNames */
export function TryInferResults(Rest: TSchema[], Right: TSchema, Result: TSchema[] = []) {
  const m1 = matches<[ Hole<"Head", TSchema>, ...Hole<"Tail", TSchema[]> ]>(Rest)
  if (m1) {
    if (matches<Result.TExtendsTrueLike>(TExtendsLeft({}, m1.Head, Right))) {
      return TryInferResults(m1.Tail, Right, [...Result, m1.Head])
    }
    return Undefined
  }
  return Result
}
/* compiles to:
 * export type TryInferResults<Rest extends TSchema[], Right extends TSchema, Result extends TSchema[] = []> = Rest extends [infer Head extends TSchema, ...(infer Tail extends TSchema[])] ? TExtendsLeft<{}, Head, Right> extends Result.TExtendsTrueLike ? TryInferResults<Tail, Right, [...Result, Head]> : undefined : Result
 */

// ✓ TInferTupleResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TInferTupleResult(Inferred: TProperties, Name: string, Left: TSchema[], Right: TSchema, Results: TSchema[] | undefined = TryInferResults(Left, Right)) {
  const m1 = matches<[ ...Hole<"Results", TSchema[]> ]>(Results)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(Name)) {
      out[_] = TTuple(m1.Results)
    }
    return Result.TExtendsTrue(Memory.TAssign(Inferred, out))
  }
  return Result.TExtendsFalse
}
/* compiles to:
 * export type TInferTupleResult<Inferred extends TProperties, Name extends string, Left extends TSchema[], Right extends TSchema, Results extends TSchema[] | undefined = TryInferResults<Left, Right>> = Results extends [...(infer Results extends TSchema[])] ? Result.TExtendsTrue<Memory.TAssign<Inferred, { [_ in Name]: TTuple<Results> }>> : Result.TExtendsFalse
 */

// ✓ TInferUnionResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TInferUnionResult(Inferred: TProperties, Name: string, Left: TSchema[], Right: TSchema, Results: TSchema[] | undefined = TryInferResults(Left, Right)) {
  const m1 = matches<[ ...Hole<"Results", TSchema[]> ]>(Results)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(Name)) {
      out[_] = TUnion(m1.Results)
    }
    return Result.TExtendsTrue(Memory.TAssign(Inferred, out))
  }
  return Result.TExtendsFalse
}
/* compiles to:
 * export type TInferUnionResult<Inferred extends TProperties, Name extends string, Left extends TSchema[], Right extends TSchema, Results extends TSchema[] | undefined = TryInferResults<Left, Right>> = Results extends [...(infer Results extends TSchema[])] ? Result.TExtendsTrue<Memory.TAssign<Inferred, { [_ in Name]: TUnion<Results> }>> : Result.TExtendsFalse
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/script/mapping.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 132 type-level aliases: 68 covered, 63 typecheck-error, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Memory: any
declare const PropertyKey: any
declare const S: any
declare const T: any
declare const TInteger: any
declare const TIntegerKey: any
declare const TIntersect: any
declare const TLiteralValue: any
declare const TModifierOperation: any
declare const TNumber: any
declare const TNumberKey: any
declare const TParameter: any
declare const TProperties: any
declare const TSchema: any
declare const TString: any
declare const TStringKey: any
declare const TSymbol: any
declare const TUnion: any
declare const patternProperties: any
declare const properties: any
type Memory<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type S<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type T<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TInteger<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TIntegerKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TIntersect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TLiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TModifierOperation<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNumber<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TNumberKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TParameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TString<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TStringKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TSymbol<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TUnion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type patternProperties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type properties<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ TIntrinsicOrCall: the ScriptType does not itself typecheck as TypeScript
//   TIntrinsicOrCall.st.ts(2:62) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TIntrinsicOrCall(Target: string, Parameters: T.TSchema[]) {
  const m1 = matches<[ 'Array', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m1) {
    return T.TArray(m1.Type)
  }
  const m2 = matches<[ 'Capitalize', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m2) {
    return S.TCapitalizeDeferred(m2.Type)
  }
  const m3 = matches<[ 'ConstructorParameters', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m3) {
    return S.TConstructorParametersDeferred(m3.Type)
  }
  const m4 = matches<[ 'Evaluate', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m4) {
    return S.TEvaluateDeferred(m4.Type)
  }
  const m5 = matches<[ 'Exclude', [ Hole<"Left", T.TSchema>, Hole<"Right", T.TSchema> ] ]>([Target, Parameters])
  if (m5) {
    return S.TExcludeDeferred(m5.Left, m5.Right)
  }
  const m6 = matches<[ 'Extract', [ Hole<"Left", T.TSchema>, Hole<"Right", T.TSchema> ] ]>([Target, Parameters])
  if (m6) {
    return S.TExtractDeferred(m6.Left, m6.Right)
  }
  const m7 = matches<[ 'Index', [ Hole<"Type", T.TSchema>, Hole<"Indexer", T.TSchema> ] ]>([Target, Parameters])
  if (m7) {
    return S.TIndexDeferred(m7.Type, m7.Indexer)
  }
  const m8 = matches<[ 'InstanceType', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m8) {
    return S.TInstanceTypeDeferred(m8.Type)
  }
  const m9 = matches<[ 'KeyOf', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m9) {
    return S.TKeyOfDeferred(m9.Type)
  }
  const m10 = matches<[ 'Lowercase', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m10) {
    return S.TLowercaseDeferred(m10.Type)
  }
  const m11 = matches<[ 'NonNullable', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m11) {
    return S.TNonNullableDeferred(m11.Type)
  }
  const m12 = matches<[ 'Omit', [ Hole<"Type", T.TSchema>, Hole<"Indexer", T.TSchema> ] ]>([Target, Parameters])
  if (m12) {
    return S.TOmitDeferred(m12.Type, m12.Indexer)
  }
  const m13 = matches<[ 'Parameters', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m13) {
    return S.TParametersDeferred(m13.Type)
  }
  const m14 = matches<[ 'Partial', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m14) {
    return S.TPartialDeferred(m14.Type)
  }
  const m15 = matches<[ 'Pick', [ Hole<"Type", T.TSchema>, Hole<"Indexer", T.TSchema> ] ]>([Target, Parameters])
  if (m15) {
    return S.TPickDeferred(m15.Type, m15.Indexer)
  }
  const m16 = matches<[ 'Readonly', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m16) {
    return S.TReadonlyObjectDeferred(m16.Type)
  }
  const m17 = matches<[ 'Record', [ Hole<"Key", T.TSchema>, Hole<"Value", T.TSchema> ] ]>([Target, Parameters])
  if (m17) {
    return T.TRecordDeferred(m17.Key, m17.Value)
  }
  const m18 = matches<[ 'Required', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m18) {
    return S.TRequiredDeferred(m18.Type)
  }
  const m19 = matches<[ 'ReturnType', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m19) {
    return S.TReturnTypeDeferred(m19.Type)
  }
  const m20 = matches<[ 'Uncapitalize', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m20) {
    return S.TUncapitalizeDeferred(m20.Type)
  }
  const m21 = matches<[ 'Uppercase', [ Hole<"Type", T.TSchema> ] ]>([Target, Parameters])
  if (m21) {
    return S.TUppercaseDeferred(m21.Type)
  }
  return T.TCallConstruct(T.TRef(Target), Parameters)
}
/* compiles to:
 * export type TIntrinsicOrCall<Target extends string, Parameters extends T.TSchema[]> = [Target, Parameters] extends ['Array', [infer Type extends T.TSchema]] ? T.TArray<Type> : [Target, Parameters] extends ['Capitalize', [infer Type extends T.TSchema]] ? S.TCapitalizeDeferred<Type> : [Target, Parameters] extends ['ConstructorParameters', [infer Type extends T.TSchema]] ? S.TConstructorParametersDeferred<Type> : [Target, Parameters] extends ['Evaluate', [infer Type extends T.TSchema]] ? S.TEvaluateDeferred<Type> : [Target, Parameters] extends ['Exclude', [infer Left extends T.TSchema, infer Right extends T.TSchema]] ? S.TExcludeDeferred<Left, Right> : [Target, Parameters] extends ['Extract', [infer Left extends T.TSchema, infer Right extends T.TSchema]] ? S.TExtractDeferred<Left, Right> : [Target, Parameters] extends ['Index', [infer Type extends T.TSchema, infer Indexer extends T.TSchema]] ? S.TIndexDeferred<Type, Indexer> : [Target, Parameters] extends ['InstanceType', [infer Type extends T.TSchema]] ? S.TInstanceTypeDeferred<Type> : [Target, Parameters] extends ['KeyOf', [infer Type extends T.TSchema]] ? S.TKeyOfDeferred<Type> : [Target, Parameters] extends ['Lowercase', [infer Type extends T.TSchema]] ? S.TLowercaseDeferred<Type> : [Target, Parameters] extends ['NonNullable', [infer Type extends T.TSchema]] ? S.TNonNullableDeferred<Type> : [Target, Parameters] extends ['Omit', [infer Type extends T.TSchema, infer Indexer extends T.TSchema]] ? S.TOmitDeferred<Type, Indexer> : [Target, Parameters] extends ['Parameters', [infer Type extends T.TSchema]] ? S.TParametersDeferred<Type> : [Target, Parameters] extends ['Partial', [infer Type extends T.TSchema]] ? S.TPartialDeferred<Type> : [Target, Parameters] extends ['Pick', [infer Type extends T.TSchema, infer Indexer extends T.TSchema]] ? S.TPickDeferred<Type, Indexer> : [Target, Parameters] extends ['Readonly', [infer Type extends T.TSchema]] ? S.TReadonlyObjectDeferred<Type> : [Target, Parameters] extends ['Record', [infer Key extends T.TSchema, infer Value extends T.TSchema]] ? T.TRecordDeferred<Key, Value> : [Target, Parameters] extends ['Required', [infer Type extends T.TSchema]] ? S.TRequiredDeferred<Type> : [Target, Parameters] extends ['ReturnType', [infer Type extends T.TSchema]] ? S.TReturnTypeDeferred<Type> : [Target, Parameters] extends ['Uncapitalize', [infer Type extends T.TSchema]] ? S.TUncapitalizeDeferred<Type> : [Target, Parameters] extends ['Uppercase', [infer Type extends T.TSchema]] ? S.TUppercaseDeferred<Type> : T.TCallConstruct<T.TRef<Target>, Parameters>
 */

// ✓ TDelimitedDecode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TDelimitedDecode(Input: ([ unknown, unknown ] | unknown)[], Result: unknown[] = []) {
  const m1 = matches<[ Hole<"Left">, ...Hole<"Right"> ]>(Input)
  if (m1) {
    const m2 = matches<[ Hole<"Item">, Hole<"_"> ]>(m1.Left)
    if (m2) {
      return TDelimitedDecode(m1.Right, [...Result, m2.Item])
    }
    return TDelimitedDecode(m1.Right, [...Result, m1.Left])
  }
  return Result
}
/* compiles to:
 * export type TDelimitedDecode<Input extends ([unknown, unknown] | unknown)[], Result extends unknown[] = []> = Input extends [infer Left, ...infer Right] ? Left extends [infer Item, unknown] ? TDelimitedDecode<Right, [...Result, Item]> : TDelimitedDecode<Right, [...Result, Left]> : Result
 */

// ✓ TDelimited: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TDelimited(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"Left", unknown[]>, Hole<"Right", unknown[]> ]>(Input)
  if (m1) {
    return TDelimitedDecode([...m1.Left, ...m1.Right])
  }
  return []
}
/* compiles to:
 * export type TDelimited<Input extends [unknown, unknown]> = Input extends [infer Left extends unknown[], infer Right extends unknown[]] ? TDelimitedDecode<[...Left, ...Right]> : []
 */

// ✗ TGenericParameterExtendsEqualsMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericParameterExtendsEqualsMapping.st.ts(3:73) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericParameterExtendsEqualsMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"Name", string>, 'extends', Hole<"Extends", T.TSchema>, '=', Hole<"Equals", T.TSchema> ]>(Input)
  if (m1) {
    return T.TParameter(m1.Name, m1.Extends, m1.Equals)
  }
  return never
}
/* compiles to:
 * export type TGenericParameterExtendsEqualsMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends [infer Name extends string, 'extends', infer Extends extends T.TSchema, '=', infer Equals extends T.TSchema] ? T.TParameter<Name, Extends, Equals> : never
 */

// ✗ TGenericParameterExtendsMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericParameterExtendsMapping.st.ts(3:73) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericParameterExtendsMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"Name", string>, 'extends', Hole<"Extends", T.TSchema> ]>(Input)
  if (m1) {
    return T.TParameter(m1.Name, m1.Extends, m1.Extends)
  }
  return never
}
/* compiles to:
 * export type TGenericParameterExtendsMapping<Input extends [unknown, unknown, unknown]> = Input extends [infer Name extends string, 'extends', infer Extends extends T.TSchema] ? T.TParameter<Name, Extends, Extends> : never
 */

// ✗ TGenericParameterEqualsMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericParameterEqualsMapping.st.ts(3:66) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericParameterEqualsMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"Name", string>, '=', Hole<"Equals", T.TSchema> ]>(Input)
  if (m1) {
    return T.TParameter(m1.Name, T.TUnknown, m1.Equals)
  }
  return never
}
/* compiles to:
 * export type TGenericParameterEqualsMapping<Input extends [unknown, unknown, unknown]> = Input extends [infer Name extends string, '=', infer Equals extends T.TSchema] ? T.TParameter<Name, T.TUnknown, Equals> : never
 */

// ✗ TGenericParameterIdentifierMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericParameterIdentifierMapping.st.ts(2:75) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericParameterIdentifierMapping(Input: string, Result: T.TSchema = T.TParameter(Input, T.TUnknown, T.TUnknown)) {
  return Result
}
/* compiles to:
 * export type TGenericParameterIdentifierMapping<Input extends string, Result extends T.TSchema = T.TParameter<Input, T.TUnknown, T.TUnknown>> = Result
 */

// ✓ TGenericParameterMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TGenericParameterMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TGenericParameterMapping<Input> = Input
 */

// ✓ TGenericParameterListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TGenericParameterListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TGenericParameterListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ TGenericParametersMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericParametersMapping.st.ts(3:48) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericParametersMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '<', Hole<"Parameters", T.TParameter[]>, '>' ]>(Input)
  if (m1) {
    return m1.Parameters
  }
  return never
}
/* compiles to:
 * export type TGenericParametersMapping<Input extends [unknown, unknown, unknown]> = Input extends ['<', infer Parameters extends T.TParameter[], '>'] ? Parameters : never
 */

// ✓ TGenericCallArgumentListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TGenericCallArgumentListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TGenericCallArgumentListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ TGenericCallArgumentsMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericCallArgumentsMapping.st.ts(3:47) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericCallArgumentsMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '<', Hole<"Arguments", T.TSchema[]>, '>' ]>(Input)
  if (m1) {
    return m1.Arguments
  }
  return never
}
/* compiles to:
 * export type TGenericCallArgumentsMapping<Input extends [unknown, unknown, unknown]> = Input extends ['<', infer Arguments extends T.TSchema[], '>'] ? Arguments : never
 */

// ✗ TGenericCallMapping: does not compile yet
//   Cannot find name 'm1'.
/* @scripttype preserveParamNames */
export function TGenericCallMapping(Input: [ unknown, unknown ], Result = m1 ? TIntrinsicOrCall(m1.Ref, m1.Arguments) : never) {
  return Result
}
/* compiles to:
 * export type TGenericCallMapping<Input extends [unknown, unknown], Result = m1 extends true ? TIntrinsicOrCall<m1.Ref, m1.Arguments> : never> = Result
 */

// ✓ TOptionalSemiColonMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TOptionalSemiColonMapping(Input: [ unknown ] | [ ]) {
  return Null
}
/* compiles to:
 * export type TOptionalSemiColonMapping<Input extends [unknown] | []> = null
 */

// ✓ TKeywordStringMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordStringMapping(Input: 'string') {
  return T.TString
}
/* compiles to:
 * export type TKeywordStringMapping<Input extends 'string'> = T.TString
 */

// ✓ TKeywordNumberMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordNumberMapping(Input: 'number') {
  return T.TNumber
}
/* compiles to:
 * export type TKeywordNumberMapping<Input extends 'number'> = T.TNumber
 */

// ✓ TKeywordBooleanMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordBooleanMapping(Input: 'boolean') {
  return T.TBoolean
}
/* compiles to:
 * export type TKeywordBooleanMapping<Input extends 'boolean'> = T.TBoolean
 */

// ✓ TKeywordUndefinedMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordUndefinedMapping(Input: 'undefined') {
  return T.TUndefined
}
/* compiles to:
 * export type TKeywordUndefinedMapping<Input extends 'undefined'> = T.TUndefined
 */

// ✓ TKeywordNullMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordNullMapping(Input: 'null') {
  return T.TNull
}
/* compiles to:
 * export type TKeywordNullMapping<Input extends 'null'> = T.TNull
 */

// ✓ TKeywordIntegerMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordIntegerMapping(Input: 'integer') {
  return T.TInteger
}
/* compiles to:
 * export type TKeywordIntegerMapping<Input extends 'integer'> = T.TInteger
 */

// ✓ TKeywordBigIntMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordBigIntMapping(Input: 'bigint') {
  return T.TBigInt
}
/* compiles to:
 * export type TKeywordBigIntMapping<Input extends 'bigint'> = T.TBigInt
 */

// ✓ TKeywordUnknownMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordUnknownMapping(Input: 'unknown') {
  return T.TUnknown
}
/* compiles to:
 * export type TKeywordUnknownMapping<Input extends 'unknown'> = T.TUnknown
 */

// ✓ TKeywordAnyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordAnyMapping(Input: 'any') {
  return T.TAny
}
/* compiles to:
 * export type TKeywordAnyMapping<Input extends 'any'> = T.TAny
 */

// ✓ TKeywordObjectMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordObjectMapping(Input: 'object') {
  return T.TObject({})
}
/* compiles to:
 * export type TKeywordObjectMapping<Input extends 'object'> = T.TObject<{}>
 */

// ✓ TKeywordNeverMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordNeverMapping(Input: 'never') {
  return T.TNever
}
/* compiles to:
 * export type TKeywordNeverMapping<Input extends 'never'> = T.TNever
 */

// ✓ TKeywordSymbolMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordSymbolMapping(Input: 'symbol') {
  return T.TSymbol
}
/* compiles to:
 * export type TKeywordSymbolMapping<Input extends 'symbol'> = T.TSymbol
 */

// ✓ TKeywordVoidMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordVoidMapping(Input: 'void') {
  return T.TVoid
}
/* compiles to:
 * export type TKeywordVoidMapping<Input extends 'void'> = T.TVoid
 */

// ✓ TKeywordThisMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeywordThisMapping(Input: 'this') {
  return T.TThis
}
/* compiles to:
 * export type TKeywordThisMapping<Input extends 'this'> = T.TThis
 */

// ✓ TLiteralBigIntMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TLiteralBigIntMapping(Input: string) {
  const m1 = matches<`${Hole<"Value", bigint>}`>(Input)
  if (m1) {
    return T.TLiteral(m1.Value)
  }
  return never
}
/* compiles to:
 * export type TLiteralBigIntMapping<Input extends string> = Input extends `${infer Value extends bigint}` ? T.TLiteral<Value> : never
 */

// ✓ TLiteralBooleanMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TLiteralBooleanMapping(Input: 'true' | 'false') {
  if (matches<'true'>(Input)) {
    return T.TLiteral(true)
  }
  return T.TLiteral(false)
}
/* compiles to:
 * export type TLiteralBooleanMapping<Input extends 'true' | 'false'> = Input extends 'true' ? T.TLiteral<true> : T.TLiteral<false>
 */

// ✓ TLiteralNumberMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TLiteralNumberMapping(Input: string) {
  const m1 = matches<`${Hole<"Value", number>}`>(Input)
  if (m1) {
    return T.TLiteral(m1.Value)
  }
  return never
}
/* compiles to:
 * export type TLiteralNumberMapping<Input extends string> = Input extends `${infer Value extends number}` ? T.TLiteral<Value> : never
 */

// ✗ TLiteralStringMapping: the ScriptType does not itself typecheck as TypeScript
//   TLiteralStringMapping.st.ts(3:15) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TLiteralStringMapping(Input: string) {
  if (matches<T.TLiteralValue>(Input)) {
    return T.TLiteral(Input)
  }
  return never
}
/* compiles to:
 * export type TLiteralStringMapping<Input extends string> = Input extends T.TLiteralValue ? T.TLiteral<Input> : never
 */

// ✗ TTemplateInterpolateMapping: the ScriptType does not itself typecheck as TypeScript
//   TTemplateInterpolateMapping.st.ts(3:43) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTemplateInterpolateMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '${', Hole<"Type", T.TSchema>, '}' ]>(Input)
  if (m1) {
    return m1.Type
  }
  return never
}
/* compiles to:
 * export type TTemplateInterpolateMapping<Input extends [unknown, unknown, unknown]> = Input extends ['${', infer Type extends T.TSchema, '}'] ? Type : never
 */

// ✗ TTemplateSpanMapping: the ScriptType does not itself typecheck as TypeScript
//   TTemplateSpanMapping.st.ts(2:61) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTemplateSpanMapping(Input: string, Result: T.TSchema = T.TLiteral(Input)) {
  return Result
}
/* compiles to:
 * export type TTemplateSpanMapping<Input extends string, Result extends T.TSchema = T.TLiteral<Input>> = Result
 */

// ✗ TTemplateBodyMapping: the ScriptType does not itself typecheck as TypeScript
//   TTemplateBodyMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTemplateBodyMapping(Input: [ unknown, unknown, unknown ] | [ unknown ]) {
  const m1 = matches<[ Hole<"Text", T.TSchema>, Hole<"Type", T.TSchema>, Hole<"Rest", T.TSchema[]> ]>(Input)
  if (m1) {
    return [m1.Text, m1.Type, ...m1.Rest]
  }
  const m2 = matches<[ Hole<"Text", T.TSchema> ]>(Input)
  if (m2) {
    return [m2.Text]
  }
  return []
}
/* compiles to:
 * export type TTemplateBodyMapping<Input extends [unknown, unknown, unknown] | [unknown]> = Input extends [infer Text extends T.TSchema, infer Type extends T.TSchema, infer Rest extends T.TSchema[]] ? [Text, Type, ...Rest] : Input extends [infer Text extends T.TSchema] ? [Text] : []
 */

// ✗ TTemplateLiteralTypesMapping: the ScriptType does not itself typecheck as TypeScript
//   TTemplateLiteralTypesMapping.st.ts(2:92) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTemplateLiteralTypesMapping(Input: [ unknown, unknown, unknown ], Result: T.TSchema = m1 ? m1.Types : []) {
  return Result
}
/* compiles to:
 * export type TTemplateLiteralTypesMapping<Input extends [unknown, unknown, unknown], Result extends T.TSchema = m1 extends true ? m1.Types : []> = Result
 */

// ✗ TTemplateLiteralMapping: the ScriptType does not itself typecheck as TypeScript
//   TTemplateLiteralMapping.st.ts(3:36) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTemplateLiteralMapping(Input: unknown) {
  const m1 = matches<Hole<"Types", T.TSchema[]>>(Input)
  if (m1) {
    return T.TTemplateLiteralDeferred(m1.Types)
  }
  return never
}
/* compiles to:
 * export type TTemplateLiteralMapping<Input> = Input extends (infer Types extends T.TSchema[]) ? T.TTemplateLiteralDeferred<Types> : never
 */

// ✗ TDependentMapping: the ScriptType does not itself typecheck as TypeScript
//   TDependentMapping.st.ts(3:41) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TDependentMapping(Input: [ unknown, unknown, unknown, unknown, unknown, unknown ] | [ unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'if', Hole<"If", T.TSchema>, 'then', Hole<"Then", T.TSchema>, 'else', Hole<"Else", T.TSchema> ]>(Input)
  if (m1) {
    return T.TDependent(m1.If, m1.Then, m1.Else)
  }
  const m2 = matches<[ 'if', Hole<"If", T.TSchema>, 'then', Hole<"Then", T.TSchema> ]>(Input)
  if (m2) {
    return T.TDependent(m2.If, m2.Then, T.TUnknown)
  }
  return never
}
/* compiles to:
 * export type TDependentMapping<Input extends [unknown, unknown, unknown, unknown, unknown, unknown] | [unknown, unknown, unknown, unknown]> = Input extends ['if', infer If extends T.TSchema, 'then', infer Then extends T.TSchema, 'else', infer Else extends T.TSchema] ? T.TDependent<If, Then, Else> : Input extends ['if', infer If extends T.TSchema, 'then', infer Then extends T.TSchema] ? T.TDependent<If, Then, T.TUnknown> : never
 */

// ✓ TKeyOfMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TKeyOfMapping(Input: [ unknown ] | [ ]) {
  if (matches<[ unknown ]>(Input)) {
    return true
  }
  return false
}
/* compiles to:
 * export type TKeyOfMapping<Input extends [unknown] | []> = Input extends [unknown] ? true : false
 */

// ✗ TIndexArrayMappingReduce: the ScriptType does not itself typecheck as TypeScript
//   TIndexArrayMappingReduce.st.ts(5:44) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TIndexArrayMappingReduce(Input: unknown[], Result: unknown[] = []) {
  const m1 = matches<[ Hole<"Left", unknown>, ...Hole<"Right", unknown[]> ]>(Input)
  if (m1) {
    const m2 = matches<[ '[', Hole<"Type", T.TSchema>, ']' ]>(m1.Left)
    if (m2) {
      return TIndexArrayMappingReduce(m1.Right, [...Result, [m2.Type]])
    }
    return TIndexArrayMappingReduce(m1.Right, [...Result, []])
  }
  return Result
}
/* compiles to:
 * export type TIndexArrayMappingReduce<Input extends unknown[], Result extends unknown[] = []> = Input extends [infer Left extends unknown, ...(infer Right extends unknown[])] ? Left extends ['[', infer Type extends T.TSchema, ']'] ? TIndexArrayMappingReduce<Right, [...Result, [Type]]> : TIndexArrayMappingReduce<Right, [...Result, []]> : Result
 */

// ✓ TIndexArrayMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TIndexArrayMapping(Input: ([ unknown, unknown, unknown ] | [ unknown, unknown ])[]) {
  if (matches<unknown[]>(Input)) {
    return TIndexArrayMappingReduce(Input)
  }
  return []
}
/* compiles to:
 * export type TIndexArrayMapping<Input extends ([unknown, unknown, unknown] | [unknown, unknown])[]> = Input extends unknown[] ? TIndexArrayMappingReduce<Input> : []
 */

// ✗ TExtendsMapping: the ScriptType does not itself typecheck as TypeScript
//   TExtendsMapping.st.ts(3:48) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExtendsMapping(Input: [ unknown, unknown, unknown, unknown, unknown, unknown ] | [ ]) {
  const m1 = matches<[ 'extends', Hole<"Type", T.TSchema>, '?', Hole<"True", T.TSchema>, ':', Hole<"False", T.TSchema> ]>(Input)
  if (m1) {
    return [m1.Type, m1.True, m1.False]
  }
  return []
}
/* compiles to:
 * export type TExtendsMapping<Input extends [unknown, unknown, unknown, unknown, unknown, unknown] | []> = Input extends ['extends', infer Type extends T.TSchema, '?', infer True extends T.TSchema, ':', infer False extends T.TSchema] ? [Type, True, False] : []
 */

// ✗ TBaseMapping: the ScriptType does not itself typecheck as TypeScript
//   TBaseMapping.st.ts(3:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TBaseMapping(Input: [ unknown, unknown, unknown ] | unknown) {
  const m1 = matches<[ '(', Hole<"Type", T.TSchema>, ')' ]>(Input)
  if (m1) {
    return m1.Type
  }
  const m2 = matches<Hole<"Type", T.TSchema>>(Input)
  if (m2) {
    return m2.Type
  }
  return never
}
/* compiles to:
 * export type TBaseMapping<Input extends [unknown, unknown, unknown] | unknown> = Input extends ['(', infer Type extends T.TSchema, ')'] ? Type : Input extends (infer Type extends T.TSchema) ? Type : never
 */

// ✓ TWithMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithMapping(Input: [ unknown, unknown ] | [ ]) {
  const m1 = matches<[ 'with', Hole<"WithObject", Record<PropertyKey, unknown>> ]>(Input)
  if (m1) {
    return m1.WithObject
  }
  return []
}
/* compiles to:
 * export type TWithMapping<Input extends [unknown, unknown] | []> = Input extends ['with', infer WithObject extends Record<PropertyKey, unknown>] ? WithObject : []
 */

// ✗ TFactorIndexArray: the ScriptType does not itself typecheck as TypeScript
//   TFactorIndexArray.st.ts(2:41) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TFactorIndexArray(Type: T.TSchema, IndexArray: unknown[]) {
  const m1 = matches<[ Hole<"Left", T.TSchema[]>, ...Hole<"Right", unknown[]> ]>(IndexArray)
  if (m1) {
    const m2 = matches<[ Hole<"Indexer", T.TSchema> ]>(m1.Left)
    if (m2) {
      return TFactorIndexArray(S.TIndexDeferred(Type, m2.Indexer), m1.Right)
    }
    if (matches<[ ]>(m1.Left)) {
      return TFactorIndexArray(T.TArray(Type), m1.Right)
    }
    return T.TNever
  }
  return Type
}
/* compiles to:
 * export type TFactorIndexArray<Type extends T.TSchema, IndexArray extends unknown[]> = IndexArray extends [infer Left extends T.TSchema[], ...(infer Right extends unknown[])] ? Left extends [infer Indexer extends T.TSchema] ? TFactorIndexArray<S.TIndexDeferred<Type, Indexer>, Right> : Left extends [] ? TFactorIndexArray<T.TArray<Type>, Right> : T.TNever : Type
 */

// ✗ TFactorExtends: the ScriptType does not itself typecheck as TypeScript
//   TFactorExtends.st.ts(2:38) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TFactorExtends(Type: T.TSchema, Extends: unknown[]) {
  const m1 = matches<[ Hole<"Right", T.TSchema>, Hole<"True", T.TSchema>, Hole<"False", T.TSchema> ]>(Extends)
  if (m1) {
    return S.TConditionalDeferred(Type, m1.Right, m1.True, m1.False)
  }
  return Type
}
/* compiles to:
 * export type TFactorExtends<Type extends T.TSchema, Extends extends unknown[]> = Extends extends [infer Right extends T.TSchema, infer True extends T.TSchema, infer False extends T.TSchema] ? S.TConditionalDeferred<Type, Right, True, False> : Type
 */

// ✗ TFactorWith: the ScriptType does not itself typecheck as TypeScript
//   TFactorWith.st.ts(2:35) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TFactorWith(Type: T.TSchema, With: unknown) {
  if (matches<Record<PropertyKey, unknown>>(With)) {
    return S.TWithDeferred(Type, With)
  }
  return Type
}
/* compiles to:
 * export type TFactorWith<Type extends T.TSchema, With> = With extends Record<PropertyKey, unknown> ? S.TWithDeferred<Type, With> : Type
 */

// ✗ TFactorMapping: the ScriptType does not itself typecheck as TypeScript
//   TFactorMapping.st.ts(3:61) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TFactorMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"KeyOf", boolean>, Hole<"Type", T.TSchema>, Hole<"IndexArray", unknown[]>, Hole<"Extend", unknown[]>, Hole<"WithClause", unknown> ]>(Input)
  if (m1) {
    return TFactorWith(matches<true>(m1.KeyOf) ? TFactorExtends(S.TKeyOfDeferred(TFactorIndexArray(m1.Type, m1.IndexArray)), m1.Extend) : TFactorExtends(TFactorIndexArray(m1.Type, m1.IndexArray), m1.Extend), m1.WithClause)
  }
  return never
}
/* compiles to:
 * export type TFactorMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends [infer KeyOf extends boolean, infer Type extends T.TSchema, infer IndexArray extends unknown[], infer Extend extends unknown[], infer WithClause extends unknown] ? TFactorWith<KeyOf extends true ? TFactorExtends<S.TKeyOfDeferred<TFactorIndexArray<Type, IndexArray>>, Extend> : TFactorExtends<TFactorIndexArray<Type, IndexArray>, Extend>, WithClause> : never
 */

// ✗ TExprBinaryMapping: the ScriptType does not itself typecheck as TypeScript
//   TExprBinaryMapping.st.ts(2:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExprBinaryMapping(Left: T.TSchema, Rest: unknown[]) {
  const m1 = matches<[ Hole<"Operator", unknown>, Hole<"Right", T.TSchema>, Hole<"Next", unknown[]> ]>(Rest)
  if (m1) {
    const m2 = matches<Hole<"Schema", T.TSchema>>(TExprBinaryMapping(m1.Right, m1.Next))
    if (m2) {
      if (matches<'&'>(m1.Operator)) {
        const m3 = matches<T.TIntersect<Hole<"Types", T.TSchema[]>>>(m2.Schema)
        if (m3) {
          return T.TIntersect([Left, ...m3.Types])
        }
        return T.TIntersect([Left, m2.Schema])
      }
      if (matches<'|'>(m1.Operator)) {
        const m4 = matches<T.TUnion<Hole<"Types", T.TSchema[]>>>(m2.Schema)
        if (m4) {
          return T.TUnion([Left, ...m4.Types])
        }
        return T.TUnion([Left, m2.Schema])
      }
      return never
    }
    return never
  }
  return Left
}
/* compiles to:
 * export type TExprBinaryMapping<Left extends T.TSchema, Rest extends unknown[]> = Rest extends [infer Operator extends unknown, infer Right extends T.TSchema, infer Next extends unknown[]] ? TExprBinaryMapping<Right, Next> extends (infer Schema extends T.TSchema) ? Operator extends '&' ? Schema extends T.TIntersect<infer Types extends T.TSchema[]> ? T.TIntersect<[Left, ...Types]> : T.TIntersect<[Left, Schema]> : Operator extends '|' ? Schema extends T.TUnion<infer Types extends T.TSchema[]> ? T.TUnion<[Left, ...Types]> : T.TUnion<[Left, Schema]> : never : never : Left
 */

// ✓ TExprTermTailMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExprTermTailMapping(Input: [ unknown, unknown, unknown ] | [ ]) {
  return Input
}
/* compiles to:
 * export type TExprTermTailMapping<Input extends [unknown, unknown, unknown] | []> = Input
 */

// ✗ TExprTermMapping: the ScriptType does not itself typecheck as TypeScript
//   TExprTermMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExprTermMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"Left", T.TSchema>, Hole<"Rest", unknown[]> ]>(Input)
  if (m1) {
    return TExprBinaryMapping(m1.Left, m1.Rest)
  }
  return []
}
/* compiles to:
 * export type TExprTermMapping<Input extends [unknown, unknown]> = Input extends [infer Left extends T.TSchema, infer Rest extends unknown[]] ? TExprBinaryMapping<Left, Rest> : []
 */

// ✓ TExprTailMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExprTailMapping(Input: [ unknown, unknown, unknown ] | [ ]) {
  return Input
}
/* compiles to:
 * export type TExprTailMapping<Input extends [unknown, unknown, unknown] | []> = Input
 */

// ✗ TExprMapping: the ScriptType does not itself typecheck as TypeScript
//   TExprMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExprMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"Left", T.TSchema>, Hole<"Rest", unknown[]> ]>(Input)
  if (m1) {
    return TExprBinaryMapping(m1.Left, m1.Rest)
  }
  return []
}
/* compiles to:
 * export type TExprMapping<Input extends [unknown, unknown]> = Input extends [infer Left extends T.TSchema, infer Rest extends unknown[]] ? TExprBinaryMapping<Left, Rest> : []
 */

// ✗ TExprReadonlyMapping: the ScriptType does not itself typecheck as TypeScript
//   TExprReadonlyMapping.st.ts(3:49) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExprReadonlyMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddImmutableDeferred(m1.Type)
  }
  return never
}
/* compiles to:
 * export type TExprReadonlyMapping<Input extends [unknown, unknown]> = Input extends ['readonly', infer Type extends T.TSchema] ? S.TAddImmutableDeferred<Type> : never
 */

// ✗ TExprPipeMapping: the ScriptType does not itself typecheck as TypeScript
//   TExprPipeMapping.st.ts(3:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TExprPipeMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ '|', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return m1.Type
  }
  return never
}
/* compiles to:
 * export type TExprPipeMapping<Input extends [unknown, unknown]> = Input extends ['|', infer Type extends T.TSchema] ? Type : never
 */

// ✗ TGenericTypeMapping: the ScriptType does not itself typecheck as TypeScript
//   TGenericTypeMapping.st.ts(3:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TGenericTypeMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"Arguments", T.TParameter[]>, '=', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return T.TGeneric(m1.Arguments, m1.Type)
  }
  return never
}
/* compiles to:
 * export type TGenericTypeMapping<Input extends [unknown, unknown, unknown]> = Input extends [infer Arguments extends T.TParameter[], '=', infer Type extends T.TSchema] ? T.TGeneric<Arguments, Type> : never
 */

// ✗ TInferTypeMapping: the ScriptType does not itself typecheck as TypeScript
//   TInferTypeMapping.st.ts(3:79) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TInferTypeMapping(Input: [ unknown, unknown, unknown, unknown ] | [ unknown, unknown ]) {
  const m1 = matches<[ 'infer', Hole<"Name", string>, 'extends', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return T.TInfer(m1.Name, m1.Type)
  }
  const m2 = matches<[ 'infer', Hole<"Name", string> ]>(Input)
  if (m2) {
    return T.TInfer(m2.Name, T.TUnknown)
  }
  return never
}
/* compiles to:
 * export type TInferTypeMapping<Input extends [unknown, unknown, unknown, unknown] | [unknown, unknown]> = Input extends ['infer', infer Name extends string, 'extends', infer Type extends T.TSchema] ? T.TInfer<Name, Type> : Input extends ['infer', infer Name extends string] ? T.TInfer<Name, T.TUnknown> : never
 */

// ✓ TTypeMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTypeMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TTypeMapping<Input> = Input
 */

// ✓ TPropertyKeyNumberMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyKeyNumberMapping(Input: string) {
  return `${Input}`
}
/* compiles to:
 * export type TPropertyKeyNumberMapping<Input extends string> = `${Input}`
 */

// ✓ TPropertyKeyIdentMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyKeyIdentMapping(Input: string) {
  return Input
}
/* compiles to:
 * export type TPropertyKeyIdentMapping<Input extends string> = Input
 */

// ✓ TPropertyKeyQuotedMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyKeyQuotedMapping(Input: string) {
  return Input
}
/* compiles to:
 * export type TPropertyKeyQuotedMapping<Input extends string> = Input
 */

// ✗ TPropertyKeyIndexMapping: the ScriptType does not itself typecheck as TypeScript
//   TPropertyKeyIndexMapping.st.ts(3:35) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPropertyKeyIndexMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  if (matches<[ '[', string, ':', T.TInteger, ']' ]>(Input)) {
    return T.TIntegerKey
  }
  if (matches<[ '[', string, ':', T.TNumber, ']' ]>(Input)) {
    return T.TNumberKey
  }
  if (matches<[ '[', string, ':', T.TString, ']' ]>(Input)) {
    return T.TStringKey
  }
  if (matches<[ '[', string, ':', T.TSymbol, ']' ]>(Input)) {
    return T.TStringKey
  }
  return never
}
/* compiles to:
 * export type TPropertyKeyIndexMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends ['[', string, ':', T.TInteger, ']'] ? T.TIntegerKey : Input extends ['[', string, ':', T.TNumber, ']'] ? T.TNumberKey : Input extends ['[', string, ':', T.TString, ']'] ? T.TStringKey : Input extends ['[', string, ':', T.TSymbol, ']'] ? T.TStringKey : never
 */

// ✓ TPropertyKeyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyKeyMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TPropertyKeyMapping<Input> = Input
 */

// ✓ TReadonlyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TReadonlyMapping(Input: [ unknown ] | [ ]) {
  if (matches<[ unknown ]>(Input)) {
    return true
  }
  return false
}
/* compiles to:
 * export type TReadonlyMapping<Input extends [unknown] | []> = Input extends [unknown] ? true : false
 */

// ✓ TOptionalMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TOptionalMapping(Input: [ unknown ] | [ ]) {
  if (matches<[ unknown ]>(Input)) {
    return true
  }
  return false
}
/* compiles to:
 * export type TOptionalMapping<Input extends [unknown] | []> = Input extends [unknown] ? true : false
 */

// ✗ TPropertyMapping: the ScriptType does not itself typecheck as TypeScript
//   TPropertyMapping.st.ts(3:124) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPropertyMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"IsReadonly", boolean>, Hole<"Key", string>, Hole<"IsOptional", boolean>, string, Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Key)) {
      out[_] = matches<[ true, true ]>([m1.IsReadonly, m1.IsOptional]) ? S.TAddReadonlyDeferred(S.TAddOptionalDeferred(m1.Type)) : (matches<[ true, false ]>([m1.IsReadonly, m1.IsOptional]) ? S.TAddReadonlyDeferred(m1.Type) : (matches<[ false, true ]>([m1.IsReadonly, m1.IsOptional]) ? S.TAddOptionalDeferred(m1.Type) : m1.Type))
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TPropertyMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends [infer IsReadonly extends boolean, infer Key extends string, infer IsOptional extends boolean, string, infer Type extends T.TSchema] ? { [_ in Key]: [IsReadonly, IsOptional] extends [true, true] ? S.TAddReadonlyDeferred<S.TAddOptionalDeferred<Type>> : [IsReadonly, IsOptional] extends [true, false] ? S.TAddReadonlyDeferred<Type> : [IsReadonly, IsOptional] extends [false, true] ? S.TAddOptionalDeferred<Type> : Type } : never
 */

// ✓ TPropertyDelimiterMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyDelimiterMapping(Input: [ unknown, unknown ] | [ unknown ]) {
  return Input
}
/* compiles to:
 * export type TPropertyDelimiterMapping<Input extends [unknown, unknown] | [unknown]> = Input
 */

// ✓ TPropertyListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPropertyListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TPropertyListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ TPropertiesReduce: the ScriptType does not itself typecheck as TypeScript
//   TPropertiesReduce.st.ts(2:51) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPropertiesReduce(PropertiesList: T.TProperties[], Result: [ properties: T.TProperties, patternProperties: T.TProperties ] = [{}, {}]) {
  const m1 = matches<[ Hole<"Left", T.TProperties>, ...Hole<"Right", T.TProperties[]> ]>(PropertiesList)
  if (m1) {
    if (matches<{ [_ in T.TIntegerKey]: T.TSchema; }>(m1.Left)) {
      return TPropertiesReduce(m1.Right, [Result[0], Memory.TAssign(Result[1], m1.Left)])
    }
    if (matches<{ [_ in T.TNumberKey]: T.TSchema; }>(m1.Left)) {
      return TPropertiesReduce(m1.Right, [Result[0], Memory.TAssign(Result[1], m1.Left)])
    }
    if (matches<{ [_ in T.TStringKey]: T.TSchema; }>(m1.Left)) {
      return TPropertiesReduce(m1.Right, [Result[0], Memory.TAssign(Result[1], m1.Left)])
    }
    return TPropertiesReduce(m1.Right, [Memory.TAssign(Result[0], m1.Left), Result[1]])
  }
  const out = emptyObject
  for (const Key in keyof(Result)) {
    out[Key] = Result[Key]
  }
  return out
}
/* compiles to:
 * export type TPropertiesReduce<PropertiesList extends T.TProperties[], Result extends [T.TProperties, T.TProperties] = [{}, {}]> = PropertiesList extends [infer Left extends T.TProperties, ...(infer Right extends T.TProperties[])] ? Left extends { [_ in T.TIntegerKey]: T.TSchema; } ? TPropertiesReduce<Right, [Result[0], Memory.TAssign<Result[1], Left>]> : Left extends { [_ in T.TNumberKey]: T.TSchema; } ? TPropertiesReduce<Right, [Result[0], Memory.TAssign<Result[1], Left>]> : Left extends { [_ in T.TStringKey]: T.TSchema; } ? TPropertiesReduce<Right, [Result[0], Memory.TAssign<Result[1], Left>]> : TPropertiesReduce<Right, [Memory.TAssign<Result[0], Left>, Result[1]]> : { [Key in keyof Result]: Result[Key] }
 */

// ✗ TPropertiesMapping: the ScriptType does not itself typecheck as TypeScript
//   TPropertiesMapping.st.ts(2:84) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPropertiesMapping(Input: [ unknown, unknown, unknown ], Result: [ T.TProperties, T.TProperties ] = m1 ? TPropertiesReduce(m1.PropertyList) : [{}, {}]) {
  return Result
}
/* compiles to:
 * export type TPropertiesMapping<Input extends [unknown, unknown, unknown], Result extends [T.TProperties, T.TProperties] = m1 extends true ? TPropertiesReduce<m1.PropertyList> : [{}, {}]> = Result
 */

// ✗ T_Object_Mapping: the ScriptType does not itself typecheck as TypeScript
//   T_Object_Mapping.st.ts(3:43) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function T_Object_Mapping(Input: unknown) {
  const m1 = matches<[ Hole<"Properties", T.TProperties>, Hole<"_PatternProperties", T.TProperties> ]>(Input)
  if (m1) {
    return T.TObject(m1.Properties)
  }
  return never
}
/* compiles to:
 * export type T_Object_Mapping<Input> = Input extends [infer Properties extends T.TProperties, T.TProperties] ? T.TObject<Properties> : never
 */

// ✗ TElementNamedMapping: the ScriptType does not itself typecheck as TypeScript
//   TElementNamedMapping.st.ts(3:67) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TElementNamedMapping(Input: [ unknown, unknown, unknown, unknown, unknown ] | [ unknown, unknown, unknown, unknown ] | [ unknown, unknown, unknown ]) {
  const m1 = matches<[ string, '?', ':', 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddReadonlyDeferred(S.TAddOptionalDeferred(m1.Type))
  }
  const m2 = matches<[ string, ':', 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m2) {
    return S.TAddReadonlyDeferred(m2.Type)
  }
  const m3 = matches<[ string, '?', ':', Hole<"Type", T.TSchema> ]>(Input)
  if (m3) {
    return S.TAddOptionalDeferred(m3.Type)
  }
  const m4 = matches<[ string, ':', Hole<"Type", T.TSchema> ]>(Input)
  if (m4) {
    return m4.Type
  }
  return never
}
/* compiles to:
 * export type TElementNamedMapping<Input extends [unknown, unknown, unknown, unknown, unknown] | [unknown, unknown, unknown, unknown] | [unknown, unknown, unknown]> = Input extends [string, '?', ':', 'readonly', infer Type extends T.TSchema] ? S.TAddReadonlyDeferred<S.TAddOptionalDeferred<Type>> : Input extends [string, ':', 'readonly', infer Type extends T.TSchema] ? S.TAddReadonlyDeferred<Type> : Input extends [string, '?', ':', infer Type extends T.TSchema] ? S.TAddOptionalDeferred<Type> : Input extends [string, ':', infer Type extends T.TSchema] ? Type : never
 */

// ✗ TElementReadonlyOptionalMapping: the ScriptType does not itself typecheck as TypeScript
//   TElementReadonlyOptionalMapping.st.ts(3:49) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TElementReadonlyOptionalMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ 'readonly', Hole<"Type", T.TSchema>, '?' ]>(Input)
  if (m1) {
    return S.TAddReadonlyDeferred(S.TAddOptionalDeferred(m1.Type))
  }
  return never
}
/* compiles to:
 * export type TElementReadonlyOptionalMapping<Input extends [unknown, unknown, unknown]> = Input extends ['readonly', infer Type extends T.TSchema, '?'] ? S.TAddReadonlyDeferred<S.TAddOptionalDeferred<Type>> : never
 */

// ✗ TElementReadonlyMapping: the ScriptType does not itself typecheck as TypeScript
//   TElementReadonlyMapping.st.ts(3:49) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TElementReadonlyMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddReadonlyDeferred(m1.Type)
  }
  return never
}
/* compiles to:
 * export type TElementReadonlyMapping<Input extends [unknown, unknown]> = Input extends ['readonly', infer Type extends T.TSchema] ? S.TAddReadonlyDeferred<Type> : never
 */

// ✗ TElementOptionalMapping: the ScriptType does not itself typecheck as TypeScript
//   TElementOptionalMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TElementOptionalMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"Type", T.TSchema>, '?' ]>(Input)
  if (m1) {
    return S.TAddOptionalDeferred(m1.Type)
  }
  return never
}
/* compiles to:
 * export type TElementOptionalMapping<Input extends [unknown, unknown]> = Input extends [infer Type extends T.TSchema, '?'] ? S.TAddOptionalDeferred<Type> : never
 */

// ✓ TElementBaseMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TElementBaseMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TElementBaseMapping<Input> = Input
 */

// ✗ TElementMapping: the ScriptType does not itself typecheck as TypeScript
//   TElementMapping.st.ts(3:44) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TElementMapping(Input: [ unknown, unknown ] | [ unknown ]) {
  const m1 = matches<[ '...', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return T.TRest(m1.Type)
  }
  const m2 = matches<[ Hole<"Type", T.TSchema> ]>(Input)
  if (m2) {
    return m2.Type
  }
  return never
}
/* compiles to:
 * export type TElementMapping<Input extends [unknown, unknown] | [unknown]> = Input extends ['...', infer Type extends T.TSchema] ? T.TRest<Type> : Input extends [infer Type extends T.TSchema] ? Type : never
 */

// ✓ TElementListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TElementListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TElementListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ T_Tuple_Mapping: the ScriptType does not itself typecheck as TypeScript
//   T_Tuple_Mapping.st.ts(3:43) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function T_Tuple_Mapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '[', Hole<"Types", T.TSchema[]>, ']' ]>(Input)
  if (m1) {
    return T.TTuple(m1.Types)
  }
  return never
}
/* compiles to:
 * export type T_Tuple_Mapping<Input extends [unknown, unknown, unknown]> = Input extends ['[', infer Types extends T.TSchema[], ']'] ? T.TTuple<Types> : never
 */

// ✗ TParameterReadonlyOptionalMapping: the ScriptType does not itself typecheck as TypeScript
//   TParameterReadonlyOptionalMapping.st.ts(3:67) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TParameterReadonlyOptionalMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ string, '?', ':', 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddReadonlyDeferred(S.TAddOptionalDeferred(m1.Type))
  }
  return never
}
/* compiles to:
 * export type TParameterReadonlyOptionalMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends [string, '?', ':', 'readonly', infer Type extends T.TSchema] ? S.TAddReadonlyDeferred<S.TAddOptionalDeferred<Type>> : never
 */

// ✗ TParameterReadonlyMapping: the ScriptType does not itself typecheck as TypeScript
//   TParameterReadonlyMapping.st.ts(3:62) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TParameterReadonlyMapping(Input: [ unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ string, ':', 'readonly', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddReadonlyDeferred(m1.Type)
  }
  return never
}
/* compiles to:
 * export type TParameterReadonlyMapping<Input extends [unknown, unknown, unknown, unknown]> = Input extends [string, ':', 'readonly', infer Type extends T.TSchema] ? S.TAddReadonlyDeferred<Type> : never
 */

// ✗ TParameterOptionalMapping: the ScriptType does not itself typecheck as TypeScript
//   TParameterOptionalMapping.st.ts(3:55) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TParameterOptionalMapping(Input: [ unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ string, '?', ':', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return S.TAddOptionalDeferred(m1.Type)
  }
  return never
}
/* compiles to:
 * export type TParameterOptionalMapping<Input extends [unknown, unknown, unknown, unknown]> = Input extends [string, '?', ':', infer Type extends T.TSchema] ? S.TAddOptionalDeferred<Type> : never
 */

// ✗ TParameterTypeMapping: the ScriptType does not itself typecheck as TypeScript
//   TParameterTypeMapping.st.ts(3:50) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TParameterTypeMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ string, ':', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return m1.Type
  }
  return never
}
/* compiles to:
 * export type TParameterTypeMapping<Input extends [unknown, unknown, unknown]> = Input extends [string, ':', infer Type extends T.TSchema] ? Type : never
 */

// ✓ TParameterBaseMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TParameterBaseMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TParameterBaseMapping<Input> = Input
 */

// ✗ TParameterMapping: the ScriptType does not itself typecheck as TypeScript
//   TParameterMapping.st.ts(3:44) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TParameterMapping(Input: [ unknown, unknown ] | [ unknown ]) {
  const m1 = matches<[ '...', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return T.TRest(m1.Type)
  }
  const m2 = matches<[ Hole<"Type", T.TSchema> ]>(Input)
  if (m2) {
    return m2.Type
  }
  return never
}
/* compiles to:
 * export type TParameterMapping<Input extends [unknown, unknown] | [unknown]> = Input extends ['...', infer Type extends T.TSchema] ? T.TRest<Type> : Input extends [infer Type extends T.TSchema] ? Type : never
 */

// ✓ TParameterListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TParameterListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TParameterListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ T_Function_Mapping: the ScriptType does not itself typecheck as TypeScript
//   T_Function_Mapping.st.ts(3:51) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function T_Function_Mapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ '(', Hole<"ParameterList", T.TSchema[]>, ')', '=>', Hole<"ReturnType", T.TSchema> ]>(Input)
  if (m1) {
    return T.TFunction(m1.ParameterList, m1.ReturnType)
  }
  return never
}
/* compiles to:
 * export type T_Function_Mapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends ['(', infer ParameterList extends T.TSchema[], ')', '=>', infer ReturnType extends T.TSchema] ? T.TFunction<ParameterList, ReturnType> : never
 */

// ✗ T_Constructor_Mapping: the ScriptType does not itself typecheck as TypeScript
//   T_Constructor_Mapping.st.ts(3:58) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function T_Constructor_Mapping(Input: [ unknown, unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'new', '(', Hole<"ParameterList", T.TSchema[]>, ')', '=>', Hole<"InstanceType", T.TSchema> ]>(Input)
  if (m1) {
    return T.TConstructor(m1.ParameterList, m1.InstanceType)
  }
  return never
}
/* compiles to:
 * export type T_Constructor_Mapping<Input extends [unknown, unknown, unknown, unknown, unknown, unknown]> = Input extends ['new', '(', infer ParameterList extends T.TSchema[], ')', '=>', infer InstanceType extends T.TSchema] ? T.TConstructor<ParameterList, InstanceType> : never
 */

// ✗ TApplyReadonly: the ScriptType does not itself typecheck as TypeScript
//   TApplyReadonly.st.ts(2:68) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TApplyReadonly(Readonly: TModifierOperation, Type: T.TSchema) {
  if (matches<'remove'>(Readonly)) {
    return S.TRemoveReadonlyDeferred(Type)
  }
  if (matches<'add'>(Readonly)) {
    return S.TAddReadonlyDeferred(Type)
  }
  return Type
}
/* compiles to:
 * export type TApplyReadonly<Readonly extends TModifierOperation, Type extends T.TSchema> = Readonly extends 'remove' ? S.TRemoveReadonlyDeferred<Type> : Readonly extends 'add' ? S.TAddReadonlyDeferred<Type> : Type
 */

// ✓ TMappedReadonlyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TMappedReadonlyMapping(Input: [ unknown, unknown ] | [ unknown ] | [ ]) {
  if (matches<[ '-', 'readonly' ]>(Input)) {
    return 'remove'
  }
  if (matches<[ '+', 'readonly' ]>(Input)) {
    return 'add'
  }
  if (matches<[ 'readonly' ]>(Input)) {
    return 'add'
  }
  return 'none'
}
/* compiles to:
 * export type TMappedReadonlyMapping<Input extends [unknown, unknown] | [unknown] | []> = Input extends ['-', 'readonly'] ? 'remove' : Input extends ['+', 'readonly'] ? 'add' : Input extends ['readonly'] ? 'add' : 'none'
 */

// ✗ TApplyOptional: the ScriptType does not itself typecheck as TypeScript
//   TApplyOptional.st.ts(2:68) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TApplyOptional(Optional: TModifierOperation, Type: T.TSchema) {
  if (matches<'remove'>(Optional)) {
    return S.TRemoveOptionalDeferred(Type)
  }
  if (matches<'add'>(Optional)) {
    return S.TAddOptionalDeferred(Type)
  }
  return Type
}
/* compiles to:
 * export type TApplyOptional<Optional extends TModifierOperation, Type extends T.TSchema> = Optional extends 'remove' ? S.TRemoveOptionalDeferred<Type> : Optional extends 'add' ? S.TAddOptionalDeferred<Type> : Type
 */

// ✓ TMappedOptionalMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TMappedOptionalMapping(Input: [ unknown, unknown ] | [ unknown ] | [ ]) {
  if (matches<[ '-', '?' ]>(Input)) {
    return 'remove'
  }
  if (matches<[ '+', '?' ]>(Input)) {
    return 'add'
  }
  if (matches<[ '?' ]>(Input)) {
    return 'add'
  }
  return 'none'
}
/* compiles to:
 * export type TMappedOptionalMapping<Input extends [unknown, unknown] | [unknown] | []> = Input extends ['-', '?'] ? 'remove' : Input extends ['+', '?'] ? 'add' : Input extends ['?'] ? 'add' : 'none'
 */

// ✗ TMappedAsMapping: the ScriptType does not itself typecheck as TypeScript
//   TMappedAsMapping.st.ts(3:43) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TMappedAsMapping(Input: [ unknown, unknown ] | [ ]) {
  const m1 = matches<[ 'as', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    return [m1.Type]
  }
  return []
}
/* compiles to:
 * export type TMappedAsMapping<Input extends [unknown, unknown] | []> = Input extends ['as', infer Type extends T.TSchema] ? [Type] : []
 */

// ✗ T_Mapped_Mapping: the ScriptType does not itself typecheck as TypeScript
//   T_Mapped_Mapping.st.ts(3:112) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function T_Mapped_Mapping(Input: [ unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ '{', Hole<"Readonly", TModifierOperation>, '[', Hole<"Key", string>, 'in', Hole<"Type", T.TSchema>, Hole<"As", T.TSchema[]>, ']', Hole<"Optional", TModifierOperation>, ':', Hole<"Property", T.TSchema>, null, '}' ]>(Input)
  if (m1) {
    const m2 = matches<[ Hole<"As", T.TSchema> ]>(m1.As)
    if (m2) {
      return S.TMappedDeferred(T.TIdentifier(m1.Key), m1.Type, m2.As, TApplyReadonly(m1.Readonly, TApplyOptional(m1.Optional, m1.Property)))
    }
    return S.TMappedDeferred(T.TIdentifier(m1.Key), m1.Type, T.TRef(m1.Key), TApplyReadonly(m1.Readonly, TApplyOptional(m1.Optional, m1.Property)))
  }
  return never
}
/* compiles to:
 * export type T_Mapped_Mapping<Input extends [unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown]> = Input extends ['{', infer Readonly extends TModifierOperation, '[', infer Key extends string, 'in', infer Type extends T.TSchema, infer As extends T.TSchema[], ']', infer Optional extends TModifierOperation, ':', infer Property extends T.TSchema, null, '}'] ? As extends [infer As extends T.TSchema] ? S.TMappedDeferred<T.TIdentifier<Key>, Type, As, TApplyReadonly<Readonly, TApplyOptional<Optional, Property>>> : S.TMappedDeferred<T.TIdentifier<Key>, Type, T.TRef<Key>, TApplyReadonly<Readonly, TApplyOptional<Optional, Property>>> : never
 */

// ✗ TReferenceMapping: the ScriptType does not itself typecheck as TypeScript
//   TReferenceMapping.st.ts(2:58) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TReferenceMapping(Input: string, Result: T.TSchema = T.TRef(Input)) {
  return Result
}
/* compiles to:
 * export type TReferenceMapping<Input extends string, Result extends T.TSchema = T.TRef<Input>> = Result
 */

// ✓ TWithBigIntMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithBigIntMapping(Input: string) {
  const m1 = matches<`${Hole<"Value", bigint>}`>(Input)
  if (m1) {
    return m1.Value
  }
  return never
}
/* compiles to:
 * export type TWithBigIntMapping<Input extends string> = Input extends `${infer Value extends bigint}` ? Value : never
 */

// ✓ TWithNumberMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithNumberMapping(Input: string) {
  const m1 = matches<`${Hole<"Value", number>}`>(Input)
  if (m1) {
    return m1.Value
  }
  return never
}
/* compiles to:
 * export type TWithNumberMapping<Input extends string> = Input extends `${infer Value extends number}` ? Value : never
 */

// ✓ TWithBooleanMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithBooleanMapping(Input: 'true' | 'false') {
  if (matches<'true'>(Input)) {
    return true
  }
  return false
}
/* compiles to:
 * export type TWithBooleanMapping<Input extends 'true' | 'false'> = Input extends 'true' ? true : false
 */

// ✓ TWithStringMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithStringMapping(Input: string) {
  return Input
}
/* compiles to:
 * export type TWithStringMapping<Input extends string> = Input
 */

// ✓ TWithNullMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithNullMapping(Input: 'null') {
  return Null
}
/* compiles to:
 * export type TWithNullMapping<Input extends 'null'> = null
 */

// ✓ TWithUndefinedMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithUndefinedMapping(Input: 'undefined') {
  return Undefined
}
/* compiles to:
 * export type TWithUndefinedMapping<Input extends 'undefined'> = undefined
 */

// ✓ TWithPropertyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithPropertyMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ Hole<"Key", string>, ':', Hole<"Value", unknown> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Key)) {
      out[_] = m1.Value
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TWithPropertyMapping<Input extends [unknown, unknown, unknown]> = Input extends [infer Key extends string, ':', infer Value extends unknown] ? { [_ in Key]: Value } : never
 */

// ✓ TWithPropertyListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithPropertyListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TWithPropertyListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✓ TWithObjectMappingReduce: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithObjectMappingReduce(PropertyList: Record<PropertyKey, unknown>[]) {
  let Result = {}
  let propertyList = PropertyList
  while (true) {
    const m1 = matches<[ Hole<"Left", Record<PropertyKey, unknown>>, ...Hole<"Right", Record<PropertyKey, unknown>[]> ]>(propertyList)
    if (!m1) {
      break
    }
    propertyList = m1.Right
    Result = Memory.TAssign(Result, m1.Left)
  }
  const out = emptyObject
  for (const Key in keyof(Result)) {
    out[Key] = Result[Key]
  }
  return out
}
/* compiles to:
 * export type TWithObjectMappingReduce<PropertyList extends Record<PropertyKey, unknown>[]> = TWithObjectMappingReduce__loop<PropertyList, {}>
 * type TWithObjectMappingReduce__loop<PropertyList extends Record<PropertyKey, unknown>[], Result> = PropertyList extends [infer Left extends Record<PropertyKey, unknown>, ...(infer Right extends Record<PropertyKey, unknown>[])] ? TWithObjectMappingReduce__loop<Right, Memory.TAssign<Result, Left>> : { [Key in keyof Result]: Result[Key] }
 */

// ✓ TWithObjectMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithObjectMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '{', Hole<"PropertyList", Record<PropertyKey, unknown>[]>, '}' ]>(Input)
  if (m1) {
    return TWithObjectMappingReduce(m1.PropertyList)
  }
  return {}
}
/* compiles to:
 * export type TWithObjectMapping<Input extends [unknown, unknown, unknown]> = Input extends ['{', infer PropertyList extends Record<PropertyKey, unknown>[], '}'] ? TWithObjectMappingReduce<PropertyList> : {}
 */

// ✓ TWithElementListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithElementListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TWithElementListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✓ TWithArrayMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithArrayMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '[', Hole<"Elements", unknown[]>, ']' ]>(Input)
  if (m1) {
    return m1.Elements
  }
  return never
}
/* compiles to:
 * export type TWithArrayMapping<Input extends [unknown, unknown, unknown]> = Input extends ['[', infer Elements extends unknown[], ']'] ? Elements : never
 */

// ✓ TWithValueMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TWithValueMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TWithValueMapping<Input> = Input
 */

// ✓ TPatternBigIntMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternBigIntMapping(Input: '-?(?:0|[1-9][0-9]*)n') {
  return T.TBigInt
}
/* compiles to:
 * export type TPatternBigIntMapping<Input extends '-?(?:0|[1-9][0-9]*)n'> = T.TBigInt
 */

// ✓ TPatternStringMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternStringMapping(Input: '.*') {
  return T.TString
}
/* compiles to:
 * export type TPatternStringMapping<Input extends '.*'> = T.TString
 */

// ✓ TPatternNumberMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternNumberMapping(Input: '-?(?:0|[1-9][0-9]*)(?:\\.[0-9]+)?') {
  return T.TNumber
}
/* compiles to:
 * export type TPatternNumberMapping<Input extends '-?(?:0|[1-9][0-9]*)(?:\\.[0-9]+)?'> = T.TNumber
 */

// ✓ TPatternIntegerMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternIntegerMapping(Input: '-?(?:0|[1-9][0-9]*)') {
  return T.TInteger
}
/* compiles to:
 * export type TPatternIntegerMapping<Input extends '-?(?:0|[1-9][0-9]*)'> = T.TInteger
 */

// ✓ TPatternNeverMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternNeverMapping(Input: '(?!)') {
  return T.TNever
}
/* compiles to:
 * export type TPatternNeverMapping<Input extends '(?!)'> = T.TNever
 */

// ✗ TPatternTextMapping: the ScriptType does not itself typecheck as TypeScript
//   TPatternTextMapping.st.ts(2:60) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPatternTextMapping(Input: string, Result: T.TSchema = T.TLiteral(Input)) {
  return Result
}
/* compiles to:
 * export type TPatternTextMapping<Input extends string, Result extends T.TSchema = T.TLiteral<Input>> = Result
 */

// ✓ TPatternBaseMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternBaseMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TPatternBaseMapping<Input> = Input
 */

// ✗ TPatternGroupMapping: the ScriptType does not itself typecheck as TypeScript
//   TPatternGroupMapping.st.ts(3:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPatternGroupMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '(', Hole<"Body", T.TSchema[]>, ')' ]>(Input)
  if (m1) {
    return T.TUnion(m1.Body)
  }
  return never
}
/* compiles to:
 * export type TPatternGroupMapping<Input extends [unknown, unknown, unknown]> = Input extends ['(', infer Body extends T.TSchema[], ')'] ? T.TUnion<Body> : never
 */

// ✗ TPatternUnionMapping: the ScriptType does not itself typecheck as TypeScript
//   TPatternUnionMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPatternUnionMapping(Input: [ unknown, unknown, unknown ] | [ unknown ] | [ ]) {
  const m1 = matches<[ Hole<"Term", T.TSchema[]>, '|', Hole<"Union", T.TSchema[]> ]>(Input)
  if (m1) {
    return [...m1.Term, ...m1.Union]
  }
  const m2 = matches<[ Hole<"Term", T.TSchema[]> ]>(Input)
  if (m2) {
    return [...m2.Term]
  }
  return []
}
/* compiles to:
 * export type TPatternUnionMapping<Input extends [unknown, unknown, unknown] | [unknown] | []> = Input extends [infer Term extends T.TSchema[], '|', infer Union extends T.TSchema[]] ? [...Term, ...Union] : Input extends [infer Term extends T.TSchema[]] ? [...Term] : []
 */

// ✗ TPatternTermMapping: the ScriptType does not itself typecheck as TypeScript
//   TPatternTermMapping.st.ts(3:37) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPatternTermMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"Left", T.TSchema>, Hole<"Right", T.TSchema[]> ]>(Input)
  if (m1) {
    return [m1.Left, ...m1.Right]
  }
  return never
}
/* compiles to:
 * export type TPatternTermMapping<Input extends [unknown, unknown]> = Input extends [infer Left extends T.TSchema, infer Right extends T.TSchema[]] ? [Left, ...Right] : never
 */

// ✓ TPatternBodyMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TPatternBodyMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TPatternBodyMapping<Input> = Input
 */

// ✗ TPatternMapping: the ScriptType does not itself typecheck as TypeScript
//   TPatternMapping.st.ts(3:42) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TPatternMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ '^', Hole<"Body", T.TSchema[]>, '$' ]>(Input)
  if (m1) {
    return m1.Body
  }
  return never
}
/* compiles to:
 * export type TPatternMapping<Input extends [unknown, unknown, unknown]> = Input extends ['^', infer Body extends T.TSchema[], '$'] ? Body : never
 */

// ✓ TInterfaceDeclarationHeritageListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TInterfaceDeclarationHeritageListMapping(Input: [ unknown, unknown ]) {
  return TDelimited(Input)
}
/* compiles to:
 * export type TInterfaceDeclarationHeritageListMapping<Input extends [unknown, unknown]> = TDelimited<Input>
 */

// ✗ TInterfaceDeclarationHeritageMapping: the ScriptType does not itself typecheck as TypeScript
//   TInterfaceDeclarationHeritageMapping.st.ts(3:52) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TInterfaceDeclarationHeritageMapping(Input: [ unknown, unknown ] | [ ]) {
  const m1 = matches<[ 'extends', Hole<"Heritage", T.TSchema[]> ]>(Input)
  if (m1) {
    return m1.Heritage
  }
  return []
}
/* compiles to:
 * export type TInterfaceDeclarationHeritageMapping<Input extends [unknown, unknown] | []> = Input extends ['extends', infer Heritage extends T.TSchema[]] ? Heritage : []
 */

// ✗ TInterfaceDeclarationGenericMapping: the ScriptType does not itself typecheck as TypeScript
//   TInterfaceDeclarationGenericMapping.st.ts(3:78) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TInterfaceDeclarationGenericMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'interface', Hole<"Name", string>, Hole<"Parameters", T.TParameter[]>, Hole<"Heritage", T.TSchema[]>, Hole<"Properties", [ T.TProperties, T.TProperties ]> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Name)) {
      out[_] = T.TGeneric(m1.Parameters, S.TInterfaceDeferred(m1.Heritage, m1.Properties[0]))
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TInterfaceDeclarationGenericMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends ['interface', infer Name extends string, infer Parameters extends T.TParameter[], infer Heritage extends T.TSchema[], infer Properties extends [T.TProperties, T.TProperties]] ? { [_ in Name]: T.TGeneric<Parameters, S.TInterfaceDeferred<Heritage, Properties[0]>> } : never
 */

// ✗ TInterfaceDeclarationMapping: the ScriptType does not itself typecheck as TypeScript
//   TInterfaceDeclarationMapping.st.ts(3:76) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TInterfaceDeclarationMapping(Input: [ unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'interface', Hole<"Name", string>, Hole<"Heritage", T.TSchema[]>, Hole<"Properties", [ T.TProperties, T.TProperties ]> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Name)) {
      out[_] = S.TInterfaceDeferred(m1.Heritage, m1.Properties[0])
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TInterfaceDeclarationMapping<Input extends [unknown, unknown, unknown, unknown]> = Input extends ['interface', infer Name extends string, infer Heritage extends T.TSchema[], infer Properties extends [T.TProperties, T.TProperties]] ? { [_ in Name]: S.TInterfaceDeferred<Heritage, Properties[0]> } : never
 */

// ✗ TTypeAliasDeclarationGenericMapping: the ScriptType does not itself typecheck as TypeScript
//   TTypeAliasDeclarationGenericMapping.st.ts(3:73) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTypeAliasDeclarationGenericMapping(Input: [ unknown, unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'type', Hole<"Name", string>, Hole<"Parameters", T.TParameter[]>, '=', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Name)) {
      out[_] = T.TGeneric(m1.Parameters, m1.Type)
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TTypeAliasDeclarationGenericMapping<Input extends [unknown, unknown, unknown, unknown, unknown]> = Input extends ['type', infer Name extends string, infer Parameters extends T.TParameter[], '=', infer Type extends T.TSchema] ? { [_ in Name]: T.TGeneric<Parameters, Type> } : never
 */

// ✗ TTypeAliasDeclarationMapping: the ScriptType does not itself typecheck as TypeScript
//   TTypeAliasDeclarationMapping.st.ts(3:72) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TTypeAliasDeclarationMapping(Input: [ unknown, unknown, unknown, unknown ]) {
  const m1 = matches<[ 'type', Hole<"Name", string>, '=', Hole<"Type", T.TSchema> ]>(Input)
  if (m1) {
    const out = emptyObject
    for (const _ in keySet(m1.Name)) {
      out[_] = m1.Type
    }
    return out
  }
  return never
}
/* compiles to:
 * export type TTypeAliasDeclarationMapping<Input extends [unknown, unknown, unknown, unknown]> = Input extends ['type', infer Name extends string, '=', infer Type extends T.TSchema] ? { [_ in Name]: Type } : never
 */

// ✓ TExportKeywordMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TExportKeywordMapping(Input: [ unknown ] | [ ]) {
  return Null
}
/* compiles to:
 * export type TExportKeywordMapping<Input extends [unknown] | []> = null
 */

// ✓ TModuleDeclarationDelimiterMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TModuleDeclarationDelimiterMapping(Input: [ unknown, unknown ] | [ unknown ]) {
  return Input
}
/* compiles to:
 * export type TModuleDeclarationDelimiterMapping<Input extends [unknown, unknown] | [unknown]> = Input
 */

// ✓ TModuleDeclarationListMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TModuleDeclarationListMapping(Input: [ unknown, unknown ]) {
  return TPropertiesReduce(TDelimited(Input))
}
/* compiles to:
 * export type TModuleDeclarationListMapping<Input extends [unknown, unknown]> = TPropertiesReduce<TDelimited<Input>>
 */

// ✗ TModuleDeclarationMapping: the ScriptType does not itself typecheck as TypeScript
//   TModuleDeclarationMapping.st.ts(3:56) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TModuleDeclarationMapping(Input: [ unknown, unknown, unknown ]) {
  const m1 = matches<[ null, Hole<"ModuleDeclaration", T.TProperties>, null ]>(Input)
  if (m1) {
    return m1.ModuleDeclaration
  }
  return never
}
/* compiles to:
 * export type TModuleDeclarationMapping<Input extends [unknown, unknown, unknown]> = Input extends [null, infer ModuleDeclaration extends T.TProperties, null] ? ModuleDeclaration : never
 */

// ✗ TModuleMapping: the ScriptType does not itself typecheck as TypeScript
//   TModuleMapping.st.ts(3:50) TS2702: 'T' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TModuleMapping(Input: [ unknown, unknown ]) {
  const m1 = matches<[ Hole<"ModuleDeclaration", T.TProperties>, Hole<"ModuleDeclarationList", [ T.TProperties, T.TProperties ]> ]>(Input)
  if (m1) {
    return S.TModuleDeferred(Memory.TAssign(m1.ModuleDeclaration, m1.ModuleDeclarationList[0]))
  }
  return never
}
/* compiles to:
 * export type TModuleMapping<Input extends [unknown, unknown]> = Input extends [infer ModuleDeclaration extends T.TProperties, infer ModuleDeclarationList extends [T.TProperties, T.TProperties]] ? S.TModuleDeferred<Memory.TAssign<ModuleDeclaration, ModuleDeclarationList[0]>> : never
 */

// ✓ TScriptMapping: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TScriptMapping(Input: unknown) {
  return Input
}
/* compiles to:
 * export type TScriptMapping<Input> = Input
 */

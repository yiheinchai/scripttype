/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/script/token/const.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TNewLine: any
declare const TTake: any
declare const TTrim: any
declare const TTrimWhitespace: any
declare const TWhiteSpace: any
type TNewLine<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTake<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTrim<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTrimWhitespace<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TWhiteSpace<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TTakeConst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTakeConst(Const: string, Input: string) {
  return TTake([Const], Input)
}
/* compiles to:
 * export type TTakeConst<Const extends string, Input extends string> = TTake<[Const], Input>
 */

// ✓ TConst: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TConst(Const: string, Input: string) {
  if (matches<''>(Const)) {
    return ['', Input]
  }
  const m1 = matches<`${Hole<"First", string>}${string}`>(Const)
  if (m1) {
    if (matches<TNewLine>(m1.First)) {
      return TTakeConst(Const, TTrimWhitespace(Input))
    }
    if (matches<TWhiteSpace>(m1.First)) {
      return TTakeConst(Const, Input)
    }
    return TTakeConst(Const, TTrim(Input))
  }
  return never
}
/* compiles to:
 * export type TConst<Const extends string, Input extends string> = Const extends '' ? ['', Input] : Const extends `${infer First extends string}${string}` ? First extends TNewLine ? TTakeConst<Const, TTrimWhitespace<Input>> : First extends TWhiteSpace ? TTakeConst<Const, Input> : TTakeConst<Const, TTrim<Input>> : never
 */

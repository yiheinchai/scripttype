/**
 * ScriptType for 03-schema-and-type-level-parsers/typebox/src/type/script/token/ident.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 4 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TInitial: any
declare const TRemaining: any
declare const TTake: any
declare const TTrim: any
type TInitial<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TRemaining<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTake<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TTrim<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TTakeInitial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTakeInitial(Input: string) {
  return TTake(TInitial, Input)
}
/* compiles to:
 * export type TTakeInitial<Input extends string> = TTake<TInitial, Input>
 */

// ✓ TTakeRemaining: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTakeRemaining(Input: string) {
  let Result = ''
  let input = Input
  while (true) {
    const m1 = matches<[ Hole<"Remaining", string>, Hole<"RemainingRest", string> ]>(TTake(TRemaining, input))
    if (!m1) {
      break
    }
    input = m1.RemainingRest
    Result = `${Result}${m1.Remaining}`
  }
  return [Result, input]
}
/* compiles to:
 * export type TTakeRemaining<Input extends string> = TTakeRemaining__loop<Input, ''>
 * type TTakeRemaining__loop<Input extends string, Result extends string> = TTake<TRemaining, Input> extends [infer Remaining extends string, infer RemainingRest extends string] ? TTakeRemaining__loop<RemainingRest, `${Result}${Remaining}`> : [Result, Input]
 */

// ✓ TTakeIdent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TTakeIdent(Input: string) {
  const m1 = matches<[ Hole<"Initial", string>, Hole<"InitialRest", string> ]>(TTakeInitial(Input))
  if (m1) {
    const m2 = matches<[ Hole<"Remaining", string>, Hole<"RemainingRest", string> ]>(TTakeRemaining(m1.InitialRest))
    if (m2) {
      return [`${m1.Initial}${m2.Remaining}`, m2.RemainingRest]
    }
    return []
  }
  return []
}
/* compiles to:
 * export type TTakeIdent<Input extends string> = TTakeInitial<Input> extends [infer Initial extends string, infer InitialRest extends string] ? TTakeRemaining<InitialRest> extends [infer Remaining extends string, infer RemainingRest extends string] ? [`${Initial}${Remaining}`, RemainingRest] : [] : []
 */

// ✓ TIdent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TIdent(Input: string) {
  return TTakeIdent(TTrim(Input))
}
/* compiles to:
 * export type TIdent<Input extends string> = TTakeIdent<TTrim<Input>>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/actions/guard/guard.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 raw, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
type GuardFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ GuardFunction: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function GuardFunction(TInput) {
  return fnType([TInput], raw('input is any'))
}

// ✓ InferGuardOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferGuardOutput(TGuard: GuardFunction<any>) {
  const m1 = matches<(input: any) => input is Hole<"TOutput">>(TGuard)
  if (m1) {
    return m1.TOutput
  }
  return unknown
}
/* compiles to:
 * export type InferGuardOutput<TGuard extends GuardFunction<any>> = TGuard extends (input: any) => input is infer TOutput ? TOutput : unknown
 */

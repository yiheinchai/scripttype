/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/schemas/enum/enum.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Enum: any
type Enum<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ EnumValues: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function EnumValues(TEnum: Enum) {
  const out = emptyObject
  for (const TKey in keyof(TEnum)) {
    out[TKey] = matches<number>(TKey) ? (matches<string>(TEnum[TKey]) ? (matches<typeof TKey>(TEnum[TEnum[TKey]]) ? never : TEnum[TKey]) : TEnum[TKey]) : (matches<'NaN' | 'Infinity' | '-Infinity'>(TKey) ? (matches<string>(TEnum[TKey]) ? (matches<number>(TEnum[TEnum[TKey]]) ? never : TEnum[TKey]) : TEnum[TKey]) : (matches<`+${number}`>(TKey) ? TEnum[TKey] : (raw('TKey extends `${infer TNumber extends number}` ? TEnum[TKey] extends string ? TEnum[TEnum[TKey]] extends TNumber ? never : TEnum[TKey] : TEnum[TKey] : TEnum[TKey]'))))
  }
  return out[keyof(TEnum)]
}

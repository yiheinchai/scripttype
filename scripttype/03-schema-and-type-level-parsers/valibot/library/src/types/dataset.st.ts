/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/types/dataset.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { FailureDataset, PartialDataset, SuccessDataset } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/dataset.js'
import type { BaseIssue } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/issue.js'
declare const BaseIssue: any
declare const FailureDataset: any
declare const PartialDataset: any
declare const SuccessDataset: any
// ✓ OutputDataset: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OutputDataset(TValue, TIssue: BaseIssue<unknown>): any {
  return SuccessDataset(TValue) | PartialDataset(TValue, TIssue) | FailureDataset(TIssue)
}
/* compiles to:
 * export type OutputDataset<TValue, TIssue extends BaseIssue<unknown>> =
 *   SuccessDataset<TValue> | PartialDataset<TValue, TIssue> | FailureDataset<TIssue>
 */

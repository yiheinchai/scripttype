/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/types/schema.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BaseIssue } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/issue.js'
import type { BaseSchema, BaseSchemaAsync } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/schema.js'
declare const BaseIssue: any
declare const BaseSchema: any
declare const BaseSchemaAsync: any
// ✓ GenericSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericSchema(TInput = unknown, TOutput = TInput, TIssue: BaseIssue<unknown> = BaseIssue(unknown)): any {
  return BaseSchema(TInput, TOutput, TIssue)
}
/* compiles to:
 * export type GenericSchema<
 *   TInput = unknown,
 *   TOutput = TInput,
 *   TIssue extends BaseIssue<unknown> = BaseIssue<unknown>
 * > =
 *   BaseSchema<TInput, TOutput, TIssue>
 */

// ✓ GenericSchemaAsync: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericSchemaAsync(TInput = unknown, TOutput = TInput, TIssue: BaseIssue<unknown> = BaseIssue(unknown)): any {
  return BaseSchemaAsync(TInput, TOutput, TIssue)
}
/* compiles to:
 * export type GenericSchemaAsync<
 *   TInput = unknown,
 *   TOutput = TInput,
 *   TIssue extends BaseIssue<unknown> = BaseIssue<unknown>
 * > =
 *   BaseSchemaAsync<TInput, TOutput, TIssue>
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/types/standard.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { StandardFailureResult, StandardSuccessResult } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/standard.js'
declare const StandardFailureResult: any
declare const StandardSuccessResult: any
// ✓ StandardResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StandardResult(TOutput): any {
  return StandardSuccessResult(TOutput) | StandardFailureResult
}
/* compiles to:
 * export type StandardResult<TOutput> = StandardSuccessResult<TOutput> | StandardFailureResult
 */

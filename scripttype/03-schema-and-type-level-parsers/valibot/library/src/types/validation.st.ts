/**
 * ScriptType for 03-schema-and-type-level-parsers/valibot/library/src/types/validation.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BaseIssue } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/issue.js'
import type { BaseValidation, BaseValidationAsync } from '../../../../../../03-schema-and-type-level-parsers/valibot/library/src/types/validation.js'
declare const BaseIssue: any
declare const BaseValidation: any
declare const BaseValidationAsync: any
// ✓ GenericValidation: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericValidation(TInput = any, TOutput = TInput, TIssue: BaseIssue<unknown> = BaseIssue(unknown)): any {
  return BaseValidation(TInput, TOutput, TIssue)
}
/* compiles to:
 * export type GenericValidation<
 *   TInput = any,
 *   TOutput = TInput,
 *   TIssue extends BaseIssue<unknown> = BaseIssue<unknown>
 * > =
 *   BaseValidation<TInput, TOutput, TIssue>
 */

// ✓ GenericValidationAsync: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GenericValidationAsync(TInput = any, TOutput = TInput, TIssue: BaseIssue<unknown> = BaseIssue(unknown)): any {
  return BaseValidationAsync(TInput, TOutput, TIssue)
}
/* compiles to:
 * export type GenericValidationAsync<
 *   TInput = any,
 *   TOutput = TInput,
 *   TIssue extends BaseIssue<unknown> = BaseIssue<unknown>
 * > =
 *   BaseValidationAsync<TInput, TOutput, TIssue>
 */

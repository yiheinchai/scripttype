/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v3/ZodError.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 6 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TypeOf: any
declare const ZodType: any
declare const util: any
type TypeOf<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type util<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ allKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function allKeys(T) {
  if (matches<any>(T)) {
    return keyof(T)
  }
  return never
}
/* compiles to:
 * export type allKeys<T> = T extends any ? keyof T : never
 */

// ✓ inferFlattenedErrors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferFlattenedErrors(T: ZodType<any, any, any>, U = string) {
  return typeToFlattenedError(TypeOf(T), U)
}
/* compiles to:
 * export type inferFlattenedErrors<T extends ZodType<any, any, any>, U = string> = typeToFlattenedError<TypeOf<T>, U>
 */

// ✓ typeToFlattenedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function typeToFlattenedError(T, U = string) {
  const out = emptyObject
  for (const P in keySet(allKeys(T))) {
    out[P] = optional(arrayOf(U))
  }
  return { formErrors: arrayOf(U), fieldErrors: out }
}
/* compiles to:
 * export type typeToFlattenedError<T, U = string> = { formErrors: U[]; fieldErrors: { [P in allKeys<T>]?: U[] } }
 */

// ✗ recursiveZodFormattedError: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function recursiveZodFormattedError(T) {
  if (matches<[ any, ...any[] ]>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = optional(ZodFormattedError(T[K]))
    }
    return out
  }
  if (matches<any[]>(T)) {
    return raw('{ [k: number]: ZodFormattedError<T[number]>; }')
  }
  if (matches<object>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = optional(ZodFormattedError(T[K]))
    }
    return out
  }
  return unknown
}

// ✓ ZodFormattedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ZodFormattedError(T, U = string) {
  return merge({ _errors: arrayOf(U) }, recursiveZodFormattedError(NonNullable(T)))
}
/* compiles to:
 * export type ZodFormattedError<T, U = string> = { _errors: U[] } & recursiveZodFormattedError<NonNullable<T>>
 */

// ✓ inferFormattedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function inferFormattedError(T: ZodType<any, any, any>, U = string) {
  return ZodFormattedError(TypeOf(T), U)
}
/* compiles to:
 * export type inferFormattedError<T extends ZodType<any, any, any>, U = string> = ZodFormattedError<TypeOf<T>, U>
 */

// ✓ stripPath: verified type-identical to the original
/* @scripttype preserveParamNames */
export function stripPath(T: object) {
  if (matches<any>(T)) {
    return util.OmitKeys(T, 'path')
  }
  return never
}
/* compiles to:
 * export type stripPath<T extends object> = T extends any ? util.OmitKeys<T, 'path'> : never
 */

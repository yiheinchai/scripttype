/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v3/helpers/enumUtil.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 2 compile-error, 2 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✗ UnionToIntersectionFn: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function UnionToIntersectionFn(T) {
  const m1 = matches<(k: Hole<"Intersection">) => void>(matches<unknown>(T) ? fnType([fnType([], T)], voidType()) : never)
  if (m1) {
    return m1.Intersection
  }
  return never
}
/* compiles to:
 * export type UnionToIntersectionFn<T> = (T extends unknown ? (a0: () => T) => void : never) extends (k: infer Intersection) => void ? Intersection : never
 */

// ✗ GetUnionLast: does not compile yet
//   Cannot find name 'UnionToIntersectionFn'. Did you mean 'UnionToIntersectionFn__st0'?
/* @scripttype preserveParamNames */
export function GetUnionLast(T) {
  const m1 = matches<() => Hole<"Last">>(UnionToIntersectionFn(T))
  if (m1) {
    return m1.Last
  }
  return never
}
/* compiles to:
 * export type GetUnionLast<T> = UnionToIntersectionFn<T> extends () => infer Last ? Last : never
 */

// ✓ UnionToTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToTuple(T) {
  let Tuple: any[] = []
  let t = T
  while (true) {
    if (matches<[ never ]>([t])) {
      break
    }
    t = Exclude(t, GetUnionLast(t))
    Tuple.unshift(GetUnionLast(t))
  }
  return Tuple
}
/* compiles to:
 * export type UnionToTuple<T> = UnionToTuple__loop<T, []>
 * type UnionToTuple__loop<T, Tuple extends any[]> = [T] extends [never] ? Tuple : UnionToTuple__loop<Exclude<T, GetUnionLast<T>>, [GetUnionLast<Exclude<T, GetUnionLast<T>>>, ...Tuple]>
 */

// ✓ CastToStringTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CastToStringTuple(T) {
  if (matches<[ string, ...string[] ]>(T)) {
    return T
  }
  return never
}
/* compiles to:
 * export type CastToStringTuple<T> = T extends [string, ...string[]] ? T : never
 */

// ✗ UnionToTupleString: does not compile yet
//   Cannot find name 'CastToStringTuple'. Did you mean 'CastToStringTuple__st3'?
/* @scripttype preserveParamNames */
export function UnionToTupleString(T) {
  return CastToStringTuple(UnionToTuple(T))
}
/* compiles to:
 * export type UnionToTupleString<T> = CastToStringTuple<UnionToTuple<T>>
 */

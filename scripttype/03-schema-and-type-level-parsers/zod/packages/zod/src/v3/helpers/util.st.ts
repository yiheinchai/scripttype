/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v3/helpers/util.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 19 type-level aliases: 7 mismatch, 5 covered, 4 compile-error, 2 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const objectUtil: any
type objectUtil<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ AssertEqual: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function AssertEqual(T, U) {
  if (matches<<V>() => V extends typeof U ? 1 : 2>(raw('<V>() => V extends T ? 1 : 2'))) {
    return true
  }
  return false
}

// ✗ isAny: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function isAny(T) {
  if (matches<1 & typeof T>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type isAny<T> = 0 extends 1 & T ? true : false
 */

// ✗ Omit: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Omit(T, K: keyof typeof T) {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>
 */

// ✗ OmitKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function OmitKeys(T, K: string) {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type OmitKeys<T, K extends string> = Pick<T, Exclude<keyof T, K>>
 */

// ✗ MakePartial: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function MakePartial(T, K: keyof typeof T) {
  return Omit(T, K) & Partial(Pick(T, K))
}
/* compiles to:
 * export type MakePartial<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>
 */

// ✓ Exactly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Exactly(T, X) {
  return T & Record(Exclude(keyof(X), keyof(T)), never)
}
/* compiles to:
 * export type Exactly<T, X> = T & Record<Exclude<keyof X, keyof T>, never>
 */

// ✗ InexactPartial: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function InexactPartial(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = optional(anyOf(T[k], Undefined))
  }
  return out
}
/* compiles to:
 * export type InexactPartial<T> = { [K in keyof T]?: T[K] | undefined }
 */

// ✓ identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function identity(T) {
  return objectUtil.identity(T)
}
/* compiles to:
 * export type identity<T> = objectUtil.identity<T>
 */

// ✗ flatten: does not compile yet
//   Cannot find name 'identity'.
/* @scripttype preserveParamNames */
export function flatten(T) {
  return objectUtil.flatten(T)
}
/* compiles to:
 * export type flatten<T> = objectUtil.flatten<T>
 */

// ✓ noUndefined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function noUndefined(T) {
  if (matches<undefined>(T)) {
    return never
  }
  return T
}
/* compiles to:
 * export type noUndefined<T> = T extends undefined ? never : T
 */

// ✓ MergeShapes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeShapes(U, V) {
  if (matches<never>(keyof(U) & keyof(V))) {
    return U & V
  }
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(U), keyof(V)))) {
    out[k] = U[k]
  }
  return out & V
}
/* compiles to:
 * export type MergeShapes<U, V> = keyof U & keyof V extends never ? U & V : { [K in Exclude<keyof U, keyof V>]: U[K] } & V
 */

// ✗ optionalKeys: the ScriptType does not itself typecheck as TypeScript
//   optionalKeys.st.ts(5:33) TS2537: Type '{}' has no matching index signature for type 'string'.
/* @scripttype preserveParamNames */
export function optionalKeys(T: object) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<(typeof T)[typeof k]>(Undefined) ? k : never
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type optionalKeys<T extends object> = { [K in keyof T]: undefined extends T[K] ? K : never }[keyof T]
 */

// ✗ requiredKeys: the ScriptType does not itself typecheck as TypeScript
//   requiredKeys.st.ts(5:33) TS2537: Type '{}' has no matching index signature for type 'string'.
/* @scripttype preserveParamNames */
export function requiredKeys(T: object) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<(typeof T)[typeof k]>(Undefined) ? never : k
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type requiredKeys<T extends object> = { [K in keyof T]: undefined extends T[K] ? never : K }[keyof T]
 */

// ✗ addQuestionMarks: does not compile yet
//   Cannot find name 'requiredKeys'. Did you mean 'Required'?
/* @scripttype preserveParamNames */
export function addQuestionMarks(T: object, _O = any) {
  const out = emptyObject
  for (const K in keySet(requiredKeys(T))) {
    out[K] = T[K]
  }
  const out2 = emptyObject
  for (const K in keySet(optionalKeys(T))) {
    out2[K] = optional(T[K])
  }
  const out3 = emptyObject
  for (const k in keyof(T)) {
    out3[k] = optional(unknown)
  }
  return out & out2 & out3
}
/* compiles to:
 * export type addQuestionMarks<T extends object, _O = any> = { [K in requiredKeys<T>]: T[K] } & { [K1 in optionalKeys<T>]?: T[K1] } & { [K2 in keyof T]?: unknown }
 */

// ✓ identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function identity(T) {
  return T
}
/* compiles to:
 * export type identity<T> = T
 */

// ✗ flatten: does not compile yet
//   Cannot find name 'identity'.
/* @scripttype preserveParamNames */
export function flatten(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = T[k]
  }
  return identity(out)
}
/* compiles to:
 * export type flatten<T> = identity<{ [K in keyof T]: T[K] }>
 */

// ✗ noNeverKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function noNeverKeys(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<[ never ]>([T[k]]) ? never : k
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type noNeverKeys<T> = { [K in keyof T]: [T[K]] extends [never] ? never : K }[keyof T]
 */

// ✗ noNever: does not compile yet
//   Cannot find name 'identity'.
/* @scripttype preserveParamNames */
export function noNever(T) {
  const out = emptyObject
  for (const k in keySet(noNeverKeys(T))) {
    out[k] = matches<keyof typeof T>(k) ? T[k] : never
  }
  return identity(out)
}
/* compiles to:
 * export type noNever<T> = identity<{ [K in noNeverKeys<T>]: K extends keyof T ? T[K] : never }>
 */

// ✗ extendShape: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function extendShape(A: object, B: object) {
  if (matches<never>(keyof(A) & keyof(B))) {
    return merge(A, B)
  }
  const out = emptyObject
  for (const K in keyof(A)) {
    out[matches<keyof typeof B>(K) ? never : K] = A[K]
  }
  const out2 = emptyObject
  for (const K in keyof(B)) {
    out2[K] = B[K]
  }
  return out & out2
}
/* compiles to:
 * export type extendShape<A extends object, B extends object> = keyof A & keyof B extends never ? A & B : { [K in keyof A as K extends keyof B ? never : K]: A[K] } & { [K1 in keyof B]: B[K1] }
 */

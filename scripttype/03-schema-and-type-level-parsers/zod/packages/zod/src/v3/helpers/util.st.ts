/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v3/helpers/util.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 19 type-level aliases: 15 covered, 3 mismatch, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace objectUtil {
  export type flatten<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type identity<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const objectUtil: any
type objectUtil<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ AssertEqual: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function AssertEqual(T, U): any {
  if (matches<<V>() => V extends typeof U ? 1 : 2>(genericFnType(['V'], [], matches<typeof T>(V) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type AssertEqual<T, U> =
 *   (<V>() => V extends T ? 1 : 2) extends <V>() => V extends U ? 1 : 2 ? true : false
 */

// ✓ isAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function isAny(T): any {
  if (matches<1 & typeof T>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type isAny<T> = 0 extends 1 & T ? true : false
 */

// ✓ Omit: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof T} K
 */
export function Omit(T, K): any {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>
 */

// ✓ OmitKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitKeys(T, K: string): any {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type OmitKeys<T, K extends string> = Pick<T, Exclude<keyof T, K>>
 */

// ✓ MakePartial: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof T} K
 */
export function MakePartial(T, K): any {
  return Omit(T, K) & Partial(Pick(T, K))
}
/* compiles to:
 * export type MakePartial<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>
 */

// ✓ Exactly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Exactly(T, X): any {
  return T & Record(Exclude(keyof(X), keyof(T)), never)
}
/* compiles to:
 * export type Exactly<T, X> = T & Record<Exclude<keyof X, keyof T>, never>
 */

// ✓ InexactPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InexactPartial(T): any {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = optional(anyOf(T[k], Undefined))
  }
  return out
}
/* compiles to:
 * export type InexactPartial<T> = { [K in keyof T]?: T[K] | undefined }
 */

// ✓ identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function identity(T): any {
  return t<objectUtil.identity<typeof T>>()
}
/* compiles to:
 * export type identity<T> = objectUtil.identity<T>
 */

// ✓ flatten: verified type-identical to the original
/* @scripttype preserveParamNames */
export function flatten(T): any {
  return t<objectUtil.flatten<typeof T>>()
}
/* compiles to:
 * export type flatten<T> = objectUtil.flatten<T>
 */

// ✓ noUndefined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function noUndefined(T): any {
  if (typeof T === 'undefined') {
    return never
  }
  return T
}
/* compiles to:
 * export type noUndefined<T> = T extends undefined ? never : T
 */

// ✓ MergeShapes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeShapes(U, V): any {
  if (matches<never>(keyof(U) & keyof(V))) {
    return U & V
  }
  const out = emptyObject
  for (const k in keySet(Exclude(keyof(U), keyof(V)))) {
    out[k] = U[k]
  }
  return out & V
}
/* compiles to:
 * export type MergeShapes<U, V> =
 *   keyof U & keyof V extends never ? U & V : { [K in Exclude<keyof U, keyof V>]: U[K] } & V
 */

// ✗ optionalKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
/**
 * @param {object} T
 */
export function optionalKeys(T): any {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<(typeof T)[typeof k]>(Undefined) ? k : never
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type optionalKeys<T extends object> =
 *   { [K in keyof T]: undefined extends T[K] ? K : never }[keyof T]
 */

// ✗ requiredKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
/**
 * @param {object} T
 */
export function requiredKeys(T): any {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<(typeof T)[typeof k]>(Undefined) ? never : k
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type requiredKeys<T extends object> =
 *   { [K in keyof T]: undefined extends T[K] ? never : K }[keyof T]
 */

// ✗ addQuestionMarks: does not compile yet
//   Cannot find name 'requiredKeys'. Did you mean 'Required'?
/* @scripttype preserveParamNames */
/**
 * @param {object} T
 */
export function addQuestionMarks(T, _O = any): any {
  const out = emptyObject
  for (const K in keySet(requiredKeys(T))) {
    out[K] = T[K]
  }
  const out2 = emptyObject
  for (const K in keySet(optionalKeys(T))) {
    out2[K] = optional(T[K])
  }
  const out3 = emptyObject
  for (const k in keyof(T)) {
    out3[k] = optional(unknown)
  }
  return out & out2 & out3
}
/* compiles to:
 * export type addQuestionMarks<T extends object, _O = any> =
 *   & { [K in requiredKeys<T>]: T[K] }
 *   & { [K1 in optionalKeys<T>]?: T[K1] }
 *   & { [K2 in keyof T]?: unknown }
 */

// ✓ identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function identity(T): any {
  return T
}
/* compiles to:
 * export type identity<T> = T
 */

// ✓ flatten: verified type-identical to the original
/* @scripttype preserveParamNames */
export function flatten(T): any {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = T[k]
  }
  return identity(out)
}
/* compiles to:
 * export type flatten<T> = identity<{ [K in keyof T]: T[K] }>
 */

// ✓ noNeverKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function noNeverKeys(T): any {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<[ never ]>([T[k]]) ? never : k
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type noNeverKeys<T> = { [K in keyof T]: [T[K]] extends [never] ? never : K }[keyof T]
 */

// ✓ noNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function noNever(T): any {
  const out = emptyObject
  for (const k in keySet(noNeverKeys(T))) {
    out[k] = k in T ? T[k] : never
  }
  return identity(out)
}
/* compiles to:
 * export type noNever<T> = identity<{ [K in noNeverKeys<T>]: K extends keyof T ? T[K] : never }>
 */

// ✓ extendShape: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} A
 * @param {object} B
 */
export function extendShape(A, B): any {
  if (matches<never>(keyof(A) & keyof(B))) {
    return A & B
  }
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K in B ? never : K] = A[K]
  }
  const out2 = emptyObject
  for (const K in keyof(B)) {
    out2[K] = B[K]
  }
  return out & out2
}
/* compiles to:
 * export type extendShape<A extends object, B extends object> =
 *   keyof A & keyof B extends never
 *     ? A & B
 *     : { [K in keyof A as K extends keyof B ? never : K]: A[K] } & { [K1 in keyof B]: B[K1] }
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v3/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 43 type-level aliases: 35 covered, 3 raw, 3 typecheck-error, 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ArrayCardinality: any
declare const ArrayKeys: any
declare const BuiltIn: any
declare const ReadonlyMap: any
declare const ReadonlySet: any
declare const RefinementCtx: any
declare const UnknownKeysParam: any
declare const ZodArray: any
declare const ZodError: any
declare const ZodNullable: any
declare const ZodObject: any
declare const ZodOptional: any
declare const ZodRawShape: any
declare const ZodTuple: any
declare const ZodTupleItems: any
declare const ZodType: any
declare const ZodTypeAny: any
declare const objectUtil: any
type ArrayCardinality<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ArrayKeys<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BuiltIn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnumValues<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlyMap<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlySet<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RefinementCtx<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownKeysParam<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodNullable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodOptional<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodRawShape<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodTuple<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodTupleItems<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ZodTypeAny<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type objectUtil<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ TypeOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TypeOf(T: ZodType<any, any, any>) {
  return T['_output']
}
/* compiles to:
 * export type TypeOf<T extends ZodType<any, any, any>> = T['_output']
 */

// ✓ input: verified type-identical to the original
/* @scripttype preserveParamNames */
export function input(T: ZodType<any, any, any>) {
  return T['_input']
}
/* compiles to:
 * export type input<T extends ZodType<any, any, any>> = T['_input']
 */

// ✓ output: verified type-identical to the original
/* @scripttype preserveParamNames */
export function output(T: ZodType<any, any, any>) {
  return T['_output']
}
/* compiles to:
 * export type output<T extends ZodType<any, any, any>> = T['_output']
 */

// ✓ SafeParseSuccess: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseSuccess(Output) {
  return { success: true, data: Output, error: optional(never) }
}
/* compiles to:
 * export type SafeParseSuccess<Output> = { success: true; data: Output; error?: never }
 */

// ✓ SafeParseError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseError(Input) {
  return { success: false, error: ZodError(Input), data: optional(never) }
}
/* compiles to:
 * export type SafeParseError<Input> = { success: false; error: ZodError<Input>; data?: never }
 */

// ✓ SafeParseReturnType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseReturnType(Input, Output) {
  return SafeParseSuccess(Output) | SafeParseError(Input)
}
/* compiles to:
 * export type SafeParseReturnType<Input, Output> = SafeParseSuccess<Output> | SafeParseError<Input>
 */

// ✓ arrayOutputType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function arrayOutputType(T: ZodTypeAny, Cardinality: ArrayCardinality = 'many') {
  if (matches<"atleastone">(Cardinality)) {
    return [T['_output'], ...arrayOf(T['_output'])]
  }
  return arrayOf(T['_output'])
}
/* compiles to:
 * export type arrayOutputType<T extends ZodTypeAny, Cardinality extends ArrayCardinality = 'many'> = Cardinality extends 'atleastone' ? [T['_output'], ...T['_output'][]] : T['_output'][]
 */

// ✓ ZodNonEmptyArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ZodNonEmptyArray(T: ZodTypeAny) {
  return ZodArray(T, 'atleastone')
}
/* compiles to:
 * export type ZodNonEmptyArray<T extends ZodTypeAny> = ZodArray<T, 'atleastone'>
 */

// ✓ mergeTypes: verified type-identical to the original
/* @scripttype preserveParamNames */
export function mergeTypes(A, B) {
  const out = emptyObject
  for (const k in keySet(keyof(A) | keyof(B))) {
    out[k] = matches<keyof typeof B>(k) ? B[k] : (matches<keyof typeof A>(k) ? A[k] : never)
  }
  return out
}
/* compiles to:
 * export type mergeTypes<A, B> = { [K in keyof A | keyof B]: K extends keyof B ? B[K] : K extends keyof A ? A[K] : never }
 */

// ✓ objectOutputType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function objectOutputType(Shape: ZodRawShape, Catchall: ZodTypeAny, UnknownKeys: UnknownKeysParam = UnknownKeysParam) {
  return objectUtil.flatten(objectUtil.addQuestionMarks(baseObjectOutputType(Shape))) & CatchallOutput(Catchall) & PassthroughType(UnknownKeys)
}
/* compiles to:
 * export type objectOutputType<Shape extends ZodRawShape, Catchall extends ZodTypeAny, UnknownKeys extends UnknownKeysParam = UnknownKeysParam> = objectUtil.flatten<objectUtil.addQuestionMarks<baseObjectOutputType<Shape>>> & CatchallOutput<Catchall> & PassthroughType<UnknownKeys>
 */

// ✓ baseObjectOutputType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function baseObjectOutputType(Shape: ZodRawShape) {
  const out = emptyObject
  for (const k in keyof(Shape)) {
    out[k] = Shape[k]['_output']
  }
  return out
}
/* compiles to:
 * export type baseObjectOutputType<Shape extends ZodRawShape> = { [K in keyof Shape]: Shape[K]['_output'] }
 */

// ✓ objectInputType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function objectInputType(Shape: ZodRawShape, Catchall: ZodTypeAny, UnknownKeys: UnknownKeysParam = UnknownKeysParam) {
  return objectUtil.flatten(baseObjectInputType(Shape)) & CatchallInput(Catchall) & PassthroughType(UnknownKeys)
}
/* compiles to:
 * export type objectInputType<Shape extends ZodRawShape, Catchall extends ZodTypeAny, UnknownKeys extends UnknownKeysParam = UnknownKeysParam> = objectUtil.flatten<baseObjectInputType<Shape>> & CatchallInput<Catchall> & PassthroughType<UnknownKeys>
 */

// ✓ baseObjectInputType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function baseObjectInputType(Shape: ZodRawShape) {
  const out = emptyObject
  for (const k in keyof(Shape)) {
    out[k] = Shape[k]['_input']
  }
  return objectUtil.addQuestionMarks(out)
}
/* compiles to:
 * export type baseObjectInputType<Shape extends ZodRawShape> = objectUtil.addQuestionMarks<{ [K in keyof Shape]: Shape[K]['_input'] }>
 */

// ✗ CatchallOutput: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function CatchallOutput(T: ZodType) {
  if (matches<typeof T>(ZodType)) {
    return unknown
  }
  return raw('{ [k: string]: T["_output"]; }')
}

// ✗ CatchallInput: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function CatchallInput(T: ZodType) {
  if (matches<typeof T>(ZodType)) {
    return unknown
  }
  return raw('{ [k: string]: T["_input"]; }')
}

// ✗ PassthroughType: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function PassthroughType(T: UnknownKeysParam) {
  if (matches<"passthrough">(T)) {
    return raw('{ [k: string]: unknown; }')
  }
  return unknown
}

// ✓ deoptional: verified type-identical to the original
/* @scripttype preserveParamNames */
export function deoptional(T: ZodTypeAny) {
  let t = T
  while (true) {
    const m1 = matches<ZodOptional<Hole<"U">>>(t)
    if (!m1) {
      break
    }
    t = m1.U
  }
  const m2 = matches<ZodNullable<Hole<"U">>>(t)
  return m2 ? ZodNullable(deoptional(m2.U)) : t
}
/* compiles to:
 * export type deoptional<T extends ZodTypeAny> = Deoptional__loop<T>
 * type Deoptional__loop<T extends ZodTypeAny> = T extends ZodOptional<infer U> ? Deoptional__loop<U> : T extends ZodNullable<infer U> ? ZodNullable<deoptional<U>> : T
 */

// ✓ noUnrecognized: verified type-identical to the original
/* @scripttype preserveParamNames */
export function noUnrecognized(Obj: object, Shape: object) {
  const out = emptyObject
  for (const k in keyof(Obj)) {
    out[k] = matches<keyof typeof Shape>(k) ? Obj[k] : never
  }
  return out
}
/* compiles to:
 * export type noUnrecognized<Obj extends object, Shape extends object> = { [K in keyof Obj]: K extends keyof Shape ? Obj[K] : never }
 */

// ✓ ZodDiscriminatedUnionOption: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ZodDiscriminatedUnionOption(Discriminator: string) {
  const out = emptyObject
  for (const key in keySet(Discriminator)) {
    out[key] = ZodTypeAny
  }
  return ZodObject(out & ZodRawShape, UnknownKeysParam, ZodTypeAny)
}
/* compiles to:
 * export type ZodDiscriminatedUnionOption<Discriminator extends string> = ZodObject<{ [Key in Discriminator]: ZodTypeAny } & ZodRawShape, UnknownKeysParam, ZodTypeAny>
 */

// ✓ AssertArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AssertArray(T) {
  if (matches<any[]>(T)) {
    return T
  }
  return never
}
/* compiles to:
 * export type AssertArray<T> = T extends any[] ? T : never
 */

// ✓ OutputTypeOfTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OutputTypeOfTuple(T: ZodTupleItems | [ ]) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<ZodType<any, any, any>>(T[k]) ? T[k]['_output'] : never
  }
  return AssertArray(out)
}
/* compiles to:
 * export type OutputTypeOfTuple<T extends ZodTupleItems | []> = AssertArray<{ [K in keyof T]: T[K] extends ZodType<any, any, any> ? T[K]['_output'] : never }>
 */

// ✓ OutputTypeOfTupleWithRest: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OutputTypeOfTupleWithRest(T: ZodTupleItems | [ ], Rest: ZodTypeAny | null = Null) {
  if (matches<ZodTypeAny>(Rest)) {
    return [...OutputTypeOfTuple(T), ...arrayOf(Rest['_output'])]
  }
  return OutputTypeOfTuple(T)
}
/* compiles to:
 * export type OutputTypeOfTupleWithRest<T extends ZodTupleItems | [], Rest extends ZodTypeAny | null = null> = Rest extends ZodTypeAny ? [...OutputTypeOfTuple<T>, ...Rest['_output'][]] : OutputTypeOfTuple<T>
 */

// ✓ InputTypeOfTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InputTypeOfTuple(T: ZodTupleItems | [ ]) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<ZodType<any, any, any>>(T[k]) ? T[k]['_input'] : never
  }
  return AssertArray(out)
}
/* compiles to:
 * export type InputTypeOfTuple<T extends ZodTupleItems | []> = AssertArray<{ [K in keyof T]: T[K] extends ZodType<any, any, any> ? T[K]['_input'] : never }>
 */

// ✓ InputTypeOfTupleWithRest: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InputTypeOfTupleWithRest(T: ZodTupleItems | [ ], Rest: ZodTypeAny | null = Null) {
  if (matches<ZodTypeAny>(Rest)) {
    return [...InputTypeOfTuple(T), ...arrayOf(Rest['_input'])]
  }
  return InputTypeOfTuple(T)
}
/* compiles to:
 * export type InputTypeOfTupleWithRest<T extends ZodTupleItems | [], Rest extends ZodTypeAny | null = null> = Rest extends ZodTypeAny ? [...InputTypeOfTuple<T>, ...Rest['_input'][]] : InputTypeOfTuple<T>
 */

// ✓ RecordType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RecordType(K: string | number | symbol, V) {
  if (matches<[ typeof K ]>([string])) {
    return Record(K, V)
  }
  if (matches<[ typeof K ]>([number])) {
    return Record(K, V)
  }
  if (matches<[ typeof K ]>([symbol])) {
    return Record(K, V)
  }
  if (matches<[ typeof K ]>([BRAND(string | number | symbol)])) {
    return Record(K, V)
  }
  return Partial(Record(K, V))
}
/* compiles to:
 * export type RecordType<K extends string | number | symbol, V> = [string] extends [K] ? Record<K, V> : [number] extends [K] ? Record<K, V> : [symbol] extends [K] ? Record<K, V> : [BRAND<string | number | symbol>] extends [K] ? Record<K, V> : Partial<Record<K, V>>
 */

// ✗ OuterTypeOfFunction: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function OuterTypeOfFunction(Args: ZodTuple<any, any>, Returns: ZodTypeAny) {
  if (matches<Array<any>>(Args['_input'])) {
    return fnType([Args['_input']], Returns['_output'])
  }
  return never
}
/* compiles to:
 * export type OuterTypeOfFunction<Args extends ZodTuple<any, any>, Returns extends ZodTypeAny> = Args['_input'] extends Array<any> ? (a0: Args['_input']) => Returns['_output'] : never
 */

// ✗ InnerTypeOfFunction: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function InnerTypeOfFunction(Args: ZodTuple<any, any>, Returns: ZodTypeAny) {
  if (matches<Array<any>>(Args['_output'])) {
    return fnType([Args['_output']], Returns['_input'])
  }
  return never
}
/* compiles to:
 * export type InnerTypeOfFunction<Args extends ZodTuple<any, any>, Returns extends ZodTypeAny> = Args['_output'] extends Array<any> ? (a0: Args['_output']) => Returns['_input'] : never
 */

// ✓ Indices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Indices(T) {
  return Exclude(keyof(T), ArrayKeys)
}
/* compiles to:
 * export type Indices<T> = Exclude<keyof T, ArrayKeys>
 */

// ✓ EnumValues: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EnumValues(T: string = string) {
  return asReadonly([T, ...arrayOf(T)])
}
/* compiles to:
 * export type EnumValues<T extends string = string> = readonly [T, ...T[]]
 */

// ✓ Values: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Values(T: EnumValues) {
  const out = emptyObject
  for (const k in keySet(T[number])) {
    out[k] = k
  }
  return out
}
/* compiles to:
 * export type Values<T extends EnumValues> = { [K in T[number]]: K }
 */

// ✓ Writeable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Writeable(T) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = mutable(T[P])
  }
  return out
}
/* compiles to:
 * export type Writeable<T> = { -readonly [P in keyof T]: T[P] }
 */

// ✓ FilterEnum: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FilterEnum(Values, ToExclude) {
  if (matches<[ ]>(Values)) {
    return []
  }
  const m1 = matches<[ Hole<"Head">, ...Hole<"Rest"> ]>(Values)
  if (m1) {
    if (matches<typeof ToExclude>(m1.Head)) {
      return FilterEnum(m1.Rest, ToExclude)
    }
    return [m1.Head, ...FilterEnum(m1.Rest, ToExclude)]
  }
  return never
}
/* compiles to:
 * export type FilterEnum<Values, ToExclude> = Values extends [] ? [] : Values extends [infer Head, ...infer Rest] ? Head extends ToExclude ? FilterEnum<Rest, ToExclude> : [Head, ...FilterEnum<Rest, ToExclude>] : never
 */

// ✓ typecast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function typecast(A, T) {
  if (matches<typeof T>(A)) {
    return A
  }
  return never
}
/* compiles to:
 * export type typecast<A, T> = A extends T ? A : never
 */

// ✓ Refinement: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Refinement(T) {
  return fnType([T, RefinementCtx], any)
}
/* compiles to:
 * export type Refinement<T> = (a0: T, a1: RefinementCtx) => any
 */

// ✗ SuperRefinement: the ScriptType does not itself typecheck as TypeScript
//   SuperRefinement.st.ts(3:50) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function SuperRefinement(T) {
  return fnType([T, RefinementCtx], voidType() | Promise(voidType()))
}
/* compiles to:
 * export type SuperRefinement<T> = (a0: T, a1: RefinementCtx) => void | Promise<void>
 */

// ✓ RefinementEffect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RefinementEffect(T) {
  return { type: 'refinement', refinement: fnType([T, RefinementCtx], any) }
}
/* compiles to:
 * export type RefinementEffect<T> = { type: 'refinement'; refinement: (a0: T, a1: RefinementCtx) => any }
 */

// ✓ TransformEffect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransformEffect(T) {
  return { type: 'transform', transform: fnType([T, RefinementCtx], any) }
}
/* compiles to:
 * export type TransformEffect<T> = { type: 'transform'; transform: (a0: T, a1: RefinementCtx) => any }
 */

// ✓ PreprocessEffect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PreprocessEffect(T) {
  return { type: 'preprocess', transform: fnType([T, RefinementCtx], any) }
}
/* compiles to:
 * export type PreprocessEffect<T> = { type: 'preprocess'; transform: (a0: T, a1: RefinementCtx) => any }
 */

// ✓ Effect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Effect(T) {
  return RefinementEffect(T) | TransformEffect(T) | PreprocessEffect(T)
}
/* compiles to:
 * export type Effect<T> = RefinementEffect<T> | TransformEffect<T> | PreprocessEffect<T>
 */

// ✓ ZodOptionalType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ZodOptionalType(T: ZodTypeAny) {
  return ZodOptional(T)
}
/* compiles to:
 * export type ZodOptionalType<T extends ZodTypeAny> = ZodOptional<T>
 */

// ✓ ZodNullableType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ZodNullableType(T: ZodTypeAny) {
  return ZodNullable(T)
}
/* compiles to:
 * export type ZodNullableType<T extends ZodTypeAny> = ZodNullable<T>
 */

// ✗ BRAND: the ScriptType does not itself typecheck as TypeScript
//   BRAND.st.ts(7:12) TS2464: A computed property name must be of type 'string', 'number', 'symbol', or 'any'.
/* @scripttype preserveParamNames */
export function BRAND(T: string | number | symbol) {
  const out = emptyObject
  for (const k in keySet(T)) {
    out[k] = true
  }
  return { [BRAND]: out }
}
/* compiles to:
 * export type BRAND<T extends string | number | symbol> = { [BRAND]: { [K in T]: true } }
 */

// ✗ MakeReadonly: the ScriptType does not itself typecheck as TypeScript
//   MakeReadonly.st.ts(5:12) TS2693: 'ReadonlyMap' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function MakeReadonly(T) {
  const m1 = matches<Map<Hole<"K">, Hole<"V">>>(T)
  if (m1) {
    return ReadonlyMap(m1.K, m1.V)
  }
  const m2 = matches<Set<Hole<"V">>>(T)
  if (m2) {
    return ReadonlySet(m2.V)
  }
  const m3 = matches<[ Hole<"Head">, ...Hole<"Tail"> ]>(T)
  if (m3) {
    return asReadonly([m3.Head, ...m3.Tail])
  }
  const m4 = matches<Array<Hole<"V">>>(T)
  if (m4) {
    return ReadonlyArray(m4.V)
  }
  if (matches<BuiltIn>(T)) {
    return T
  }
  return Readonly(T)
}
/* compiles to:
 * export type MakeReadonly<T> = T extends Map<infer K, infer V> ? ReadonlyMap<K, V> : T extends Set<infer V> ? ReadonlySet<V> : T extends [infer Head, ...infer Tail] ? readonly [Head, ...Tail] : T extends Array<infer V> ? ReadonlyArray<V> : T extends BuiltIn ? T : Readonly<T>
 */

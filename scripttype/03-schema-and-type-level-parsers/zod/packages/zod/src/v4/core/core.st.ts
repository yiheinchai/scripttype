/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v4/core/core.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 3 typecheck-error, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const SomeType: any
declare const schemas: any
type SomeType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type schemas<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ $brand: the ScriptType does not itself typecheck as TypeScript
//   $brand.st.ts(7:12) TS2464: A computed property name must be of type 'string', 'number', 'symbol', or 'any'.
/* @scripttype preserveParamNames */
export function $brand(T: string | number | symbol = string | number | symbol) {
  const out = emptyObject
  for (const k in keySet(T)) {
    out[k] = true
  }
  return { [$brand]: out }
}
/* compiles to:
 * export type $brand<T extends string | number | symbol = string | number | symbol> = { [$brand]: { [K in T]: true } }
 */

// ✗ $ZodBranded: the ScriptType does not itself typecheck as TypeScript
//   $ZodBranded.st.ts(2:32) TS2702: 'schemas' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $ZodBranded(T: schemas.SomeType, Brand: string | number | symbol, Dir: "in" | "out" | "inout" = 'out') {
  return merge(T, matches<"inout">(Dir) ? { _zod: { input: input(T) & $brand(Brand), output: output(T) & $brand(Brand) } } : (matches<"in">(Dir) ? { _zod: { input: input(T) & $brand(Brand) } } : { _zod: { output: output(T) & $brand(Brand) } }))
}
/* compiles to:
 * export type $ZodBranded<T extends schemas.SomeType, Brand extends string | number | symbol, Dir extends 'in' | 'out' | 'inout' = 'out'> = T & (Dir extends 'inout' ? { _zod: { input: input<T> & $brand<Brand>; output: output<T> & $brand<Brand> } } : Dir extends 'in' ? { _zod: { input: input<T> & $brand<Brand> } } : { _zod: { output: output<T> & $brand<Brand> } })
 */

// ✗ $ZodNarrow: the ScriptType does not itself typecheck as TypeScript
//   $ZodNarrow.st.ts(2:31) TS2702: 'schemas' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $ZodNarrow(T: schemas.SomeType, Out) {
  return merge(T, { _zod: { output: Out } })
}
/* compiles to:
 * export type $ZodNarrow<T extends schemas.SomeType, Out> = T & { _zod: { output: Out } }
 */

// ✓ input: verified type-identical to the original
/* @scripttype preserveParamNames */
export function input(T) {
  if (matches<{ _zod: { input: any; }; }>(T)) {
    return T['_zod']['input']
  }
  return unknown
}
/* compiles to:
 * export type input<T> = T extends { _zod: { input: any; }; } ? T['_zod']['input'] : unknown
 */

// ✓ output: verified type-identical to the original
/* @scripttype preserveParamNames */
export function output(T) {
  if (matches<{ _zod: { output: any; }; }>(T)) {
    return T['_zod']['output']
  }
  return unknown
}
/* compiles to:
 * export type output<T> = T extends { _zod: { output: any; }; } ? T['_zod']['output'] : unknown
 */

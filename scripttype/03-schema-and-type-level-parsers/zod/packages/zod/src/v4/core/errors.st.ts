/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v4/core/errors.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 8 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace util {
  export type Flatten<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type MakePartial<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Primitive<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const $ZodCheck: any
declare const $ZodIssue: any
declare const $ZodIssueBase: any
declare const $ZodType: any
declare const Primitive: any
declare const util: any
type $ZodCheck<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type $ZodErrorTree<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type $ZodIssue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type $ZodIssueBase<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type $ZodType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Primitive<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type util<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ $ZodInternalIssue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodInternalIssue(T: $ZodIssueBase = $ZodIssue) {
  if (matches<any>(T)) {
    return RawIssue(T)
  }
  return never
}
/* compiles to:
 * export type $ZodInternalIssue<T extends $ZodIssueBase = $ZodIssue> =
 *   T extends any ? RawIssue<T> : never
 */

// ✓ RawIssue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RawIssue(T: $ZodIssueBase) {
  if (matches<any>(T)) {
    return util.Flatten(merge(util.MakePartial(T, anyOf('message', 'path')), { input: readonlyProp(unknown), inst: readonlyProp(optional($ZodType | $ZodCheck)), continue: readonlyProp(optional(anyOf(boolean, Undefined))) }, Record(string, unknown)))
  }
  return never
}
/* compiles to:
 * export type RawIssue<T extends $ZodIssueBase> =
 *   T extends any
 *     ? util.Flatten<
 *       & util.MakePartial<T, 'message' | 'path'>
 *       & {
 *           readonly input: unknown
 *           readonly inst?: $ZodType | $ZodCheck
 *           readonly continue?: boolean | undefined
 *         }
 *       & Record<string, unknown>
 *     >
 *     : never
 */

// ✓ $ZodRawIssue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodRawIssue(T: $ZodIssueBase = $ZodIssue) {
  return $ZodInternalIssue(T)
}
/* compiles to:
 * export type $ZodRawIssue<T extends $ZodIssueBase = $ZodIssue> = $ZodInternalIssue<T>
 */

// ✓ $ZodFlattenedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodFlattenedError(T, U = string) {
  return _FlattenedError(T, U)
}
/* compiles to:
 * export type $ZodFlattenedError<T, U = string> = _FlattenedError<T, U>
 */

// ✓ _FlattenedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _FlattenedError(T, U = string) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = optional(arrayOf(U))
  }
  return { formErrors: arrayOf(U), fieldErrors: out }
}
/* compiles to:
 * export type _FlattenedError<T, U = string> = {
 *   formErrors: U[]
 *   fieldErrors: { [P in keyof T]?: U[] }
 * }
 */

// ✓ _ZodFormattedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _ZodFormattedError(T, U = string) {
  if (matches<[ any, ...any[] ]>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = optional($ZodFormattedError(T[K], U))
    }
    return out
  }
  if (Array.isArray(T)) {
    return indexRecord(number, $ZodFormattedError(T[number], U))
  }
  if (matches<object>(T)) {
    const out2 = emptyObject
    for (const K in keyof(T)) {
      out2[K] = optional($ZodFormattedError(T[K], U))
    }
    return util.Flatten(out2)
  }
  return any
}
/* compiles to:
 * export type _ZodFormattedError<T, U = string> =
 *   T extends [any, ...any[]] ? { [K in keyof T]?: $ZodFormattedError<T[K], U> }
 *   : T extends any[] ? { [key: number]: $ZodFormattedError<T[number], U> }
 *   : T extends object ? util.Flatten<{ [K1 in keyof T]?: $ZodFormattedError<T[K1], U> }>
 *   : any
 */

// ✓ $ZodFormattedError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodFormattedError(T, U = string) {
  return merge({ _errors: arrayOf(U) }, util.Flatten(_ZodFormattedError(T, U)))
}
/* compiles to:
 * export type $ZodFormattedError<T, U = string> =
 *   { _errors: U[] } & util.Flatten<_ZodFormattedError<T, U>>
 */

// ✓ $ZodErrorTree: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodErrorTree(T, U = string) {
  if (matches<util.Primitive>(T)) {
    return { errors: arrayOf(U) }
  }
  if (matches<[ any, ...any[] ]>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = optional($ZodErrorTree(T[K], U))
    }
    return { errors: arrayOf(U), items: optional(out) }
  }
  if (Array.isArray(T)) {
    return { errors: arrayOf(U), items: optional(t<Array<$ZodErrorTree<(typeof T)[number], typeof U>>>()) }
  }
  if (matches<object>(T)) {
    const out2 = emptyObject
    for (const K in keyof(T)) {
      out2[K] = optional($ZodErrorTree(T[K], U))
    }
    return { errors: arrayOf(U), properties: optional(out2) }
  }
  return { errors: arrayOf(U) }
}
/* compiles to:
 * export type $ZodErrorTree<T, U = string> =
 *   T extends util.Primitive ? { errors: U[] }
 *   : T extends [any, ...any[]]
 *     ? { errors: U[]; items?: { [K in keyof T]?: $ZodErrorTree<T[K], U> } }
 *   : T extends any[] ? { errors: U[]; items?: Array<$ZodErrorTree<T[number], U>> }
 *   : T extends object
 *     ? { errors: U[]; properties?: { [K1 in keyof T]?: $ZodErrorTree<T[K1], U> } }
 *   : { errors: U[] }
 */

/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v4/core/schemas.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 29 type-level aliases: 13 covered, 8 typecheck-error, 5 raw, 3 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const $ZodFunctionIn: any
declare const $ZodFunctionOut: any
declare const $ZodLooseShape: any
declare const $ZodRecordKey: any
declare const $ZodTemplateLiteralPart: any
declare const $ZodType: any
declare const $partial: any
declare const EnumLike: any
declare const LiteralPart: any
declare const OptionalInSchema: any
declare const OptionalOutSchema: any
declare const ParsePayload: any
declare const PropertyKey: any
declare const SomeType: any
declare const StandardSchemaV1: any
declare const TupleItems: any
declare const core: any
declare const util: any
type $ZodFunctionIn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $ZodFunctionOut<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $ZodLooseShape<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $ZodRecordKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $ZodTemplateLiteralPart<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $ZodType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type $partial<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnumLike<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LiteralPart<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OptionalInSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OptionalOutSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParsePayload<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SomeType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StandardSchemaV1<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TupleItems<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type core<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type util<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ CheckFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CheckFn(T) {
  return fnType([ParsePayload(T)], util.MaybeAsync(voidType()))
}
/* compiles to:
 * export type CheckFn<T> = (a0: ParsePayload<T>) => util.MaybeAsync<void>
 */

// ✓ $ZodStandardSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $ZodStandardSchema(T) {
  return StandardSchemaV1.Props(core.input(T), core.output(T))
}
/* compiles to:
 * export type $ZodStandardSchema<T> = StandardSchemaV1.Props<core.input<T>, core.output<T>>
 */

// ✓ $InferObjectOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $InferObjectOutput(T: $ZodLooseShape, Extra: Record<string, unknown>) {
  if (matches<keyof typeof T>(string)) {
    if (matches<true>(util.IsAny(T[keyof(T)]))) {
      return Record(string, unknown)
    }
    return Record(string, core.output(T[keyof(T)]))
  }
  if (matches<never>(keyof(merge(T, Extra)))) {
    return Record(string, never)
  }
  const out = emptyObject
  for (const k in keyof(T)) {
    out[matches<OptionalOutSchema>(T[k]) ? never : k] = mutable(T[k]['_zod']['output'])
  }
  const out2 = emptyObject
  for (const k in keyof(T)) {
    out2[matches<OptionalOutSchema>(T[k]) ? k : never] = mutable(optional(T[k]['_zod']['output']))
  }
  return util.Prettify(merge(out, out2, Extra))
}
/* compiles to:
 * export type $InferObjectOutput<T extends $ZodLooseShape, Extra extends Record<string, unknown>> = string extends keyof T ? util.IsAny<T[keyof T]> extends true ? Record<string, unknown> : Record<string, core.output<T[keyof T]>> : keyof (T & Extra) extends never ? Record<string, never> : util.Prettify<{ -readonly [K in keyof T as T[K] extends OptionalOutSchema ? never : K]: T[K]['_zod']['output'] } & { -readonly [K1 in keyof T as T[K1] extends OptionalOutSchema ? K1 : never]?: T[K1]['_zod']['output'] } & Extra>
 */

// ✓ $InferObjectInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $InferObjectInput(T: $ZodLooseShape, Extra: Record<string, unknown>) {
  if (matches<keyof typeof T>(string)) {
    if (matches<true>(util.IsAny(T[keyof(T)]))) {
      return Record(string, unknown)
    }
    return Record(string, core.input(T[keyof(T)]))
  }
  if (matches<never>(keyof(merge(T, Extra)))) {
    return Record(string, never)
  }
  const out = emptyObject
  for (const k in keyof(T)) {
    out[matches<OptionalInSchema>(T[k]) ? never : k] = mutable(T[k]['_zod']['input'])
  }
  const out2 = emptyObject
  for (const k in keyof(T)) {
    out2[matches<OptionalInSchema>(T[k]) ? k : never] = mutable(optional(T[k]['_zod']['input']))
  }
  return util.Prettify(merge(out, out2, Extra))
}
/* compiles to:
 * export type $InferObjectInput<T extends $ZodLooseShape, Extra extends Record<string, unknown>> = string extends keyof T ? util.IsAny<T[keyof T]> extends true ? Record<string, unknown> : Record<string, core.input<T[keyof T]>> : keyof (T & Extra) extends never ? Record<string, never> : util.Prettify<{ -readonly [K in keyof T as T[K] extends OptionalInSchema ? never : K]: T[K]['_zod']['input'] } & { -readonly [K1 in keyof T as T[K1] extends OptionalInSchema ? K1 : never]?: T[K1]['_zod']['input'] } & Extra>
 */

// ✗ $catchall: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function $catchall(T: SomeType) {
  return { out: raw('{ [k: string]: core.output<T>; }'), in: raw('{ [k: string]: core.input<T>; }') }
}

// ✓ $InferUnionOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $InferUnionOutput(T: SomeType) {
  if (matches<any>(T)) {
    return core.output(T)
  }
  return never
}
/* compiles to:
 * export type $InferUnionOutput<T extends SomeType> = T extends any ? core.output<T> : never
 */

// ✓ $InferUnionInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $InferUnionInput(T: SomeType) {
  if (matches<any>(T)) {
    return core.input(T)
  }
  return never
}
/* compiles to:
 * export type $InferUnionInput<T extends SomeType> = T extends any ? core.input<T> : never
 */

// ✓ IsOptionalIn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsOptionalIn(T: SomeType) {
  if (matches<OptionalInSchema>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsOptionalIn<T extends SomeType> = T extends OptionalInSchema ? true : false
 */

// ✓ IsOptionalOut: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsOptionalOut(T: SomeType) {
  if (matches<OptionalOutSchema>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsOptionalOut<T extends SomeType> = T extends OptionalOutSchema ? true : false
 */

// ✗ $InferTupleInputType: the ScriptType does not itself typecheck as TypeScript
//   $InferTupleInputType.st.ts(2:41) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $InferTupleInputType(T: util.TupleItems, Rest: SomeType | null) {
  return [...TupleInputTypeWithOptionals(T), ...matches<SomeType>(Rest) ? arrayOf(core.input(Rest)) : []]
}
/* compiles to:
 * export type $InferTupleInputType<T extends util.TupleItems, Rest extends SomeType | null> = [...TupleInputTypeWithOptionals<T>, ...(Rest extends SomeType ? core.input<Rest>[] : [])]
 */

// ✗ TupleInputTypeNoOptionals: the ScriptType does not itself typecheck as TypeScript
//   TupleInputTypeNoOptionals.st.ts(2:46) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TupleInputTypeNoOptionals(T: util.TupleItems) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = core.input(T[k])
  }
  return out
}
/* compiles to:
 * export type TupleInputTypeNoOptionals<T extends util.TupleItems> = { [K in keyof T]: core.input<T[K]> }
 */

// ✗ TupleInputTypeWithOptionals: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function TupleInputTypeWithOptionals(T: util.TupleItems) {
  const m1 = matches<readonly [ ...Hole<"Prefix", SomeType[]>, Hole<"Tail", SomeType> ]>(T)
  if (m1) {
    if (matches<"optional">(m1.Tail['_zod']['optin'])) {
      return [...TupleInputTypeWithOptionals(m1.Prefix), raw('core.input<Tail>?')]
    }
    return TupleInputTypeNoOptionals(T)
  }
  return []
}

// ✗ $InferTupleOutputType: the ScriptType does not itself typecheck as TypeScript
//   $InferTupleOutputType.st.ts(2:42) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $InferTupleOutputType(T: util.TupleItems, Rest: SomeType | null) {
  return [...TupleOutputTypeWithOptionals(T), ...matches<SomeType>(Rest) ? arrayOf(core.output(Rest)) : []]
}
/* compiles to:
 * export type $InferTupleOutputType<T extends util.TupleItems, Rest extends SomeType | null> = [...TupleOutputTypeWithOptionals<T>, ...(Rest extends SomeType ? core.output<Rest>[] : [])]
 */

// ✗ TupleOutputTypeNoOptionals: the ScriptType does not itself typecheck as TypeScript
//   TupleOutputTypeNoOptionals.st.ts(2:47) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TupleOutputTypeNoOptionals(T: util.TupleItems) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = core.output(T[k])
  }
  return out
}
/* compiles to:
 * export type TupleOutputTypeNoOptionals<T extends util.TupleItems> = { [K in keyof T]: core.output<T[K]> }
 */

// ✗ TupleOutputTypeWithOptionals: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function TupleOutputTypeWithOptionals(T: util.TupleItems) {
  const m1 = matches<readonly [ ...Hole<"Prefix", SomeType[]>, Hole<"Tail", SomeType> ]>(T)
  if (m1) {
    if (matches<"optional">(m1.Tail['_zod']['optout'])) {
      return [...TupleOutputTypeWithOptionals(m1.Prefix), raw('core.output<Tail>?')]
    }
    return TupleOutputTypeNoOptionals(T)
  }
  return []
}

// ✓ $InferZodRecordOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function $InferZodRecordOutput(Key: $ZodRecordKey = $ZodRecordKey, Value: SomeType = $ZodType) {
  if (matches<$partial>(Key)) {
    return Partial(Record(core.output(Key), core.output(Value)))
  }
  return Record(core.output(Key), core.output(Value))
}
/* compiles to:
 * export type $InferZodRecordOutput<Key extends $ZodRecordKey = $ZodRecordKey, Value extends SomeType = $ZodType> = Key extends $partial ? Partial<Record<core.output<Key>, core.output<Value>>> : Record<core.output<Key>, core.output<Value>>
 */

// ✗ $InferZodRecordInput: the ScriptType does not itself typecheck as TypeScript
//   $InferZodRecordInput.st.ts(4:45) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function $InferZodRecordInput(Key: $ZodRecordKey = $ZodRecordKey, Value: SomeType = $ZodType) {
  if (matches<$partial>(Key)) {
    return Partial(Record(core.input(Key) & PropertyKey, core.input(Value)))
  }
  return Record(core.input(Key) & PropertyKey, core.input(Value))
}
/* compiles to:
 * export type $InferZodRecordInput<Key extends $ZodRecordKey = $ZodRecordKey, Value extends SomeType = $ZodType> = Key extends $partial ? Partial<Record<core.input<Key> & PropertyKey, core.input<Value>>> : Record<core.input<Key> & PropertyKey, core.input<Value>>
 */

// ✗ $InferEnumOutput: the ScriptType does not itself typecheck as TypeScript
//   $InferEnumOutput.st.ts(2:37) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $InferEnumOutput(T: util.EnumLike) {
  return merge(T[keyof(T)], {})
}
/* compiles to:
 * export type $InferEnumOutput<T extends util.EnumLike> = T[keyof T] & {}
 */

// ✗ $InferEnumInput: the ScriptType does not itself typecheck as TypeScript
//   $InferEnumInput.st.ts(2:36) TS2702: 'util' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function $InferEnumInput(T: util.EnumLike) {
  return merge(T[keyof(T)], {})
}
/* compiles to:
 * export type $InferEnumInput<T extends util.EnumLike> = T[keyof T] & {}
 */

// ✓ UndefinedToEmptyString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UndefinedToEmptyString(T) {
  if (matches<undefined>(T)) {
    return ''
  }
  return T
}
/* compiles to:
 * export type UndefinedToEmptyString<T> = T extends undefined ? '' : T
 */

// ✓ AppendToTemplateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AppendToTemplateLiteral(Template: string, Suffix: LiteralPart | $ZodType) {
  if (matches<LiteralPart>(Suffix)) {
    return `${Template}${UndefinedToEmptyString(Suffix)}`
  }
  if (matches<$ZodType>(Suffix)) {
    const m1 = matches<Hole<"T", LiteralPart>>(core.output(Suffix))
    return `${Template}${m1 ? UndefinedToEmptyString(m1.T) : never}`
  }
  return never
}
/* compiles to:
 * export type AppendToTemplateLiteral<Template extends string, Suffix extends LiteralPart | $ZodType> = Suffix extends LiteralPart ? `${Template}${UndefinedToEmptyString<Suffix>}` : Suffix extends $ZodType ? `${Template}${core.output<Suffix> extends (infer T extends LiteralPart) ? UndefinedToEmptyString<T> : never}` : never
 */

// ✓ ConcatenateTupleOfStrings: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConcatenateTupleOfStrings(T: string[]) {
  const m1 = matches<[ Hole<"First", string>, ...Hole<"Rest", string[]> ]>(T)
  if (m1) {
    if (matches<string[]>(m1.Rest)) {
      if (matches<"">(m1.First)) {
        return ConcatenateTupleOfStrings(m1.Rest)
      }
      return `${m1.First}${ConcatenateTupleOfStrings(m1.Rest)}`
    }
    return never
  }
  return ''
}
/* compiles to:
 * export type ConcatenateTupleOfStrings<T extends string[]> = T extends [infer First extends string, ...(infer Rest extends string[])] ? Rest extends string[] ? First extends '' ? ConcatenateTupleOfStrings<Rest> : `${First}${ConcatenateTupleOfStrings<Rest>}` : never : ''
 */

// ✗ ConvertPartsToStringTuple: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ConvertPartsToStringTuple(Parts: $ZodTemplateLiteralPart[]) {
  const out = emptyObject
  for (const K in keyof(Parts)) {
    out[K] = matches<LiteralPart>(Parts[K]) ? `${UndefinedToEmptyString(Parts[K])}` : (matches<$ZodType>(Parts[K]) ? `${raw('core.output<Parts[K]> extends infer T extends LiteralPart ? UndefinedToEmptyString<T> : never')}` : never)
  }
  return out
}

// ✓ ToTemplateLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToTemplateLiteral(Parts: $ZodTemplateLiteralPart[]) {
  return ConcatenateTupleOfStrings(ConvertPartsToStringTuple(Parts))
}
/* compiles to:
 * export type ToTemplateLiteral<Parts extends $ZodTemplateLiteralPart[]> = ConcatenateTupleOfStrings<ConvertPartsToStringTuple<Parts>>
 */

// ✗ $PartsToTemplateLiteral: uses raw() — language gap, does not count as covered
//   gap: literal type FirstTemplateToken
/* @scripttype preserveParamNames */
export function $PartsToTemplateLiteral(Parts: $ZodTemplateLiteralPart[]) {
  if (matches<typeof Parts>([])) {
    return raw('``')
  }
  const m1 = matches<[ ...Hole<"Rest">, Hole<"Last", $ZodTemplateLiteralPart> ]>(Parts)
  if (m1) {
    if (matches<$ZodTemplateLiteralPart[]>(m1.Rest)) {
      return AppendToTemplateLiteral($PartsToTemplateLiteral(m1.Rest), m1.Last)
    }
    return never
  }
  return never
}

// ✗ $InferInnerFunctionType: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function $InferInnerFunctionType(Args: $ZodFunctionIn, Returns: $ZodFunctionOut) {
  return fnType([matches<typeof Args>($ZodFunctionIn) ? arrayOf(never) : core.output(Args)], core.input(Returns))
}
/* compiles to:
 * export type $InferInnerFunctionType<Args extends $ZodFunctionIn, Returns extends $ZodFunctionOut> = (a0: $ZodFunctionIn extends Args ? never[] : core.output<Args>) => core.input<Returns>
 */

// ✗ $InferInnerFunctionTypeAsync: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function $InferInnerFunctionTypeAsync(Args: $ZodFunctionIn, Returns: $ZodFunctionOut) {
  return fnType([matches<typeof Args>($ZodFunctionIn) ? arrayOf(never) : core.output(Args)], util.MaybeAsync(core.input(Returns)))
}
/* compiles to:
 * export type $InferInnerFunctionTypeAsync<Args extends $ZodFunctionIn, Returns extends $ZodFunctionOut> = (a0: $ZodFunctionIn extends Args ? never[] : core.output<Args>) => util.MaybeAsync<core.input<Returns>>
 */

// ✗ $InferOuterFunctionType: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function $InferOuterFunctionType(Args: $ZodFunctionIn, Returns: $ZodFunctionOut) {
  return fnType([matches<typeof Args>($ZodFunctionIn) ? arrayOf(never) : core.input(Args)], core.output(Returns))
}
/* compiles to:
 * export type $InferOuterFunctionType<Args extends $ZodFunctionIn, Returns extends $ZodFunctionOut> = (a0: $ZodFunctionIn extends Args ? never[] : core.input<Args>) => core.output<Returns>
 */

// ✗ $InferOuterFunctionTypeAsync: the ScriptType does not itself typecheck as TypeScript
//   $InferOuterFunctionTypeAsync.st.ts(3:93) TS2348: Value of type 'PromiseConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function $InferOuterFunctionTypeAsync(Args: $ZodFunctionIn, Returns: $ZodFunctionOut) {
  return fnType([matches<typeof Args>($ZodFunctionIn) ? arrayOf(never) : core.input(Args)], Promise(core.output(Returns)))
}
/* compiles to:
 * export type $InferOuterFunctionTypeAsync<Args extends $ZodFunctionIn, Returns extends $ZodFunctionOut> = (a0: $ZodFunctionIn extends Args ? never[] : core.input<Args>) => Promise<core.output<Returns>>
 */

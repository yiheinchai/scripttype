/**
 * ScriptType for 03-schema-and-type-level-parsers/zod/packages/zod/src/v4/core/util.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 41 type-level aliases: 33 covered, 6 raw, 2 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const $ZodLooseShape: any
declare const AnyFunc: any
declare const BuiltIn: any
declare const EnumValue: any
declare const PropertyKey: any
declare const ReadonlyMap: any
declare const ReadonlySet: any
declare const SomeObject: any
declare const SomeType: any
declare const V: any
declare const errors: any
declare const schemas: any
type $ZodLooseShape<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyFunc<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BuiltIn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EnumValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlyMap<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReadonlySet<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SomeObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SomeType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type V<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type errors<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type schemas<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ AssertEqual: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function AssertEqual(T, U) {
  if (matches<<V>() => V extends typeof U ? 1 : 2>(raw('<V>() => V extends T ? 1 : 2'))) {
    return true
  }
  return false
}

// ✗ AssertNotEqual: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function AssertNotEqual(T, U) {
  if (matches<<V>() => V extends typeof U ? 1 : 2>(raw('<V>() => V extends T ? 1 : 2'))) {
    return false
  }
  return true
}

// ✓ AssertExtends: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AssertExtends(T, U) {
  if (matches<typeof U>(T)) {
    return T
  }
  return never
}
/* compiles to:
 * export type AssertExtends<T, U> = T extends U ? T : never
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T) {
  if (matches<1 & typeof T>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = 0 extends 1 & T ? true : false
 */

// ✓ Omit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Omit(T, K: keyof typeof T) {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>
 */

// ✓ OmitKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitKeys(T, K: string) {
  return Pick(T, Exclude(keyof(T), K))
}
/* compiles to:
 * export type OmitKeys<T, K extends string> = Pick<T, Exclude<keyof T, K>>
 */

// ✓ MakePartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MakePartial(T, K: keyof typeof T) {
  return Omit(T, K) & InexactPartial(Pick(T, K))
}
/* compiles to:
 * export type MakePartial<T, K extends keyof T> = Omit<T, K> & InexactPartial<Pick<T, K>>
 */

// ✓ MakeRequired: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MakeRequired(T, K: keyof typeof T) {
  return Omit(T, K) & Required(Pick(T, K))
}
/* compiles to:
 * export type MakeRequired<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>
 */

// ✓ Exactly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Exactly(T, X) {
  return T & Record(Exclude(keyof(X), keyof(T)), never)
}
/* compiles to:
 * export type Exactly<T, X> = T & Record<Exclude<keyof X, keyof T>, never>
 */

// ✓ NoUndefined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoUndefined(T) {
  if (matches<undefined>(T)) {
    return never
  }
  return T
}
/* compiles to:
 * export type NoUndefined<T> = T extends undefined ? never : T
 */

// ✗ LoosePartial: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function LoosePartial(T: object) {
  return merge(InexactPartial(T), raw('{ [k: string]: unknown; }'))
}

// ✓ Mask: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mask(Keys: PropertyKey) {
  const out = emptyObject
  for (const K in keySet(Keys)) {
    out[K] = optional(true)
  }
  return out
}
/* compiles to:
 * export type Mask<Keys extends PropertyKey> = { [K in Keys]?: true }
 */

// ✓ Writeable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Writeable(T) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = mutable(T[P])
  }
  return merge(out, {})
}
/* compiles to:
 * export type Writeable<T> = { -readonly [P in keyof T]: T[P] } & {}
 */

// ✓ InexactPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InexactPartial(T) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = optional(anyOf(T[P], Undefined))
  }
  return out
}
/* compiles to:
 * export type InexactPartial<T> = { [P in keyof T]?: T[P] | undefined }
 */

// ✗ MakeReadonly: the ScriptType does not itself typecheck as TypeScript
//   MakeReadonly.st.ts(5:12) TS2693: 'ReadonlyMap' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function MakeReadonly(T) {
  const m1 = matches<Map<Hole<"K">, Hole<"V">>>(T)
  if (m1) {
    return ReadonlyMap(m1.K, m1.V)
  }
  const m2 = matches<Set<Hole<"V">>>(T)
  if (m2) {
    return ReadonlySet(m2.V)
  }
  const m3 = matches<[ Hole<"Head">, ...Hole<"Tail"> ]>(T)
  if (m3) {
    return asReadonly([m3.Head, ...m3.Tail])
  }
  const m4 = matches<Array<Hole<"V">>>(T)
  if (m4) {
    return t<ReadonlyArray<typeof m4.V>>()
  }
  if (matches<BuiltIn>(T)) {
    return T
  }
  return Readonly(T)
}
/* compiles to:
 * export type MakeReadonly<T> = T extends Map<infer K, infer V> ? ReadonlyMap<K, V> : T extends Set<infer V> ? ReadonlySet<V> : T extends [infer Head, ...infer Tail] ? readonly [Head, ...Tail] : T extends Array<infer V> ? ReadonlyArray<V> : T extends BuiltIn ? T : Readonly<T>
 */

// ✓ Identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Identity(T) {
  return T
}
/* compiles to:
 * export type Identity<T> = T
 */

// ✓ Flatten: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Flatten(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = T[k]
  }
  return Identity(out)
}
/* compiles to:
 * export type Flatten<T> = Identity<{ [K in keyof T]: T[K] }>
 */

// ✓ Mapped: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mapped(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = T[k]
  }
  return out
}
/* compiles to:
 * export type Mapped<T> = { [K in keyof T]: T[K] }
 */

// ✓ Prettify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prettify(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Prettify<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ NoNeverKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoNeverKeys(T) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = matches<[ never ]>([T[k]]) ? never : k
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type NoNeverKeys<T> = { [K in keyof T]: [T[K]] extends [never] ? never : K }[keyof T]
 */

// ✓ NoNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoNever(T) {
  const out = emptyObject
  for (const k in keySet(NoNeverKeys(T))) {
    out[k] = matches<keyof typeof T>(k) ? T[k] : never
  }
  return Identity(out)
}
/* compiles to:
 * export type NoNever<T> = Identity<{ [K in NoNeverKeys<T>]: K extends keyof T ? T[K] : never }>
 */

// ✓ Extend: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Extend(A: SomeObject, B: SomeObject) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[matches<keyof typeof B>(K) ? never : K] = A[K]
  }
  const out2 = emptyObject
  for (const K in keyof(B)) {
    out2[K] = B[K]
  }
  return Flatten(matches<never>(keyof(A) & keyof(B)) ? (merge(A, B)) : (out & out2))
}
/* compiles to:
 * export type Extend<A extends SomeObject, B extends SomeObject> = Flatten<keyof A & keyof B extends never ? A & B : { [K in keyof A as K extends keyof B ? never : K]: A[K] } & { [K1 in keyof B]: B[K1] }>
 */

// ✓ IsProp: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsProp(T, K: keyof typeof T) {
  if (matches<AnyFunc>(T[K])) {
    return never
  }
  return K
}
/* compiles to:
 * export type IsProp<T, K extends keyof T> = T[K] extends AnyFunc ? never : K
 */

// ✓ MaybeAsync: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeAsync(T) {
  return T | t<Promise<typeof T>>()
}
/* compiles to:
 * export type MaybeAsync<T> = T | Promise<T>
 */

// ✓ KeyOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KeyOf(T) {
  return keyof(OmitIndexSignature(T))
}
/* compiles to:
 * export type KeyOf<T> = keyof OmitIndexSignature<T>
 */

// ✓ OmitIndexSignature: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitIndexSignature(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[matches<typeof K>(string) ? never : (matches<string>(K) ? K : never)] = T[K]
  }
  return out
}
/* compiles to:
 * export type OmitIndexSignature<T> = { [K in keyof T as string extends K ? never : K extends string ? K : never]: T[K] }
 */

// ✓ ExtractIndexSignature: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractIndexSignature(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[matches<typeof K>(string) ? K : (matches<string>(K) ? never : K)] = T[K]
  }
  return out
}
/* compiles to:
 * export type ExtractIndexSignature<T> = { [K in keyof T as string extends K ? K : K extends string ? never : K]: T[K] }
 */

// ✓ Keys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Keys(T: object) {
  return keyof(OmitIndexSignature(T))
}
/* compiles to:
 * export type Keys<T extends object> = keyof OmitIndexSignature<T>
 */

// ✗ SchemaClass: uses raw() — language gap, does not count as covered
//   gap: object member ConstructSignature
/* @scripttype preserveParamNames */
export function SchemaClass(T: schemas.SomeType) {
  return raw('{ new (def: T["_zod"]["def"]): T; }')
}

// ✓ ToEnum: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToEnum(T: EnumValue) {
  const out = emptyObject
  for (const k in keySet(T)) {
    out[k] = k
  }
  return Flatten(out)
}
/* compiles to:
 * export type ToEnum<T extends EnumValue> = Flatten<{ [K in T]: K }>
 */

// ✓ KeysEnum: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KeysEnum(T: object) {
  return ToEnum(Exclude(keyof(T), symbol))
}
/* compiles to:
 * export type KeysEnum<T extends object> = ToEnum<Exclude<keyof T, symbol>>
 */

// ✓ KeysArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function KeysArray(T: object) {
  return Flatten(arrayOf(keyof(T) & string))
}
/* compiles to:
 * export type KeysArray<T extends object> = Flatten<(keyof T & string)[]>
 */

// ✓ SafeParseResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseResult(T) {
  return SafeParseSuccess(T) | SafeParseError(T)
}
/* compiles to:
 * export type SafeParseResult<T> = SafeParseSuccess<T> | SafeParseError<T>
 */

// ✓ SafeParseSuccess: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseSuccess(T) {
  return { success: true, data: T, error: optional(never) }
}
/* compiles to:
 * export type SafeParseSuccess<T> = { success: true; data: T; error?: never }
 */

// ✓ SafeParseError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeParseError(T) {
  return { success: false, data: optional(never), error: errors.$ZodError(T) }
}
/* compiles to:
 * export type SafeParseError<T> = { success: false; data?: never; error: errors.$ZodError<T> }
 */

// ✓ EmptyToNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EmptyToNever(T) {
  if (matches<never>(keyof(T))) {
    return never
  }
  return T
}
/* compiles to:
 * export type EmptyToNever<T> = keyof T extends never ? never : T
 */

// ✓ Normalize: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Normalize(T) {
  if (matches<undefined>(T)) {
    return never
  }
  if (matches<Record<any, any>>(T)) {
    const out = emptyObject
    for (const k in keyof(Omit(T, anyOf('error', 'message')))) {
      out[k] = T[k]
    }
    return Flatten(out & (matches<keyof typeof T>('error') ? { error: optional(Exclude(T['error'], string)) } : unknown))
  }
  return never
}
/* compiles to:
 * export type Normalize<T> = T extends undefined ? never : T extends Record<any, any> ? Flatten<{ [K in keyof Omit<T, 'error' | 'message'>]: T[K] } & ('error' extends keyof T ? { error?: Exclude<T['error'], string> } : unknown)> : never
 */

// ✓ CleanKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CleanKey(T: PropertyKey) {
  const m1 = matches<`?${Hole<"K">}`>(T)
  if (m1) {
    return m1.K
  }
  const m2 = matches<`${Hole<"K">}?`>(T)
  if (m2) {
    return m2.K
  }
  return T
}
/* compiles to:
 * export type CleanKey<T extends PropertyKey> = T extends `?${infer K}` ? K : T extends `${infer K}?` ? K : T
 */

// ✗ ToCleanMap: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ToCleanMap(T: schemas.$ZodLooseShape) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[k] = raw('k extends `?${infer K}` ? K : k extends `${infer K}?` ? K : k')
  }
  return out
}

// ✗ FromCleanMap: the ScriptType does not itself typecheck as TypeScript
//   FromCleanMap.st.ts(2:33) TS2702: 'schemas' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function FromCleanMap(T: schemas.$ZodLooseShape) {
  const out = emptyObject
  for (const k in keyof(T)) {
    out[m1 ? m1.K : (m2 ? m2.K : k)] = k
  }
  return out
}
/* compiles to:
 * export type FromCleanMap<T extends schemas.$ZodLooseShape> = { [K in keyof T as m1 extends true ? m1.K : m2 extends true ? m2.K : K]: K }
 */

// ✗ Constructor: uses raw() — language gap, does not count as covered
//   gap: type node ConstructorType
/* @scripttype preserveParamNames */
export function Constructor(T, Def: any[] = arrayOf(any)) {
  return raw('new (...args: Def) => T')
}

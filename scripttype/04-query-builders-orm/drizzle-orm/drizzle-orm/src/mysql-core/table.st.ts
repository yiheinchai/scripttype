/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/mysql-core/table.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/mysql-core/table.js'
import type { UpdateTableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/table.js'
declare const MySqlTable: any
declare const TableConfig: any
declare const UpdateTableConfig: any
type MySqlTable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ AnyMySqlTable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnyMySqlTable(TPartial: Partial<TableConfig> = {}): any {
  return MySqlTable(UpdateTableConfig(TableConfig, TPartial))
}
/* compiles to:
 * export type AnyMySqlTable<TPartial extends Partial<TableConfig> = {}> = MySqlTable<
 *   UpdateTableConfig<TableConfig, TPartial>
 * >
 */

// ✓ MySqlTableWithColumns: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MySqlTableWithColumns(T: TableConfig): any {
  const out = emptyObject
  for (const Key in keyof(T['columns'])) {
    out[Key] = T['columns'][Key]
  }
  return MySqlTable(T) & out
}
/* compiles to:
 * export type MySqlTableWithColumns<T extends TableConfig> =
 *   MySqlTable<T> & { [Key in keyof T['columns']]: T['columns'][Key] }
 */

/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/mysql2/driver.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Mode } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/mysql-core/session.js'
import type { DrizzleConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/utils.js'
declare const DrizzleConfig: any
declare const Mode: any
// ✓ MySql2DrizzleConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MySql2DrizzleConfig(TSchema: Record<string, unknown> = Record(string, never)): any {
  return merge(Omit(DrizzleConfig(TSchema), 'schema'), anyOf({ schema: TSchema, mode: Mode }, { schema: optional(Undefined), mode: optional(Mode) }))
}
/* compiles to:
 * export type MySql2DrizzleConfig<
 *   TSchema extends Record<string, unknown> = Record<string, never>
 * > =
 *   & Omit<DrizzleConfig<TSchema>, 'schema'>
 *   & ({ schema: TSchema; mode: Mode } | { schema?: undefined; mode?: Mode })
 */

/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/pg-core/columns/custom.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { CustomTypeValues } from '../../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/pg-core/columns/custom.js'
declare const CustomTypeValues: any
// ✓ ConvertCustomConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConvertCustomConfig(TName: string, T: Partial<CustomTypeValues>): any {
  return merge({ name: TName, dataType: 'custom', columnType: 'PgCustomColumn', data: T['data'], driverParam: T['driverData'], enumValues: Undefined }, matches<true>(T['notNull']) ? { notNull: true } : {}, matches<true>(T['default']) ? { hasDefault: true } : {})
}
/* compiles to:
 * export type ConvertCustomConfig<TName extends string, T extends Partial<CustomTypeValues>> =
 *   & {
 *       name: TName
 *       dataType: 'custom'
 *       columnType: 'PgCustomColumn'
 *       data: T['data']
 *       driverParam: T['driverData']
 *       enumValues: undefined
 *     }
 *   & (T['notNull'] extends true ? { notNull: true } : {})
 *   & (T['default'] extends true ? { hasDefault: true } : {})
 */

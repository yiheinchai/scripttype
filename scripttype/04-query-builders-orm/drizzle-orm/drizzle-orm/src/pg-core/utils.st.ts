/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/pg-core/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyPgColumn } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/pg-core/columns/common.js'
declare const AnyPgColumn: any
// ✓ ColumnsWithTable: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {string} TTableName
 * @param {AnyPgColumn<{ tableName: typeof TTableName; }>[]} TColumns
 */
export function ColumnsWithTable(TTableName, TForeignTableName: string, TColumns): any {
  const out = emptyObject
  for (const Key in keyof(TColumns)) {
    out[Key] = AnyPgColumn({ tableName: TForeignTableName })
  }
  return out
}
/* compiles to:
 * export type ColumnsWithTable<
 *   TTableName extends string,
 *   TForeignTableName extends string,
 *   TColumns extends AnyPgColumn<{ tableName: TTableName; }>[]
 * > =
 *   { [Key in keyof TColumns]: AnyPgColumn<{ tableName: TForeignTableName }> }
 */

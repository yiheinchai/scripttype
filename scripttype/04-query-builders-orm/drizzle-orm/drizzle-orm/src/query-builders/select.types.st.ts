/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/query-builders/select.types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 15 type-level aliases: 11 covered, 3 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Aliased: any
declare const Assume: any
declare const ChangeColumnTableName: any
declare const Column: any
declare const ColumnBaseConfig: any
declare const ColumnDataType: any
declare const ColumnsSelection: any
declare const Dialect: any
declare const DrizzleTypeError: any
declare const Equal: any
declare const GetColumnData: any
declare const IsAny: any
declare const JoinNullability: any
declare const JoinType: any
declare const SQL: any
declare const SelectMode: any
declare const SelectedFields: any
declare const Simplify: any
declare const Subquery: any
declare const Table: any
declare const TableLike: any
declare const UpdateColConfig: any
declare const View: any
type Aliased<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Assume<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ChangeColumnTableName<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Column<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ColumnBaseConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ColumnDataType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ColumnsSelection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Dialect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DrizzleTypeError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Equal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GetColumnData<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsAny<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type JoinNullability<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type JoinType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SQL<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SelectMode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SelectedFields<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Simplify<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Subquery<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Table<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TableLike<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UpdateColConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type View<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ApplyNullability: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyNullability(T, TNullability: JoinNullability) {
  if (matches<'nullable'>(TNullability)) {
    return T | Null
  }
  if (matches<'null'>(TNullability)) {
    return Null
  }
  return T
}
/* compiles to:
 * export type ApplyNullability<T, TNullability extends JoinNullability> = TNullability extends 'nullable' ? T | null : TNullability extends 'null' ? null : T
 */

// ✓ ApplyNullabilityToColumn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyNullabilityToColumn(TColumn: Column, TNullability: JoinNullability) {
  if (matches<'not-null'>(TNullability)) {
    return TColumn
  }
  return Column(Assume(UpdateColConfig(TColumn['_'], { notNull: matches<'nullable'>(TNullability) ? false : TColumn['_']['notNull'] }), ColumnBaseConfig(ColumnDataType, string)))
}
/* compiles to:
 * export type ApplyNullabilityToColumn<TColumn extends Column, TNullability extends JoinNullability> = TNullability extends 'not-null' ? TColumn : Column<Assume<UpdateColConfig<TColumn['_'], { notNull: TNullability extends 'nullable' ? false : TColumn['_']['notNull'] }>, ColumnBaseConfig<ColumnDataType, string>>>
 */

// ✓ ApplyNotNullMapToJoins: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyNotNullMapToJoins(TResult, TNullabilityMap: Record<string, JoinNullability>) {
  const out = emptyObject
  for (const TTableName in keySet(keyof(TResult) & keyof(TNullabilityMap) & string)) {
    out[TTableName] = ApplyNullability(TResult[TTableName], TNullabilityMap[TTableName])
  }
  return merge(out, {})
}
/* compiles to:
 * export type ApplyNotNullMapToJoins<TResult, TNullabilityMap extends Record<string, JoinNullability>> = { [TTableName in keyof TResult & keyof TNullabilityMap & string]: ApplyNullability<TResult[TTableName], TNullabilityMap[TTableName]> } & {}
 */

// ✓ SelectResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectResult(TResult, TSelectMode: SelectMode, TNullabilityMap: Record<string, JoinNullability>) {
  if (matches<'partial'>(TSelectMode)) {
    return SelectPartialResult(TResult, TNullabilityMap)
  }
  if (matches<'single'>(TSelectMode)) {
    return SelectResultFields(TResult)
  }
  return ApplyNotNullMapToJoins(SelectResultFields(TResult), TNullabilityMap)
}
/* compiles to:
 * export type SelectResult<TResult, TSelectMode extends SelectMode, TNullabilityMap extends Record<string, JoinNullability>> = TSelectMode extends 'partial' ? SelectPartialResult<TResult, TNullabilityMap> : TSelectMode extends 'single' ? SelectResultFields<TResult> : ApplyNotNullMapToJoins<SelectResultFields<TResult>, TNullabilityMap>
 */

// ✗ SelectPartialResult: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function SelectPartialResult(TFields, TNullability: Record<string, JoinNullability>) {
  if (matches<typeof TNullability>(TNullability)) {
    const out = emptyObject
    for (const Key in keyof(TFields)) {
      out[Key] = raw('TFields[Key] extends infer TField ? TField extends Table ? TField[\'_\'][\'name\'] extends keyof TNullability ? ApplyNullability<SelectResultFields<TField[\'_\'][\'columns\']>, TNullability[TField[\'_\'][\'name\']]> : never : TField extends Column ? TField[\'_\'][\'tableName\'] extends keyof TNullability ? ApplyNullability<SelectResultField<TField>, TNullability[TField[\'_\'][\'tableName\']]> : never : TField extends SQL | SQL.Aliased ? SelectResultField<TField> : TField extends Subquery ? FromSingleKeyObject<TField[\'_\'][\'selectedFields\'], TField[\'_\'][\'selectedFields\'] extends { [key: string]: infer TValue; } ? SelectResultField<TValue> : never, \'You can only select one column in the subquery\'> : TField extends Record<string, any> ? TField[keyof TField] extends AnyColumn<{ tableName: infer TTableName extends string; }> | SQL | SQL.Aliased ? Not<IsUnion<TTableName>> extends true ? ApplyNullability<SelectResultFields<TField>, TNullability[TTableName]> : SelectPartialResult<TField, TNullability> : never : never : never')
    }
    return out
  }
  return never
}

// ✓ MapColumnsToTableAlias: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MapColumnsToTableAlias(TColumns: ColumnsSelection, TAlias: string, TDialect: Dialect) {
  const out = emptyObject
  for (const Key in keyof(TColumns)) {
    out[Key] = matches<Column>(TColumns[Key]) ? ChangeColumnTableName(Assume(TColumns[Key], Column), TAlias, TDialect) : TColumns[Key]
  }
  return merge(out, {})
}
/* compiles to:
 * export type MapColumnsToTableAlias<TColumns extends ColumnsSelection, TAlias extends string, TDialect extends Dialect> = { [Key in keyof TColumns]: TColumns[Key] extends Column ? ChangeColumnTableName<Assume<TColumns[Key], Column>, TAlias, TDialect> : TColumns[Key] } & {}
 */

// ✗ AddAliasToSelection: the ScriptType does not itself typecheck as TypeScript
//   AddAliasToSelection.st.ts(5:233) TS2702: 'SQL' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function AddAliasToSelection(TSelection: ColumnsSelection, TAlias: string, TDialect: Dialect) {
  const out = emptyObject
  for (const Key in keyof(TSelection)) {
    out[Key] = matches<Column>(TSelection[Key]) ? ChangeColumnTableName(TSelection[Key], TAlias, TDialect) : (matches<Table>(TSelection[Key]) ? AddAliasToSelection(TSelection[Key]['_']['columns'], TAlias, TDialect) : (matches<SQL | SQL.Aliased>(TSelection[Key]) ? TSelection[Key] : (matches<ColumnsSelection>(TSelection[Key]) ? MapColumnsToTableAlias(TSelection[Key], TAlias, TDialect) : never)))
  }
  return Simplify(matches<true>(IsAny(TSelection)) ? any : out)
}
/* compiles to:
 * export type AddAliasToSelection<TSelection extends ColumnsSelection, TAlias extends string, TDialect extends Dialect> = Simplify<IsAny<TSelection> extends true ? any : { [Key in keyof TSelection]: TSelection[Key] extends Column ? ChangeColumnTableName<TSelection[Key], TAlias, TDialect> : TSelection[Key] extends Table ? AddAliasToSelection<TSelection[Key]['_']['columns'], TAlias, TDialect> : TSelection[Key] extends SQL | SQL.Aliased ? TSelection[Key] : TSelection[Key] extends ColumnsSelection ? MapColumnsToTableAlias<TSelection[Key], TAlias, TDialect> : never }>
 */

// ✓ AppendToResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AppendToResult(TTableName: string | undefined, TResult, TJoinedName: string | undefined, TSelectedFields: SelectedFields<Column, Table>, TOldSelectMode: SelectMode) {
  if (matches<'partial'>(TOldSelectMode)) {
    return TResult
  }
  if (matches<'single'>(TOldSelectMode)) {
    return (matches<string>(TTableName) ? Record(TTableName, TResult) : TResult) & (matches<string>(TJoinedName) ? Record(TJoinedName, TSelectedFields) : TSelectedFields)
  }
  return TResult & (matches<string>(TJoinedName) ? Record(TJoinedName, TSelectedFields) : TSelectedFields)
}
/* compiles to:
 * export type AppendToResult<TTableName extends string | undefined, TResult, TJoinedName extends string | undefined, TSelectedFields extends SelectedFields<Column, Table>, TOldSelectMode extends SelectMode> = TOldSelectMode extends 'partial' ? TResult : TOldSelectMode extends 'single' ? (TTableName extends string ? Record<TTableName, TResult> : TResult) & (TJoinedName extends string ? Record<TJoinedName, TSelectedFields> : TSelectedFields) : TResult & (TJoinedName extends string ? Record<TJoinedName, TSelectedFields> : TSelectedFields)
 */

// ✗ BuildSubquerySelection: the ScriptType does not itself typecheck as TypeScript
//   BuildSubquerySelection.st.ts(8:168) TS2702: 'SQL' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function BuildSubquerySelection(TSelection: ColumnsSelection, TNullability: Record<string, JoinNullability>) {
  if (matches<never>(TSelection)) {
    return any
  }
  const out = emptyObject
  for (const Key in keyof(TSelection)) {
    out[Key] = matches<SQL>(TSelection[Key]) ? DrizzleTypeError('You cannot reference this field without assigning it an alias first - use `.as(<alias>)`') : (matches<SQL.Aliased>(TSelection[Key]) ? TSelection[Key] : (matches<Table>(TSelection[Key]) ? BuildSubquerySelection(TSelection[Key]['_']['columns'], TNullability) : (matches<Column>(TSelection[Key]) ? ApplyNullabilityToColumn(TSelection[Key], TNullability[TSelection[Key]['_']['tableName']]) : (matches<ColumnsSelection>(TSelection[Key]) ? BuildSubquerySelection(TSelection[Key], TNullability) : never))))
  }
  return merge(out, {})
}
/* compiles to:
 * export type BuildSubquerySelection<TSelection extends ColumnsSelection, TNullability extends Record<string, JoinNullability>> = TSelection extends never ? any : { [Key in keyof TSelection]: TSelection[Key] extends SQL ? DrizzleTypeError<'You cannot reference this field without assigning it an alias first - use `.as(<alias>)`'> : TSelection[Key] extends SQL.Aliased ? TSelection[Key] : TSelection[Key] extends Table ? BuildSubquerySelection<TSelection[Key]['_']['columns'], TNullability> : TSelection[Key] extends Column ? ApplyNullabilityToColumn<TSelection[Key], TNullability[TSelection[Key]['_']['tableName']]> : TSelection[Key] extends ColumnsSelection ? BuildSubquerySelection<TSelection[Key], TNullability> : never } & {}
 */

// ✓ SetJoinsNullability: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SetJoinsNullability(TNullabilityMap: Record<string, JoinNullability>, TValue: JoinNullability) {
  const out = emptyObject
  for (const Key in keyof(TNullabilityMap)) {
    out[Key] = TValue
  }
  return out
}
/* compiles to:
 * export type SetJoinsNullability<TNullabilityMap extends Record<string, JoinNullability>, TValue extends JoinNullability> = { [Key in keyof TNullabilityMap]: TValue }
 */

// ✓ AppendToNullabilityMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AppendToNullabilityMap(TJoinsNotNull: Record<string, JoinNullability>, TJoinedName: string | undefined, TJoinType: JoinType) {
  if (matches<string>(TJoinedName)) {
    if (matches<typeof TJoinType>('left')) {
      const out = emptyObject
      for (const name in keySet(TJoinedName)) {
        out[name] = 'nullable'
      }
      return merge(TJoinsNotNull, out)
    }
    if (matches<typeof TJoinType>('right')) {
      const out2 = emptyObject
      for (const name in keySet(TJoinedName)) {
        out2[name] = 'not-null'
      }
      return SetJoinsNullability(TJoinsNotNull, 'nullable') & out2
    }
    if (matches<typeof TJoinType>('inner')) {
      const out3 = emptyObject
      for (const name in keySet(TJoinedName)) {
        out3[name] = 'not-null'
      }
      return merge(TJoinsNotNull, out3)
    }
    if (matches<typeof TJoinType>('cross')) {
      const out4 = emptyObject
      for (const name in keySet(TJoinedName)) {
        out4[name] = 'not-null'
      }
      return merge(TJoinsNotNull, out4)
    }
    if (matches<typeof TJoinType>('full')) {
      const out5 = emptyObject
      for (const name in keySet(TJoinedName)) {
        out5[name] = 'nullable'
      }
      return SetJoinsNullability(TJoinsNotNull, 'nullable') & out5
    }
    return never
  }
  return TJoinsNotNull
}
/* compiles to:
 * export type AppendToNullabilityMap<TJoinsNotNull extends Record<string, JoinNullability>, TJoinedName extends string | undefined, TJoinType extends JoinType> = TJoinedName extends string ? 'left' extends TJoinType ? TJoinsNotNull & { [Name in TJoinedName]: 'nullable' } : 'right' extends TJoinType ? SetJoinsNullability<TJoinsNotNull, 'nullable'> & { [Name1 in TJoinedName]: 'not-null' } : 'inner' extends TJoinType ? TJoinsNotNull & { [Name2 in TJoinedName]: 'not-null' } : 'cross' extends TJoinType ? TJoinsNotNull & { [Name3 in TJoinedName]: 'not-null' } : 'full' extends TJoinType ? SetJoinsNullability<TJoinsNotNull, 'nullable'> & { [Name4 in TJoinedName]: 'nullable' } : never : TJoinsNotNull
 */

// ✓ GetSelectTableName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetSelectTableName(TTable: TableLike) {
  if (matches<Table>(TTable)) {
    return TTable['_']['name']
  }
  if (matches<Subquery>(TTable)) {
    return TTable['_']['alias']
  }
  if (matches<View>(TTable)) {
    return TTable['_']['name']
  }
  if (matches<SQL>(TTable)) {
    return Undefined
  }
  return never
}
/* compiles to:
 * export type GetSelectTableName<TTable extends TableLike> = TTable extends Table ? TTable['_']['name'] : TTable extends Subquery ? TTable['_']['alias'] : TTable extends View ? TTable['_']['name'] : TTable extends SQL ? undefined : never
 */

// ✓ GetSelectTableSelection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetSelectTableSelection(TTable: TableLike) {
  if (matches<Table>(TTable)) {
    return TTable['_']['columns']
  }
  if (matches<Subquery | View>(TTable)) {
    return Assume(TTable['_']['selectedFields'], ColumnsSelection)
  }
  if (matches<SQL>(TTable)) {
    return {}
  }
  return never
}
/* compiles to:
 * export type GetSelectTableSelection<TTable extends TableLike> = TTable extends Table ? TTable['_']['columns'] : TTable extends Subquery | View ? Assume<TTable['_']['selectedFields'], ColumnsSelection> : TTable extends SQL ? {} : never
 */

// ✗ SelectResultField: the ScriptType does not itself typecheck as TypeScript
//   SelectResultField.st.ts(15:21) TS2702: 'SQL' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SelectResultField(T, TDeep: boolean = true) {
  if (matches<DrizzleTypeError<any>>(T)) {
    return T
  }
  if (matches<Table>(T)) {
    if (matches<true>(Equal(TDeep, true))) {
      return SelectResultField(T['_']['columns'], false)
    }
    return never
  }
  if (matches<Column<any>>(T)) {
    return GetColumnData(T)
  }
  if (matches<SQL | SQL.Aliased>(T)) {
    return T['_']['type']
  }
  if (matches<Record<string, any>>(T)) {
    return SelectResultFields(T, true)
  }
  return never
}
/* compiles to:
 * export type SelectResultField<T, TDeep extends boolean = true> = T extends DrizzleTypeError<any> ? T : T extends Table ? Equal<TDeep, true> extends true ? SelectResultField<T['_']['columns'], false> : never : T extends Column<any> ? GetColumnData<T> : T extends SQL | SQL.Aliased ? T['_']['type'] : T extends Record<string, any> ? SelectResultFields<T, true> : never
 */

// ✓ SelectResultFields: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectResultFields(TSelectedFields, TDeep: boolean = true) {
  const out = emptyObject
  for (const Key in keyof(TSelectedFields)) {
    out[Key] = SelectResultField(TSelectedFields[Key], TDeep)
  }
  return Simplify(out)
}
/* compiles to:
 * export type SelectResultFields<TSelectedFields, TDeep extends boolean = true> = Simplify<{ [Key in keyof TSelectedFields]: SelectResultField<TSelectedFields[Key], TDeep> }>
 */

/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/singlestore-core/table.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/singlestore-core/table.js'
import type { UpdateTableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/table.js'
declare const SingleStoreTable: any
declare const TableConfig: any
declare const UpdateTableConfig: any
type SingleStoreTable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ AnySingleStoreTable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnySingleStoreTable(TPartial: Partial<TableConfig> = {}): any {
  return SingleStoreTable(UpdateTableConfig(TableConfig, TPartial))
}
/* compiles to:
 * export type AnySingleStoreTable<TPartial extends Partial<TableConfig> = {}> = SingleStoreTable<
 *   UpdateTableConfig<TableConfig, TPartial>
 * >
 */

// ✓ SingleStoreTableWithColumns: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SingleStoreTableWithColumns(T: TableConfig): any {
  const out = emptyObject
  for (const Key in keyof(T['columns'])) {
    out[Key] = T['columns'][Key]
  }
  return SingleStoreTable(T) & out
}
/* compiles to:
 * export type SingleStoreTableWithColumns<T extends TableConfig> =
 *   SingleStoreTable<T> & { [Key in keyof T['columns']]: T['columns'][Key] }
 */

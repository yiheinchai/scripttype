/**
 * ScriptType for 04-query-builders-orm/drizzle-orm/drizzle-orm/src/sqlite-core/table.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/sqlite-core/table.js'
import type { UpdateTableConfig } from '../../../../../../04-query-builders-orm/drizzle-orm/drizzle-orm/src/table.js'
declare const SQLiteTable: any
declare const TableConfig: any
declare const UpdateTableConfig: any
type SQLiteTable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ AnySQLiteTable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnySQLiteTable(TPartial: Partial<TableConfig> = {}): any {
  return SQLiteTable(UpdateTableConfig(TableConfig, TPartial))
}
/* compiles to:
 * export type AnySQLiteTable<TPartial extends Partial<TableConfig> = {}> = SQLiteTable<
 *   UpdateTableConfig<TableConfig, TPartial>
 * >
 */

// ✓ SQLiteTableWithColumns: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SQLiteTableWithColumns(T: TableConfig): any {
  const out = emptyObject
  for (const Key in keyof(T['columns'])) {
    out[Key] = T['columns'][Key]
  }
  return SQLiteTable(T) & out
}
/* compiles to:
 * export type SQLiteTableWithColumns<T extends TableConfig> =
 *   SQLiteTable<T> & { [Key in keyof T['columns']]: T['columns'][Key] }
 */

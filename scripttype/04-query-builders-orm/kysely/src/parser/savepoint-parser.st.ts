/**
 * ScriptType for 04-query-builders-orm/kysely/src/parser/savepoint-parser.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ RollbackToSavepoint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RollbackToSavepoint(S: string[], SN: (typeof S)[number]) {
  const m1 = matches<[ ...Hole<"L">, Hole<"R"> ]>(S)
  if (m1) {
    if (matches<typeof SN>(m1.R)) {
      return S
    }
    return RollbackToSavepoint(matches<string[]>(m1.L) ? m1.L : never, SN)
  }
  return never
}
/* compiles to:
 * export type RollbackToSavepoint<S extends string[], SN extends S[number]> = S extends [...infer L, infer R] ? R extends SN ? S : RollbackToSavepoint<L extends string[] ? L : never, SN> : never
 */

// ✓ ReleaseSavepoint: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReleaseSavepoint(S: string[], SN: (typeof S)[number]) {
  const m1 = matches<[ ...Hole<"L">, Hole<"R"> ]>(S)
  if (m1) {
    if (matches<typeof SN>(m1.R)) {
      return m1.L
    }
    return ReleaseSavepoint(matches<string[]>(m1.L) ? m1.L : never, SN)
  }
  return never
}
/* compiles to:
 * export type ReleaseSavepoint<S extends string[], SN extends S[number]> = S extends [...infer L, infer R] ? R extends SN ? L : ReleaseSavepoint<L extends string[] ? L : never, SN> : never
 */

/**
 * ScriptType for 04-query-builders-orm/kysely/src/parser/select-parser.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 11 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AliasedExpression: any
declare const AliasedExpressionOrFactory: any
declare const AliasedSelectQueryBuilder: any
declare const AnyAliasedColumn: any
declare const AnyAliasedColumnWithTable: any
declare const AnyColumn: any
declare const AnyColumnWithTable: any
declare const DrainOuterGeneric: any
declare const DynamicReferenceBuilder: any
declare const ExpressionBuilder: any
declare const ExtractColumnType: any
declare const SelectType: any
type AliasedExpression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AliasedExpressionOrFactory<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AliasedSelectQueryBuilder<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyAliasedColumn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyAliasedColumnWithTable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyColumn<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AnyColumnWithTable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DrainOuterGeneric<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DynamicReferenceBuilder<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ExpressionBuilder<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ExtractColumnType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type SelectExpression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type SelectType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ SelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectExpression(DB, TB: keyof typeof DB) {
  return AnyAliasedColumnWithTable(DB, TB) | AnyAliasedColumn(DB, TB) | AnyColumnWithTable(DB, TB) | AnyColumn(DB, TB) | DynamicReferenceBuilder(any) | AliasedExpressionOrFactory(DB, TB)
}
/* compiles to:
 * export type SelectExpression<DB, TB extends keyof DB> =
 *   | AnyAliasedColumnWithTable<DB, TB>
 *   | AnyAliasedColumn<DB, TB>
 *   | AnyColumnWithTable<DB, TB>
 *   | AnyColumn<DB, TB>
 *   | DynamicReferenceBuilder<any>
 *   | AliasedExpressionOrFactory<DB, TB>
 */

// ✓ SelectCallback: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectCallback(DB, TB: keyof typeof DB) {
  return fnType([ExpressionBuilder(DB, TB)], t<ReadonlyArray<SelectExpression<typeof DB, typeof TB>>>())
}
/* compiles to:
 * export type SelectCallback<DB, TB extends keyof DB> =
 *   (a0: ExpressionBuilder<DB, TB>) => ReadonlyArray<SelectExpression<DB, TB>>
 */

// ✓ Selection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Selection(DB, TB: keyof typeof DB, SE) {
  if (matches<[ unknown ]>([DB])) {
    const out = emptyObject
    for (const E in keySet(FlattenSelectExpression(SE))) {
      out[ExtractAliasFromSelectExpression(E)] = SelectType(ExtractTypeFromSelectExpression(DB, TB, E))
    }
    return out
  }
  return {}
}
/* compiles to:
 * export type Selection<DB, TB extends keyof DB, SE> =
 *   [DB] extends [unknown]
 *     ? {
 *       [E in FlattenSelectExpression<SE> as ExtractAliasFromSelectExpression<E>]: SelectType<
 *         ExtractTypeFromSelectExpression<DB, TB, E>
 *       >
 *     }
 *     : {}
 */

// ✓ CallbackSelection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallbackSelection(DB, TB: keyof typeof DB, CB) {
  const m1 = matches<(eb: any) => ReadonlyArray<Hole<"SE">>>(CB)
  if (m1) {
    return Selection(DB, TB, m1.SE)
  }
  return never
}
/* compiles to:
 * export type CallbackSelection<DB, TB extends keyof DB, CB> =
 *   CB extends (eb: any) => ReadonlyArray<infer SE> ? Selection<DB, TB, SE> : never
 */

// ✓ SelectArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SelectArg(DB, TB: keyof typeof DB, SE: SelectExpression<typeof DB, typeof TB>) {
  return anyOf(SE, t<ReadonlyArray<typeof SE>>(), fnType([ExpressionBuilder(DB, TB)], t<ReadonlyArray<typeof SE>>()))
}
/* compiles to:
 * export type SelectArg<DB, TB extends keyof DB, SE extends SelectExpression<DB, TB>> =
 *   SE | ReadonlyArray<SE> | ((a0: ExpressionBuilder<DB, TB>) => ReadonlyArray<SE>)
 */

// ✓ FlattenSelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FlattenSelectExpression(SE) {
  const m1 = matches<DynamicReferenceBuilder<Hole<"RA">>>(SE)
  if (m1) {
    const out = emptyObject
    for (const R in keySet(m1.RA)) {
      out[R] = DynamicReferenceBuilder(R)
    }
    return out[m1.RA]
  }
  return SE
}
/* compiles to:
 * export type FlattenSelectExpression<SE> =
 *   SE extends DynamicReferenceBuilder<infer RA>
 *     ? { [R in RA]: DynamicReferenceBuilder<R> }[RA]
 *     : SE
 */

// ✓ ExtractAliasFromSelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAliasFromSelectExpression(SE) {
  if (typeof SE === 'string') {
    return ExtractAliasFromStringSelectExpression(SE)
  }
  const m1 = matches<AliasedExpression<any, Hole<"EA">>>(SE)
  if (m1) {
    return m1.EA
  }
  const m2 = matches<(qb: any) => AliasedExpression<any, Hole<"EA">>>(SE)
  if (m2) {
    return m2.EA
  }
  const m3 = matches<DynamicReferenceBuilder<Hole<"RA">>>(SE)
  if (m3) {
    return ExtractAliasFromStringSelectExpression(m3.RA)
  }
  return never
}
/* compiles to:
 * export type ExtractAliasFromSelectExpression<SE> =
 *   SE extends string ? ExtractAliasFromStringSelectExpression<SE>
 *   : SE extends AliasedExpression<any, infer EA> ? EA
 *   : SE extends (qb: any) => AliasedExpression<any, infer EA> ? EA
 *   : SE extends DynamicReferenceBuilder<infer RA> ? ExtractAliasFromStringSelectExpression<RA>
 *   : never
 */

// ✓ ExtractAliasFromStringSelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAliasFromStringSelectExpression(SE: string) {
  const m1 = matches<`${string}.${string}.${string} as ${Hole<"A">}`>(SE)
  if (m1) {
    return m1.A
  }
  const m2 = matches<`${string}.${string} as ${Hole<"A">}`>(SE)
  if (m2) {
    return m2.A
  }
  const m3 = matches<`${string} as ${Hole<"A">}`>(SE)
  if (m3) {
    return m3.A
  }
  const m4 = matches<`${string}.${string}.${Hole<"C">}`>(SE)
  if (m4) {
    return m4.C
  }
  const m5 = matches<`${string}.${Hole<"C">}`>(SE)
  if (m5) {
    return m5.C
  }
  return SE
}
/* compiles to:
 * export type ExtractAliasFromStringSelectExpression<SE extends string> =
 *   SE extends `${string}.${string}.${string} as ${infer A}` ? A
 *   : SE extends `${string}.${string} as ${infer A}` ? A
 *   : SE extends `${string} as ${infer A}` ? A
 *   : SE extends `${string}.${string}.${infer C}` ? C
 *   : SE extends `${string}.${infer C}` ? C
 *   : SE
 */

// ✓ ExtractTypeFromSelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractTypeFromSelectExpression(DB, TB: keyof typeof DB, SE) {
  if (typeof SE === 'string') {
    return ExtractTypeFromStringSelectExpression(DB, TB, SE)
  }
  const m1 = matches<AliasedSelectQueryBuilder<Hole<"O">, any>>(SE)
  if (m1) {
    return m1.O[keyof(m1.O)] | Null
  }
  const m2 = matches<(eb: any) => AliasedSelectQueryBuilder<Hole<"O">, any>>(SE)
  if (m2) {
    return m2.O[keyof(m2.O)] | Null
  }
  const m3 = matches<AliasedExpression<Hole<"O">, any>>(SE)
  if (m3) {
    return m3.O
  }
  const m4 = matches<(eb: any) => AliasedExpression<Hole<"O">, any>>(SE)
  if (m4) {
    return m4.O
  }
  const m5 = matches<DynamicReferenceBuilder<Hole<"RA">>>(SE)
  if (m5) {
    return anyOf(ExtractTypeFromStringSelectExpression(DB, TB, m5.RA), Undefined)
  }
  return never
}
/* compiles to:
 * export type ExtractTypeFromSelectExpression<DB, TB extends keyof DB, SE> =
 *   SE extends string ? ExtractTypeFromStringSelectExpression<DB, TB, SE>
 *   : SE extends AliasedSelectQueryBuilder<infer O, any> ? O[keyof O] | null
 *   : SE extends (eb: any) => AliasedSelectQueryBuilder<infer O, any> ? O[keyof O] | null
 *   : SE extends AliasedExpression<infer O, any> ? O
 *   : SE extends (eb: any) => AliasedExpression<infer O, any> ? O
 *   : SE extends DynamicReferenceBuilder<infer RA>
 *     ? ExtractTypeFromStringSelectExpression<DB, TB, RA> | undefined
 *   : never
 */

// ✓ ExtractTypeFromStringSelectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractTypeFromStringSelectExpression(DB, TB: keyof typeof DB, SE: string) {
  const m1 = matches<`${Hole<"SC">}.${Hole<"T">}.${Hole<"C">} as ${string}`>(SE)
  if (m1) {
    if (matches<typeof TB>(`${m1.SC}.${m1.T}`)) {
      if (m1.C in DB[`${m1.SC}.${m1.T}`]) {
        return DB[`${m1.SC}.${m1.T}`][m1.C]
      }
      return never
    }
    return never
  }
  const m2 = matches<`${Hole<"T">}.${Hole<"C">} as ${string}`>(SE)
  if (m2) {
    if (matches<typeof TB>(m2.T)) {
      if (m2.C in DB[m2.T]) {
        return DB[m2.T][m2.C]
      }
      return never
    }
    return never
  }
  const m3 = matches<`${Hole<"C">} as ${string}`>(SE)
  if (m3) {
    if (matches<AnyColumn<typeof DB, typeof TB>>(m3.C)) {
      return ExtractColumnType(DB, TB, m3.C)
    }
    return never
  }
  const m4 = matches<`${Hole<"SC">}.${Hole<"T">}.${Hole<"C">}`>(SE)
  if (m4) {
    if (matches<typeof TB>(`${m4.SC}.${m4.T}`)) {
      if (m4.C in DB[`${m4.SC}.${m4.T}`]) {
        return DB[`${m4.SC}.${m4.T}`][m4.C]
      }
      return never
    }
    return never
  }
  const m5 = matches<`${Hole<"T">}.${Hole<"C">}`>(SE)
  if (m5) {
    if (matches<typeof TB>(m5.T)) {
      if (m5.C in DB[m5.T]) {
        return DB[m5.T][m5.C]
      }
      return never
    }
    return never
  }
  if (matches<AnyColumn<typeof DB, typeof TB>>(SE)) {
    return ExtractColumnType(DB, TB, SE)
  }
  return never
}
/* compiles to:
 * export type ExtractTypeFromStringSelectExpression<DB, TB extends keyof DB, SE extends string> =
 *   SE extends `${infer SC}.${infer T}.${infer C} as ${string}`
 *     ? `${SC}.${T}` extends TB
 *       ? C extends keyof DB[`${SC}.${T}`] ? DB[`${SC}.${T}`][C] : never
 *       : never
 *   : SE extends `${infer T}.${infer C} as ${string}`
 *     ? T extends TB ? C extends keyof DB[T] ? DB[T][C] : never : never
 *   : SE extends `${infer C} as ${string}`
 *     ? C extends AnyColumn<DB, TB> ? ExtractColumnType<DB, TB, C> : never
 *   : SE extends `${infer SC}.${infer T}.${infer C}`
 *     ? `${SC}.${T}` extends TB
 *       ? C extends keyof DB[`${SC}.${T}`] ? DB[`${SC}.${T}`][C] : never
 *       : never
 *   : SE extends `${infer T}.${infer C}`
 *     ? T extends TB ? C extends keyof DB[T] ? DB[T][C] : never : never
 *   : SE extends AnyColumn<DB, TB> ? ExtractColumnType<DB, TB, SE>
 *   : never
 */

// ✓ AllSelection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AllSelection__v1(DB, TB, C) {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = SelectType(C in DB[T] ? DB[T][C] : never)
  }
  return out
}
export function AllSelection(DB, TB: keyof typeof DB) {
  const out = emptyObject
  for (const C in keySet(AnyColumn(DB, TB))) {
    out[C] = AllSelection__v1(DB, TB, C)[TB]
  }
  return DrainOuterGeneric(out)
}
/* compiles to:
 * export type AllSelection__v1<DB, TB, C> = {
 *   [T in TB]: SelectType<C extends keyof DB[T] ? DB[T][C] : never>
 * }
 * export type AllSelection<DB, TB extends keyof DB> = DrainOuterGeneric<
 *   { [C in AnyColumn<DB, TB>]: AllSelection__v1<DB, TB, C>[TB] }
 * >
 */

/**
 * ScriptType for 04-query-builders-orm/kysely/src/parser/update-set-parser.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 3 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyColumn: any
declare const DrainOuterGeneric: any
declare const ExpressionBuilder: any
declare const ExtractRawTypeFromReferenceExpression: any
declare const UpdateType: any
declare const ValueExpression: any
type AnyColumn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DrainOuterGeneric<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExpressionBuilder<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExtractRawTypeFromReferenceExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UpdateType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValueExpression<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ UpdateObject: the ScriptType does not itself typecheck as TypeScript
//   UpdateObject.st.ts(5:52) TS2304: Cannot find name 'C'.
/* @scripttype preserveParamNames */
export function UpdateObject(DB, TB: keyof typeof DB, UT: keyof typeof DB = TB) {
  const out2 = emptyObject
  for (const T in keySet(UT)) {
    out2[T] = matches<keyof (typeof DB)[typeof T]>(C) ? (anyOf(ValueExpression(DB, TB, UpdateType(DB[T][C])), Undefined)) : never
  }
  const out = emptyObject
  for (const C in keySet(AnyColumn(DB, UT))) {
    out[C] = optional(out2[UT])
  }
  return DrainOuterGeneric(out)
}
/* compiles to:
 * export type UpdateObject<DB, TB extends keyof DB, UT extends keyof DB = TB> = DrainOuterGeneric<{ [C in AnyColumn<DB, UT>]?: { [T in UT]: C extends keyof DB[T] ? ValueExpression<DB, TB, UpdateType<DB[T][C]>> | undefined : never }[UT] }>
 */

// ✓ UpdateObjectFactory: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UpdateObjectFactory(DB, TB: keyof typeof DB, UT: keyof typeof DB) {
  return fnType([ExpressionBuilder(DB, TB)], UpdateObject(DB, TB, UT))
}
/* compiles to:
 * export type UpdateObjectFactory<DB, TB extends keyof DB, UT extends keyof DB> = (a0: ExpressionBuilder<DB, TB>) => UpdateObject<DB, TB, UT>
 */

// ✓ UpdateObjectExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UpdateObjectExpression(DB, TB: keyof typeof DB, UT: keyof typeof DB = TB) {
  return UpdateObject(DB, TB, UT) | UpdateObjectFactory(DB, TB, UT)
}
/* compiles to:
 * export type UpdateObjectExpression<DB, TB extends keyof DB, UT extends keyof DB = TB> = UpdateObject<DB, TB, UT> | UpdateObjectFactory<DB, TB, UT>
 */

// ✓ ExtractUpdateTypeFromReferenceExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractUpdateTypeFromReferenceExpression(DB, TB: keyof typeof DB, RE, DV = unknown) {
  return UpdateType(ExtractRawTypeFromReferenceExpression(DB, TB, RE, DV))
}
/* compiles to:
 * export type ExtractUpdateTypeFromReferenceExpression<DB, TB extends keyof DB, RE, DV = unknown> = UpdateType<ExtractRawTypeFromReferenceExpression<DB, TB, RE, DV>>
 */

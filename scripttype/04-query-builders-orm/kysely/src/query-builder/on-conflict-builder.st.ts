/**
 * ScriptType for 04-query-builders-orm/kysely/src/query-builder/on-conflict-builder.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Updateable } from '../../../../../04-query-builders-orm/kysely/src/util/column-type.js'
declare const Updateable: any
// ✓ OnConflictDatabase: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function OnConflictDatabase(DB, TB): any {
  const out = emptyObject
  for (const K in keySet(anyOf(keyof(DB), 'excluded'))) {
    out[K] = Updateable(K in DB ? DB[K] : DB[TB])
  }
  return out
}
/* compiles to:
 * export type OnConflictDatabase<DB, TB extends keyof DB> = {
 *   [K in keyof DB | 'excluded']: Updateable<K extends keyof DB ? DB[K] : DB[TB]>
 * }
 */

// ✓ OnConflictTables: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OnConflictTables(TB): any {
  return anyOf(TB, 'excluded')
}
/* compiles to:
 * export type OnConflictTables<TB> = TB | 'excluded'
 */

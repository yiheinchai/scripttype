/**
 * ScriptType for 04-query-builders-orm/kysely/src/util/type-utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 21 type-level aliases: 19 covered, 1 raw, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const DeleteResult: any
declare const G: any
declare const InsertResult: any
declare const KyselyTypeError: any
declare const MergeResult: any
declare const NotNull: any
declare const NumbersWhenDataTypeNotAvailable: any
declare const StringsWhenDataTypeNotAvailable: any
declare const UpdateResult: any
type DeleteResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type G<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InsertResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type KyselyTypeError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MergeResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NotNull<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NumbersWhenDataTypeNotAvailable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StringsWhenDataTypeNotAvailable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UpdateResult<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ AnyColumn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnyColumn(DB, TB: keyof typeof DB) {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = keyof(DB[T])
  }
  return out[TB] & string
}
/* compiles to:
 * export type AnyColumn<DB, TB extends keyof DB> = { [T in TB]: keyof DB[T] }[TB] & string
 */

// ✓ ExtractColumnType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractColumnType(DB, TB: keyof typeof DB, C) {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = matches<keyof (typeof DB)[typeof T]>(C) ? DB[T][C] : never
  }
  return out[TB]
}
/* compiles to:
 * export type ExtractColumnType<DB, TB extends keyof DB, C> = { [T in TB]: C extends keyof DB[T] ? DB[T][C] : never }[TB]
 */

// ✓ AnyColumnWithTable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnyColumnWithTable(DB, TB: keyof typeof DB) {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = `${merge(T, string)}.${keyof(DB[T]) & string}`
  }
  return out[TB]
}
/* compiles to:
 * export type AnyColumnWithTable<DB, TB extends keyof DB> = { [T in TB]: `${T & string}.${keyof DB[T] & string}` }[TB]
 */

// ✓ AnyAliasedColumn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnyAliasedColumn(DB, TB: keyof typeof DB) {
  return `${AnyColumn(DB, TB)} as ${string}`
}
/* compiles to:
 * export type AnyAliasedColumn<DB, TB extends keyof DB> = `${AnyColumn<DB, TB>} as ${string}`
 */

// ✓ AnyAliasedColumnWithTable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AnyAliasedColumnWithTable(DB, TB: keyof typeof DB) {
  return `${AnyColumnWithTable(DB, TB)} as ${string}`
}
/* compiles to:
 * export type AnyAliasedColumnWithTable<DB, TB extends keyof DB> = `${AnyColumnWithTable<DB, TB>} as ${string}`
 */

// ✓ ArrayItemType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayItemType(T) {
  const m1 = matches<ReadonlyArray<Hole<"I">>>(T)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type ArrayItemType<T> = T extends ReadonlyArray<infer I> ? I : never
 */

// ✓ SimplifySingleResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifySingleResult(O) {
  if (matches<InsertResult | UpdateResult | DeleteResult | MergeResult>(O)) {
    return O
  }
  return anyOf(Simplify(O), Undefined)
}
/* compiles to:
 * export type SimplifySingleResult<O> = O extends InsertResult | UpdateResult | DeleteResult | MergeResult ? O : Simplify<O> | undefined
 */

// ✓ SimplifyResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyResult(O) {
  if (matches<InsertResult | UpdateResult | DeleteResult | MergeResult>(O)) {
    return O
  }
  return Simplify(O)
}
/* compiles to:
 * export type SimplifyResult<O> = O extends InsertResult | UpdateResult | DeleteResult | MergeResult ? O : Simplify<O>
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return DrainOuterGeneric(merge(out, {}))
}
/* compiles to:
 * export type Simplify<T> = DrainOuterGeneric<{ [K in keyof T]: T[K] } & {}>
 */

// ✓ Nullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Nullable(T) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = T[P] | Null
  }
  return out
}
/* compiles to:
 * export type Nullable<T> = { [P in keyof T]: T[P] | null }
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T) {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsNullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNullable(T) {
  if (matches<[ NonNullable<typeof T> ]>([T])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsNullable<T> = [T] extends [NonNullable<T>] ? false : true
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T) {
  if (matches<typeof T & 1>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = 0 extends T & 1 ? true : false
 */

// ✗ Equals: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function Equals(T, U) {
  if (matches<<G>() => G extends typeof U ? 1 : 2>(raw('<G>() => G extends T ? 1 : 2'))) {
    return true
  }
  return false
}

// ✓ NarrowPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NarrowPartial(O, T) {
  if (matches<object>(T)) {
    const out = emptyObject
    for (const K in keySet(keyof(O) & string)) {
      out[K] = matches<keyof typeof T>(K) ? (matches<NotNull>(T[K]) ? Exclude(O[K], Null) : (matches<(typeof O)[typeof K]>(T[K]) ? T[K] : (matches<object>(T[K]) ? SimplifyDeep(O[K] & NarrowPartial(O[K], T[K])) : KyselyTypeError(`$narrowType() call failed: passed type does not exist in '${K}'s type union`)))) : O[K]
    }
    return DrainOuterGeneric(out)
  }
  return never
}
/* compiles to:
 * export type NarrowPartial<O, T> = T extends object ? DrainOuterGeneric<{ [K in keyof O & string]: K extends keyof T ? T[K] extends NotNull ? Exclude<O[K], null> : T[K] extends O[K] ? T[K] : T[K] extends object ? SimplifyDeep<O[K] & NarrowPartial<O[K], T[K]>> : KyselyTypeError<`$narrowType() call failed: passed type does not exist in '${K}'s type union`> : O[K] }> : never
 */

// ✓ SimplifyDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyDeep(T) {
  if (matches<object>(T)) {
    if (matches<Date | RegExp | Map<any, any> | Set<any>>(T)) {
      return T
    }
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = SimplifyDeep(T[K])
    }
    return DrainOuterGeneric(merge(out, {}))
  }
  return T
}
/* compiles to:
 * export type SimplifyDeep<T> = T extends object ? T extends Date | RegExp | Map<any, any> | Set<any> ? T : DrainOuterGeneric<{ [K in keyof T]: SimplifyDeep<T[K]> } & {}> : T
 */

// ✓ DrainOuterGeneric: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DrainOuterGeneric(T) {
  if (matches<[ unknown ]>([T])) {
    return T
  }
  return never
}
/* compiles to:
 * export type DrainOuterGeneric<T> = [T] extends [unknown] ? T : never
 */

// ✓ ShallowRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShallowRecord(K: keyof any, T) {
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P] = T
  }
  return DrainOuterGeneric(out)
}
/* compiles to:
 * export type ShallowRecord<K extends keyof any, T> = DrainOuterGeneric<{ [P in K]: T }>
 */

// ✓ ShallowDehydrateObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShallowDehydrateObject(O) {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = ShallowDehydrateValue(O[K])
  }
  return out
}
/* compiles to:
 * export type ShallowDehydrateObject<O> = { [K in keyof O]: ShallowDehydrateValue<O[K]> }
 */

// ✗ ShallowDehydrateValue: the ScriptType does not itself typecheck as TypeScript
//   ShallowDehydrateValue.st.ts(11:12) TS2362: The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
/* @scripttype preserveParamNames */
export function ShallowDehydrateValue(T) {
  if (matches<null | undefined>(T)) {
    return T
  }
  if (matches<keyof typeof T & {}>('__kysely_dehydrate__')) {
    return T
  }
  const m1 = matches<(Hole<"U">)[]>(merge(T, {}))
  if (m1) {
    return Array(ShallowDehydrateValue(m1.U)) | Extract(T, anyOf(Null, Undefined))
  }
  return Exclude(T, StringsWhenDataTypeNotAvailable | NumbersWhenDataTypeNotAvailable) | (matches<true>(IsNever(Extract(T, NumbersWhenDataTypeNotAvailable))) ? never : number) | (matches<true>(IsNever(Extract(T, StringsWhenDataTypeNotAvailable))) ? never : string)
}
/* compiles to:
 * export type ShallowDehydrateValue<T> = T extends null | undefined ? T : '__kysely_dehydrate__' extends keyof T & {} ? T : T & {} extends (infer U)[] ? Array<ShallowDehydrateValue<U>> | Extract<T, null | undefined> : Exclude<T, StringsWhenDataTypeNotAvailable | NumbersWhenDataTypeNotAvailable> | (IsNever<Extract<T, NumbersWhenDataTypeNotAvailable>> extends true ? never : number) | (IsNever<Extract<T, StringsWhenDataTypeNotAvailable>> extends true ? never : string)
 */

// ✓ AllProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AllProps(T) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = required(unknown)
  }
  return T & out
}
/* compiles to:
 * export type AllProps<T> = T & { [P in keyof T]-?: unknown }
 */

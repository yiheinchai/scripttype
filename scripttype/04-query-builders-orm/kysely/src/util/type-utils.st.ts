/**
 * ScriptType for 04-query-builders-orm/kysely/src/util/type-utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 21 type-level aliases: 21 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { KyselyTypeError } from '../../../../../04-query-builders-orm/kysely/src/util/type-error.js'
import type { NotNull, NumbersWhenDataTypeNotAvailable, StringsWhenDataTypeNotAvailable } from '../../../../../04-query-builders-orm/kysely/src/util/type-utils.js'
declare namespace m1 {
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const DeleteResult: any
declare const G: any
declare const InsertResult: any
declare const KyselyTypeError: any
declare const MergeResult: any
declare const NotNull: any
declare const NumbersWhenDataTypeNotAvailable: any
declare const StringsWhenDataTypeNotAvailable: any
declare const UpdateResult: any
type DeleteResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type G<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InsertResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MergeResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UpdateResult<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ AnyColumn: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function AnyColumn(DB, TB): any {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = keyof(DB[T])
  }
  return out[TB] & string
}
/* compiles to:
 * export type AnyColumn<DB, TB extends keyof DB> = { [T in TB]: keyof DB[T] }[TB] & string
 */

// ✓ ExtractColumnType: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function ExtractColumnType(DB, TB, C): any {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = C in DB[T] ? DB[T][C] : never
  }
  return out[TB]
}
/* compiles to:
 * export type ExtractColumnType<DB, TB extends keyof DB, C> =
 *   { [T in TB]: C extends keyof DB[T] ? DB[T][C] : never }[TB]
 */

// ✓ AnyColumnWithTable: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function AnyColumnWithTable(DB, TB): any {
  const out = emptyObject
  for (const T in keySet(TB)) {
    out[T] = `${merge(T, string)}.${keyof(DB[T]) & string}`
  }
  return out[TB]
}
/* compiles to:
 * export type AnyColumnWithTable<DB, TB extends keyof DB> =
 *   { [T in TB]: `${T & string}.${keyof DB[T] & string}` }[TB]
 */

// ✓ AnyAliasedColumn: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function AnyAliasedColumn(DB, TB): any {
  return `${AnyColumn(DB, TB)} as ${string}`
}
/* compiles to:
 * export type AnyAliasedColumn<DB, TB extends keyof DB> = `${AnyColumn<DB, TB>} as ${string}`
 */

// ✓ AnyAliasedColumnWithTable: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof DB} TB
 */
export function AnyAliasedColumnWithTable(DB, TB): any {
  return `${AnyColumnWithTable(DB, TB)} as ${string}`
}
/* compiles to:
 * export type AnyAliasedColumnWithTable<DB, TB extends keyof DB> = `${AnyColumnWithTable<DB, TB>} as ${string}`
 */

// ✓ ArrayItemType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayItemType(T): any {
  const m1 = matches<ReadonlyArray<Hole<"I">>>(T)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type ArrayItemType<T> = T extends ReadonlyArray<infer I> ? I : never
 */

// ✓ SimplifySingleResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifySingleResult(O): any {
  if (matches<InsertResult | UpdateResult | DeleteResult | MergeResult>(O)) {
    return O
  }
  return anyOf(Simplify(O), Undefined)
}
/* compiles to:
 * export type SimplifySingleResult<O> =
 *   O extends InsertResult | UpdateResult | DeleteResult | MergeResult
 *     ? O
 *     : Simplify<O> | undefined
 */

// ✓ SimplifyResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyResult(O): any {
  if (matches<InsertResult | UpdateResult | DeleteResult | MergeResult>(O)) {
    return O
  }
  return Simplify(O)
}
/* compiles to:
 * export type SimplifyResult<O> =
 *   O extends InsertResult | UpdateResult | DeleteResult | MergeResult ? O : Simplify<O>
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return DrainOuterGeneric(merge(out, {}))
}
/* compiles to:
 * export type Simplify<T> = DrainOuterGeneric<{ [K in keyof T]: T[K] } & {}>
 */

// ✓ Nullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Nullable(T): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = T[P] | Null
  }
  return out
}
/* compiles to:
 * export type Nullable<T> = { [P in keyof T]: T[P] | null }
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsNullable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNullable(T): any {
  if (matches<[ NonNullable<typeof T> ]>([T])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsNullable<T> = [T] extends [NonNullable<T>] ? false : true
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  if (matches<typeof T & 1>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = 0 extends T & 1 ? true : false
 */

// ✓ Equals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equals(T, U): any {
  if (matches<<G>() => G extends typeof U ? 1 : 2>(genericFnType(['G'], [], matches<typeof T>(G) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equals<T, U> =
 *   (<G>() => G extends T ? 1 : 2) extends <G>() => G extends U ? 1 : 2 ? true : false
 */

// ✓ NarrowPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NarrowPartial(O, T): any {
  if (matches<object>(T)) {
    const out = emptyObject
    for (const K in keySet(keyof(O) & string)) {
      out[K] = K in T ? (matches<NotNull>(T[K]) ? Exclude(O[K], Null) : (matches<(typeof O)[typeof K]>(T[K]) ? T[K] : (matches<object>(T[K]) ? SimplifyDeep(merge(O[K], NarrowPartial(O[K], T[K]))) : KyselyTypeError(`$narrowType() call failed: passed type does not exist in '${K}'s type union`)))) : O[K]
    }
    return DrainOuterGeneric(out)
  }
  return never
}
/* compiles to:
 * export type NarrowPartial<O, T> =
 *   T extends object
 *     ? DrainOuterGeneric<
 *       {
 *         [K in keyof O & string]: K extends keyof T
 *           ? T[K] extends NotNull ? Exclude<O[K], null>
 *           : T[K] extends O[K] ? T[K]
 *           : T[K] extends object ? SimplifyDeep<O[K] & NarrowPartial<O[K], T[K]>>
 *           : KyselyTypeError<
 *             `$narrowType() call failed: passed type does not exist in '${K}'s type union`
 *           >
 *           : O[K]
 *       }
 *     >
 *     : never
 */

// ✓ SimplifyDeep: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimplifyDeep(T): any {
  if (matches<object>(T)) {
    if (matches<Date | RegExp | Map<any, any> | Set<any>>(T)) {
      return T
    }
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = SimplifyDeep(T[K])
    }
    return DrainOuterGeneric(merge(out, {}))
  }
  return T
}
/* compiles to:
 * export type SimplifyDeep<T> =
 *   T extends object
 *     ? T extends Date | RegExp | Map<any, any> | Set<any>
 *       ? T
 *       : DrainOuterGeneric<{ [K in keyof T]: SimplifyDeep<T[K]> } & {}>
 *     : T
 */

// ✓ DrainOuterGeneric: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DrainOuterGeneric(T): any {
  if (matches<[ unknown ]>([T])) {
    return T
  }
  return never
}
/* compiles to:
 * export type DrainOuterGeneric<T> = [T] extends [unknown] ? T : never
 */

// ✓ ShallowRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShallowRecord(K: keyof any, T): any {
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P] = T
  }
  return DrainOuterGeneric(out)
}
/* compiles to:
 * export type ShallowRecord<K extends keyof any, T> = DrainOuterGeneric<{ [P in K]: T }>
 */

// ✓ ShallowDehydrateObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShallowDehydrateObject(O): any {
  const out = emptyObject
  for (const K in keyof(O)) {
    out[K] = ShallowDehydrateValue(O[K])
  }
  return out
}
/* compiles to:
 * export type ShallowDehydrateObject<O> = { [K in keyof O]: ShallowDehydrateValue<O[K]> }
 */

// ✓ ShallowDehydrateValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ShallowDehydrateValue(T): any {
  if (matches<null | undefined>(T)) {
    return T
  }
  if (matches<keyof typeof T & {}>('__kysely_dehydrate__')) {
    return T
  }
  const m1 = matches<(Hole<"U">)[]>(merge(T, {}))
  if (m1) {
    return arrayOf(ShallowDehydrateValue(m1.U)) | Extract(T, anyOf(Null, Undefined))
  }
  return Exclude(T, anyOf(StringsWhenDataTypeNotAvailable, NumbersWhenDataTypeNotAvailable)) | (matches<true>(IsNever(Extract(T, NumbersWhenDataTypeNotAvailable))) ? never : number) | (matches<true>(IsNever(Extract(T, StringsWhenDataTypeNotAvailable))) ? never : string)
}
/* compiles to:
 * export type ShallowDehydrateValue<T> =
 *   T extends null | undefined ? T
 *   : '__kysely_dehydrate__' extends keyof T & {} ? T
 *   : T & {} extends (infer U)[] ? ShallowDehydrateValue<U>[] | Extract<T, null | undefined>
 *   : | Exclude<T, StringsWhenDataTypeNotAvailable | NumbersWhenDataTypeNotAvailable>
 *   | (IsNever<Extract<T, NumbersWhenDataTypeNotAvailable>> extends true ? never : number)
 *   | (IsNever<Extract<T, StringsWhenDataTypeNotAvailable>> extends true ? never : string)
 */

// ✓ AllProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AllProps(T): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = required(unknown)
  }
  return T & out
}
/* compiles to:
 * export type AllProps<T> = T & { [P in keyof T]-?: unknown }
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Brand.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 2 compile-error, 1 mismatch, 1 raw, 1 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Brand: any
declare const Constructor: any
declare const Types: any
type Brand<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Brands<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constructor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Types<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ FromConstructor: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function FromConstructor(C) {
  const m1 = matches<Constructor<Hole<"B">>>(C)
  if (m1) {
    return m1.B
  }
  return never
}
/* compiles to:
 * export type FromConstructor<C> = C extends Constructor<infer B> ? B : never
 */

// ✗ Unbranded: does not compile yet
//   Cannot find name 'Brands'. Did you mean 'Brand'?
/* @scripttype preserveParamNames */
export function Unbranded(B: Brand<any>) {
  const m1 = matches<Hole<"U"> & Brands<typeof B>>(B)
  if (m1) {
    return m1.U
  }
  return B
}
/* compiles to:
 * export type Unbranded<B extends Brand<any>> = B extends infer U & Brands<B> ? U : B
 */

// ✗ Keys: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function Keys(B: Brand<any>) {
  return keyof(B[raw('typeof TypeId')])
}

// ✗ Brands: does not compile yet
//   Cannot find name 'Keys'.
/* @scripttype preserveParamNames */
export function Brands(B: Brand<any>) {
  const out = emptyObject
  for (const K in keySet(Keys(B))) {
    out[K] = matches<string>(K) ? Brand(K) : never
  }
  return Types.UnionToIntersection(out[Keys(B)])
}
/* compiles to:
 * export type Brands<B extends Brand<any>> = Types.UnionToIntersection<{ [K in Keys<B>]: K extends string ? Brand<K> : never }[Keys<B>]>
 */

// ✗ EnsureCommonBase: the ScriptType does not itself typecheck as TypeScript
//   EnsureCommonBase.st.ts(5:22) TS2702: 'Brand' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EnsureCommonBase(Brands: readonly [ Constructor<any>, ...Array<Constructor<any>> ]) {
  const out = emptyObject
  for (const B in keyof(Brands)) {
    out[B] = matches<Brand.Unbranded<Brand.FromConstructor<(typeof Brands)[typeof B]>>>(Brand.Unbranded(Brand.FromConstructor(Brands[0]))) ? (matches<Brand.Unbranded<Brand.FromConstructor<(typeof Brands)[0]>>>(Brand.Unbranded(Brand.FromConstructor(Brands[B]))) ? Brands[B] : Brands[B]) : 'ERROR: All brands should have the same base type'
  }
  return out
}
/* compiles to:
 * export type EnsureCommonBase<Brands extends readonly [Constructor<any>, ...Array<Constructor<any>>]> = { [B in keyof Brands]: Brand.Unbranded<Brand.FromConstructor<Brands[0]>> extends Brand.Unbranded<Brand.FromConstructor<Brands[B]>> ? Brand.Unbranded<Brand.FromConstructor<Brands[B]>> extends Brand.Unbranded<Brand.FromConstructor<Brands[0]>> ? Brands[B] : Brands[B] : 'ERROR: All brands should have the same base type' }
 */

// ✓ Branded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Branded(A, Key: string) {
  return A & Brand(Key)
}
/* compiles to:
 * export type Branded<A, Key extends string> = A & Brand<Key>
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Cause.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 4 mismatch, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Cause: any
declare const Die: any
declare const Done: any
declare const Fail: any
declare const Interrupt: any
type Cause<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Die<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Done<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Fail<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Interrupt<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Reason<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Reason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Reason(E) {
  return Fail(E) | Die | Interrupt
}
/* compiles to:
 * export type Reason<E> = Fail<E> | Die | Interrupt
 */

// ✗ Error: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Error(T) {
  const m1 = matches<Cause<Hole<"E">>>(T)
  if (m1) {
    return m1.E
  }
  return never
}
/* compiles to:
 * export type Error<T> = T extends Cause<infer E> ? E : never
 */

// ✗ Error: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Error(T) {
  const m1 = matches<Reason<Hole<"E">>>(T)
  if (m1) {
    return m1.E
  }
  return never
}
/* compiles to:
 * export type Error<T> = T extends Reason<infer E> ? E : never
 */

// ✗ Extract: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Extract(E) {
  const m1 = matches<Done<Hole<"L">>>(E)
  if (m1) {
    return m1.L
  }
  return never
}
/* compiles to:
 * export type Extract<E> = E extends Done<infer L> ? L : never
 */

// ✗ Only: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Only(E) {
  const m1 = matches<Done<Hole<"L">>>(E)
  if (m1) {
    return Done(m1.L)
  }
  return never
}
/* compiles to:
 * export type Only<E> = E extends Done<infer L> ? Done<L> : never
 */

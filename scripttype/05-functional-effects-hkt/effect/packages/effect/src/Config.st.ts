/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Config.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Config: any
type Config<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Success: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Success(T) {
  const m1 = matches<[ Config<Hole<"A">> ]>([T])
  if (m1) {
    return m1.A
  }
  return never
}
/* compiles to:
 * export type Success<T> = [T] extends [Config<infer A>] ? A : never
 */

// ✓ Wrap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Wrap(A) {
  const m1 = matches<[ Hole<"T"> ]>([NonNullable(A)])
  if (m1) {
    if (matches<[ true ]>([IsPlainObject(m1.T)])) {
      const out = emptyObject
      for (const K in keyof(A)) {
        out[K] = readonlyProp(Wrap(A[K]))
      }
      return out | Config(A)
    }
    return Config(A)
  }
  return Config(A)
}
/* compiles to:
 * export type Wrap<A> =
 *   [NonNullable<A>] extends [infer T]
 *     ? [IsPlainObject<T>] extends [true]
 *       ? { readonly [K in keyof A]: Wrap<A[K]> } | Config<A>
 *       : Config<A>
 *     : Config<A>
 */

// ✓ IsPlainObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPlainObject(A) {
  if (matches<[ Record<string, any> ]>([A])) {
    if (matches<[ never ]>([keyof(A)])) {
      return false
    }
    if (matches<[ string ]>([keyof(A)])) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsPlainObject<A> =
 *   [A] extends [Record<string, any>]
 *     ? [keyof A] extends [never] ? false : [keyof A] extends [string] ? true : false
 *     : false
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/ExecutionPlan.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 typecheck-error, 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Context: any
declare const ExecutionPlan: any
declare const Layer: any
declare const Schedule: any
type Context<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ExecutionPlan<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Layer<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schedule<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ StepProvides: the ScriptType does not itself typecheck as TypeScript
//   StepProvides.st.ts(5:44) TS2702: 'Context' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function StepProvides(Steps: ReadonlyArray<any>, Out = unknown) {
  const m3 = matches<readonly [ Hole<"Step">, ...Hole<"Rest"> ]>(Steps)
  if (m3) {
    const m4 = matches<{ readonly provide: Context.Context<Hole<"P">> | Layer.Layer<Hole<"P">, Hole<"_E">, Hole<"_R">>; }>(m3.Step)
    return StepProvides(m3.Rest, Out & (m4 ? m4.P : unknown))
  }
  return Out
}
/* compiles to:
 * export type StepProvides<Steps extends ReadonlyArray<any>, Out = unknown> = Steps extends readonly [infer Step, ...infer Rest] ? StepProvides<Rest, Out & (Step extends { readonly provide: Context.Context<infer P> | Layer.Layer<infer P, infer _E, infer _R>; } ? P : unknown)> : Out
 */

// ✓ PlanProvides: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PlanProvides(Plans: ReadonlyArray<any>, Out = unknown) {
  const m3 = matches<readonly [ Hole<"Plan">, ...Hole<"Rest"> ]>(Plans)
  if (m3) {
    const m4 = matches<ExecutionPlan<Hole<"T">>>(m3.Plan)
    return PlanProvides(m3.Rest, Out & (m4 ? m4.T['provides'] : unknown))
  }
  return Out
}
/* compiles to:
 * export type PlanProvides<Plans extends ReadonlyArray<any>, Out = unknown> = Plans extends readonly [infer Plan, ...infer Rest] ? PlanProvides<Rest, Out & (Plan extends ExecutionPlan<infer T> ? T['provides'] : unknown)> : Out
 */

// ✗ StepInput: the ScriptType does not itself typecheck as TypeScript
//   StepInput.st.ts(6:45) TS2702: 'Schedule' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function StepInput(Steps: ReadonlyArray<any>, Out = unknown) {
  const m4 = matches<readonly [ Hole<"Step">, ...Hole<"Rest"> ]>(Steps)
  if (m4) {
    const m5 = matches<{ readonly while: (input: Hole<"I">) => Hole<"_">; }>(m4.Step)
    const m6 = matches<{ readonly schedule: Schedule.Schedule<Hole<"_O">, Hole<"I">, Hole<"_R">>; }>(m4.Step)
    return StepInput(m4.Rest, merge(Out, (m5 ? m5.I : unknown) & (m6 ? m6.I : unknown)))
  }
  return Out
}
/* compiles to:
 * export type StepInput<Steps extends ReadonlyArray<any>, Out = unknown> = Steps extends readonly [infer Step, ...infer Rest] ? StepInput<Rest, Out & (Step extends { readonly while: (input: infer I) => infer _; } ? I : unknown) & (Step extends { readonly schedule: Schedule.Schedule<infer _O, infer I, infer _R>; } ? I : unknown)> : Out
 */

// ✓ PlanInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PlanInput(Plans: ReadonlyArray<any>, Out = unknown) {
  const m3 = matches<readonly [ Hole<"Plan">, ...Hole<"Rest"> ]>(Plans)
  if (m3) {
    const m4 = matches<ExecutionPlan<Hole<"T">>>(m3.Plan)
    return PlanInput(m3.Rest, Out & (m4 ? m4.T['input'] : unknown))
  }
  return Out
}
/* compiles to:
 * export type PlanInput<Plans extends ReadonlyArray<any>, Out = unknown> = Plans extends readonly [infer Plan, ...infer Rest] ? PlanInput<Rest, Out & (Plan extends ExecutionPlan<infer T> ? T['input'] : unknown)> : Out
 */

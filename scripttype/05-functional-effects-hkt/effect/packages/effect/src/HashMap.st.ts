/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/HashMap.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 covered, 1 mismatch, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const HashMap: any
declare const Option: any
type HashMap<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Option<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ UpdateFn: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function UpdateFn(V) {
  return fnType([Option(V)], Option(V))
}
/* compiles to:
 * export type UpdateFn<V> = (a0: Option<V>) => Option<V>
 */

// ✓ Key: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Key(T: HashMap<any, any>) {
  const m1 = matches<[ HashMap<Hole<"_K">, Hole<"_V">> ]>([T])
  if (m1) {
    return m1._K
  }
  return never
}
/* compiles to:
 * export type Key<T extends HashMap<any, any>> = [T] extends [HashMap<infer _K, unknown>] ? _K : never
 */

// ✓ Value: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Value(T: HashMap<any, any>) {
  const m1 = matches<[ HashMap<Hole<"_K">, Hole<"_V">> ]>([T])
  if (m1) {
    return m1._V
  }
  return never
}
/* compiles to:
 * export type Value<T extends HashMap<any, any>> = [T] extends [HashMap<unknown, infer _V>] ? _V : never
 */

// ✗ Entry: does not compile yet
//   Cannot find name 'Key'.
/* @scripttype preserveParamNames */
export function Entry(T: HashMap<any, any>) {
  return [Key(T), Value(T)]
}
/* compiles to:
 * export type Entry<T extends HashMap<any, any>> = [Key<T>, Value<T>]
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/LayerMap.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 3 typecheck-error, 2 mismatch, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Duration: any
declare const Layer: any
type Duration<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Layer<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ IdleTimeToLiveInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IdleTimeToLiveInput(K) {
  return Duration.Input | fnType([K], Duration.Input)
}
/* compiles to:
 * export type IdleTimeToLiveInput<K> = Duration.Input | ((a0: K) => Duration.Input)
 */

// ✗ Key: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Key(Options) {
  const m1 = matches<{ readonly lookup: (key: Hole<"K">) => any; }>(Options)
  if (m1) {
    return m1.K
  }
  const m2 = matches<{ readonly layers: Hole<"Layers">; }>(Options)
  if (m2) {
    return keyof(m2.Layers)
  }
  return never
}
/* compiles to:
 * export type Key<Options> = Options extends { readonly lookup: (key: infer K) => any; } ? K : Options extends { readonly layers: infer Layers; } ? keyof Layers : never
 */

// ✗ Layers: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Layers(Options) {
  const m1 = matches<{ readonly lookup: (key: Hole<"_K">) => Hole<"Layers">; }>(Options)
  if (m1) {
    return m1.Layers
  }
  const m2 = matches<{ readonly layers: Hole<"Layers">; }>(Options)
  if (m2) {
    return m2.Layers[keyof(m2.Layers)]
  }
  return never
}
/* compiles to:
 * export type Layers<Options> = Options extends { readonly lookup: (key: infer _K) => infer Layers; } ? Layers : Options extends { readonly layers: infer Layers; } ? Layers[keyof Layers] : never
 */

// ✗ Success: the ScriptType does not itself typecheck as TypeScript
//   Success.st.ts(3:22) TS2702: 'Layer' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Success(Options) {
  const m1 = matches<Layer.Layer<Hole<"_A">, Hole<"_E">, Hole<"_R">>>(Layers(Options))
  if (m1) {
    return m1._A
  }
  return never
}
/* compiles to:
 * export type Success<Options> = Layers<Options> extends Layer.Layer<infer _A, unknown, unknown> ? _A : never
 */

// ✗ Error: the ScriptType does not itself typecheck as TypeScript
//   Error.st.ts(3:22) TS2702: 'Layer' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Error(Options) {
  const m1 = matches<Layer.Layer<Hole<"_A">, Hole<"_E">, Hole<"_R">>>(Layers(Options))
  if (m1) {
    return m1._E
  }
  return never
}
/* compiles to:
 * export type Error<Options> = Layers<Options> extends Layer.Layer<unknown, infer _E, unknown> ? _E : never
 */

// ✗ Services: the ScriptType does not itself typecheck as TypeScript
//   Services.st.ts(3:22) TS2702: 'Layer' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Services(Options) {
  const m1 = matches<Layer.Layer<Hole<"_A">, Hole<"_E">, Hole<"_R">>>(Layers(Options))
  if (m1) {
    return m1._R
  }
  return never
}
/* compiles to:
 * export type Services<Options> = Layers<Options> extends Layer.Layer<unknown, unknown, infer _R> ? _R : never
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/ManagedRuntime.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ManagedRuntime: any
type ManagedRuntime<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Services: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Services(T: ManagedRuntime<never, any>) {
  const m1 = matches<[ ManagedRuntime<Hole<"R">, Hole<"_E">> ]>([T])
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type Services<T extends ManagedRuntime<never, any>> = [T] extends [ManagedRuntime<infer R, unknown>] ? R : never
 */

// ✗ Error: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Error(T: ManagedRuntime<never, any>) {
  const m1 = matches<[ ManagedRuntime<Hole<"_R">, Hole<"E">> ]>([T])
  if (m1) {
    return m1.E
  }
  return never
}
/* compiles to:
 * export type Error<T extends ManagedRuntime<never, any>> = [T] extends [ManagedRuntime<unknown, infer E>] ? E : never
 */

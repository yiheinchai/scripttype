/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Match.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 26 type-level aliases: 12 compile-error, 5 covered, 5 typecheck-error, 4 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BuiltInObjects: any
declare const Fail: any
declare const OX: any
declare const Only: any
declare const Predicate: any
declare const Refinement: any
declare const SafeRefinement: any
declare const TypeMatcher: any
declare const ValueMatcher: any
declare const WX: any
declare const Without: any
declare const _R: any
type BuiltInObjects<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Fail<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonFailKeys<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type OX<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Only<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Predicate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PredicateA<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Refinement<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SafeRefinement<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypeMatcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ValueMatcher<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WX<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Without<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type _R<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Matcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Matcher(Input, Filters, RemainingApplied, Result, Provided, Return = any) {
  return TypeMatcher(Input, Filters, RemainingApplied, Result, Return) | ValueMatcher(Input, Filters, RemainingApplied, Result, Provided, Return)
}
/* compiles to:
 * export type Matcher<Input, Filters, RemainingApplied, Result, Provided, Return = any> = TypeMatcher<Input, Filters, RemainingApplied, Result, Return> | ValueMatcher<Input, Filters, RemainingApplied, Result, Provided, Return>
 */

// ✗ WhenMatch: the ScriptType does not itself typecheck as TypeScript
//   WhenMatch.st.ts(10:22) TS2702: 'Predicate' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function WhenMatch(R, P) {
  if (matches<[ 1 & typeof R ]>([0])) {
    return ResolvePred(P)
  }
  const m1 = matches<SafeRefinement<Hole<"SP">, never>>(P)
  if (m1) {
    return m1.SP
  }
  const m2 = matches<Predicate.Refinement<Hole<"_R">, Hole<"RP">>>(P)
  if (m2) {
    const m3 = matches<[ Hole<"X"> ]>([Extract(R, m2.RP)])
    if (m3) {
      if (matches<[ never ]>([m3.X])) {
        return m2.RP
      }
      return m3.X
    }
    return never
  }
  const m4 = matches<PredicateA<Hole<"PP">>>(P)
  if (m4) {
    return m4.PP
  }
  return ExtractMatch(R, P)
}
/* compiles to:
 * export type WhenMatch<R, P> = [0] extends [1 & R] ? ResolvePred<P> : P extends SafeRefinement<infer SP, never> ? SP : P extends Predicate.Refinement<unknown, infer RP> ? [Extract<R, RP>] extends [infer X] ? [X] extends [never] ? RP : X : never : P extends PredicateA<infer PP> ? PP : ExtractMatch<R, P>
 */

// ✗ NotMatch: does not compile yet
//   Cannot find name 'ExtractMatch'.
/* @scripttype preserveParamNames */
export function NotMatch(R, P) {
  return Exclude(R, ExtractMatch(R, PForNotMatch(P)))
}
/* compiles to:
 * export type NotMatch<R, P> = Exclude<R, ExtractMatch<R, PForNotMatch<P>>>
 */

// ✗ PForNotMatch: does not compile yet
//   Cannot find name 'ToInvertedRefinement'.
/* @scripttype preserveParamNames */
export function PForNotMatch(P) {
  const m1 = matches<[ Hole<"X"> ]>([ToInvertedRefinement(P)])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForNotMatch<P> = [ToInvertedRefinement<P>] extends [infer X] ? X : never
 */

// ✗ PForMatch: does not compile yet
//   Cannot find name 'ResolvePred'.
/* @scripttype preserveParamNames */
export function PForMatch(P) {
  const m1 = matches<[ Hole<"X"> ]>([ResolvePred(P)])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForMatch<P> = [ResolvePred<P>] extends [infer X] ? X : never
 */

// ✗ PForExclude: does not compile yet
//   Cannot find name 'SafeRefinementR'. Did you mean 'SafeRefinementR__st7'?
/* @scripttype preserveParamNames */
export function PForExclude(P) {
  const m1 = matches<[ Hole<"X"> ]>([SafeRefinementR(ToSafeRefinement(P))])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForExclude<P> = [SafeRefinementR<ToSafeRefinement<P>>] extends [infer X] ? X : never
 */

// ✗ PredicateA: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function PredicateA(A) {
  return Predicate.Predicate(A) | Predicate.Refinement(A, A)
}
/* compiles to:
 * export type PredicateA<A> = Predicate.Predicate<A> | Predicate.Refinement<A, A>
 */

// ✓ SafeRefinementR: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeRefinementR(A) {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<SafeRefinement<Hole<"_">, Hole<"R">>>(A)
  if (m1) {
    return m1.R
  }
  if (matches<Function>(A)) {
    return A
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = SafeRefinementR(A[K])
    }
    return out
  }
  return A
}
/* compiles to:
 * export type SafeRefinementR<A> = A extends never ? never : A extends SafeRefinement<unknown, infer R> ? R : A extends Function ? A : A extends Record<string, any> ? { [K in keyof A]: SafeRefinementR<A[K]> } : A
 */

// ✗ ResolvePred: the ScriptType does not itself typecheck as TypeScript
//   ResolvePred.st.ts(10:22) TS2702: 'Predicate' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ResolvePred(A, Input = any) {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<SafeRefinement<Hole<"_A">, Hole<"_R">>>(A)
  if (m1) {
    return m1._A
  }
  const m2 = matches<Predicate.Refinement<typeof Input, Hole<"P">>>(A)
  if (m2) {
    return m2.P
  }
  const m3 = matches<Predicate.Predicate<Hole<"P">>>(A)
  if (m3) {
    return m3.P
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ResolvePred(A[K])
    }
    return out
  }
  return A
}
/* compiles to:
 * export type ResolvePred<A, Input = any> = A extends never ? never : A extends SafeRefinement<infer _A, unknown> ? _A : A extends Predicate.Refinement<Input, infer P> ? P : A extends Predicate.Predicate<infer P> ? P : A extends Record<string, any> ? { [K in keyof A]: ResolvePred<A[K]> } : A
 */

// ✗ ToSafeRefinement: the ScriptType does not itself typecheck as TypeScript
//   ToSafeRefinement.st.ts(6:22) TS2702: 'Predicate' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ToSafeRefinement(A) {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<Predicate.Refinement<any, Hole<"P">>>(A)
  if (m1) {
    return SafeRefinement(m1.P, m1.P)
  }
  const m2 = matches<Predicate.Predicate<Hole<"P">>>(A)
  if (m2) {
    return SafeRefinement(m2.P, never)
  }
  if (matches<SafeRefinement<any>>(A)) {
    return A
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ToSafeRefinement(A[K])
    }
    return out
  }
  return NonLiteralsTo(A, never)
}
/* compiles to:
 * export type ToSafeRefinement<A> = A extends never ? never : A extends Predicate.Refinement<any, infer P> ? SafeRefinement<P, P> : A extends Predicate.Predicate<infer P> ? SafeRefinement<P, never> : A extends SafeRefinement<any> ? A : A extends Record<string, any> ? { [K in keyof A]: ToSafeRefinement<A[K]> } : NonLiteralsTo<A, never>
 */

// ✗ ToInvertedRefinement: the ScriptType does not itself typecheck as TypeScript
//   ToInvertedRefinement.st.ts(6:22) TS2702: 'Predicate' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ToInvertedRefinement(A) {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<Predicate.Refinement<any, Hole<"P">>>(A)
  if (m1) {
    return SafeRefinement(m1.P)
  }
  const m2 = matches<Predicate.Predicate<Hole<"_P">>>(A)
  if (m2) {
    return SafeRefinement(never)
  }
  const m3 = matches<SafeRefinement<Hole<"_A">, Hole<"_R">>>(A)
  if (m3) {
    return SafeRefinement(m3._R)
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ToInvertedRefinement(A[K])
    }
    return out
  }
  return NonLiteralsTo(A, never)
}
/* compiles to:
 * export type ToInvertedRefinement<A> = A extends never ? never : A extends Predicate.Refinement<any, infer P> ? SafeRefinement<P> : A extends Predicate.Predicate<unknown> ? SafeRefinement<never> : A extends SafeRefinement<unknown, infer _R> ? SafeRefinement<_R> : A extends Record<string, any> ? { [K in keyof A]: ToInvertedRefinement<A[K]> } : NonLiteralsTo<A, never>
 */

// ✓ NonLiteralsTo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonLiteralsTo(A, T) {
  if (matches<[ string | number | boolean | bigint ]>([A])) {
    if (matches<[ typeof A ]>([string])) {
      return T
    }
    if (matches<[ typeof A ]>([number])) {
      return T
    }
    if (matches<[ typeof A ]>([boolean])) {
      return T
    }
    if (matches<[ typeof A ]>([bigint])) {
      return T
    }
    return A
  }
  return A
}
/* compiles to:
 * export type NonLiteralsTo<A, T> = [A] extends [string | number | boolean | bigint] ? [string] extends [A] ? T : [number] extends [A] ? T : [boolean] extends [A] ? T : [bigint] extends [A] ? T : A : A
 */

// ✗ PatternBase: does not compile yet
//   Cannot find name 'PatternPrimitive'.
/* @scripttype preserveParamNames */
export function PatternBase(A) {
  const m1 = matches<ReadonlyArray<Hole<"_T">>>(A)
  if (m1) {
    return ReadonlyArray(any) | PatternPrimitive(A)
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = PatternPrimitive(merge(A[K], {})) | PatternBase(merge(A[K], {}))
    }
    return Partial(out)
  }
  return never
}
/* compiles to:
 * export type PatternBase<A> = A extends ReadonlyArray<unknown> ? ReadonlyArray<any> | PatternPrimitive<A> : A extends Record<string, any> ? Partial<{ [K in keyof A]: PatternPrimitive<A[K] & {}> | PatternBase<A[K] & {}> }> : never
 */

// ✗ PatternPrimitive: does not compile yet
//   Cannot find name 'PredicateA'.
/* @scripttype preserveParamNames */
export function PatternPrimitive(A) {
  return PredicateA(A) | A | SafeRefinement(any)
}
/* compiles to:
 * export type PatternPrimitive<A> = PredicateA<A> | A | SafeRefinement<any>
 */

// ✗ AddWithout: does not compile yet
//   Cannot find name 'Without'.
/* @scripttype preserveParamNames */
export function AddWithout(A, X) {
  const m1 = matches<[ Without<Hole<"WX">> ]>([A])
  if (m1) {
    return Without(X | m1.WX)
  }
  const m2 = matches<[ Only<Hole<"OX">> ]>([A])
  if (m2) {
    return Only(Exclude(m2.OX, X))
  }
  return never
}
/* compiles to:
 * export type AddWithout<A, X> = [A] extends [Without<infer WX>] ? Without<X | WX> : [A] extends [Only<infer OX>] ? Only<Exclude<OX, X>> : never
 */

// ✗ AddOnly: does not compile yet
//   Cannot find name 'Without'.
/* @scripttype preserveParamNames */
export function AddOnly(A, X) {
  const m1 = matches<[ Without<Hole<"WX">> ]>([A])
  if (m1) {
    if (matches<[ typeof m1.WX ]>([X])) {
      return never
    }
    return Only(X)
  }
  const m2 = matches<[ Only<Hole<"OX">> ]>([A])
  if (m2) {
    if (matches<[ typeof m2.OX ]>([X])) {
      return Only(X)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type AddOnly<A, X> = [A] extends [Without<infer WX>] ? [X] extends [WX] ? never : Only<X> : [A] extends [Only<infer OX>] ? [X] extends [OX] ? Only<X> : never : never
 */

// ✗ ApplyFilters: does not compile yet
//   Cannot find name 'Only'.
/* @scripttype preserveParamNames */
export function ApplyFilters(I, A) {
  const m1 = matches<Only<Hole<"X">>>(A)
  if (m1) {
    return m1.X
  }
  const m2 = matches<Without<Hole<"X">>>(A)
  if (m2) {
    return Exclude(I, m2.X)
  }
  return never
}
/* compiles to:
 * export type ApplyFilters<I, A> = A extends Only<infer X> ? X : A extends Without<infer X> ? Exclude<I, X> : never
 */

// ✗ Tags: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Tags(D: string, P) {
  const m1 = matches<Record<typeof D, Hole<"X">>>(P)
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type Tags<D extends string, P> = P extends Record<D, infer X> ? X : never
 */

// ✓ ArrayToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayToIntersection(A: ReadonlyArray<any>) {
  return T.UnionToIntersection(A[number])
}
/* compiles to:
 * export type ArrayToIntersection<A extends ReadonlyArray<any>> = T.UnionToIntersection<A[number]>
 */

// ✗ ExtractMatch: does not compile yet
//   Cannot find name 'ExtractAndNarrow'.
/* @scripttype preserveParamNames */
export function ExtractMatch(I, P) {
  const m1 = matches<[ Hole<"EI"> ]>([ExtractAndNarrow(I, P)])
  if (m1) {
    return m1.EI
  }
  return never
}
/* compiles to:
 * export type ExtractMatch<I, P> = [ExtractAndNarrow<I, P>] extends [infer EI] ? EI : never
 */

// ✓ Replace: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Replace(A, B) {
  if (matches<Function>(A)) {
    return A
  }
  if (matches<Record<string | number, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = matches<keyof typeof B>(K) ? Replace(A[K], B[K]) : A[K]
    }
    return out
  }
  if (matches<[ typeof A ]>([B])) {
    return B
  }
  return A
}
/* compiles to:
 * export type Replace<A, B> = A extends Function ? A : A extends Record<string | number, any> ? { [K in keyof A]: K extends keyof B ? Replace<A[K], B[K]> : A[K] } : [B] extends [A] ? B : A
 */

// ✗ MaybeReplace: does not compile yet
//   Cannot find name 'Replace'.
/* @scripttype preserveParamNames */
export function MaybeReplace(I, P) {
  if (matches<[ typeof I ]>([P])) {
    return P
  }
  if (matches<[ typeof P ]>([I])) {
    return Replace(I, P)
  }
  return Fail
}
/* compiles to:
 * export type MaybeReplace<I, P> = [P] extends [I] ? P : [I] extends [P] ? Replace<I, P> : Fail
 */

// ✗ IsPlainObject: does not compile yet
//   Cannot find name 'BuiltInObjects'.
/* @scripttype preserveParamNames */
export function IsPlainObject(T) {
  if (matches<BuiltInObjects>(T)) {
    return false
  }
  if (matches<Record<string, any>>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsPlainObject<T> = T extends BuiltInObjects ? false : T extends Record<string, any> ? true : false
 */

// ✗ Simplify: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Simplify(A) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<A> = { [K in keyof A]: A[K] } & {}
 */

// ✗ ExtractAndNarrow: the ScriptType does not itself typecheck as TypeScript
//   ExtractAndNarrow.st.ts(3:22) TS2702: 'Predicate' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ExtractAndNarrow(Input, P) {
  const m1 = matches<Predicate.Refinement<Hole<"_In">, Hole<"_Out">>>(P)
  if (m1) {
    if (matches<typeof Input>(m1._Out)) {
      return Extract(m1._Out, Input)
    }
    return Extract(Input, m1._Out)
  }
  const m2 = matches<SafeRefinement<Hole<"_In">, Hole<"_R">>>(P)
  if (m2) {
    if (matches<[ 1 & typeof m2._R ]>([0])) {
      return Input
    }
    if (matches<typeof Input>(m2._In)) {
      return Extract(m2._In, Input)
    }
    return Extract(Input, m2._In)
  }
  const m3 = matches<Predicate.Predicate<Hole<"_In">>>(P)
  if (m3) {
    return Extract(Input, m3._In)
  }
  const m4 = matches<Hole<"I">>(Input)
  if (m4) {
    const out = emptyObject
    for (const K in keyof(m4.I)) {
      out[K] = readonlyProp(matches<keyof typeof P>(K) ? ExtractAndNarrow(m4.I[K], P[K]) : m4.I[K])
    }
    const m5 = matches<Hole<"R">>(out)
    const out2 = emptyObject
    for (const RK in keySet(Extract(keyof(m4.I), keyof(P)))) {
      out2[RK] = required(ExtractAndNarrow(m4.I[RK], P[RK]))
    }
    const m6 = matches<Hole<"R">>(Simplify(out2 & Omit(m4.I, keyof(P))))
    const m7 = matches<Hole<"R">>(MaybeReplace(m4.I, P))
    return Exclude(matches<ReadonlyArray<any>>(m4.I) ? (matches<ReadonlyArray<any>>(P) ? (m5 ? (matches<(typeof m5.R)[keyof typeof m5.R]>(Fail) ? never : m5.R) : never) : never) : (matches<true>(IsPlainObject(m4.I)) ? (matches<keyof typeof m4.I>(string) ? (matches<typeof P>(m4.I) ? m4.I : never) : (matches<keyof typeof m4.I>(symbol) ? (matches<typeof P>(m4.I) ? m4.I : never) : (m6 ? (matches<NonFailKeys<typeof m6.R>>(keyof(P)) ? m6.R : never) : never))) : (m7 ? (matches<[ typeof m7.R ]>([m4.I]) ? m4.I : m7.R) : never)), Fail)
  }
  return never
}
/* compiles to:
 * export type ExtractAndNarrow<Input, P> = P extends Predicate.Refinement<unknown, infer _Out> ? _Out extends Input ? Extract<_Out, Input> : Extract<Input, _Out> : P extends SafeRefinement<infer _In, infer _R> ? [0] extends [1 & _R] ? Input : _In extends Input ? Extract<_In, Input> : Extract<Input, _In> : P extends Predicate.Predicate<infer _In> ? Extract<Input, _In> : Input extends infer I ? Exclude<I extends ReadonlyArray<any> ? P extends ReadonlyArray<any> ? { readonly [K in keyof I]: K extends keyof P ? ExtractAndNarrow<I[K], P[K]> : I[K] } extends infer R ? Fail extends R[keyof R] ? never : R : never : never : IsPlainObject<I> extends true ? string extends keyof I ? I extends P ? I : never : symbol extends keyof I ? I extends P ? I : never : Simplify<{ [RK in Extract<keyof I, keyof P>]-?: ExtractAndNarrow<I[RK], P[RK]> } & Omit<I, keyof P>> extends infer R ? keyof P extends NonFailKeys<R> ? R : never : never : MaybeReplace<I, P> extends infer R ? [I] extends [R] ? I : R : never, Fail> : never
 */

// ✗ NonFailKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function NonFailKeys(A) {
  const m1 = matches<Hole<"K">>(merge(keyof(A), {}))
  if (m1) {
    if (matches<keyof typeof A>(m1.K)) {
      if (matches<Fail>(A[m1.K])) {
        return never
      }
      return m1.K
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NonFailKeys<A> = keyof A & {} extends infer K ? K extends keyof A ? A[K] extends Fail ? never : K : never : never
 */

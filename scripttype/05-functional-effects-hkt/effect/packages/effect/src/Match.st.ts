/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Match.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 26 type-level aliases: 26 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Fail } from '../../../../../../05-functional-effects-hkt/effect/packages/effect/src/Cause.js'
import type { SafeRefinement, TypeMatcher, ValueMatcher } from '../../../../../../05-functional-effects-hkt/effect/packages/effect/src/Match.js'
import type { Refinement } from '../../../../../../05-functional-effects-hkt/effect/packages/effect/src/Predicate.js'
import type { UnionToIntersection } from '../../../../../../05-functional-effects-hkt/effect/packages/effect/src/Types.js'
declare namespace Predicate {
  export type Predicate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Refinement<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace T {
  export type UnionToIntersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type EI<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type K<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type SP<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type WX<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type X<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _A<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _Out<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type OX<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type RP<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type X<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type X<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _In<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m4 {
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type PP<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m5 {
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m6 {
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m7 {
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BuiltInObjects: any
declare const Fail: any
declare const OX: any
declare const Only: any
declare const Predicate: any
declare const Refinement: any
declare const SafeRefinement: any
declare const TypeMatcher: any
declare const UnionToIntersection: any
declare const ValueMatcher: any
declare const WX: any
declare const Without: any
declare const _R: any
type BuiltInObjects<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NonFailKeys<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OX<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Only<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Predicate<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PredicateA<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type WX<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Without<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type _R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Matcher: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Matcher(Input, Filters, RemainingApplied, Result, Provided, Return = any): any {
  return TypeMatcher(Input, Filters, RemainingApplied, Result, Return) | ValueMatcher(Input, Filters, RemainingApplied, Result, Provided, Return)
}
/* compiles to:
 * export type Matcher<Input, Filters, RemainingApplied, Result, Provided, Return = any> =
 *   | TypeMatcher<Input, Filters, RemainingApplied, Result, Return>
 *   | ValueMatcher<Input, Filters, RemainingApplied, Result, Provided, Return>
 */

// ✓ WhenMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WhenMatch(R, P): any {
  if (matches<[ 1 & typeof R ]>([0])) {
    return ResolvePred(P)
  }
  const m1 = matches<SafeRefinement<Hole<"SP">, never>>(P)
  if (m1) {
    return m1.SP
  }
  const m2 = matches<Predicate.Refinement<Hole<"_R">, Hole<"RP">>>(P)
  if (m2) {
    const m3 = matches<[ Hole<"X"> ]>([Extract(R, m2.RP)])
    if (m3) {
      if (matches<[ never ]>([m3.X])) {
        return m2.RP
      }
      return m3.X
    }
    return never
  }
  const m4 = matches<PredicateA<Hole<"PP">>>(P)
  if (m4) {
    return m4.PP
  }
  return ExtractMatch(R, P)
}
/* compiles to:
 * export type WhenMatch<R, P> =
 *   [0] extends [1 & R] ? ResolvePred<P>
 *   : P extends SafeRefinement<infer SP, never> ? SP
 *   : P extends Predicate.Refinement<any, infer RP>
 *     ? [Extract<R, RP>] extends [infer X] ? [X] extends [never] ? RP : X : never
 *   : P extends PredicateA<infer PP> ? PP
 *   : ExtractMatch<R, P>
 */

// ✓ NotMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotMatch(R, P): any {
  return Exclude(R, ExtractMatch(R, PForNotMatch(P)))
}
/* compiles to:
 * export type NotMatch<R, P> = Exclude<R, ExtractMatch<R, PForNotMatch<P>>>
 */

// ✓ PForNotMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PForNotMatch(P): any {
  const m1 = matches<[ Hole<"X"> ]>([ToInvertedRefinement(P)])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForNotMatch<P> = [ToInvertedRefinement<P>] extends [infer X] ? X : never
 */

// ✓ PForMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PForMatch(P): any {
  const m1 = matches<[ Hole<"X"> ]>([ResolvePred(P)])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForMatch<P> = [ResolvePred<P>] extends [infer X] ? X : never
 */

// ✓ PForExclude: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PForExclude(P): any {
  const m1 = matches<[ Hole<"X"> ]>([SafeRefinementR(ToSafeRefinement(P))])
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type PForExclude<P> =
 *   [SafeRefinementR<ToSafeRefinement<P>>] extends [infer X] ? X : never
 */

// ✓ PredicateA: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PredicateA(A): any {
  return t<Predicate.Predicate<typeof A>>() | t<Predicate.Refinement<typeof A, typeof A>>()
}
/* compiles to:
 * export type PredicateA<A> = Predicate.Predicate<A> | Predicate.Refinement<A, A>
 */

// ✓ SafeRefinementR: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SafeRefinementR(A): any {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<SafeRefinement<Hole<"_">, Hole<"R">>>(A)
  if (m1) {
    return m1.R
  }
  if (matches<Function>(A)) {
    return A
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = SafeRefinementR(A[K])
    }
    return out
  }
  return A
}
/* compiles to:
 * export type SafeRefinementR<A> =
 *   A extends never ? never
 *   : A extends SafeRefinement<any, infer R> ? R
 *   : A extends Function ? A
 *   : A extends Record<string, any> ? { [K in keyof A]: SafeRefinementR<A[K]> }
 *   : A
 */

// ✓ ResolvePred: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolvePred(A, Input = any): any {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<SafeRefinement<Hole<"_A">, Hole<"_R">>>(A)
  if (m1) {
    return m1._A
  }
  const m2 = matches<Predicate.Refinement<typeof Input, Hole<"P">>>(A)
  if (m2) {
    return m2.P
  }
  const m3 = matches<Predicate.Predicate<Hole<"P">>>(A)
  if (m3) {
    return m3.P
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ResolvePred(A[K])
    }
    return out
  }
  return A
}
/* compiles to:
 * export type ResolvePred<A, Input = any> =
 *   A extends never ? never
 *   : A extends SafeRefinement<infer _A, any> ? _A
 *   : A extends Predicate.Refinement<Input, infer P> ? P
 *   : A extends Predicate.Predicate<infer P> ? P
 *   : A extends Record<string, any> ? { [K in keyof A]: ResolvePred<A[K]> }
 *   : A
 */

// ✓ ToSafeRefinement: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToSafeRefinement(A): any {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<Predicate.Refinement<any, Hole<"P">>>(A)
  if (m1) {
    return SafeRefinement(m1.P, m1.P)
  }
  const m2 = matches<Predicate.Predicate<Hole<"P">>>(A)
  if (m2) {
    return SafeRefinement(m2.P, never)
  }
  if (matches<SafeRefinement<any>>(A)) {
    return A
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ToSafeRefinement(A[K])
    }
    return out
  }
  return NonLiteralsTo(A, never)
}
/* compiles to:
 * export type ToSafeRefinement<A> =
 *   A extends never ? never
 *   : A extends Predicate.Refinement<any, infer P> ? SafeRefinement<P, P>
 *   : A extends Predicate.Predicate<infer P> ? SafeRefinement<P, never>
 *   : A extends SafeRefinement<any> ? A
 *   : A extends Record<string, any> ? { [K in keyof A]: ToSafeRefinement<A[K]> }
 *   : NonLiteralsTo<A, never>
 */

// ✓ ToInvertedRefinement: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToInvertedRefinement(A): any {
  if (matches<never>(A)) {
    return never
  }
  const m1 = matches<Predicate.Refinement<any, Hole<"P">>>(A)
  if (m1) {
    return SafeRefinement(m1.P)
  }
  const m2 = matches<Predicate.Predicate<Hole<"_P">>>(A)
  if (m2) {
    return SafeRefinement(never)
  }
  const m3 = matches<SafeRefinement<Hole<"_A">, Hole<"_R">>>(A)
  if (m3) {
    return SafeRefinement(m3._R)
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = ToInvertedRefinement(A[K])
    }
    return out
  }
  return NonLiteralsTo(A, never)
}
/* compiles to:
 * export type ToInvertedRefinement<A> =
 *   A extends never ? never
 *   : A extends Predicate.Refinement<any, infer P> ? SafeRefinement<P>
 *   : A extends Predicate.Predicate<any> ? SafeRefinement<never>
 *   : A extends SafeRefinement<any, infer _R> ? SafeRefinement<_R>
 *   : A extends Record<string, any> ? { [K in keyof A]: ToInvertedRefinement<A[K]> }
 *   : NonLiteralsTo<A, never>
 */

// ✓ NonLiteralsTo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonLiteralsTo(A, T): any {
  if (matches<[ string | number | boolean | bigint ]>([A])) {
    if (matches<[ typeof A ]>([string])) {
      return T
    }
    if (matches<[ typeof A ]>([number])) {
      return T
    }
    if (matches<[ typeof A ]>([boolean])) {
      return T
    }
    if (matches<[ typeof A ]>([bigint])) {
      return T
    }
    return A
  }
  return A
}
/* compiles to:
 * export type NonLiteralsTo<A, T> =
 *   [A] extends [string | number | boolean | bigint]
 *     ? [string] extends [A] ? T
 *     : [number] extends [A] ? T
 *     : [boolean] extends [A] ? T
 *     : [bigint] extends [A] ? T
 *     : A
 *     : A
 */

// ✓ PatternBase: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PatternBase(A): any {
  const m1 = matches<ReadonlyArray<Hole<"_T">>>(A)
  if (m1) {
    return readonlyArrayOf(any) | PatternPrimitive(A)
  }
  if (matches<Record<string, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = PatternPrimitive(merge(A[K], {})) | PatternBase(merge(A[K], {}))
    }
    return Partial(out)
  }
  return never
}
/* compiles to:
 * export type PatternBase<A> =
 *   A extends ReadonlyArray<any> ? readonly any[] | PatternPrimitive<A>
 *   : A extends Record<string, any>
 *     ? Partial<{ [K in keyof A]: PatternPrimitive<A[K] & {}> | PatternBase<A[K] & {}> }>
 *   : never
 */

// ✓ PatternPrimitive: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PatternPrimitive(A): any {
  return PredicateA(A) | A | SafeRefinement(any)
}
/* compiles to:
 * export type PatternPrimitive<A> = PredicateA<A> | A | SafeRefinement<any>
 */

// ✓ AddWithout: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddWithout(A, X): any {
  const m1 = matches<[ Without<Hole<"WX">> ]>([A])
  if (m1) {
    return Without(anyOf(X, m1.WX))
  }
  const m2 = matches<[ Only<Hole<"OX">> ]>([A])
  if (m2) {
    return Only(Exclude(m2.OX, X))
  }
  return never
}
/* compiles to:
 * export type AddWithout<A, X> =
 *   [A] extends [Without<infer WX>] ? Without<X | WX>
 *   : [A] extends [Only<infer OX>] ? Only<Exclude<OX, X>>
 *   : never
 */

// ✓ AddOnly: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddOnly(A, X): any {
  const m1 = matches<[ Without<Hole<"WX">> ]>([A])
  if (m1) {
    if (matches<[ typeof m1.WX ]>([X])) {
      return never
    }
    return Only(X)
  }
  const m2 = matches<[ Only<Hole<"OX">> ]>([A])
  if (m2) {
    if (matches<[ typeof m2.OX ]>([X])) {
      return Only(X)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type AddOnly<A, X> =
 *   [A] extends [Without<infer WX>] ? [X] extends [WX] ? never : Only<X>
 *   : [A] extends [Only<infer OX>] ? [X] extends [OX] ? Only<X> : never
 *   : never
 */

// ✓ ApplyFilters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyFilters(I, A): any {
  const m1 = matches<Only<Hole<"X">>>(A)
  if (m1) {
    return m1.X
  }
  const m2 = matches<Without<Hole<"X">>>(A)
  if (m2) {
    return Exclude(I, m2.X)
  }
  return never
}
/* compiles to:
 * export type ApplyFilters<I, A> =
 *   A extends Only<infer X> ? X : A extends Without<infer X> ? Exclude<I, X> : never
 */

// ✓ Tags: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tags(D: string, P): any {
  const m1 = matches<Record<typeof D, Hole<"X">>>(P)
  if (m1) {
    return m1.X
  }
  return never
}
/* compiles to:
 * export type Tags<D extends string, P> = P extends Record<D, infer X> ? X : never
 */

// ✓ ArrayToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArrayToIntersection(A: ReadonlyArray<any>): any {
  return t<T.UnionToIntersection<(typeof A)[number]>>()
}
/* compiles to:
 * export type ArrayToIntersection<A extends ReadonlyArray<any>> = T.UnionToIntersection<A[number]>
 */

// ✓ ExtractMatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractMatch(I, P): any {
  const m1 = matches<[ Hole<"EI"> ]>([ExtractAndNarrow(I, P)])
  if (m1) {
    return m1.EI
  }
  return never
}
/* compiles to:
 * export type ExtractMatch<I, P> = [ExtractAndNarrow<I, P>] extends [infer EI] ? EI : never
 */

// ✓ Replace: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Replace(A, B): any {
  if (matches<Function>(A)) {
    return A
  }
  if (matches<Record<string | number, any>>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = K in B ? Replace(A[K], B[K]) : A[K]
    }
    return out
  }
  if (matches<[ typeof A ]>([B])) {
    return B
  }
  return A
}
/* compiles to:
 * export type Replace<A, B> =
 *   A extends Function ? A
 *   : A extends Record<string | number, any>
 *     ? { [K in keyof A]: K extends keyof B ? Replace<A[K], B[K]> : A[K] }
 *   : [B] extends [A] ? B
 *   : A
 */

// ✓ MaybeReplace: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeReplace(I, P): any {
  if (matches<[ typeof I ]>([P])) {
    return P
  }
  if (matches<[ typeof P ]>([I])) {
    return Replace(I, P)
  }
  return Fail
}
/* compiles to:
 * export type MaybeReplace<I, P> = [P] extends [I] ? P : [I] extends [P] ? Replace<I, P> : Fail
 */

// ✓ IsPlainObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPlainObject(T): any {
  if (matches<BuiltInObjects>(T)) {
    return false
  }
  if (matches<Record<string, any>>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsPlainObject<T> =
 *   T extends BuiltInObjects ? false : T extends Record<string, any> ? true : false
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(A): any {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<A> = { [K in keyof A]: A[K] } & {}
 */

// ✓ ExtractAndNarrow: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractAndNarrow(Input, P): any {
  const m1 = matches<Predicate.Refinement<Hole<"_In">, Hole<"_Out">>>(P)
  if (m1) {
    if (matches<typeof Input>(m1._Out)) {
      return Extract(m1._Out, Input)
    }
    return Extract(Input, m1._Out)
  }
  const m2 = matches<SafeRefinement<Hole<"_In">, Hole<"_R">>>(P)
  if (m2) {
    if (matches<[ 1 & typeof m2._R ]>([0])) {
      return Input
    }
    if (matches<typeof Input>(m2._In)) {
      return Extract(m2._In, Input)
    }
    return Extract(Input, m2._In)
  }
  const m3 = matches<Predicate.Predicate<Hole<"_In">>>(P)
  if (m3) {
    return Extract(Input, m3._In)
  }
  const m4 = matches<Hole<"I">>(Input)
  if (m4) {
    const out = emptyObject
    for (const K in keyof(m4.I)) {
      out[K] = readonlyProp(K in P ? ExtractAndNarrow(m4.I[K], P[K]) : m4.I[K])
    }
    const m5 = matches<Hole<"R">>(out)
    const out2 = emptyObject
    for (const RK in keySet(Extract(keyof(m4.I), keyof(P)))) {
      out2[RK] = required(ExtractAndNarrow(m4.I[RK], P[RK]))
    }
    const m6 = matches<Hole<"R">>(Simplify(merge(out2, Omit(m4.I, keyof(P)))))
    const m7 = matches<Hole<"R">>(MaybeReplace(m4.I, P))
    return Exclude(matches<ReadonlyArray<any>>(m4.I) ? (matches<ReadonlyArray<any>>(P) ? (m5 ? (matches<(typeof m5.R)[keyof typeof m5.R]>(Fail) ? never : m5.R) : never) : never) : (matches<true>(IsPlainObject(m4.I)) ? (string in m4.I ? (matches<typeof P>(m4.I) ? m4.I : never) : (symbol in m4.I ? (matches<typeof P>(m4.I) ? m4.I : never) : (m6 ? (matches<NonFailKeys<typeof m6.R>>(keyof(P)) ? m6.R : never) : never))) : (m7 ? (matches<[ typeof m7.R ]>([m4.I]) ? m4.I : m7.R) : never)), Fail)
  }
  return never
}
/* compiles to:
 * export type ExtractAndNarrow<Input, P> =
 *   P extends Predicate.Refinement<any, infer _Out>
 *     ? _Out extends Input ? Extract<_Out, Input> : Extract<Input, _Out>
 *   : P extends SafeRefinement<infer _In, infer _R>
 *     ? [0] extends [1 & _R] ? Input
 *     : _In extends Input ? Extract<_In, Input>
 *     : Extract<Input, _In>
 *   : P extends Predicate.Predicate<infer _In> ? Extract<Input, _In>
 *   : Input extends infer I
 *     ? Exclude<
 *       I extends ReadonlyArray<any>
 *         ? P extends ReadonlyArray<any>
 *           ? { readonly [K in keyof I]: K extends keyof P ? ExtractAndNarrow<I[K], P[K]> : I[K] } extends infer R
 *             ? Fail extends R[keyof R] ? never : R
 *             : never
 *           : never
 *       : IsPlainObject<I> extends true
 *         ? string extends keyof I ? I extends P ? I : never
 *         : symbol extends keyof I ? I extends P ? I : never
 *         : Simplify<{ [RK in Extract<keyof I, keyof P>]-?: ExtractAndNarrow<I[RK], P[RK]> } & Omit<I, keyof P>> extends infer R
 *           ? keyof P extends NonFailKeys<R> ? R : never
 *         : never
 *       : MaybeReplace<I, P> extends infer R ? [I] extends [R] ? I : R
 *       : never,
 *       Fail
 *     >
 *   : never
 */

// ✓ NonFailKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonFailKeys(A): any {
  const m1 = matches<Hole<"K">>(merge(keyof(A), {}))
  if (m1) {
    if (m1.K in A) {
      if (matches<Fail>(A[m1.K])) {
        return never
      }
      return m1.K
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NonFailKeys<A> =
 *   keyof A & {} extends infer K
 *     ? K extends keyof A ? A[K] extends Fail ? never : K : never
 *     : never
 */

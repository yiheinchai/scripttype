/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Metric.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Metric: any
type Metric<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Input: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Input(A) {
  const m1 = matches<Metric<Hole<"_Input">, Hole<"_State">>>(A)
  if (m1) {
    return m1._Input
  }
  return never
}
/* compiles to:
 * export type Input<A> = A extends Metric<infer _Input, unknown> ? _Input : never
 */

// ✗ State: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function State(A) {
  const m1 = matches<Metric<Hole<"_Input">, Hole<"_State">>>(A)
  if (m1) {
    return m1._State
  }
  return never
}
/* compiles to:
 * export type State<A> = A extends Metric<unknown, infer _State> ? _State : never
 */

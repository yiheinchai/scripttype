/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Queue.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 typecheck-error, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Effect: any
declare const Failure: any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Failure<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ State: the ScriptType does not itself typecheck as TypeScript
//   State.st.ts(3:67) TS2348: Value of type 'SetConstructor' is not callable. Did you mean to include 'new'?
/* @scripttype preserveParamNames */
export function State(A, E) {
  return anyOf({ _tag: readonlyProp('Open'), takers: readonlyProp(Set(fnType([Effect(voidType(), E)], voidType()))), offers: readonlyProp(Set(OfferEntry(A))), awaiters: readonlyProp(Set(fnType([Effect(voidType(), E)], voidType()))) }, { _tag: readonlyProp('Closing'), takers: readonlyProp(Set(fnType([Effect(voidType(), E)], voidType()))), offers: readonlyProp(Set(OfferEntry(A))), awaiters: readonlyProp(Set(fnType([Effect(voidType(), E)], voidType()))), exit: readonlyProp(Failure(never, E)) }, { _tag: readonlyProp('Done'), exit: readonlyProp(Failure(never, E)) })
}
/* compiles to:
 * export type State<A, E> = { readonly _tag: 'Open'; readonly takers: Set<(a0: Effect<void, E>) => void>; readonly offers: Set<OfferEntry<A>>; readonly awaiters: Set<(a0: Effect<void, E>) => void> } | { readonly _tag: 'Closing'; readonly takers: Set<(a0: Effect<void, E>) => void>; readonly offers: Set<OfferEntry<A>>; readonly awaiters: Set<(a0: Effect<void, E>) => void>; readonly exit: Failure<never, E> } | { readonly _tag: 'Done'; readonly exit: Failure<never, E> }
 */

// ✗ OfferEntry: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function OfferEntry(A) {
  return anyOf({ _tag: readonlyProp('Array'), remaining: readonlyProp(Array(A)), offset: number, resume: readonlyProp(fnType([Effect(Array(A))], voidType())) }, { _tag: readonlyProp('Single'), message: readonlyProp(A), resume: readonlyProp(fnType([Effect(boolean)], voidType())) })
}
/* compiles to:
 * export type OfferEntry<A> = { readonly _tag: 'Array'; readonly remaining: Array<A>; offset: number; readonly resume: (a0: Effect<Array<A>>) => void } | { readonly _tag: 'Single'; readonly message: A; readonly resume: (a0: Effect<boolean>) => void }
 */

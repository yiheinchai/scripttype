/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Record.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 compile-error, 1 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Head: any
type Head<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ReadonlyRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyRecord(K: string | symbol, A) {
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P] = readonlyProp(A)
  }
  return out
}
/* compiles to:
 * export type ReadonlyRecord<K extends string | symbol, A> = { readonly [P in K]: A }
 */

// ✗ IsFiniteString: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function IsFiniteString(T: string) {
  if (matches<"">(T)) {
    return true
  }
  const m1 = matches<[ `${Hole<"Head">}${Hole<"Rest">}` ]>([T])
  if (m1) {
    if (matches<typeof m1.Head>(string)) {
      return false
    }
    if (matches<typeof m1.Head>(`${number}`)) {
      return false
    }
    if (matches<"">(m1.Rest)) {
      return true
    }
    return IsFiniteString(m1.Rest)
  }
  return false
}
/* compiles to:
 * export type IsFiniteString<T extends string> = T extends '' ? true : [T] extends [`${infer Head}${infer Rest}`] ? string extends Head ? false : `${number}` extends Head ? false : Rest extends '' ? true : IsFiniteString<Rest> : false
 */

// ✗ NonLiteralKey: does not compile yet
//   Cannot find name 'IsFiniteString'.
/* @scripttype preserveParamNames */
export function NonLiteralKey(K: string | symbol) {
  if (matches<string>(K)) {
    if (matches<true>(IsFiniteString(K))) {
      return string
    }
    return K
  }
  return symbol
}
/* compiles to:
 * export type NonLiteralKey<K extends string | symbol> = K extends string ? IsFiniteString<K> extends true ? string : K : symbol
 */

// ✗ IntersectKeys: does not compile yet
//   Cannot find name 'NonLiteralKey'.
/* @scripttype preserveParamNames */
export function IntersectKeys(K1: string, K2: string) {
  if (matches<[ typeof K1 | typeof K2 ]>([string])) {
    return NonLiteralKey(K1) & NonLiteralKey(K2)
  }
  return merge(K1, K2)
}
/* compiles to:
 * export type IntersectKeys<K1 extends string, K2 extends string> = [string] extends [K1 | K2] ? NonLiteralKey<K1> & NonLiteralKey<K2> : K1 & K2
 */

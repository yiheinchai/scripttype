/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Schedule.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 4 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Schedule: any
type Schedule<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Output: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Output(S) {
  const m1 = matches<Schedule<Hole<"Output">, any, any, any>>(S)
  if (m1) {
    return m1.Output
  }
  return never
}
/* compiles to:
 * export type Output<S> = S extends Schedule<infer Output, any, any, any> ? Output : never
 */

// ✓ Input: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Input(S) {
  const m1 = matches<Schedule<any, Hole<"Input">, any, any>>(S)
  if (m1) {
    return m1.Input
  }
  return never
}
/* compiles to:
 * export type Input<S> = S extends Schedule<any, infer Input, any, any> ? Input : never
 */

// ✓ Error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Error(S) {
  const m1 = matches<Schedule<any, any, Hole<"Error">, any>>(S)
  if (m1) {
    return m1.Error
  }
  return never
}
/* compiles to:
 * export type Error<S> = S extends Schedule<any, any, infer Error, any> ? Error : never
 */

// ✓ Env: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Env(S) {
  const m1 = matches<Schedule<any, any, any, Hole<"Env">>>(S)
  if (m1) {
    return m1.Env
  }
  return never
}
/* compiles to:
 * export type Env<S> = S extends Schedule<any, any, any, infer Env> ? Env : never
 */

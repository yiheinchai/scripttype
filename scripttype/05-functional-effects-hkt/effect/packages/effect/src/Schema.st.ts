/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Schema.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 82 type-level aliases: 45 compile-error, 15 covered, 11 typecheck-error, 6 raw, 5 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Brand: any
declare const Codec: any
declare const Constraint: any
declare const ConstraintDecoder: any
declare const Derivation: any
declare const FastCheck: any
declare const LiteralPart: any
declare const LiteralValue: any
declare const Objects: any
declare const PropertyKey: any
declare const SchemaAST: any
declare const Simplify: any
declare const Struct: any
declare const StructWithRest: any
declare const TemplateLiteral: any
declare const TreeRecord: any
declare const TupleWithRest: any
declare const Union: any
declare const UnionToIntersection: any
declare const tag: any
type Brand<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Codec<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConstraintDecoder<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Derivation<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type FastCheck<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LiteralPart<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type LiteralValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Objects<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SchemaAST<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Simplify<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Struct<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StructWithRest<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TemplateLiteral<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TreeRecord<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TupleWithRest<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Union<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnionToIntersection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type tag<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(S) {
  const m1 = matches<{ readonly "Type": Hole<"T">; }>(S)
  if (m1) {
    return m1.T
  }
  return never
}
/* compiles to:
 * export type Type<S> = S extends { readonly "Type": infer T; } ? T : never
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(S) {
  const m1 = matches<{ readonly "Encoded": Hole<"E">; }>(S)
  if (m1) {
    return m1.E
  }
  return never
}
/* compiles to:
 * export type Encoded<S> = S extends { readonly "Encoded": infer E; } ? E : never
 */

// ✗ DecodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function DecodingServices(S) {
  const m1 = matches<{ readonly "DecodingServices": Hole<"R">; }>(S)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type DecodingServices<S> = S extends { readonly "DecodingServices": infer R; } ? R : never
 */

// ✗ EncodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function EncodingServices(S) {
  const m1 = matches<{ readonly "EncodingServices": Hole<"R">; }>(S)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type EncodingServices<S> = S extends { readonly "EncodingServices": infer R; } ? R : never
 */

// ✗ AppendType: does not compile yet
//   Cannot find name 'LiteralPart'.
/* @scripttype preserveParamNames */
export function AppendType(Template: string, Next) {
  if (matches<LiteralPart>(Next)) {
    return `${Template}${Next}`
  }
  const m1 = matches<{ readonly Encoded: Hole<"E", LiteralPart>; }>(Next)
  if (m1) {
    return `${Template}${m1.E}`
  }
  return never
}
/* compiles to:
 * export type AppendType<Template extends string, Next> = Next extends LiteralPart ? `${Template}${Next}` : Next extends { readonly Encoded: infer E extends LiteralPart; } ? `${Template}${E}` : never
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(Parts) {
  const m1 = matches<readonly [ ...Hole<"Init">, Hole<"Last"> ]>(Parts)
  if (m1) {
    return AppendType(Encoded(m1.Init), m1.Last)
  }
  return raw('``')
}
/* compiles to:
 * export type Encoded<Parts> = Parts extends readonly [...infer Init, infer Last] ? AppendType<Encoded<Init>, Last> : ``
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(Parts) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(Parts)
  if (m1) {
    const m2 = matches<ConstraintDecoder<Hole<"T">, unknown>>(m1.Head)
    return asReadonly([matches<TemplateLiteral.LiteralPart>(m1.Head) ? m1.Head : (m2 ? m2.T : never), ...Type(m1.Tail)])
  }
  return []
}
/* compiles to:
 * export type Type<Parts> = Parts extends readonly [infer Head, ...infer Tail] ? readonly [Head extends TemplateLiteral.LiteralPart ? Head : Head extends ConstraintDecoder<infer T, unknown> ? T : never, ...Type<Tail>] : []
 */

// ✗ TypeOptionalKeys: the ScriptType does not itself typecheck as TypeScript
//   TypeOptionalKeys.st.ts(2:42) TS2702: 'Struct' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TypeOptionalKeys(Fields: Struct.Fields) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[K] = matches<{ readonly "~type.optionality": "optional"; }>(Fields[K]) ? K : never
  }
  return out[keyof(Fields)]
}
/* compiles to:
 * export type TypeOptionalKeys<Fields extends Struct.Fields> = { [K in keyof Fields]: Fields[K] extends { readonly "~type.optionality": "optional"; } ? K : never }[keyof Fields]
 */

// ✗ TypeMutableKeys: the ScriptType does not itself typecheck as TypeScript
//   TypeMutableKeys.st.ts(2:41) TS2702: 'Struct' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TypeMutableKeys(Fields: Struct.Fields) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[K] = matches<{ readonly "~type.mutability": "mutable"; }>(Fields[K]) ? K : never
  }
  return out[keyof(Fields)]
}
/* compiles to:
 * export type TypeMutableKeys<Fields extends Struct.Fields> = { [K in keyof Fields]: Fields[K] extends { readonly "~type.mutability": "mutable"; } ? K : never }[keyof Fields]
 */

// ✗ SetOptional: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function SetOptional(A, K: keyof typeof A) {
  return Omit(A, K) & Partial(Pick(A, K))
}
/* compiles to:
 * export type SetOptional<A, K extends keyof A> = Omit<A, K> & Partial<Pick<A, K>>
 */

// ✗ Mutable: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Mutable(A) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = mutable(A[K])
  }
  return out
}
/* compiles to:
 * export type Mutable<A> = { -readonly [K in keyof A]: A[K] }
 */

// ✗ SetMutable: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function SetMutable(A, K: keyof typeof A) {
  return Omit(A, K) & Mutable(Pick(A, K))
}
/* compiles to:
 * export type SetMutable<A, K extends keyof A> = Omit<A, K> & Mutable<Pick<A, K>>
 */

// ✗ EncodedOptionalKeys: the ScriptType does not itself typecheck as TypeScript
//   EncodedOptionalKeys.st.ts(2:45) TS2702: 'Struct' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EncodedOptionalKeys(Fields: Struct.Fields) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[K] = matches<{ readonly "~encoded.optionality": "optional"; }>(Fields[K]) ? K : never
  }
  return out[keyof(Fields)]
}
/* compiles to:
 * export type EncodedOptionalKeys<Fields extends Struct.Fields> = { [K in keyof Fields]: Fields[K] extends { readonly "~encoded.optionality": "optional"; } ? K : never }[keyof Fields]
 */

// ✗ EncodedMutableKeys: the ScriptType does not itself typecheck as TypeScript
//   EncodedMutableKeys.st.ts(2:44) TS2702: 'Struct' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EncodedMutableKeys(Fields: Struct.Fields) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[K] = matches<{ readonly "~encoded.mutability": "mutable"; }>(Fields[K]) ? K : never
  }
  return out[keyof(Fields)]
}
/* compiles to:
 * export type EncodedMutableKeys<Fields extends Struct.Fields> = { [K in keyof Fields]: Fields[K] extends { readonly "~encoded.mutability": "mutable"; } ? K : never }[keyof Fields]
 */

// ✗ SideOptionalKeys: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function SideOptionalKeys(F: Fields, S: Side) {
  if (matches<"Encoded">(S)) {
    return EncodedOptionalKeys(F)
  }
  return TypeOptionalKeys(F)
}
/* compiles to:
 * export type SideOptionalKeys<F extends Fields, S extends Side> = S extends 'Encoded' ? EncodedOptionalKeys<F> : TypeOptionalKeys<F>
 */

// ✗ SideMutableKeys: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function SideMutableKeys(F: Fields, S: Side) {
  if (matches<"Encoded">(S)) {
    return EncodedMutableKeys(F)
  }
  return TypeMutableKeys(F)
}
/* compiles to:
 * export type SideMutableKeys<F extends Fields, S extends Side> = S extends 'Encoded' ? EncodedMutableKeys<F> : TypeMutableKeys<F>
 */

// ✗ ReadonlySide: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function ReadonlySide(F: Fields, S: Side) {
  const out = emptyObject
  for (const K in keyof(F)) {
    out[K] = readonlyProp(F[K][S])
  }
  return out
}
/* compiles to:
 * export type ReadonlySide<F extends Fields, S extends Side> = { readonly [K in keyof F]: F[K][S] }
 */

// ✗ View: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function View(F: Fields, S: Side, O: keyof typeof F = SideOptionalKeys(F, S), M: keyof typeof F = SideMutableKeys(F, S)) {
  if (matches<[ never ]>([anyOf(O, M)])) {
    return Simplify(ReadonlySide(F, S))
  }
  if (matches<[ never ]>([M])) {
    return Simplify(SetOptional(ReadonlySide(F, S), O))
  }
  if (matches<[ never ]>([O])) {
    return Simplify(SetMutable(ReadonlySide(F, S), M))
  }
  return Simplify(SetMutable(SetOptional(ReadonlySide(F, S), O), Extract(keyof(SetOptional(ReadonlySide(F, S), O)), M)))
}
/* compiles to:
 * export type View<F extends Fields, S extends Side, O extends keyof F = SideOptionalKeys<F, S>, M extends keyof F = SideMutableKeys<F, S>> = [O | M] extends [never] ? Simplify<ReadonlySide<F, S>> : [M] extends [never] ? Simplify<SetOptional<ReadonlySide<F, S>, O>> : [O] extends [never] ? Simplify<SetMutable<ReadonlySide<F, S>, M>> : Simplify<SetMutable<SetOptional<ReadonlySide<F, S>, O>, Extract<keyof SetOptional<ReadonlySide<F, S>, O>, M>>>
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(F: Fields) {
  return View(F, 'Type')
}
/* compiles to:
 * export type Type<F extends Fields> = View<F, 'Type'>
 */

// ✗ Iso: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function Iso(F: Fields) {
  return View(F, 'Iso')
}
/* compiles to:
 * export type Iso<F extends Fields> = View<F, 'Iso'>
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(F: Fields) {
  return View(F, 'Encoded')
}
/* compiles to:
 * export type Encoded<F extends Fields> = View<F, 'Encoded'>
 */

// ✗ DecodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function DecodingServices(F: Fields) {
  const out = emptyObject
  for (const K in keyof(F)) {
    out[K] = readonlyProp(F[K]['DecodingServices'])
  }
  return out[keyof(F)]
}
/* compiles to:
 * export type DecodingServices<F extends Fields> = { readonly [K in keyof F]: F[K]['DecodingServices'] }[keyof F]
 */

// ✗ EncodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function EncodingServices(F: Fields) {
  const out = emptyObject
  for (const K in keyof(F)) {
    out[K] = readonlyProp(F[K]['EncodingServices'])
  }
  return out[keyof(F)]
}
/* compiles to:
 * export type EncodingServices<F extends Fields> = { readonly [K in keyof F]: F[K]['EncodingServices'] }[keyof F]
 */

// ✗ TypeConstructorDefaultedKeys: the ScriptType does not itself typecheck as TypeScript
//   TypeConstructorDefaultedKeys.st.ts(2:54) TS2702: 'Struct' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TypeConstructorDefaultedKeys(Fields: Struct.Fields) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[K] = matches<{ readonly "~type.constructor.default": "with-default"; }>(Fields[K]) ? K : never
  }
  return out[keyof(Fields)]
}
/* compiles to:
 * export type TypeConstructorDefaultedKeys<Fields extends Struct.Fields> = { [K in keyof Fields]: Fields[K] extends { readonly "~type.constructor.default": "with-default"; } ? K : never }[keyof Fields]
 */

// ✗ ReadonlyMakeIn: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function ReadonlyMakeIn(F: Fields) {
  const out = emptyObject
  for (const K in keyof(F)) {
    out[K] = readonlyProp(F[K]['~type.make'])
  }
  return out
}
/* compiles to:
 * export type ReadonlyMakeIn<F extends Fields> = { readonly [K in keyof F]: F[K]['~type.make'] }
 */

// ✗ MakeInView: does not compile yet
//   Cannot find name 'Fields'.
/* @scripttype preserveParamNames */
export function MakeInView(F: Fields, O: keyof typeof F = TypeOptionalKeys(F) | TypeConstructorDefaultedKeys(F)) {
  if (matches<[ never ]>([O])) {
    return ReadonlyMakeIn(F)
  }
  return Simplify(SetOptional(ReadonlyMakeIn(F), O))
}
/* compiles to:
 * export type MakeInView<F extends Fields, O extends keyof F = TypeOptionalKeys<F> | TypeConstructorDefaultedKeys<F>> = [O] extends [never] ? ReadonlyMakeIn<F> : Simplify<SetOptional<ReadonlyMakeIn<F>, O>>
 */

// ✗ MakeIn: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function MakeIn(F: Fields) {
  return MakeInView(F)
}
/* compiles to:
 * export type MakeIn<F extends Fields> = MakeInView<F>
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(Key: Record.Key, Value: Constraint) {
  if (matches<{ readonly "~type.optionality": "optional"; }>(Value)) {
    if (matches<{ readonly "~type.mutability": "mutable"; }>(Value)) {
      const out = emptyObject
      for (const P in keySet(Key['Type'])) {
        out[P] = optional(Value['Type'])
      }
      return out
    }
    const out = emptyObject
    for (const P in keySet(Key['Type'])) {
      out[P] = readonlyProp(optional(Value['Type']))
    }
    return out
  }
  if (matches<{ readonly "~type.mutability": "mutable"; }>(Value)) {
    const out = emptyObject
    for (const P in keySet(Key['Type'])) {
      out[P] = Value['Type']
    }
    return out
  }
  const out = emptyObject
  for (const P in keySet(Key['Type'])) {
    out[P] = readonlyProp(Value['Type'])
  }
  return out
}
/* compiles to:
 * export type Type<Key extends Record.Key, Value extends Constraint> = Value extends { readonly "~type.optionality": "optional"; } ? Value extends { readonly "~type.mutability": "mutable"; } ? { [P in Key['Type']]?: Value['Type'] } : { readonly [P1 in Key['Type']]?: Value['Type'] } : Value extends { readonly "~type.mutability": "mutable"; } ? { [P2 in Key['Type']]: Value['Type'] } : { readonly [P3 in Key['Type']]: Value['Type'] }
 */

// ✗ Iso: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function Iso(Key: Record.Key, Value: Constraint) {
  if (matches<{ readonly "~type.optionality": "optional"; }>(Value)) {
    if (matches<{ readonly "~type.mutability": "mutable"; }>(Value)) {
      const out = emptyObject
      for (const P in keySet(Key['Iso'])) {
        out[P] = optional(Value['Iso'])
      }
      return out
    }
    const out = emptyObject
    for (const P in keySet(Key['Iso'])) {
      out[P] = readonlyProp(optional(Value['Iso']))
    }
    return out
  }
  if (matches<{ readonly "~type.mutability": "mutable"; }>(Value)) {
    const out = emptyObject
    for (const P in keySet(Key['Iso'])) {
      out[P] = Value['Iso']
    }
    return out
  }
  const out = emptyObject
  for (const P in keySet(Key['Iso'])) {
    out[P] = readonlyProp(Value['Iso'])
  }
  return out
}
/* compiles to:
 * export type Iso<Key extends Record.Key, Value extends Constraint> = Value extends { readonly "~type.optionality": "optional"; } ? Value extends { readonly "~type.mutability": "mutable"; } ? { [P in Key['Iso']]?: Value['Iso'] } : { readonly [P1 in Key['Iso']]?: Value['Iso'] } : Value extends { readonly "~type.mutability": "mutable"; } ? { [P2 in Key['Iso']]: Value['Iso'] } : { readonly [P3 in Key['Iso']]: Value['Iso'] }
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(Key: Record.Key, Value: Constraint) {
  if (matches<{ readonly "~encoded.optionality": "optional"; }>(Value)) {
    if (matches<{ readonly "~encoded.mutability": "mutable"; }>(Value)) {
      const out = emptyObject
      for (const P in keySet(Key['Encoded'])) {
        out[P] = optional(Value['Encoded'])
      }
      return out
    }
    const out = emptyObject
    for (const P in keySet(Key['Encoded'])) {
      out[P] = readonlyProp(optional(Value['Encoded']))
    }
    return out
  }
  if (matches<{ readonly "~encoded.mutability": "mutable"; }>(Value)) {
    const out = emptyObject
    for (const P in keySet(Key['Encoded'])) {
      out[P] = Value['Encoded']
    }
    return out
  }
  const out = emptyObject
  for (const P in keySet(Key['Encoded'])) {
    out[P] = readonlyProp(Value['Encoded'])
  }
  return out
}
/* compiles to:
 * export type Encoded<Key extends Record.Key, Value extends Constraint> = Value extends { readonly "~encoded.optionality": "optional"; } ? Value extends { readonly "~encoded.mutability": "mutable"; } ? { [P in Key['Encoded']]?: Value['Encoded'] } : { readonly [P1 in Key['Encoded']]?: Value['Encoded'] } : Value extends { readonly "~encoded.mutability": "mutable"; } ? { [P2 in Key['Encoded']]: Value['Encoded'] } : { readonly [P3 in Key['Encoded']]: Value['Encoded'] }
 */

// ✗ DecodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function DecodingServices(Key: Record.Key, Value: Constraint) {
  return Key['DecodingServices'] | Value['DecodingServices']
}
/* compiles to:
 * export type DecodingServices<Key extends Record.Key, Value extends Constraint> = Key['DecodingServices'] | Value['DecodingServices']
 */

// ✗ EncodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function EncodingServices(Key: Record.Key, Value: Constraint) {
  return Key['EncodingServices'] | Value['EncodingServices']
}
/* compiles to:
 * export type EncodingServices<Key extends Record.Key, Value extends Constraint> = Key['EncodingServices'] | Value['EncodingServices']
 */

// ✗ MakeIn: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function MakeIn(Key: Record.Key, Value: Constraint) {
  if (matches<{ readonly "~encoded.optionality": "optional"; }>(Value)) {
    if (matches<{ readonly "~encoded.mutability": "mutable"; }>(Value)) {
      const out = emptyObject
      for (const P in keySet(Key['~type.make'])) {
        out[P] = optional(Value['~type.make'])
      }
      return out
    }
    const out = emptyObject
    for (const P in keySet(Key['~type.make'])) {
      out[P] = readonlyProp(optional(Value['~type.make']))
    }
    return out
  }
  if (matches<{ readonly "~encoded.mutability": "mutable"; }>(Value)) {
    const out = emptyObject
    for (const P in keySet(Key['~type.make'])) {
      out[P] = Value['~type.make']
    }
    return out
  }
  const out = emptyObject
  for (const P in keySet(Key['~type.make'])) {
    out[P] = readonlyProp(Value['~type.make'])
  }
  return out
}
/* compiles to:
 * export type MakeIn<Key extends Record.Key, Value extends Constraint> = Value extends { readonly "~encoded.optionality": "optional"; } ? Value extends { readonly "~encoded.mutability": "mutable"; } ? { [P in Key['~type.make']]?: Value['~type.make'] } : { readonly [P1 in Key['~type.make']]?: Value['~type.make'] } : Value extends { readonly "~encoded.mutability": "mutable"; } ? { [P2 in Key['~type.make']]: Value['~type.make'] } : { readonly [P3 in Key['~type.make']]: Value['~type.make'] }
 */

// ✗ MergeTuple: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function MergeTuple(T: ReadonlyArray<unknown>) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(T)
  if (m1) {
    return m1.Head & MergeTuple(m1.Tail)
  }
  return {}
}
/* compiles to:
 * export type MergeTuple<T extends ReadonlyArray<unknown>> = T extends readonly [infer Head, ...infer Tail] ? Head & MergeTuple<Tail> : {}
 */

// ✗ Intersect: the ScriptType does not itself typecheck as TypeScript
//   Intersect.st.ts(2:48) TS2702: 'StructWithRest' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Intersect(S: Objects, Records: StructWithRest.Records, Side: "Type" | "Iso" | "Encoded" | "~type.make") {
  const out = emptyObject
  for (const K in keyof(Records)) {
    out[K] = readonlyProp(Records[K][Side])
  }
  return S[Side] & MergeTuple(out)
}
/* compiles to:
 * export type Intersect<S extends Objects, Records extends StructWithRest.Records, Side extends 'Type' | 'Iso' | 'Encoded' | '~type.make'> = S[Side] & MergeTuple<{ readonly [K in keyof Records]: Records[K][Side] }>
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(S: Objects, Records: StructWithRest.Records) {
  return Intersect(S, Records, 'Type')
}
/* compiles to:
 * export type Type<S extends Objects, Records extends StructWithRest.Records> = Intersect<S, Records, 'Type'>
 */

// ✗ Iso: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function Iso(S: Objects, Records: StructWithRest.Records) {
  return Intersect(S, Records, 'Iso')
}
/* compiles to:
 * export type Iso<S extends Objects, Records extends StructWithRest.Records> = Intersect<S, Records, 'Iso'>
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(S: Objects, Records: StructWithRest.Records) {
  return Intersect(S, Records, 'Encoded')
}
/* compiles to:
 * export type Encoded<S extends Objects, Records extends StructWithRest.Records> = Intersect<S, Records, 'Encoded'>
 */

// ✗ MakeIn: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function MakeIn(S: Objects, Records: StructWithRest.Records) {
  return Intersect(S, Records, '~type.make')
}
/* compiles to:
 * export type MakeIn<S extends Objects, Records extends StructWithRest.Records> = Intersect<S, Records, '~type.make'>
 */

// ✗ Services: the ScriptType does not itself typecheck as TypeScript
//   Services.st.ts(2:47) TS2702: 'StructWithRest' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Services(S: Objects, Records: StructWithRest.Records, Side: "DecodingServices" | "EncodingServices") {
  const out = emptyObject
  for (const K in keyof(Records)) {
    out[K] = Records[K][Side]
  }
  return S[Side] | out[number]
}
/* compiles to:
 * export type Services<S extends Objects, Records extends StructWithRest.Records, Side extends 'DecodingServices' | 'EncodingServices'> = S[Side] | { [K in keyof Records]: Records[K][Side] }[number]
 */

// ✗ DecodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function DecodingServices(S: Objects, Records: StructWithRest.Records) {
  return Services(S, Records, 'DecodingServices')
}
/* compiles to:
 * export type DecodingServices<S extends Objects, Records extends StructWithRest.Records> = Services<S, Records, 'DecodingServices'>
 */

// ✗ EncodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function EncodingServices(S: Objects, Records: StructWithRest.Records) {
  return Services(S, Records, 'EncodingServices')
}
/* compiles to:
 * export type EncodingServices<S extends Objects, Records extends StructWithRest.Records> = Services<S, Records, 'EncodingServices'>
 */

// ✗ IncompatibleKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function IncompatibleKeys(A, B, OK: (keyof typeof A & keyof typeof B) = Extract(keyof(A), keyof(B))) {
  const out = emptyObject
  for (const K in keySet(OK)) {
    out[K] = matches<(typeof B)[typeof K]>(Required(Pick(A, K))[K]) ? never : K
  }
  return out[OK]
}
/* compiles to:
 * export type IncompatibleKeys<A, B, OK extends keyof A & keyof B = Extract<keyof A, keyof B>> = { [K in OK]: Required<Pick<A, K>>[K] extends B[K] ? never : K }[OK]
 */

// ✗ IncompatibleSideKeys: the ScriptType does not itself typecheck as TypeScript
//   IncompatibleSideKeys.st.ts(2:59) TS2702: 'StructWithRest' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IncompatibleSideKeys(S: Objects, Records: StructWithRest.Records, Side: "Type" | "Encoded" | "Iso" | "~type.make") {
  const out = emptyObject
  for (const I in keyof(Records)) {
    out[I] = matches<object>(Records[I][Side]) ? IncompatibleKeys(S[Side], Records[I][Side]) : never
  }
  return out[number]
}
/* compiles to:
 * export type IncompatibleSideKeys<S extends Objects, Records extends StructWithRest.Records, Side extends 'Type' | 'Encoded' | 'Iso' | '~type.make'> = { [I in keyof Records]: Records[I][Side] extends object ? IncompatibleKeys<S[Side], Records[I][Side]> : never }[number]
 */

// ✗ IncompatibleRecords: the ScriptType does not itself typecheck as TypeScript
//   IncompatibleRecords.st.ts(2:58) TS2702: 'StructWithRest' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IncompatibleRecords(S: Objects, Records: StructWithRest.Records) {
  return IncompatibleSideKeys(S, Records, 'Type') | IncompatibleSideKeys(S, Records, 'Encoded') | IncompatibleSideKeys(S, Records, 'Iso') | IncompatibleSideKeys(S, Records, '~type.make')
}
/* compiles to:
 * export type IncompatibleRecords<S extends Objects, Records extends StructWithRest.Records> = IncompatibleSideKeys<S, Records, 'Type'> | IncompatibleSideKeys<S, Records, 'Encoded'> | IncompatibleSideKeys<S, Records, 'Iso'> | IncompatibleSideKeys<S, Records, '~type.make'>
 */

// ✗ ValidateRecords: the ScriptType does not itself typecheck as TypeScript
//   ValidateRecords.st.ts(2:54) TS2702: 'StructWithRest' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ValidateRecords(S: Objects, Records: StructWithRest.Records) {
  if (matches<[ never ]>([IncompatibleRecords(S, Records)])) {
    return true
  }
  return { "incompatible index signatures": IncompatibleRecords(S, Records) }
}
/* compiles to:
 * export type ValidateRecords<S extends Objects, Records extends StructWithRest.Records> = [IncompatibleRecords<S, Records>] extends [never] ? true : { 'incompatible index signatures': IncompatibleRecords<S, Records> }
 */

// ✗ Type_: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function Type_(Elements, Out: ReadonlyArray<any> = asReadonly([])) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(Elements)
  if (m1) {
    const m2 = matches<{ readonly "Type": Hole<"T">; }>(m1.Head)
    if (m2) {
      if (matches<{ readonly "~type.optionality": "optional"; }>(m1.Head)) {
        return Type_(m1.Tail, asReadonly([...Out, raw('T?')]))
      }
      return Type_(m1.Tail, asReadonly([...Out, m2.T]))
    }
    return Out
  }
  return Out
}

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(E: Elements) {
  return Type_(E)
}
/* compiles to:
 * export type Type<E extends Elements> = Type_<E>
 */

// ✗ Iso_: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function Iso_(Elements, Out: ReadonlyArray<any> = asReadonly([])) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(Elements)
  if (m1) {
    const m2 = matches<{ readonly "Iso": Hole<"T">; }>(m1.Head)
    if (m2) {
      if (matches<{ readonly "~type.optionality": "optional"; }>(m1.Head)) {
        return Iso_(m1.Tail, asReadonly([...Out, raw('T?')]))
      }
      return Iso_(m1.Tail, asReadonly([...Out, m2.T]))
    }
    return Out
  }
  return Out
}

// ✗ Iso: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function Iso(E: Elements) {
  return Iso_(E)
}
/* compiles to:
 * export type Iso<E extends Elements> = Iso_<E>
 */

// ✗ Encoded_: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function Encoded_(Elements, Out: ReadonlyArray<any> = asReadonly([])) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(Elements)
  if (m1) {
    const m2 = matches<{ readonly "Encoded": Hole<"T">; }>(m1.Head)
    if (m2) {
      if (matches<{ readonly "~encoded.optionality": "optional"; }>(m1.Head)) {
        return Encoded_(m1.Tail, asReadonly([...Out, raw('T?')]))
      }
      return Encoded_(m1.Tail, asReadonly([...Out, m2.T]))
    }
    return Out
  }
  return Out
}

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(E: Elements) {
  return Encoded_(E)
}
/* compiles to:
 * export type Encoded<E extends Elements> = Encoded_<E>
 */

// ✗ DecodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function DecodingServices(E: Elements) {
  return E[number]['DecodingServices']
}
/* compiles to:
 * export type DecodingServices<E extends Elements> = E[number]['DecodingServices']
 */

// ✗ EncodingServices: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function EncodingServices(E: Elements) {
  return E[number]['EncodingServices']
}
/* compiles to:
 * export type EncodingServices<E extends Elements> = E[number]['EncodingServices']
 */

// ✗ MakeIn_: uses raw() — language gap, does not count as covered
//   gap: optional tuple element
/* @scripttype preserveParamNames */
export function MakeIn_(E, Out: ReadonlyArray<any> = asReadonly([])) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(E)
  if (m1) {
    const m2 = matches<{ "~type.make": Hole<"T">; }>(m1.Head)
    if (m2) {
      if (matches<{ readonly "~type.optionality": "optional"; } | { readonly "~type.constructor.default": "with-default"; }>(m1.Head)) {
        return MakeIn_(m1.Tail, asReadonly([...Out, raw('T?')]))
      }
      return MakeIn_(m1.Tail, asReadonly([...Out, m2.T]))
    }
    return Out
  }
  return Out
}

// ✗ MakeIn: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function MakeIn(E: Elements) {
  return MakeIn_(E)
}
/* compiles to:
 * export type MakeIn<E extends Elements> = MakeIn_<E>
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(T: ReadonlyArray<unknown>, Rest: TupleWithRest.Rest) {
  const m1 = matches<readonly [ Hole<"Head", Constraint>, ...Hole<"Tail", ReadonlyArray<Constraint>> ]>(Rest)
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.Tail)) {
      out[K] = readonlyProp(m1.Tail[K]['Type'])
    }
    return Readonly([...T, ...Array(m1.Head['Type']), ...out])
  }
  return T
}
/* compiles to:
 * export type Type<T extends ReadonlyArray<unknown>, Rest extends TupleWithRest.Rest> = Rest extends readonly [infer Head extends Constraint, ...(infer Tail extends ReadonlyArray<Constraint>)] ? Readonly<[...T, ...Array<Head['Type']>, ...{ readonly [K in keyof Tail]: Tail[K]['Type'] }]> : T
 */

// ✗ Iso: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function Iso(T: ReadonlyArray<unknown>, Rest: TupleWithRest.Rest) {
  const m1 = matches<readonly [ Hole<"Head", Constraint>, ...Hole<"Tail", ReadonlyArray<Constraint>> ]>(Rest)
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.Tail)) {
      out[K] = readonlyProp(m1.Tail[K]['Iso'])
    }
    return Readonly([...T, ...Array(m1.Head['Iso']), ...out])
  }
  return T
}
/* compiles to:
 * export type Iso<T extends ReadonlyArray<unknown>, Rest extends TupleWithRest.Rest> = Rest extends readonly [infer Head extends Constraint, ...(infer Tail extends ReadonlyArray<Constraint>)] ? Readonly<[...T, ...Array<Head['Iso']>, ...{ readonly [K in keyof Tail]: Tail[K]['Iso'] }]> : T
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(E: ReadonlyArray<unknown>, Rest: TupleWithRest.Rest) {
  const m1 = matches<readonly [ Hole<"Head", Constraint>, ...Hole<"Tail", ReadonlyArray<Constraint>> ]>(Rest)
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.Tail)) {
      out[K] = readonlyProp(m1.Tail[K]['Encoded'])
    }
    return asReadonly([...E, ...Array(m1.Head['Encoded']), ...out])
  }
  return E
}
/* compiles to:
 * export type Encoded<E extends ReadonlyArray<unknown>, Rest extends TupleWithRest.Rest> = Rest extends readonly [infer Head extends Constraint, ...(infer Tail extends ReadonlyArray<Constraint>)] ? readonly [...E, ...Array<Head['Encoded']>, ...{ readonly [K in keyof Tail]: Tail[K]['Encoded'] }] : E
 */

// ✗ MakeIn: does not compile yet
//   Cannot find name 'Elements'.
/* @scripttype preserveParamNames */
export function MakeIn(M: ReadonlyArray<unknown>, Rest: TupleWithRest.Rest) {
  const m1 = matches<readonly [ Hole<"Head", Constraint>, ...Hole<"Tail", ReadonlyArray<Constraint>> ]>(Rest)
  if (m1) {
    const out = emptyObject
    for (const K in keyof(m1.Tail)) {
      out[K] = readonlyProp(m1.Tail[K]['~type.make'])
    }
    return asReadonly([...M, ...Array(m1.Head['~type.make']), ...out])
  }
  return M
}
/* compiles to:
 * export type MakeIn<M extends ReadonlyArray<unknown>, Rest extends TupleWithRest.Rest> = Rest extends readonly [infer Head extends Constraint, ...(infer Tail extends ReadonlyArray<Constraint>)] ? readonly [...M, ...Array<Head['~type.make']>, ...{ readonly [K in keyof Tail]: Tail[K]['~type.make'] }] : M
 */

// ✓ DistributeBrands: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DistributeBrands(B) {
  const m1 = matches<Hole<"U", string>>(B)
  return UnionToIntersection(m1 ? Brand.Brand(m1.U) : never)
}
/* compiles to:
 * export type DistributeBrands<B> = UnionToIntersection<B extends (infer U extends string) ? Brand.Brand<U> : never>
 */

// ✗ TaggedStruct: the ScriptType does not itself typecheck as TypeScript
//   TaggedStruct.st.ts(2:35) TS2702: 'SchemaAST' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TaggedStruct(Tag: SchemaAST.LiteralValue, Fields: Struct.Fields) {
  return Struct(Simplify(merge({ _tag: readonlyProp(tag(Tag)) }, Fields)))
}
/* compiles to:
 * export type TaggedStruct<Tag extends SchemaAST.LiteralValue, Fields extends Struct.Fields> = Struct<Simplify<{ readonly _tag: tag<Tag> } & Fields>>
 */

// ✓ Flatten: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Flatten(Schemas) {
  const m1 = matches<readonly [ Hole<"Head">, ...Hole<"Tail"> ]>(Schemas)
  if (m1) {
    const m2 = matches<Union<Hole<"Inner">>>(m1.Head)
    if (m2) {
      return [...Flatten(m2.Inner), ...Flatten(m1.Tail)]
    }
    return [m1.Head, ...Flatten(m1.Tail)]
  }
  return []
}
/* compiles to:
 * export type Flatten<Schemas> = Schemas extends readonly [infer Head, ...infer Tail] ? Head extends Union<infer Inner> ? [...Flatten<Inner>, ...Flatten<Tail>] : [Head, ...Flatten<Tail>] : []
 */

// ✗ TaggedUnionUtils: uses raw() — language gap, does not count as covered
//   gap: generic function type; type node FirstTypeNode; object member CallSignature
/* @scripttype preserveParamNames */
export function TaggedUnionUtils(Tag: PropertyKey, Members: ReadonlyArray<Constraint & { readonly Type: { readonly [K in typeof Tag]: PropertyKey; }; }>, Flattened: ReadonlyArray<Constraint & { readonly Type: { readonly [K in typeof Tag]: PropertyKey; }; }> = Flatten(Members)) {
  const out = emptyObject
  for (const I in keyof(Flattened)) {
    out[I] = readonlyProp(Flattened[I]['Type'][Tag])
  }
  const out2 = emptyObject
  for (const M in keySet(Flattened[number])) {
    out2[M['Type'][Tag]] = M
  }
  const out3 = emptyObject
  for (const M in keySet(Flattened[number])) {
    out3[M['Type'][Tag]] = fnType([unknown], raw('u is M["Type"]'))
  }
  return { discriminants: readonlyProp(out), cases: readonlyProp(Simplify(out2)), isAnyOf: readonlyProp(raw('<const Keys>(keys: ReadonlyArray<Keys>) => (value: Members[number]["Type"]) => value is Extract<Members[number]["Type"], { readonly [K in Tag]: Keys; }>')), guards: readonlyProp(out3), match: readonlyProp(raw('{ <Cases extends { [M in Flattened[number] as M["Type"][Tag]]: (value: M["Type"]) => any; }>(value: Members[number]["Type"], cases: Cases): Cases[keyof Cases] extends (value: any) => infer R ? Unify<R> : never; <Cases extends { [M in Flattened[number] as M["Type"][Tag]]: (value: M["Type"]) => any; }>(cases: Cases): (value: Members[number]["Type"]) => Cases[keyof Cases] extends (value: any) => infer R ? Unify<R> : never; }')) }
}

// ✗ toTaggedUnion: does not compile yet
//   'Tag' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function toTaggedUnion(Tag: PropertyKey, Members: ReadonlyArray<Constraint & { readonly Type: { readonly [K in typeof Tag]: PropertyKey; }; }>) {
  return Union(Members) & TaggedUnionUtils(Tag, Members)
}
/* compiles to:
 * export type toTaggedUnion<Tag extends PropertyKey, Members extends ReadonlyArray<Constraint & { readonly Type: { readonly [K in typeof Tag]: PropertyKey; }; }>> = Union<Members> & TaggedUnionUtils<Tag, Members>
 */

// ✓ OptionIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionIso(A: Constraint) {
  return anyOf({ _tag: readonlyProp('None') }, { _tag: readonlyProp('Some'), value: readonlyProp(A['Iso']) })
}
/* compiles to:
 * export type OptionIso<A extends Constraint> = { readonly _tag: 'None' } | { readonly _tag: 'Some'; readonly value: A['Iso'] }
 */

// ✓ ResultIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResultIso(A: Constraint, E: Constraint) {
  return anyOf({ _tag: readonlyProp('Success'), success: readonlyProp(A['Iso']) }, { _tag: readonlyProp('Failure'), failure: readonlyProp(E['Iso']) })
}
/* compiles to:
 * export type ResultIso<A extends Constraint, E extends Constraint> = { readonly _tag: 'Success'; readonly success: A['Iso'] } | { readonly _tag: 'Failure'; readonly failure: E['Iso'] }
 */

// ✓ CauseReasonIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CauseReasonIso(E: Constraint, D: Constraint) {
  return anyOf({ _tag: readonlyProp('Fail'), error: readonlyProp(E['Iso']) }, { _tag: readonlyProp('Die'), error: readonlyProp(D['Iso']) }, { _tag: readonlyProp('Interrupt'), fiberId: readonlyProp(anyOf(number, Undefined)) })
}
/* compiles to:
 * export type CauseReasonIso<E extends Constraint, D extends Constraint> = { readonly _tag: 'Fail'; readonly error: E['Iso'] } | { readonly _tag: 'Die'; readonly error: D['Iso'] } | { readonly _tag: 'Interrupt'; readonly fiberId: number | undefined }
 */

// ✓ CauseIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CauseIso(E: Constraint, D: Constraint) {
  return ReadonlyArray(CauseReasonIso(E, D))
}
/* compiles to:
 * export type CauseIso<E extends Constraint, D extends Constraint> = ReadonlyArray<CauseReasonIso<E, D>>
 */

// ✓ ExitIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExitIso(A: Constraint, E: Constraint, D: Constraint) {
  return anyOf({ _tag: readonlyProp('Success'), value: readonlyProp(A['Iso']) }, { _tag: readonlyProp('Failure'), cause: readonlyProp(CauseIso(E, D)) })
}
/* compiles to:
 * export type ExitIso<A extends Constraint, E extends Constraint, D extends Constraint> = { readonly _tag: 'Success'; readonly value: A['Iso'] } | { readonly _tag: 'Failure'; readonly cause: CauseIso<E, D> }
 */

// ✓ ReadonlyMapIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyMapIso(Key: Constraint, Value: Constraint) {
  return ReadonlyArray(asReadonly([Key['Iso'], Value['Iso']]))
}
/* compiles to:
 * export type ReadonlyMapIso<Key extends Constraint, Value extends Constraint> = ReadonlyArray<readonly [Key['Iso'], Value['Iso']]>
 */

// ✓ HashMapIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HashMapIso(Key: Constraint, Value: Constraint) {
  return ReadonlyArray(asReadonly([Key['Iso'], Value['Iso']]))
}
/* compiles to:
 * export type HashMapIso<Key extends Constraint, Value extends Constraint> = ReadonlyArray<readonly [Key['Iso'], Value['Iso']]>
 */

// ✓ ReadonlySetIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlySetIso(Value: Constraint) {
  return ReadonlyArray(Value['Iso'])
}
/* compiles to:
 * export type ReadonlySetIso<Value extends Constraint> = ReadonlyArray<Value['Iso']>
 */

// ✓ HashSetIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HashSetIso(Value: Constraint) {
  return ReadonlyArray(Value['Iso'])
}
/* compiles to:
 * export type HashSetIso<Value extends Constraint> = ReadonlyArray<Value['Iso']>
 */

// ✓ ChunkIso: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ChunkIso(Value: Constraint) {
  return ReadonlyArray(Value['Iso'])
}
/* compiles to:
 * export type ChunkIso<Value extends Constraint> = ReadonlyArray<Value['Iso']>
 */

// ✓ InheritStaticMembers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InheritStaticMembers(C, Static) {
  return C & Pick(Static, Exclude(keyof(Static), keyof(C)))
}
/* compiles to:
 * export type InheritStaticMembers<C, Static> = C & Pick<Static, Exclude<keyof Static, keyof C>>
 */

// ✓ MissingSelfGeneric: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MissingSelfGeneric(Usage: string) {
  return `Missing \`Self\` generic - use \`class Self extends ${Usage}<Self>(...)\``
}
/* compiles to:
 * export type MissingSelfGeneric<Usage extends string> = `Missing \`Self\` generic - use \`class Self extends ${Usage}<Self>(...)\``
 */

// ✗ LazyArbitrary: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function LazyArbitrary(T) {
  return fnType([raw('typeof FastCheck')], FastCheck.Arbitrary(T))
}

// ✓ Tree: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tree(Node) {
  return Node | TreeRecord(Node) | ReadonlyArray(Tree(Node))
}
/* compiles to:
 * export type Tree<Node> = Node | TreeRecord<Node> | ReadonlyArray<Tree<Node>>
 */

// ✗ Type: does not compile yet
//   Type '"Type__st79"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Type(TypeParameters: ReadonlyArray<Constraint>) {
  const out = emptyObject
  for (const K in keyof(TypeParameters)) {
    out[K] = readonlyProp(Codec(TypeParameters[K]['Type']))
  }
  return out
}
/* compiles to:
 * export type Type<TypeParameters extends ReadonlyArray<Constraint>> = { readonly [K in keyof TypeParameters]: Codec<TypeParameters[K]['Type']> }
 */

// ✗ Encoded: does not compile yet
//   Type '"Encoded__st80"' cannot be used to index type 'TypeParameters[K]'.
/* @scripttype preserveParamNames */
export function Encoded(TypeParameters: ReadonlyArray<Constraint>) {
  const out = emptyObject
  for (const K in keyof(TypeParameters)) {
    out[K] = readonlyProp(Codec(TypeParameters[K]['Encoded']))
  }
  return out
}
/* compiles to:
 * export type Encoded<TypeParameters extends ReadonlyArray<Constraint>> = { readonly [K in keyof TypeParameters]: Codec<TypeParameters[K]['Encoded']> }
 */

// ✗ Output: does not compile yet
//   Namespace '"/Users/yihein.chai/Documents/learn/ScriptType/05-functional-effects-hkt/effect/packages/effect/src/testing/FastCheck"' has no exported member 'Arbit
/* @scripttype preserveParamNames */
export function Output(T) {
  return FastCheck.Arbitrary(T) | Derivation(T)
}
/* compiles to:
 * export type Output<T> = FastCheck.Arbitrary<T> | Derivation<T>
 */

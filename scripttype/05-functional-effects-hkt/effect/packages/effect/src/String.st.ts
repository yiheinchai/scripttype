/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/String.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 2 covered, 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Concat: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Concat(A: string, B: string) {
  return `${A}${B}`
}
/* compiles to:
 * export type Concat<A extends string, B extends string> = `${A}${B}`
 */

// ✓ Trim: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Trim(A: string) {
  return TrimEnd(TrimStart(A))
}
/* compiles to:
 * export type Trim<A extends string> = TrimEnd<TrimStart<A>>
 */

// ✗ TrimStart: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function TrimStart(A: string) {
  let a = A
  while (true) {
    const m1 = matches<`${" " | "\n" | "\t" | "\r"}${Hole<"B">}`>(a)
    if (!m1) {
      break
    }
    a = m1.B
  }
  return a
}
/* compiles to:
 * export type TrimStart<A extends string> = TrimStart__loop<A>
 * type TrimStart__loop<A extends string> = A extends `${' ' | '
 * ' | '	' | ''}${infer B}` ? TrimStart__loop<B> : A
 */

// ✗ TrimEnd: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function TrimEnd(A: string) {
  let a = A
  while (true) {
    const m1 = matches<`${Hole<"B">}${" " | "\n" | "\t" | "\r"}`>(a)
    if (!m1) {
      break
    }
    a = m1.B
  }
  return a
}
/* compiles to:
 * export type TrimEnd<A extends string> = TrimEnd__loop<A>
 * type TrimEnd__loop<A extends string> = A extends `${infer B}${' ' | '
 * ' | '	' | ''}` ? TrimEnd__loop<B> : A
 */

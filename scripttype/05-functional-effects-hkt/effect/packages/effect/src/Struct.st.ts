/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Struct.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 7 covered, 2 typecheck-error, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Lambda } from '../../../../../../05-functional-effects-hkt/effect/packages/effect/src/Struct.js'
declare namespace m1 {
  export type NK<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Lambda: any
declare const PropertyKey: any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ Mutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mutable(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = mutable(T[K])
  }
  return merge(out, {})
}
/* compiles to:
 * export type Mutable<T> = { -readonly [K in keyof T]: T[K] } & {}
 */

// ✓ Assign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Assign(T, U): any {
  return Simplify(matches<never>(keyof(T) & keyof(U)) ? (T & U) : (Omit(T, merge(keyof(T), keyof(U))) & U))
}
/* compiles to:
 * export type Assign<T, U> = Simplify<
 *   keyof T & keyof U extends never ? T & U : Omit<T, keyof T & keyof U> & U
 * >
 */

// ✓ Evolver: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Evolver(S): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([S[K]], unknown)))
  }
  return out
}
/* compiles to:
 * export type Evolver<S> = { readonly [K in keyof S]?: (a0: S[K]) => unknown }
 */

// ✓ Evolved: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Evolved__v1(S, E, K): any {
  const m1 = matches<(...a: any) => Hole<"R">>(E[K])
  if (m1) {
    return m1.R
  }
  return S[K]
}
export function Evolved(S, E): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = K in E ? (Evolved__v1(S, E, K)) : S[K]
  }
  return Simplify(out)
}
/* compiles to:
 * export type Evolved__v1<S, E, K> = E[K] extends (...a: any) => infer R ? R : S[K]
 * export type Evolved<S, E> = Simplify<
 *   { [K in keyof S]: K extends keyof E ? Evolved__v1<S, E, K> : S[K] }
 * >
 */

// ✗ KeyEvolver: the ScriptType does not itself typecheck as TypeScript
//   KeyEvolver.st.ts(5:48) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function KeyEvolver(S): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([K], PropertyKey)))
  }
  return out
}
/* compiles to:
 * export type KeyEvolver<S> = { readonly [K in keyof S]?: (a0: K) => PropertyKey }
 */

// ✗ KeyEvolved: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function KeyEvolved__v1(E, K): any {
  const m1 = matches<((k: typeof K) => Hole<"R", PropertyKey>)>(E[K])
  if (m1) {
    return m1.R
  }
  return K
}
export function KeyEvolved(S, E): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K in E ? (KeyEvolved__v1(E, K)) : K] = S[K]
  }
  return Simplify(out)
}
/* compiles to:
 * export type KeyEvolved__v1<E, K> = E[K] extends (k: K) => infer R extends PropertyKey ? R : K
 * export type KeyEvolved<S, E> = Simplify<
 *   { [K in keyof S as K extends keyof E ? KeyEvolved__v1<E, K> : K]: S[K] }
 * >
 */

// ✗ EntryEvolver: the ScriptType does not itself typecheck as TypeScript
//   EntryEvolver.st.ts(5:55) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function EntryEvolver(S): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([K, S[K]], [PropertyKey, unknown])))
  }
  return out
}
/* compiles to:
 * export type EntryEvolver<S> = {
 *   readonly [K in keyof S]?: (a0: K, a1: S[K]) => [PropertyKey, unknown]
 * }
 */

// ✓ EntryEvolved: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EntryEvolved__v1(S, E, K): any {
  const m1 = matches<((k: typeof K, v: (typeof S)[typeof K]) => [ Hole<"NK", PropertyKey>, Hole<"_V"> ])>(E[K])
  if (m1) {
    return m1.NK
  }
  return K
}

export function EntryEvolved__v2(S, E, K): any {
  const m1 = matches<((k: typeof K, v: (typeof S)[typeof K]) => [ Hole<"_NK">, Hole<"V"> ])>(E[K])
  if (m1) {
    return m1.V
  }
  return S[K]
}
export function EntryEvolved(S, E): any {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K in E ? (EntryEvolved__v1(S, E, K)) : K] = K in E ? (EntryEvolved__v2(S, E, K)) : S[K]
  }
  return out
}
/* compiles to:
 * export type EntryEvolved__v1<S, E, K> =
 *   E[K] extends (k: K, v: (S)[K]) => [ infer NK extends PropertyKey, infer _V ] ? NK : K
 * export type EntryEvolved__v2<S, E, K> =
 *   E[K] extends (k: K, v: (S)[K]) => [ infer _NK, infer V ] ? V : S[K]
 * export type EntryEvolved<S, E> = {
 *   [K in keyof S as K extends keyof E ? EntryEvolved__v1<S, E, K> : K]: K extends keyof E
 *     ? EntryEvolved__v2<S, E, K>
 *     : S[K]
 * }
 */

// ✓ Apply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Apply(L: Lambda, V): any {
  return (merge(L, { "~lambda.in": readonlyProp(V) }))['~lambda.out']
}
/* compiles to:
 * export type Apply<L extends Lambda, V> = (L & { readonly '~lambda.in': V })['~lambda.out']
 */

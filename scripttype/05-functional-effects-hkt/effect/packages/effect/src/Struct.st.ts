/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Struct.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 5 covered, 3 typecheck-error, 2 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Lambda: any
declare const PropertyKey: any
type Lambda<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ Mutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mutable(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = mutable(T[K])
  }
  return merge(out, {})
}
/* compiles to:
 * export type Mutable<T> = { -readonly [K in keyof T]: T[K] } & {}
 */

// ✓ Assign: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Assign(T, U) {
  return Simplify(matches<never>(keyof(T) & keyof(U)) ? (T & U) : (Omit(T, keyof(T) & keyof(U)) & U))
}
/* compiles to:
 * export type Assign<T, U> = Simplify<keyof T & keyof U extends never ? T & U : Omit<T, keyof T & keyof U> & U>
 */

// ✓ Evolver: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Evolver(S) {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([S[K]], unknown)))
  }
  return out
}
/* compiles to:
 * export type Evolver<S> = { readonly [K in keyof S]?: (a0: S[K]) => unknown }
 */

// ✗ Evolved: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function Evolved(S, E) {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = matches<keyof typeof E>(K) ? (raw('E[K] extends (...a: any) => infer R ? R : S[K]')) : S[K]
  }
  return Simplify(out)
}

// ✗ KeyEvolver: the ScriptType does not itself typecheck as TypeScript
//   KeyEvolver.st.ts(5:48) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function KeyEvolver(S) {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([K], PropertyKey)))
  }
  return out
}
/* compiles to:
 * export type KeyEvolver<S> = { readonly [K in keyof S]?: (a0: K) => PropertyKey }
 */

// ✗ KeyEvolved: the ScriptType does not itself typecheck as TypeScript
//   KeyEvolved.st.ts(3:34) TS2552: Cannot find name 'K'. Did you mean 'k'?
/* @scripttype preserveParamNames */
export function KeyEvolved(S, E) {
  const m1 = matches<((k: typeof K) => Hole<"R", PropertyKey>)>(E[K])
  const out = emptyObject
  for (const K in keyof(S)) {
    out[matches<keyof typeof E>(K) ? (m1 ? m1.R : K) : K] = S[K]
  }
  return Simplify(out)
}
/* compiles to:
 * export type KeyEvolved<S, E> = Simplify<{ [K in keyof S as K extends keyof E ? E[K] extends (k: typeof K) => infer R extends PropertyKey ? R : K : K]: S[K] }>
 */

// ✗ EntryEvolver: the ScriptType does not itself typecheck as TypeScript
//   EntryEvolver.st.ts(5:55) TS2693: 'PropertyKey' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function EntryEvolver(S) {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[K] = readonlyProp(optional(fnType([K, S[K]], [PropertyKey, unknown])))
  }
  return out
}
/* compiles to:
 * export type EntryEvolver<S> = { readonly [K in keyof S]?: (a0: K, a1: S[K]) => [PropertyKey, unknown] }
 */

// ✗ EntryEvolved: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function EntryEvolved(S, E) {
  const out = emptyObject
  for (const K in keyof(S)) {
    out[matches<keyof typeof E>(K) ? (m1 ? m1.NK : K) : K] = matches<keyof typeof E>(K) ? (raw('E[K] extends ((k: K, v: S[K]) => [ infer _NK, infer V ]) ? V : S[K]')) : S[K]
  }
  return out
}

// ✓ Apply: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Apply(L: Lambda, V) {
  return (merge(L, { "~lambda.in": readonlyProp(V) }))['~lambda.out']
}
/* compiles to:
 * export type Apply<L extends Lambda, V> = (L & { readonly '~lambda.in': V })['~lambda.out']
 */

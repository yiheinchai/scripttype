/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Tuple.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 6 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace I {
  export type push<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
// ✓ Indices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Indices(T: ReadonlyArray<unknown>): any {
  return Exclude(Partial(T)['length'], T['length'])
}
/* compiles to:
 * export type Indices<T extends ReadonlyArray<unknown>> = Exclude<
 *   Partial<T>['length'],
 *   T['length']
 * >
 */

// ✓ _BuildTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _BuildTuple(T: ReadonlyArray<unknown>, K): any {
  let Acc: any[] = []
  let I: any[] = []
  while (true) {
    if (matches<(typeof T)["length"]>(I['length'])) {
      break
    }
    Acc = matches<typeof K>(I['length']) ? [...Acc, T[I['length']]] : Acc
    I.push(unknown)
  }
  return Acc
}
/* compiles to:
 * export type _BuildTuple<T extends ReadonlyArray<unknown>, K> = _BuildTuple__loop<[], [], T, K>
 * type _BuildTuple__loop<
 *   Acc extends any[],
 *   I extends any[],
 *   T extends ReadonlyArray<unknown>,
 *   K
 * > =
 *   I['length'] extends T['length']
 *     ? Acc
 *     : _BuildTuple__loop<
 *       I['length'] extends K ? [...Acc, T[I['length']]] : Acc,
 *       [...I, unknown],
 *       T,
 *       K
 *     >
 */

// ✓ PickTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PickTuple(T: ReadonlyArray<unknown>, K): any {
  return _BuildTuple(T, K)
}
/* compiles to:
 * export type PickTuple<T extends ReadonlyArray<unknown>, K> = _BuildTuple<T, K>
 */

// ✓ OmitTuple: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitTuple(T: ReadonlyArray<unknown>, K): any {
  return _BuildTuple(T, Exclude(Indices(T), K))
}
/* compiles to:
 * export type OmitTuple<T extends ReadonlyArray<unknown>, K> = _BuildTuple<
 *   T,
 *   Exclude<Indices<T>, K>
 * >
 */

// ✓ Evolver: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Evolver(T): any {
  const out = emptyObject
  for (const I in keyof(T)) {
    out[I] = readonlyProp(optional(anyOf(fnType([T[I]], unknown), Undefined)))
  }
  return out
}
/* compiles to:
 * export type Evolver<T> = { readonly [I in keyof T]?: ((a0: T[I]) => unknown) | undefined }
 */

// ✓ Evolved: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Evolved__v1(T, E, I): any {
  const m1 = matches<(...a: any) => Hole<"R">>(E[I])
  if (m1) {
    return m1.R
  }
  return T[I]
}
export function Evolved(T, E): any {
  const out = emptyObject
  for (const I in keyof(T)) {
    out[I] = I in E ? (Evolved__v1(T, E, I)) : T[I]
  }
  return out
}
/* compiles to:
 * export type Evolved__v1<T, E, I> = E[I] extends (...a: any) => infer R ? R : T[I]
 * export type Evolved<T, E> = { [I in keyof T]: I extends keyof E ? Evolved__v1<T, E, I> : T[I] }
 */

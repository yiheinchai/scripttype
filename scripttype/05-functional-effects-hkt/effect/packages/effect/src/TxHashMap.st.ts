/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/TxHashMap.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const TxHashMap: any
type TxHashMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Key: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Key(T: TxHashMap<any, any>) {
  const m1 = matches<TxHashMap<Hole<"K">, any>>(T)
  if (m1) {
    return m1.K
  }
  return never
}
/* compiles to:
 * export type Key<T extends TxHashMap<any, any>> = T extends TxHashMap<infer K, any> ? K : never
 */

// ✓ Value: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Value(T: TxHashMap<any, any>) {
  const m1 = matches<TxHashMap<any, Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return never
}
/* compiles to:
 * export type Value<T extends TxHashMap<any, any>> = T extends TxHashMap<any, infer V> ? V : never
 */

// ✗ Entry: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Entry(T: TxHashMap<any, any>) {
  const m1 = matches<TxHashMap<Hole<"K">, Hole<"V">>>(T)
  if (m1) {
    return asReadonly([m1.K, m1.V])
  }
  return never
}
/* compiles to:
 * export type Entry<T extends TxHashMap<any, any>> =
 *   T extends TxHashMap<infer K, infer V> ? readonly [K, V] : never
 */

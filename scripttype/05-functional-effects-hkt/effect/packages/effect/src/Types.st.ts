/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/Types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 33 type-level aliases: 33 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace m1 {
  export type B<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type K<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type T<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type T<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const V: any
type Contravariant<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Covariant<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type DeepMutable<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Invariant<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnionToIntersection<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ TupleOf_: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TupleOf_(T, N: number, R: Array<unknown>): any {
  if (matches<`-${number}`>(`${N}`)) {
    return never
  }
  if (matches<typeof N>(R['length'])) {
    return R
  }
  return TupleOf_(T, N, [T, ...R])
}
/* compiles to:
 * export type TupleOf_<T, N extends number, R extends Array<unknown>> =
 *   `${N}` extends `-${number}` ? never : R['length'] extends N ? R : TupleOf_<T, N, [T, ...R]>
 */

// ✓ TupleOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TupleOf(N: number, T): any {
  if (matches<typeof N>(N)) {
    if (matches<typeof N>(number)) {
      return arrayOf(T)
    }
    return TupleOf_(T, N, [])
  }
  return never
}
/* compiles to:
 * export type TupleOf<N extends number, T> =
 *   N extends N ? number extends N ? T[] : TupleOf_<T, N, []> : never
 */

// ✓ TupleOfAtLeast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TupleOfAtLeast(N: number, T): any {
  return [...TupleOf(N, T), ...arrayOf(T)]
}
/* compiles to:
 * export type TupleOfAtLeast<N extends number, T> = [...TupleOf<N, T>, ...T[]]
 */

// ✓ Tags: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Tags(E): any {
  if (matches<{ readonly _tag: string; }>(E)) {
    return E['_tag']
  }
  return never
}
/* compiles to:
 * export type Tags<E> = E extends { readonly _tag: string; } ? E['_tag'] : never
 */

// ✓ ExcludeTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeTag(E, K: string): any {
  return Exclude(E, { _tag: readonlyProp(K) })
}
/* compiles to:
 * export type ExcludeTag<E, K extends string> = Exclude<E, { readonly _tag: K }>
 */

// ✓ ExtractTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractTag(E, K: string): any {
  const m1 = matches<{ readonly _tag: Hole<"T">; }>(E)
  if (m1) {
    if (matches<typeof m1.T>(K)) {
      return E
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractTag<E, K extends string> =
 *   E extends { readonly _tag: infer T; } ? K extends T ? E : never : never
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(T): any {
  const m1 = matches<(x: Hole<"R">) => any>(matches<any>(T) ? fnType([T], any) : never)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<T> =
 *   (T extends any ? (a0: T) => any : never) extends (x: infer R) => any ? R : never
 */

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(A): any {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  const m1 = matches<Hole<"B">>(out)
  if (m1) {
    return m1.B
  }
  return never
}
/* compiles to:
 * export type Simplify<A> = { [K in keyof A]: A[K] } extends infer B ? B : never
 */

// ✓ Equals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equals(X, Y): any {
  if (matches<<T>() => T extends typeof Y ? 1 : 2>(genericFnType(['T'], [], matches<typeof X>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equals<X, Y> =
 *   (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y ? 1 : 2 ? true : false
 */

// ✓ EqualsWith: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EqualsWith(A, B, Y, N): any {
  if (matches<(<T>() => T extends typeof B ? 1 : 2)>(genericFnType(['T'], [], matches<typeof A>(T) ? 1 : 2))) {
    return Y
  }
  return N
}
/* compiles to:
 * export type EqualsWith<A, B, Y, N> =
 *   (<T>() => T extends A ? 1 : 2) extends <T>() => T extends B ? 1 : 2 ? Y : N
 */

// ✓ Has: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Has(A, Key: string): any {
  const m1 = matches<Hole<"K">>(Key)
  if (matches<never>(m1 ? (m1.K in A ? true : never) : never)) {
    return false
  }
  return true
}
/* compiles to:
 * export type Has<A, Key extends string> =
 *   (Key extends infer K ? K extends keyof A ? true : never : never) extends never ? false : true
 */

// ✓ MergeLeft: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeLeft(Source, Target): any {
  return MergeRight(Target, Source)
}
/* compiles to:
 * export type MergeLeft<Source, Target> = MergeRight<Target, Source>
 */

// ✓ MergeRight: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeRight(Target, Source): any {
  const out = emptyObject
  for (const Key in keyof(Target)) {
    out[Key in Source ? never : Key] = Target[Key]
  }
  return Simplify(merge(Source, out))
}
/* compiles to:
 * export type MergeRight<Target, Source> = Simplify<
 *   Source & { [Key in keyof Target as Key extends keyof Source ? never : Key]: Target[Key] }
 * >
 */

// ✓ Mutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mutable(T): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[P] = mutable(T[P])
  }
  return out
}
/* compiles to:
 * export type Mutable<T> = { -readonly [P in keyof T]: T[P] }
 */

// ✓ DeepMutable: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepMutable(T): any {
  const m1 = matches<ReadonlyMap<Hole<"K">, Hole<"V">>>(T)
  if (m1) {
    return t<Map<DeepMutable<typeof m1.K>, DeepMutable<typeof m1.V>>>()
  }
  const m2 = matches<ReadonlySet<Hole<"V">>>(T)
  if (m2) {
    return t<Set<DeepMutable<typeof m2.V>>>()
  }
  if (matches<string | number | boolean | bigint | symbol | Function>(T)) {
    return T
  }
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = mutable(DeepMutable(T[K]))
  }
  return out
}
/* compiles to:
 * export type DeepMutable<T> =
 *   T extends ReadonlyMap<infer K, infer V> ? Map<DeepMutable<K>, DeepMutable<V>>
 *   : T extends ReadonlySet<infer V> ? Set<DeepMutable<V>>
 *   : T extends string | number | boolean | bigint | symbol | Function ? T
 *   : { -readonly [K1 in keyof T]: DeepMutable<T[K1]> }
 */

// ✓ NoInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoInfer(A): any {
  return [A][matches<any>(A) ? 0 : never]
}
/* compiles to:
 * export type NoInfer<A> = [A][A extends any ? 0 : never]
 */

// ✓ Invariant: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Invariant(A): any {
  return fnType([A], A)
}
/* compiles to:
 * export type Invariant<A> = (a0: A) => A
 */

// ✓ Type: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Type(A): any {
  const m1 = matches<Invariant<Hole<"U">>>(A)
  if (m1) {
    return m1.U
  }
  return never
}
/* compiles to:
 * export type Type<A> = A extends Invariant<infer U> ? U : never
 */

// ✓ Covariant: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Covariant(A): any {
  return fnType([never], A)
}
/* compiles to:
 * export type Covariant<A> = (a0: never) => A
 */

// ✓ Type: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Type(A): any {
  const m1 = matches<Covariant<Hole<"U">>>(A)
  if (m1) {
    return m1.U
  }
  return never
}
/* compiles to:
 * export type Type<A> = A extends Covariant<infer U> ? U : never
 */

// ✓ Contravariant: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Contravariant(A): any {
  return fnType([A], voidType())
}
/* compiles to:
 * export type Contravariant<A> = (a0: A) => void
 */

// ✓ Type: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Type(A): any {
  const m1 = matches<Contravariant<Hole<"U">>>(A)
  if (m1) {
    return m1.U
  }
  return never
}
/* compiles to:
 * export type Type<A> = A extends Contravariant<infer U> ? U : never
 */

// ✓ VoidIfEmpty: verified type-identical to the original
/* @scripttype preserveParamNames */
export function VoidIfEmpty(S): any {
  if (matches<never>(keyof(S))) {
    return voidType()
  }
  return S
}
/* compiles to:
 * export type VoidIfEmpty<S> = keyof S extends never ? void : S
 */

// ✓ NotFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotFunction(T): any {
  if (matches<Function>(T)) {
    return never
  }
  return T
}
/* compiles to:
 * export type NotFunction<T> = T extends Function ? never : T
 */

// ✓ NoExcessProperties: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoExcessProperties(T, U): any {
  return T & Readonly(Record(Exclude(keyof(U), keyof(T)), never))
}
/* compiles to:
 * export type NoExcessProperties<T, U> = T & Readonly<Record<Exclude<keyof U, keyof T>, never>>
 */

// ✓ IsUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsUnion(T): any {
  if (matches<[ UnionToIntersection<typeof T> ]>([T])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsUnion<T> = [T] extends [UnionToIntersection<T>] ? false : true
 */

// ✓ ReasonOf: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReasonOf(E): any {
  const m1 = matches<{ readonly reason: Hole<"R">; }>(E)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type ReasonOf<E> = E extends { readonly reason: infer R; } ? R : never
 */

// ✓ ReasonTags: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReasonTags(E): any {
  if (matches<{ readonly reason: { readonly _tag: string; }; }>(E)) {
    return E['reason']['_tag']
  }
  return never
}
/* compiles to:
 * export type ReasonTags<E> =
 *   E extends { readonly reason: { readonly _tag: string; }; } ? E['reason']['_tag'] : never
 */

// ✓ ExtractReason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractReason(E, K: string): any {
  const m1 = matches<{ readonly reason: Hole<"R">; }>(E)
  if (m1) {
    const m2 = matches<{ readonly _tag: Hole<"T">; }>(m1.R)
    if (m2) {
      if (matches<typeof m2.T>(K)) {
        return m1.R
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractReason<E, K extends string> =
 *   E extends { readonly reason: infer R; }
 *     ? R extends { readonly _tag: infer T; } ? K extends T ? R : never : never
 *     : never
 */

// ✓ NarrowReason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NarrowReason(E, K: string): any {
  const m1 = matches<{ readonly reason: Hole<"R">; }>(E)
  if (m1) {
    const m2 = matches<{ readonly _tag: Hole<"T">; }>(m1.R)
    if (m2) {
      if (matches<typeof m2.T>(K)) {
        return merge(E, { reason: readonlyProp(m1.R) })
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NarrowReason<E, K extends string> =
 *   E extends { readonly reason: infer R; }
 *     ? R extends { readonly _tag: infer T; }
 *       ? K extends T ? E & { readonly reason: R } : never
 *       : never
 *     : never
 */

// ✓ OmitReason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OmitReason(E, K: string): any {
  const m1 = matches<{ readonly reason: Hole<"R">; }>(E)
  if (m1) {
    const m2 = matches<{ readonly _tag: Hole<"T">; }>(m1.R)
    if (m2) {
      if (matches<typeof m2.T>(K)) {
        return never
      }
      return merge(E, { reason: readonlyProp(m1.R) })
    }
    return never
  }
  return never
}
/* compiles to:
 * export type OmitReason<E, K extends string> =
 *   E extends { readonly reason: infer R; }
 *     ? R extends { readonly _tag: infer T; }
 *       ? K extends T ? never : E & { readonly reason: R }
 *       : never
 *     : never
 */

// ✓ ExcludeReason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeReason(E, K: string): any {
  const m1 = matches<{ readonly reason: Hole<"R">; }>(E)
  if (m1) {
    return Exclude(m1.R, { _tag: readonlyProp(K) })
  }
  return never
}
/* compiles to:
 * export type ExcludeReason<E, K extends string> =
 *   E extends { readonly reason: infer R; } ? Exclude<R, { readonly _tag: K }> : never
 */

// ✓ RequiredKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredKeys(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = required(matches<Pick<typeof T, typeof K>>({}) ? never : K)
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type RequiredKeys<T> = { [K in keyof T]-?: {} extends Pick<T, K> ? never : K }[keyof T]
 */

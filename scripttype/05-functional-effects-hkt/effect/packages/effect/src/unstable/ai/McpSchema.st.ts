/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/McpSchema.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 4 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Any: any
declare const Rpc: any
declare const RpcGroup: any
type Any<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Rpc<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RpcGroup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ RequestEncoded: the ScriptType does not itself typecheck as TypeScript
//   RequestEncoded.st.ts(2:39) TS2702: 'RpcGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function RequestEncoded(Group: RpcGroup.Any) {
  const m1 = matches<Hole<"Rpc">>(RpcGroup.Rpcs(Group))
  if (m1) {
    const m2 = matches<Rpc.Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">>>(m1.Rpc)
    if (m2) {
      return { _tag: readonlyProp('Request'), id: readonlyProp(string | number), method: readonlyProp(m2._Tag), payload: readonlyProp(m2._Payload['Encoded']) }
    }
    return never
  }
  return never
}
/* compiles to:
 * export type RequestEncoded<Group extends RpcGroup.Any> = RpcGroup.Rpcs<Group> extends infer Rpc ? Rpc extends Rpc.Rpc<infer _Tag, infer _Payload, unknown, unknown, unknown> ? { readonly _tag: 'Request'; readonly id: string | number; readonly method: _Tag; readonly payload: _Payload['Encoded'] } : never : never
 */

// ✗ NotificationEncoded: the ScriptType does not itself typecheck as TypeScript
//   NotificationEncoded.st.ts(2:44) TS2702: 'RpcGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function NotificationEncoded(Group: RpcGroup.Any) {
  const m1 = matches<Hole<"Rpc">>(RpcGroup.Rpcs(Group))
  if (m1) {
    const m2 = matches<Rpc.Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">>>(m1.Rpc)
    if (m2) {
      return { _tag: readonlyProp('Notification'), method: readonlyProp(m2._Tag), payload: readonlyProp(m2._Payload['Encoded']) }
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NotificationEncoded<Group extends RpcGroup.Any> = RpcGroup.Rpcs<Group> extends infer Rpc ? Rpc extends Rpc.Rpc<infer _Tag, infer _Payload, unknown, unknown, unknown> ? { readonly _tag: 'Notification'; readonly method: _Tag; readonly payload: _Payload['Encoded'] } : never : never
 */

// ✗ SuccessEncoded: the ScriptType does not itself typecheck as TypeScript
//   SuccessEncoded.st.ts(2:39) TS2702: 'RpcGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SuccessEncoded(Group: RpcGroup.Any) {
  const m1 = matches<Hole<"Rpc">>(RpcGroup.Rpcs(Group))
  if (m1) {
    const m2 = matches<Rpc.Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">>>(m1.Rpc)
    if (m2) {
      return { _tag: readonlyProp('Success'), id: readonlyProp(string | number), result: readonlyProp(m2._Success['Encoded']) }
    }
    return never
  }
  return never
}
/* compiles to:
 * export type SuccessEncoded<Group extends RpcGroup.Any> = RpcGroup.Rpcs<Group> extends infer Rpc ? Rpc extends Rpc.Rpc<unknown, unknown, infer _Success, unknown, unknown> ? { readonly _tag: 'Success'; readonly id: string | number; readonly result: _Success['Encoded'] } : never : never
 */

// ✗ FailureEncoded: the ScriptType does not itself typecheck as TypeScript
//   FailureEncoded.st.ts(2:39) TS2702: 'RpcGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function FailureEncoded(Group: RpcGroup.Any) {
  const m1 = matches<Hole<"Rpc">>(RpcGroup.Rpcs(Group))
  if (m1) {
    const m2 = matches<Rpc.Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">>>(m1.Rpc)
    if (m2) {
      return { _tag: readonlyProp('Failure'), id: readonlyProp(string | number), error: readonlyProp(m2._Error['Encoded']) }
    }
    return never
  }
  return never
}
/* compiles to:
 * export type FailureEncoded<Group extends RpcGroup.Any> = RpcGroup.Rpcs<Group> extends infer Rpc ? Rpc extends Rpc.Rpc<unknown, unknown, unknown, infer _Error, unknown> ? { readonly _tag: 'Failure'; readonly id: string | number; readonly error: _Error['Encoded'] } : never : never
 */

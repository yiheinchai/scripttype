/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/Prompt.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Message: any
declare const Part: any
type Message<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Part<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ PartConstructorParams: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function PartConstructorParams(P: Part) {
  return merge(Omit(P, anyOf(raw('typeof PartTypeId'), 'type', 'options')), { options: readonlyProp(optional(anyOf(Part['options'], Undefined))) })
}

// ✗ MessageConstructorParams: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function MessageConstructorParams(M: Message) {
  return merge(Omit(M, anyOf(raw('typeof MessageTypeId'), 'role', 'options')), { options: readonlyProp(optional(anyOf(Part['options'], Undefined))) })
}

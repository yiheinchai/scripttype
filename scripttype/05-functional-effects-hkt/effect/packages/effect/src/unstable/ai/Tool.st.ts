/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/Tool.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 22 type-level aliases: 22 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AiErrorEncoded } from '../../../../../../../../05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/AiError.js'
import type { Any, Handler, NeedsApprovalContext, ProviderDefined } from '../../../../../../../../05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/Tool.js'
declare namespace AiError {
  export type AiError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type AiErrorEncoded<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Effect {
  export type Effect<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Schema {
  export type Constraint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type _Config<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _Name<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _Requirements<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type _RequiresHandler<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AiError: any
declare const AiErrorEncoded: any
declare const Any: any
declare const Constraint: any
declare const Effect: any
declare const Handler: any
declare const NeedsApprovalContext: any
declare const ProviderDefined: any
declare const Schema: any
type AiError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Constraint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Effect<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Schema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ NeedsApprovalFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NeedsApprovalFunction(Params: Schema.Constraint): any {
  return fnType([Params['Type'], NeedsApprovalContext], boolean | t<Effect.Effect<boolean>>())
}
/* compiles to:
 * export type NeedsApprovalFunction<Params extends Schema.Constraint> =
 *   (a0: Params['Type'], a1: NeedsApprovalContext) => boolean | Effect.Effect<boolean>
 */

// ✓ NeedsApproval: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NeedsApproval(Params: Schema.Constraint): any {
  return boolean | NeedsApprovalFunction(Params)
}
/* compiles to:
 * export type NeedsApproval<Params extends Schema.Constraint> =
 *   boolean | NeedsApprovalFunction<Params>
 */

// ✓ Name: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Name(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Name
  }
  return never
}
/* compiles to:
 * export type Name<T> = T extends Tool<infer _Name, any, any> ? _Name : never
 */

// ✓ Parameters: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Parameters(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['Type']
  }
  return never
}
/* compiles to:
 * export type Parameters<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['parameters']['Type'] : never
 */

// ✓ ParametersEncoded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParametersEncoded(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['Encoded']
  }
  return never
}
/* compiles to:
 * export type ParametersEncoded<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['parameters']['Encoded'] : never
 */

// ✓ ParametersSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ParametersSchema(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']
  }
  return never
}
/* compiles to:
 * export type ParametersSchema<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['parameters'] : never
 */

// ✓ Success: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Success(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['Type']
  }
  return never
}
/* compiles to:
 * export type Success<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['success']['Type'] : never
 */

// ✓ SuccessEncoded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuccessEncoded(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['Encoded']
  }
  return never
}
/* compiles to:
 * export type SuccessEncoded<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['success']['Encoded'] : never
 */

// ✓ SuccessSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuccessSchema(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']
  }
  return never
}
/* compiles to:
 * export type SuccessSchema<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['success'] : never
 */

// ✓ Failure: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Failure(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['failure']['Type']
  }
  return never
}
/* compiles to:
 * export type Failure<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['failure']['Type'] : never
 */

// ✓ FailureEncoded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FailureEncoded(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['failure']['Encoded']
  }
  return never
}
/* compiles to:
 * export type FailureEncoded<T> =
 *   T extends Tool<any, infer _Config, any> ? _Config['failure']['Encoded'] : never
 */

// ✓ FailureResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FailureResult(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Type'] | t<AiError.AiError>()
    }
    return m1._Config['failure']['Type']
  }
  return never
}
/* compiles to:
 * export type FailureResult<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['failureMode'] extends 'return'
 *       ? _Config['failure']['Type'] | AiError.AiError
 *       : _Config['failure']['Type']
 *     : never
 */

// ✓ FailureResultEncoded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FailureResultEncoded(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Encoded'] | t<AiError.AiErrorEncoded>()
    }
    return m1._Config['failure']['Encoded']
  }
  return never
}
/* compiles to:
 * export type FailureResultEncoded<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['failureMode'] extends 'return'
 *       ? _Config['failure']['Encoded'] | AiError.AiErrorEncoded
 *       : _Config['failure']['Encoded']
 *     : never
 */

// ✓ Result: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Result(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return Success(T) | Failure(T) | t<AiError.AiError>()
    }
    return Success(T) | Failure(T)
  }
  return never
}
/* compiles to:
 * export type Result<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['failureMode'] extends 'return'
 *       ? Success<T> | Failure<T> | AiError.AiError
 *       : Success<T> | Failure<T>
 *     : never
 */

// ✓ ResultEncoded: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResultEncoded(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return SuccessEncoded(T) | FailureEncoded(T) | t<AiError.AiErrorEncoded>()
    }
    return SuccessEncoded(T) | FailureEncoded(T)
  }
  return never
}
/* compiles to:
 * export type ResultEncoded<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['failureMode'] extends 'return'
 *       ? SuccessEncoded<T> | FailureEncoded<T> | AiError.AiErrorEncoded
 *       : SuccessEncoded<T> | FailureEncoded<T>
 *     : never
 */

// ✓ HandlerServices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerServices(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['DecodingServices'] | ResultEncodingServices(T) | m1._Requirements
  }
  return never
}
/* compiles to:
 * export type HandlerServices<T> =
 *   T extends Tool<any, infer _Config, infer _Requirements>
 *     ? _Config['parameters']['DecodingServices'] | ResultEncodingServices<T> | _Requirements
 *     : never
 */

// ✓ ResultEncodingServices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResultEncodingServices(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['EncodingServices'] | m1._Config['failure']['EncodingServices']
  }
  return never
}
/* compiles to:
 * export type ResultEncodingServices<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['success']['EncodingServices'] | _Config['failure']['EncodingServices']
 *     : never
 */

// ✓ ResultDecodingServices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResultDecodingServices(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['DecodingServices'] | m1._Config['failure']['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type ResultDecodingServices<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['success']['DecodingServices'] | _Config['failure']['DecodingServices']
 *     : never
 */

// ✓ HandlerOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerOutput(Success): any {
  return anyOf({ _tag: readonlyProp('Preliminary'), value: readonlyProp(Success) }, { _tag: readonlyProp('Final'), value: readonlyProp(Success) })
}
/* compiles to:
 * export type HandlerOutput<Success> =
 *   | { readonly _tag: 'Preliminary'; readonly value: Success }
 *   | { readonly _tag: 'Final'; readonly value: Success }
 */

// ✓ HandlerError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerError(T): any {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"error">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Type'] | t<AiError.AiError>()
    }
    return never
  }
  return never
}
/* compiles to:
 * export type HandlerError<T> =
 *   T extends Tool<any, infer _Config, any>
 *     ? _Config['failureMode'] extends 'error'
 *       ? _Config['failure']['Type'] | AiError.AiError
 *       : never
 *     : never
 */

// ✓ HandlersFor: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<string, Any>} Tools
 */
export function HandlersFor(Tools): any {
  const out = emptyObject
  for (const Name in keyof(Tools)) {
    out[Name] = matches<true>(RequiresHandler(Tools[Name])) ? Handler(Tools[Name]['name']) : never
  }
  return out[keyof(Tools)]
}
/* compiles to:
 * export type HandlersFor<Tools extends Record<string, Any>> =
 *   {
 *     [Name in keyof Tools]: RequiresHandler<Tools[Name]> extends true
 *       ? Handler<Tools[Name]['name']>
 *       : never
 *   }[keyof Tools]
 */

// ✓ RequiresHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiresHandler(Tool: Any): any {
  const m1 = matches<ProviderDefined<Hole<"_Name">, Hole<"_Config">, Hole<"_RequiresHandler">>>(Tool)
  if (m1) {
    return m1._RequiresHandler
  }
  return true
}
/* compiles to:
 * export type RequiresHandler<Tool extends Any> =
 *   Tool extends ProviderDefined<any, any, infer _RequiresHandler> ? _RequiresHandler : true
 */

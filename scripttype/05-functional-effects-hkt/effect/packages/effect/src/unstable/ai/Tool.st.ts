/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/ai/Tool.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 22 type-level aliases: 18 compile-error, 4 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace AiError {
  export type AiError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type AiErrorEncoded<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Effect {
  export type Effect<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace Schema {
  export type Constraint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AiError: any
declare const Any: any
declare const Constraint: any
declare const Effect: any
declare const Handler: any
declare const NeedsApprovalContext: any
declare const ProviderDefined: any
declare const Schema: any
type AiError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Any<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Constraint<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Effect<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Handler<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NeedsApprovalContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ProviderDefined<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Schema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ NeedsApprovalFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NeedsApprovalFunction(Params: Schema.Constraint) {
  return fnType([Params['Type'], NeedsApprovalContext], boolean | Effect.Effect(boolean))
}
/* compiles to:
 * export type NeedsApprovalFunction<Params extends Schema.Constraint> =
 *   (a0: Params['Type'], a1: NeedsApprovalContext) => boolean | Effect.Effect<boolean>
 */

// ✓ NeedsApproval: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NeedsApproval(Params: Schema.Constraint) {
  return boolean | NeedsApprovalFunction(Params)
}
/* compiles to:
 * export type NeedsApproval<Params extends Schema.Constraint> =
 *   boolean | NeedsApprovalFunction<Params>
 */

// ✗ Name: does not compile yet
//   Type 'unknown' does not satisfy the constraint '{ readonly parameters: Constraint; readonly success: Constraint; readonly failure: Constraint; readonly failureM
/* @scripttype preserveParamNames */
export function Name(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Name
  }
  return never
}
/* compiles to:
 * export type Name<T> = T extends Tool<infer _Name, unknown, unknown> ? _Name : never
 */

// ✗ Parameters: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Parameters(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['Type']
  }
  return never
}
/* compiles to:
 * export type Parameters<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['parameters']['Type'] : never
 */

// ✗ ParametersEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ParametersEncoded(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['Encoded']
  }
  return never
}
/* compiles to:
 * export type ParametersEncoded<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['parameters']['Encoded'] : never
 */

// ✗ ParametersSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ParametersSchema(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']
  }
  return never
}
/* compiles to:
 * export type ParametersSchema<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['parameters'] : never
 */

// ✗ Success: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Success(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['Type']
  }
  return never
}
/* compiles to:
 * export type Success<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['success']['Type'] : never
 */

// ✗ SuccessEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function SuccessEncoded(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['Encoded']
  }
  return never
}
/* compiles to:
 * export type SuccessEncoded<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['success']['Encoded'] : never
 */

// ✗ SuccessSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function SuccessSchema(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']
  }
  return never
}
/* compiles to:
 * export type SuccessSchema<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['success'] : never
 */

// ✗ Failure: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Failure(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['failure']['Type']
  }
  return never
}
/* compiles to:
 * export type Failure<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['failure']['Type'] : never
 */

// ✗ FailureEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function FailureEncoded(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['failure']['Encoded']
  }
  return never
}
/* compiles to:
 * export type FailureEncoded<T> =
 *   T extends Tool<unknown, infer _Config, unknown> ? _Config['failure']['Encoded'] : never
 */

// ✗ FailureResult: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function FailureResult(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Type'] | AiError.AiError
    }
    return m1._Config['failure']['Type']
  }
  return never
}
/* compiles to:
 * export type FailureResult<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['failureMode'] extends 'return'
 *       ? _Config['failure']['Type'] | AiError.AiError
 *       : _Config['failure']['Type']
 *     : never
 */

// ✗ FailureResultEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function FailureResultEncoded(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Encoded'] | AiError.AiErrorEncoded
    }
    return m1._Config['failure']['Encoded']
  }
  return never
}
/* compiles to:
 * export type FailureResultEncoded<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['failureMode'] extends 'return'
 *       ? _Config['failure']['Encoded'] | AiError.AiErrorEncoded
 *       : _Config['failure']['Encoded']
 *     : never
 */

// ✗ Result: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Result(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return Success(T) | Failure(T) | AiError.AiError
    }
    return Success(T) | Failure(T)
  }
  return never
}
/* compiles to:
 * export type Result<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['failureMode'] extends 'return'
 *       ? Success<T> | Failure<T> | AiError.AiError
 *       : Success<T> | Failure<T>
 *     : never
 */

// ✗ ResultEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ResultEncoded(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"return">(m1._Config['failureMode'])) {
      return SuccessEncoded(T) | FailureEncoded(T) | AiError.AiErrorEncoded
    }
    return SuccessEncoded(T) | FailureEncoded(T)
  }
  return never
}
/* compiles to:
 * export type ResultEncoded<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['failureMode'] extends 'return'
 *       ? SuccessEncoded<T> | FailureEncoded<T> | AiError.AiErrorEncoded
 *       : SuccessEncoded<T> | FailureEncoded<T>
 *     : never
 */

// ✗ HandlerServices: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function HandlerServices(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['parameters']['DecodingServices'] | ResultEncodingServices(T) | m1._Requirements
  }
  return never
}
/* compiles to:
 * export type HandlerServices<T> =
 *   T extends Tool<unknown, infer _Config, infer _Requirements>
 *     ? _Config['parameters']['DecodingServices'] | ResultEncodingServices<T> | _Requirements
 *     : never
 */

// ✗ ResultEncodingServices: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ResultEncodingServices(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['EncodingServices'] | m1._Config['failure']['EncodingServices']
  }
  return never
}
/* compiles to:
 * export type ResultEncodingServices<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['success']['EncodingServices'] | _Config['failure']['EncodingServices']
 *     : never
 */

// ✗ ResultDecodingServices: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ResultDecodingServices(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    return m1._Config['success']['DecodingServices'] | m1._Config['failure']['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type ResultDecodingServices<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['success']['DecodingServices'] | _Config['failure']['DecodingServices']
 *     : never
 */

// ✓ HandlerOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlerOutput(Success) {
  return anyOf({ _tag: readonlyProp('Preliminary'), value: readonlyProp(Success) }, { _tag: readonlyProp('Final'), value: readonlyProp(Success) })
}
/* compiles to:
 * export type HandlerOutput<Success> =
 *   | { readonly _tag: 'Preliminary'; readonly value: Success }
 *   | { readonly _tag: 'Final'; readonly value: Success }
 */

// ✗ HandlerError: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function HandlerError(T) {
  const m1 = matches<Tool<Hole<"_Name">, Hole<"_Config">, Hole<"_Requirements">>>(T)
  if (m1) {
    if (matches<"error">(m1._Config['failureMode'])) {
      return m1._Config['failure']['Type'] | AiError.AiError
    }
    return never
  }
  return never
}
/* compiles to:
 * export type HandlerError<T> =
 *   T extends Tool<unknown, infer _Config, unknown>
 *     ? _Config['failureMode'] extends 'error'
 *       ? _Config['failure']['Type'] | AiError.AiError
 *       : never
 *     : never
 */

// ✓ HandlersFor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandlersFor(Tools: Record<string, Any>) {
  const out = emptyObject
  for (const Name in keyof(Tools)) {
    out[Name] = matches<true>(RequiresHandler(Tools[Name])) ? Handler(Tools[Name]['name']) : never
  }
  return out[keyof(Tools)]
}
/* compiles to:
 * export type HandlersFor<Tools extends Record<string, Any>> =
 *   {
 *     [Name in keyof Tools]: RequiresHandler<Tools[Name]> extends true
 *       ? Handler<Tools[Name]['name']>
 *       : never
 *   }[keyof Tools]
 */

// ✗ RequiresHandler: does not compile yet
//   Type 'unknown' does not satisfy the constraint '`${string}.${string}`'.
/* @scripttype preserveParamNames */
export function RequiresHandler(Tool: Any) {
  const m1 = matches<ProviderDefined<Hole<"_Name">, Hole<"_Config">, Hole<"_RequiresHandler">>>(Tool)
  if (m1) {
    return m1._RequiresHandler
  }
  return true
}
/* compiles to:
 * export type RequiresHandler<Tool extends Any> =
 *   Tool extends ProviderDefined<unknown, unknown, infer _RequiresHandler>
 *     ? _RequiresHandler
 *     : true
 */

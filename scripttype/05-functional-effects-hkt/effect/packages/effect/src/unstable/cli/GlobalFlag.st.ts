/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/cli/GlobalFlag.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Action, Setting } from '../../../../../../../../05-functional-effects-hkt/effect/packages/effect/src/unstable/cli/GlobalFlag.js'
declare const Action: any
declare const Setting: any
// ✓ Identifier: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Identifier(Id: string): any {
  return `effect/unstable/cli/GlobalFlag/${Id}`
}
/* compiles to:
 * export type Identifier<Id extends string> = `effect/unstable/cli/GlobalFlag/${Id}`
 */

// ✓ GlobalFlag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GlobalFlag(A): any {
  return Action(A) | Setting(any, A)
}
/* compiles to:
 * export type GlobalFlag<A> = Action<A> | Setting<any, A>
 */

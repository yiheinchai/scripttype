/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/cli/Prompt.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 3 covered, 2 raw, 1 mismatch, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Any: any
declare const Data: any
declare const Iterable: any
declare const Prompt: any
declare const Terminal: any
type Any<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Data<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Iterable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Prompt<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Terminal<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Action: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Action(State, Output) {
  return Data.TaggedEnum({ Beep: readonlyProp({}), NextFrame: readonlyProp({ state: readonlyProp(State) }), Submit: readonlyProp({ value: readonlyProp(Output) }) })
}
/* compiles to:
 * export type Action<State, Output> = Data.TaggedEnum<{ readonly Beep: {}; readonly NextFrame: { readonly state: State }; readonly Submit: { readonly value: Output } }>
 */

// ✓ ProcessInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ProcessInput(A) {
  return Data.TaggedEnum({ Input: readonlyProp({ input: readonlyProp(Terminal.UserInput) }), Event: readonlyProp({ value: readonlyProp(A) }) })
}
/* compiles to:
 * export type ProcessInput<A> = Data.TaggedEnum<{ readonly Input: { readonly input: Terminal.UserInput }; readonly Event: { readonly value: A } }>
 */

// ✗ ReturnIterable: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function ReturnIterable(T: Iterable<Any>) {
  const m1 = matches<[ Iterable<Prompt<Hole<"A">>> ]>([T])
  if (m1) {
    return Prompt(t<Array<typeof m1.A>>())
  }
  return never
}
/* compiles to:
 * export type ReturnIterable<T extends Iterable<Any>> = [T] extends [Iterable<Prompt<infer A>>] ? Prompt<Array<A>> : never
 */

// ✗ ReturnTuple: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ReturnTuple(T: ReadonlyArray<unknown>) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = mutable(raw('[ T[K] ] extends [ Prompt<infer _A> ] ? _A : never'))
  }
  const m1 = matches<Hole<"X">>(Prompt(matches<never>(T[number]) ? [] : out))
  if (m1) {
    return m1.X
  }
  return never
}

// ✗ ReturnObject: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ReturnObject(T) {
  if (matches<[ { [K: string]: Any; } ]>([T])) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = mutable(raw('[ T[K] ] extends [ Prompt<infer _A> ] ? _A : never'))
    }
    return Prompt(out)
  }
  return never
}

// ✗ Return: does not compile yet
//   Cannot find name 'ReturnTuple'. Did you mean 'ReturnType'?
/* @scripttype preserveParamNames */
export function Return(Arg: Iterable<Any> | Record<string, Any>) {
  if (matches<[ ReadonlyArray<Any> ]>([Arg])) {
    return ReturnTuple(Arg)
  }
  if (matches<[ Iterable<Any> ]>([Arg])) {
    return ReturnIterable(Arg)
  }
  if (matches<[ Record<string, Any> ]>([Arg])) {
    return ReturnObject(Arg)
  }
  return never
}
/* compiles to:
 * export type Return<Arg extends Iterable<Any> | Record<string, Any>> = [Arg] extends [ReadonlyArray<Any>] ? ReturnTuple<Arg> : [Arg] extends [Iterable<Any>] ? ReturnIterable<Arg> : [Arg] extends [Record<string, Any>] ? ReturnObject<Arg> : never
 */

// ✓ Op: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Op(Tag: string, Body = {}) {
  return merge(Prompt(never), Body, { _tag: readonlyProp(Tag) })
}
/* compiles to:
 * export type Op<Tag extends string, Body = {}> = Prompt<never> & Body & { readonly _tag: Tag }
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/eventlog/Event.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 20 type-level aliases: 10 compile-error, 8 covered, 2 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Any: any
declare const Event: any
declare const EventHandler: any
declare const Schema: any
declare const Top: any
type Any<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Error<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Event<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EventHandler<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Top<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WithTag<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ ToService: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function ToService(A) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return EventHandler(m1._Tag)
  }
  return never
}
/* compiles to:
 * export type ToService<A> = A extends Event<infer _Tag, unknown, unknown, unknown> ? EventHandler<_Tag> : never
 */

// ✗ Tag: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function Tag(A) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Tag
  }
  return never
}
/* compiles to:
 * export type Tag<A> = A extends Event<infer _Tag, unknown, unknown, unknown> ? _Tag : never
 */

// ✗ ErrorSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ErrorSchema(A: Any) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Error
  }
  return never
}
/* compiles to:
 * export type ErrorSchema<A extends Any> = A extends Event<unknown, unknown, unknown, infer _Error> ? _Error : never
 */

// ✓ Error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Error(A: Any) {
  return Schema.Schema.Type(ErrorSchema(A))
}
/* compiles to:
 * export type Error<A extends Any> = Schema.Schema.Type<ErrorSchema<A>>
 */

// ✗ AddError: the ScriptType does not itself typecheck as TypeScript
//   AddError.st.ts(2:41) TS2702: 'Schema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function AddError(A: Any, Error: Schema.Top) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return Event(m1._Tag, m1._Payload, m1._Success, anyOf(m1._Error, t<typeof Error>()))
  }
  return never
}
/* compiles to:
 * export type AddError<A extends Any, Error extends Schema.Top> = A extends Event<infer _Tag, infer _Payload, infer _Success, infer _Error> ? Event<_Tag, _Payload, _Success, _Error | Error> : never
 */

// ✗ PayloadSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function PayloadSchema(A: Any) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Payload
  }
  return never
}
/* compiles to:
 * export type PayloadSchema<A extends Any> = A extends Event<unknown, infer _Payload, unknown, unknown> ? _Payload : never
 */

// ✗ PayloadSchemaWithTag: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function PayloadSchemaWithTag(A: Any, Tag: string) {
  const m1 = matches<Event<typeof Tag, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Payload
  }
  return never
}
/* compiles to:
 * export type PayloadSchemaWithTag<A extends Any, Tag extends string> = A extends Event<Tag, infer _Payload, unknown, unknown> ? _Payload : never
 */

// ✓ Payload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Payload(A: Any) {
  return Schema.Schema.Type(PayloadSchema(A))
}
/* compiles to:
 * export type Payload<A extends Any> = Schema.Schema.Type<PayloadSchema<A>>
 */

// ✗ TaggedPayload: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function TaggedPayload(A: Any) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return { _tag: readonlyProp(m1._Tag), payload: readonlyProp(Schema.Schema.Type(m1._Payload)) }
  }
  return never
}
/* compiles to:
 * export type TaggedPayload<A extends Any> = A extends Event<infer _Tag, infer _Payload, unknown, unknown> ? { readonly _tag: _Tag; readonly payload: Schema.Schema.Type<_Payload> } : never
 */

// ✗ SuccessSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function SuccessSchema(A: Any) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Success
  }
  return never
}
/* compiles to:
 * export type SuccessSchema<A extends Any> = A extends Event<unknown, unknown, infer _Success, unknown> ? _Success : never
 */

// ✓ Success: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Success(A: Any) {
  return Schema.Schema.Type(SuccessSchema(A))
}
/* compiles to:
 * export type Success<A extends Any> = Schema.Schema.Type<SuccessSchema<A>>
 */

// ✗ ServicesClient: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ServicesClient(A) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Payload['EncodingServices'] | m1._Success['DecodingServices'] | m1._Error['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type ServicesClient<A> = A extends Event<unknown, infer _Payload, infer _Success, infer _Error> ? _Payload['EncodingServices'] | _Success['DecodingServices'] | _Error['DecodingServices'] : never
 */

// ✗ ServicesServer: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ServicesServer(A) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Payload['DecodingServices'] | m1._Success['EncodingServices'] | m1._Error['EncodingServices']
  }
  return never
}
/* compiles to:
 * export type ServicesServer<A> = A extends Event<unknown, infer _Payload, infer _Success, infer _Error> ? _Payload['DecodingServices'] | _Success['EncodingServices'] | _Error['EncodingServices'] : never
 */

// ✗ Services: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Services(A) {
  const m1 = matches<Event<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">>>(A)
  if (m1) {
    return m1._Payload['DecodingServices'] | m1._Success['EncodingServices'] | m1._Error['EncodingServices'] | m1._Payload['EncodingServices'] | m1._Success['DecodingServices'] | m1._Error['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type Services<A> = A extends Event<unknown, infer _Payload, infer _Success, infer _Error> ? _Payload['DecodingServices'] | _Success['EncodingServices'] | _Error['EncodingServices'] | _Payload['EncodingServices'] | _Success['DecodingServices'] | _Error['DecodingServices'] : never
 */

// ✓ WithTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithTag(Events: Any, Tag: string) {
  return Extract(Events, { tag: readonlyProp(Tag) })
}
/* compiles to:
 * export type WithTag<Events extends Any, Tag extends string> = Extract<Events, { readonly tag: Tag }>
 */

// ✓ ExcludeTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeTag(Events: Any, Tag: string) {
  return Exclude(Events, { tag: readonlyProp(Tag) })
}
/* compiles to:
 * export type ExcludeTag<Events extends Any, Tag extends string> = Exclude<Events, { readonly tag: Tag }>
 */

// ✓ PayloadWithTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PayloadWithTag(Events: Any, Tag: string) {
  return Payload(WithTag(Events, Tag))
}
/* compiles to:
 * export type PayloadWithTag<Events extends Any, Tag extends string> = Payload<WithTag<Events, Tag>>
 */

// ✓ SuccessWithTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuccessWithTag(Events: Any, Tag: string) {
  return Success(WithTag(Events, Tag))
}
/* compiles to:
 * export type SuccessWithTag<Events extends Any, Tag extends string> = Success<WithTag<Events, Tag>>
 */

// ✗ ErrorWithTag: the ScriptType does not itself typecheck as TypeScript
//   ErrorWithTag.st.ts(3:12) TS2315: Type 'Error' is not generic.
/* @scripttype preserveParamNames */
export function ErrorWithTag(Events: Any, Tag: string) {
  return t<Error<WithTag<typeof Events, typeof Tag>>>()
}
/* compiles to:
 * export type ErrorWithTag<Events extends Any, Tag extends string> = Error<WithTag<Events, Tag>>
 */

// ✓ ServicesClientWithTag: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ServicesClientWithTag(Events: Any, Tag: string) {
  return ServicesClient(WithTag(Events, Tag))
}
/* compiles to:
 * export type ServicesClientWithTag<Events extends Any, Tag extends string> = ServicesClient<WithTag<Events, Tag>>
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/http/HttpRouter.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 2 covered, 2 mismatch, 1 compile-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Request: any
declare const Route: any
type Request<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Route<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Error(R: Route<any, any>) {
  const m1 = matches<Route<Hole<"E">, Hole<"_R">>>(R)
  if (m1) {
    return m1.E
  }
  return never
}
/* compiles to:
 * export type Error<R extends Route<any, any>> = R extends Route<infer E, unknown> ? E : never
 */

// ✓ Context: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Context(T: Route<any, any>) {
  const m1 = matches<Route<Hole<"_E">, Hole<"R">>>(T)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type Context<T extends Route<any, any>> = T extends Route<unknown, infer R> ? R : never
 */

// ✗ From: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function From(Kind: string, R) {
  const m1 = matches<Hole<"T">>(R)
  if (m1) {
    return Request(Kind, m1.T)
  }
  return never
}
/* compiles to:
 * export type From<Kind extends string, R> = R extends infer T ? Request<Kind, T> : never
 */

// ✗ Only: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Only(Kind: string, A) {
  const m1 = matches<Request<typeof Kind, Hole<"T">>>(A)
  if (m1) {
    return m1.T
  }
  return never
}
/* compiles to:
 * export type Only<Kind extends string, A> = A extends Request<Kind, infer T> ? T : never
 */

// ✗ Without: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Without(A) {
  const m1 = matches<Request<Hole<"_Kind">, Hole<"_">>>(A)
  if (m1) {
    return never
  }
  return A
}
/* compiles to:
 * export type Without<A> = A extends Request<unknown, unknown> ? never : A
 */

// ✗ Make: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature
/* @scripttype preserveParamNames */
export function Make(Provides = never, Handles = never) {
  return raw('{ <E, R, EX, RX, const Global extends boolean = false>(middleware: Effect.Effect<(effect: Effect.Effect<HttpServerResponse.HttpServerResponse, Types.NoInfer<Handles | Types.unhandled>, Types.NoInfer<Provides>>) => Effect.Effect<HttpServerResponse.HttpServerResponse, E, R> & (Types.unhandled extends E ? unknown : "You must only handle the configured errors"), EX, RX>, options?: { readonly global?: Global | undefined; }): Global extends true ? Layer.Layer<Request.From<"Requires", Provides> | Request.From<"Error", Handles> | Request.From<"GlobalRequires", Provides> | Request.From<"GlobalError", Handles>, EX, HttpRouter | Exclude<RX, Scope.Scope> | Request.From<"GlobalRequires", Exclude<R, GlobalProvided>> | Request.From<"GlobalError", Exclude<E, Types.unhandled>>> : Middleware<{ provides: Provides; handles: Handles; error: Exclude<E, Types.unhandled>; requires: Exclude<R, Provided>; layerError: EX; layerRequires: Exclude<RX, Scope.Scope>; }>; <E, R, const Global extends boolean = false>(middleware: ((effect: Effect.Effect<HttpServerResponse.HttpServerResponse, Types.NoInfer<Handles | Types.unhandled>, Types.NoInfer<Provides>>) => Effect.Effect<HttpServerResponse.HttpServerResponse, E, R>) & (Types.unhandled extends E ? unknown : "You must only handle the configured errors"), options?: { readonly global?: Global | undefined; }): Global extends true ? Layer.Layer<Request.From<"Requires", Provides> | Request.From<"Error", Handles> | Request.From<"GlobalRequires", Provides> | Request.From<"GlobalError", Handles>, never, HttpRouter | Request.From<"GlobalRequires", Exclude<R, GlobalProvided>> | Request.From<"GlobalError", Exclude<E, Types.unhandled>>> : Middleware<{ provides: Provides; handles: Handles; error: Exclude<E, Types.unhandled>; requires: Exclude<R, Provided>; layerError: never; layerRequires: never; }>; }')
}

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/http/UrlParams.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Coercible } from '../../../../../../../../05-functional-effects-hkt/effect/packages/effect/src/unstable/http/UrlParams.js'
declare namespace m1 {
  export type Item<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const Coercible: any
// ✓ CoercibleRecordField: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CoercibleRecordField(A): any {
  if (matches<Coercible>(A)) {
    return A
  }
  const m1 = matches<ReadonlyArray<Hole<"Item">>>(A)
  if (m1) {
    return readonlyArrayOf(matches<Coercible>(m1.Item) ? m1.Item : never)
  }
  if (matches<object>(A)) {
    return CoercibleRecord(A)
  }
  return never
}
/* compiles to:
 * export type CoercibleRecordField<A> =
 *   A extends Coercible ? A
 *   : A extends ReadonlyArray<infer Item> ? readonly (Item extends Coercible ? Item : never)[]
 *   : A extends object ? CoercibleRecord<A>
 *   : never
 */

// ✓ CoercibleRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {object} A
 */
export function CoercibleRecord(A = any): any {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = readonlyProp(CoercibleRecordField(A[K]))
  }
  return out
}
/* compiles to:
 * export type CoercibleRecord<A extends object = any> = {
 *   readonly [K in keyof A]: CoercibleRecordField<A[K]>
 * }
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/http/UrlParams.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Coercible: any
type Coercible<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ CoercibleRecordField: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CoercibleRecordField(A) {
  if (matches<Coercible>(A)) {
    return A
  }
  const m1 = matches<ReadonlyArray<Hole<"Item">>>(A)
  if (m1) {
    return ReadonlyArray(matches<Coercible>(m1.Item) ? m1.Item : never)
  }
  if (matches<object>(A)) {
    return CoercibleRecord(A)
  }
  return never
}
/* compiles to:
 * export type CoercibleRecordField<A> = A extends Coercible ? A : A extends ReadonlyArray<infer Item> ? ReadonlyArray<Item extends Coercible ? Item : never> : A extends object ? CoercibleRecord<A> : never
 */

// ✓ CoercibleRecord: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CoercibleRecord(A: object = any) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = readonlyProp(CoercibleRecordField(A[K]))
  }
  return out
}
/* compiles to:
 * export type CoercibleRecord<A extends object = any> = { readonly [K in keyof A]: CoercibleRecordField<A[K]> }
 */

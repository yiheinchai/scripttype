/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/httpapi/HttpApiBuilder.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 16 type-level aliases: 10 typecheck-error, 4 covered, 2 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Constraint: any
declare const Effect: any
declare const Etag: any
declare const FileSystem: any
declare const HandlerOptions: any
declare const Handlers: any
declare const HttpApiEndpoint: any
declare const HttpApiGroup: any
declare const HttpPlatform: any
declare const HttpRouter: any
declare const HttpServerRequest: any
declare const HttpServerResponse: any
declare const Path: any
declare const PropertyKey: any
declare const Request: any
declare const WithIdentifier: any
type Constraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Etag<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type FileSystem<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HandleAllHandlers<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HandlerOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Handlers<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiEndpoint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiGroup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpPlatform<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpRouter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpServerRequest<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpServerResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Path<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Request<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WithIdentifier<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ EndpointMap: the ScriptType does not itself typecheck as TypeScript
//   EndpointMap.st.ts(2:40) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EndpointMap(Endpoints: HttpApiEndpoint.Constraint) {
  const out = emptyObject
  for (const Endpoint in keySet(Endpoints)) {
    out[HttpApiEndpoint.Identifier(Endpoint)] = readonlyProp(Endpoint)
  }
  return out
}
/* compiles to:
 * export type EndpointMap<Endpoints extends HttpApiEndpoint.Constraint> = { readonly [Endpoint in Endpoints as HttpApiEndpoint.Identifier<Endpoint>]: Endpoint }
 */

// ✗ HandlerRequirements: the ScriptType does not itself typecheck as TypeScript
//   HandlerRequirements.st.ts(2:47) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HandlerRequirements(Endpoint: HttpApiEndpoint.Constraint, R1, R = HttpApiEndpoint.ExcludeProvided(Endpoint, R1 | HttpApiEndpoint.ServerServices(Endpoint))) {
  return HttpApiEndpoint.Middleware(Endpoint) | HttpApiEndpoint.MiddlewareServices(Endpoint) | (matches<[ never ]>([R]) ? never : HttpRouter.Request.From('Requires', R))
}
/* compiles to:
 * export type HandlerRequirements<Endpoint extends HttpApiEndpoint.Constraint, R1, R = HttpApiEndpoint.ExcludeProvided<Endpoint, R1 | HttpApiEndpoint.ServerServices<Endpoint>>> = HttpApiEndpoint.Middleware<Endpoint> | HttpApiEndpoint.MiddlewareServices<Endpoint> | ([R] extends [never] ? never : HttpRouter.Request.From<'Requires', R>)
 */

// ✗ HandleAllEntry: the ScriptType does not itself typecheck as TypeScript
//   HandleAllEntry.st.ts(2:42) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HandleAllEntry(Endpoint: HttpApiEndpoint.Constraint) {
  return anyOf(HttpApiEndpoint.Handler(Endpoint, HttpApiEndpoint.MiddlewareError(Endpoint), unknown), { handler: readonlyProp(HttpApiEndpoint.Handler(Endpoint, HttpApiEndpoint.MiddlewareError(Endpoint), unknown)), options: readonlyProp(optional(anyOf(HandlerOptions, Undefined))) })
}
/* compiles to:
 * export type HandleAllEntry<Endpoint extends HttpApiEndpoint.Constraint> = HttpApiEndpoint.Handler<Endpoint, HttpApiEndpoint.MiddlewareError<Endpoint>, unknown> | { readonly handler: HttpApiEndpoint.Handler<Endpoint, HttpApiEndpoint.MiddlewareError<Endpoint>, unknown>; readonly options?: HandlerOptions | undefined }
 */

// ✗ HandleAllHandlers: the ScriptType does not itself typecheck as TypeScript
//   HandleAllHandlers.st.ts(2:73) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HandleAllHandlers(EndpointsByIdentifier: Record<string, HttpApiEndpoint.Constraint>) {
  const out = emptyObject
  for (const Identifier in keyof(EndpointsByIdentifier)) {
    out[Identifier] = readonlyProp(optional(HandleAllEntry(EndpointsByIdentifier[Identifier])))
  }
  return out
}
/* compiles to:
 * export type HandleAllHandlers<EndpointsByIdentifier extends Record<string, HttpApiEndpoint.Constraint>> = { readonly [Identifier in keyof EndpointsByIdentifier]?: HandleAllEntry<EndpointsByIdentifier[Identifier]> }
 */

// ✗ HandleAllExtraKeys: the ScriptType does not itself typecheck as TypeScript
//   HandleAllExtraKeys.st.ts(2:74) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HandleAllExtraKeys(EndpointsByIdentifier: Record<string, HttpApiEndpoint.Constraint>, HandlersByIdentifier) {
  const out = emptyObject
  for (const Identifier in keySet(Exclude(keyof(HandlersByIdentifier), keyof(EndpointsByIdentifier)))) {
    out[Identifier] = readonlyProp(never)
  }
  return out
}
/* compiles to:
 * export type HandleAllExtraKeys<EndpointsByIdentifier extends Record<string, HttpApiEndpoint.Constraint>, HandlersByIdentifier> = { readonly [Identifier in Exclude<keyof HandlersByIdentifier, keyof EndpointsByIdentifier>]: never }
 */

// ✓ NotHandledIdentifier: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotHandledIdentifier(Identifier: PropertyKey, HandledIdentifiers: PropertyKey) {
  if (matches<typeof HandledIdentifiers>(Identifier)) {
    return never
  }
  return unknown
}
/* compiles to:
 * export type NotHandledIdentifier<Identifier extends PropertyKey, HandledIdentifiers extends PropertyKey> = Identifier extends HandledIdentifiers ? never : unknown
 */

// ✓ HandleAllEntryHandler: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HandleAllEntryHandler(Entry) {
  const m1 = matches<{ readonly handler: Hole<"Handler">; }>(Entry)
  if (m1) {
    return m1.Handler
  }
  return Entry
}
/* compiles to:
 * export type HandleAllEntryHandler<Entry> = Entry extends { readonly handler: infer Handler; } ? Handler : Entry
 */

// ✗ HandleAllRequirements: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function HandleAllRequirements(EndpointsByIdentifier: Record<string, HttpApiEndpoint.Constraint>, HandlersByIdentifier: HandleAllHandlers<typeof EndpointsByIdentifier>) {
  const out = emptyObject
  for (const Identifier in keySet(keyof(HandlersByIdentifier) & keyof(EndpointsByIdentifier))) {
    out[Identifier] = readonlyProp(raw('HandleAllEntryHandler<HandlersByIdentifier[Identifier]> extends HttpApiEndpoint.Handler<EndpointsByIdentifier[Identifier], HttpApiEndpoint.MiddlewareError<EndpointsByIdentifier[Identifier]>, infer R1> ? HandlerRequirements<EndpointsByIdentifier[Identifier], R1> : never'))
  }
  return out[keyof(HandlersByIdentifier) & keyof(EndpointsByIdentifier)]
}

// ✗ HandlersResult: the ScriptType does not itself typecheck as TypeScript
//   HandlersResult.st.ts(3:22) TS2702: 'Effect' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HandlersResult(A) {
  const m1 = matches<Effect.Effect<Hole<"H">, any, any>>(A)
  if (m1) {
    return m1.H
  }
  return A
}
/* compiles to:
 * export type HandlersResult<A> = A extends Effect.Effect<infer H, any, any> ? H : A
 */

// ✓ MissingHandlerNames: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MissingHandlerNames(H: Handlers<any, any, any>) {
  return Exclude(keyof(H['~EndpointsByIdentifier']), H['~HandledIdentifiers'])
}
/* compiles to:
 * export type MissingHandlerNames<H extends Handlers<any, any, any>> = Exclude<keyof H['~EndpointsByIdentifier'], H['~HandledIdentifiers']>
 */

// ✗ ValidateHandlersReturn: uses raw() — language gap, does not count as covered
//   gap: literal type FirstTemplateToken
/* @scripttype preserveParamNames */
export function ValidateHandlersReturn(A, H = HandlersResult(A), Missing = matches<Handlers<any, any, any>>(H) ? MissingHandlerNames(H) : never) {
  if (matches<Handlers<any, any, any>>(H)) {
    if (matches<[ never ]>([Missing])) {
      return A
    }
    return `Endpoint not handled: ${Missing & string}`
  }
  return raw('`Must return the implemented handlers`')
}

// ✗ FromGroup: the ScriptType does not itself typecheck as TypeScript
//   FromGroup.st.ts(2:34) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function FromGroup(Group: HttpApiGroup.Constraint) {
  return Handlers(never, Group['endpoints'])
}
/* compiles to:
 * export type FromGroup<Group extends HttpApiGroup.Constraint> = Handlers<never, Group['endpoints']>
 */

// ✓ ValidateReturn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateReturn(A) {
  return ValidateHandlersReturn(A)
}
/* compiles to:
 * export type ValidateReturn<A> = ValidateHandlersReturn<A>
 */

// ✗ Error: the ScriptType does not itself typecheck as TypeScript
//   Error.st.ts(3:22) TS2702: 'Effect' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Error(A) {
  const m1 = matches<Effect.Effect<Handlers<Hole<"_R">, Hole<"_EndpointsByIdentifier">, Hole<"_HandledIdentifiers">>, Hole<"_EX">, Hole<"_RX">>>(A)
  if (m1) {
    return m1._EX
  }
  return never
}
/* compiles to:
 * export type Error<A> = A extends Effect.Effect<Handlers<unknown, unknown, unknown>, infer _EX, unknown> ? _EX : never
 */

// ✗ Context: the ScriptType does not itself typecheck as TypeScript
//   Context.st.ts(7:22) TS2702: 'Effect' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Context(A) {
  const m1 = matches<Handlers<Hole<"_R">, Hole<"_EndpointsByIdentifier">, Hole<"_HandledIdentifiers">>>(A)
  if (m1) {
    return m1._R
  }
  const m2 = matches<Effect.Effect<Handlers<Hole<"_R">, Hole<"_EndpointsByIdentifier">, Hole<"_HandledIdentifiers">>, Hole<"_EX">, Hole<"_RX">>>(A)
  if (m2) {
    return m2._R | m2._RX
  }
  return never
}
/* compiles to:
 * export type Context<A> = A extends Handlers<infer _R, unknown, unknown> ? _R : A extends Effect.Effect<Handlers<infer _R, unknown, unknown>, unknown, infer _RX> ? _R | _RX : never
 */

// ✗ EndpointReturn: the ScriptType does not itself typecheck as TypeScript
//   EndpointReturn.st.ts(2:40) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EndpointReturn(Groups: HttpApiGroup.Constraint, GroupIdentifier: HttpApiGroup.Identifier<typeof Groups>, EndpointIdentifier: HttpApiGroup.Endpoints<HttpApiGroup.WithIdentifier<typeof Groups, typeof GroupIdentifier>>["identifier"], R, Endpoint: HttpApiEndpoint.Constraint = HttpApiEndpoint.WithIdentifier(HttpApiGroup.Endpoints(HttpApiGroup.WithIdentifier(Groups, GroupIdentifier)), EndpointIdentifier)) {
  return Effect.Effect(Effect.Effect(HttpServerResponse, never, HttpServerRequest | HttpRouter.RouteContext | Request.ParsedSearchParams | Exclude(R, HttpApiEndpoint.MiddlewareProvides(Endpoint))), never, HttpApiEndpoint.ServerServices(Endpoint) | HttpApiEndpoint.Middleware(Endpoint) | HttpApiEndpoint.MiddlewareServices(Endpoint) | Etag.Generator | FileSystem | HttpPlatform | Path)
}
/* compiles to:
 * export type EndpointReturn<Groups extends HttpApiGroup.Constraint, GroupIdentifier extends HttpApiGroup.Identifier<Groups>, EndpointIdentifier extends HttpApiGroup.Endpoints<HttpApiGroup.WithIdentifier<Groups, GroupIdentifier>>['identifier'], R, Endpoint extends HttpApiEndpoint.Constraint = HttpApiEndpoint.WithIdentifier<HttpApiGroup.Endpoints<HttpApiGroup.WithIdentifier<Groups, GroupIdentifier>>, EndpointIdentifier>> = Effect.Effect<Effect.Effect<HttpServerResponse, never, HttpServerRequest | HttpRouter.RouteContext | Request.ParsedSearchParams | Exclude<R, HttpApiEndpoint.MiddlewareProvides<Endpoint>>>, never, HttpApiEndpoint.ServerServices<Endpoint> | HttpApiEndpoint.Middleware<Endpoint> | HttpApiEndpoint.MiddlewareServices<Endpoint> | Etag.Generator | FileSystem | HttpPlatform | Path>
 */

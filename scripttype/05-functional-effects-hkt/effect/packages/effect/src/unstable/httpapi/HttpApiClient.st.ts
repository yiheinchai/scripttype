/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/httpapi/HttpApiClient.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 19 type-level aliases: 15 typecheck-error, 1 compile-error, 1 raw, 1 covered, 1 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Constraint: any
declare const ConstraintRequest: any
declare const Effect: any
declare const EndpointsWithIdentifier: any
declare const HttpApi: any
declare const HttpApiEndpoint: any
declare const HttpApiGroup: any
declare const HttpApiMiddleware: any
declare const HttpApiSchema: any
declare const HttpClientError: any
declare const HttpClientResponse: any
declare const ResponseMode: any
declare const Schema: any
declare const Simplify: any
declare const Sse: any
declare const Stream: any
declare const StreamSse: any
declare const StreamUint8Array: any
declare const Uint8Array: any
type Constraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ConstraintRequest<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EndpointsWithIdentifier<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApi<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiEndpoint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiGroup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiMiddleware<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpClientError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpClientResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResponseMode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Simplify<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Sse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Stream<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StreamSse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StreamUint8Array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Uint8Array<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Client: the ScriptType does not itself typecheck as TypeScript
//   Client.st.ts(2:32) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Client(Groups: HttpApiGroup.Constraint, E = never, R = never) {
  const out = emptyObject
  for (const Group in keySet(Extract(Groups, { topLevel: readonlyProp(false) }))) {
    out[HttpApiGroup.Identifier(Group)] = readonlyProp(Client.Group(Group, E, R))
  }
  return Simplify(out & Client.TopLevelMethods(Groups, E, R))
}
/* compiles to:
 * export type Client<Groups extends HttpApiGroup.Constraint, E = never, R = never> = Simplify<{ readonly [Group in Extract<Groups, { readonly topLevel: false }> as HttpApiGroup.Identifier<Group>]: Client.Group<Group, E, R> } & Client.TopLevelMethods<Groups, E, R>>
 */

// ✗ ForApi: the ScriptType does not itself typecheck as TypeScript
//   ForApi.st.ts(2:29) TS2702: 'HttpApi' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ForApi(Api: HttpApi.Constraint, E = never, R = never) {
  const m1 = matches<HttpApi.HttpApi<Hole<"_Id">, Hole<"Groups">>>(Api)
  if (m1) {
    return Client(m1.Groups, E, R)
  }
  return never
}
/* compiles to:
 * export type ForApi<Api extends HttpApi.Constraint, E = never, R = never> = Api extends HttpApi.HttpApi<unknown, infer Groups> ? Client<Groups, E, R> : never
 */

// ✗ SuccessType: the ScriptType does not itself typecheck as TypeScript
//   SuccessType.st.ts(3:22) TS2702: 'HttpApiSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SuccessType(S) {
  const m1 = matches<HttpApiSchema.StreamSse<Hole<"_Events">, Hole<"_Error">, Hole<"_Value">>>(S)
  if (m1) {
    return Stream.Stream(m1._Value, m1._Error['Type'] | HttpClientError.HttpClientError | Schema.SchemaError | Sse.Retry, never)
  }
  if (matches<HttpApiSchema.StreamUint8Array>(S)) {
    return Stream.Stream(t<Uint8Array>(), HttpClientError.HttpClientError, never)
  }
  if (matches<Schema.Constraint>(S)) {
    return S['Type']
  }
  return never
}
/* compiles to:
 * export type SuccessType<S> = S extends HttpApiSchema.StreamSse<unknown, infer _Error, infer _Value> ? Stream.Stream<_Value, _Error['Type'] | HttpClientError.HttpClientError | Schema.SchemaError | Sse.Retry, never> : S extends HttpApiSchema.StreamUint8Array ? Stream.Stream<Uint8Array, HttpClientError.HttpClientError, never> : S extends Schema.Constraint ? S['Type'] : never
 */

// ✗ SuccessDecodingServices: the ScriptType does not itself typecheck as TypeScript
//   SuccessDecodingServices.st.ts(3:22) TS2702: 'HttpApiSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SuccessDecodingServices(S) {
  const m1 = matches<HttpApiSchema.StreamSse<Hole<"_Events">, Hole<"_Error">, Hole<"_Value">>>(S)
  if (m1) {
    return m1._Events['DecodingServices'] | m1._Error['DecodingServices']
  }
  if (matches<HttpApiSchema.StreamUint8Array>(S)) {
    return never
  }
  if (matches<Schema.Constraint>(S)) {
    return S['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type SuccessDecodingServices<S> = S extends HttpApiSchema.StreamSse<infer _Events, infer _Error, unknown> ? _Events['DecodingServices'] | _Error['DecodingServices'] : S extends HttpApiSchema.StreamUint8Array ? never : S extends Schema.Constraint ? S['DecodingServices'] : never
 */

// ✗ Response: does not compile yet
//   Cannot find name 'ResponseMode'. Did you mean 'Response'?
/* @scripttype preserveParamNames */
export function Response(Success, Mode: ResponseMode) {
  if (matches<[ "decoded-and-response" ]>([Mode])) {
    return [Success, HttpClientResponse.HttpClientResponse]
  }
  if (matches<[ "response-only" ]>([Mode])) {
    return HttpClientResponse.HttpClientResponse
  }
  return Success
}
/* compiles to:
 * export type Response<Success, Mode extends ResponseMode> = [Mode] extends ['decoded-and-response'] ? [Success, HttpClientResponse.HttpClientResponse] : [Mode] extends ['response-only'] ? HttpClientResponse.HttpClientResponse : Success
 */

// ✗ GroupByEndpoint: the ScriptType does not itself typecheck as TypeScript
//   GroupByEndpoint.st.ts(2:40) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function GroupByEndpoint(Group: HttpApiGroup.Constraint, E, R) {
  const m1 = matches<Hole<"Endpoints", Readonly<Record<string, HttpApiEndpoint.ConstraintRequest>>>>(Group['endpoints'])
  if (m1) {
    const out = emptyObject
    for (const Identifier in keyof(m1.Endpoints)) {
      out[Identifier] = readonlyProp(Method(m1.Endpoints[Identifier], E, R))
    }
    return out
  }
  return {}
}
/* compiles to:
 * export type GroupByEndpoint<Group extends HttpApiGroup.Constraint, E, R> = Group['endpoints'] extends (infer Endpoints extends Readonly<Record<string, HttpApiEndpoint.ConstraintRequest>>) ? { readonly [Identifier in keyof Endpoints]: Method<Endpoints[Identifier], E, R> } : {}
 */

// ✗ Group: the ScriptType does not itself typecheck as TypeScript
//   Group.st.ts(2:30) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Group(Group: HttpApiGroup.Constraint, E, R) {
  return GroupByEndpoint(Group, E, R)
}
/* compiles to:
 * export type Group<Group extends HttpApiGroup.Constraint, E, R> = GroupByEndpoint<Group, E, R>
 */

// ✗ MethodReturn: the ScriptType does not itself typecheck as TypeScript
//   MethodReturn.st.ts(2:40) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function MethodReturn(Endpoint: HttpApiEndpoint.ConstraintRequest, E, R, Mode: ResponseMode) {
  return Effect.Effect(Response(SuccessType(Endpoint['~Success']), Mode), HttpApiMiddleware.Error(Endpoint['~Middleware']) | HttpApiMiddleware.ClientError(Endpoint['~Middleware']) | E | HttpClientError.HttpClientError | (matches<[ "response-only" ]>([Mode]) ? never : (Endpoint['~Error']['Type'] | Schema.SchemaError)), R | Endpoint['~Params']['EncodingServices'] | Endpoint['~Query']['EncodingServices'] | Endpoint['~Payload']['EncodingServices'] | Endpoint['~Headers']['EncodingServices'] | (matches<[ "response-only" ]>([Mode]) ? never : (SuccessDecodingServices(Endpoint['~Success']) | Endpoint['~Error']['DecodingServices'])))
}
/* compiles to:
 * export type MethodReturn<Endpoint extends HttpApiEndpoint.ConstraintRequest, E, R, Mode extends ResponseMode> = Effect.Effect<Response<SuccessType<Endpoint['~Success']>, Mode>, HttpApiMiddleware.Error<Endpoint['~Middleware']> | HttpApiMiddleware.ClientError<Endpoint['~Middleware']> | E | HttpClientError.HttpClientError | ([Mode] extends ['response-only'] ? never : Endpoint['~Error']['Type'] | Schema.SchemaError), R | Endpoint['~Params']['EncodingServices'] | Endpoint['~Query']['EncodingServices'] | Endpoint['~Payload']['EncodingServices'] | Endpoint['~Headers']['EncodingServices'] | ([Mode] extends ['response-only'] ? never : SuccessDecodingServices<Endpoint['~Success']> | Endpoint['~Error']['DecodingServices'])>
 */

// ✗ Method: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function Method(Endpoint: HttpApiEndpoint.ConstraintRequest, E, R) {
  return raw('<Mode extends ResponseMode = ResponseMode>(request: Simplify<HttpApiEndpoint.ClientRequest<Endpoint["~Params"], Endpoint["~Query"], Endpoint["~Payload"], Endpoint["~Headers"], Mode>>) => MethodReturn<Endpoint, E, R, Mode>')
}

// ✗ TopLevelMethods: the ScriptType does not itself typecheck as TypeScript
//   TopLevelMethods.st.ts(2:41) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function TopLevelMethods(Groups: HttpApiGroup.Constraint, E, R) {
  const out = emptyObject
  for (const Endpoint in keySet(Extract(HttpApiGroup.Endpoints(Extract(Groups, { topLevel: readonlyProp(true) })), HttpApiEndpoint.ConstraintRequest))) {
    out[Endpoint['identifier']] = readonlyProp(Method(Endpoint, E, R))
  }
  return out
}
/* compiles to:
 * export type TopLevelMethods<Groups extends HttpApiGroup.Constraint, E, R> = { readonly [Endpoint in Extract<HttpApiGroup.Endpoints<Extract<Groups, { readonly topLevel: true }>>, HttpApiEndpoint.ConstraintRequest> as Endpoint['identifier']]: Method<Endpoint, E, R> }
 */

// ✓ UrlBuilderRequestPart: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UrlBuilderRequestPart(Key: string, Value) {
  if (matches<[ never ]>([Value])) {
    return {}
  }
  const out = emptyObject
  for (const K in keySet(Key)) {
    out[K] = readonlyProp(Value)
  }
  return out
}
/* compiles to:
 * export type UrlBuilderRequestPart<Key extends string, Value> = [Value] extends [never] ? {} : { readonly [K in Key]: Value }
 */

// ✗ UrlBuilderRequest: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilderRequest.st.ts(2:45) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilderRequest(Endpoint: HttpApiEndpoint.Constraint, Params = HttpApiEndpoint.Params(Endpoint)['Type'], Query = HttpApiEndpoint.Query(Endpoint)['Type']) {
  const m1 = matches<Hole<"Request">>(UrlBuilderRequestPart('params', Params) & UrlBuilderRequestPart('query', Query))
  if (m1) {
    if (matches<never>(keyof(m1.Request))) {
      return anyOf(voidType(), Undefined)
    }
    return m1.Request
  }
  return never
}
/* compiles to:
 * export type UrlBuilderRequest<Endpoint extends HttpApiEndpoint.Constraint, Params = HttpApiEndpoint.Params<Endpoint>['Type'], Query = HttpApiEndpoint.Query<Endpoint>['Type']> = UrlBuilderRequestPart<'params', Params> & UrlBuilderRequestPart<'query', Query> extends infer Request ? keyof Request extends never ? void | undefined : Request : never
 */

// ✗ UrlBuilderArgs: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function UrlBuilderArgs(Request) {
  if (matches<[ void | undefined ]>([Request])) {
    return [Request]
  }
  return [Request]
}
/* compiles to:
 * export type UrlBuilderArgs<Request> = [Request] extends [void | undefined] ? [Request] : [Request]
 */

// ✗ UrlBuilder: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilder.st.ts(2:33) TS2702: 'HttpApi' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilder(Api: HttpApi.Constraint) {
  const m1 = matches<HttpApi.HttpApi<Hole<"_ApiId">, Hole<"Groups">>>(Api)
  if (m1) {
    if (matches<[ never ]>([Extract(m1.Groups, { topLevel: readonlyProp(true) })])) {
      return UrlBuilderGroups(m1.Groups)
    }
    if (matches<[ never ]>([Extract(m1.Groups, { topLevel: readonlyProp(false) })])) {
      return UrlBuilderTopLevelMethods(m1.Groups)
    }
    return Simplify(UrlBuilderGroups(m1.Groups) & UrlBuilderTopLevelMethods(m1.Groups))
  }
  return never
}
/* compiles to:
 * export type UrlBuilder<Api extends HttpApi.Constraint> = Api extends HttpApi.HttpApi<unknown, infer Groups> ? [Extract<Groups, { readonly topLevel: true }>] extends [never] ? UrlBuilderGroups<Groups> : [Extract<Groups, { readonly topLevel: false }>] extends [never] ? UrlBuilderTopLevelMethods<Groups> : Simplify<UrlBuilderGroups<Groups> & UrlBuilderTopLevelMethods<Groups>> : never
 */

// ✗ UrlBuilderGroups: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilderGroups.st.ts(2:42) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilderGroups(Groups: HttpApiGroup.Constraint) {
  const out = emptyObject
  for (const Group in keySet(Extract(Groups, { topLevel: readonlyProp(false) }))) {
    out[HttpApiGroup.Identifier(Group)] = readonlyProp(UrlBuilderGroup(HttpApiGroup.Endpoints(Group)))
  }
  return out
}
/* compiles to:
 * export type UrlBuilderGroups<Groups extends HttpApiGroup.Constraint> = { readonly [Group in Extract<Groups, { readonly topLevel: false }> as HttpApiGroup.Identifier<Group>]: UrlBuilderGroup<HttpApiGroup.Endpoints<Group>> }
 */

// ✗ UrlBuilderGroup: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilderGroup.st.ts(2:44) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilderGroup(Endpoints: HttpApiEndpoint.Constraint) {
  const out = emptyObject
  for (const Endpoint in keySet(Endpoints)) {
    out[HttpApiEndpoint.Identifier(Endpoint)] = readonlyProp(UrlBuilderMethod(Endpoint))
  }
  return out
}
/* compiles to:
 * export type UrlBuilderGroup<Endpoints extends HttpApiEndpoint.Constraint> = { readonly [Endpoint in Endpoints as HttpApiEndpoint.Identifier<Endpoint>]: UrlBuilderMethod<Endpoint> }
 */

// ✗ UrlBuilderMethod: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilderMethod.st.ts(2:44) TS2702: 'HttpApiEndpoint' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilderMethod(Endpoint: HttpApiEndpoint.Constraint) {
  return fnType([UrlBuilderArgs(UrlBuilderRequest(Endpoint))], string)
}
/* compiles to:
 * export type UrlBuilderMethod<Endpoint extends HttpApiEndpoint.Constraint> = (a0: UrlBuilderArgs<UrlBuilderRequest<Endpoint>>) => string
 */

// ✗ UrlBuilderTopLevelMethods: the ScriptType does not itself typecheck as TypeScript
//   UrlBuilderTopLevelMethods.st.ts(2:51) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function UrlBuilderTopLevelMethods(Groups: HttpApiGroup.Constraint) {
  const out = emptyObject
  for (const Endpoint in keySet(HttpApiGroup.Endpoints(Extract(Groups, { topLevel: readonlyProp(true) })))) {
    out[HttpApiEndpoint.Identifier(Endpoint)] = readonlyProp(UrlBuilderMethod(Endpoint))
  }
  return out
}
/* compiles to:
 * export type UrlBuilderTopLevelMethods<Groups extends HttpApiGroup.Constraint> = { readonly [Endpoint in HttpApiGroup.Endpoints<Extract<Groups, { readonly topLevel: true }>> as HttpApiEndpoint.Identifier<Endpoint>]: UrlBuilderMethod<Endpoint> }
 */

// ✗ EndpointReturn: the ScriptType does not itself typecheck as TypeScript
//   EndpointReturn.st.ts(2:40) TS2702: 'HttpApiGroup' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EndpointReturn(Groups: HttpApiGroup.Constraint, GroupIdentifier: HttpApiGroup.Identifier<typeof Groups>, EndpointIdentifier: HttpApiGroup.EndpointsWithIdentifier<typeof Groups, typeof GroupIdentifier>["identifier"], E, R, Endpoint: HttpApiEndpoint.ConstraintRequest = Extract(HttpApiEndpoint.WithIdentifier(HttpApiGroup.EndpointsWithIdentifier(Groups, GroupIdentifier), EndpointIdentifier), HttpApiEndpoint.ConstraintRequest)) {
  return Effect.Effect(Client.Method(Endpoint, E, R), never, HttpApiEndpoint.MiddlewareClient(Endpoint))
}
/* compiles to:
 * export type EndpointReturn<Groups extends HttpApiGroup.Constraint, GroupIdentifier extends HttpApiGroup.Identifier<Groups>, EndpointIdentifier extends HttpApiGroup.EndpointsWithIdentifier<Groups, GroupIdentifier>['identifier'], E, R, Endpoint extends HttpApiEndpoint.ConstraintRequest = Extract<HttpApiEndpoint.WithIdentifier<HttpApiGroup.EndpointsWithIdentifier<Groups, GroupIdentifier>, EndpointIdentifier>, HttpApiEndpoint.ConstraintRequest>> = Effect.Effect<Client.Method<Endpoint, E, R>, never, HttpApiEndpoint.MiddlewareClient<Endpoint>>
 */

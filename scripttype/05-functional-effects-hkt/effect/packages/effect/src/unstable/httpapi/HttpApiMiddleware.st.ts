/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/httpapi/HttpApiMiddleware.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 10 covered, 2 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyId: any
declare const Constraint: any
declare const Context: any
declare const Effect: any
declare const ErrorConstraint: any
declare const ForClient: any
declare const HttpApiEndpoint: any
declare const HttpApiGroup: any
declare const HttpApiSecurity: any
declare const HttpRouter: any
declare const HttpServerResponse: any
declare const Schema: any
declare const SecurityTypeId: any
declare const TypeId: any
declare const unhandled: any
type AnyId<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Context<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorConstraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ForClient<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiEndpoint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiGroup<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpApiSecurity<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpRouter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type HttpServerResponse<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SecurityTypeId<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TypeId<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type unhandled<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ ErrorSchemaFromConstraint: the ScriptType does not itself typecheck as TypeScript
//   ErrorSchemaFromConstraint.st.ts(3:29) TS2702: 'Schema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ErrorSchemaFromConstraint(E) {
  if (matches<ReadonlyArray<Schema.Constraint>>(E)) {
    return E[number]
  }
  if (matches<Schema.Constraint>(E)) {
    return E
  }
  return never
}
/* compiles to:
 * export type ErrorSchemaFromConstraint<E> = E extends ReadonlyArray<Schema.Constraint> ? E[number] : E extends Schema.Constraint ? E : never
 */

// ✓ HttpApiMiddleware: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HttpApiMiddleware(Provides, E: ErrorConstraint, Requires) {
  return fnType([Effect.Effect(HttpServerResponse, unhandled, Provides), { endpoint: readonlyProp(HttpApiEndpoint.Top), group: readonlyProp(HttpApiGroup.Top) }], Effect.Effect(HttpServerResponse, unhandled | ErrorSchemaFromConstraint(E)['Type'], Requires | HttpRouter.Provided))
}
/* compiles to:
 * export type HttpApiMiddleware<Provides, E extends ErrorConstraint, Requires> = (a0: Effect.Effect<HttpServerResponse, unhandled, Provides>, a1: { readonly endpoint: HttpApiEndpoint.Top; readonly group: HttpApiGroup.Top }) => Effect.Effect<HttpServerResponse, unhandled | ErrorSchemaFromConstraint<E>['Type'], Requires | HttpRouter.Provided>
 */

// ✗ HttpApiMiddlewareSecurity: the ScriptType does not itself typecheck as TypeScript
//   HttpApiMiddlewareSecurity.st.ts(2:68) TS2702: 'HttpApiSecurity' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function HttpApiMiddlewareSecurity(Security: Record<string, HttpApiSecurity.HttpApiSecurity>, Provides, E: ErrorConstraint, Requires) {
  const out = emptyObject
  for (const K in keyof(Security)) {
    out[K] = readonlyProp(fnType([Effect.Effect(HttpServerResponse, unhandled, Provides), { credential: readonlyProp(HttpApiSecurity.HttpApiSecurity.Type(Security[K])), endpoint: readonlyProp(HttpApiEndpoint.Top), group: readonlyProp(HttpApiGroup.Top) }], Effect.Effect(HttpServerResponse, unhandled | ErrorSchemaFromConstraint(E)['Type'], Requires | HttpRouter.Provided)))
  }
  return out
}
/* compiles to:
 * export type HttpApiMiddlewareSecurity<Security extends Record<string, HttpApiSecurity.HttpApiSecurity>, Provides, E extends ErrorConstraint, Requires> = { readonly [K in keyof Security]: (a0: Effect.Effect<HttpServerResponse, unhandled, Provides>, a1: { readonly credential: HttpApiSecurity.HttpApiSecurity.Type<Security[K]>; readonly endpoint: HttpApiEndpoint.Top; readonly group: HttpApiGroup.Top }) => Effect.Effect<HttpServerResponse, unhandled | ErrorSchemaFromConstraint<E>['Type'], Requires | HttpRouter.Provided> }
 */

// ✓ Provides: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Provides(A) {
  const m1 = matches<{ readonly [TypeId]: { readonly provides: Hole<"P">; }; }>(A)
  if (m1) {
    return m1.P
  }
  return never
}
/* compiles to:
 * export type Provides<A> = A extends { readonly [TypeId]: { readonly provides: infer P; }; } ? P : never
 */

// ✓ Requires: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Requires(A) {
  const m1 = matches<{ readonly [TypeId]: { readonly requires: Hole<"R">; }; }>(A)
  if (m1) {
    return m1.R
  }
  return never
}
/* compiles to:
 * export type Requires<A> = A extends { readonly [TypeId]: { readonly requires: infer R; }; } ? R : never
 */

// ✓ ApplyServices: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ApplyServices(A: AnyId, R) {
  return Exclude(R, Provides(A)) | Requires(A)
}
/* compiles to:
 * export type ApplyServices<A extends AnyId, R> = Exclude<R, Provides<A>> | Requires<A>
 */

// ✓ ErrorSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ErrorSchema(A) {
  const m1 = matches<{ readonly [TypeId]: { readonly error: Hole<"E">; }; }>(A)
  if (m1) {
    return ErrorSchemaFromConstraint(m1.E)
  }
  return never
}
/* compiles to:
 * export type ErrorSchema<A> = A extends { readonly [TypeId]: { readonly error: infer E; }; } ? ErrorSchemaFromConstraint<E> : never
 */

// ✓ Error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Error(A) {
  return ErrorSchema(A)['Type']
}
/* compiles to:
 * export type Error<A> = ErrorSchema<A>['Type']
 */

// ✓ ClientError: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ClientError(A) {
  const m1 = matches<{ readonly [TypeId]: { readonly clientError: Hole<"CE">; readonly requiredForClient: true; }; }>(A)
  if (m1) {
    return m1.CE
  }
  return never
}
/* compiles to:
 * export type ClientError<A> = A extends { readonly [TypeId]: { readonly clientError: infer CE; readonly requiredForClient: true; }; } ? CE : never
 */

// ✓ MiddlewareClient: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MiddlewareClient(A) {
  if (matches<{ readonly [TypeId]: { readonly requiredForClient: true; }; }>(A)) {
    return ForClient(A)
  }
  return never
}
/* compiles to:
 * export type MiddlewareClient<A> = A extends { readonly [TypeId]: { readonly requiredForClient: true; }; } ? ForClient<A> : never
 */

// ✓ ErrorServicesEncode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ErrorServicesEncode(A) {
  return ErrorSchema(A)['EncodingServices']
}
/* compiles to:
 * export type ErrorServicesEncode<A> = ErrorSchema<A>['EncodingServices']
 */

// ✓ ErrorServicesDecode: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ErrorServicesDecode(A) {
  return ErrorSchema(A)['DecodingServices']
}
/* compiles to:
 * export type ErrorServicesDecode<A> = ErrorSchema<A>['DecodingServices']
 */

// ✗ ServiceClass: uses raw() — language gap, does not count as covered
//   gap: object member ConstructSignature; type node TypeQuery
/* @scripttype preserveParamNames */
export function ServiceClass(Self, Id: string, Config: { requires: any; provides: any; error: ErrorConstraint; clientError: any; requiredForClient: boolean; security: Record<string, HttpApiSecurity.HttpApiSecurity>; }, Service = matches<[ never ]>([Config['security']]) ? HttpApiMiddleware(Config['provides'], Config['error'], Config['requires']) : HttpApiMiddlewareSecurity(Config['security'], Config['provides'], Config['error'], Config['requires'])) {
  return merge(Context.Service(Self, Service), raw('{ new (_: never): Context.ServiceClass.Shape<Id, Service> & { readonly [TypeId]: { readonly error: Config["error"]; readonly requires: Config["requires"]; readonly provides: Config["provides"]; readonly clientError: Config["clientError"]; readonly requiredForClient: Config["requiredForClient"]; }; }; readonly [TypeId]: typeof TypeId; readonly error: ReadonlySet<Schema.Top>; readonly requiredForClient: Config["requiredForClient"]; readonly "~ClientError": Config["clientError"]; }'), matches<[ never ]>([keyof(Config['security'])]) ? {} : { [SecurityTypeId]: readonlyProp(raw('typeof SecurityTypeId')), security: readonlyProp(Config['security']) })
}

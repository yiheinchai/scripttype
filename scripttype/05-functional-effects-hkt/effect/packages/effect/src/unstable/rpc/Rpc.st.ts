/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/rpc/Rpc.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 32 type-level aliases: 15 compile-error, 9 covered, 8 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Any: any
declare const AnyService: any
declare const Cause: any
declare const Constraint: any
declare const Context: any
declare const Custom: any
declare const Deferred: any
declare const Effect: any
declare const Exit_: any
declare const Handler: any
declare const Headers: any
declare const Queue: any
declare const RequestId: any
declare const Rpc: any
declare const RpcMiddleware: any
declare const RpcSchema: any
declare const Schema: any
declare const ServerClient: any
declare const Stream: any
declare const Top: any
declare const Wrapper: any
type Any<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyService<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Cause<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Constraint<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Context<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Custom<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Deferred<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Exit_<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Handler<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Headers<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Queue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RequestId<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Rpc<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RpcMiddleware<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RpcSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ServerClient<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Stream<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Top<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Wrapper<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Tag: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function Tag(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Tag
  }
  return never
}
/* compiles to:
 * export type Tag<R> = R extends Rpc<infer _Tag, unknown, unknown, unknown, unknown, unknown> ? _Tag : never
 */

// ✗ SuccessSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function SuccessSchema(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Success
  }
  return never
}
/* compiles to:
 * export type SuccessSchema<R> = R extends Rpc<unknown, unknown, infer _Success, unknown, unknown, unknown> ? _Success : never
 */

// ✓ Success: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Success(R) {
  return SuccessSchema(R)['Type']
}
/* compiles to:
 * export type Success<R> = SuccessSchema<R>['Type']
 */

// ✗ SuccessEncoded: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function SuccessEncoded(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Success['Encoded']
  }
  return never
}
/* compiles to:
 * export type SuccessEncoded<R> = R extends Rpc<unknown, unknown, infer _Success, unknown, unknown, unknown> ? _Success['Encoded'] : never
 */

// ✗ SuccessExitSchema: the ScriptType does not itself typecheck as TypeScript
//   SuccessExitSchema.st.ts(3:22) TS2702: 'RpcSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SuccessExitSchema(R) {
  const m1 = matches<RpcSchema.Stream<Hole<"_A">, Hole<"_E">>>(SuccessSchema(R))
  if (m1) {
    return m1._A
  }
  return SuccessSchema(R)
}
/* compiles to:
 * export type SuccessExitSchema<R> = SuccessSchema<R> extends RpcSchema.Stream<infer _A, unknown> ? _A : SuccessSchema<R>
 */

// ✓ SuccessExit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuccessExit(R) {
  const m1 = matches<Hole<"T">>(Success(R))
  if (m1) {
    const m2 = matches<Stream<Hole<"_A">, Hole<"_E">, Hole<"_Env">>>(m1.T)
    if (m2) {
      return voidType()
    }
    return m1.T
  }
  return never
}
/* compiles to:
 * export type SuccessExit<R> = Success<R> extends infer T ? T extends Stream<unknown, unknown, unknown> ? void : T : never
 */

// ✓ SuccessChunk: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SuccessChunk(R) {
  const m1 = matches<Stream<Hole<"_A">, Hole<"_E">, Hole<"_Env">>>(Success(R))
  if (m1) {
    return m1._A
  }
  return never
}
/* compiles to:
 * export type SuccessChunk<R> = Success<R> extends Stream<infer _A, unknown, unknown> ? _A : never
 */

// ✗ ErrorSchema: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ErrorSchema(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Error | m1._Middleware['error']
  }
  return never
}
/* compiles to:
 * export type ErrorSchema<R> = R extends Rpc<unknown, unknown, unknown, infer _Error, infer _Middleware, unknown> ? _Error | _Middleware['error'] : never
 */

// ✓ Error: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Error(R) {
  return Schema.Schema.Type(ErrorSchema(R))
}
/* compiles to:
 * export type Error<R> = Schema.Schema.Type<ErrorSchema<R>>
 */

// ✗ ErrorExitSchema: the ScriptType does not itself typecheck as TypeScript
//   ErrorExitSchema.st.ts(3:22) TS2702: 'RpcSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ErrorExitSchema(R) {
  const m1 = matches<RpcSchema.Stream<Hole<"_A">, Hole<"_E">>>(SuccessSchema(R))
  if (m1) {
    return m1._E | ErrorSchema(R)
  }
  return ErrorSchema(R)
}
/* compiles to:
 * export type ErrorExitSchema<R> = SuccessSchema<R> extends RpcSchema.Stream<unknown, infer _E> ? _E | ErrorSchema<R> : ErrorSchema<R>
 */

// ✗ ErrorExit: the ScriptType does not itself typecheck as TypeScript
//   ErrorExit.st.ts(5:20) TS2363: The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type.
/* @scripttype preserveParamNames */
export function ErrorExit(R) {
  const m1 = matches<Stream<Hole<"_A">, Hole<"_E">, Hole<"_Env">>>(Success(R))
  if (m1) {
    return m1._E | Error(R)
  }
  return Error(R)
}
/* compiles to:
 * export type ErrorExit<R> = Success<R> extends Stream<unknown, infer _E, unknown> ? _E | Error<R> : Error<R>
 */

// ✓ Exit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Exit(R) {
  return Exit_(SuccessExit(R), ErrorExit(R))
}
/* compiles to:
 * export type Exit<R> = Exit_<SuccessExit<R>, ErrorExit<R>>
 */

// ✗ PayloadConstructor: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function PayloadConstructor(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Payload['~type.make.in']
  }
  return never
}
/* compiles to:
 * export type PayloadConstructor<R> = R extends Rpc<unknown, infer _Payload, unknown, unknown, unknown, unknown> ? _Payload['~type.make.in'] : never
 */

// ✗ Payload: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Payload(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Payload['Type']
  }
  return never
}
/* compiles to:
 * export type Payload<R> = R extends Rpc<unknown, infer _Payload, unknown, unknown, unknown, unknown> ? _Payload['Type'] : never
 */

// ✗ Services: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Services(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Payload['DecodingServices'] | m1._Payload['EncodingServices'] | m1._Success['DecodingServices'] | m1._Success['EncodingServices'] | m1._Error['DecodingServices'] | m1._Error['EncodingServices'] | m1._Middleware['error']['DecodingServices'] | m1._Middleware['error']['EncodingServices']
  }
  return never
}
/* compiles to:
 * export type Services<R> = R extends Rpc<unknown, infer _Payload, infer _Success, infer _Error, infer _Middleware, unknown> ? _Payload['DecodingServices'] | _Payload['EncodingServices'] | _Success['DecodingServices'] | _Success['EncodingServices'] | _Error['DecodingServices'] | _Error['EncodingServices'] | _Middleware['error']['DecodingServices'] | _Middleware['error']['EncodingServices'] : never
 */

// ✗ ServicesClient: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ServicesClient(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Payload['EncodingServices'] | m1._Success['DecodingServices'] | m1._Error['DecodingServices'] | m1._Middleware['error']['DecodingServices']
  }
  return never
}
/* compiles to:
 * export type ServicesClient<R> = R extends Rpc<unknown, infer _Payload, infer _Success, infer _Error, infer _Middleware, unknown> ? _Payload['EncodingServices'] | _Success['DecodingServices'] | _Error['DecodingServices'] | _Middleware['error']['DecodingServices'] : never
 */

// ✗ ServicesServer: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function ServicesServer(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Payload['DecodingServices'] | m1._Success['EncodingServices'] | m1._Error['EncodingServices'] | m1._Middleware['error']['EncodingServices']
  }
  return never
}
/* compiles to:
 * export type ServicesServer<R> = R extends Rpc<unknown, infer _Payload, infer _Success, infer _Error, infer _Middleware, unknown> ? _Payload['DecodingServices'] | _Success['EncodingServices'] | _Error['EncodingServices'] | _Middleware['error']['EncodingServices'] : never
 */

// ✗ Middleware: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function Middleware(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return Context.Service.Identifier(m1._Middleware)
  }
  return never
}
/* compiles to:
 * export type Middleware<R> = R extends Rpc<unknown, unknown, unknown, unknown, infer _Middleware, unknown> ? Context.Service.Identifier<_Middleware> : never
 */

// ✗ MiddlewareClient: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'string'.
/* @scripttype preserveParamNames */
export function MiddlewareClient(R) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    if (matches<{ readonly requiredForClient: true; }>(m1._Middleware)) {
      return RpcMiddleware.ForClient(m1._Middleware['Identifier'])
    }
    return never
  }
  return never
}
/* compiles to:
 * export type MiddlewareClient<R> = R extends Rpc<unknown, unknown, unknown, unknown, infer _Middleware, unknown> ? _Middleware extends { readonly requiredForClient: true; } ? RpcMiddleware.ForClient<_Middleware['Identifier']> : never : never
 */

// ✗ AddError: the ScriptType does not itself typecheck as TypeScript
//   AddError.st.ts(2:41) TS2702: 'Schema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function AddError(R: Any, Error: Schema.Top) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return Rpc(m1._Tag, m1._Payload, m1._Success, anyOf(m1._Error, Error), m1._Middleware, m1._Requires)
  }
  return never
}
/* compiles to:
 * export type AddError<R extends Any, Error extends Schema.Top> = R extends Rpc<infer _Tag, infer _Payload, infer _Success, infer _Error, infer _Middleware, infer _Requires> ? Rpc<_Tag, _Payload, _Success, _Error | Error, _Middleware, _Requires> : never
 */

// ✗ AddMiddleware: the ScriptType does not itself typecheck as TypeScript
//   AddMiddleware.st.ts(2:51) TS2702: 'RpcMiddleware' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function AddMiddleware(R: Any, Middleware: RpcMiddleware.AnyService) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return Rpc(m1._Tag, m1._Payload, m1._Success, m1._Error, anyOf(m1._Middleware, Middleware), RpcMiddleware.ApplyServices(Middleware['Identifier'], m1._Requires))
  }
  return never
}
/* compiles to:
 * export type AddMiddleware<R extends Any, Middleware extends RpcMiddleware.AnyService> = R extends Rpc<infer _Tag, infer _Payload, infer _Success, infer _Error, infer _Middleware, infer _Requires> ? Rpc<_Tag, _Payload, _Success, _Error, _Middleware | Middleware, RpcMiddleware.ApplyServices<Middleware['Identifier'], _Requires>> : never
 */

// ✗ ToHandler: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function ToHandler(R: Any) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return Handler(m1._Tag)
  }
  return never
}
/* compiles to:
 * export type ToHandler<R extends Any> = R extends Rpc<infer _Tag, unknown, unknown, unknown, unknown, unknown> ? Handler<_Tag> : never
 */

// ✓ ToHandlerFn: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToHandlerFn(Current: Any, R = any) {
  return fnType([Payload(Current), { client: readonlyProp(ServerClient), requestId: readonlyProp(RequestId), headers: readonlyProp(Headers), rpc: readonlyProp(Current) }], WrapperOr(ResultFrom(Current, R)))
}
/* compiles to:
 * export type ToHandlerFn<Current extends Any, R = any> = (a0: Payload<Current>, a1: { readonly client: ServerClient; readonly requestId: RequestId; readonly headers: Headers; readonly rpc: Current }) => WrapperOr<ResultFrom<Current, R>>
 */

// ✗ IsStream: the ScriptType does not itself typecheck as TypeScript
//   IsStream.st.ts(3:56) TS2702: 'RpcSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IsStream(R: Any, Tag: string) {
  const m1 = matches<Rpc<typeof Tag, Hole<"_Payload">, RpcSchema.Stream<Hole<"_A">, Hole<"_E">>, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return true
  }
  return never
}
/* compiles to:
 * export type IsStream<R extends Any, Tag extends string> = R extends Rpc<Tag, unknown, RpcSchema.Stream<unknown, unknown>, unknown, unknown, unknown> ? true : never
 */

// ✗ ExtractTag: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function ExtractTag(R: Any, Tag: string) {
  const m1 = matches<Rpc<typeof Tag, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return R
  }
  return never
}
/* compiles to:
 * export type ExtractTag<R extends Any, Tag extends string> = R extends Rpc<Tag, unknown, unknown, unknown, unknown, unknown> ? R : never
 */

// ✗ ExtractProvides: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function ExtractProvides(R: Any, Tag: string) {
  const m1 = matches<Rpc<typeof Tag, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return RpcMiddleware.Provides(m1._Middleware['Identifier'])
  }
  return never
}
/* compiles to:
 * export type ExtractProvides<R extends Any, Tag extends string> = R extends Rpc<Tag, unknown, unknown, unknown, infer _Middleware, unknown> ? RpcMiddleware.Provides<_Middleware['Identifier']> : never
 */

// ✗ ExtractRequires: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Top'.
/* @scripttype preserveParamNames */
export function ExtractRequires(R: Any, Tag: string) {
  const m1 = matches<Rpc<typeof Tag, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    return m1._Requires
  }
  return never
}
/* compiles to:
 * export type ExtractRequires<R extends Any, Tag extends string> = R extends Rpc<Tag, unknown, unknown, unknown, unknown, infer _Requires> ? _Requires : never
 */

// ✓ ExcludeProvides: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExcludeProvides(Env, R: Any, Tag: string) {
  return Exclude(Env, ExtractProvides(R, Tag))
}
/* compiles to:
 * export type ExcludeProvides<Env, R extends Any, Tag extends string> = Exclude<Env, ExtractProvides<R, Tag>>
 */

// ✗ ResultFrom: the ScriptType does not itself typecheck as TypeScript
//   ResultFrom.st.ts(5:26) TS2702: 'RpcSchema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ResultFrom(R: Any, Services) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(R)
  if (m1) {
    const m2 = matches<[ RpcSchema.Stream<Hole<"_SA">, Hole<"_SE">> ]>([m1._Success])
    if (m2) {
      return Stream(m2._SA['Type'], m2._SE['Type'] | m1._Error['Type'], Services) | Effect(Queue.Dequeue(m2._SA['Type'], m2._SE['Type'] | m1._Error['Type'] | Cause.Done), m2._SE['Type'] | Schema.Schema.Type(m1._Error), Services)
    }
    return Effect(m1._Success['Type'] | Deferred(m1._Success['Type'], m1._Error['Type']), m1._Error['Type'], Services)
  }
  return never
}
/* compiles to:
 * export type ResultFrom<R extends Any, Services> = R extends Rpc<unknown, unknown, infer _Success, infer _Error, unknown, unknown> ? [_Success] extends [RpcSchema.Stream<infer _SA, infer _SE>] ? Stream<_SA['Type'], _SE['Type'] | _Error['Type'], Services> | Effect<Queue.Dequeue<_SA['Type'], _SE['Type'] | _Error['Type'] | Cause.Done>, _SE['Type'] | Schema.Schema.Type<_Error>, Services> : Effect<_Success['Type'] | Deferred<_Success['Type'], _Error['Type']>, _Error['Type'], Services> : never
 */

// ✓ Prefixed: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prefixed(Rpcs: Any, Prefix: string) {
  const m1 = matches<Rpc<Hole<"_Tag">, Hole<"_Payload">, Hole<"_Success">, Hole<"_Error">, Hole<"_Middleware">, Hole<"_Requires">>>(Rpcs)
  if (m1) {
    return Rpc(`${Prefix}${m1._Tag}`, m1._Payload, m1._Success, m1._Error, m1._Middleware, m1._Requires)
  }
  return never
}
/* compiles to:
 * export type Prefixed<Rpcs extends Any, Prefix extends string> = Rpcs extends Rpc<infer _Tag, infer _Payload, infer _Success, infer _Error, infer _Middleware, infer _Requires> ? Rpc<`${Prefix}${_Tag}`, _Payload, _Success, _Error, _Middleware, _Requires> : never
 */

// ✗ Kind: the ScriptType does not itself typecheck as TypeScript
//   Kind.st.ts(2:44) TS2702: 'Schema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Kind(Def: Custom, Success: Schema.Constraint, Error: Schema.Constraint) {
  return (merge(Def, { success: readonlyProp(Success), error: readonlyProp(Error) }))['out']
}
/* compiles to:
 * export type Kind<Def extends Custom, Success extends Schema.Constraint, Error extends Schema.Constraint> = (Def & { readonly success: Success; readonly error: Error })['out']
 */

// ✓ WrapperOr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WrapperOr(A) {
  return A | Wrapper(A)
}
/* compiles to:
 * export type WrapperOr<A> = A | Wrapper<A>
 */

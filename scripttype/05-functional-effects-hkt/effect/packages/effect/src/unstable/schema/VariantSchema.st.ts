/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/src/unstable/schema/VariantSchema.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 2 raw, 1 mismatch, 1 typecheck-error, 1 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Config: any
declare const Schema: any
declare const Struct: any
declare const Struct_: any
declare const Top: any
type Config<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Struct<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Struct_<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Top<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Validate: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function Validate(A, Variant: string) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = readonlyProp(raw('A[K] extends { readonly [TypeId]: infer _; } ? Validate<A[K], Variant> : A[K] extends Field<infer Config> ? [ keyof Config ] extends [ Variant ] ? {} : "field must have valid variants" : {}'))
  }
  return out
}

// ✗ ConfigWithKeys: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function ConfigWithKeys(K: string) {
  const out = emptyObject
  for (const P in keySet(K)) {
    out[P] = readonlyProp(optional(Schema.Top))
  }
  return out
}
/* compiles to:
 * export type ConfigWithKeys<K extends string> = { readonly [P in K]?: Schema.Top }
 */

// ✗ ExtractFields: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ExtractFields(V: string, Fields: Struct.Fields, IsDefault = false) {
  const out = emptyObject
  for (const K in keyof(Fields)) {
    out[m1 ? (matches<keyof typeof m1.Config>(V) ? K : never) : K] = readonlyProp(raw('[ Fields[K] ] extends [ Struct<infer _> ] ? Extract<V, Fields[K], IsDefault> : [ Fields[K] ] extends [ Field<infer Config> ] ? [ Config[V] ] extends [ Schema.Top ] ? Config[V] : never : [ Fields[K] ] extends [ Schema.Top ] ? Fields[K] : never'))
  }
  return out
}

// ✗ Extract: the ScriptType does not itself typecheck as TypeScript
//   Extract.st.ts(6:21) TS2702: 'Schema' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function Extract(V: string, A: Struct<any>, IsDefault = false) {
  const m1 = matches<[ Struct<Hole<"Fields">> ]>([A])
  if (m1) {
    if (matches<true>(IsDefault)) {
      if (matches<[ Schema.Top ]>([A])) {
        return A
      }
      return Schema.Struct(Struct_.Simplify(ExtractFields(V, m1.Fields)))
    }
    return Schema.Struct(Struct_.Simplify(ExtractFields(V, m1.Fields)))
  }
  return never
}
/* compiles to:
 * export type Extract<V extends string, A extends Struct<any>, IsDefault = false> = [A] extends [Struct<infer Fields>] ? IsDefault extends true ? [A] extends [Schema.Top] ? A : Schema.Struct<Struct_.Simplify<ExtractFields<V, Fields>>> : Schema.Struct<Struct_.Simplify<ExtractFields<V, Fields>>> : never
 */

// ✓ MissingSelfGeneric: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MissingSelfGeneric(Params: string = '') {
  return `Missing \`Self\` generic - use \`class Self extends Class<Self>()(${Params}{ ... })\``
}
/* compiles to:
 * export type MissingSelfGeneric<Params extends string = ''> = `Missing \`Self\` generic - use \`class Self extends Class<Self>()(${Params}{ ... })\``
 */

// ✗ Variants: does not compile yet
//   Type '{}' does not satisfy the constraint 'readonly Constraint[]'.   Type '{}' is missing the following properties from type 'readonly Constraint[]': length, co
/* @scripttype preserveParamNames */
export function Variants(Members: ReadonlyArray<Struct<any>>, Variants: string) {
  const out = emptyObject
  for (const Variant in keySet(Variants)) {
    out[Variant] = readonlyProp(Schema.Union(out))
  }
  return out
}
/* compiles to:
 * export type Variants<Members extends ReadonlyArray<Struct<any>>, Variants extends string> = { readonly [Variant in Variants]: Schema.Union<{}> }
 */

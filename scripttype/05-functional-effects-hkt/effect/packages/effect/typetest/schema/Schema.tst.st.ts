/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/effect/typetest/schema/Schema.tst.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Effect: any
declare const Schema: any
type Effect<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ Make: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Make(In, Out) {
  return fnType([In, anyOf(Schema.MakeOptions, Undefined)], Out)
}
/* compiles to:
 * export type Make<In, Out> = (a0: In, a1: Schema.MakeOptions | undefined) => Out
 */

// ✗ MakeEffect: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function MakeEffect(In, Out) {
  return fnType([In, anyOf(Schema.MakeOptions, Undefined)], Effect.Effect(Out, Schema.SchemaError))
}
/* compiles to:
 * export type MakeEffect<In, Out> = (a0: In, a1: Schema.MakeOptions | undefined) => Effect.Effect<Out, Schema.SchemaError>
 */

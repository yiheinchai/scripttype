/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/platform-browser/src/IndexedDbDatabase.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 typecheck-error, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnyWithProps: any
declare const IndexedDbTable: any
declare const IndexedDbVersion: any
type AnyWithProps<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexedDbTable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexedDbVersion<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ IndexFromTable: the ScriptType does not itself typecheck as TypeScript
//   IndexFromTable.st.ts(2:39) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IndexFromTable(Table: IndexedDbTable.AnyWithProps) {
  if (matches<true>(IsStringLiteral(Extract(keyof(IndexedDbTable.Indexes(Table)), string)))) {
    return Extract(keyof(IndexedDbTable.Indexes(Table)), string)
  }
  return never
}
/* compiles to:
 * export type IndexFromTable<Table extends IndexedDbTable.AnyWithProps> = IsStringLiteral<Extract<keyof IndexedDbTable.Indexes<Table>, string>> extends true ? Extract<keyof IndexedDbTable.Indexes<Table>, string> : never
 */

// ✗ IndexFromTableName: the ScriptType does not itself typecheck as TypeScript
//   IndexFromTableName.st.ts(2:45) TS2702: 'IndexedDbVersion' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IndexFromTableName(Version: IndexedDbVersion.AnyWithProps, Table: string) {
  return IndexFromTable(IndexedDbTable.WithName(IndexedDbVersion.Tables(Version), Table))
}
/* compiles to:
 * export type IndexFromTableName<Version extends IndexedDbVersion.AnyWithProps, Table extends string> = IndexFromTable<IndexedDbTable.WithName<IndexedDbVersion.Tables<Version>, Table>>
 */

// ✓ IsStringLiteral: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsStringLiteral(T) {
  if (matches<string>(T)) {
    if (matches<typeof T>(string)) {
      return false
    }
    return true
  }
  return false
}
/* compiles to:
 * export type IsStringLiteral<T> = T extends string ? string extends T ? false : true : false
 */

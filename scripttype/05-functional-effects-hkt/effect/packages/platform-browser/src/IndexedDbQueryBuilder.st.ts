/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/platform-browser/src/IndexedDbQueryBuilder.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 11 type-level aliases: 10 typecheck-error, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AnySchemaStruct: any
declare const AnyWithProps: any
declare const Delete: any
declare const IDBValidKey: any
declare const IndexFromTable: any
declare const IndexedDbDatabase: any
declare const IndexedDbTable: any
declare const MakeIn: any
declare const NonEmptyReadonlyArray: any
declare const Schema: any
declare const Select: any
declare const Struct: any
type AnySchemaStruct<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyWithProps<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Delete<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IDBValidKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexFromTable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexedDbDatabase<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IndexedDbTable<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MakeIn<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonEmptyReadonlyArray<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Schema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Select<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Struct<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ KeyPath: the ScriptType does not itself typecheck as TypeScript
//   KeyPath.st.ts(2:38) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function KeyPath(TableSchema: IndexedDbTable.AnySchemaStruct) {
  return IndexedDbValidKeys(TableSchema) | NonEmptyReadonlyArray(IndexedDbValidKeys(TableSchema))
}
/* compiles to:
 * export type KeyPath<TableSchema extends IndexedDbTable.AnySchemaStruct> = IndexedDbValidKeys<TableSchema> | NonEmptyReadonlyArray<IndexedDbValidKeys<TableSchema>>
 */

// ✗ KeyPathNumber: the ScriptType does not itself typecheck as TypeScript
//   KeyPathNumber.st.ts(2:44) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function KeyPathNumber(TableSchema: IndexedDbTable.AnySchemaStruct) {
  return IndexedDbValidNumberKeys(TableSchema) | NonEmptyReadonlyArray(IndexedDbValidNumberKeys(TableSchema))
}
/* compiles to:
 * export type KeyPathNumber<TableSchema extends IndexedDbTable.AnySchemaStruct> = IndexedDbValidNumberKeys<TableSchema> | NonEmptyReadonlyArray<IndexedDbValidNumberKeys<TableSchema>>
 */

// ✗ SelectType: uses raw() — language gap, does not count as covered
//   gap: type node TypeQuery
/* @scripttype preserveParamNames */
export function SelectType(Table: IndexedDbTable.AnyWithProps) {
  if (matches<[ undefined ]>([IndexedDbTable.KeyPath(Table)])) {
    return merge(IndexedDbTable.TableSchema(Table)['Type'], { key: readonlyProp(raw('typeof IndexedDb.IDBValidKey')['Type']) })
  }
  return IndexedDbTable.TableSchema(Table)['Type']
}

// ✗ ModifyType: the ScriptType does not itself typecheck as TypeScript
//   ModifyType.st.ts(2:35) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ModifyType(Table: IndexedDbTable.AnyWithProps) {
  const out = emptyObject
  for (const key in keyof(Schema.Struct.MakeIn(Omit(IndexedDbTable.TableSchema(Table)['fields'], IndexedDbTable.KeyPath(Table))))) {
    out[key] = matches<keyof Schema.Struct.MakeIn<IndexedDbTable.TableSchema<typeof Table>["fields"]>>(key) ? Schema.Struct.MakeIn(IndexedDbTable.TableSchema(Table)['fields'])[key] : never
  }
  const out2 = emptyObject
  for (const key in keySet(IndexedDbTable.KeyPath(Table))) {
    out2[key] = optional(anyOf(number, Undefined))
  }
  return (matches<true>(IndexedDbTable.AutoIncrement(Table)) ? (out & out2) : Schema.Struct.MakeIn(IndexedDbTable.TableSchema(Table)['fields'])) & (matches<[ undefined ]>([IndexedDbTable.KeyPath(Table)]) ? { key: IDBValidKey } : {})
}
/* compiles to:
 * export type ModifyType<Table extends IndexedDbTable.AnyWithProps> = (IndexedDbTable.AutoIncrement<Table> extends true ? { [Key in keyof Schema.Struct.MakeIn<Omit<IndexedDbTable.TableSchema<Table>['fields'], IndexedDbTable.KeyPath<Table>>>]: Key extends keyof Schema.Struct.MakeIn<IndexedDbTable.TableSchema<Table>['fields']> ? Schema.Struct.MakeIn<IndexedDbTable.TableSchema<Table>['fields']>[Key] : never } & { [Key1 in IndexedDbTable.KeyPath<Table>]?: number | undefined } : Schema.Struct.MakeIn<IndexedDbTable.TableSchema<Table>['fields']>) & ([IndexedDbTable.KeyPath<Table>] extends [undefined] ? { key: IDBValidKey } : {})
 */

// ✗ EqualsType: the ScriptType does not itself typecheck as TypeScript
//   EqualsType.st.ts(2:35) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function EqualsType(Table: IndexedDbTable.AnyWithProps, Index: keyof (typeof Table)["indexes"], KeyPath = matches<[ never ]>([Index]) ? Table['keyPath'] : Table['indexes'][Index], Type = Table['tableSchema']['Encoded']) {
  if (matches<keyof typeof Type>(KeyPath)) {
    return Type[KeyPath]
  }
  const out = emptyObject
  for (const I in keyof(KeyPath)) {
    out[I] = matches<keyof typeof Type>(KeyPath[I]) ? (anyOf(Type[KeyPath[I]], [])) : never
  }
  return out
}
/* compiles to:
 * export type EqualsType<Table extends IndexedDbTable.AnyWithProps, Index extends keyof Table['indexes'], KeyPath = [Index] extends [never] ? Table['keyPath'] : Table['indexes'][Index], Type = Table['tableSchema']['Encoded']> = KeyPath extends keyof Type ? Type[KeyPath] : { [I in keyof KeyPath]: KeyPath[I] extends keyof Type ? Type[KeyPath[I]] | [] : never }
 */

// ✗ ExtractIndexType: the ScriptType does not itself typecheck as TypeScript
//   ExtractIndexType.st.ts(2:41) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ExtractIndexType(Table: IndexedDbTable.AnyWithProps, Index: keyof (typeof Table)["indexes"], KeyPath = matches<[ never ]>([Index]) ? Table['keyPath'] : Table['indexes'][Index], Type = Table['tableSchema']['Encoded']) {
  if (matches<keyof typeof Type>(KeyPath)) {
    return Type[KeyPath]
  }
  const m1 = matches<readonly [ Hole<"K">, ...Hole<"Rest"> ]>(KeyPath)
  if (m1) {
    if (matches<keyof typeof Type>(m1.K)) {
      const out = emptyObject
      for (const P in keyof(m1.Rest)) {
        out[P] = optional(matches<keyof typeof Type>(m1.Rest[P]) ? (anyOf(Type[m1.Rest[P]], [])) : never)
      }
      return [Type[m1.K], ...out]
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractIndexType<Table extends IndexedDbTable.AnyWithProps, Index extends keyof Table['indexes'], KeyPath = [Index] extends [never] ? Table['keyPath'] : Table['indexes'][Index], Type = Table['tableSchema']['Encoded']> = KeyPath extends keyof Type ? Type[KeyPath] : KeyPath extends readonly [infer K, ...infer Rest] ? K extends keyof Type ? [Type[K], ...{ [P in keyof Rest]?: Rest[P] extends keyof Type ? Type[Rest[P]] | [] : never }] : never : never
 */

// ✗ ModifyWithKey: the ScriptType does not itself typecheck as TypeScript
//   ModifyWithKey.st.ts(2:38) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function ModifyWithKey(Table: IndexedDbTable.AnyWithProps) {
  return ModifyType(Table)
}
/* compiles to:
 * export type ModifyWithKey<Table extends IndexedDbTable.AnyWithProps> = ModifyType<Table>
 */

// ✗ DeleteWithout: the ScriptType does not itself typecheck as TypeScript
//   DeleteWithout.st.ts(2:38) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function DeleteWithout(Table: IndexedDbTable.AnyWithProps, Index: IndexedDbDatabase.IndexFromTable<typeof Table>, ExcludedKeys: string) {
  return Omit(Delete(Table, Index, ExcludedKeys), ExcludedKeys)
}
/* compiles to:
 * export type DeleteWithout<Table extends IndexedDbTable.AnyWithProps, Index extends IndexedDbDatabase.IndexFromTable<Table>, ExcludedKeys extends string> = Omit<Delete<Table, Index, ExcludedKeys>, ExcludedKeys>
 */

// ✗ SelectWithout: the ScriptType does not itself typecheck as TypeScript
//   SelectWithout.st.ts(2:38) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function SelectWithout(Table: IndexedDbTable.AnyWithProps, Index: IndexedDbDatabase.IndexFromTable<typeof Table>, ExcludedKeys: string) {
  return Omit(Select(Table, Index, ExcludedKeys), ExcludedKeys)
}
/* compiles to:
 * export type SelectWithout<Table extends IndexedDbTable.AnyWithProps, Index extends IndexedDbDatabase.IndexFromTable<Table>, ExcludedKeys extends string> = Omit<Select<Table, Index, ExcludedKeys>, ExcludedKeys>
 */

// ✗ IndexedDbValidKeys: the ScriptType does not itself typecheck as TypeScript
//   IndexedDbValidKeys.st.ts(2:49) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IndexedDbValidKeys(TableSchema: IndexedDbTable.AnySchemaStruct) {
  const m1 = matches<Hole<"K">>(keyof(TableSchema['Encoded']))
  if (m1) {
    if (matches<keyof (typeof TableSchema)["Encoded"]>(m1.K)) {
      if (matches<Readonly<IDBValidKey>>(TableSchema['Encoded'][m1.K])) {
        return m1.K
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type IndexedDbValidKeys<TableSchema extends IndexedDbTable.AnySchemaStruct> = keyof TableSchema['Encoded'] extends infer K ? K extends keyof TableSchema['Encoded'] ? TableSchema['Encoded'][K] extends Readonly<IDBValidKey> ? K : never : never : never
 */

// ✗ IndexedDbValidNumberKeys: the ScriptType does not itself typecheck as TypeScript
//   IndexedDbValidNumberKeys.st.ts(2:55) TS2702: 'IndexedDbTable' only refers to a type, but is being used as a namespace here.
/* @scripttype preserveParamNames */
export function IndexedDbValidNumberKeys(TableSchema: IndexedDbTable.AnySchemaStruct) {
  const m1 = matches<Hole<"K">>(keyof(TableSchema['Encoded']))
  if (m1) {
    if (matches<keyof (typeof TableSchema)["Encoded"]>(m1.K)) {
      if (matches<[ number | undefined ]>([TableSchema['Encoded'][m1.K]])) {
        return m1.K
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type IndexedDbValidNumberKeys<TableSchema extends IndexedDbTable.AnySchemaStruct> = keyof TableSchema['Encoded'] extends infer K ? K extends keyof TableSchema['Encoded'] ? [TableSchema['Encoded'][K]] extends [number | undefined] ? K : never : never : never
 */

/**
 * ScriptType for 05-functional-effects-hkt/effect/packages/sql/mssql/src/Procedure.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 1 raw, 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Parameter: any
type Parameter<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✗ ParametersRecord: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ParametersRecord(A: Record<string, Parameter.Parameter<any>>) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = readonlyProp(raw('A[K] extends Parameter.Parameter<infer T> ? T : never'))
  }
  return merge(out, {})
}

// ✓ Simplify: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Simplify(A) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Simplify<A> = { [K in keyof A]: A[K] } & {}
 */

/**
 * ScriptType for 06-state-and-forms/react-hook-form/src/__typetest__/__fixtures__/matchers.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 2 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Expect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Expect(T: true) {
  return T
}
/* compiles to:
 * export type Expect<T extends true> = T
 */

// ✗ Equal: does not compile yet
//   'Y' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function Equal(X, Y) {
  if (matches<<T>() => T extends typeof Y ? 1 : 2>(genericFnType(['T'], [], matches<typeof X>(T) ? 1 : 2))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equal<X, Y> =
 *   (<T>() => T extends X ? 1 : 2) extends <T>() => T extends typeof Y ? 1 : 2 ? true : false
 */

// ✓ NotEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotEqual(X, Y) {
  if (matches<true>(Equal(X, Y))) {
    return false
  }
  return true
}
/* compiles to:
 * export type NotEqual<X, Y> = Equal<X, Y> extends true ? false : true
 */

/**
 * ScriptType for 06-state-and-forms/react-hook-form/src/types/utils.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 12 type-level aliases: 12 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { NestedValue } from '../../../../../06-state-and-forms/react-hook-form/src/types/form.js'
import type { BrowserNativeObject, Primitive } from '../../../../../06-state-and-forms/react-hook-form/src/types/utils.js'
declare namespace m1 {
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BrowserNativeObject: any
declare const FileList: any
declare const G: any
declare const NestedValue: any
declare const Primitive: any
type FileList<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type G<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ NonUndefined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonUndefined(T): any {
  if (typeof T === 'undefined') {
    return never
  }
  return T
}
/* compiles to:
 * export type NonUndefined<T> = T extends undefined ? never : T
 */

// ✓ LiteralUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {typeof U} T
 * @param {Primitive} U
 */
export function LiteralUnion(T, U): any {
  return anyOf(T, merge(U, { _: optional(never) }))
}
/* compiles to:
 * export type LiteralUnion<T extends U, U extends Primitive> = T | U & { _?: never }
 */

// ✓ ExtractObjects: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractObjects(T): any {
  const m1 = matches<Hole<"U">>(T)
  if (m1) {
    if (matches<object>(m1.U)) {
      return m1.U
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractObjects<T> = T extends infer U ? U extends object ? U : never : never
 */

// ✓ IsPrimitiveLike: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsPrimitiveLike(T): any {
  if (matches<Primitive>(T)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsPrimitiveLike<T> = T extends Primitive ? true : false
 */

// ✓ DeepPartial: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepPartial(T): any {
  if (matches<true>(IsPrimitiveLike(T))) {
    return T
  }
  if (matches<BrowserNativeObject | NestedValue>(T)) {
    return T
  }
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = optional(matches<never>(ExtractObjects(T[K])) ? T[K] : DeepPartial(T[K]))
  }
  return out
}
/* compiles to:
 * export type DeepPartial<T> =
 *   IsPrimitiveLike<T> extends true ? T
 *   : T extends BrowserNativeObject | NestedValue ? T
 *   : { [K in keyof T]?: ExtractObjects<T[K]> extends never ? T[K] : DeepPartial<T[K]> }
 */

// ✓ DeepPartialSkipArrayKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepPartialSkipArrayKey(T): any {
  if (matches<true>(IsPrimitiveLike(T))) {
    return T
  }
  if (matches<BrowserNativeObject | NestedValue>(T)) {
    return T
  }
  if (matches<ReadonlyArray<any>>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = DeepPartialSkipArrayKey(T[K])
    }
    return out
  }
  const out2 = emptyObject
  for (const K in keyof(T)) {
    out2[K] = optional(DeepPartialSkipArrayKey(T[K]))
  }
  return out2
}
/* compiles to:
 * export type DeepPartialSkipArrayKey<T> =
 *   IsPrimitiveLike<T> extends true ? T
 *   : T extends BrowserNativeObject | NestedValue ? T
 *   : T extends ReadonlyArray<any> ? { [K in keyof T]: DeepPartialSkipArrayKey<T[K]> }
 *   : { [K1 in keyof T]?: DeepPartialSkipArrayKey<T[K1]> }
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  if (matches<1 & typeof T>(0)) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsAny<T> = 0 extends 1 & T ? true : false
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsEqual: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsEqual(T1, T2): any {
  if (matches<typeof T2>(T1)) {
    if (matches<<G>() => G extends typeof T2 ? 1 : 2>(genericFnType(['G'], [], matches<typeof T1>(G) ? 1 : 2))) {
      return true
    }
    return false
  }
  return false
}
/* compiles to:
 * export type IsEqual<T1, T2> =
 *   T1 extends T2
 *     ? (<G>() => G extends T1 ? 1 : 2) extends <G>() => G extends T2 ? 1 : 2 ? true : false
 *     : false
 */

// ✓ DeepMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DeepMap(T, TValue): any {
  if (matches<true>(IsAny(T))) {
    return any
  }
  if (matches<BrowserNativeObject | NestedValue>(T)) {
    return TValue
  }
  const m1 = matches<ReadonlyArray<Hole<"U">>>(T)
  if (m1) {
    return arrayOf(anyOf(DeepMap(NonUndefined(m1.U), TValue), Undefined))
  }
  if (matches<object>(T)) {
    const out = emptyObject
    for (const K in keyof(T)) {
      out[K] = DeepMap(NonUndefined(T[K]), TValue)
    }
    return out
  }
  return TValue
}
/* compiles to:
 * export type DeepMap<T, TValue> =
 *   IsAny<T> extends true ? any
 *   : T extends BrowserNativeObject | NestedValue ? TValue
 *   : T extends ReadonlyArray<infer U> ? (DeepMap<NonUndefined<U>, TValue> | undefined)[]
 *   : T extends object ? { [K in keyof T]: DeepMap<NonUndefined<T[K]>, TValue> }
 *   : TValue
 */

// ✓ IsFlatObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsFlatObject(T: object): any {
  if (matches<never>(Extract(Exclude(T[keyof(T)], anyOf(NestedValue, t<Date>(), FileList)), anyOf(arrayOf(any), object)))) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsFlatObject<T extends object> =
 *   Extract<Exclude<T[keyof T], NestedValue | Date | FileList>, any[] | object> extends never
 *     ? true
 *     : false
 */

// ✓ Merge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Merge(A, B): any {
  const out = emptyObject
  for (const K in keySet(keyof(A) | keyof(B))) {
    out[K] = optional(matches<keyof typeof A & keyof typeof B>(K) ? (matches<[ object, object ]>([A[K], B[K]]) ? Merge(A[K], B[K]) : B[K]) : (K in A ? A[K] : (K in B ? B[K] : never)))
  }
  return out
}
/* compiles to:
 * export type Merge<A, B> = {
 *   [K in keyof A | keyof B]?: K extends keyof A & keyof B ? [A[K], B[K]] extends [object, object] ? Merge<A[K], B[K]> : B[K]
 *   : K extends keyof A ? A[K]
 *   : K extends keyof B ? B[K]
 *   : never
 * }
 */

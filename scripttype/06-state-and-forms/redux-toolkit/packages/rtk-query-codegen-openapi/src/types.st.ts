/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/rtk-query-codegen-openapi/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 4 type-level aliases: 4 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// ✓ Require: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Require(T, K: keyof typeof T) {
  const out = emptyObject
  for (const k in keySet(K)) {
    out[k] = required(NonNullable(T[k]))
  }
  return out & Omit(T, K)
}
/* compiles to:
 * export type Require<T, K extends keyof T> = { [K1 in K]-?: NonNullable<T[K1]> } & Omit<T, K>
 */

// ✓ Optional: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Optional(T, K: keyof typeof T) {
  const out = emptyObject
  for (const k in keySet(K)) {
    out[k] = optional(NonNullable(T[k]))
  }
  return out & Omit(T, K)
}
/* compiles to:
 * export type Optional<T, K extends keyof T> = { [K1 in K]?: NonNullable<T[K1]> } & Omit<T, K>
 */

// ✓ Id: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Id(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Id<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ AtLeastOneKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AtLeastOneKey(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = required(Pick(T, K) & Partial(T))
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type AtLeastOneKey<T> = { [K in keyof T]-?: Pick<T, K> & Partial<T> }[keyof T]
 */

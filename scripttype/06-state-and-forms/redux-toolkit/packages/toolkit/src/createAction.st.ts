/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/createAction.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 6 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ActionCreatorWithNonInferrablePayload, ActionCreatorWithOptionalPayload, ActionCreatorWithPayload, ActionCreatorWithPreparedPayload, ActionCreatorWithoutPayload } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAction.js'
import type { IfMaybeUndefined, IfVoid, IsUnknownOrNonInferrable } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/tsHelpers.js'
declare namespace m1 {
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type E<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type M<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ActionCreatorWithNonInferrablePayload: any
declare const ActionCreatorWithOptionalPayload: any
declare const ActionCreatorWithPayload: any
declare const ActionCreatorWithPreparedPayload: any
declare const ActionCreatorWithoutPayload: any
declare const IfMaybeUndefined: any
declare const IfVoid: any
declare const IsAny: any
declare const IsUnknownOrNonInferrable: any
type IsAny<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PrepareAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ PayloadAction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PayloadAction(P = voidType(), T: string = string, M = never, E = never): any {
  return merge({ payload: P, type: T }, matches<[ never ]>([M]) ? {} : { meta: M }, matches<[ never ]>([E]) ? {} : { error: E })
}
/* compiles to:
 * export type PayloadAction<P = void, T extends string = string, M = never, E = never> =
 *   & { payload: P; type: T }
 *   & ([M] extends [never] ? {} : { meta: M })
 *   & ([E] extends [never] ? {} : { error: E })
 */

// ✓ PrepareAction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PrepareAction(P): any {
  return fnType([...arrayOf(any)], { payload: P }) | fnType([...arrayOf(any)], { payload: P, meta: any }) | fnType([...arrayOf(any)], { payload: P, error: any }) | fnType([...arrayOf(any)], { payload: P, meta: any, error: any })
}
/* compiles to:
 * export type PrepareAction<P> =
 *   | ((...a0: any[]) => { payload: P })
 *   | ((...a0: any[]) => { payload: P; meta: any })
 *   | ((...a0: any[]) => { payload: P; error: any })
 *   | ((...a0: any[]) => { payload: P; meta: any; error: any })
 */

// ✓ _ActionCreatorWithPreparedPayload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _ActionCreatorWithPreparedPayload(PA: PrepareAction<any> | void, T: string = string): any {
  const m1 = matches<PrepareAction<Hole<"P">>>(PA)
  if (m1) {
    const m2 = matches<{ error: Hole<"E">; }>(ReturnType(PA))
    const m3 = matches<{ meta: Hole<"M">; }>(ReturnType(PA))
    return ActionCreatorWithPreparedPayload(Parameters(PA), m1.P, T, m2 ? m2.E : never, m3 ? m3.M : never)
  }
  return voidType()
}
/* compiles to:
 * export type _ActionCreatorWithPreparedPayload<
 *   PA extends PrepareAction<any> | void,
 *   T extends string = string
 * > =
 *   PA extends PrepareAction<infer P>
 *     ? ActionCreatorWithPreparedPayload<
 *       Parameters<PA>,
 *       P,
 *       T,
 *       ReturnType<PA> extends { error: infer E; } ? E : never,
 *       ReturnType<PA> extends { meta: infer M; } ? M : never
 *     >
 *     : void
 */

// ✓ BaseActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseActionCreator(P, T: string, M = never, E = never): any {
  return { type: T, match: fnType([unknown], paramIs(0, PayloadAction(P, T, M, E))) }
}
/* compiles to:
 * export type BaseActionCreator<P, T extends string, M = never, E = never> = {
 *   type: T
 *   match: (a0: unknown) => a0 is PayloadAction<P, T, M, E>
 * }
 */

// ✓ PayloadActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {PrepareAction<typeof P> | void} PA
 */
export function PayloadActionCreator(P = voidType(), T: string = string, PA = voidType()): any {
  return IfPrepareActionMethodProvided(PA, _ActionCreatorWithPreparedPayload(PA, T), IsAny(P, ActionCreatorWithPayload(any, T), IsUnknownOrNonInferrable(P, ActionCreatorWithNonInferrablePayload(T), IfVoid(P, ActionCreatorWithoutPayload(T), IfMaybeUndefined(P, ActionCreatorWithOptionalPayload(P, T), ActionCreatorWithPayload(P, T))))))
}
/* compiles to:
 * export type PayloadActionCreator<
 *   P = void,
 *   T extends string = string,
 *   PA extends PrepareAction<P> | void = void
 * > =
 *   IfPrepareActionMethodProvided<
 *     PA,
 *     _ActionCreatorWithPreparedPayload<PA, T>,
 *     IsAny<
 *       P,
 *       ActionCreatorWithPayload<any, T>,
 *       IsUnknownOrNonInferrable<
 *         P,
 *         ActionCreatorWithNonInferrablePayload<T>,
 *         IfVoid<
 *           P,
 *           ActionCreatorWithoutPayload<T>,
 *           IfMaybeUndefined<
 *             P,
 *             ActionCreatorWithOptionalPayload<P, T>,
 *             ActionCreatorWithPayload<P, T>
 *           >
 *         >
 *       >
 *     >
 *   >
 */

// ✓ IfPrepareActionMethodProvided: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IfPrepareActionMethodProvided(PA: PrepareAction<any> | void, True, False): any {
  if (matches<(...args: any[]) => any>(PA)) {
    return True
  }
  return False
}
/* compiles to:
 * export type IfPrepareActionMethodProvided<PA extends PrepareAction<any> | void, True, False> =
 *   PA extends (...args: any[]) => any ? True : False
 */

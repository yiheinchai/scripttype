/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/createAsyncThunk.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 25 type-level aliases: 18 covered, 4 raw, 3 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const AbortSignal: any
declare const ActionCreatorWithPreparedPayload: any
declare const AsyncThunkConfig: any
declare const AsyncThunkDispatchConfig: any
declare const Dispatch: any
declare const FallbackIfUnknown: any
declare const FulfillWithMeta: any
declare const FulfilledValue: any
declare const Id: any
declare const IsAny: any
declare const IsUnknown: any
declare const RejectWithValue: any
declare const SafePromise: any
declare const SerializedError: any
declare const ThunkDispatch: any
declare const UnknownAction: any
declare const UnwrappableAction: any
type AbortSignal<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ActionCreatorWithPreparedPayload<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AsyncThunkConfig<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type AsyncThunkDispatchConfig<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Dispatch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FallbackIfUnknown<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FulfillWithMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type FulfilledValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Id<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsAny<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type IsUnknown<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type RejectWithValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type SafePromise<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type SerializedError<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ThunkDispatch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnknownAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnwrappableAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ BaseThunkAPI: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function BaseThunkAPI(S, E, D: Dispatch = Dispatch, RejectedValue = unknown, RejectedMeta = unknown, FulfilledMeta = unknown) {
  return { dispatch: D, getState: fnType([], S), extra: E, requestId: string, signal: AbortSignal, abort: fnType([string], voidType()), rejectWithValue: IsUnknown(RejectedMeta, fnType([RejectedValue], RejectWithValue(RejectedValue, RejectedMeta)), fnType([RejectedValue, RejectedMeta], RejectWithValue(RejectedValue, RejectedMeta))), fulfillWithValue: IsUnknown(FulfilledMeta, genericFnType(['FulfilledValue'], [FulfilledValue], FulfilledValue), genericFnType(['FulfilledValue'], [FulfilledValue, FulfilledMeta], FulfillWithMeta(FulfilledValue, FulfilledMeta))) }
}
/* compiles to:
 * export type BaseThunkAPI<
 *   S,
 *   E,
 *   D extends Dispatch = Dispatch,
 *   RejectedValue = unknown,
 *   RejectedMeta = unknown,
 *   FulfilledMeta = unknown
 * > =
 *   {
 *     dispatch: D
 *     getState: () => S
 *     extra: E
 *     requestId: string
 *     signal: AbortSignal
 *     abort: (a0: string) => void
 *     rejectWithValue: IsUnknown<
 *       RejectedMeta,
 *       (a0: RejectedValue) => RejectWithValue<RejectedValue, RejectedMeta>,
 *       (a0: RejectedValue, a1: RejectedMeta) => RejectWithValue<RejectedValue, RejectedMeta>
 *     >
 *     fulfillWithValue: IsUnknown<
 *       FulfilledMeta,
 *       <FulfilledValue>(a0: FulfilledValue) => FulfilledValue,
 *       <FulfilledValue>(a0: FulfilledValue, a1: FulfilledMeta) => FulfillWithMeta<
 *         FulfilledValue,
 *         FulfilledMeta
 *       >
 *     >
 *   }
 */

// ✓ GetState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetState(ThunkApiConfig) {
  const m1 = matches<{ state: Hole<"State">; }>(ThunkApiConfig)
  if (m1) {
    return m1.State
  }
  return unknown
}
/* compiles to:
 * export type GetState<ThunkApiConfig> =
 *   ThunkApiConfig extends { state: infer State; } ? State : unknown
 */

// ✓ GetExtra: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetExtra(ThunkApiConfig) {
  const m1 = matches<{ extra: Hole<"Extra">; }>(ThunkApiConfig)
  if (m1) {
    return m1.Extra
  }
  return unknown
}
/* compiles to:
 * export type GetExtra<ThunkApiConfig> =
 *   ThunkApiConfig extends { extra: infer Extra; } ? Extra : unknown
 */

// ✓ GetDispatch: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetDispatch(ThunkApiConfig) {
  const m1 = matches<{ dispatch: Hole<"Dispatch">; }>(ThunkApiConfig)
  if (m1) {
    return FallbackIfUnknown(m1.Dispatch, ThunkDispatch(GetState(ThunkApiConfig), GetExtra(ThunkApiConfig), UnknownAction))
  }
  return ThunkDispatch(GetState(ThunkApiConfig), GetExtra(ThunkApiConfig), UnknownAction)
}
/* compiles to:
 * export type GetDispatch<ThunkApiConfig> =
 *   ThunkApiConfig extends { dispatch: infer Dispatch; }
 *     ? FallbackIfUnknown<
 *       Dispatch,
 *       ThunkDispatch<GetState<ThunkApiConfig>, GetExtra<ThunkApiConfig>, UnknownAction>
 *     >
 *     : ThunkDispatch<GetState<ThunkApiConfig>, GetExtra<ThunkApiConfig>, UnknownAction>
 */

// ✓ GetThunkAPI: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetThunkAPI(ThunkApiConfig) {
  return BaseThunkAPI(GetState(ThunkApiConfig), GetExtra(ThunkApiConfig), GetDispatch(ThunkApiConfig), GetRejectValue(ThunkApiConfig), GetRejectedMeta(ThunkApiConfig), GetFulfilledMeta(ThunkApiConfig))
}
/* compiles to:
 * export type GetThunkAPI<ThunkApiConfig> = BaseThunkAPI<
 *   GetState<ThunkApiConfig>,
 *   GetExtra<ThunkApiConfig>,
 *   GetDispatch<ThunkApiConfig>,
 *   GetRejectValue<ThunkApiConfig>,
 *   GetRejectedMeta<ThunkApiConfig>,
 *   GetFulfilledMeta<ThunkApiConfig>
 * >
 */

// ✓ GetRejectValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetRejectValue(ThunkApiConfig) {
  const m1 = matches<{ rejectValue: Hole<"RejectValue">; }>(ThunkApiConfig)
  if (m1) {
    return m1.RejectValue
  }
  return unknown
}
/* compiles to:
 * export type GetRejectValue<ThunkApiConfig> =
 *   ThunkApiConfig extends { rejectValue: infer RejectValue; } ? RejectValue : unknown
 */

// ✓ GetPendingMeta: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetPendingMeta(ThunkApiConfig) {
  const m1 = matches<{ pendingMeta: Hole<"PendingMeta">; }>(ThunkApiConfig)
  if (m1) {
    return m1.PendingMeta
  }
  return unknown
}
/* compiles to:
 * export type GetPendingMeta<ThunkApiConfig> =
 *   ThunkApiConfig extends { pendingMeta: infer PendingMeta; } ? PendingMeta : unknown
 */

// ✓ GetFulfilledMeta: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetFulfilledMeta(ThunkApiConfig) {
  const m1 = matches<{ fulfilledMeta: Hole<"FulfilledMeta">; }>(ThunkApiConfig)
  if (m1) {
    return m1.FulfilledMeta
  }
  return unknown
}
/* compiles to:
 * export type GetFulfilledMeta<ThunkApiConfig> =
 *   ThunkApiConfig extends { fulfilledMeta: infer FulfilledMeta; } ? FulfilledMeta : unknown
 */

// ✓ GetRejectedMeta: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetRejectedMeta(ThunkApiConfig) {
  const m1 = matches<{ rejectedMeta: Hole<"RejectedMeta">; }>(ThunkApiConfig)
  if (m1) {
    return m1.RejectedMeta
  }
  return unknown
}
/* compiles to:
 * export type GetRejectedMeta<ThunkApiConfig> =
 *   ThunkApiConfig extends { rejectedMeta: infer RejectedMeta; } ? RejectedMeta : unknown
 */

// ✓ GetSerializedErrorType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetSerializedErrorType(ThunkApiConfig) {
  const m1 = matches<{ serializedErrorType: Hole<"GetSerializedErrorType">; }>(ThunkApiConfig)
  if (m1) {
    return m1.GetSerializedErrorType
  }
  return SerializedError
}
/* compiles to:
 * export type GetSerializedErrorType<ThunkApiConfig> =
 *   ThunkApiConfig extends { serializedErrorType: infer GetSerializedErrorType; }
 *     ? GetSerializedErrorType
 *     : SerializedError
 */

// ✓ MaybePromise: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybePromise(T) {
  return T | t<Promise<typeof T>>() | (matches<any>(T) ? t<Promise<typeof T>>() : never)
}
/* compiles to:
 * export type MaybePromise<T> = T | Promise<T> | (T extends any ? Promise<T> : never)
 */

// ✓ AsyncThunkPayloadCreatorReturnValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkPayloadCreatorReturnValue(Returned, ThunkApiConfig: AsyncThunkConfig) {
  return MaybePromise(IsUnknown(GetFulfilledMeta(ThunkApiConfig), Returned, FulfillWithMeta(Returned, GetFulfilledMeta(ThunkApiConfig))) | RejectWithValue(GetRejectValue(ThunkApiConfig), GetRejectedMeta(ThunkApiConfig)))
}
/* compiles to:
 * export type AsyncThunkPayloadCreatorReturnValue<
 *   Returned,
 *   ThunkApiConfig extends AsyncThunkConfig
 * > =
 *   MaybePromise<
 *     | IsUnknown<
 *         GetFulfilledMeta<ThunkApiConfig>,
 *         Returned,
 *         FulfillWithMeta<Returned, GetFulfilledMeta<ThunkApiConfig>>
 *       >
 *     | RejectWithValue<GetRejectValue<ThunkApiConfig>, GetRejectedMeta<ThunkApiConfig>>
 *   >
 */

// ✓ AsyncThunkPayloadCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkPayloadCreator(Returned, ThunkArg = voidType(), ThunkApiConfig: AsyncThunkConfig = {}) {
  return fnType([ThunkArg, GetThunkAPI(ThunkApiConfig)], AsyncThunkPayloadCreatorReturnValue(Returned, ThunkApiConfig))
}
/* compiles to:
 * export type AsyncThunkPayloadCreator<
 *   Returned,
 *   ThunkArg = void,
 *   ThunkApiConfig extends AsyncThunkConfig = {}
 * > =
 *   (a0: ThunkArg, a1: GetThunkAPI<ThunkApiConfig>) => AsyncThunkPayloadCreatorReturnValue<
 *     Returned,
 *     ThunkApiConfig
 *   >
 */

// ✗ AsyncThunkAction: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig: AsyncThunkConfig) {
  return fnType([NonNullable(GetDispatch(ThunkApiConfig)), fnType([], GetState(ThunkApiConfig)), GetExtra(ThunkApiConfig)], merge(SafePromise(ReturnType(AsyncThunkFulfilledActionCreator(Returned, ThunkArg)) | ReturnType(AsyncThunkRejectedActionCreator(ThunkArg, ThunkApiConfig))), { abort: fnType([string], voidType()), requestId: string, arg: ThunkArg, unwrap: fnType([], t<Promise<typeof Returned>>()) }))
}
/* compiles to:
 * export type AsyncThunkAction<Returned, ThunkArg, ThunkApiConfig extends AsyncThunkConfig> =
 *   (
 *     a0: NonNullable<GetDispatch<ThunkApiConfig>>,
 *     a1: () => GetState<ThunkApiConfig>,
 *     a2: GetExtra<ThunkApiConfig>
 *   ) => & SafePromise<
 *       | ReturnType<AsyncThunkFulfilledActionCreator<Returned, ThunkArg>>
 *       | ReturnType<AsyncThunkRejectedActionCreator<ThunkArg, ThunkApiConfig>>
 *     >
 *   & {
 *       abort: (a0: string) => void
 *       requestId: string
 *       arg: ThunkArg
 *       unwrap: () => Promise<Returned>
 *     }
 */

// ✗ AsyncThunkActionCreator: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function AsyncThunkActionCreator(Returned, ThunkArg, ThunkApiConfig: AsyncThunkConfig) {
  return IsAny(ThunkArg, fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig)), matches<typeof ThunkArg>(unknown) ? fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig)) : (matches<[ void ] | [ undefined ]>([ThunkArg]) ? fnType([Undefined, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig)) : (matches<[ typeof ThunkArg ]>([voidType()]) ? fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig)) : (matches<[ typeof ThunkArg ]>([Undefined]) ? WithStrictNullChecks(fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig)), fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig))) : fnType([ThunkArg, AsyncThunkDispatchConfig], AsyncThunkAction(Returned, ThunkArg, ThunkApiConfig))))))
}
/* compiles to:
 * export type AsyncThunkActionCreator<
 *   Returned,
 *   ThunkArg,
 *   ThunkApiConfig extends AsyncThunkConfig
 * > =
 *   IsAny<
 *     ThunkArg,
 *     (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *       Returned,
 *       ThunkArg,
 *       ThunkApiConfig
 *     >,
 *     unknown extends ThunkArg
 *       ? (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *         Returned,
 *         ThunkArg,
 *         ThunkApiConfig
 *       >
 *     : [ThunkArg] extends [void] | [undefined]
 *       ? (a0: undefined, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *         Returned,
 *         ThunkArg,
 *         ThunkApiConfig
 *       >
 *     : [void] extends [ThunkArg]
 *       ? (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *         Returned,
 *         ThunkArg,
 *         ThunkApiConfig
 *       >
 *     : [undefined] extends [ThunkArg]
 *       ? WithStrictNullChecks<
 *         (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *           Returned,
 *           ThunkArg,
 *           ThunkApiConfig
 *         >,
 *         (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *           Returned,
 *           ThunkArg,
 *           ThunkApiConfig
 *         >
 *       >
 *     : (a0: ThunkArg, a1: AsyncThunkDispatchConfig) => AsyncThunkAction<
 *       Returned,
 *       ThunkArg,
 *       ThunkApiConfig
 *     >
 *   >
 */

// ✗ AsyncThunkOptions: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function AsyncThunkOptions(ThunkArg = voidType(), ThunkApiConfig: AsyncThunkConfig = {}) {
  return merge(raw('{ condition?(arg: ThunkArg, api: Pick<GetThunkAPI<ThunkApiConfig>, \'getState\' | \'extra\'>): MaybePromise<boolean | undefined>; dispatchConditionRejection?: boolean; serializeError?: (x: unknown) => GetSerializedErrorType<ThunkApiConfig>; idGenerator?: (arg: ThunkArg) => string; }'), IsUnknown(GetPendingMeta(ThunkApiConfig), raw('{ getPendingMeta?(base: { arg: ThunkArg; requestId: string; }, api: Pick<GetThunkAPI<ThunkApiConfig>, \'getState\' | \'extra\'>): GetPendingMeta<ThunkApiConfig>; }'), raw('{ getPendingMeta(base: { arg: ThunkArg; requestId: string; }, api: Pick<GetThunkAPI<ThunkApiConfig>, \'getState\' | \'extra\'>): GetPendingMeta<ThunkApiConfig>; }')))
}

// ✓ AsyncThunkPendingActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkPendingActionCreator(ThunkArg, ThunkApiConfig = {}) {
  return ActionCreatorWithPreparedPayload([string, ThunkArg, optElem(GetPendingMeta(ThunkApiConfig))], Undefined, string, never, merge({ arg: ThunkArg, requestId: string, requestStatus: 'pending' }, GetPendingMeta(ThunkApiConfig)))
}
/* compiles to:
 * export type AsyncThunkPendingActionCreator<ThunkArg, ThunkApiConfig = {}> = ActionCreatorWithPreparedPayload<
 *   [string, ThunkArg, GetPendingMeta<ThunkApiConfig>?],
 *   undefined,
 *   string,
 *   never,
 *   & { arg: ThunkArg; requestId: string; requestStatus: 'pending' }
 *   & GetPendingMeta<ThunkApiConfig>
 * >
 */

// ✓ AsyncThunkRejectedActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkRejectedActionCreator(ThunkArg, ThunkApiConfig = {}) {
  const out = emptyObject
  for (const K in keyof(GetRejectedMeta(ThunkApiConfig))) {
    out[K] = optional(Undefined)
  }
  return ActionCreatorWithPreparedPayload([t<Error>() | Null, string, ThunkArg, optElem(GetRejectValue(ThunkApiConfig)), optElem(GetRejectedMeta(ThunkApiConfig))], anyOf(GetRejectValue(ThunkApiConfig), Undefined), string, GetSerializedErrorType(ThunkApiConfig), merge({ arg: ThunkArg, requestId: string, requestStatus: 'rejected', aborted: boolean, condition: boolean }, anyOf(merge({ rejectedWithValue: false }, out), merge({ rejectedWithValue: true }, GetRejectedMeta(ThunkApiConfig)))))
}
/* compiles to:
 * export type AsyncThunkRejectedActionCreator<ThunkArg, ThunkApiConfig = {}> = ActionCreatorWithPreparedPayload<
 *   [
 *     Error | null,
 *     string,
 *     ThunkArg,
 *     GetRejectValue<ThunkApiConfig>?,
 *     GetRejectedMeta<ThunkApiConfig>?
 *   ],
 *   GetRejectValue<ThunkApiConfig> | undefined,
 *   string,
 *   GetSerializedErrorType<ThunkApiConfig>,
 *   & {
 *       arg: ThunkArg
 *       requestId: string
 *       requestStatus: 'rejected'
 *       aborted: boolean
 *       condition: boolean
 *     }
 *   & (
 *       | & { rejectedWithValue: false }
 *         & { [K in keyof GetRejectedMeta<ThunkApiConfig>]?: undefined }
 *       | { rejectedWithValue: true } & GetRejectedMeta<ThunkApiConfig>
 *     )
 * >
 */

// ✓ AsyncThunkFulfilledActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkFulfilledActionCreator(Returned, ThunkArg, ThunkApiConfig = {}) {
  return ActionCreatorWithPreparedPayload([Returned, string, ThunkArg, optElem(GetFulfilledMeta(ThunkApiConfig))], Returned, string, never, merge({ arg: ThunkArg, requestId: string, requestStatus: 'fulfilled' }, GetFulfilledMeta(ThunkApiConfig)))
}
/* compiles to:
 * export type AsyncThunkFulfilledActionCreator<Returned, ThunkArg, ThunkApiConfig = {}> = ActionCreatorWithPreparedPayload<
 *   [Returned, string, ThunkArg, GetFulfilledMeta<ThunkApiConfig>?],
 *   Returned,
 *   string,
 *   never,
 *   & { arg: ThunkArg; requestId: string; requestStatus: 'fulfilled' }
 *   & GetFulfilledMeta<ThunkApiConfig>
 * >
 */

// ✗ AsyncThunk: uses raw() — language gap, does not count as covered
//   gap: type node FirstTypeNode
/* @scripttype preserveParamNames */
export function AsyncThunk(Returned, ThunkArg, ThunkApiConfig: AsyncThunkConfig) {
  return merge(AsyncThunkActionCreator(Returned, ThunkArg, ThunkApiConfig), { pending: AsyncThunkPendingActionCreator(ThunkArg, ThunkApiConfig), rejected: AsyncThunkRejectedActionCreator(ThunkArg, ThunkApiConfig), fulfilled: AsyncThunkFulfilledActionCreator(Returned, ThunkArg, ThunkApiConfig), settled: fnType([any], raw('action is ReturnType<AsyncThunkRejectedActionCreator<ThunkArg, ThunkApiConfig> | AsyncThunkFulfilledActionCreator<Returned, ThunkArg, ThunkApiConfig>>')), typePrefix: string })
}

// ✓ OverrideThunkApiConfigs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OverrideThunkApiConfigs(OldConfig, NewConfig) {
  return Id(NewConfig & Omit(OldConfig, keyof(NewConfig)))
}
/* compiles to:
 * export type OverrideThunkApiConfigs<OldConfig, NewConfig> = Id<
 *   NewConfig & Omit<OldConfig, keyof NewConfig>
 * >
 */

// ✗ CreateAsyncThunkFunction: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature
/* @scripttype preserveParamNames */
export function CreateAsyncThunkFunction(CurriedThunkApiConfig: AsyncThunkConfig) {
  return raw('{ <Returned, ThunkArg = void>(typePrefix: string, payloadCreator: AsyncThunkPayloadCreator<Returned, ThunkArg, CurriedThunkApiConfig>, options?: AsyncThunkOptions<ThunkArg, CurriedThunkApiConfig>): AsyncThunk<Returned, ThunkArg, CurriedThunkApiConfig>; <Returned, ThunkArg, ThunkApiConfig extends AsyncThunkConfig>(typePrefix: string, payloadCreator: AsyncThunkPayloadCreator<Returned, ThunkArg, OverrideThunkApiConfigs<CurriedThunkApiConfig, ThunkApiConfig>>, options?: AsyncThunkOptions<ThunkArg, OverrideThunkApiConfigs<CurriedThunkApiConfig, ThunkApiConfig>>): AsyncThunk<Returned, ThunkArg, OverrideThunkApiConfigs<CurriedThunkApiConfig, ThunkApiConfig>>; }')
}

// ✗ CreateAsyncThunk: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function CreateAsyncThunk(CurriedThunkApiConfig: AsyncThunkConfig) {
  return merge(CreateAsyncThunkFunction(CurriedThunkApiConfig), raw('{ withTypes<ThunkApiConfig extends AsyncThunkConfig>(): CreateAsyncThunk<OverrideThunkApiConfigs<CurriedThunkApiConfig, ThunkApiConfig>>; }'))
}

// ✓ UnwrappedActionPayload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrappedActionPayload(T: UnwrappableAction) {
  return Exclude(T, { error: any })['payload']
}
/* compiles to:
 * export type UnwrappedActionPayload<T extends UnwrappableAction> = Exclude<T, { error: any }>['payload']
 */

// ✓ WithStrictNullChecks: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithStrictNullChecks(True, False) {
  if (typeof Undefined === 'boolean') {
    return False
  }
  return True
}
/* compiles to:
 * export type WithStrictNullChecks<True, False> = undefined extends boolean ? False : True
 */

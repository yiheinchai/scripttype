/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/createReducer.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 8 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { TypeGuard } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/tsHelpers.js'
declare const Action: any
declare const Draft: any
declare const Reducer: any
declare const TypeGuard: any
declare const UnknownAction: any
type Action<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Draft<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type NotFunction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Reducer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnknownAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Actions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Actions(T: keyof any = string): any {
  return Record(T, Action)
}
/* compiles to:
 * export type Actions<T extends keyof any = string> = Record<T, Action>
 */

// ✓ ActionMatcherDescription: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionMatcherDescription(S, A: Action): any {
  return { matcher: TypeGuard(A), reducer: CaseReducer(S, NoInfer(A)) }
}
/* compiles to:
 * export type ActionMatcherDescription<S, A extends Action> = {
 *   matcher: TypeGuard<A>
 *   reducer: CaseReducer<S, NoInfer<A>>
 * }
 */

// ✓ ReadonlyActionMatcherDescriptionCollection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReadonlyActionMatcherDescriptionCollection(S): any {
  return readonlyArrayOf(ActionMatcherDescription(S, any))
}
/* compiles to:
 * export type ReadonlyActionMatcherDescriptionCollection<S> = readonly ActionMatcherDescription<S, any>[]
 */

// ✓ ActionMatcherDescriptionCollection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionMatcherDescriptionCollection(S): any {
  return arrayOf(ActionMatcherDescription(S, any))
}
/* compiles to:
 * export type ActionMatcherDescriptionCollection<S> = ActionMatcherDescription<S, any>[]
 */

// ✓ CaseReducer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducer(S = any, A: Action = UnknownAction): any {
  return fnType([Draft(S), A], NoInfer(S) | voidType() | Draft(NoInfer(S)))
}
/* compiles to:
 * export type CaseReducer<S = any, A extends Action = UnknownAction> =
 *   (a0: Draft<S>, a1: A) => NoInfer<S> | void | Draft<NoInfer<S>>
 */

// ✓ CaseReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Actions} AS
 */
export function CaseReducers(S, AS): any {
  const out = emptyObject
  for (const T in keyof(AS)) {
    out[T] = matches<Action>(AS[T]) ? CaseReducer(S, AS[T]) : voidType()
  }
  return out
}
/* compiles to:
 * export type CaseReducers<S, AS extends Actions> = {
 *   [T in keyof AS]: AS[T] extends Action ? CaseReducer<S, AS[T]> : void
 * }
 */

// ✓ NotFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NotFunction(T): any {
  if (matches<Function>(T)) {
    return never
  }
  return T
}
/* compiles to:
 * export type NotFunction<T> = T extends Function ? never : T
 */

// ✓ ReducerWithInitialState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReducerWithInitialState(S: NotFunction<any>): any {
  return merge(Reducer(S), { getInitialState: fnType([], S) })
}
/* compiles to:
 * export type ReducerWithInitialState<S extends NotFunction<any>> =
 *   Reducer<S> & { getInitialState: () => S }
 */

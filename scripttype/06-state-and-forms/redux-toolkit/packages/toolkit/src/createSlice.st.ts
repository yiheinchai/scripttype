/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/createSlice.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 18 type-level aliases: 12 covered, 5 raw, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ActionCreatorWithoutPayload: any
declare const AsyncThunkConfig: any
declare const AsyncThunkOptions: any
declare const AsyncThunkPayloadCreator: any
declare const AsyncThunkReducers: any
declare const CaseReducer: any
declare const InjectConfig: any
declare const PayloadAction: any
declare const PayloadActionCreator: any
declare const PrepareAction: any
declare const ReducerType: any
declare const Selector: any
declare const Slice: any
declare const UnknownAction: any
declare const _ActionCreatorWithPreparedPayload: any
type ActionCreatorWithoutPayload<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncThunkConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncThunkOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncThunkPayloadCreator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AsyncThunkReducers<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type CaseReducer<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InjectConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PayloadAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PayloadActionCreator<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PrepareAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ReducerType<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Selector<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Slice<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SliceCaseReducers<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SliceSelectors<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type _ActionCreatorWithPreparedPayload<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ InjectIntoConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InjectIntoConfig(NewReducerPath: string) {
  return merge(InjectConfig, { reducerPath: optional(NewReducerPath) })
}
/* compiles to:
 * export type InjectIntoConfig<NewReducerPath extends string> = InjectConfig & { reducerPath?: NewReducerPath }
 */

// ✗ InjectedSlice: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function InjectedSlice(State = any, CaseReducers: SliceCaseReducers<typeof State> = SliceCaseReducers(State), Name: string = string, ReducerPath: string = Name, Selectors: SliceSelectors<typeof State> = SliceSelectors(State)) {
  return merge(Omit(Slice(State, CaseReducers, Name, ReducerPath, Selectors), anyOf('getSelectors', 'selectors')), raw('{ getSelectors(): Id<SliceDefinedSelectors<State, Selectors, State | undefined>>; getSelectors<RootState>(selectState: (rootState: RootState) => State | undefined): Id<SliceDefinedSelectors<State, Selectors, RootState>>; get selectors(): Id<SliceDefinedSelectors<State, Selectors, { [K in ReducerPath]?: State | undefined; }>>; selectSlice(state: { [K in ReducerPath]?: State | undefined; }): State; }'))
}

// ✓ ReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReducerDefinition(T: ReducerType = ReducerType) {
  return { _reducerDefinitionType: T }
}
/* compiles to:
 * export type ReducerDefinition<T extends ReducerType = ReducerType> = { _reducerDefinitionType: T }
 */

// ✓ CaseReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducerDefinition(S = any, A: Action = UnknownAction) {
  return CaseReducer(S, A) & ReducerDefinition(ReducerType.reducer)
}
/* compiles to:
 * export type CaseReducerDefinition<S = any, A extends Action = UnknownAction> = CaseReducer<S, A> & ReducerDefinition<ReducerType.reducer>
 */

// ✓ CaseReducerWithPrepare: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducerWithPrepare(State, Action: PayloadAction) {
  return { reducer: CaseReducer(State, Action), prepare: PrepareAction(Action['payload']) }
}
/* compiles to:
 * export type CaseReducerWithPrepare<State, Action extends PayloadAction> = { reducer: CaseReducer<State, Action>; prepare: PrepareAction<Action['payload']> }
 */

// ✓ AsyncThunkSliceReducerConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkSliceReducerConfig(State, ThunkArg: any, Returned = unknown, ThunkApiConfig: AsyncThunkConfig = {}) {
  return merge(AsyncThunkReducers(State, ThunkArg, Returned, ThunkApiConfig), { options: optional(AsyncThunkOptions(ThunkArg, ThunkApiConfig)) })
}
/* compiles to:
 * export type AsyncThunkSliceReducerConfig<State, ThunkArg extends any, Returned = unknown, ThunkApiConfig extends AsyncThunkConfig = {}> = AsyncThunkReducers<State, ThunkArg, Returned, ThunkApiConfig> & { options?: AsyncThunkOptions<ThunkArg, ThunkApiConfig> }
 */

// ✓ AsyncThunkSliceReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkSliceReducerDefinition(State, ThunkArg: any, Returned = unknown, ThunkApiConfig: AsyncThunkConfig = {}) {
  return merge(AsyncThunkSliceReducerConfig(State, ThunkArg, Returned, ThunkApiConfig), ReducerDefinition(ReducerType.asyncThunk), { payloadCreator: AsyncThunkPayloadCreator(Returned, ThunkArg, ThunkApiConfig) })
}
/* compiles to:
 * export type AsyncThunkSliceReducerDefinition<State, ThunkArg extends any, Returned = unknown, ThunkApiConfig extends AsyncThunkConfig = {}> = AsyncThunkSliceReducerConfig<State, ThunkArg, Returned, ThunkApiConfig> & ReducerDefinition<ReducerType.asyncThunk> & { payloadCreator: AsyncThunkPayloadCreator<Returned, ThunkArg, ThunkApiConfig> }
 */

// ✓ PreventCircular: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PreventCircular(ThunkApiConfig) {
  const out = emptyObject
  for (const K in keyof(ThunkApiConfig)) {
    out[K] = matches<'state' | 'dispatch'>(K) ? never : ThunkApiConfig[K]
  }
  return out
}
/* compiles to:
 * export type PreventCircular<ThunkApiConfig> = { [K in keyof ThunkApiConfig]: K extends 'state' | 'dispatch' ? never : ThunkApiConfig[K] }
 */

// ✓ SliceCaseReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SliceCaseReducers(State) {
  return Record(string, ReducerDefinition) | Record(string, CaseReducer(State, PayloadAction(any)) | CaseReducerWithPrepare(State, PayloadAction(any, string, any, any)))
}
/* compiles to:
 * export type SliceCaseReducers<State> = Record<string, ReducerDefinition> | Record<string, CaseReducer<State, PayloadAction<any>> | CaseReducerWithPrepare<State, PayloadAction<any, string, any, any>>>
 */

// ✗ SliceSelectors: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function SliceSelectors(State) {
  return raw('{ [K: string]: (sliceState: State, ...args: any[]) => any; }')
}

// ✗ SliceActionType: the ScriptType does not itself typecheck as TypeScript
//   SliceActionType.st.ts(4:28) TS2731: Implicit conversion of a 'symbol' to a 'string' will fail at runtime. Consider wrapping this expression in 'String(...)'.
/* @scripttype preserveParamNames */
export function SliceActionType(SliceName: string, ActionName: keyof any) {
  if (matches<string | number>(ActionName)) {
    return `${SliceName}/${ActionName}`
  }
  return string
}
/* compiles to:
 * export type SliceActionType<SliceName extends string, ActionName extends keyof any> = ActionName extends string | number ? `${SliceName}/${ActionName}` : string
 */

// ✗ CaseReducerActions: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function CaseReducerActions(CaseReducers: SliceCaseReducers<any>, SliceName: string) {
  const out = emptyObject
  for (const Type in keyof(CaseReducers)) {
    out[Type] = raw('CaseReducers[Type] extends infer Definition ? Definition extends { prepare: any; } ? ActionCreatorForCaseReducerWithPrepare<Definition, SliceActionType<SliceName, Type>> : Definition extends AsyncThunkSliceReducerDefinition<any, infer ThunkArg, infer Returned, infer ThunkApiConfig> ? AsyncThunk<Returned, ThunkArg, ThunkApiConfig> : Definition extends { reducer: any; } ? ActionCreatorForCaseReducer<Definition[\'reducer\'], SliceActionType<SliceName, Type>> : ActionCreatorForCaseReducer<Definition, SliceActionType<SliceName, Type>> : never')
  }
  return out
}

// ✓ ActionCreatorForCaseReducerWithPrepare: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionCreatorForCaseReducerWithPrepare(CR: { prepare: any; }, Type: string) {
  return _ActionCreatorWithPreparedPayload(CR['prepare'], Type)
}
/* compiles to:
 * export type ActionCreatorForCaseReducerWithPrepare<CR extends { prepare: any; }, Type extends string> = _ActionCreatorWithPreparedPayload<CR['prepare'], Type>
 */

// ✓ ActionCreatorForCaseReducer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionCreatorForCaseReducer(CR, Type: string) {
  const m1 = matches<(state: any, action: Hole<"Action">) => any>(CR)
  if (m1) {
    const m2 = matches<{ payload: Hole<"P">; }>(m1.Action)
    if (m2) {
      return PayloadActionCreator(m2.P, Type)
    }
    return ActionCreatorWithoutPayload(Type)
  }
  return ActionCreatorWithoutPayload(Type)
}
/* compiles to:
 * export type ActionCreatorForCaseReducer<CR, Type extends string> = CR extends (state: any, action: infer Action) => any ? Action extends { payload: infer P; } ? PayloadActionCreator<P, Type> : ActionCreatorWithoutPayload<Type> : ActionCreatorWithoutPayload<Type>
 */

// ✗ SliceDefinedCaseReducers: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function SliceDefinedCaseReducers(CaseReducers: SliceCaseReducers<any>) {
  const out = emptyObject
  for (const Type in keyof(CaseReducers)) {
    out[Type] = raw('CaseReducers[Type] extends infer Definition ? Definition extends AsyncThunkSliceReducerDefinition<any, any, any> ? Id<Pick<Required<Definition>, \'fulfilled\' | \'rejected\' | \'pending\' | \'settled\'>> : Definition extends { reducer: infer Reducer; } ? Reducer : Definition : never')
  }
  return out
}

// ✓ RemappedSelector: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemappedSelector(S: Selector, NewState) {
  const m1 = matches<Selector<any, Hole<"R">, Hole<"P">>>(S)
  if (m1) {
    return merge(Selector(NewState, m1.R, m1.P), { unwrapped: S })
  }
  return never
}
/* compiles to:
 * export type RemappedSelector<S extends Selector, NewState> = S extends Selector<any, infer R, infer P> ? Selector<NewState, R, P> & { unwrapped: S } : never
 */

// ✓ SliceDefinedSelectors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SliceDefinedSelectors(State, Selectors: SliceSelectors<typeof State>, RootState) {
  const out = emptyObject
  for (const K in keyof(Selectors)) {
    out[matches<typeof K>(string) ? never : K] = RemappedSelector(Selectors[K], RootState)
  }
  return out
}
/* compiles to:
 * export type SliceDefinedSelectors<State, Selectors extends SliceSelectors<State>, RootState> = { [K in keyof Selectors as string extends K ? never : K]: RemappedSelector<Selectors[K], RootState> }
 */

// ✗ ValidateSliceCaseReducers: uses raw() — language gap, does not count as covered
//   gap: pattern bindings inside a mapped-type value
/* @scripttype preserveParamNames */
export function ValidateSliceCaseReducers(S, ACR: SliceCaseReducers<typeof S>) {
  const out = emptyObject
  for (const T in keyof(ACR)) {
    out[T] = raw('ACR[T] extends { reducer(s: S, action?: infer A): any; } ? { prepare(...a: never[]): Omit<A, \'type\'>; } : {}')
  }
  return merge(ACR, out)
}

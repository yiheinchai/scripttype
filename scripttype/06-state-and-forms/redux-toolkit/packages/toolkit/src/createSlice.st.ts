/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/createSlice.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 18 type-level aliases: 17 covered, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { InjectConfig } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/combineSlices.js'
import type { ActionCreatorWithoutPayload, PayloadAction, PayloadActionCreator, PrepareAction, _ActionCreatorWithPreparedPayload } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAction.js'
import type { AsyncThunk, AsyncThunkConfig, AsyncThunkOptions, AsyncThunkPayloadCreator } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAsyncThunk.js'
import type { CaseReducer } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createReducer.js'
import type { Slice } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createSlice.js'
import type { AsyncThunkReducers } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/mapBuilders.js'
declare namespace ReducerType {
  export type asyncThunk<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type reducer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type A<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Action<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Definition<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type P<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Reducer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type Returned<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type ThunkApiConfig<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type ThunkArg<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ActionCreatorWithoutPayload: any
declare const AsyncThunk: any
declare const AsyncThunkConfig: any
declare const AsyncThunkOptions: any
declare const AsyncThunkPayloadCreator: any
declare const AsyncThunkReducers: any
declare const CaseReducer: any
declare const Id: any
declare const InjectConfig: any
declare const PayloadAction: any
declare const PayloadActionCreator: any
declare const PrepareAction: any
declare const ReducerType: any
declare const Selector: any
declare const Slice: any
declare const UnknownAction: any
declare const _ActionCreatorWithPreparedPayload: any
declare const asyncThunk: any
declare const reducer: any
type AsyncThunkSliceReducerDefinition<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Id<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type ReducerType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Selector<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnknownAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type asyncThunk<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type reducer<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ InjectIntoConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InjectIntoConfig(NewReducerPath: string): any {
  return merge(InjectConfig, { reducerPath: optional(NewReducerPath) })
}
/* compiles to:
 * export type InjectIntoConfig<NewReducerPath extends string> =
 *   InjectConfig & { reducerPath?: NewReducerPath }
 */

// ✓ InjectedSlice: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {SliceCaseReducers<typeof State>} CaseReducers
 * @param {SliceSelectors<typeof State>} Selectors
 */
export function InjectedSlice(State = any, CaseReducers = SliceCaseReducers(State), Name: string = string, ReducerPath: string = Name, Selectors = SliceSelectors(State)): any {
  const out = emptyObject
  for (const K in keySet(ReducerPath)) {
    out[K] = optional(anyOf(State, Undefined))
  }
  const out2 = emptyObject
  for (const K in keySet(ReducerPath)) {
    out2[K] = optional(anyOf(State, Undefined))
  }
  return merge(Omit(Slice(State, CaseReducers, Name, ReducerPath, Selectors), anyOf('getSelectors', 'selectors')), { ...methodType('getSelectors', [], Id(SliceDefinedSelectors(State, Selectors, anyOf(State, Undefined)))), ...genericMethodType('getSelectors', ['RootState'], [fnType([RootState], anyOf(State, Undefined))], Id(SliceDefinedSelectors(State, Selectors, RootState))), selectors: readonlyProp(Id(SliceDefinedSelectors(State, Selectors, out))), selectSlice: methodType([out2], State) })
}
/* compiles to:
 * export type InjectedSlice<
 *   State = any,
 *   CaseReducers extends SliceCaseReducers<State> = SliceCaseReducers<State>,
 *   Name extends string = string,
 *   ReducerPath extends string = Name,
 *   Selectors extends SliceSelectors<State> = SliceSelectors<State>
 * > =
 *   & Omit<Slice<State, CaseReducers, Name, ReducerPath, Selectors>, 'getSelectors' | 'selectors'>
 *   & {
 *       getSelectors(): Id<SliceDefinedSelectors<State, Selectors, State | undefined>>
 *       getSelectors<RootState>(a0: (a0: RootState) => State | undefined): Id<
 *         SliceDefinedSelectors<State, Selectors, RootState>
 *       >
 *       readonly selectors: Id<
 *         SliceDefinedSelectors<State, Selectors, { [K in ReducerPath]?: State | undefined }>
 *       >
 *       selectSlice(a0: { [K1 in ReducerPath]?: State | undefined }): State
 *     }
 */

// ✓ ReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReducerDefinition(T: ReducerType = ReducerType): any {
  return { _reducerDefinitionType: T }
}
/* compiles to:
 * export type ReducerDefinition<T extends ReducerType = ReducerType> = {
 *   _reducerDefinitionType: T
 * }
 */

// ✓ CaseReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducerDefinition(S = any, A: Action = UnknownAction): any {
  return CaseReducer(S, A) & ReducerDefinition(t<ReducerType.reducer>())
}
/* compiles to:
 * export type CaseReducerDefinition<S = any, A extends Action = UnknownAction> =
 *   CaseReducer<S, A> & ReducerDefinition<ReducerType.reducer>
 */

// ✓ CaseReducerWithPrepare: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducerWithPrepare(State, Action: PayloadAction): any {
  return { reducer: CaseReducer(State, Action), prepare: PrepareAction(Action['payload']) }
}
/* compiles to:
 * export type CaseReducerWithPrepare<State, Action extends PayloadAction> = {
 *   reducer: CaseReducer<State, Action>
 *   prepare: PrepareAction<Action['payload']>
 * }
 */

// ✓ AsyncThunkSliceReducerConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkSliceReducerConfig(State, ThunkArg: any, Returned = unknown, ThunkApiConfig: AsyncThunkConfig = {}): any {
  return merge(AsyncThunkReducers(State, ThunkArg, Returned, ThunkApiConfig), { options: optional(AsyncThunkOptions(ThunkArg, ThunkApiConfig)) })
}
/* compiles to:
 * export type AsyncThunkSliceReducerConfig<
 *   State,
 *   ThunkArg extends any,
 *   Returned = unknown,
 *   ThunkApiConfig extends AsyncThunkConfig = {}
 * > =
 *   & AsyncThunkReducers<State, ThunkArg, Returned, ThunkApiConfig>
 *   & { options?: AsyncThunkOptions<ThunkArg, ThunkApiConfig> }
 */

// ✓ AsyncThunkSliceReducerDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkSliceReducerDefinition(State, ThunkArg: any, Returned = unknown, ThunkApiConfig: AsyncThunkConfig = {}): any {
  return merge(AsyncThunkSliceReducerConfig(State, ThunkArg, Returned, ThunkApiConfig), ReducerDefinition(t<ReducerType.asyncThunk>()), { payloadCreator: AsyncThunkPayloadCreator(Returned, ThunkArg, ThunkApiConfig) })
}
/* compiles to:
 * export type AsyncThunkSliceReducerDefinition<
 *   State,
 *   ThunkArg extends any,
 *   Returned = unknown,
 *   ThunkApiConfig extends AsyncThunkConfig = {}
 * > =
 *   & AsyncThunkSliceReducerConfig<State, ThunkArg, Returned, ThunkApiConfig>
 *   & ReducerDefinition<ReducerType.asyncThunk>
 *   & { payloadCreator: AsyncThunkPayloadCreator<Returned, ThunkArg, ThunkApiConfig> }
 */

// ✓ PreventCircular: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PreventCircular(ThunkApiConfig): any {
  const out = emptyObject
  for (const K in keyof(ThunkApiConfig)) {
    out[K] = matches<'state' | 'dispatch'>(K) ? never : ThunkApiConfig[K]
  }
  return out
}
/* compiles to:
 * export type PreventCircular<ThunkApiConfig> = {
 *   [K in keyof ThunkApiConfig]: K extends 'state' | 'dispatch' ? never : ThunkApiConfig[K]
 * }
 */

// ✓ SliceCaseReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SliceCaseReducers(State): any {
  return Record(string, ReducerDefinition) | Record(string, anyOf(CaseReducer(State, PayloadAction(any)), CaseReducerWithPrepare(State, PayloadAction(any, string, any, any))))
}
/* compiles to:
 * export type SliceCaseReducers<State> =
 *   | Record<string, ReducerDefinition>
 *   | Record<
 *       string,
 *       | CaseReducer<State, PayloadAction<any>>
 *       | CaseReducerWithPrepare<State, PayloadAction<any, string, any, any>>
 *     >
 */

// ✓ SliceSelectors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SliceSelectors(State): any {
  return indexRecord(string, fnType([State, ...arrayOf(any)], any))
}
/* compiles to:
 * export type SliceSelectors<State> = { [key: string]: (a0: State, ...a1: any[]) => any }
 */

// ✗ SliceActionType: the ScriptType does not itself typecheck as TypeScript
//   SliceActionType.st.ts(4:28) TS2731: Implicit conversion of a 'symbol' to a 'string' will fail at runtime. Consider wrapping this expression in 'String(...)'.
/* @scripttype preserveParamNames */
export function SliceActionType(SliceName: string, ActionName: keyof any): any {
  if (matches<string | number>(ActionName)) {
    return `${SliceName}/${ActionName}`
  }
  return string
}
/* compiles to:
 * export type SliceActionType<SliceName extends string, ActionName extends keyof any> =
 *   ActionName extends string | number ? `${SliceName}/${ActionName}` : string
 */

// ✓ CaseReducerActions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CaseReducerActions__v1(CaseReducers, SliceName, Type): any {
  const m1 = matches<Hole<"Definition">>(CaseReducers[Type])
  if (m1) {
    if (matches<{ prepare: any; }>(m1.Definition)) {
      return ActionCreatorForCaseReducerWithPrepare(m1.Definition, SliceActionType(SliceName, Type))
    }
    const m2 = matches<AsyncThunkSliceReducerDefinition<any, Hole<"ThunkArg">, Hole<"Returned">, Hole<"ThunkApiConfig">>>(m1.Definition)
    if (m2) {
      return AsyncThunk(m2.Returned, m2.ThunkArg, m2.ThunkApiConfig)
    }
    if (matches<{ reducer: any; }>(m1.Definition)) {
      return ActionCreatorForCaseReducer(m1.Definition['reducer'], SliceActionType(SliceName, Type))
    }
    return ActionCreatorForCaseReducer(m1.Definition, SliceActionType(SliceName, Type))
  }
  return never
}
/**
 * @param {SliceCaseReducers<any>} CaseReducers
 */
export function CaseReducerActions(CaseReducers, SliceName: string): any {
  const out = emptyObject
  for (const Type in keyof(CaseReducers)) {
    out[Type] = CaseReducerActions__v1(CaseReducers, SliceName, Type)
  }
  return out
}
/* compiles to:
 * export type CaseReducerActions__v1<CaseReducers, SliceName, Type> =
 *   CaseReducers[Type] extends infer Definition
 *     ? Definition extends { prepare: any; }
 *       ? ActionCreatorForCaseReducerWithPrepare<Definition, SliceActionType<SliceName, Type>>
 *     : Definition extends AsyncThunkSliceReducerDefinition<any, infer ThunkArg, infer Returned, infer ThunkApiConfig>
 *       ? AsyncThunk<Returned, ThunkArg, ThunkApiConfig>
 *     : Definition extends { reducer: any; }
 *       ? ActionCreatorForCaseReducer<Definition['reducer'], SliceActionType<SliceName, Type>>
 *     : ActionCreatorForCaseReducer<Definition, SliceActionType<SliceName, Type>>
 *     : never
 * export type CaseReducerActions<
 *   CaseReducers extends SliceCaseReducers<any>,
 *   SliceName extends string
 * > =
 *   { [Type in keyof CaseReducers]: CaseReducerActions__v1<CaseReducers, SliceName, Type> }
 */

// ✓ ActionCreatorForCaseReducerWithPrepare: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionCreatorForCaseReducerWithPrepare(CR: { prepare: any; }, Type: string): any {
  return _ActionCreatorWithPreparedPayload(CR['prepare'], Type)
}
/* compiles to:
 * export type ActionCreatorForCaseReducerWithPrepare<
 *   CR extends { prepare: any; },
 *   Type extends string
 * > =
 *   _ActionCreatorWithPreparedPayload<CR['prepare'], Type>
 */

// ✓ ActionCreatorForCaseReducer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionCreatorForCaseReducer(CR, Type: string): any {
  const m1 = matches<(state: any, action: Hole<"Action">) => any>(CR)
  if (m1) {
    const m2 = matches<{ payload: Hole<"P">; }>(m1.Action)
    if (m2) {
      return PayloadActionCreator(m2.P, Type)
    }
    return ActionCreatorWithoutPayload(Type)
  }
  return ActionCreatorWithoutPayload(Type)
}
/* compiles to:
 * export type ActionCreatorForCaseReducer<CR, Type extends string> =
 *   CR extends (state: any, action: infer Action) => any
 *     ? Action extends { payload: infer P; }
 *       ? PayloadActionCreator<P, Type>
 *       : ActionCreatorWithoutPayload<Type>
 *     : ActionCreatorWithoutPayload<Type>
 */

// ✓ SliceDefinedCaseReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SliceDefinedCaseReducers__v1(CaseReducers, Type): any {
  const m1 = matches<Hole<"Definition">>(CaseReducers[Type])
  if (m1) {
    if (matches<AsyncThunkSliceReducerDefinition<any, any, any>>(m1.Definition)) {
      return Id(Pick(Required(m1.Definition), anyOf('fulfilled', 'rejected', 'pending', 'settled')))
    }
    const m2 = matches<{ reducer: Hole<"Reducer">; }>(m1.Definition)
    if (m2) {
      return m2.Reducer
    }
    return m1.Definition
  }
  return never
}
/**
 * @param {SliceCaseReducers<any>} CaseReducers
 */
export function SliceDefinedCaseReducers(CaseReducers): any {
  const out = emptyObject
  for (const Type in keyof(CaseReducers)) {
    out[Type] = SliceDefinedCaseReducers__v1(CaseReducers, Type)
  }
  return out
}
/* compiles to:
 * export type SliceDefinedCaseReducers__v1<CaseReducers, Type> =
 *   CaseReducers[Type] extends infer Definition
 *     ? Definition extends AsyncThunkSliceReducerDefinition<any, any, any>
 *       ? Id<Pick<Required<Definition>, 'fulfilled' | 'rejected' | 'pending' | 'settled'>>
 *     : Definition extends { reducer: infer Reducer; } ? Reducer
 *     : Definition
 *     : never
 * export type SliceDefinedCaseReducers<CaseReducers extends SliceCaseReducers<any>> = {
 *   [Type in keyof CaseReducers]: SliceDefinedCaseReducers__v1<CaseReducers, Type>
 * }
 */

// ✓ RemappedSelector: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RemappedSelector(S: Selector, NewState): any {
  const m1 = matches<Selector<any, Hole<"R">, Hole<"P">>>(S)
  if (m1) {
    return merge(Selector(NewState, m1.R, m1.P), { unwrapped: S })
  }
  return never
}
/* compiles to:
 * export type RemappedSelector<S extends Selector, NewState> =
 *   S extends Selector<any, infer R, infer P>
 *     ? Selector<NewState, R, P> & { unwrapped: S }
 *     : never
 */

// ✓ SliceDefinedSelectors: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {SliceSelectors<typeof State>} Selectors
 */
export function SliceDefinedSelectors(State, Selectors, RootState): any {
  const out = emptyObject
  for (const K in keyof(Selectors)) {
    out[matches<typeof K>(string) ? never : K] = RemappedSelector(Selectors[K], RootState)
  }
  return out
}
/* compiles to:
 * export type SliceDefinedSelectors<State, Selectors extends SliceSelectors<State>, RootState> = {
 *   [K in keyof Selectors as string extends K ? never : K]: RemappedSelector<
 *     Selectors[K],
 *     RootState
 *   >
 * }
 */

// ✓ ValidateSliceCaseReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValidateSliceCaseReducers__v1(S, ACR, T): any {
  const m1 = matches<{ reducer(s: typeof S, action?: Hole<"A">): any; }>(ACR[T])
  if (m1) {
    return { prepare: methodType([...arrayOf(never)], Omit(m1.A, 'type')) }
  }
  return {}
}
/**
 * @param {SliceCaseReducers<typeof S>} ACR
 */
export function ValidateSliceCaseReducers(S, ACR): any {
  const out = emptyObject
  for (const T in keyof(ACR)) {
    out[T] = ValidateSliceCaseReducers__v1(S, ACR, T)
  }
  return ACR & out
}
/* compiles to:
 * export type ValidateSliceCaseReducers__v1<S, ACR, T> =
 *   ACR[T] extends { reducer(s: S, action?: infer A): any; }
 *     ? { prepare(...a0: never[]): Omit<A, 'type'> }
 *     : {}
 * export type ValidateSliceCaseReducers<S, ACR extends SliceCaseReducers<S>> =
 *   ACR & { [T in keyof ACR]: ValidateSliceCaseReducers__v1<S, ACR, T> }
 */

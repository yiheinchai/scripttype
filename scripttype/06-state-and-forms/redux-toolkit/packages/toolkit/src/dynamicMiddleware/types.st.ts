/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/dynamicMiddleware/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 7 type-level aliases: 7 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { Middlewares } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/configureStore.js'
import type { BaseActionCreator, PayloadAction } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAction.js'
import type { GetState } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAsyncThunk.js'
import type { DynamicDispatch } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/dynamicMiddleware/types.js'
import type { FallbackIfUnknown } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/tsHelpers.js'
declare namespace m1 {
  export type DispatchType<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BaseActionCreator: any
declare const Dispatch: any
declare const DynamicDispatch: any
declare const FallbackIfUnknown: any
declare const GetState: any
declare const Middleware: any
declare const MiddlewareAPI: any
declare const MiddlewareConfig: any
declare const Middlewares: any
declare const PayloadAction: any
declare const UnknownAction: any
type Dispatch<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Middleware<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MiddlewareAPI<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type MiddlewareConfig<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type UnknownAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ GetMiddlewareApi: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetMiddlewareApi(MiddlewareApiConfig): any {
  return MiddlewareAPI(GetDispatchType(MiddlewareApiConfig), GetState(MiddlewareApiConfig))
}
/* compiles to:
 * export type GetMiddlewareApi<MiddlewareApiConfig> = MiddlewareAPI<
 *   GetDispatchType<MiddlewareApiConfig>,
 *   GetState<MiddlewareApiConfig>
 * >
 */

// ✓ GetDispatchType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetDispatchType(MiddlewareApiConfig): any {
  const m1 = matches<{ dispatch: Hole<"DispatchType">; }>(MiddlewareApiConfig)
  if (m1) {
    return FallbackIfUnknown(m1.DispatchType, Dispatch)
  }
  return Dispatch
}
/* compiles to:
 * export type GetDispatchType<MiddlewareApiConfig> =
 *   MiddlewareApiConfig extends { dispatch: infer DispatchType; }
 *     ? FallbackIfUnknown<DispatchType, Dispatch>
 *     : Dispatch
 */

// ✓ AddMiddleware: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AddMiddleware(State = any, DispatchType: Dispatch<UnknownAction> = Dispatch(UnknownAction)): any {
  return { ...callSig([...arrayOf(Middleware(any, State, DispatchType))], voidType()), withTypes: genericMethodType(['MiddlewareConfig extends MiddlewareApiConfig'], [], AddMiddleware(GetState(MiddlewareConfig), GetDispatchType(MiddlewareConfig))) }
}
/* compiles to:
 * export type AddMiddleware<
 *   State = any,
 *   DispatchType extends Dispatch<UnknownAction> = Dispatch<UnknownAction>
 * > =
 *   {
 *     (...a0: Middleware<any, State, DispatchType>[]): void
 *     withTypes<MiddlewareConfig extends MiddlewareApiConfig>(): AddMiddleware<
 *       GetState<MiddlewareConfig>,
 *       GetDispatchType<MiddlewareConfig>
 *     >
 *   }
 */

// ✓ WithMiddleware: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithMiddleware(State = any, DispatchType: Dispatch<UnknownAction> = Dispatch(UnknownAction)): any {
  return merge(BaseActionCreator(arrayOf(Middleware(any, State, DispatchType)), 'dynamicMiddleware/add', { instanceId: string }), { ...genericCallSig(['Middlewares extends Middleware<any, State, DispatchType>[]'], [...Middlewares], PayloadAction(Middlewares, 'dynamicMiddleware/add', { instanceId: string })), withTypes: genericMethodType(['MiddlewareConfig extends MiddlewareApiConfig'], [], WithMiddleware(GetState(MiddlewareConfig), GetDispatchType(MiddlewareConfig))) })
}
/* compiles to:
 * export type WithMiddleware<
 *   State = any,
 *   DispatchType extends Dispatch<UnknownAction> = Dispatch<UnknownAction>
 * > =
 *   & BaseActionCreator<
 *       Middleware<any, State, DispatchType>[],
 *       'dynamicMiddleware/add',
 *       { instanceId: string }
 *     >
 *   & {
 *       <Middlewares extends Middleware<any, State, DispatchType>[]>(...a0: Middlewares): PayloadAction<
 *         Middlewares,
 *         'dynamicMiddleware/add',
 *         { instanceId: string }
 *       >
 *       withTypes<MiddlewareConfig extends MiddlewareApiConfig>(): WithMiddleware<
 *         GetState<MiddlewareConfig>,
 *         GetDispatchType<MiddlewareConfig>
 *       >
 *     }
 */

// ✓ MiddlewareEntry: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MiddlewareEntry(State = unknown, DispatchType: Dispatch<UnknownAction> = Dispatch(UnknownAction)): any {
  return { middleware: Middleware(any, State, DispatchType), applied: t<Map<MiddlewareAPI<typeof DispatchType, typeof State>, ReturnType<Middleware<any, typeof State, typeof DispatchType>>>>() }
}
/* compiles to:
 * export type MiddlewareEntry<
 *   State = unknown,
 *   DispatchType extends Dispatch<UnknownAction> = Dispatch<UnknownAction>
 * > =
 *   {
 *     middleware: Middleware<any, State, DispatchType>
 *     applied: Map<
 *       MiddlewareAPI<DispatchType, State>,
 *       ReturnType<Middleware<any, State, DispatchType>>
 *     >
 *   }
 */

// ✓ DynamicMiddleware: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DynamicMiddleware(State = unknown, DispatchType: Dispatch<UnknownAction> = Dispatch(UnknownAction)): any {
  return Middleware(DynamicDispatch, State, DispatchType)
}
/* compiles to:
 * export type DynamicMiddleware<
 *   State = unknown,
 *   DispatchType extends Dispatch<UnknownAction> = Dispatch<UnknownAction>
 * > =
 *   Middleware<DynamicDispatch, State, DispatchType>
 */

// ✓ DynamicMiddlewareInstance: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DynamicMiddlewareInstance(State = unknown, DispatchType: Dispatch<UnknownAction> = Dispatch(UnknownAction)): any {
  return { middleware: DynamicMiddleware(State, DispatchType), addMiddleware: AddMiddleware(State, DispatchType), withMiddleware: WithMiddleware(State, DispatchType), instanceId: string }
}
/* compiles to:
 * export type DynamicMiddlewareInstance<
 *   State = unknown,
 *   DispatchType extends Dispatch<UnknownAction> = Dispatch<UnknownAction>
 * > =
 *   {
 *     middleware: DynamicMiddleware<State, DispatchType>
 *     addMiddleware: AddMiddleware<State, DispatchType>
 *     withMiddleware: WithMiddleware<State, DispatchType>
 *     instanceId: string
 *   }
 */

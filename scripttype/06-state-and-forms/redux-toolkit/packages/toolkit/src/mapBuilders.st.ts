/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/mapBuilders.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AsyncThunk, AsyncThunkConfig } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAsyncThunk.js'
import type { CaseReducer } from '../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createReducer.js'
declare const Action: any
declare const AsyncThunk: any
declare const AsyncThunkConfig: any
declare const CaseReducer: any
type Action<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ AsyncThunkReducers: verified type-identical to the original
/* @scripttype preserveParamNames */
export function AsyncThunkReducers(State, ThunkArg: any, Returned = unknown, ThunkApiConfig: AsyncThunkConfig = {}): any {
  return { pending: optional(CaseReducer(State, ReturnType(AsyncThunk(Returned, ThunkArg, ThunkApiConfig)['pending']))), rejected: optional(CaseReducer(State, ReturnType(AsyncThunk(Returned, ThunkArg, ThunkApiConfig)['rejected']))), fulfilled: optional(CaseReducer(State, ReturnType(AsyncThunk(Returned, ThunkArg, ThunkApiConfig)['fulfilled']))), settled: optional(CaseReducer(State, ReturnType(AsyncThunk(Returned, ThunkArg, ThunkApiConfig)[anyOf('rejected', 'fulfilled')]))) }
}
/* compiles to:
 * export type AsyncThunkReducers<
 *   State,
 *   ThunkArg extends any,
 *   Returned = unknown,
 *   ThunkApiConfig extends AsyncThunkConfig = {}
 * > =
 *   {
 *     pending?: CaseReducer<
 *       State,
 *       ReturnType<AsyncThunk<Returned, ThunkArg, ThunkApiConfig>['pending']>
 *     >
 *     rejected?: CaseReducer<
 *       State,
 *       ReturnType<AsyncThunk<Returned, ThunkArg, ThunkApiConfig>['rejected']>
 *     >
 *     fulfilled?: CaseReducer<
 *       State,
 *       ReturnType<AsyncThunk<Returned, ThunkArg, ThunkApiConfig>['fulfilled']>
 *     >
 *     settled?: CaseReducer<
 *       State,
 *       ReturnType<AsyncThunk<Returned, ThunkArg, ThunkApiConfig>['rejected' | 'fulfilled']>
 *     >
 *   }
 */

// ✓ TypedActionCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TypedActionCreator(Type: string): any {
  return { ...callSig([...arrayOf(any)], Action(Type)), type: Type }
}
/* compiles to:
 * export type TypedActionCreator<Type extends string> = {
 *   (...a0: any[]): Action<Type>
 *   type: Type
 * }
 */

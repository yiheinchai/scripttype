/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/apiState.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 16 type-level aliases: 15 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { SerializedError } from '../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/createAsyncThunk.js'
import type { BaseQueryError } from '../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/baseQueryTypes.js'
import type { InfiniteQueryDirection, ModifiableConfigState, QueryCacheKey, RefetchConfigOptions, SubscriptionState } from '../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/apiState.js'
import type { BaseEndpointDefinition, EndpointDefinitions, FullTagDescription, InfiniteQueryDefinition, MutationDefinition, PageParamFrom, QueryArgFromAnyQuery, QueryDefinition, ResultTypeFrom } from '../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/endpointDefinitions.js'
declare namespace QueryStatus {
  export type fulfilled<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type pending<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type rejected<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type uninitialized<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type BaseQuery<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const BaseEndpointDefinition: any
declare const BaseQueryError: any
declare const EndpointDefinitions: any
declare const FullTagDescription: any
declare const Id: any
declare const InfiniteQueryDefinition: any
declare const InfiniteQueryDirection: any
declare const ModifiableConfigState: any
declare const MutationDefinition: any
declare const PageParamFrom: any
declare const QueryArgFromAnyQuery: any
declare const QueryCacheKey: any
declare const QueryDefinition: any
declare const QueryStatus: any
declare const RefetchConfigOptions: any
declare const ResultTypeFrom: any
declare const SerializedError: any
declare const SubscriptionState: any
declare const WithRequiredProp: any
declare const fulfilled: any
declare const pending: any
declare const rejected: any
declare const uninitialized: any
type Id<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type QueryStatus<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type WithRequiredProp<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type fulfilled<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type pending<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type rejected<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type uninitialized<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ InfiniteQueryConfigOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQueryConfigOptions(DataType, PageParam, QueryArg): any {
  return { initialPageParam: PageParam, getNextPageParam: fnType([DataType, arrayOf(DataType), PageParam, arrayOf(PageParam), QueryArg], anyOf(PageParam, Undefined, Null)), getPreviousPageParam: optional(fnType([DataType, arrayOf(DataType), PageParam, arrayOf(PageParam), QueryArg], anyOf(PageParam, Undefined, Null))), maxPages: optional(number), refetchCachedPages: optional(boolean) }
}
/* compiles to:
 * export type InfiniteQueryConfigOptions<DataType, PageParam, QueryArg> = {
 *   initialPageParam: PageParam
 *   getNextPageParam: (
 *     a0: DataType,
 *     a1: DataType[],
 *     a2: PageParam,
 *     a3: PageParam[],
 *     a4: QueryArg
 *   ) => PageParam | undefined | null
 *   getPreviousPageParam?: (
 *     a0: DataType,
 *     a1: DataType[],
 *     a2: PageParam,
 *     a3: PageParam[],
 *     a4: QueryArg
 *   ) => PageParam | undefined | null
 *   maxPages?: number
 *   refetchCachedPages?: boolean
 * }
 */

// ✓ InfiniteData: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteData(DataType, PageParam): any {
  return { pages: arrayOf(DataType), pageParams: arrayOf(PageParam) }
}
/* compiles to:
 * export type InfiniteData<DataType, PageParam> = { pages: DataType[]; pageParams: PageParam[] }
 */

// ✓ QueryKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EndpointDefinitions} Definitions
 */
export function QueryKeys(Definitions): any {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<QueryDefinition<any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type QueryKeys<Definitions extends EndpointDefinitions> =
 *   {
 *     [K in keyof Definitions]: Definitions[K] extends QueryDefinition<any, any, any, any>
 *       ? K
 *       : never
 *   }[keyof Definitions]
 */

// ✓ InfiniteQueryKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EndpointDefinitions} Definitions
 */
export function InfiniteQueryKeys(Definitions): any {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<InfiniteQueryDefinition<any, any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type InfiniteQueryKeys<Definitions extends EndpointDefinitions> =
 *   {
 *     [K in keyof Definitions]: Definitions[K] extends InfiniteQueryDefinition<any, any, any, any, any>
 *       ? K
 *       : never
 *   }[keyof Definitions]
 */

// ✓ MutationKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EndpointDefinitions} Definitions
 */
export function MutationKeys(Definitions): any {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<MutationDefinition<any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type MutationKeys<Definitions extends EndpointDefinitions> =
 *   {
 *     [K in keyof Definitions]: Definitions[K] extends MutationDefinition<any, any, any, any>
 *       ? K
 *       : never
 *   }[keyof Definitions]
 */

// ✓ BaseQuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseQuerySubState(D: BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom(D)): any {
  const m1 = matches<QueryDefinition<any, Hole<"BaseQuery">, any, any>>(D)
  return { originalArgs: QueryArgFromAnyQuery(D), requestId: string, data: optional(DataType), error: optional(SerializedError | (m1 ? BaseQueryError(m1.BaseQuery) : never)), endpointName: string, startedTimeStamp: number, fulfilledTimeStamp: optional(number) }
}
/* compiles to:
 * export type BaseQuerySubState<
 *   D extends BaseEndpointDefinition<any, any, any, any>,
 *   DataType = ResultTypeFrom<D>
 * > =
 *   {
 *     originalArgs: QueryArgFromAnyQuery<D>
 *     requestId: string
 *     data?: DataType
 *     error?: | SerializedError
 *     | (
 *         D extends QueryDefinition<any, infer BaseQuery, any, any>
 *           ? BaseQueryError<BaseQuery>
 *           : never
 *       )
 *     endpointName: string
 *     startedTimeStamp: number
 *     fulfilledTimeStamp?: number
 *   }
 */

// ✓ QuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QuerySubState(D: BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom(D)): any {
  return Id(anyOf(merge({ status: t<QueryStatus.fulfilled>() }, WithRequiredProp(BaseQuerySubState(D, DataType), anyOf('data', 'fulfilledTimeStamp')), { error: Undefined }), merge({ status: t<QueryStatus.pending>() }, BaseQuerySubState(D, DataType)), merge({ status: t<QueryStatus.rejected>() }, WithRequiredProp(BaseQuerySubState(D, DataType), 'error')), { status: t<QueryStatus.uninitialized>(), originalArgs: optional(Undefined), data: optional(Undefined), error: optional(Undefined), requestId: optional(Undefined), endpointName: optional(string), startedTimeStamp: optional(Undefined), fulfilledTimeStamp: optional(Undefined) }))
}
/* compiles to:
 * export type QuerySubState<
 *   D extends BaseEndpointDefinition<any, any, any, any>,
 *   DataType = ResultTypeFrom<D>
 * > =
 *   Id<
 *     | & { status: QueryStatus.fulfilled }
 *       & WithRequiredProp<BaseQuerySubState<D, DataType>, 'data' | 'fulfilledTimeStamp'>
 *       & { error: undefined }
 *     | { status: QueryStatus.pending } & BaseQuerySubState<D, DataType>
 *     | & { status: QueryStatus.rejected }
 *       & WithRequiredProp<BaseQuerySubState<D, DataType>, 'error'>
 *     | {
 *         status: QueryStatus.uninitialized
 *         originalArgs?: undefined
 *         data?: undefined
 *         error?: undefined
 *         requestId?: undefined
 *         endpointName?: string
 *         startedTimeStamp?: undefined
 *         fulfilledTimeStamp?: undefined
 *       }
 *   >
 */

// ✓ InfiniteQuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQuerySubState(D: BaseEndpointDefinition<any, any, any, any>): any {
  if (matches<InfiniteQueryDefinition<any, any, any, any, any>>(D)) {
    return merge(QuerySubState(D, InfiniteData(ResultTypeFrom(D), PageParamFrom(D))), { direction: optional(InfiniteQueryDirection) })
  }
  return never
}
/* compiles to:
 * export type InfiniteQuerySubState<D extends BaseEndpointDefinition<any, any, any, any>> =
 *   D extends InfiniteQueryDefinition<any, any, any, any, any>
 *     ? & QuerySubState<D, InfiniteData<ResultTypeFrom<D>, PageParamFrom<D>>>
 *     & { direction?: InfiniteQueryDirection }
 *     : never
 */

// ✓ BaseMutationSubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseMutationSubState(D: BaseEndpointDefinition<any, any, any, any>): any {
  const m1 = matches<MutationDefinition<any, Hole<"BaseQuery">, any, any>>(D)
  return { requestId: string, data: optional(ResultTypeFrom(D)), error: optional(SerializedError | (m1 ? BaseQueryError(m1.BaseQuery) : never)), endpointName: string, startedTimeStamp: number, fulfilledTimeStamp: optional(number) }
}
/* compiles to:
 * export type BaseMutationSubState<D extends BaseEndpointDefinition<any, any, any, any>> = {
 *   requestId: string
 *   data?: ResultTypeFrom<D>
 *   error?: | SerializedError
 *   | (
 *       D extends MutationDefinition<any, infer BaseQuery, any, any>
 *         ? BaseQueryError<BaseQuery>
 *         : never
 *     )
 *   endpointName: string
 *   startedTimeStamp: number
 *   fulfilledTimeStamp?: number
 * }
 */

// ✓ MutationSubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationSubState(D: BaseEndpointDefinition<any, any, any, any>): any {
  return anyOf(merge(merge({ status: t<QueryStatus.fulfilled>() }, WithRequiredProp(BaseMutationSubState(D), anyOf('data', 'fulfilledTimeStamp'))), { error: Undefined }), merge(merge({ status: t<QueryStatus.pending>() }, BaseMutationSubState(D)), { data: optional(Undefined) }), merge({ status: t<QueryStatus.rejected>() }, WithRequiredProp(BaseMutationSubState(D), 'error')), { requestId: optional(Undefined), status: t<QueryStatus.uninitialized>(), data: optional(Undefined), error: optional(Undefined), endpointName: optional(string), startedTimeStamp: optional(Undefined), fulfilledTimeStamp: optional(Undefined) })
}
/* compiles to:
 * export type MutationSubState<D extends BaseEndpointDefinition<any, any, any, any>> =
 *   | & { status: QueryStatus.fulfilled }
 *     & WithRequiredProp<BaseMutationSubState<D>, 'data' | 'fulfilledTimeStamp'>
 *     & { error: undefined }
 *   | { status: QueryStatus.pending } & BaseMutationSubState<D> & { data?: undefined }
 *   | { status: QueryStatus.rejected } & WithRequiredProp<BaseMutationSubState<D>, 'error'>
 *   | {
 *       requestId?: undefined
 *       status: QueryStatus.uninitialized
 *       data?: undefined
 *       error?: undefined
 *       endpointName?: string
 *       startedTimeStamp?: undefined
 *       fulfilledTimeStamp?: undefined
 *     }
 */

// ✓ CombinedState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CombinedState(D: EndpointDefinitions, E: string, ReducerPath: string): any {
  return { queries: QueryState(D), mutations: MutationState(D), provided: InvalidationState(E), subscriptions: SubscriptionState, config: ConfigState(ReducerPath) }
}
/* compiles to:
 * export type CombinedState<
 *   D extends EndpointDefinitions,
 *   E extends string,
 *   ReducerPath extends string
 * > =
 *   {
 *     queries: QueryState<D>
 *     mutations: MutationState<D>
 *     provided: InvalidationState<E>
 *     subscriptions: SubscriptionState
 *     config: ConfigState<ReducerPath>
 *   }
 */

// ✗ InvalidationState: does not compile yet
//   an object type can have only one index signature
/* @scripttype preserveParamNames */
export function InvalidationState(TagTypes: string): any {
  const out = emptyObject
  for (const _ in keySet(TagTypes)) {
    out[_] = { ...indexRecord(string, arrayOf(QueryCacheKey)), ...indexRecord(number, arrayOf(QueryCacheKey)) }
  }
  return { tags: out, keys: Record(QueryCacheKey, arrayOf(FullTagDescription(any))) }
}

// ✓ QueryState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryState(D: EndpointDefinitions): any {
  return indexRecord(string, anyOf(QuerySubState(D[string]), InfiniteQuerySubState(D[string]), Undefined))
}
/* compiles to:
 * export type QueryState<D extends EndpointDefinitions> = {
 *   [key: string]: QuerySubState<D[string]> | InfiniteQuerySubState<D[string]> | undefined
 * }
 */

// ✓ ConfigState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConfigState(ReducerPath): any {
  return merge(RefetchConfigOptions, { reducerPath: ReducerPath, online: boolean, focused: boolean, middlewareRegistered: anyOf(boolean, 'conflict') }, ModifiableConfigState)
}
/* compiles to:
 * export type ConfigState<ReducerPath> =
 *   & RefetchConfigOptions
 *   & {
 *       reducerPath: ReducerPath
 *       online: boolean
 *       focused: boolean
 *       middlewareRegistered: boolean | 'conflict'
 *     }
 *   & ModifiableConfigState
 */

// ✓ MutationState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationState(D: EndpointDefinitions): any {
  return indexRecord(string, anyOf(MutationSubState(D[string]), Undefined))
}
/* compiles to:
 * export type MutationState<D extends EndpointDefinitions> = {
 *   [key: string]: MutationSubState<D[string]> | undefined
 * }
 */

// ✓ RootState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RootState(Definitions: EndpointDefinitions, TagTypes: string, ReducerPath: string): any {
  const out = emptyObject
  for (const P in keySet(ReducerPath)) {
    out[P] = CombinedState(Definitions, TagTypes, P)
  }
  return out
}
/* compiles to:
 * export type RootState<
 *   Definitions extends EndpointDefinitions,
 *   TagTypes extends string,
 *   ReducerPath extends string
 * > =
 *   { [P in ReducerPath]: CombinedState<Definitions, TagTypes, P> }
 */

/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/apiState.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 16 type-level aliases: 13 covered, 3 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const BaseEndpointDefinition: any
declare const BaseQueryError: any
declare const EndpointDefinitions: any
declare const FullTagDescription: any
declare const Id: any
declare const InfiniteQueryDefinition: any
declare const InfiniteQueryDirection: any
declare const ModifiableConfigState: any
declare const MutationDefinition: any
declare const PageParamFrom: any
declare const QueryArgFromAnyQuery: any
declare const QueryCacheKey: any
declare const QueryDefinition: any
declare const QueryStatus: any
declare const RefetchConfigOptions: any
declare const ResultTypeFrom: any
declare const SerializedError: any
declare const SubscriptionState: any
declare const WithRequiredProp: any
type BaseEndpointDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type BaseQueryError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EndpointDefinitions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type FullTagDescription<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Id<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InfiniteQueryDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InfiniteQueryDirection<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ModifiableConfigState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MutationDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PageParamFrom<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type QueryArgFromAnyQuery<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type QueryCacheKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type QueryDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type QueryStatus<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type RefetchConfigOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResultTypeFrom<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SerializedError<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SubscriptionState<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type WithRequiredProp<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ InfiniteQueryConfigOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQueryConfigOptions(DataType, PageParam, QueryArg) {
  return { initialPageParam: PageParam, getNextPageParam: fnType([DataType, t<Array<typeof DataType>>(), PageParam, t<Array<typeof PageParam>>(), QueryArg], anyOf(PageParam, Undefined, Null)), getPreviousPageParam: optional(fnType([DataType, t<Array<typeof DataType>>(), PageParam, t<Array<typeof PageParam>>(), QueryArg], anyOf(PageParam, Undefined, Null))), maxPages: optional(number), refetchCachedPages: optional(boolean) }
}
/* compiles to:
 * export type InfiniteQueryConfigOptions<DataType, PageParam, QueryArg> = { initialPageParam: PageParam; getNextPageParam: (a0: DataType, a1: Array<DataType>, a2: PageParam, a3: Array<PageParam>, a4: QueryArg) => PageParam | undefined | null; getPreviousPageParam?: (a0: DataType, a1: Array<DataType>, a2: PageParam, a3: Array<PageParam>, a4: QueryArg) => PageParam | undefined | null; maxPages?: number; refetchCachedPages?: boolean }
 */

// ✓ InfiniteData: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteData(DataType, PageParam) {
  return { pages: t<Array<typeof DataType>>(), pageParams: t<Array<typeof PageParam>>() }
}
/* compiles to:
 * export type InfiniteData<DataType, PageParam> = { pages: Array<DataType>; pageParams: Array<PageParam> }
 */

// ✓ QueryKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryKeys(Definitions: EndpointDefinitions) {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<QueryDefinition<any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type QueryKeys<Definitions extends EndpointDefinitions> = { [K in keyof Definitions]: Definitions[K] extends QueryDefinition<any, any, any, any> ? K : never }[keyof Definitions]
 */

// ✓ InfiniteQueryKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQueryKeys(Definitions: EndpointDefinitions) {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<InfiniteQueryDefinition<any, any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type InfiniteQueryKeys<Definitions extends EndpointDefinitions> = { [K in keyof Definitions]: Definitions[K] extends InfiniteQueryDefinition<any, any, any, any, any> ? K : never }[keyof Definitions]
 */

// ✓ MutationKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationKeys(Definitions: EndpointDefinitions) {
  const out = emptyObject
  for (const K in keyof(Definitions)) {
    out[K] = matches<MutationDefinition<any, any, any, any>>(Definitions[K]) ? K : never
  }
  return out[keyof(Definitions)]
}
/* compiles to:
 * export type MutationKeys<Definitions extends EndpointDefinitions> = { [K in keyof Definitions]: Definitions[K] extends MutationDefinition<any, any, any, any> ? K : never }[keyof Definitions]
 */

// ✓ BaseQuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseQuerySubState(D: BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom(D)) {
  const m1 = matches<QueryDefinition<any, Hole<"BaseQuery">, any, any>>(D)
  return { originalArgs: QueryArgFromAnyQuery(D), requestId: string, data: optional(DataType), error: optional(SerializedError | (m1 ? BaseQueryError(m1.BaseQuery) : never)), endpointName: string, startedTimeStamp: number, fulfilledTimeStamp: optional(number) }
}
/* compiles to:
 * export type BaseQuerySubState<D extends BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom<D>> = { originalArgs: QueryArgFromAnyQuery<D>; requestId: string; data?: DataType; error?: SerializedError | (D extends QueryDefinition<any, infer BaseQuery, any, any> ? BaseQueryError<BaseQuery> : never); endpointName: string; startedTimeStamp: number; fulfilledTimeStamp?: number }
 */

// ✓ QuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QuerySubState(D: BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom(D)) {
  return Id(anyOf(merge({ status: QueryStatus.fulfilled }, WithRequiredProp(BaseQuerySubState(D, DataType), anyOf('data', 'fulfilledTimeStamp')), { error: Undefined }), merge({ status: QueryStatus.pending }, BaseQuerySubState(D, DataType)), merge({ status: QueryStatus.rejected }, WithRequiredProp(BaseQuerySubState(D, DataType), 'error')), { status: QueryStatus.uninitialized, originalArgs: optional(Undefined), data: optional(Undefined), error: optional(Undefined), requestId: optional(Undefined), endpointName: optional(string), startedTimeStamp: optional(Undefined), fulfilledTimeStamp: optional(Undefined) }))
}
/* compiles to:
 * export type QuerySubState<D extends BaseEndpointDefinition<any, any, any, any>, DataType = ResultTypeFrom<D>> = Id<{ status: QueryStatus.fulfilled } & WithRequiredProp<BaseQuerySubState<D, DataType>, 'data' | 'fulfilledTimeStamp'> & { error: undefined } | { status: QueryStatus.pending } & BaseQuerySubState<D, DataType> | { status: QueryStatus.rejected } & WithRequiredProp<BaseQuerySubState<D, DataType>, 'error'> | { status: QueryStatus.uninitialized; originalArgs?: undefined; data?: undefined; error?: undefined; requestId?: undefined; endpointName?: string; startedTimeStamp?: undefined; fulfilledTimeStamp?: undefined }>
 */

// ✓ InfiniteQuerySubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InfiniteQuerySubState(D: BaseEndpointDefinition<any, any, any, any>) {
  if (matches<InfiniteQueryDefinition<any, any, any, any, any>>(D)) {
    return merge(QuerySubState(D, InfiniteData(ResultTypeFrom(D), PageParamFrom(D))), { direction: optional(InfiniteQueryDirection) })
  }
  return never
}
/* compiles to:
 * export type InfiniteQuerySubState<D extends BaseEndpointDefinition<any, any, any, any>> = D extends InfiniteQueryDefinition<any, any, any, any, any> ? QuerySubState<D, InfiniteData<ResultTypeFrom<D>, PageParamFrom<D>>> & { direction?: InfiniteQueryDirection } : never
 */

// ✓ BaseMutationSubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function BaseMutationSubState(D: BaseEndpointDefinition<any, any, any, any>) {
  const m1 = matches<MutationDefinition<any, Hole<"BaseQuery">, any, any>>(D)
  return { requestId: string, data: optional(ResultTypeFrom(D)), error: optional(SerializedError | (m1 ? BaseQueryError(m1.BaseQuery) : never)), endpointName: string, startedTimeStamp: number, fulfilledTimeStamp: optional(number) }
}
/* compiles to:
 * export type BaseMutationSubState<D extends BaseEndpointDefinition<any, any, any, any>> = { requestId: string; data?: ResultTypeFrom<D>; error?: SerializedError | (D extends MutationDefinition<any, infer BaseQuery, any, any> ? BaseQueryError<BaseQuery> : never); endpointName: string; startedTimeStamp: number; fulfilledTimeStamp?: number }
 */

// ✓ MutationSubState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationSubState(D: BaseEndpointDefinition<any, any, any, any>) {
  return anyOf(merge(merge({ status: QueryStatus.fulfilled }, WithRequiredProp(BaseMutationSubState(D), anyOf('data', 'fulfilledTimeStamp'))), { error: Undefined }), merge(merge({ status: QueryStatus.pending }, BaseMutationSubState(D)), { data: optional(Undefined) }), merge({ status: QueryStatus.rejected }, WithRequiredProp(BaseMutationSubState(D), 'error')), { requestId: optional(Undefined), status: QueryStatus.uninitialized, data: optional(Undefined), error: optional(Undefined), endpointName: optional(string), startedTimeStamp: optional(Undefined), fulfilledTimeStamp: optional(Undefined) })
}
/* compiles to:
 * export type MutationSubState<D extends BaseEndpointDefinition<any, any, any, any>> = { status: QueryStatus.fulfilled } & WithRequiredProp<BaseMutationSubState<D>, 'data' | 'fulfilledTimeStamp'> & { error: undefined } | { status: QueryStatus.pending } & BaseMutationSubState<D> & { data?: undefined } | { status: QueryStatus.rejected } & WithRequiredProp<BaseMutationSubState<D>, 'error'> | { requestId?: undefined; status: QueryStatus.uninitialized; data?: undefined; error?: undefined; endpointName?: string; startedTimeStamp?: undefined; fulfilledTimeStamp?: undefined }
 */

// ✓ CombinedState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CombinedState(D: EndpointDefinitions, E: string, ReducerPath: string) {
  return { queries: QueryState(D), mutations: MutationState(D), provided: InvalidationState(E), subscriptions: SubscriptionState, config: ConfigState(ReducerPath) }
}
/* compiles to:
 * export type CombinedState<D extends EndpointDefinitions, E extends string, ReducerPath extends string> = { queries: QueryState<D>; mutations: MutationState<D>; provided: InvalidationState<E>; subscriptions: SubscriptionState; config: ConfigState<ReducerPath> }
 */

// ✗ InvalidationState: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function InvalidationState(TagTypes: string) {
  const out = emptyObject
  for (const _ in keySet(TagTypes)) {
    out[_] = raw('{ [id: string]: Array<QueryCacheKey>; [id: number]: Array<QueryCacheKey>; }')
  }
  return { tags: out, keys: Record(QueryCacheKey, t<Array<FullTagDescription<any>>>()) }
}

// ✗ QueryState: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function QueryState(D: EndpointDefinitions) {
  return raw('{ [queryCacheKey: string]: QuerySubState<D[string]> | InfiniteQuerySubState<D[string]> | undefined; }')
}

// ✓ ConfigState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConfigState(ReducerPath) {
  return merge(RefetchConfigOptions, { reducerPath: ReducerPath, online: boolean, focused: boolean, middlewareRegistered: anyOf(boolean, 'conflict') }, ModifiableConfigState)
}
/* compiles to:
 * export type ConfigState<ReducerPath> = RefetchConfigOptions & { reducerPath: ReducerPath; online: boolean; focused: boolean; middlewareRegistered: boolean | 'conflict' } & ModifiableConfigState
 */

// ✗ MutationState: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function MutationState(D: EndpointDefinitions) {
  return raw('{ [requestId: string]: MutationSubState<D[string]> | undefined; }')
}

// ✓ RootState: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RootState(Definitions: EndpointDefinitions, TagTypes: string, ReducerPath: string) {
  const out = emptyObject
  for (const P in keySet(ReducerPath)) {
    out[P] = CombinedState(Definitions, TagTypes, P)
  }
  return out
}
/* compiles to:
 * export type RootState<Definitions extends EndpointDefinitions, TagTypes extends string, ReducerPath extends string> = { [P in ReducerPath]: CombinedState<Definitions, TagTypes, P> }
 */

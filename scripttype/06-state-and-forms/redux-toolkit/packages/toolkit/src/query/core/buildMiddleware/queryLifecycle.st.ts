/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/buildMiddleware/queryLifecycle.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 8 type-level aliases: 8 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BaseQueryError, BaseQueryFn, BaseQueryMeta } from '../../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/baseQueryTypes.js'
import type { MutationBaseLifecycleApi } from '../../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/buildMiddleware/cacheLifecycle.js'
import type { QueryLifecycleApi } from '../../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/buildMiddleware/queryLifecycle.js'
import type { PromiseWithKnownReason } from '../../../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/core/buildMiddleware/types.js'
declare const BaseQueryError: any
declare const BaseQueryFn: any
declare const BaseQueryMeta: any
declare const MutationBaseLifecycleApi: any
declare const PromiseWithKnownReason: any
declare const QueryLifecycleApi: any
// ✓ QueryLifecyclePromises: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryLifecyclePromises(ResultType, BaseQuery: BaseQueryFn): any {
  return { queryFulfilled: PromiseWithKnownReason({ data: ResultType, meta: BaseQueryMeta(BaseQuery) }, QueryFulfilledRejectionReason(BaseQuery)) }
}
/* compiles to:
 * export type QueryLifecyclePromises<ResultType, BaseQuery extends BaseQueryFn> = {
 *   queryFulfilled: PromiseWithKnownReason<
 *     { data: ResultType; meta: BaseQueryMeta<BaseQuery> },
 *     QueryFulfilledRejectionReason<BaseQuery>
 *   >
 * }
 */

// ✓ QueryFulfilledRejectionReason: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryFulfilledRejectionReason(BaseQuery: BaseQueryFn): any {
  return anyOf({ error: BaseQueryError(BaseQuery), isUnhandledError: false, meta: BaseQueryMeta(BaseQuery) }, { error: unknown, meta: optional(Undefined), isUnhandledError: true })
}
/* compiles to:
 * export type QueryFulfilledRejectionReason<BaseQuery extends BaseQueryFn> =
 *   | {
 *       error: BaseQueryError<BaseQuery>
 *       isUnhandledError: false
 *       meta: BaseQueryMeta<BaseQuery>
 *     }
 *   | { error: unknown; meta?: undefined; isUnhandledError: true }
 */

// ✓ QueryLifecycleQueryExtraOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryLifecycleQueryExtraOptions(ResultType, QueryArg, BaseQuery: BaseQueryFn, ReducerPath: string = string): any {
  return { onQueryStarted: optional(methodType([QueryArg, QueryLifecycleApi(QueryArg, BaseQuery, ResultType, ReducerPath)], t<Promise<void>>() | voidType())) }
}
/* compiles to:
 * export type QueryLifecycleQueryExtraOptions<
 *   ResultType,
 *   QueryArg,
 *   BaseQuery extends BaseQueryFn,
 *   ReducerPath extends string = string
 * > =
 *   {
 *     onQueryStarted?(a0: QueryArg, a1: QueryLifecycleApi<QueryArg, BaseQuery, ResultType, ReducerPath>): | Promise<void>
 *     | void
 *   }
 */

// ✓ QueryLifecycleInfiniteQueryExtraOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryLifecycleInfiniteQueryExtraOptions(ResultType, QueryArg, BaseQuery: BaseQueryFn, ReducerPath: string = string): any {
  return QueryLifecycleQueryExtraOptions(ResultType, QueryArg, BaseQuery, ReducerPath)
}
/* compiles to:
 * export type QueryLifecycleInfiniteQueryExtraOptions<
 *   ResultType,
 *   QueryArg,
 *   BaseQuery extends BaseQueryFn,
 *   ReducerPath extends string = string
 * > =
 *   QueryLifecycleQueryExtraOptions<ResultType, QueryArg, BaseQuery, ReducerPath>
 */

// ✓ QueryLifecycleMutationExtraOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function QueryLifecycleMutationExtraOptions(ResultType, QueryArg, BaseQuery: BaseQueryFn, ReducerPath: string = string): any {
  return { onQueryStarted: optional(methodType([QueryArg, MutationLifecycleApi(QueryArg, BaseQuery, ResultType, ReducerPath)], t<Promise<void>>() | voidType())) }
}
/* compiles to:
 * export type QueryLifecycleMutationExtraOptions<
 *   ResultType,
 *   QueryArg,
 *   BaseQuery extends BaseQueryFn,
 *   ReducerPath extends string = string
 * > =
 *   {
 *     onQueryStarted?(a0: QueryArg, a1: MutationLifecycleApi<QueryArg, BaseQuery, ResultType, ReducerPath>): | Promise<void>
 *     | void
 *   }
 */

// ✓ MutationLifecycleApi: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MutationLifecycleApi(QueryArg, BaseQuery: BaseQueryFn, ResultType, ReducerPath: string = string): any {
  return MutationBaseLifecycleApi(QueryArg, BaseQuery, ResultType, ReducerPath) & QueryLifecyclePromises(ResultType, BaseQuery)
}
/* compiles to:
 * export type MutationLifecycleApi<
 *   QueryArg,
 *   BaseQuery extends BaseQueryFn,
 *   ResultType,
 *   ReducerPath extends string = string
 * > =
 *   & MutationBaseLifecycleApi<QueryArg, BaseQuery, ResultType, ReducerPath>
 *   & QueryLifecyclePromises<ResultType, BaseQuery>
 */

// ✓ TypedQueryOnQueryStarted: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TypedQueryOnQueryStarted(ResultType, QueryArgumentType, BaseQueryFunctionType: BaseQueryFn, ReducerPath: string = string): any {
  return QueryLifecycleQueryExtraOptions(ResultType, QueryArgumentType, BaseQueryFunctionType, ReducerPath)['onQueryStarted']
}
/* compiles to:
 * export type TypedQueryOnQueryStarted<
 *   ResultType,
 *   QueryArgumentType,
 *   BaseQueryFunctionType extends BaseQueryFn,
 *   ReducerPath extends string = string
 * > =
 *   QueryLifecycleQueryExtraOptions<
 *     ResultType,
 *     QueryArgumentType,
 *     BaseQueryFunctionType,
 *     ReducerPath
 *   >['onQueryStarted']
 */

// ✓ TypedMutationOnQueryStarted: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TypedMutationOnQueryStarted(ResultType, QueryArgumentType, BaseQueryFunctionType: BaseQueryFn, ReducerPath: string = string): any {
  return QueryLifecycleMutationExtraOptions(ResultType, QueryArgumentType, BaseQueryFunctionType, ReducerPath)['onQueryStarted']
}
/* compiles to:
 * export type TypedMutationOnQueryStarted<
 *   ResultType,
 *   QueryArgumentType,
 *   BaseQueryFunctionType extends BaseQueryFn,
 *   ReducerPath extends string = string
 * > =
 *   QueryLifecycleMutationExtraOptions<
 *     ResultType,
 *     QueryArgumentType,
 *     BaseQueryFunctionType,
 *     ReducerPath
 *   >['onQueryStarted']
 */

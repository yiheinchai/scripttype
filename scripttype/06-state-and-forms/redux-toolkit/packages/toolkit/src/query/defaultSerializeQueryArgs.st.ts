/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/query/defaultSerializeQueryArgs.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { EndpointDefinition } from '../../../../../../../06-state-and-forms/redux-toolkit/packages/toolkit/src/query/endpointDefinitions.js'
declare const EndpointDefinition: any
// ✓ SerializeQueryArgs: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SerializeQueryArgs(QueryArgs, ReturnType = string): any {
  return fnType([{ queryArgs: QueryArgs, endpointDefinition: EndpointDefinition(any, any, any, any), endpointName: string }], ReturnType)
}
/* compiles to:
 * export type SerializeQueryArgs<QueryArgs, ReturnType = string> =
 *   (
 *     a0: { queryArgs: QueryArgs; endpointDefinition: EndpointDefinition<any, any, any, any>; endpointName: string }
 *   ) => ReturnType
 */

/**
 * ScriptType for 06-state-and-forms/redux-toolkit/packages/toolkit/src/query/tsHelpers.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 13 type-level aliases: 12 covered, 1 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace m1 {
  export type I<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type V<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
// ✓ Id: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Id(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return merge(out, {})
}
/* compiles to:
 * export type Id<T> = { [K in keyof T]: T[K] } & {}
 */

// ✓ WithRequiredProp: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof T} K
 */
export function WithRequiredProp(T, K): any {
  return Omit(T, K) & Required(Pick(T, K))
}
/* compiles to:
 * export type WithRequiredProp<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>
 */

// ✓ Override: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Override(T1, T2): any {
  if (matches<any>(T2)) {
    return Omit(T1, keyof(T2)) & T2
  }
  return never
}
/* compiles to:
 * export type Override<T1, T2> = T2 extends any ? Omit<T1, keyof T2> & T2 : never
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U): any {
  const m1 = matches<(k: Hole<"I">) => void>(matches<any>(U) ? fnType([U], voidType()) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> =
 *   (U extends any ? (a0: U) => void : never) extends (k: infer I) => void ? I : never
 */

// ✓ NonOptionalKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonOptionalKeys(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = required(matches<(typeof T)[typeof K]>(Undefined) ? never : K)
  }
  return out[keyof(T)]
}
/* compiles to:
 * export type NonOptionalKeys<T> = { [K in keyof T]-?: undefined extends T[K] ? never : K }[keyof T]
 */

// ✓ HasRequiredProps: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HasRequiredProps(T, True, False): any {
  if (matches<never>(NonOptionalKeys(T))) {
    return False
  }
  return True
}
/* compiles to:
 * export type HasRequiredProps<T, True, False> = NonOptionalKeys<T> extends never ? False : True
 */

// ✓ OptionalIfAllPropsOptional: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OptionalIfAllPropsOptional(T): any {
  return HasRequiredProps(T, T, anyOf(T, never))
}
/* compiles to:
 * export type OptionalIfAllPropsOptional<T> = HasRequiredProps<T, T, T>
 */

// ✓ NonUndefined: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonUndefined(T): any {
  if (typeof T === 'undefined') {
    return never
  }
  return T
}
/* compiles to:
 * export type NonUndefined<T> = T extends undefined ? never : T
 */

// ✓ UnwrapPromise: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnwrapPromise(T): any {
  const m1 = matches<PromiseLike<Hole<"V">>>(T)
  if (m1) {
    return m1.V
  }
  return T
}
/* compiles to:
 * export type UnwrapPromise<T> = T extends PromiseLike<infer V> ? V : T
 */

// ✓ MaybePromise: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybePromise(T): any {
  return T | PromiseLike(T)
}
/* compiles to:
 * export type MaybePromise<T> = T | PromiseLike<T>
 */

// ✓ OmitFromUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof T} K
 */
export function OmitFromUnion(T, K): any {
  if (matches<any>(T)) {
    return Omit(T, K)
  }
  return never
}
/* compiles to:
 * export type OmitFromUnion<T, K extends keyof T> = T extends any ? Omit<T, K> : never
 */

// ✗ IsAny: does not compile yet
//   Cannot find name 'extends'.
/* @scripttype preserveParamNames */
export function IsAny(T, True, False = never): any {
  if (matches<(typeof T extends never ? true : false)>(anyOf(true, false))) {
    return True
  }
  return False
}
/* compiles to:
 * export type IsAny<T, True, False = never> =
 *   true | false extends T extends never ? true : false ? True : False
 */

// ✓ CastAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CastAny(T, CastTo): any {
  return IsAny(T, CastTo, T)
}
/* compiles to:
 * export type CastAny<T, CastTo> = IsAny<T, CastTo, T>
 */

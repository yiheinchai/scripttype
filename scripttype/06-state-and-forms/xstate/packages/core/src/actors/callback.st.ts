/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/actors/callback.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 5 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyActorSystem } from '../../../../../../../06-state-and-forms/xstate/packages/core/src/system.js'
import type { ActorLogic, ActorRefFromLogic, AnyEventObject, NonReducibleUnknown } from '../../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
declare const ActorLogic: any
declare const ActorRefFromLogic: any
declare const AnyActorSystem: any
declare const AnyEventObject: any
declare const EventObject: any
declare const NonReducibleUnknown: any
declare const Snapshot: any
type EventObject<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Snapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ CallbackSnapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallbackSnapshot(TInput): any {
  return merge(Snapshot(Undefined), { input: TInput })
}
/* compiles to:
 * export type CallbackSnapshot<TInput> = Snapshot<undefined> & { input: TInput }
 */

// ✓ CallbackActorLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallbackActorLogic(TEvent: EventObject, TInput = NonReducibleUnknown, TEmitted: EventObject = EventObject): any {
  return ActorLogic(CallbackSnapshot(TInput), TEvent, TInput, AnyActorSystem, TEmitted)
}
/* compiles to:
 * export type CallbackActorLogic<
 *   TEvent extends EventObject,
 *   TInput = NonReducibleUnknown,
 *   TEmitted extends EventObject = EventObject
 * > =
 *   ActorLogic<CallbackSnapshot<TInput>, TEvent, TInput, AnyActorSystem, TEmitted>
 */

// ✓ CallbackActorRef: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallbackActorRef(TEvent: EventObject, TInput = NonReducibleUnknown): any {
  return ActorRefFromLogic(CallbackActorLogic(TEvent, TInput))
}
/* compiles to:
 * export type CallbackActorRef<TEvent extends EventObject, TInput = NonReducibleUnknown> = ActorRefFromLogic<
 *   CallbackActorLogic<TEvent, TInput>
 * >
 */

// ✓ Receiver: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Receiver(TEvent: EventObject): any {
  return fnType([{ bivarianceHack: methodType([TEvent], voidType()) }['bivarianceHack']], voidType())
}
/* compiles to:
 * export type Receiver<TEvent extends EventObject> =
 *   (a0: { bivarianceHack(a0: TEvent): void }['bivarianceHack']) => void
 */

// ✓ CallbackLogicFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CallbackLogicFunction(TEvent: EventObject = AnyEventObject, TSentEvent: EventObject = AnyEventObject, TInput = NonReducibleUnknown, TEmitted: EventObject = EventObject): any {
  return fnType([{ input: TInput, system: AnyActorSystem, self: CallbackActorRef(TEvent), sendBack: fnType([TSentEvent], voidType()), receive: Receiver(TEvent), emit: fnType([TEmitted], voidType()) }], fnType([], voidType()) | voidType())
}
/* compiles to:
 * export type CallbackLogicFunction<
 *   TEvent extends EventObject = AnyEventObject,
 *   TSentEvent extends EventObject = AnyEventObject,
 *   TInput = NonReducibleUnknown,
 *   TEmitted extends EventObject = EventObject
 * > =
 *   (
 *     a0: { input: TInput; system: AnyActorSystem; self: CallbackActorRef<TEvent>; sendBack: (a0: TSentEvent) => void; receive: Receiver<TEvent>; emit: (a0: TEmitted) => void }
 *   ) => (() => void) | void
 */

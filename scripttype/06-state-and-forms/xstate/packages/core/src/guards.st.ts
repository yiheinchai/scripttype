/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/guards.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 5 type-level aliases: 5 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { GuardArgs } from '../../../../../../06-state-and-forms/xstate/packages/core/src/guards.js'
import type { Elements, Identity, MachineContext, NoRequiredParams, ParameterizedObject, WithDynamicParams } from '../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
declare const Elements: any
declare const EventObject: any
declare const GuardArgs: any
declare const Identity: any
declare const MachineContext: any
declare const NoRequiredParams: any
declare const ParameterizedObject: any
declare const WithDynamicParams: any
type EventObject<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ SingleGuardArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SingleGuardArg(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TGuardArg): any {
  if (matches<[ { type: string; } ]>([TGuardArg])) {
    return Identity(TGuardArg)
  }
  if (matches<[ string ]>([TGuardArg])) {
    return TGuardArg
  }
  return GuardPredicate(TContext, TExpressionEvent, TParams, ParameterizedObject)
}
/* compiles to:
 * export type SingleGuardArg<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TGuardArg
 * > =
 *   [TGuardArg] extends [{ type: string; }] ? Identity<TGuardArg>
 *   : [TGuardArg] extends [string] ? TGuardArg
 *   : GuardPredicate<TContext, TExpressionEvent, TParams, ParameterizedObject>
 */

// ✓ NormalizeGuardArg: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizeGuardArg(TGuardArg): any {
  if (matches<{ type: string; }>(TGuardArg)) {
    return merge(Identity(TGuardArg), { params: unknown })
  }
  if (typeof TGuardArg === 'string') {
    return { type: TGuardArg, params: Undefined }
  }
  if ('_out_TGuard' in TGuardArg) {
    return TGuardArg['_out_TGuard'] & ParameterizedObject
  }
  return never
}
/* compiles to:
 * export type NormalizeGuardArg<TGuardArg> =
 *   TGuardArg extends { type: string; } ? Identity<TGuardArg> & { params: unknown }
 *   : TGuardArg extends string ? { type: TGuardArg; params: undefined }
 *   : '_out_TGuard' extends keyof TGuardArg ? TGuardArg['_out_TGuard'] & ParameterizedObject
 *   : never
 */

// ✓ NormalizeGuardArgArray: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {unknown[]} TArg
 */
export function NormalizeGuardArgArray(TArg): any {
  const out = emptyObject
  for (const K in keyof(TArg)) {
    out[K] = NormalizeGuardArg(TArg[K])
  }
  return Elements(out)
}
/* compiles to:
 * export type NormalizeGuardArgArray<TArg extends unknown[]> = Elements<
 *   { [K in keyof TArg]: NormalizeGuardArg<TArg[K]> }
 * >
 */

// ✓ GuardPredicate: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GuardPredicate(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TGuard: ParameterizedObject): any {
  return { ...callSig([GuardArgs(TContext, TExpressionEvent), TParams], boolean), _out_TGuard: optional(TGuard) }
}
/* compiles to:
 * export type GuardPredicate<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TGuard extends ParameterizedObject
 * > =
 *   { (a0: GuardArgs<TContext, TExpressionEvent>, a1: TParams): boolean; _out_TGuard?: TGuard }
 */

// ✓ Guard: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Guard(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TGuard: ParameterizedObject): any {
  return NoRequiredParams(TGuard) | WithDynamicParams(TContext, TExpressionEvent, TGuard) | GuardPredicate(TContext, TExpressionEvent, TParams, TGuard)
}
/* compiles to:
 * export type Guard<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TGuard extends ParameterizedObject
 * > =
 *   | NoRequiredParams<TGuard>
 *   | WithDynamicParams<TContext, TExpressionEvent, TGuard>
 *   | GuardPredicate<TContext, TExpressionEvent, TParams, TGuard>
 */

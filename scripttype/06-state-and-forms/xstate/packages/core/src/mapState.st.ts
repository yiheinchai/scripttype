/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/mapState.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyMachineSnapshot, StateSchema } from '../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
declare const AnyMachineSnapshot: any
declare const StateSchema: any
// ✓ StateSchemaMapper: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateSchemaMapper(TSnapshot: AnyMachineSnapshot, T: StateSchema, TResult): any {
  const out = emptyObject
  for (const K in keyof(T['states'])) {
    out[K] = optional(matches<StateSchema>(T['states'][K]) ? StateSchemaMapper(TSnapshot, T['states'][K], TResult) : never)
  }
  return { map: optional(fnType([TSnapshot], TResult)), states: optional(out) }
}
/* compiles to:
 * export type StateSchemaMapper<
 *   TSnapshot extends AnyMachineSnapshot,
 *   T extends StateSchema,
 *   TResult
 * > =
 *   {
 *     map?: (a0: TSnapshot) => TResult
 *     states?: {
 *       [K in keyof T['states']]?: T['states'][K] extends StateSchema
 *         ? StateSchemaMapper<TSnapshot, T['states'][K], TResult>
 *         : never
 *     }
 *   }
 */

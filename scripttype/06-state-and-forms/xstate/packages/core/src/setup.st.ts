/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/setup.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 10 type-level aliases: 9 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ActionFunction: any
declare const AnyEventObject: any
declare const EventObject: any
declare const Invert: any
declare const IsNever: any
declare const MachineContext: any
declare const MetaObject: any
declare const NonReducibleUnknown: any
declare const ParameterizedObject: any
declare const StateSchema: any
declare const UnknownActorLogic: any
declare const Values: any
type ActionFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyEventObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EventObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Invert<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type IsNever<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MachineContext<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MetaObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonReducibleUnknown<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParameterizedObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type UnknownActorLogic<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Values<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ ToParameterizedObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToParameterizedObject(TParameterizedMap: Record<string, ParameterizedObject['params'] | undefined>) {
  const out = emptyObject
  for (const K in keyof(TParameterizedMap)) {
    out[merge(K, string)] = { type: merge(K, string), params: TParameterizedMap[K] }
  }
  return Values(out)
}
/* compiles to:
 * export type ToParameterizedObject<TParameterizedMap extends Record<string, ParameterizedObject['params'] | undefined>> = Values<{ [K in keyof TParameterizedMap as K & string]: { type: K & string; params: TParameterizedMap[K] } }>
 */

// ✓ ToProvidedActor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToProvidedActor(TChildrenMap: Record<string, string>, TActors: Record<string, UnknownActorLogic>) {
  const out = emptyObject
  for (const K in keyof(TActors)) {
    out[merge(K, string)] = { src: merge(K, string), logic: TActors[K], id: matches<true>(IsNever(TChildrenMap)) ? (anyOf(string, Undefined)) : (matches<keyof Invert<typeof TChildrenMap>>(K) ? (Invert(TChildrenMap)[K] & string) : (anyOf(string, Undefined))) }
  }
  return Values(out)
}
/* compiles to:
 * export type ToProvidedActor<TChildrenMap extends Record<string, string>, TActors extends Record<string, UnknownActorLogic>> = Values<{ [K in keyof TActors as K & string]: { src: K & string; logic: TActors[K]; id: IsNever<TChildrenMap> extends true ? string | undefined : K extends keyof Invert<TChildrenMap> ? Invert<TChildrenMap>[K] & string : string | undefined } }>
 */

// ✓ ToStateSchema: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToStateSchema(TSchema: StateSchema) {
  const out = emptyObject
  for (const K in keyof(TSchema)) {
    out[merge(K, anyOf('id', 'states'))] = mutable(matches<'states'>(K) ? out : TSchema[K])
  }
  return out
}
/* compiles to:
 * export type ToStateSchema<TSchema extends StateSchema> = { -readonly [K in keyof TSchema as K & ('id' | 'states')]: K extends 'states' ? {} : TSchema[K] }
 */

// ✓ RequiredSetupKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredSetupKeys(TChildrenMap) {
  if (matches<true>(IsNever(keyof(TChildrenMap)))) {
    return never
  }
  return 'actors'
}
/* compiles to:
 * export type RequiredSetupKeys<TChildrenMap> = IsNever<keyof TChildrenMap> extends true ? never : 'actors'
 */

// ✓ ExtractInvokeEntry: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractInvokeEntry(T) {
  const m1 = matches<{ id: Hole<"TId", string>; src: Hole<"TSrc", string>; }>(T)
  if (m1) {
    const out = emptyObject
    for (const K in keySet(m1.TId)) {
      out[K] = m1.TSrc
    }
    return out
  }
  return {}
}
/* compiles to:
 * export type ExtractInvokeEntry<T> = T extends { id: infer TId extends string; src: infer TSrc extends string; } ? { [K in TId]: TSrc } : {}
 */

// ✓ ExtractInvokeChildren: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractInvokeChildren(T) {
  const m1 = matches<readonly (Hole<"E">)[]>(T)
  if (m1) {
    return UnionToIntersection(ExtractInvokeEntry(m1.E))
  }
  return ExtractInvokeEntry(T)
}
/* compiles to:
 * export type ExtractInvokeChildren<T> = T extends readonly (infer E)[] ? UnionToIntersection<ExtractInvokeEntry<E>> : ExtractInvokeEntry<T>
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U) {
  const m1 = matches<(x: Hole<"I">) => void>(matches<any>(U) ? fnType([U], voidType()) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> = (U extends any ? (a0: U) => void : never) extends (x: infer I) => void ? I : never
 */

// ✓ ExtractConfigChildren: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractConfigChildren(TConfig) {
  const m1 = matches<{ invoke: Hole<"TInvoke">; }>(TConfig)
  if (m1) {
    return ExtractInvokeChildren(m1.TInvoke)
  }
  return {}
}
/* compiles to:
 * export type ExtractConfigChildren<TConfig> = TConfig extends { invoke: infer TInvoke; } ? ExtractInvokeChildren<TInvoke> : {}
 */

// ✓ MergeChildrenMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MergeChildrenMap(TExplicit: Record<string, string>, TInferred: Record<string, string>) {
  if (matches<true>(IsNever(keyof(TExplicit)))) {
    return TInferred
  }
  return TExplicit
}
/* compiles to:
 * export type MergeChildrenMap<TExplicit extends Record<string, string>, TInferred extends Record<string, string>> = IsNever<keyof TExplicit> extends true ? TInferred : TExplicit
 */

// ✗ SetupReturn: uses raw() — language gap, does not count as covered
//   gap: generic function type; type node TypeQuery
/* @scripttype preserveParamNames */
export function SetupReturn(TContext: MachineContext, TEvent: AnyEventObject, TActors: Record<string, UnknownActorLogic>, TChildrenMap: Record<string, string>, TActions: Record<string, ParameterizedObject['params'] | undefined>, TGuards: Record<string, ParameterizedObject['params'] | undefined>, TDelay: string, TTag: string, TInput, TOutput: NonReducibleUnknown, TEmitted: EventObject, TMeta: MetaObject) {
  return { extend: raw('<TExtendActions extends Record<string, ParameterizedObject[\'params\'] | undefined> = {}, TExtendGuards extends Record<string, ParameterizedObject[\'params\'] | undefined> = {}, TExtendDelays extends string = never>({ actions, guards, delays }: { actions?: { [K in keyof TExtendActions]: ActionFunction<TContext, TEvent, TEvent, TExtendActions[K], ToProvidedActor<TChildrenMap, TActors>, ToParameterizedObject<TActions & TExtendActions>, ToParameterizedObject<TGuards & TExtendGuards>, TDelay | TExtendDelays, TEmitted>; }; guards?: { [K in keyof TExtendGuards]: GuardPredicate<TContext, TEvent, TExtendGuards[K], ToParameterizedObject<TGuards & TExtendGuards>>; }; delays?: { [K in TExtendDelays]: DelayConfig<TContext, TEvent, ToParameterizedObject<TActions & TExtendActions>[\'params\'], TEvent>; }; }) => SetupReturn<TContext, TEvent, TActors, TChildrenMap, TActions & TExtendActions, TGuards & TExtendGuards, TDelay | TExtendDelays, TTag, TInput, TOutput, TEmitted, TMeta>'), createStateConfig: raw('<TStateConfig extends StateNodeConfig<TContext, TEvent, ToProvidedActor<TChildrenMap, TActors>, ToParameterizedObject<TActions>, ToParameterizedObject<TGuards>, TDelay, TTag, unknown, TEmitted, TMeta>>(config: TStateConfig) => TStateConfig'), createAction: fnType([ActionFunction(TContext, TEvent, TEvent, unknown, ToProvidedActor(TChildrenMap, TActors), ToParameterizedObject(TActions), ToParameterizedObject(TGuards), TDelay, TEmitted)], raw('typeof action')), createMachine: raw('<const TConfig extends MachineConfig<TContext, TEvent, ToProvidedActor<TChildrenMap, TActors>, ToParameterizedObject<TActions>, ToParameterizedObject<TGuards>, TDelay, TTag, TInput, TOutput, TEmitted, TMeta>, TResolvedChildren extends Record<string, string> = MergeChildrenMap<TChildrenMap, Cast<ExtractConfigChildren<TConfig>, Record<string, string>>>>(config: TConfig) => StateMachine<TContext, TEvent | ([ RoutableStateId<TConfig> ] extends [ never ] ? never : { type: \'xstate.route\'; to: RoutableStateId<TConfig>; }), Cast<ToChildren<ToProvidedActor<TResolvedChildren, TActors>>, Record<string, AnyActorRef | undefined>>, ToProvidedActor<TResolvedChildren, TActors>, ToParameterizedObject<TActions>, ToParameterizedObject<TGuards>, TDelay, ToStateValue<TConfig>, TTag, TInput, TOutput, TEmitted, TMeta, ToStateSchema<TConfig>>'), assign: raw('typeof assign<TContext, TEvent, undefined, TEvent, ToProvidedActor<TChildrenMap, TActors>>'), sendTo: raw('<TTargetActor extends AnyActorRef>(...args: Parameters<typeof sendTo<TContext, TEvent, undefined, TTargetActor, TEvent, TDelay, TDelay>>) => ReturnType<typeof sendTo<TContext, TEvent, undefined, TTargetActor, TEvent, TDelay, TDelay>>'), raise: raw('typeof raise<TContext, TEvent, TEvent, undefined, TDelay, TDelay>'), log: raw('typeof log<TContext, TEvent, undefined, TEvent>'), cancel: raw('typeof cancel<TContext, TEvent, undefined, TEvent>'), stopChild: raw('typeof stopChild<TContext, TEvent, undefined, TEvent>'), enqueueActions: raw('typeof enqueueActions<TContext, TEvent, undefined, TEvent, ToProvidedActor<TChildrenMap, TActors>, ToParameterizedObject<TActions>, ToParameterizedObject<TGuards>, TDelay, TEmitted>'), emit: raw('typeof emit<TContext, TEvent, undefined, TEvent, TEmitted>'), spawnChild: raw('typeof spawnChild<TContext, TEvent, undefined, TEvent, ToProvidedActor<TChildrenMap, TActors>>') }
}

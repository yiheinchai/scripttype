/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/spawn.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 mismatch
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ActorRefFromLogic, AnyActorLogic, AnyActorRef, ConditionalRequired, GetConcreteByKey, InputFrom, IsLiteralString, IsNotNever, ProvidedActor, RequiredActorOptions, RequiredLogicInput } from '../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
declare const ActorRefFromLogic: any
declare const AnyActorLogic: any
declare const AnyActorRef: any
declare const ConditionalRequired: any
declare const GetConcreteByKey: any
declare const InputFrom: any
declare const IsLiteralString: any
declare const IsNotNever: any
declare const ProvidedActor: any
declare const RequiredActorOptions: any
declare const RequiredLogicInput: any
declare const TLogic: any
type TLogic<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ SpawnOptions: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
/**
 * @param {ProvidedActor} TActor
 * @param {(typeof TActor)['src']} TSrc
 */
export function SpawnOptions(TActor, TSrc): any {
  if (matches<{ src: typeof TSrc; }>(TActor)) {
    const out = emptyObject
    for (const K in keySet(RequiredActorOptions(TActor))) {
      out[K] = unknown
    }
    return ConditionalRequired([merge({ id: optional(TActor['id']), systemId: optional(string), input: optional(InputFrom(TActor['logic'])), syncSnapshot: optional(boolean) }, out)], IsNotNever(RequiredActorOptions(TActor)))
  }
  return never
}
/* compiles to:
 * export type SpawnOptions<TActor extends ProvidedActor, TSrc extends TActor['src']> =
 *   TActor extends { src: TSrc; }
 *     ? ConditionalRequired<
 *       [
 *         & {
 *             id?: TActor['id']
 *             systemId?: string
 *             input?: InputFrom<TActor['logic']>
 *             syncSnapshot?: boolean
 *           }
 *         & { [K in RequiredActorOptions<TActor>]: unknown }
 *       ],
 *       IsNotNever<RequiredActorOptions<TActor>>
 *     >
 *     : never
 */

// ✗ Spawner: compiles but is not type-identical yet
//   eq=false
/* @scripttype preserveParamNames */
export function Spawner(TActor: ProvidedActor): any {
  if (matches<true>(IsLiteralString(TActor['src']))) {
    const out = emptyObject
    for (const K in keySet(RequiredLogicInput(TLogic))) {
      out[K] = unknown
    }
    return { ...genericCallSig(['TSrc extends TActor[\'src\']'], [TSrc, ...SpawnOptions(TActor, TSrc)], ActorRefFromLogic(GetConcreteByKey(TActor, 'src', TSrc)['logic'])), ...genericCallSig(['TLogic extends AnyActorLogic'], [TLogic, ...ConditionalRequired([merge({ id: optional(never), systemId: optional(string), input: optional(InputFrom(TLogic)), syncSnapshot: optional(boolean) }, out)], IsNotNever(RequiredLogicInput(TLogic)))], ActorRefFromLogic(TLogic)) }
  }
  const out2 = emptyObject
  for (const K in keySet(RequiredLogicInput(TLogic))) {
    out2[K] = unknown
  }
  return genericFnType(['TLogic extends AnyActorLogic | string'], [TLogic, ...ConditionalRequired([merge({ id: optional(string), systemId: optional(string), input: optional(typeof TLogic === 'string' ? unknown : InputFrom(TLogic)), syncSnapshot: optional(boolean) }, matches<AnyActorLogic>(TLogic) ? out2 : {})], IsNotNever(matches<AnyActorLogic>(TLogic) ? RequiredLogicInput(TLogic) : never))], matches<AnyActorLogic>(TLogic) ? ActorRefFromLogic(TLogic) : AnyActorRef)
}
/* compiles to:
 * export type Spawner<TActor extends ProvidedActor> =
 *   IsLiteralString<TActor['src']> extends true
 *     ? {
 *       <TSrc extends TActor['src']>(a0: TSrc, ...a1: SpawnOptions<TActor, TSrc>): ActorRefFromLogic<
 *         GetConcreteByKey<TActor, 'src', TSrc>['logic']
 *       >
 *       <TLogic extends AnyActorLogic>(a0: TLogic, ...a1: ConditionalRequired<[{ id?: never; systemId?: string; input?: InputFrom<TLogic>; syncSnapshot?: boolean } & { [K in RequiredLogicInput<TLogic>]: unknown }], IsNotNever<RequiredLogicInput<TLogic>>>): ActorRefFromLogic<
 *         TLogic
 *       >
 *     }
 *     : <TLogic extends AnyActorLogic | string>(
 *       a0: TLogic,
 *       ...a1: ConditionalRequired<[{ id?: string; systemId?: string; input?: TLogic extends string ? unknown : InputFrom<TLogic>; syncSnapshot?: boolean } & (TLogic extends AnyActorLogic ? { [K1 in RequiredLogicInput<TLogic>]: unknown } : {})], IsNotNever<TLogic extends AnyActorLogic ? RequiredLogicInput<TLogic> : never>>
 *     ) => TLogic extends AnyActorLogic ? ActorRefFromLogic<TLogic> : AnyActorRef
 */

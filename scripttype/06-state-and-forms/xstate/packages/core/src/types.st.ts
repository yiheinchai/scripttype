/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 101 type-level aliases: 99 covered, 2 compile-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { MachineSnapshot } from '../../../../../../06-state-and-forms/xstate/packages/core/src/State.js'
import type { AssignArgs } from '../../../../../../06-state-and-forms/xstate/packages/core/src/actions/assign.js'
import type { PromiseActorLogic } from '../../../../../../06-state-and-forms/xstate/packages/core/src/actors/promise.js'
import type { Guard, GuardPredicate } from '../../../../../../06-state-and-forms/xstate/packages/core/src/guards.js'
import type { Spawner } from '../../../../../../06-state-and-forms/xstate/packages/core/src/spawn.js'
import type { AnyActorSystem } from '../../../../../../06-state-and-forms/xstate/packages/core/src/system.js'
import type { ActionArgs, ActorLogic, ActorRef, ActorScope, AnyActorLogic, AnyActorRef, AnyEventObject, AnyFunction, AnyStateMachine, AtomicStateNodeConfig, DoneActorEvent, DoneStateEvent, ErrorActorEvent, EventObject, MachineContext, MachineOptions, MetaObject, NonReducibleUnknown, ParameterizedObject, ProvidedActor, ResolvedStateMachineTypes, SnapshotEvent, SpecialExecutableAction, StateMachineTypes, StateNodeConfig, StateNodeDefinition, StateSchema, StateValue, TODO, ToExecutableAction, TransitionConfig, TransitionConfigTarget, TransitionDefinition } from '../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
declare namespace m1 {
  export type R<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type S<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TAction<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TActor<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TChildren<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TDelay<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmitted<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TGuard<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TLeading<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TNormalizedDescriptor<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TOutput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSnapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSrc<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TStateSchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TStateValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TTag<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TTail<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type TChildren<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmitted<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TMeta<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TOutput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSnapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TStateSchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TStateValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TTag<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m3 {
  export type TContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TLogic<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type U<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m4 {
  export type TActorLogic<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmitted<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSnapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m5 {
  export type TContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSnapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ActionArgs: any
declare const Actor: any
declare const ActorLogic: any
declare const ActorRef: any
declare const ActorScope: any
declare const AnyActorLogic: any
declare const AnyActorRef: any
declare const AnyActorSystem: any
declare const AnyEventObject: any
declare const AnyFunction: any
declare const AnyStateMachine: any
declare const AssignArgs: any
declare const AtomicStateNodeConfig: any
declare const DoneActorEvent: any
declare const DoneStateEvent: any
declare const ErrorActorEvent: any
declare const EventObject: any
declare const Guard: any
declare const GuardPredicate: any
declare const MachineContext: any
declare const MachineOptions: any
declare const MachineSnapshot: any
declare const MetaObject: any
declare const NonReducibleUnknown: any
declare const ParameterizedObject: any
declare const PromiseActorLogic: any
declare const ProvidedActor: any
declare const ResolvedStateMachineTypes: any
declare const SnapshotEvent: any
declare const Spawner: any
declare const SpecialExecutableAction: any
declare const StateMachine: any
declare const StateMachineTypes: any
declare const StateNode: any
declare const StateNodeConfig: any
declare const StateNodeDefinition: any
declare const StateSchema: any
declare const StateValue: any
declare const TODO: any
declare const ToExecutableAction: any
declare const TransitionConfig: any
declare const TransitionConfigTarget: any
declare const TransitionDefinition: any
type Actor<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type EventDescriptorMatches<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type InputFrom<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StateMachine<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StateNode<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Identity(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return out
}
/* compiles to:
 * export type Identity<T> = { [K in keyof T]: T[K] }
 */

// ✓ HomomorphicPick: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HomomorphicPick(T, K: keyof any): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[merge(P, K)] = T[P]
  }
  return out
}
/* compiles to:
 * export type HomomorphicPick<T, K extends keyof any> = { [P in keyof T as P & K]: T[P] }
 */

// ✓ HomomorphicOmit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HomomorphicOmit(T, K: keyof any): any {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[Exclude(P, K)] = T[P]
  }
  return out
}
/* compiles to:
 * export type HomomorphicOmit<T, K extends keyof any> = { [P in keyof T as Exclude<P, K>]: T[P] }
 */

// ✓ Invert: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<PropertyKey, PropertyKey>} T
 */
export function Invert(T): any {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[T[K]] = K
  }
  return out
}
/* compiles to:
 * export type Invert<T extends Record<PropertyKey, PropertyKey>> = { [K in keyof T as T[K]]: K }
 */

// ✓ GetParameterizedParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetParameterizedParams(T: ParameterizedObject | undefined): any {
  if (matches<any>(T)) {
    if ('params' in T) {
      return T['params']
    }
    return Undefined
  }
  return never
}
/* compiles to:
 * export type GetParameterizedParams<T extends ParameterizedObject | undefined> =
 *   T extends any ? 'params' extends keyof T ? T['params'] : undefined : never
 */

// ✓ ReturnTypeOrValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReturnTypeOrValue(T): any {
  if (matches<AnyFunction>(T)) {
    return ReturnType(T)
  }
  return T
}
/* compiles to:
 * export type ReturnTypeOrValue<T> = T extends AnyFunction ? ReturnType<T> : T
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T): any {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsNotNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNotNever(T): any {
  if (matches<[ never ]>([T])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsNotNever<T> = [T] extends [never] ? false : true
 */

// ✓ Compute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Compute(A): any {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  return out & unknown
}
/* compiles to:
 * export type Compute<A> = { [K in keyof A]: A[K] } & unknown
 */

// ✓ Prop: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prop(T, K): any {
  if (K in T) {
    return T[K]
  }
  return never
}
/* compiles to:
 * export type Prop<T, K> = K extends keyof T ? T[K] : never
 */

// ✓ Values: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Values(T): any {
  return T[keyof(T)]
}
/* compiles to:
 * export type Values<T> = T[keyof T]
 */

// ✓ Elements: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Elements(T): any {
  return T[merge(keyof(T), `${number}`)]
}
/* compiles to:
 * export type Elements<T> = T[keyof T & `${number}`]
 */

// ✓ Merge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Merge(M, N): any {
  return Omit(M, keyof(N)) & N
}
/* compiles to:
 * export type Merge<M, N> = Omit<M, keyof N> & N
 */

// ✓ IndexByProp: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Record<typeof P, string>} T
 * @param {keyof typeof T} P
 */
export function IndexByProp(T, P): any {
  const out = emptyObject
  for (const E in keySet(T)) {
    out[E[P]] = E
  }
  return out
}
/* compiles to:
 * export type IndexByProp<T extends Record<P, string>, P extends keyof T> = {
 *   [E in T as E[P]]: E
 * }
 */

// ✓ IndexByType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexByType(T: { type: string; }): any {
  return IndexByProp(T, 'type')
}
/* compiles to:
 * export type IndexByType<T extends { type: string; }> = IndexByProp<T, 'type'>
 */

// ✓ Equals: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Equals(A1, A2): any {
  if (matches<<A>() => A extends typeof A1 ? true : false>(genericFnType(['A'], [], matches<typeof A2>(A) ? true : false))) {
    return true
  }
  return false
}
/* compiles to:
 * export type Equals<A1, A2> =
 *   (<A>() => A extends A2 ? true : false) extends <A>() => A extends A1 ? true : false
 *     ? true
 *     : false
 */

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T): any {
  return Equals(T, any)
}
/* compiles to:
 * export type IsAny<T> = Equals<T, any>
 */

// ✓ Cast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Cast(A, B): any {
  if (matches<typeof B>(A)) {
    return A
  }
  return B
}
/* compiles to:
 * export type Cast<A, B> = A extends B ? A : B
 */

// ✓ DoNotInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DoNotInfer(T): any {
  return [T][matches<any>(T) ? 0 : any]
}
/* compiles to:
 * export type DoNotInfer<T> = [T][T extends any ? 0 : any]
 */

// ✓ NoInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoInfer(T): any {
  return DoNotInfer(T)
}
/* compiles to:
 * export type NoInfer<T> = DoNotInfer<T>
 */

// ✓ LowInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LowInfer(T): any {
  return T & NonNullable(unknown)
}
/* compiles to:
 * export type LowInfer<T> = T & NonNullable<unknown>
 */

// ✓ Lazy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Lazy(T): any {
  return fnType([], T)
}
/* compiles to:
 * export type Lazy<T> = () => T
 */

// ✓ MaybeLazy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeLazy(T): any {
  return T | Lazy(T)
}
/* compiles to:
 * export type MaybeLazy<T> = T | Lazy<T>
 */

// ✓ InputFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InputFrom(T): any {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(T)
  if (m1) {
    return m1.TInput
  }
  const m2 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"TInput">, Hole<"_TSystem">, Hole<"_TEmitted">>>(T)
  if (m2) {
    return m2.TInput
  }
  return never
}
/* compiles to:
 * export type InputFrom<T> =
 *   T extends StateMachine<any, any, any, any, any, any, any, any, any, infer TInput, any, any, any, any>
 *     ? TInput
 *   : T extends ActorLogic<any, any, infer TInput, any, any> ? TInput
 *   : never
 */

// ✓ OutputFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function OutputFrom(T): any {
  const m1 = matches<ActorLogic<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"_TEmitted">>>(T)
  if (m1) {
    return (merge(m1.TSnapshot, { status: 'done' }))['output']
  }
  const m2 = matches<ActorRef<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TEmitted">>>(T)
  if (m2) {
    return (merge(m2.TSnapshot, { status: 'done' }))['output']
  }
  return never
}
/* compiles to:
 * export type OutputFrom<T> =
 *   T extends ActorLogic<infer TSnapshot, any, any, any, any>
 *     ? (TSnapshot & { status: 'done' })['output']
 *   : T extends ActorRef<infer TSnapshot, any, any> ? (TSnapshot & { status: 'done' })['output']
 *   : never
 */

// ✓ ActionFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActionFunction(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject): any {
  return { ...callSig([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], voidType()), _out_TEvent: optional(TEvent), _out_TActor: optional(TActor), _out_TAction: optional(TAction), _out_TGuard: optional(TGuard), _out_TDelay: optional(TDelay), _out_TEmitted: optional(TEmitted) }
}
/* compiles to:
 * export type ActionFunction<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject
 * > =
 *   {
 *     (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams): void
 *     _out_TEvent?: TEvent
 *     _out_TActor?: TActor
 *     _out_TAction?: TAction
 *     _out_TGuard?: TGuard
 *     _out_TDelay?: TDelay
 *     _out_TEmitted?: TEmitted
 *   }
 */

// ✓ NoRequiredParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoRequiredParams(T: ParameterizedObject): any {
  if (matches<any>(T)) {
    if (matches<(typeof T)['params']>(Undefined)) {
      return T['type']
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NoRequiredParams<T extends ParameterizedObject> =
 *   T extends any ? undefined extends T['params'] ? T['type'] : never : never
 */

// ✓ ConditionalRequired: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConditionalRequired(T, Condition: boolean): any {
  if (matches<true>(Condition)) {
    return Required(T)
  }
  return T
}
/* compiles to:
 * export type ConditionalRequired<T, Condition extends boolean> =
 *   Condition extends true ? Required<T> : T
 */

// ✓ WithDynamicParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDynamicParams(TContext: MachineContext, TExpressionEvent: EventObject, T: ParameterizedObject): any {
  if (matches<any>(T)) {
    return ConditionalRequired({ type: T['type'], params: optional(anyOf(T['params'], fnType([{ context: TContext, event: TExpressionEvent }], T['params']))) }, matches<(typeof T)['params']>(Undefined) ? false : true)
  }
  return never
}
/* compiles to:
 * export type WithDynamicParams<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   T extends ParameterizedObject
 * > =
 *   T extends any
 *     ? ConditionalRequired<
 *       {
 *         type: T['type']
 *         params?: | T['params']
 *         | ((a0: { context: TContext; event: TExpressionEvent }) => T['params'])
 *       },
 *       undefined extends T['params'] ? false : true
 *     >
 *     : never
 */

// ✓ Action: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Action(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject): any {
  return NoRequiredParams(TAction) | WithDynamicParams(TContext, TExpressionEvent, TAction) | ActionFunction(TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted)
}
/* compiles to:
 * export type Action<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject
 * > =
 *   | NoRequiredParams<TAction>
 *   | WithDynamicParams<TContext, TExpressionEvent, TAction>
 *   | ActionFunction<
 *       TContext,
 *       TExpressionEvent,
 *       TEvent,
 *       TParams,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TEmitted
 *     >
 */

// ✓ Actions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Actions(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject): any {
  return SingleOrArray(Action(TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted))
}
/* compiles to:
 * export type Actions<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject
 * > =
 *   SingleOrArray<
 *     Action<
 *       TContext,
 *       TExpressionEvent,
 *       TEvent,
 *       TParams,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TEmitted
 *     >
 *   >
 */

// ✓ Delay: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Delay(TDelay: string): any {
  return anyOf(TDelay, number)
}
/* compiles to:
 * export type Delay<TDelay extends string> = TDelay | number
 */

// ✓ DelayedTransitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayedTransitions(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string): any {
  const out = emptyObject
  for (const K in keySet(Delay(TDelay))) {
    out[K] = optional(string | SingleOrArray(TransitionConfig(TContext, TEvent, TEvent, TActor, TAction, TGuard, TDelay, TODO, TODO)))
  }
  return out
}
/* compiles to:
 * export type DelayedTransitions<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string
 * > =
 *   {
 *     [K in Delay<TDelay>]?: | string
 *     | SingleOrArray<
 *         TransitionConfig<TContext, TEvent, TEvent, TActor, TAction, TGuard, TDelay, TODO, TODO>
 *       >
 *   }
 */

// ✓ SingleOrArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SingleOrArray(T): any {
  return readonlyArrayOf(T) | T
}
/* compiles to:
 * export type SingleOrArray<T> = readonly T[] | T
 */

// ✓ StateNodesConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateNodesConfig(TContext: MachineContext, TEvent: EventObject): any {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNode(TContext, TEvent)
  }
  return out
}
/* compiles to:
 * export type StateNodesConfig<TContext extends MachineContext, TEvent extends EventObject> = {
 *   [K in string]: StateNode<TContext, TEvent>
 * }
 */

// ✓ StatesConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StatesConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TTag: string, TOutput, TEmitted: EventObject, TMeta: MetaObject): any {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNodeConfig(TContext, TEvent, TActor, TAction, TGuard, TDelay, TTag, TOutput, TEmitted, TMeta)
  }
  return out
}
/* compiles to:
 * export type StatesConfig<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TTag extends string,
 *   TOutput,
 *   TEmitted extends EventObject,
 *   TMeta extends MetaObject
 * > =
 *   {
 *     [K in string]: StateNodeConfig<
 *       TContext,
 *       TEvent,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TTag,
 *       TOutput,
 *       TEmitted,
 *       TMeta
 *     >
 *   }
 */

// ✓ StatesDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StatesDefinition(TContext: MachineContext, TEvent: EventObject): any {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNodeDefinition(TContext, TEvent)
  }
  return out
}
/* compiles to:
 * export type StatesDefinition<TContext extends MachineContext, TEvent extends EventObject> = {
 *   [K in string]: StateNodeDefinition<TContext, TEvent>
 * }
 */

// ✓ TransitionConfigOrTarget: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionConfigOrTarget(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject): any {
  return SingleOrArray(anyOf(TransitionConfigTarget, TransitionConfig(TContext, TExpressionEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta)))
}
/* compiles to:
 * export type TransitionConfigOrTarget<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject,
 *   TMeta extends MetaObject
 * > =
 *   SingleOrArray<
 *     | TransitionConfigTarget
 *     | TransitionConfig<
 *         TContext,
 *         TExpressionEvent,
 *         TEvent,
 *         TActor,
 *         TAction,
 *         TGuard,
 *         TDelay,
 *         TEmitted,
 *         TMeta
 *       >
 *   >
 */

// ✓ TransitionsConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionsConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject): any {
  const out = emptyObject
  for (const K in keySet(EventDescriptor(TEvent))) {
    out[K] = optional(TransitionConfigOrTarget(TContext, ExtractEvent(TEvent, K), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))
  }
  return out
}
/* compiles to:
 * export type TransitionsConfig<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject,
 *   TMeta extends MetaObject
 * > =
 *   {
 *     [K in EventDescriptor<TEvent>]?: TransitionConfigOrTarget<
 *       TContext,
 *       ExtractEvent<TEvent, K>,
 *       TEvent,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TEmitted,
 *       TMeta
 *     >
 *   }
 */

// ✓ PartialEventDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PartialEventDescriptor(TEventType: string): any {
  const m1 = matches<`${Hole<"TLeading">}.${Hole<"TTail">}`>(TEventType)
  if (m1) {
    return anyOf(`${m1.TLeading}.*`, `${m1.TLeading}.${PartialEventDescriptor(m1.TTail)}`)
  }
  return never
}
/* compiles to:
 * export type PartialEventDescriptor<TEventType extends string> =
 *   TEventType extends `${infer TLeading}.${infer TTail}`
 *     ? `${TLeading}.*` | `${TLeading}.${PartialEventDescriptor<TTail>}`
 *     : never
 */

// ✓ EventDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventDescriptor(TEvent: EventObject): any {
  return anyOf(TEvent['type'], PartialEventDescriptor(TEvent['type']), '*')
}
/* compiles to:
 * export type EventDescriptor<TEvent extends EventObject> =
 *   TEvent['type'] | PartialEventDescriptor<TEvent['type']> | '*'
 */

// ✓ NormalizeDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizeDescriptor(TDescriptor: string): any {
  if (matches<'*'>(TDescriptor)) {
    return string
  }
  const m1 = matches<`${Hole<"TLeading">}.*`>(TDescriptor)
  if (m1) {
    return `${m1.TLeading}.${string}`
  }
  return TDescriptor
}
/* compiles to:
 * export type NormalizeDescriptor<TDescriptor extends string> =
 *   TDescriptor extends '*' ? string
 *   : TDescriptor extends `${infer TLeading}.*` ? `${TLeading}.${string}`
 *   : TDescriptor
 */

// ✓ IsLiteralString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsLiteralString(T: string): any {
  if (matches<typeof T>(string)) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsLiteralString<T extends string> = string extends T ? false : true
 */

// ✓ DistributeActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DistributeActors(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject, TSpecificActor: ProvidedActor): any {
  const m1 = matches<{ src: Hole<"TSrc">; }>(TSpecificActor)
  if (m1) {
    const out = emptyObject
    for (const K in keySet(RequiredActorOptions(TSpecificActor))) {
      out[K] = unknown
    }
    return anyOf(Compute(merge({ systemId: optional(string), src: m1.TSrc, id: optional(TSpecificActor['id']), input: optional(Mapper(TContext, TEvent, InputFrom(TSpecificActor['logic']), TEvent) | InputFrom(TSpecificActor['logic'])), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(OutputFrom(TSpecificActor['logic'])), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent(SnapshotFrom(TSpecificActor['logic'])), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) }, out)), { id: optional(never), systemId: optional(string), src: AnyActorLogic, input: optional(Mapper(TContext, TEvent, NonReducibleUnknown, TEvent) | NonReducibleUnknown), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(unknown), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) })
  }
  return never
}
/* compiles to:
 * export type DistributeActors<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject,
 *   TMeta extends MetaObject,
 *   TSpecificActor extends ProvidedActor
 * > =
 *   TSpecificActor extends { src: infer TSrc; }
 *     ? | Compute<
 *         & {
 *             systemId?: string
 *             src: TSrc
 *             id?: TSpecificActor['id']
 *             input?: | Mapper<TContext, TEvent, InputFrom<TSpecificActor['logic']>, TEvent>
 *             | InputFrom<TSpecificActor['logic']>
 *             onDone?: | string
 *             | SingleOrArray<
 *                 TransitionConfigOrTarget<
 *                   TContext,
 *                   DoneActorEvent<OutputFrom<TSpecificActor['logic']>>,
 *                   TEvent,
 *                   TActor,
 *                   TAction,
 *                   TGuard,
 *                   TDelay,
 *                   TEmitted,
 *                   TMeta
 *                 >
 *               >
 *             onError?: | string
 *             | SingleOrArray<
 *                 TransitionConfigOrTarget<
 *                   TContext,
 *                   ErrorActorEvent,
 *                   TEvent,
 *                   TActor,
 *                   TAction,
 *                   TGuard,
 *                   TDelay,
 *                   TEmitted,
 *                   TMeta
 *                 >
 *               >
 *             onSnapshot?: | string
 *             | SingleOrArray<
 *                 TransitionConfigOrTarget<
 *                   TContext,
 *                   SnapshotEvent<SnapshotFrom<TSpecificActor['logic']>>,
 *                   TEvent,
 *                   TActor,
 *                   TAction,
 *                   TGuard,
 *                   TDelay,
 *                   TEmitted,
 *                   TMeta
 *                 >
 *               >
 *           }
 *         & { [K in RequiredActorOptions<TSpecificActor>]: unknown }
 *       >
 *     | {
 *         id?: never
 *         systemId?: string
 *         src: AnyActorLogic
 *         input?: Mapper<TContext, TEvent, NonReducibleUnknown, TEvent> | NonReducibleUnknown
 *         onDone?: | string
 *         | SingleOrArray<
 *             TransitionConfigOrTarget<
 *               TContext,
 *               DoneActorEvent<unknown>,
 *               TEvent,
 *               TActor,
 *               TAction,
 *               TGuard,
 *               TDelay,
 *               TEmitted,
 *               TMeta
 *             >
 *           >
 *         onError?: | string
 *         | SingleOrArray<
 *             TransitionConfigOrTarget<
 *               TContext,
 *               ErrorActorEvent,
 *               TEvent,
 *               TActor,
 *               TAction,
 *               TGuard,
 *               TDelay,
 *               TEmitted,
 *               TMeta
 *             >
 *           >
 *         onSnapshot?: | string
 *         | SingleOrArray<
 *             TransitionConfigOrTarget<
 *               TContext,
 *               SnapshotEvent,
 *               TEvent,
 *               TActor,
 *               TAction,
 *               TGuard,
 *               TDelay,
 *               TEmitted,
 *               TMeta
 *             >
 *           >
 *       }
 *     : never
 */

// ✓ InvokeConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvokeConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject): any {
  if (matches<true>(IsLiteralString(TActor['src']))) {
    return DistributeActors(TContext, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta, TActor)
  }
  return { id: optional(string), systemId: optional(string), src: AnyActorLogic | string, input: optional(Mapper(TContext, TEvent, NonReducibleUnknown, TEvent) | NonReducibleUnknown), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(any), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) }
}
/* compiles to:
 * export type InvokeConfig<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject,
 *   TGuard extends ParameterizedObject,
 *   TDelay extends string,
 *   TEmitted extends EventObject,
 *   TMeta extends MetaObject
 * > =
 *   IsLiteralString<TActor['src']> extends true
 *     ? DistributeActors<
 *       TContext,
 *       TEvent,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TEmitted,
 *       TMeta,
 *       TActor
 *     >
 *     : {
 *       id?: string
 *       systemId?: string
 *       src: AnyActorLogic | string
 *       input?: Mapper<TContext, TEvent, NonReducibleUnknown, TEvent> | NonReducibleUnknown
 *       onDone?: | string
 *       | SingleOrArray<
 *           TransitionConfigOrTarget<
 *             TContext,
 *             DoneActorEvent<any>,
 *             TEvent,
 *             TActor,
 *             TAction,
 *             TGuard,
 *             TDelay,
 *             TEmitted,
 *             TMeta
 *           >
 *         >
 *       onError?: | string
 *       | SingleOrArray<
 *           TransitionConfigOrTarget<
 *             TContext,
 *             ErrorActorEvent,
 *             TEvent,
 *             TActor,
 *             TAction,
 *             TGuard,
 *             TDelay,
 *             TEmitted,
 *             TMeta
 *           >
 *         >
 *       onSnapshot?: | string
 *       | SingleOrArray<
 *           TransitionConfigOrTarget<
 *             TContext,
 *             SnapshotEvent,
 *             TEvent,
 *             TActor,
 *             TAction,
 *             TGuard,
 *             TDelay,
 *             TEmitted,
 *             TMeta
 *           >
 *         >
 *     }
 */

// ✓ SimpleOrStateNodeConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimpleOrStateNodeConfig(TContext: MachineContext, TEvent: EventObject): any {
  return AtomicStateNodeConfig(TContext, TEvent) | StateNodeConfig(TContext, TEvent, TODO, TODO, TODO, TODO, TODO, TODO, TODO, TODO)
}
/* compiles to:
 * export type SimpleOrStateNodeConfig<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject
 * > =
 *   | AtomicStateNodeConfig<TContext, TEvent>
 *   | StateNodeConfig<TContext, TEvent, TODO, TODO, TODO, TODO, TODO, TODO, TODO, TODO>
 */

// ✗ ActionFunctionMap: does not compile yet
//   'K' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function ActionFunctionMap(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject = ParameterizedObject, TGuard: ParameterizedObject = ParameterizedObject, TDelay: string = string, TEmitted: EventObject = EventObject): any {
  const out = emptyObject
  for (const K in keySet(TAction['type'])) {
    out[K] = optional(ActionFunction(TContext, TEvent, TEvent, GetParameterizedParams(matches<{ type: typeof K; }>(TAction) ? TAction : never), TActor, TAction, TGuard, TDelay, TEmitted))
  }
  return out
}
/* compiles to:
 * export type ActionFunctionMap<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TAction extends ParameterizedObject = ParameterizedObject,
 *   TGuard extends ParameterizedObject = ParameterizedObject,
 *   TDelay extends string = string,
 *   TEmitted extends EventObject = EventObject
 * > =
 *   {
 *     [K in TAction['type']]?: ActionFunction<
 *       TContext,
 *       TEvent,
 *       TEvent,
 *       GetParameterizedParams<TAction extends { type: typeof K; } ? TAction : never>,
 *       TActor,
 *       TAction,
 *       TGuard,
 *       TDelay,
 *       TEmitted
 *     >
 *   }
 */

// ✗ GuardMap: does not compile yet
//   'K' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function GuardMap(TContext: MachineContext, TEvent: EventObject, TGuard: ParameterizedObject): any {
  const out = emptyObject
  for (const K in keySet(TGuard['type'])) {
    out[K] = optional(GuardPredicate(TContext, TEvent, GetParameterizedParams(matches<{ type: typeof K; }>(TGuard) ? TGuard : never), TGuard))
  }
  return out
}
/* compiles to:
 * export type GuardMap<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TGuard extends ParameterizedObject
 * > =
 *   {
 *     [K in TGuard['type']]?: GuardPredicate<
 *       TContext,
 *       TEvent,
 *       GetParameterizedParams<TGuard extends { type: typeof K; } ? TGuard : never>,
 *       TGuard
 *     >
 *   }
 */

// ✓ DelayFunctionMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayFunctionMap(TContext: MachineContext, TEvent: EventObject, TAction: ParameterizedObject): any {
  return Record(string, DelayConfig(TContext, TEvent, TAction['params'], TEvent))
}
/* compiles to:
 * export type DelayFunctionMap<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TAction extends ParameterizedObject
 * > =
 *   Record<string, DelayConfig<TContext, TEvent, TAction['params'], TEvent>>
 */

// ✓ DelayConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayConfig(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject): any {
  return number | DelayExpr(TContext, TExpressionEvent, TParams, TEvent)
}
/* compiles to:
 * export type DelayConfig<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject
 * > =
 *   number | DelayExpr<TContext, TExpressionEvent, TParams, TEvent>
 */

// ✓ MachineImplementationsActions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsActions(TTypes: StateMachineTypes): any {
  const out = emptyObject
  for (const K in keySet(TTypes['actions']['type'])) {
    out[K] = optional(ActionFunction(TTypes['context'], TTypes['events'], TTypes['events'], GetConcreteByKey(TTypes['actions'], 'type', K)['params'], TTypes['actors'], TTypes['actions'], TTypes['guards'], TTypes['delays'], TTypes['emitted']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsActions<TTypes extends StateMachineTypes> = {
 *   [K in TTypes['actions']['type']]?: ActionFunction<
 *     TTypes['context'],
 *     TTypes['events'],
 *     TTypes['events'],
 *     GetConcreteByKey<TTypes['actions'], 'type', K>['params'],
 *     TTypes['actors'],
 *     TTypes['actions'],
 *     TTypes['guards'],
 *     TTypes['delays'],
 *     TTypes['emitted']
 *   >
 * }
 */

// ✓ MachineImplementationsActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsActors(TTypes: StateMachineTypes): any {
  const out = emptyObject
  for (const K in keySet(TTypes['actors']['src'])) {
    out[K] = optional(GetConcreteByKey(TTypes['actors'], 'src', K)['logic'])
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsActors<TTypes extends StateMachineTypes> = {
 *   [K in TTypes['actors']['src']]?: GetConcreteByKey<TTypes['actors'], 'src', K>['logic']
 * }
 */

// ✓ MachineImplementationsDelays: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsDelays(TTypes: StateMachineTypes): any {
  const out = emptyObject
  for (const K in keySet(TTypes['delays'])) {
    out[K] = optional(DelayConfig(TTypes['context'], TTypes['events'], Undefined, TTypes['events']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsDelays<TTypes extends StateMachineTypes> = {
 *   [K in TTypes['delays']]?: DelayConfig<
 *     TTypes['context'],
 *     TTypes['events'],
 *     undefined,
 *     TTypes['events']
 *   >
 * }
 */

// ✓ MachineImplementationsGuards: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsGuards(TTypes: StateMachineTypes): any {
  const out = emptyObject
  for (const K in keySet(TTypes['guards']['type'])) {
    out[K] = optional(Guard(TTypes['context'], TTypes['events'], GetConcreteByKey(TTypes['guards'], 'type', K)['params'], TTypes['guards']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsGuards<TTypes extends StateMachineTypes> = {
 *   [K in TTypes['guards']['type']]?: Guard<
 *     TTypes['context'],
 *     TTypes['events'],
 *     GetConcreteByKey<TTypes['guards'], 'type', K>['params'],
 *     TTypes['guards']
 *   >
 * }
 */

// ✓ InternalMachineImplementations: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InternalMachineImplementations(TTypes: StateMachineTypes): any {
  return { actions: optional(MachineImplementationsActions(TTypes)), actors: optional(MachineImplementationsActors(TTypes)), delays: optional(MachineImplementationsDelays(TTypes)), guards: optional(MachineImplementationsGuards(TTypes)) }
}
/* compiles to:
 * export type InternalMachineImplementations<TTypes extends StateMachineTypes> = {
 *   actions?: MachineImplementationsActions<TTypes>
 *   actors?: MachineImplementationsActors<TTypes>
 *   delays?: MachineImplementationsDelays<TTypes>
 *   guards?: MachineImplementationsGuards<TTypes>
 * }
 */

// ✓ InitialContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InitialContext(TContext: MachineContext, TActor: ProvidedActor, TInput, TEvent: EventObject): any {
  return anyOf(TContext, ContextFactory(TContext, TActor, TInput, TEvent))
}
/* compiles to:
 * export type InitialContext<
 *   TContext extends MachineContext,
 *   TActor extends ProvidedActor,
 *   TInput,
 *   TEvent extends EventObject
 * > =
 *   TContext | ContextFactory<TContext, TActor, TInput, TEvent>
 */

// ✓ ContextFactory: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ContextFactory(TContext: MachineContext, TActor: ProvidedActor, TInput, TEvent: EventObject = EventObject): any {
  return fnType([{ spawn: Spawner(TActor), input: TInput, self: ActorRef(MachineSnapshot(TContext, TEvent, Record(string, anyOf(AnyActorRef, Undefined)), StateValue, string, unknown, TODO, TODO), TEvent, AnyEventObject) }], TContext)
}
/* compiles to:
 * export type ContextFactory<
 *   TContext extends MachineContext,
 *   TActor extends ProvidedActor,
 *   TInput,
 *   TEvent extends EventObject = EventObject
 * > =
 *   (
 *     a0: { spawn: Spawner<TActor>; input: TInput; self: ActorRef<MachineSnapshot<TContext, TEvent, Record<string, AnyActorRef | undefined>, StateValue, string, unknown, TODO, TODO>, TEvent, AnyEventObject> }
 *   ) => TContext
 */

// ✓ MachineConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor = ProvidedActor, TAction: ParameterizedObject = ParameterizedObject, TGuard: ParameterizedObject = ParameterizedObject, TDelay: string = string, TTag: string = string, TInput = any, TOutput = unknown, TEmitted: EventObject = EventObject, TMeta: MetaObject = MetaObject): any {
  return merge(merge(Omit(StateNodeConfig(DoNotInfer(TContext), DoNotInfer(TEvent), DoNotInfer(TActor), DoNotInfer(TAction), DoNotInfer(TGuard), DoNotInfer(TDelay), DoNotInfer(TTag), DoNotInfer(TOutput), DoNotInfer(TEmitted), DoNotInfer(TMeta)), 'output'), { version: optional(string), output: optional(Mapper(TContext, DoneStateEvent, TOutput, TEvent) | TOutput), options: optional(MachineOptions) }), matches<typeof TContext>(MachineContext) ? { context: optional(InitialContext(LowInfer(TContext), TActor, TInput, TEvent)) } : { context: InitialContext(LowInfer(TContext), TActor, TInput, TEvent) })
}
/* compiles to:
 * export type MachineConfig<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor = ProvidedActor,
 *   TAction extends ParameterizedObject = ParameterizedObject,
 *   TGuard extends ParameterizedObject = ParameterizedObject,
 *   TDelay extends string = string,
 *   TTag extends string = string,
 *   TInput = any,
 *   TOutput = unknown,
 *   TEmitted extends EventObject = EventObject,
 *   TMeta extends MetaObject = MetaObject
 * > =
 *   & Omit<
 *       StateNodeConfig<
 *         DoNotInfer<TContext>,
 *         DoNotInfer<TEvent>,
 *         DoNotInfer<TActor>,
 *         DoNotInfer<TAction>,
 *         DoNotInfer<TGuard>,
 *         DoNotInfer<TDelay>,
 *         DoNotInfer<TTag>,
 *         DoNotInfer<TOutput>,
 *         DoNotInfer<TEmitted>,
 *         DoNotInfer<TMeta>
 *       >,
 *       'output'
 *     >
 *   & {
 *       version?: string
 *       output?: Mapper<TContext, DoneStateEvent, TOutput, TEvent> | TOutput
 *       options?: MachineOptions
 *     }
 *   & (
 *       MachineContext extends TContext
 *         ? { context?: InitialContext<LowInfer<TContext>, TActor, TInput, TEvent> }
 *         : { context: InitialContext<LowInfer<TContext>, TActor, TInput, TEvent> }
 *     )
 */

// ✓ HistoryValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HistoryValue(TContext: MachineContext, TEvent: EventObject): any {
  return Record(string, arrayOf(StateNode(TContext, TEvent)))
}
/* compiles to:
 * export type HistoryValue<TContext extends MachineContext, TEvent extends EventObject> = Record<
 *   string,
 *   StateNode<TContext, TEvent>[]
 * >
 */

// ✓ StateFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)): any {
  if (matches<AnyStateMachine>(T)) {
    return ReturnType(T['transition'])
  }
  if (matches<(...args: any[]) => AnyStateMachine>(T)) {
    return ReturnType(ReturnType(T)['transition'])
  }
  return never
}
/* compiles to:
 * export type StateFrom<T extends AnyStateMachine | (...args: any[]) => AnyStateMachine> =
 *   T extends AnyStateMachine ? ReturnType<T['transition']>
 *   : T extends (...args: any[]) => AnyStateMachine ? ReturnType<ReturnType<T>['transition']>
 *   : never
 */

// ✓ Transitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Transitions(TContext: MachineContext, TEvent: EventObject): any {
  return arrayOf(TransitionDefinition(TContext, TEvent))
}
/* compiles to:
 * export type Transitions<TContext extends MachineContext, TEvent extends EventObject> =
 *   TransitionDefinition<TContext, TEvent>[]
 */

// ✓ DelayExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject): any {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], number)
}
/* compiles to:
 * export type DelayExpr<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject
 * > =
 *   (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => number
 */

// ✓ LogExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LogExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject): any {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], unknown)
}
/* compiles to:
 * export type LogExpr<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject
 * > =
 *   (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => unknown
 */

// ✓ SendExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SendExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TSentEvent: EventObject, TEvent: EventObject): any {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], TSentEvent)
}
/* compiles to:
 * export type SendExpr<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TSentEvent extends EventObject,
 *   TEvent extends EventObject
 * > =
 *   (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => TSentEvent
 */

// ✓ Assigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Assigner(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor): any {
  return fnType([AssignArgs(TContext, TExpressionEvent, TEvent, TActor), TParams], Partial(TContext))
}
/* compiles to:
 * export type Assigner<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor
 * > =
 *   (a0: AssignArgs<TContext, TExpressionEvent, TEvent, TActor>, a1: TParams) => Partial<TContext>
 */

// ✓ PartialAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {MachineContext} TContext
 * @param {keyof typeof TContext} TKey
 */
export function PartialAssigner(TContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor, TKey): any {
  return fnType([AssignArgs(TContext, TExpressionEvent, TEvent, TActor), TParams], TContext[TKey])
}
/* compiles to:
 * export type PartialAssigner<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor,
 *   TKey extends keyof TContext
 * > =
 *   (a0: AssignArgs<TContext, TExpressionEvent, TEvent, TActor>, a1: TParams) => TContext[TKey]
 */

// ✓ PropertyAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {MachineContext} TContext
 */
export function PropertyAssigner(TContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor): any {
  const out = emptyObject
  for (const K in keyof(TContext)) {
    out[K] = optional(PartialAssigner(TContext, TExpressionEvent, TParams, TEvent, TActor, K) | TContext[K])
  }
  return out
}
/* compiles to:
 * export type PropertyAssigner<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TParams extends ParameterizedObject['params'] | undefined,
 *   TEvent extends EventObject,
 *   TActor extends ProvidedActor
 * > =
 *   {
 *     [K in keyof TContext]?: | PartialAssigner<TContext, TExpressionEvent, TParams, TEvent, TActor, K>
 *     | TContext[K]
 *   }
 */

// ✓ Mapper: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mapper(TContext: MachineContext, TExpressionEvent: EventObject, TResult, TEvent: EventObject): any {
  return fnType([{ context: TContext, event: TExpressionEvent, self: ActorRef(MachineSnapshot(TContext, TEvent, Record(string, AnyActorRef), StateValue, string, unknown, TODO, TODO), TEvent, AnyEventObject) }], TResult)
}
/* compiles to:
 * export type Mapper<
 *   TContext extends MachineContext,
 *   TExpressionEvent extends EventObject,
 *   TResult,
 *   TEvent extends EventObject
 * > =
 *   (
 *     a0: { context: TContext; event: TExpressionEvent; self: ActorRef<MachineSnapshot<TContext, TEvent, Record<string, AnyActorRef>, StateValue, string, unknown, TODO, TODO>, TEvent, AnyEventObject> }
 *   ) => TResult
 */

// ✓ TransitionDefinitionMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionDefinitionMap(TContext: MachineContext, TEvent: EventObject): any {
  const out = emptyObject
  for (const K in keySet(EventDescriptor(TEvent))) {
    out[K] = arrayOf(TransitionDefinition(TContext, ExtractEvent(TEvent, K)))
  }
  return out
}
/* compiles to:
 * export type TransitionDefinitionMap<
 *   TContext extends MachineContext,
 *   TEvent extends EventObject
 * > =
 *   { [K in EventDescriptor<TEvent>]: TransitionDefinition<TContext, ExtractEvent<TEvent, K>>[] }
 */

// ✓ Observer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Observer(T): any {
  return { next: optional(fnType([T], voidType())), error: optional(fnType([unknown], voidType())), complete: optional(fnType([], voidType())) }
}
/* compiles to:
 * export type Observer<T> = {
 *   next?: (a0: T) => void
 *   error?: (a0: unknown) => void
 *   complete?: () => void
 * }
 */

// ✓ EventDescriptorMatches: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventDescriptorMatches(TEventType: string, TNormalizedDescriptor): any {
  if (matches<typeof TNormalizedDescriptor>(TEventType)) {
    return true
  }
  return false
}
/* compiles to:
 * export type EventDescriptorMatches<TEventType extends string, TNormalizedDescriptor> =
 *   TEventType extends TNormalizedDescriptor ? true : false
 */

// ✓ ExtractEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EventObject} TEvent
 * @param {EventDescriptor<typeof TEvent>} TDescriptor
 */
export function ExtractEvent(TEvent, TDescriptor): any {
  if (matches<(typeof TEvent)['type']>(string)) {
    return TEvent
  }
  const m1 = matches<Hole<"TNormalizedDescriptor">>(NormalizeDescriptor(TDescriptor))
  if (m1) {
    if (matches<any>(TEvent)) {
      if (matches<EventDescriptorMatches<(typeof TEvent)['type'], typeof m1.TNormalizedDescriptor>>(true)) {
        return TEvent
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractEvent<
 *   TEvent extends EventObject,
 *   TDescriptor extends EventDescriptor<TEvent>
 * > =
 *   string extends TEvent['type'] ? TEvent
 *   : NormalizeDescriptor<TDescriptor> extends infer TNormalizedDescriptor
 *     ? TEvent extends any
 *       ? true extends EventDescriptorMatches<TEvent['type'], TNormalizedDescriptor>
 *         ? TEvent
 *         : never
 *       : never
 *   : never
 */

// ✓ ActorLogicFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActorLogicFrom(T): any {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    if (matches<StateMachine<any, any, any, any, any, any, any, any, any, any, any, any, any, any>>(m1.R)) {
      return m1.R
    }
    const m2 = matches<Promise<Hole<"U">>>(m1.R)
    if (m2) {
      return PromiseActorLogic(m2.U)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ActorLogicFrom<T> =
 *   ReturnTypeOrValue<T> extends infer R
 *     ? R extends StateMachine<any, any, any, any, any, any, any, any, any, any, any, any, any, any>
 *       ? R
 *     : R extends Promise<infer U> ? PromiseActorLogic<U>
 *     : never
 *     : never
 */

// ✓ ActorRefFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActorRefFrom(T): any {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"TStateValue">, Hole<"TTag">, Hole<"_TInput">, Hole<"TOutput">, Hole<"TEmitted">, Hole<"TMeta">, Hole<"TStateSchema">>>(m1.R)
    if (m2) {
      return ActorRef(MachineSnapshot(m2.TContext, m2.TEvent, m2.TChildren, m2.TStateValue, m2.TTag, m2.TOutput, m2.TMeta, m2.TStateSchema), m2.TEvent, m2.TEmitted)
    }
    const m3 = matches<Promise<Hole<"U">>>(m1.R)
    if (m3) {
      return ActorRefFrom(PromiseActorLogic(m3.U))
    }
    const m4 = matches<ActorLogic<Hole<"TSnapshot">, Hole<"TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"TEmitted">>>(m1.R)
    if (m4) {
      return ActorRef(m4.TSnapshot, m4.TEvent, m4.TEmitted)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ActorRefFrom<T> =
 *   ReturnTypeOrValue<T> extends infer R
 *     ? R extends StateMachine<infer TContext, infer TEvent, infer TChildren, any, any, any, any, infer TStateValue, infer TTag, any, infer TOutput, infer TEmitted, infer TMeta, infer TStateSchema>
 *       ? ActorRef<
 *         MachineSnapshot<
 *           TContext,
 *           TEvent,
 *           TChildren,
 *           TStateValue,
 *           TTag,
 *           TOutput,
 *           TMeta,
 *           TStateSchema
 *         >,
 *         TEvent,
 *         TEmitted
 *       >
 *     : R extends Promise<infer U> ? ActorRefFrom<PromiseActorLogic<U>>
 *     : R extends ActorLogic<infer TSnapshot, infer TEvent, any, any, infer TEmitted>
 *       ? ActorRef<TSnapshot, TEvent, TEmitted>
 *     : never
 *     : never
 */

// ✓ ActorRefFromLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActorRefFromLogic(T: AnyActorLogic): any {
  return ActorRef(SnapshotFrom(T), EventFromLogic(T), EmittedFrom(T))
}
/* compiles to:
 * export type ActorRefFromLogic<T extends AnyActorLogic> = ActorRef<
 *   SnapshotFrom<T>,
 *   EventFromLogic<T>,
 *   EmittedFrom<T>
 * >
 */

// ✓ InterpreterFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InterpreterFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)): any {
  const m1 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"TStateValue">, Hole<"TTag">, Hole<"TInput">, Hole<"TOutput">, Hole<"TEmitted">, Hole<"TMeta">, Hole<"TStateSchema">>>(ReturnTypeOrValue(T))
  if (m1) {
    return Actor(ActorLogic(MachineSnapshot(m1.TContext, m1.TEvent, m1.TChildren, m1.TStateValue, m1.TTag, m1.TOutput, m1.TMeta, m1.TStateSchema), m1.TEvent, m1.TInput, AnyActorSystem, m1.TEmitted))
  }
  return never
}
/* compiles to:
 * export type InterpreterFrom<T extends AnyStateMachine | (...args: any[]) => AnyStateMachine> =
 *   ReturnTypeOrValue<T> extends StateMachine<infer TContext, infer TEvent, infer TChildren, any, any, any, any, infer TStateValue, infer TTag, infer TInput, infer TOutput, infer TEmitted, infer TMeta, infer TStateSchema>
 *     ? Actor<
 *       ActorLogic<
 *         MachineSnapshot<
 *           TContext,
 *           TEvent,
 *           TChildren,
 *           TStateValue,
 *           TTag,
 *           TOutput,
 *           TMeta,
 *           TStateSchema
 *         >,
 *         TEvent,
 *         TInput,
 *         AnyActorSystem,
 *         TEmitted
 *       >
 *     >
 *     : never
 */

// ✓ MachineImplementationsFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)): any {
  const m1 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"TActor">, Hole<"TAction">, Hole<"TGuard">, Hole<"TDelay">, Hole<"_TStateValue">, Hole<"TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(ReturnTypeOrValue(T))
  if (m1) {
    return InternalMachineImplementations(ResolvedStateMachineTypes(m1.TContext, m1.TEvent, m1.TActor, m1.TAction, m1.TGuard, m1.TDelay, m1.TTag, m1.TEmitted))
  }
  return never
}
/* compiles to:
 * export type MachineImplementationsFrom<
 *   T extends AnyStateMachine | (...args: any[]) => AnyStateMachine
 * > =
 *   ReturnTypeOrValue<T> extends StateMachine<infer TContext, infer TEvent, any, infer TActor, infer TAction, infer TGuard, infer TDelay, any, infer TTag, any, any, infer TEmitted, any, any>
 *     ? InternalMachineImplementations<
 *       ResolvedStateMachineTypes<
 *         TContext,
 *         TEvent,
 *         TActor,
 *         TAction,
 *         TGuard,
 *         TDelay,
 *         TTag,
 *         TEmitted
 *       >
 *     >
 *     : never
 */

// ✓ Snapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Snapshot(TOutput): any {
  return anyOf({ status: 'active', output: Undefined, error: Undefined }, { status: 'done', output: TOutput, error: Undefined }, { status: 'error', output: Undefined, error: unknown }, { status: 'stopped', output: Undefined, error: Undefined })
}
/* compiles to:
 * export type Snapshot<TOutput> =
 *   | { status: 'active'; output: undefined; error: undefined }
 *   | { status: 'done'; output: TOutput; error: undefined }
 *   | { status: 'error'; output: undefined; error: unknown }
 *   | { status: 'stopped'; output: undefined; error: undefined }
 */

// ✓ SnapshotFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SnapshotFrom(T): any {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<ActorRef<Hole<"TSnapshot">, Hole<"_">, Hole<"__">>>(m1.R)
    if (m2) {
      return m2.TSnapshot
    }
    const m3 = matches<Actor<Hole<"TLogic">>>(m1.R)
    if (m3) {
      return SnapshotFrom(m3.TLogic)
    }
    const m4 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TEmitted">, Hole<"_TSystem">>>(m1.R)
    if (m4) {
      return ReturnType(m1.R['transition'])
    }
    const m5 = matches<ActorScope<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TEmitted">, Hole<"_TSystem">>>(m1.R)
    if (m5) {
      return m5.TSnapshot
    }
    return never
  }
  return never
}
/* compiles to:
 * export type SnapshotFrom<T> =
 *   ReturnTypeOrValue<T> extends infer R
 *     ? R extends ActorRef<infer TSnapshot, any, any> ? TSnapshot
 *     : R extends Actor<infer TLogic> ? SnapshotFrom<TLogic>
 *     : R extends ActorLogic<any, any, any, any, any> ? ReturnType<R['transition']>
 *     : R extends ActorScope<infer TSnapshot, any, any, any> ? TSnapshot
 *     : never
 *     : never
 */

// ✓ EventFromLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventFromLogic(TLogic: AnyActorLogic): any {
  const m1 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"TEvent">, Hole<"_TInput">, Hole<"_TEmitted">, Hole<"_TSystem">>>(TLogic)
  if (m1) {
    return m1.TEvent
  }
  return never
}
/* compiles to:
 * export type EventFromLogic<TLogic extends AnyActorLogic> =
 *   TLogic extends ActorLogic<any, infer TEvent, any, any, any> ? TEvent : never
 */

// ✓ EmittedFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EmittedFrom(TLogic: AnyActorLogic): any {
  const m1 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"TEmitted">>>(TLogic)
  if (m1) {
    return m1.TEmitted
  }
  return never
}
/* compiles to:
 * export type EmittedFrom<TLogic extends AnyActorLogic> =
 *   TLogic extends ActorLogic<any, any, any, any, infer TEmitted> ? TEmitted : never
 */

// ✓ ResolveEventType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveEventType(T): any {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"_TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m2) {
      return m2.TEvent
    }
    const m3 = matches<MachineSnapshot<Hole<"_TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TOutput">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m3) {
      return m3.TEvent
    }
    const m4 = matches<ActorRef<Hole<"_TSnapshot">, Hole<"TEvent">, Hole<"_TEmitted">>>(m1.R)
    if (m4) {
      return m4.TEvent
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ResolveEventType<T> =
 *   ReturnTypeOrValue<T> extends infer R
 *     ? R extends StateMachine<any, infer TEvent, any, any, any, any, any, any, any, any, any, any, any, any>
 *       ? TEvent
 *     : R extends MachineSnapshot<any, infer TEvent, any, any, any, any, any, any> ? TEvent
 *     : R extends ActorRef<any, infer TEvent, any> ? TEvent
 *     : never
 *     : never
 */

// ✓ EventFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {Prop<typeof TEvent, 'type'>} K
 * @param {EventObject} TEvent
 */
export function EventFrom(T, K = never, TEvent = ResolveEventType(T)): any {
  if (matches<true>(IsNever(K))) {
    return TEvent
  }
  return ExtractEvent(TEvent, K)
}
/* compiles to:
 * export type EventFrom<
 *   T,
 *   K extends Prop<TEvent, 'type'> = never,
 *   TEvent extends EventObject = ResolveEventType<T>
 * > =
 *   IsNever<K> extends true ? TEvent : ExtractEvent<TEvent, K>
 */

// ✓ ContextFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ContextFrom(T): any {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m2) {
      return m2.TContext
    }
    const m3 = matches<MachineSnapshot<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TOutput">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m3) {
      return m3.TContext
    }
    const m4 = matches<Actor<Hole<"TActorLogic">>>(m1.R)
    if (m4) {
      const m5 = matches<StateMachine<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m4.TActorLogic)
      if (m5) {
        return m5.TContext
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ContextFrom<T> =
 *   ReturnTypeOrValue<T> extends infer R
 *     ? R extends StateMachine<infer TContext, any, any, any, any, any, any, any, any, any, any, any, any, any>
 *       ? TContext
 *     : R extends MachineSnapshot<infer TContext, any, any, any, any, any, any, any> ? TContext
 *     : R extends Actor<infer TActorLogic>
 *       ? TActorLogic extends StateMachine<infer TContext, any, any, any, any, any, any, any, any, any, any, any, any, any>
 *         ? TContext
 *         : never
 *     : never
 *     : never
 */

// ✓ InferEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferEvent(E: EventObject): any {
  const out = emptyObject
  for (const T in keySet(E['type'])) {
    out[T] = merge({ type: T }, Extract(E, { type: T }))
  }
  return out[E['type']]
}
/* compiles to:
 * export type InferEvent<E extends EventObject> =
 *   { [T in E['type']]: { type: T } & Extract<E, { type: T }> }[E['type']]
 */

// ✓ StateValueFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateValueFrom(TMachine: AnyStateMachine): any {
  return Parameters(StateFrom(TMachine)['matches'])[0]
}
/* compiles to:
 * export type StateValueFrom<TMachine extends AnyStateMachine> =
 *   Parameters<StateFrom<TMachine>['matches']>[0]
 */

// ✓ TagsFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TagsFrom(TMachine: AnyStateMachine): any {
  return Parameters(StateFrom(TMachine)['hasTag'])[0]
}
/* compiles to:
 * export type TagsFrom<TMachine extends AnyStateMachine> =
 *   Parameters<StateFrom<TMachine>['hasTag']>[0]
 */

// ✓ RequiredActorOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredActorOptions(TActor: ProvidedActor): any {
  return (matches<(typeof TActor)['id']>(Undefined) ? never : 'id') | (matches<InputFrom<(typeof TActor)['logic']>>(Undefined) ? never : 'input')
}
/* compiles to:
 * export type RequiredActorOptions<TActor extends ProvidedActor> =
 *   | (undefined extends TActor['id'] ? never : 'id')
 *   | (undefined extends InputFrom<TActor['logic']> ? never : 'input')
 */

// ✓ RequiredLogicInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredLogicInput(TLogic: AnyActorLogic): any {
  if (matches<InputFrom<typeof TLogic>>(Undefined)) {
    return never
  }
  return 'input'
}
/* compiles to:
 * export type RequiredLogicInput<TLogic extends AnyActorLogic> =
 *   undefined extends InputFrom<TLogic> ? never : 'input'
 */

// ✓ ExtractLiteralString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractLiteralString(T: string | undefined): any {
  if (typeof T === 'string') {
    if (matches<typeof T>(string)) {
      return never
    }
    return T
  }
  return never
}
/* compiles to:
 * export type ExtractLiteralString<T extends string | undefined> =
 *   T extends string ? string extends T ? never : T : never
 */

// ✓ ToConcreteChildren: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToConcreteChildren(TActor: ProvidedActor): any {
  const out = emptyObject
  for (const A in keySet(TActor)) {
    out[ExtractLiteralString(A['id'])] = optional(ActorRefFromLogic(A['logic']))
  }
  return out
}
/* compiles to:
 * export type ToConcreteChildren<TActor extends ProvidedActor> = {
 *   [A in TActor as ExtractLiteralString<A['id']>]?: ActorRefFromLogic<A['logic']>
 * }
 */

// ✓ NonConcreteActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonConcreteActors(TActor: ProvidedActor): any {
  if (matches<any>(TActor)) {
    if (matches<never>(ExtractLiteralString(TActor['id']))) {
      return TActor
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NonConcreteActors<TActor extends ProvidedActor> =
 *   TActor extends any ? ExtractLiteralString<TActor['id']> extends never ? TActor : never : never
 */

// ✓ ToChildren: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToChildren(TActor: ProvidedActor): any {
  if (matches<(typeof TActor)['src']>(string)) {
    return Record(string, AnyActorRef)
  }
  return Compute(merge(ToConcreteChildren(TActor), { include: indexRecord(string, matches<never>(NonConcreteActors(TActor)) ? (anyOf(AnyActorRef, Undefined)) : (matches<any>(NonConcreteActors(TActor)) ? (anyOf(ActorRefFromLogic(NonConcreteActors(TActor)['logic']), Undefined)) : never)), exclude: unknown }[matches<never>(NonConcreteActors(TActor)) ? 'exclude' : 'include']))
}
/* compiles to:
 * export type ToChildren<TActor extends ProvidedActor> =
 *   string extends TActor['src']
 *     ? Record<string, AnyActorRef>
 *     : Compute<
 *       & ToConcreteChildren<TActor>
 *       & {
 *           include: {
 *             [key: string]: NonConcreteActors<TActor> extends never ? AnyActorRef | undefined
 *             : NonConcreteActors<TActor> extends any
 *               ? ActorRefFromLogic<NonConcreteActors<TActor>['logic']> | undefined
 *             : never
 *           }
 *           exclude: unknown
 *         }[NonConcreteActors<TActor> extends never ? 'exclude' : 'include']
 *     >
 */

// ✓ StateSchemaFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateSchemaFrom(T: AnyStateMachine): any {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"TStateSchema">>>(T)
  if (m1) {
    return m1.TStateSchema
  }
  return never
}
/* compiles to:
 * export type StateSchemaFrom<T extends AnyStateMachine> =
 *   T extends StateMachine<any, any, any, any, any, any, any, any, any, any, any, any, any, infer TStateSchema>
 *     ? TStateSchema
 *     : never
 */

// ✓ StateId: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateId(TSchema: StateSchema, TKey: string = '(machine)', TParentKey: string | null = Null): any {
  const out = emptyObject
  for (const K in keySet(keyof(TSchema['states']) & string)) {
    out[K] = StateId(TSchema['states'][K], K, typeof TParentKey === 'string' ? `${TParentKey}.${TKey}` : (typeof TSchema['id'] === 'string' ? TSchema['id'] : TKey))
  }
  return (matches<{ id: string; }>(TSchema) ? TSchema['id'] : (matches<null>(TParentKey) ? TKey : `${TParentKey}.${TKey}`)) | (matches<Record<string, any>>(TSchema['states']) ? Values(out) : never)
}
/* compiles to:
 * export type StateId<
 *   TSchema extends StateSchema,
 *   TKey extends string = '(machine)',
 *   TParentKey extends string | null = null
 * > =
 *   | (
 *       TSchema extends { id: string; } ? TSchema['id']
 *       : TParentKey extends null ? TKey
 *       : `${TParentKey}.${TKey}`
 *     )
 *   | (
 *       TSchema['states'] extends Record<string, any>
 *         ? Values<
 *           {
 *             [K in keyof TSchema['states'] & string]: StateId<
 *               TSchema['states'][K],
 *               K,
 *               TParentKey extends string ? `${TParentKey}.${TKey}`
 *               : TSchema['id'] extends string ? TSchema['id']
 *               : TKey
 *             >
 *           }
 *         >
 *         : never
 *     )
 */

// ✓ RoutableStateId: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RoutableStateId(TSchema: StateSchema): any {
  const out = emptyObject
  for (const K in keySet(keyof(TSchema['states']) & string)) {
    out[K] = RoutableStateId(TSchema['states'][K])
  }
  return (matches<{ route: any; id: string; }>(TSchema) ? `#${TSchema['id']}` : never) | (matches<Record<string, any>>(TSchema['states']) ? Values(out) : never)
}
/* compiles to:
 * export type RoutableStateId<TSchema extends StateSchema> =
 *   | (TSchema extends { route: any; id: string; } ? `#${TSchema['id']}` : never)
 *   | (
 *       TSchema['states'] extends Record<string, any>
 *         ? Values<
 *           { [K in keyof TSchema['states'] & string]: RoutableStateId<TSchema['states'][K]> }
 *         >
 *         : never
 *     )
 */

// ✓ GetConcreteByKey: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {keyof typeof T} TKey
 * @param {(typeof T)[typeof TKey]} TValue
 */
export function GetConcreteByKey(T, TKey, TValue): any {
  return T & Record(TKey, TValue)
}
/* compiles to:
 * export type GetConcreteByKey<T, TKey extends keyof T, TValue extends T[TKey]> =
 *   T & Record<TKey, TValue>
 */

// ✓ _GroupStateKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {StateSchema} T
 * @param {keyof (typeof T)['states']} S
 */
export function _GroupStateKeys(T, S): any {
  if (matches<any>(S)) {
    if (matches<{ type: 'history'; }>(T['states'][S])) {
      return [never, never]
    }
    if (matches<{ type: 'parallel'; }>(T)) {
      return [S, never]
    }
    if ('states' in T['states'][S]) {
      return [S, never]
    }
    return [never, S]
  }
  return never
}
/* compiles to:
 * export type _GroupStateKeys<T extends StateSchema, S extends keyof T['states']> =
 *   S extends any
 *     ? T['states'][S] extends { type: 'history'; } ? [never, never]
 *     : T extends { type: 'parallel'; } ? [S, never]
 *     : 'states' extends keyof T['states'][S] ? [S, never]
 *     : [never, S]
 *     : never
 */

// ✓ GroupStateKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {StateSchema} T
 * @param {keyof (typeof T)['states']} S
 */
export function GroupStateKeys(T, S): any {
  return { nonLeaf: _GroupStateKeys(T, merge(S, string))[0], leaf: _GroupStateKeys(T, merge(S, string))[1] }
}
/* compiles to:
 * export type GroupStateKeys<T extends StateSchema, S extends keyof T['states']> = {
 *   nonLeaf: _GroupStateKeys<T, S & string>[0]
 *   leaf: _GroupStateKeys<T, S & string>[1]
 * }
 */

// ✓ ToStateValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToStateValue__v1(T, K): any {
  const out = emptyObject
  for (const StateKey in keySet(K)) {
    out[StateKey] = ToStateValue(T['states'][K])
  }
  return out
}
export function ToStateValue(T: StateSchema): any {
  const m1 = matches<{ states: Record<Hole<"S">, any>; }>(T)
  if (m1) {
    if (matches<true>(IsNever(m1.S))) {
      return {}
    }
    const out = emptyObject
    for (const K in keySet(GroupStateKeys(T, m1.S)['nonLeaf'])) {
      out[K] = ToStateValue(T['states'][K])
    }
    const out2 = emptyObject
    for (const K in keySet(GroupStateKeys(T, m1.S)['nonLeaf'])) {
      out2[K] = ToStateValue__v1(T, K)
    }
    return GroupStateKeys(T, m1.S)['leaf'] | (matches<false>(IsNever(GroupStateKeys(T, m1.S)['nonLeaf'])) ? (matches<{ type: 'parallel'; }>(T) ? out : Compute(Values(out2))) : never)
  }
  return {}
}
/* compiles to:
 * export type ToStateValue__v1<T, K> = { [StateKey in K]: ToStateValue<T['states'][K]> }
 * export type ToStateValue<T extends StateSchema> =
 *   T extends { states: Record<infer S, any>; }
 *     ? IsNever<S> extends true
 *       ? {}
 *       : | GroupStateKeys<T, S>['leaf']
 *       | (
 *           IsNever<GroupStateKeys<T, S>['nonLeaf']> extends false
 *             ? T extends { type: 'parallel'; }
 *               ? { [K in GroupStateKeys<T, S>['nonLeaf']]: ToStateValue<T['states'][K]> }
 *               : Compute<
 *                 Values<{ [K1 in GroupStateKeys<T, S>['nonLeaf']]: ToStateValue__v1<T, K1> }>
 *               >
 *             : never
 *         )
 *     : {}
 */

// ✓ ExecutableActionsFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExecutableActionsFrom(T: AnyActorLogic): any {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(T)
  if (m1) {
    return SpecialExecutableAction | (matches<(typeof m1.TAction)['type']>(string) ? never : ToExecutableAction(m1.TAction))
  }
  return never
}
/* compiles to:
 * export type ExecutableActionsFrom<T extends AnyActorLogic> =
 *   T extends StateMachine<any, any, any, any, infer TAction, any, any, any, any, any, any, any, any, any>
 *     ? | SpecialExecutableAction
 *     | (string extends TAction['type'] ? never : ToExecutableAction<TAction>)
 *     : never
 */

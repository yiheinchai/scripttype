/**
 * ScriptType for 06-state-and-forms/xstate/packages/core/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 101 type-level aliases: 81 covered, 11 typecheck-error, 6 compile-error, 3 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ActionArgs: any
declare const Actor: any
declare const ActorLogic: any
declare const ActorRef: any
declare const ActorScope: any
declare const AnyActorLogic: any
declare const AnyActorRef: any
declare const AnyActorSystem: any
declare const AnyEventObject: any
declare const AnyFunction: any
declare const AnyStateMachine: any
declare const AssignArgs: any
declare const AtomicStateNodeConfig: any
declare const DoneActorEvent: any
declare const DoneStateEvent: any
declare const ErrorActorEvent: any
declare const EventObject: any
declare const Guard: any
declare const GuardPredicate: any
declare const MachineContext: any
declare const MachineOptions: any
declare const MachineSnapshot: any
declare const MetaObject: any
declare const NonReducibleUnknown: any
declare const ParameterizedObject: any
declare const PromiseActorLogic: any
declare const PropertyKey: any
declare const ProvidedActor: any
declare const ResolvedStateMachineTypes: any
declare const SnapshotEvent: any
declare const Spawner: any
declare const SpecialExecutableAction: any
declare const StateMachine: any
declare const StateMachineTypes: any
declare const StateNode: any
declare const StateNodeConfig: any
declare const StateNodeDefinition: any
declare const StateSchema: any
declare const StateValue: any
declare const TODO: any
declare const ToExecutableAction: any
declare const TransitionConfig: any
declare const TransitionConfigTarget: any
declare const TransitionDefinition: any
type ActionArgs<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Actor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ActorLogic<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ActorRef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ActorScope<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyActorLogic<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyActorRef<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyActorSystem<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyEventObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyFunction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AnyStateMachine<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AssignArgs<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type AtomicStateNodeConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DoneActorEvent<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type DoneStateEvent<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ErrorActorEvent<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EventDescriptor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EventDescriptorMatches<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type EventObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Guard<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type GuardPredicate<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type InputFrom<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MachineContext<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MachineOptions<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MachineSnapshot<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type MetaObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type NonReducibleUnknown<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ParameterizedObject<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PromiseActorLogic<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Prop<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type PropertyKey<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ProvidedActor<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ResolvedStateMachineTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SnapshotEvent<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type Spawner<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type SpecialExecutableAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateMachine<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateMachineTypes<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateNode<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateNodeConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateNodeDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateSchema<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type StateValue<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TODO<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type ToExecutableAction<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TransitionConfig<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TransitionConfigTarget<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
type TransitionDefinition<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ Identity: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Identity(T) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = T[K]
  }
  return out
}
/* compiles to:
 * export type Identity<T> = { [K in keyof T]: T[K] }
 */

// ✓ HomomorphicPick: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HomomorphicPick(T, K: keyof any) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[merge(P, K)] = T[P]
  }
  return out
}
/* compiles to:
 * export type HomomorphicPick<T, K extends keyof any> = { [P in keyof T as P & K]: T[P] }
 */

// ✓ HomomorphicOmit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HomomorphicOmit(T, K: keyof any) {
  const out = emptyObject
  for (const P in keyof(T)) {
    out[Exclude(P, K)] = T[P]
  }
  return out
}
/* compiles to:
 * export type HomomorphicOmit<T, K extends keyof any> = { [P in keyof T as Exclude<P, K>]: T[P] }
 */

// ✓ Invert: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Invert(T: Record<PropertyKey, PropertyKey>) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[T[K]] = K
  }
  return out
}
/* compiles to:
 * export type Invert<T extends Record<PropertyKey, PropertyKey>> = { [K in keyof T as T[K]]: K }
 */

// ✓ GetParameterizedParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetParameterizedParams(T: ParameterizedObject | undefined) {
  if (matches<any>(T)) {
    if (matches<keyof typeof T>('params')) {
      return T['params']
    }
    return Undefined
  }
  return never
}
/* compiles to:
 * export type GetParameterizedParams<T extends ParameterizedObject | undefined> = T extends any ? 'params' extends keyof T ? T['params'] : undefined : never
 */

// ✓ ReturnTypeOrValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ReturnTypeOrValue(T) {
  if (matches<AnyFunction>(T)) {
    return ReturnType(T)
  }
  return T
}
/* compiles to:
 * export type ReturnTypeOrValue<T> = T extends AnyFunction ? ReturnType<T> : T
 */

// ✓ IsNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNever(T) {
  if (matches<[ never ]>([T])) {
    return true
  }
  return false
}
/* compiles to:
 * export type IsNever<T> = [T] extends [never] ? true : false
 */

// ✓ IsNotNever: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsNotNever(T) {
  if (matches<[ never ]>([T])) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsNotNever<T> = [T] extends [never] ? false : true
 */

// ✓ Compute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Compute(A) {
  const out = emptyObject
  for (const K in keyof(A)) {
    out[K] = A[K]
  }
  return out & unknown
}
/* compiles to:
 * export type Compute<A> = { [K in keyof A]: A[K] } & unknown
 */

// ✓ Prop: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Prop(T, K) {
  if (matches<keyof typeof T>(K)) {
    return T[K]
  }
  return never
}
/* compiles to:
 * export type Prop<T, K> = K extends keyof T ? T[K] : never
 */

// ✓ Values: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Values(T) {
  return T[keyof(T)]
}
/* compiles to:
 * export type Values<T> = T[keyof T]
 */

// ✓ Elements: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Elements(T) {
  return T[merge(keyof(T), `${number}`)]
}
/* compiles to:
 * export type Elements<T> = T[keyof T & `${number}`]
 */

// ✓ Merge: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Merge(M, N) {
  return Omit(M, keyof(N)) & N
}
/* compiles to:
 * export type Merge<M, N> = Omit<M, keyof N> & N
 */

// ✗ IndexByProp: the ScriptType does not itself typecheck as TypeScript
//   IndexByProp.st.ts(2:29) TS2502: 'T' is referenced directly or indirectly in its own type annotation.
/* @scripttype preserveParamNames */
export function IndexByProp(T: Record<typeof P, string>, P: keyof typeof T) {
  const out = emptyObject
  for (const E in keySet(T)) {
    out[E[P]] = E
  }
  return out
}
/* compiles to:
 * export type IndexByProp<T extends Record<P, string>, P extends keyof T> = { [E in T as E[P]]: E }
 */

// ✓ IndexByType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IndexByType(T: { type: string; }) {
  return IndexByProp(T, 'type')
}
/* compiles to:
 * export type IndexByType<T extends { type: string; }> = IndexByProp<T, 'type'>
 */

// ✗ Equals: uses raw() — language gap, does not count as covered
//   gap: generic function type
/* @scripttype preserveParamNames */
export function Equals(A1, A2) {
  if (matches<<A>() => A extends typeof A1 ? true : false>(raw('<A>() => A extends A2 ? true : false'))) {
    return true
  }
  return false
}

// ✓ IsAny: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsAny(T) {
  return Equals(T, any)
}
/* compiles to:
 * export type IsAny<T> = Equals<T, any>
 */

// ✓ Cast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Cast(A, B) {
  if (matches<typeof B>(A)) {
    return A
  }
  return B
}
/* compiles to:
 * export type Cast<A, B> = A extends B ? A : B
 */

// ✓ DoNotInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DoNotInfer(T) {
  return [T][matches<any>(T) ? 0 : any]
}
/* compiles to:
 * export type DoNotInfer<T> = [T][T extends any ? 0 : any]
 */

// ✓ NoInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoInfer(T) {
  return DoNotInfer(T)
}
/* compiles to:
 * export type NoInfer<T> = DoNotInfer<T>
 */

// ✓ LowInfer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LowInfer(T) {
  return T & NonNullable(unknown)
}
/* compiles to:
 * export type LowInfer<T> = T & NonNullable<unknown>
 */

// ✓ Lazy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Lazy(T) {
  return fnType([], T)
}
/* compiles to:
 * export type Lazy<T> = () => T
 */

// ✓ MaybeLazy: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MaybeLazy(T) {
  return T | Lazy(T)
}
/* compiles to:
 * export type MaybeLazy<T> = T | Lazy<T>
 */

// ✗ InputFrom: the ScriptType does not itself typecheck as TypeScript
//   InputFrom.st.ts(3:22) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function InputFrom(T) {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(T)
  if (m1) {
    return m1.TInput
  }
  const m2 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"TInput">, Hole<"_TSystem">, Hole<"_TEmitted">>>(T)
  if (m2) {
    return m2.TInput
  }
  return never
}
/* compiles to:
 * export type InputFrom<T> = T extends StateMachine<unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, infer TInput, unknown, unknown, unknown, unknown> ? TInput : T extends ActorLogic<unknown, unknown, infer TInput, unknown, unknown> ? TInput : never
 */

// ✗ OutputFrom: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'EventObject'.
/* @scripttype preserveParamNames */
export function OutputFrom(T) {
  const m1 = matches<ActorLogic<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"_TEmitted">>>(T)
  if (m1) {
    return (merge(m1.TSnapshot, { status: 'done' }))['output']
  }
  const m2 = matches<ActorRef<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TEmitted">>>(T)
  if (m2) {
    return (merge(m2.TSnapshot, { status: 'done' }))['output']
  }
  return never
}
/* compiles to:
 * export type OutputFrom<T> = T extends ActorLogic<infer TSnapshot, unknown, unknown, unknown, unknown> ? (TSnapshot & { status: 'done' })['output'] : T extends ActorRef<infer TSnapshot, unknown, unknown> ? (TSnapshot & { status: 'done' })['output'] : never
 */

// ✗ ActionFunction: uses raw() — language gap, does not count as covered
//   gap: object member CallSignature
/* @scripttype preserveParamNames */
export function ActionFunction(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject) {
  return raw('{ (args: ActionArgs<TContext, TExpressionEvent, TEvent>, params: TParams): void; _out_TEvent?: TEvent; _out_TActor?: TActor; _out_TAction?: TAction; _out_TGuard?: TGuard; _out_TDelay?: TDelay; _out_TEmitted?: TEmitted; }')
}

// ✓ NoRequiredParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NoRequiredParams(T: ParameterizedObject) {
  if (matches<any>(T)) {
    if (matches<(typeof T)['params']>(Undefined)) {
      return T['type']
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NoRequiredParams<T extends ParameterizedObject> = T extends any ? undefined extends T['params'] ? T['type'] : never : never
 */

// ✓ ConditionalRequired: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ConditionalRequired(T, Condition: boolean) {
  if (matches<true>(Condition)) {
    return Required(T)
  }
  return T
}
/* compiles to:
 * export type ConditionalRequired<T, Condition extends boolean> = Condition extends true ? Required<T> : T
 */

// ✓ WithDynamicParams: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDynamicParams(TContext: MachineContext, TExpressionEvent: EventObject, T: ParameterizedObject) {
  if (matches<any>(T)) {
    return ConditionalRequired({ type: T['type'], params: optional(T['params'] | fnType([{ context: TContext, event: TExpressionEvent }], T['params'])) }, matches<(typeof T)['params']>(Undefined) ? false : true)
  }
  return never
}
/* compiles to:
 * export type WithDynamicParams<TContext extends MachineContext, TExpressionEvent extends EventObject, T extends ParameterizedObject> = T extends any ? ConditionalRequired<{ type: T['type']; params?: T['params'] | ((a0: { context: TContext; event: TExpressionEvent }) => T['params']) }, undefined extends T['params'] ? false : true> : never
 */

// ✓ Action: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Action(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject) {
  return NoRequiredParams(TAction) | WithDynamicParams(TContext, TExpressionEvent, TAction) | ActionFunction(TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted)
}
/* compiles to:
 * export type Action<TContext extends MachineContext, TExpressionEvent extends EventObject, TEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject> = NoRequiredParams<TAction> | WithDynamicParams<TContext, TExpressionEvent, TAction> | ActionFunction<TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted>
 */

// ✓ Actions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Actions(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject) {
  return SingleOrArray(Action(TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted))
}
/* compiles to:
 * export type Actions<TContext extends MachineContext, TExpressionEvent extends EventObject, TEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject> = SingleOrArray<Action<TContext, TExpressionEvent, TEvent, TParams, TActor, TAction, TGuard, TDelay, TEmitted>>
 */

// ✓ Delay: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Delay(TDelay: string) {
  return anyOf(TDelay, number)
}
/* compiles to:
 * export type Delay<TDelay extends string> = TDelay | number
 */

// ✓ DelayedTransitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayedTransitions(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string) {
  const out = emptyObject
  for (const K in keySet(Delay(TDelay))) {
    out[K] = optional(string | SingleOrArray(TransitionConfig(TContext, TEvent, TEvent, TActor, TAction, TGuard, TDelay, TODO, TODO)))
  }
  return out
}
/* compiles to:
 * export type DelayedTransitions<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string> = { [K in Delay<TDelay>]?: string | SingleOrArray<TransitionConfig<TContext, TEvent, TEvent, TActor, TAction, TGuard, TDelay, TODO, TODO>> }
 */

// ✓ SingleOrArray: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SingleOrArray(T) {
  return readonlyArrayOf(T) | T
}
/* compiles to:
 * export type SingleOrArray<T> = readonly T[] | T
 */

// ✓ StateNodesConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateNodesConfig(TContext: MachineContext, TEvent: EventObject) {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNode(TContext, TEvent)
  }
  return out
}
/* compiles to:
 * export type StateNodesConfig<TContext extends MachineContext, TEvent extends EventObject> = { [K in string]: StateNode<TContext, TEvent> }
 */

// ✓ StatesConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StatesConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TTag: string, TOutput, TEmitted: EventObject, TMeta: MetaObject) {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNodeConfig(TContext, TEvent, TActor, TAction, TGuard, TDelay, TTag, TOutput, TEmitted, TMeta)
  }
  return out
}
/* compiles to:
 * export type StatesConfig<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TTag extends string, TOutput, TEmitted extends EventObject, TMeta extends MetaObject> = { [K in string]: StateNodeConfig<TContext, TEvent, TActor, TAction, TGuard, TDelay, TTag, TOutput, TEmitted, TMeta> }
 */

// ✓ StatesDefinition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StatesDefinition(TContext: MachineContext, TEvent: EventObject) {
  const out = emptyObject
  for (const K in keySet(string)) {
    out[K] = StateNodeDefinition(TContext, TEvent)
  }
  return out
}
/* compiles to:
 * export type StatesDefinition<TContext extends MachineContext, TEvent extends EventObject> = { [K in string]: StateNodeDefinition<TContext, TEvent> }
 */

// ✓ TransitionConfigOrTarget: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionConfigOrTarget(TContext: MachineContext, TExpressionEvent: EventObject, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject) {
  return SingleOrArray(TransitionConfigTarget | TransitionConfig(TContext, TExpressionEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))
}
/* compiles to:
 * export type TransitionConfigOrTarget<TContext extends MachineContext, TExpressionEvent extends EventObject, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject, TMeta extends MetaObject> = SingleOrArray<TransitionConfigTarget | TransitionConfig<TContext, TExpressionEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>
 */

// ✓ TransitionsConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionsConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject) {
  const out = emptyObject
  for (const K in keySet(EventDescriptor(TEvent))) {
    out[K] = optional(TransitionConfigOrTarget(TContext, ExtractEvent(TEvent, K), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))
  }
  return out
}
/* compiles to:
 * export type TransitionsConfig<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject, TMeta extends MetaObject> = { [K in EventDescriptor<TEvent>]?: TransitionConfigOrTarget<TContext, ExtractEvent<TEvent, K>, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta> }
 */

// ✓ PartialEventDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PartialEventDescriptor(TEventType: string) {
  const m1 = matches<`${Hole<"TLeading">}.${Hole<"TTail">}`>(TEventType)
  if (m1) {
    return anyOf(`${m1.TLeading}.*`, `${m1.TLeading}.${PartialEventDescriptor(m1.TTail)}`)
  }
  return never
}
/* compiles to:
 * export type PartialEventDescriptor<TEventType extends string> = TEventType extends `${infer TLeading}.${infer TTail}` ? `${TLeading}.*` | `${TLeading}.${PartialEventDescriptor<TTail>}` : never
 */

// ✓ EventDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventDescriptor(TEvent: EventObject) {
  return anyOf(TEvent['type'], PartialEventDescriptor(TEvent['type']), '*')
}
/* compiles to:
 * export type EventDescriptor<TEvent extends EventObject> = TEvent['type'] | PartialEventDescriptor<TEvent['type']> | '*'
 */

// ✓ NormalizeDescriptor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizeDescriptor(TDescriptor: string) {
  if (matches<'*'>(TDescriptor)) {
    return string
  }
  const m1 = matches<`${Hole<"TLeading">}.*`>(TDescriptor)
  if (m1) {
    return `${m1.TLeading}.${string}`
  }
  return TDescriptor
}
/* compiles to:
 * export type NormalizeDescriptor<TDescriptor extends string> = TDescriptor extends '*' ? string : TDescriptor extends `${infer TLeading}.*` ? `${TLeading}.${string}` : TDescriptor
 */

// ✓ IsLiteralString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function IsLiteralString(T: string) {
  if (matches<typeof T>(string)) {
    return false
  }
  return true
}
/* compiles to:
 * export type IsLiteralString<T extends string> = string extends T ? false : true
 */

// ✓ DistributeActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DistributeActors(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject, TSpecificActor: ProvidedActor) {
  const m1 = matches<{ src: Hole<"TSrc">; }>(TSpecificActor)
  if (m1) {
    const out = emptyObject
    for (const K in keySet(RequiredActorOptions(TSpecificActor))) {
      out[K] = unknown
    }
    return anyOf(Compute(merge({ systemId: optional(string), src: m1.TSrc, id: optional(TSpecificActor['id']), input: optional(Mapper(TContext, TEvent, InputFrom(TSpecificActor['logic']), TEvent) | InputFrom(TSpecificActor['logic'])), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(OutputFrom(TSpecificActor['logic'])), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent(SnapshotFrom(TSpecificActor['logic'])), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) }, out)), { id: optional(never), systemId: optional(string), src: AnyActorLogic, input: optional(Mapper(TContext, TEvent, NonReducibleUnknown, TEvent) | NonReducibleUnknown), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(unknown), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) })
  }
  return never
}
/* compiles to:
 * export type DistributeActors<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject, TMeta extends MetaObject, TSpecificActor extends ProvidedActor> = TSpecificActor extends { src: infer TSrc; } ? Compute<{ systemId?: string; src: TSrc; id?: TSpecificActor['id']; input?: Mapper<TContext, TEvent, InputFrom<TSpecificActor['logic']>, TEvent> | InputFrom<TSpecificActor['logic']>; onDone?: string | SingleOrArray<TransitionConfigOrTarget<TContext, DoneActorEvent<OutputFrom<TSpecificActor['logic']>>, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onError?: string | SingleOrArray<TransitionConfigOrTarget<TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onSnapshot?: string | SingleOrArray<TransitionConfigOrTarget<TContext, SnapshotEvent<SnapshotFrom<TSpecificActor['logic']>>, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>> } & { [K in RequiredActorOptions<TSpecificActor>]: unknown }> | { id?: never; systemId?: string; src: AnyActorLogic; input?: Mapper<TContext, TEvent, NonReducibleUnknown, TEvent> | NonReducibleUnknown; onDone?: string | SingleOrArray<TransitionConfigOrTarget<TContext, DoneActorEvent<unknown>, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onError?: string | SingleOrArray<TransitionConfigOrTarget<TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onSnapshot?: string | SingleOrArray<TransitionConfigOrTarget<TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>> } : never
 */

// ✓ InvokeConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvokeConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject, TGuard: ParameterizedObject, TDelay: string, TEmitted: EventObject, TMeta: MetaObject) {
  if (matches<true>(IsLiteralString(TActor['src']))) {
    return DistributeActors(TContext, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta, TActor)
  }
  return { id: optional(string), systemId: optional(string), src: AnyActorLogic | string, input: optional(Mapper(TContext, TEvent, NonReducibleUnknown, TEvent) | NonReducibleUnknown), onDone: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, DoneActorEvent(any), TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onError: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))), onSnapshot: optional(string | SingleOrArray(TransitionConfigOrTarget(TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta))) }
}
/* compiles to:
 * export type InvokeConfig<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject, TGuard extends ParameterizedObject, TDelay extends string, TEmitted extends EventObject, TMeta extends MetaObject> = IsLiteralString<TActor['src']> extends true ? DistributeActors<TContext, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta, TActor> : { id?: string; systemId?: string; src: AnyActorLogic | string; input?: Mapper<TContext, TEvent, NonReducibleUnknown, TEvent> | NonReducibleUnknown; onDone?: string | SingleOrArray<TransitionConfigOrTarget<TContext, DoneActorEvent<any>, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onError?: string | SingleOrArray<TransitionConfigOrTarget<TContext, ErrorActorEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>>; onSnapshot?: string | SingleOrArray<TransitionConfigOrTarget<TContext, SnapshotEvent, TEvent, TActor, TAction, TGuard, TDelay, TEmitted, TMeta>> }
 */

// ✓ SimpleOrStateNodeConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SimpleOrStateNodeConfig(TContext: MachineContext, TEvent: EventObject) {
  return AtomicStateNodeConfig(TContext, TEvent) | StateNodeConfig(TContext, TEvent, TODO, TODO, TODO, TODO, TODO, TODO, TODO, TODO)
}
/* compiles to:
 * export type SimpleOrStateNodeConfig<TContext extends MachineContext, TEvent extends EventObject> = AtomicStateNodeConfig<TContext, TEvent> | StateNodeConfig<TContext, TEvent, TODO, TODO, TODO, TODO, TODO, TODO, TODO, TODO>
 */

// ✗ ActionFunctionMap: does not compile yet
//   'K' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function ActionFunctionMap(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor, TAction: ParameterizedObject = ParameterizedObject, TGuard: ParameterizedObject = ParameterizedObject, TDelay: string = string, TEmitted: EventObject = EventObject) {
  const out = emptyObject
  for (const K in keySet(TAction['type'])) {
    out[K] = optional(ActionFunction(TContext, TEvent, TEvent, GetParameterizedParams(matches<{ type: typeof K; }>(TAction) ? TAction : never), TActor, TAction, TGuard, TDelay, TEmitted))
  }
  return out
}
/* compiles to:
 * export type ActionFunctionMap<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor, TAction extends ParameterizedObject = ParameterizedObject, TGuard extends ParameterizedObject = ParameterizedObject, TDelay extends string = string, TEmitted extends EventObject = EventObject> = { [K in TAction['type']]?: ActionFunction<TContext, TEvent, TEvent, GetParameterizedParams<TAction extends { type: typeof K; } ? TAction : never>, TActor, TAction, TGuard, TDelay, TEmitted> }
 */

// ✗ GuardMap: does not compile yet
//   'K' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function GuardMap(TContext: MachineContext, TEvent: EventObject, TGuard: ParameterizedObject) {
  const out = emptyObject
  for (const K in keySet(TGuard['type'])) {
    out[K] = optional(GuardPredicate(TContext, TEvent, GetParameterizedParams(matches<{ type: typeof K; }>(TGuard) ? TGuard : never), TGuard))
  }
  return out
}
/* compiles to:
 * export type GuardMap<TContext extends MachineContext, TEvent extends EventObject, TGuard extends ParameterizedObject> = { [K in TGuard['type']]?: GuardPredicate<TContext, TEvent, GetParameterizedParams<TGuard extends { type: typeof K; } ? TGuard : never>, TGuard> }
 */

// ✓ DelayFunctionMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayFunctionMap(TContext: MachineContext, TEvent: EventObject, TAction: ParameterizedObject) {
  return Record(string, DelayConfig(TContext, TEvent, TAction['params'], TEvent))
}
/* compiles to:
 * export type DelayFunctionMap<TContext extends MachineContext, TEvent extends EventObject, TAction extends ParameterizedObject> = Record<string, DelayConfig<TContext, TEvent, TAction['params'], TEvent>>
 */

// ✓ DelayConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayConfig(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject) {
  return number | DelayExpr(TContext, TExpressionEvent, TParams, TEvent)
}
/* compiles to:
 * export type DelayConfig<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject> = number | DelayExpr<TContext, TExpressionEvent, TParams, TEvent>
 */

// ✓ MachineImplementationsActions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsActions(TTypes: StateMachineTypes) {
  const out = emptyObject
  for (const K in keySet(TTypes['actions']['type'])) {
    out[K] = optional(ActionFunction(TTypes['context'], TTypes['events'], TTypes['events'], GetConcreteByKey(TTypes['actions'], 'type', K)['params'], TTypes['actors'], TTypes['actions'], TTypes['guards'], TTypes['delays'], TTypes['emitted']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsActions<TTypes extends StateMachineTypes> = { [K in TTypes['actions']['type']]?: ActionFunction<TTypes['context'], TTypes['events'], TTypes['events'], GetConcreteByKey<TTypes['actions'], 'type', K>['params'], TTypes['actors'], TTypes['actions'], TTypes['guards'], TTypes['delays'], TTypes['emitted']> }
 */

// ✓ MachineImplementationsActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsActors(TTypes: StateMachineTypes) {
  const out = emptyObject
  for (const K in keySet(TTypes['actors']['src'])) {
    out[K] = optional(GetConcreteByKey(TTypes['actors'], 'src', K)['logic'])
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsActors<TTypes extends StateMachineTypes> = { [K in TTypes['actors']['src']]?: GetConcreteByKey<TTypes['actors'], 'src', K>['logic'] }
 */

// ✓ MachineImplementationsDelays: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsDelays(TTypes: StateMachineTypes) {
  const out = emptyObject
  for (const K in keySet(TTypes['delays'])) {
    out[K] = optional(DelayConfig(TTypes['context'], TTypes['events'], Undefined, TTypes['events']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsDelays<TTypes extends StateMachineTypes> = { [K in TTypes['delays']]?: DelayConfig<TTypes['context'], TTypes['events'], undefined, TTypes['events']> }
 */

// ✓ MachineImplementationsGuards: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineImplementationsGuards(TTypes: StateMachineTypes) {
  const out = emptyObject
  for (const K in keySet(TTypes['guards']['type'])) {
    out[K] = optional(Guard(TTypes['context'], TTypes['events'], GetConcreteByKey(TTypes['guards'], 'type', K)['params'], TTypes['guards']))
  }
  return out
}
/* compiles to:
 * export type MachineImplementationsGuards<TTypes extends StateMachineTypes> = { [K in TTypes['guards']['type']]?: Guard<TTypes['context'], TTypes['events'], GetConcreteByKey<TTypes['guards'], 'type', K>['params'], TTypes['guards']> }
 */

// ✓ InternalMachineImplementations: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InternalMachineImplementations(TTypes: StateMachineTypes) {
  return { actions: optional(MachineImplementationsActions(TTypes)), actors: optional(MachineImplementationsActors(TTypes)), delays: optional(MachineImplementationsDelays(TTypes)), guards: optional(MachineImplementationsGuards(TTypes)) }
}
/* compiles to:
 * export type InternalMachineImplementations<TTypes extends StateMachineTypes> = { actions?: MachineImplementationsActions<TTypes>; actors?: MachineImplementationsActors<TTypes>; delays?: MachineImplementationsDelays<TTypes>; guards?: MachineImplementationsGuards<TTypes> }
 */

// ✓ InitialContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InitialContext(TContext: MachineContext, TActor: ProvidedActor, TInput, TEvent: EventObject) {
  return anyOf(TContext, ContextFactory(TContext, TActor, TInput, TEvent))
}
/* compiles to:
 * export type InitialContext<TContext extends MachineContext, TActor extends ProvidedActor, TInput, TEvent extends EventObject> = TContext | ContextFactory<TContext, TActor, TInput, TEvent>
 */

// ✓ ContextFactory: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ContextFactory(TContext: MachineContext, TActor: ProvidedActor, TInput, TEvent: EventObject = EventObject) {
  return fnType([{ spawn: Spawner(TActor), input: TInput, self: ActorRef(MachineSnapshot(TContext, TEvent, Record(string, anyOf(AnyActorRef, Undefined)), StateValue, string, unknown, TODO, TODO), TEvent, AnyEventObject) }], TContext)
}
/* compiles to:
 * export type ContextFactory<TContext extends MachineContext, TActor extends ProvidedActor, TInput, TEvent extends EventObject = EventObject> = (a0: { spawn: Spawner<TActor>; input: TInput; self: ActorRef<MachineSnapshot<TContext, TEvent, Record<string, AnyActorRef | undefined>, StateValue, string, unknown, TODO, TODO>, TEvent, AnyEventObject> }) => TContext
 */

// ✓ MachineConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function MachineConfig(TContext: MachineContext, TEvent: EventObject, TActor: ProvidedActor = ProvidedActor, TAction: ParameterizedObject = ParameterizedObject, TGuard: ParameterizedObject = ParameterizedObject, TDelay: string = string, TTag: string = string, TInput = any, TOutput = unknown, TEmitted: EventObject = EventObject, TMeta: MetaObject = MetaObject) {
  return merge(merge(Omit(StateNodeConfig(DoNotInfer(TContext), DoNotInfer(TEvent), DoNotInfer(TActor), DoNotInfer(TAction), DoNotInfer(TGuard), DoNotInfer(TDelay), DoNotInfer(TTag), DoNotInfer(TOutput), DoNotInfer(TEmitted), DoNotInfer(TMeta)), 'output'), { version: optional(string), output: optional(Mapper(TContext, DoneStateEvent, TOutput, TEvent) | TOutput), options: optional(MachineOptions) }), matches<typeof TContext>(MachineContext) ? { context: optional(InitialContext(LowInfer(TContext), TActor, TInput, TEvent)) } : { context: InitialContext(LowInfer(TContext), TActor, TInput, TEvent) })
}
/* compiles to:
 * export type MachineConfig<TContext extends MachineContext, TEvent extends EventObject, TActor extends ProvidedActor = ProvidedActor, TAction extends ParameterizedObject = ParameterizedObject, TGuard extends ParameterizedObject = ParameterizedObject, TDelay extends string = string, TTag extends string = string, TInput = any, TOutput = unknown, TEmitted extends EventObject = EventObject, TMeta extends MetaObject = MetaObject> = Omit<StateNodeConfig<DoNotInfer<TContext>, DoNotInfer<TEvent>, DoNotInfer<TActor>, DoNotInfer<TAction>, DoNotInfer<TGuard>, DoNotInfer<TDelay>, DoNotInfer<TTag>, DoNotInfer<TOutput>, DoNotInfer<TEmitted>, DoNotInfer<TMeta>>, 'output'> & { version?: string; output?: Mapper<TContext, DoneStateEvent, TOutput, TEvent> | TOutput; options?: MachineOptions } & (MachineContext extends TContext ? { context?: InitialContext<LowInfer<TContext>, TActor, TInput, TEvent> } : { context: InitialContext<LowInfer<TContext>, TActor, TInput, TEvent> })
 */

// ✓ HistoryValue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function HistoryValue(TContext: MachineContext, TEvent: EventObject) {
  return Record(string, Array(StateNode(TContext, TEvent)))
}
/* compiles to:
 * export type HistoryValue<TContext extends MachineContext, TEvent extends EventObject> = Record<string, Array<StateNode<TContext, TEvent>>>
 */

// ✓ StateFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)) {
  if (matches<AnyStateMachine>(T)) {
    return ReturnType(T['transition'])
  }
  if (matches<(...args: any[]) => AnyStateMachine>(T)) {
    return ReturnType(ReturnType(T)['transition'])
  }
  return never
}
/* compiles to:
 * export type StateFrom<T extends AnyStateMachine | (...args: any[]) => AnyStateMachine> = T extends AnyStateMachine ? ReturnType<T['transition']> : T extends (...args: any[]) => AnyStateMachine ? ReturnType<ReturnType<T>['transition']> : never
 */

// ✓ Transitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Transitions(TContext: MachineContext, TEvent: EventObject) {
  return Array(TransitionDefinition(TContext, TEvent))
}
/* compiles to:
 * export type Transitions<TContext extends MachineContext, TEvent extends EventObject> = Array<TransitionDefinition<TContext, TEvent>>
 */

// ✓ DelayExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DelayExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject) {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], number)
}
/* compiles to:
 * export type DelayExpr<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject> = (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => number
 */

// ✓ LogExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function LogExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject) {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], unknown)
}
/* compiles to:
 * export type LogExpr<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject> = (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => unknown
 */

// ✓ SendExpr: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SendExpr(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TSentEvent: EventObject, TEvent: EventObject) {
  return fnType([ActionArgs(TContext, TExpressionEvent, TEvent), TParams], TSentEvent)
}
/* compiles to:
 * export type SendExpr<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TSentEvent extends EventObject, TEvent extends EventObject> = (a0: ActionArgs<TContext, TExpressionEvent, TEvent>, a1: TParams) => TSentEvent
 */

// ✓ Assigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Assigner(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor) {
  return fnType([AssignArgs(TContext, TExpressionEvent, TEvent, TActor), TParams], Partial(TContext))
}
/* compiles to:
 * export type Assigner<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject, TActor extends ProvidedActor> = (a0: AssignArgs<TContext, TExpressionEvent, TEvent, TActor>, a1: TParams) => Partial<TContext>
 */

// ✓ PartialAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PartialAssigner(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor, TKey: keyof typeof TContext) {
  return fnType([AssignArgs(TContext, TExpressionEvent, TEvent, TActor), TParams], TContext[TKey])
}
/* compiles to:
 * export type PartialAssigner<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject, TActor extends ProvidedActor, TKey extends keyof TContext> = (a0: AssignArgs<TContext, TExpressionEvent, TEvent, TActor>, a1: TParams) => TContext[TKey]
 */

// ✓ PropertyAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PropertyAssigner(TContext: MachineContext, TExpressionEvent: EventObject, TParams: ParameterizedObject['params'] | undefined, TEvent: EventObject, TActor: ProvidedActor) {
  const out = emptyObject
  for (const K in keyof(TContext)) {
    out[K] = optional(PartialAssigner(TContext, TExpressionEvent, TParams, TEvent, TActor, K) | TContext[K])
  }
  return out
}
/* compiles to:
 * export type PropertyAssigner<TContext extends MachineContext, TExpressionEvent extends EventObject, TParams extends ParameterizedObject['params'] | undefined, TEvent extends EventObject, TActor extends ProvidedActor> = { [K in keyof TContext]?: PartialAssigner<TContext, TExpressionEvent, TParams, TEvent, TActor, K> | TContext[K] }
 */

// ✓ Mapper: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Mapper(TContext: MachineContext, TExpressionEvent: EventObject, TResult, TEvent: EventObject) {
  return fnType([{ context: TContext, event: TExpressionEvent, self: ActorRef(MachineSnapshot(TContext, TEvent, Record(string, AnyActorRef), StateValue, string, unknown, TODO, TODO), TEvent, AnyEventObject) }], TResult)
}
/* compiles to:
 * export type Mapper<TContext extends MachineContext, TExpressionEvent extends EventObject, TResult, TEvent extends EventObject> = (a0: { context: TContext; event: TExpressionEvent; self: ActorRef<MachineSnapshot<TContext, TEvent, Record<string, AnyActorRef>, StateValue, string, unknown, TODO, TODO>, TEvent, AnyEventObject> }) => TResult
 */

// ✓ TransitionDefinitionMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransitionDefinitionMap(TContext: MachineContext, TEvent: EventObject) {
  const out = emptyObject
  for (const K in keySet(EventDescriptor(TEvent))) {
    out[K] = Array(TransitionDefinition(TContext, ExtractEvent(TEvent, K)))
  }
  return out
}
/* compiles to:
 * export type TransitionDefinitionMap<TContext extends MachineContext, TEvent extends EventObject> = { [K in EventDescriptor<TEvent>]: Array<TransitionDefinition<TContext, ExtractEvent<TEvent, K>>> }
 */

// ✓ Observer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Observer(T) {
  return { next: optional(fnType([T], voidType())), error: optional(fnType([unknown], voidType())), complete: optional(fnType([], voidType())) }
}
/* compiles to:
 * export type Observer<T> = { next?: (a0: T) => void; error?: (a0: unknown) => void; complete?: () => void }
 */

// ✓ EventDescriptorMatches: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventDescriptorMatches(TEventType: string, TNormalizedDescriptor) {
  if (matches<typeof TNormalizedDescriptor>(TEventType)) {
    return true
  }
  return false
}
/* compiles to:
 * export type EventDescriptorMatches<TEventType extends string, TNormalizedDescriptor> = TEventType extends TNormalizedDescriptor ? true : false
 */

// ✓ ExtractEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractEvent(TEvent: EventObject, TDescriptor: EventDescriptor<typeof TEvent>) {
  if (matches<(typeof TEvent)['type']>(string)) {
    return TEvent
  }
  const m1 = matches<Hole<"TNormalizedDescriptor">>(NormalizeDescriptor(TDescriptor))
  if (m1) {
    if (matches<any>(TEvent)) {
      if (matches<EventDescriptorMatches<(typeof TEvent)['type'], typeof m1.TNormalizedDescriptor>>(true)) {
        return TEvent
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ExtractEvent<TEvent extends EventObject, TDescriptor extends EventDescriptor<TEvent>> = string extends TEvent['type'] ? TEvent : NormalizeDescriptor<TDescriptor> extends infer TNormalizedDescriptor ? TEvent extends any ? true extends EventDescriptorMatches<TEvent['type'], TNormalizedDescriptor> ? TEvent : never : never : never
 */

// ✗ ActorLogicFrom: the ScriptType does not itself typecheck as TypeScript
//   ActorLogicFrom.st.ts(5:17) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function ActorLogicFrom(T) {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    if (matches<StateMachine<any, any, any, any, any, any, any, any, any, any, any, any, any, any>>(m1.R)) {
      return m1.R
    }
    const m2 = matches<Promise<Hole<"U">>>(m1.R)
    if (m2) {
      return PromiseActorLogic(m2.U)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ActorLogicFrom<T> = ReturnTypeOrValue<T> extends infer R ? R extends StateMachine<any, any, any, any, any, any, any, any, any, any, any, any, any, any> ? R : R extends Promise<infer U> ? PromiseActorLogic<U> : never : never
 */

// ✗ ActorRefFrom: the ScriptType does not itself typecheck as TypeScript
//   ActorRefFrom.st.ts(5:24) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function ActorRefFrom(T) {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"TStateValue">, Hole<"TTag">, Hole<"_TInput">, Hole<"TOutput">, Hole<"TEmitted">, Hole<"TMeta">, Hole<"TStateSchema">>>(m1.R)
    if (m2) {
      return ActorRef(MachineSnapshot(m2.TContext, m2.TEvent, m2.TChildren, m2.TStateValue, m2.TTag, m2.TOutput, m2.TMeta, m2.TStateSchema), m2.TEvent, m2.TEmitted)
    }
    const m3 = matches<Promise<Hole<"U">>>(m1.R)
    if (m3) {
      return ActorRefFrom(PromiseActorLogic(m3.U))
    }
    const m4 = matches<ActorLogic<Hole<"TSnapshot">, Hole<"TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"TEmitted">>>(m1.R)
    if (m4) {
      return ActorRef(m4.TSnapshot, m4.TEvent, m4.TEmitted)
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ActorRefFrom<T> = ReturnTypeOrValue<T> extends infer R ? R extends StateMachine<infer TContext, infer TEvent, infer TChildren, unknown, unknown, unknown, unknown, infer TStateValue, infer TTag, unknown, infer TOutput, infer TEmitted, infer TMeta, infer TStateSchema> ? ActorRef<MachineSnapshot<TContext, TEvent, TChildren, TStateValue, TTag, TOutput, TMeta, TStateSchema>, TEvent, TEmitted> : R extends Promise<infer U> ? ActorRefFrom<PromiseActorLogic<U>> : R extends ActorLogic<infer TSnapshot, infer TEvent, unknown, unknown, infer TEmitted> ? ActorRef<TSnapshot, TEvent, TEmitted> : never : never
 */

// ✓ ActorRefFromLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ActorRefFromLogic(T: AnyActorLogic) {
  return ActorRef(SnapshotFrom(T), EventFromLogic(T), EmittedFrom(T))
}
/* compiles to:
 * export type ActorRefFromLogic<T extends AnyActorLogic> = ActorRef<SnapshotFrom<T>, EventFromLogic<T>, EmittedFrom<T>>
 */

// ✗ InterpreterFrom: the ScriptType does not itself typecheck as TypeScript
//   InterpreterFrom.st.ts(3:22) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function InterpreterFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)) {
  const m1 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"TStateValue">, Hole<"TTag">, Hole<"TInput">, Hole<"TOutput">, Hole<"TEmitted">, Hole<"TMeta">, Hole<"TStateSchema">>>(ReturnTypeOrValue(T))
  if (m1) {
    return Actor(ActorLogic(MachineSnapshot(m1.TContext, m1.TEvent, m1.TChildren, m1.TStateValue, m1.TTag, m1.TOutput, m1.TMeta, m1.TStateSchema), m1.TEvent, m1.TInput, AnyActorSystem, m1.TEmitted))
  }
  return never
}
/* compiles to:
 * export type InterpreterFrom<T extends AnyStateMachine | (...args: any[]) => AnyStateMachine> = ReturnTypeOrValue<T> extends StateMachine<infer TContext, infer TEvent, infer TChildren, unknown, unknown, unknown, unknown, infer TStateValue, infer TTag, infer TInput, infer TOutput, infer TEmitted, infer TMeta, infer TStateSchema> ? Actor<ActorLogic<MachineSnapshot<TContext, TEvent, TChildren, TStateValue, TTag, TOutput, TMeta, TStateSchema>, TEvent, TInput, AnyActorSystem, TEmitted>> : never
 */

// ✗ MachineImplementationsFrom: the ScriptType does not itself typecheck as TypeScript
//   MachineImplementationsFrom.st.ts(3:22) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function MachineImplementationsFrom(T: AnyStateMachine | ((...args: any[]) => AnyStateMachine)) {
  const m1 = matches<StateMachine<Hole<"TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"TActor">, Hole<"TAction">, Hole<"TGuard">, Hole<"TDelay">, Hole<"_TStateValue">, Hole<"TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(ReturnTypeOrValue(T))
  if (m1) {
    return InternalMachineImplementations(ResolvedStateMachineTypes(m1.TContext, m1.TEvent, m1.TActor, m1.TAction, m1.TGuard, m1.TDelay, m1.TTag, m1.TEmitted))
  }
  return never
}
/* compiles to:
 * export type MachineImplementationsFrom<T extends AnyStateMachine | (...args: any[]) => AnyStateMachine> = ReturnTypeOrValue<T> extends StateMachine<infer TContext, infer TEvent, unknown, infer TActor, infer TAction, infer TGuard, infer TDelay, unknown, infer TTag, unknown, unknown, infer TEmitted, unknown, unknown> ? InternalMachineImplementations<ResolvedStateMachineTypes<TContext, TEvent, TActor, TAction, TGuard, TDelay, TTag, TEmitted>> : never
 */

// ✓ Snapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Snapshot(TOutput) {
  return anyOf({ status: 'active', output: Undefined, error: Undefined }, { status: 'done', output: TOutput, error: Undefined }, { status: 'error', output: Undefined, error: unknown }, { status: 'stopped', output: Undefined, error: Undefined })
}
/* compiles to:
 * export type Snapshot<TOutput> = { status: 'active'; output: undefined; error: undefined } | { status: 'done'; output: TOutput; error: undefined } | { status: 'error'; output: undefined; error: unknown } | { status: 'stopped'; output: undefined; error: undefined }
 */

// ✗ SnapshotFrom: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'EventObject'.
/* @scripttype preserveParamNames */
export function SnapshotFrom(T) {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<ActorRef<Hole<"TSnapshot">, Hole<"_">, Hole<"__">>>(m1.R)
    if (m2) {
      return m2.TSnapshot
    }
    const m3 = matches<Actor<Hole<"TLogic">>>(m1.R)
    if (m3) {
      return SnapshotFrom(m3.TLogic)
    }
    const m4 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TEmitted">, Hole<"_TSystem">>>(m1.R)
    if (m4) {
      return ReturnType(m1.R['transition'])
    }
    const m5 = matches<ActorScope<Hole<"TSnapshot">, Hole<"_TEvent">, Hole<"_TEmitted">, Hole<"_TSystem">>>(m1.R)
    if (m5) {
      return m5.TSnapshot
    }
    return never
  }
  return never
}
/* compiles to:
 * export type SnapshotFrom<T> = ReturnTypeOrValue<T> extends infer R ? R extends ActorRef<infer TSnapshot, unknown, unknown> ? TSnapshot : R extends Actor<infer TLogic> ? SnapshotFrom<TLogic> : R extends ActorLogic<unknown, unknown, unknown, unknown, unknown> ? ReturnType<R['transition']> : R extends ActorScope<infer TSnapshot, unknown, unknown, unknown> ? TSnapshot : never : never
 */

// ✗ EventFromLogic: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Snapshot<unknown>'.
/* @scripttype preserveParamNames */
export function EventFromLogic(TLogic: AnyActorLogic) {
  const m1 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"TEvent">, Hole<"_TInput">, Hole<"_TEmitted">, Hole<"_TSystem">>>(TLogic)
  if (m1) {
    return m1.TEvent
  }
  return never
}
/* compiles to:
 * export type EventFromLogic<TLogic extends AnyActorLogic> = TLogic extends ActorLogic<unknown, infer TEvent, unknown, unknown, unknown> ? TEvent : never
 */

// ✗ EmittedFrom: does not compile yet
//   Type 'unknown' does not satisfy the constraint 'Snapshot<unknown>'.
/* @scripttype preserveParamNames */
export function EmittedFrom(TLogic: AnyActorLogic) {
  const m1 = matches<ActorLogic<Hole<"_TSnapshot">, Hole<"_TEvent">, Hole<"_TInput">, Hole<"_TSystem">, Hole<"TEmitted">>>(TLogic)
  if (m1) {
    return m1.TEmitted
  }
  return never
}
/* compiles to:
 * export type EmittedFrom<TLogic extends AnyActorLogic> = TLogic extends ActorLogic<unknown, unknown, unknown, unknown, infer TEmitted> ? TEmitted : never
 */

// ✗ ResolveEventType: the ScriptType does not itself typecheck as TypeScript
//   ResolveEventType.st.ts(5:24) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function ResolveEventType(T) {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"_TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m2) {
      return m2.TEvent
    }
    const m3 = matches<MachineSnapshot<Hole<"_TContext">, Hole<"TEvent">, Hole<"_TChildren">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TOutput">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m3) {
      return m3.TEvent
    }
    const m4 = matches<ActorRef<Hole<"_TSnapshot">, Hole<"TEvent">, Hole<"_TEmitted">>>(m1.R)
    if (m4) {
      return m4.TEvent
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ResolveEventType<T> = ReturnTypeOrValue<T> extends infer R ? R extends StateMachine<unknown, infer TEvent, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown> ? TEvent : R extends MachineSnapshot<unknown, infer TEvent, unknown, unknown, unknown, unknown, unknown, unknown> ? TEvent : R extends ActorRef<unknown, infer TEvent, unknown> ? TEvent : never : never
 */

// ✓ EventFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventFrom(T, K: Prop<typeof TEvent, 'type'> = never, TEvent: EventObject = ResolveEventType(T)) {
  if (matches<true>(IsNever(K))) {
    return TEvent
  }
  return ExtractEvent(TEvent, K)
}
/* compiles to:
 * export type EventFrom<T, K extends Prop<TEvent, 'type'> = never, TEvent extends EventObject = ResolveEventType<T>> = IsNever<K> extends true ? TEvent : ExtractEvent<TEvent, K>
 */

// ✗ ContextFrom: the ScriptType does not itself typecheck as TypeScript
//   ContextFrom.st.ts(5:24) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function ContextFrom(T) {
  const m1 = matches<Hole<"R">>(ReturnTypeOrValue(T))
  if (m1) {
    const m2 = matches<StateMachine<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m2) {
      return m2.TContext
    }
    const m3 = matches<MachineSnapshot<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TOutput">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m1.R)
    if (m3) {
      return m3.TContext
    }
    const m4 = matches<Actor<Hole<"TActorLogic">>>(m1.R)
    if (m4) {
      const m5 = matches<StateMachine<Hole<"TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(m4.TActorLogic)
      if (m5) {
        return m5.TContext
      }
      return never
    }
    return never
  }
  return never
}
/* compiles to:
 * export type ContextFrom<T> = ReturnTypeOrValue<T> extends infer R ? R extends StateMachine<infer TContext, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown> ? TContext : R extends MachineSnapshot<infer TContext, unknown, unknown, unknown, unknown, unknown, unknown, unknown> ? TContext : R extends Actor<infer TActorLogic> ? TActorLogic extends StateMachine<infer TContext, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown> ? TContext : never : never : never
 */

// ✓ InferEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferEvent(E: EventObject) {
  const out = emptyObject
  for (const T in keySet(E['type'])) {
    out[T] = merge({ type: T }, Extract(E, { type: T }))
  }
  return out[E['type']]
}
/* compiles to:
 * export type InferEvent<E extends EventObject> = { [T in E['type']]: { type: T } & Extract<E, { type: T }> }[E['type']]
 */

// ✓ StateValueFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateValueFrom(TMachine: AnyStateMachine) {
  return Parameters(StateFrom(TMachine)['matches'])[0]
}
/* compiles to:
 * export type StateValueFrom<TMachine extends AnyStateMachine> = Parameters<StateFrom<TMachine>['matches']>[0]
 */

// ✓ TagsFrom: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TagsFrom(TMachine: AnyStateMachine) {
  return Parameters(StateFrom(TMachine)['hasTag'])[0]
}
/* compiles to:
 * export type TagsFrom<TMachine extends AnyStateMachine> = Parameters<StateFrom<TMachine>['hasTag']>[0]
 */

// ✓ RequiredActorOptions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredActorOptions(TActor: ProvidedActor) {
  return (matches<(typeof TActor)['id']>(Undefined) ? never : 'id') | (matches<InputFrom<(typeof TActor)['logic']>>(Undefined) ? never : 'input')
}
/* compiles to:
 * export type RequiredActorOptions<TActor extends ProvidedActor> = (undefined extends TActor['id'] ? never : 'id') | (undefined extends InputFrom<TActor['logic']> ? never : 'input')
 */

// ✓ RequiredLogicInput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RequiredLogicInput(TLogic: AnyActorLogic) {
  if (matches<InputFrom<typeof TLogic>>(Undefined)) {
    return never
  }
  return 'input'
}
/* compiles to:
 * export type RequiredLogicInput<TLogic extends AnyActorLogic> = undefined extends InputFrom<TLogic> ? never : 'input'
 */

// ✓ ExtractLiteralString: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ExtractLiteralString(T: string | undefined) {
  if (matches<string>(T)) {
    if (matches<typeof T>(string)) {
      return never
    }
    return T
  }
  return never
}
/* compiles to:
 * export type ExtractLiteralString<T extends string | undefined> = T extends string ? string extends T ? never : T : never
 */

// ✓ ToConcreteChildren: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ToConcreteChildren(TActor: ProvidedActor) {
  const out = emptyObject
  for (const A in keySet(TActor)) {
    out[ExtractLiteralString(A['id'])] = optional(ActorRefFromLogic(A['logic']))
  }
  return out
}
/* compiles to:
 * export type ToConcreteChildren<TActor extends ProvidedActor> = { [A in TActor as ExtractLiteralString<A['id']>]?: ActorRefFromLogic<A['logic']> }
 */

// ✓ NonConcreteActors: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NonConcreteActors(TActor: ProvidedActor) {
  if (matches<any>(TActor)) {
    if (matches<never>(ExtractLiteralString(TActor['id']))) {
      return TActor
    }
    return never
  }
  return never
}
/* compiles to:
 * export type NonConcreteActors<TActor extends ProvidedActor> = TActor extends any ? ExtractLiteralString<TActor['id']> extends never ? TActor : never : never
 */

// ✗ ToChildren: uses raw() — language gap, does not count as covered
//   gap: index signature
/* @scripttype preserveParamNames */
export function ToChildren(TActor: ProvidedActor) {
  if (matches<(typeof TActor)['src']>(string)) {
    return Record(string, AnyActorRef)
  }
  return Compute(ToConcreteChildren(TActor) & { include: raw('{ [id: string]: NonConcreteActors<TActor> extends never ? AnyActorRef | undefined : NonConcreteActors<TActor> extends any ? ActorRefFromLogic<NonConcreteActors<TActor>[\'logic\']> | undefined : never; }'), exclude: unknown }[matches<never>(NonConcreteActors(TActor)) ? 'exclude' : 'include'])
}

// ✗ StateSchemaFrom: the ScriptType does not itself typecheck as TypeScript
//   StateSchemaFrom.st.ts(3:22) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function StateSchemaFrom(T: AnyStateMachine) {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"_TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"TStateSchema">>>(T)
  if (m1) {
    return m1.TStateSchema
  }
  return never
}
/* compiles to:
 * export type StateSchemaFrom<T extends AnyStateMachine> = T extends StateMachine<unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, infer TStateSchema> ? TStateSchema : never
 */

// ✓ StateId: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StateId(TSchema: StateSchema, TKey: string = '(machine)', TParentKey: string | null = Null) {
  const out = emptyObject
  for (const K in keySet(keyof(TSchema['states']) & string)) {
    out[K] = StateId(TSchema['states'][K], K, matches<string>(TParentKey) ? `${TParentKey}.${TKey}` : (matches<string>(TSchema['id']) ? TSchema['id'] : TKey))
  }
  return (matches<{ id: string; }>(TSchema) ? TSchema['id'] : (matches<null>(TParentKey) ? TKey : `${TParentKey}.${TKey}`)) | (matches<Record<string, any>>(TSchema['states']) ? Values(out) : never)
}
/* compiles to:
 * export type StateId<TSchema extends StateSchema, TKey extends string = '(machine)', TParentKey extends string | null = null> = (TSchema extends { id: string; } ? TSchema['id'] : TParentKey extends null ? TKey : `${TParentKey}.${TKey}`) | (TSchema['states'] extends Record<string, any> ? Values<{ [K in keyof TSchema['states'] & string]: StateId<TSchema['states'][K], K, TParentKey extends string ? `${TParentKey}.${TKey}` : TSchema['id'] extends string ? TSchema['id'] : TKey> }> : never)
 */

// ✓ RoutableStateId: verified type-identical to the original
/* @scripttype preserveParamNames */
export function RoutableStateId(TSchema: StateSchema) {
  const out = emptyObject
  for (const K in keySet(keyof(TSchema['states']) & string)) {
    out[K] = RoutableStateId(TSchema['states'][K])
  }
  return (matches<{ route: any; id: string; }>(TSchema) ? `#${TSchema['id']}` : never) | (matches<Record<string, any>>(TSchema['states']) ? Values(out) : never)
}
/* compiles to:
 * export type RoutableStateId<TSchema extends StateSchema> = (TSchema extends { route: any; id: string; } ? `#${TSchema['id']}` : never) | (TSchema['states'] extends Record<string, any> ? Values<{ [K in keyof TSchema['states'] & string]: RoutableStateId<TSchema['states'][K]> }> : never)
 */

// ✓ GetConcreteByKey: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetConcreteByKey(T, TKey: keyof typeof T, TValue: (typeof T)[typeof TKey]) {
  return T & Record(TKey, TValue)
}
/* compiles to:
 * export type GetConcreteByKey<T, TKey extends keyof T, TValue extends T[TKey]> = T & Record<TKey, TValue>
 */

// ✓ _GroupStateKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function _GroupStateKeys(T: StateSchema, S: keyof (typeof T)['states']) {
  if (matches<any>(S)) {
    if (matches<{ type: 'history'; }>(T['states'][S])) {
      return [never, never]
    }
    if (matches<{ type: 'parallel'; }>(T)) {
      return [S, never]
    }
    if (matches<keyof (typeof T)['states'][typeof S]>('states')) {
      return [S, never]
    }
    return [never, S]
  }
  return never
}
/* compiles to:
 * export type _GroupStateKeys<T extends StateSchema, S extends keyof T['states']> = S extends any ? T['states'][S] extends { type: 'history'; } ? [never, never] : T extends { type: 'parallel'; } ? [S, never] : 'states' extends keyof T['states'][S] ? [S, never] : [never, S] : never
 */

// ✓ GroupStateKeys: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GroupStateKeys(T: StateSchema, S: keyof (typeof T)['states']) {
  return { nonLeaf: _GroupStateKeys(T, merge(S, string))[0], leaf: _GroupStateKeys(T, merge(S, string))[1] }
}
/* compiles to:
 * export type GroupStateKeys<T extends StateSchema, S extends keyof T['states']> = { nonLeaf: _GroupStateKeys<T, S & string>[0]; leaf: _GroupStateKeys<T, S & string>[1] }
 */

// ✗ ToStateValue: the ScriptType does not itself typecheck as TypeScript
//   ToStateValue.st.ts(13:35) TS2304: Cannot find name 'K'.
/* @scripttype preserveParamNames */
export function ToStateValue(T: StateSchema) {
  const m1 = matches<{ states: Record<Hole<"S">, any>; }>(T)
  if (m1) {
    if (matches<true>(IsNever(m1.S))) {
      return {}
    }
    const out = emptyObject
    for (const K in keySet(GroupStateKeys(T, m1.S)['nonLeaf'])) {
      out[K] = ToStateValue(T['states'][K])
    }
    const out3 = emptyObject
    for (const StateKey in keySet(K)) {
      out3[StateKey] = ToStateValue(T['states'][K])
    }
    const out2 = emptyObject
    for (const K in keySet(GroupStateKeys(T, m1.S)['nonLeaf'])) {
      out2[K] = out3
    }
    return GroupStateKeys(T, m1.S)['leaf'] | (matches<false>(IsNever(GroupStateKeys(T, m1.S)['nonLeaf'])) ? (matches<{ type: 'parallel'; }>(T) ? out : Compute(Values(out2))) : never)
  }
  return {}
}
/* compiles to:
 * export type ToStateValue<T extends StateSchema> = T extends { states: Record<infer S, any>; } ? IsNever<S> extends true ? {} : GroupStateKeys<T, S>['leaf'] | (IsNever<GroupStateKeys<T, S>['nonLeaf']> extends false ? T extends { type: 'parallel'; } ? { [K in GroupStateKeys<T, S>['nonLeaf']]: ToStateValue<T['states'][K]> } : Compute<Values<{ [K1 in GroupStateKeys<T, S>['nonLeaf']]: { [StateKey in K]: ToStateValue<T['states'][K]> } }>> : never) : {}
 */

// ✗ ExecutableActionsFrom: the ScriptType does not itself typecheck as TypeScript
//   ExecutableActionsFrom.st.ts(3:22) TS2707: Generic type 'StateMachine' requires between 0 and 8 type arguments.
/* @scripttype preserveParamNames */
export function ExecutableActionsFrom(T: AnyActorLogic) {
  const m1 = matches<StateMachine<Hole<"_TContext">, Hole<"_TEvent">, Hole<"_TChildren">, Hole<"_TActor">, Hole<"TAction">, Hole<"_TGuard">, Hole<"_TDelay">, Hole<"_TStateValue">, Hole<"_TTag">, Hole<"_TInput">, Hole<"_TOutput">, Hole<"_TEmitted">, Hole<"_TMeta">, Hole<"_TStateSchema">>>(T)
  if (m1) {
    return SpecialExecutableAction | (matches<(typeof m1.TAction)['type']>(string) ? never : ToExecutableAction(m1.TAction))
  }
  return never
}
/* compiles to:
 * export type ExecutableActionsFrom<T extends AnyActorLogic> = T extends StateMachine<unknown, unknown, unknown, unknown, infer TAction, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown, unknown> ? SpecialExecutableAction | (string extends TAction['type'] ? never : ToExecutableAction<TAction>) : never
 */

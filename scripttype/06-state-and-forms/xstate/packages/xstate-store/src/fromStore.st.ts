/**
 * ScriptType for 06-state-and-forms/xstate/packages/xstate-store/src/fromStore.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 9 type-level aliases: 9 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ActorLogic } from '../../../../../../06-state-and-forms/xstate/packages/core/src/types.js'
import type { StandardSchemaV1 } from '../../../../../../06-state-and-forms/xstate/packages/xstate-store/src/schema.js'
import type { TransitionsFromEventPayloadMap } from '../../../../../../06-state-and-forms/xstate/packages/xstate-store/src/store.js'
import type { EnqueueObject, EventPayloadMap, ExtractEvents, InferSchemaPayloadMap, ResolveStoreContext, ResolveStoreEmittedPayloadMap, StandardSchemaMap, StoreContext, StoreSchemas, StoreSnapshot } from '../../../../../../06-state-and-forms/xstate/packages/xstate-store/src/types.js'
declare namespace m1 {
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const ActorLogic: any
declare const EnqueueObject: any
declare const EventObject: any
declare const EventPayloadMap: any
declare const ExtractEvents: any
declare const InferSchemaPayloadMap: any
declare const ResolveStoreContext: any
declare const ResolveStoreEmittedPayloadMap: any
declare const StandardSchemaMap: any
declare const StandardSchemaV1: any
declare const StoreContext: any
declare const StoreSchemas: any
declare const StoreSnapshot: any
declare const TransitionsFromEventPayloadMap: any
type EventObject<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ StoreLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreLogic(TContext: StoreContext, TEvent: EventObject, TInput, TEmitted: EventObject): any {
  return ActorLogic(StoreSnapshot(TContext), TEvent, TInput, any, TEmitted)
}
/* compiles to:
 * export type StoreLogic<
 *   TContext extends StoreContext,
 *   TEvent extends EventObject,
 *   TInput,
 *   TEmitted extends EventObject
 * > =
 *   ActorLogic<StoreSnapshot<TContext>, TEvent, TInput, any, TEmitted>
 */

// ✓ FromStoreEmittedEvents: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FromStoreEmittedEvents(TEmittedPayloadMap: EventPayloadMap, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return ExtractEvents(ResolveStoreEmittedPayloadMap(TEmittedPayloadMap, TEmittedSchemaMap))
}
/* compiles to:
 * export type FromStoreEmittedEvents<
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   ExtractEvents<ResolveStoreEmittedPayloadMap<TEmittedPayloadMap, TEmittedSchemaMap>>
 */

// ✓ InferredFromStoreTransitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferredFromStoreTransitions(TContext: StoreContext, TTransitions: Record<string, (...args: any[]) => any>, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return Record(string, fnType([NoInfer(ResolveStoreContext(TContext, TContextSchema)), any, EnqueueObject(ResolveStoreContext(TContext, TContextSchema), FromStoreEmittedEvents(TEmittedPayloadMap, TEmittedSchemaMap), InferredEventPayloadMap(TTransitions))], ResolveStoreContext(TContext, TContextSchema) | voidType()))
}
/* compiles to:
 * export type InferredFromStoreTransitions<
 *   TContext extends StoreContext,
 *   TTransitions extends Record<string, (...args: any[]) => any>,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   Record<
 *     string,
 *     (
 *       a0: NoInfer<ResolveStoreContext<TContext, TContextSchema>>,
 *       a1: any,
 *       a2: EnqueueObject<ResolveStoreContext<TContext, TContextSchema>, FromStoreEmittedEvents<TEmittedPayloadMap, TEmittedSchemaMap>, InferredEventPayloadMap<TTransitions>>
 *     ) => ResolveStoreContext<TContext, TContextSchema> | void
 *   >
 */

// ✓ InferredEventPayloadMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferredEventPayloadMap__v1(TTransitions, K): any {
  const m1 = matches<(context: any, event: Hole<"TEvent">, ...args: any[]) => any>(TTransitions[K])
  if (m1) {
    return Omit(m1.TEvent, 'type')
  }
  return {}
}
/**
 * @param {Record<string, (...args: any[]) => any>} TTransitions
 */
export function InferredEventPayloadMap(TTransitions): any {
  const out = emptyObject
  for (const K in keySet(keyof(TTransitions) & string)) {
    out[K] = InferredEventPayloadMap__v1(TTransitions, K)
  }
  return out
}
/* compiles to:
 * export type InferredEventPayloadMap__v1<TTransitions, K> =
 *   TTransitions[K] extends (context: any, event: infer TEvent, ...args: any[]) => any
 *     ? Omit<TEvent, 'type'>
 *     : {}
 * export type InferredEventPayloadMap<
 *   TTransitions extends Record<string, (...args: any[]) => any>
 * > =
 *   { [K in keyof TTransitions & string]: InferredEventPayloadMap__v1<TTransitions, K> }
 */

// ✓ SchemaFromStoreTransitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SchemaFromStoreTransitions(TContext: StoreContext, TEventSchemaMap: StandardSchemaMap, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return TransitionsFromEventPayloadMap(InferSchemaPayloadMap(TEventSchemaMap), NoInfer(ResolveStoreContext(TContext, TContextSchema)), FromStoreEmittedEvents(TEmittedPayloadMap, TEmittedSchemaMap))
}
/* compiles to:
 * export type SchemaFromStoreTransitions<
 *   TContext extends StoreContext,
 *   TEventSchemaMap extends StandardSchemaMap,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   TransitionsFromEventPayloadMap<
 *     InferSchemaPayloadMap<TEventSchemaMap>,
 *     NoInfer<ResolveStoreContext<TContext, TContextSchema>>,
 *     FromStoreEmittedEvents<TEmittedPayloadMap, TEmittedSchemaMap>
 *   >
 */

// ✓ FromStoreValueConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FromStoreValueConfig(TContext: StoreContext, TTransitions: Record<string, (...args: any[]) => any>, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return { context: ResolveStoreContext(TContext, TContextSchema), schemas: optional(StoreSchemas(TContextSchema, Undefined, TEmittedSchemaMap)), on: merge(TTransitions, InferredFromStoreTransitions(TContext, TTransitions, TEmittedPayloadMap, TContextSchema, TEmittedSchemaMap)) }
}
/* compiles to:
 * export type FromStoreValueConfig<
 *   TContext extends StoreContext,
 *   TTransitions extends Record<string, (...args: any[]) => any>,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   {
 *     context: ResolveStoreContext<TContext, TContextSchema>
 *     schemas?: StoreSchemas<TContextSchema, undefined, TEmittedSchemaMap>
 *     on: & TTransitions
 *     & InferredFromStoreTransitions<
 *         TContext,
 *         TTransitions,
 *         TEmittedPayloadMap,
 *         TContextSchema,
 *         TEmittedSchemaMap
 *       >
 *   }
 */

// ✓ FromStoreInputConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function FromStoreInputConfig(TContext: StoreContext, TTransitions: Record<string, (...args: any[]) => any>, TInput, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return { context: fnType([TInput], ResolveStoreContext(TContext, TContextSchema)), schemas: optional(StoreSchemas(TContextSchema, Undefined, TEmittedSchemaMap)), on: merge(TTransitions, InferredFromStoreTransitions(TContext, TTransitions, TEmittedPayloadMap, TContextSchema, TEmittedSchemaMap)) }
}
/* compiles to:
 * export type FromStoreInputConfig<
 *   TContext extends StoreContext,
 *   TTransitions extends Record<string, (...args: any[]) => any>,
 *   TInput,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   {
 *     context: (a0: TInput) => ResolveStoreContext<TContext, TContextSchema>
 *     schemas?: StoreSchemas<TContextSchema, undefined, TEmittedSchemaMap>
 *     on: & TTransitions
 *     & InferredFromStoreTransitions<
 *         TContext,
 *         TTransitions,
 *         TEmittedPayloadMap,
 *         TContextSchema,
 *         TEmittedSchemaMap
 *       >
 *   }
 */

// ✓ SchemaFromStoreValueConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SchemaFromStoreValueConfig(TContext: StoreContext, TEventSchemaMap: StandardSchemaMap, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return { context: ResolveStoreContext(TContext, TContextSchema), schemas: merge(StoreSchemas(TContextSchema, TEventSchemaMap, TEmittedSchemaMap), { events: TEventSchemaMap }), on: SchemaFromStoreTransitions(TContext, TEventSchemaMap, TEmittedPayloadMap, TContextSchema, TEmittedSchemaMap) }
}
/* compiles to:
 * export type SchemaFromStoreValueConfig<
 *   TContext extends StoreContext,
 *   TEventSchemaMap extends StandardSchemaMap,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   {
 *     context: ResolveStoreContext<TContext, TContextSchema>
 *     schemas: & StoreSchemas<TContextSchema, TEventSchemaMap, TEmittedSchemaMap>
 *     & { events: TEventSchemaMap }
 *     on: SchemaFromStoreTransitions<
 *       TContext,
 *       TEventSchemaMap,
 *       TEmittedPayloadMap,
 *       TContextSchema,
 *       TEmittedSchemaMap
 *     >
 *   }
 */

// ✓ SchemaFromStoreInputConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SchemaFromStoreInputConfig(TContext: StoreContext, TEventSchemaMap: StandardSchemaMap, TInput, TEmittedPayloadMap: EventPayloadMap, TContextSchema: StandardSchemaV1 | undefined, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  return { context: fnType([TInput], ResolveStoreContext(TContext, TContextSchema)), schemas: merge(StoreSchemas(TContextSchema, TEventSchemaMap, TEmittedSchemaMap), { events: TEventSchemaMap }), on: SchemaFromStoreTransitions(TContext, TEventSchemaMap, TEmittedPayloadMap, TContextSchema, TEmittedSchemaMap) }
}
/* compiles to:
 * export type SchemaFromStoreInputConfig<
 *   TContext extends StoreContext,
 *   TEventSchemaMap extends StandardSchemaMap,
 *   TInput,
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TContextSchema extends StandardSchemaV1 | undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   {
 *     context: (a0: TInput) => ResolveStoreContext<TContext, TContextSchema>
 *     schemas: & StoreSchemas<TContextSchema, TEventSchemaMap, TEmittedSchemaMap>
 *     & { events: TEventSchemaMap }
 *     on: SchemaFromStoreTransitions<
 *       TContext,
 *       TEventSchemaMap,
 *       TEmittedPayloadMap,
 *       TContextSchema,
 *       TEmittedSchemaMap
 *     >
 *   }
 */

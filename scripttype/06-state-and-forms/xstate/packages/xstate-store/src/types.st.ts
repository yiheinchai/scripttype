/**
 * ScriptType for 06-state-and-forms/xstate/packages/xstate-store/src/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 50 type-level aliases: 50 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { AnyAtomConfig, AnyStoreConfig, AnyStoreLogicCreator, AtomConfig, EventObject, EventPayloadMap, Readable, StandardSchemaMap, Store, StoreContext, StoreLogicCreator } from '../../../../../../06-state-and-forms/xstate/packages/xstate-store/src/types.js'
declare namespace StandardSchemaV1 {
  export type InferOutput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m1 {
  export type TContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TContextSchema<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmitted<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmittedPayloadMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEmittedSchemaMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEvent<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEventPayloadMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TEventSchemaMap<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TInput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TSelectors<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
  export type TValue<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare namespace m2 {
  export type TTransitions<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
declare const AnyAtomConfig: any
declare const AnyStoreConfig: any
declare const AnyStoreLogicCreator: any
declare const AtomConfig: any
declare const EventObject: any
declare const EventPayloadMap: any
declare const InferOutput: any
declare const PropertyKey: any
declare const Readable: any
declare const StandardSchemaMap: any
declare const StandardSchemaV1: any
declare const Store: any
declare const StoreContext: any
declare const StoreLogicCreator: any
type InferOutput<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PropertyKey<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StandardSchemaV1<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StoreConfig<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type StoreSnapshot<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ ExtractEvents: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EventPayloadMap} T
 */
export function ExtractEvents(T): any {
  const out = emptyObject
  for (const K in keySet(keyof(T) & string)) {
    out[K] = merge(T[K], { type: K })
  }
  return Values(out)
}
/* compiles to:
 * export type ExtractEvents<T extends EventPayloadMap> = Values<
 *   { [K in keyof T & string]: T[K] & { type: K } }
 * >
 */

// ✓ InferSchemaOutput: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InferSchemaOutput(TSchema: StandardSchemaV1, TFallback): any {
  return Compute(matches<typeof TFallback>(t<StandardSchemaV1.InferOutput<typeof TSchema>>()) ? t<StandardSchemaV1.InferOutput<typeof TSchema>>() : never)
}
/* compiles to:
 * export type InferSchemaOutput<TSchema extends StandardSchemaV1, TFallback> = Compute<
 *   StandardSchemaV1.InferOutput<TSchema> extends TFallback
 *     ? StandardSchemaV1.InferOutput<TSchema>
 *     : never
 * >
 */

// ✓ InferSchemaPayloadMap: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {StandardSchemaMap} TSchemaMap
 */
export function InferSchemaPayloadMap(TSchemaMap): any {
  const out = emptyObject
  for (const K in keySet(keyof(TSchemaMap) & string)) {
    out[K] = NormalizeSchemaPayload(InferSchemaOutput(TSchemaMap[K], EventPayloadMap[string]))
  }
  return out
}
/* compiles to:
 * export type InferSchemaPayloadMap<TSchemaMap extends StandardSchemaMap> = {
 *   [K in keyof TSchemaMap & string]: NormalizeSchemaPayload<
 *     InferSchemaOutput<TSchemaMap[K], EventPayloadMap[string]>
 *   >
 * }
 */

// ✓ NormalizeSchemaPayload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NormalizeSchemaPayload(TPayload): any {
  if (string in TPayload) {
    return {}
  }
  return TPayload
}
/* compiles to:
 * export type NormalizeSchemaPayload<TPayload> = string extends keyof TPayload ? {} : TPayload
 */

// ✓ StoreSchemas: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreSchemas(TContextSchema: StandardSchemaV1 | undefined = Undefined, TEventSchemaMap: StandardSchemaMap | undefined = Undefined, TEmittedSchemaMap: StandardSchemaMap | undefined = Undefined): any {
  return { context: optional(TContextSchema), events: optional(TEventSchemaMap), emitted: optional(TEmittedSchemaMap) }
}
/* compiles to:
 * export type StoreSchemas<
 *   TContextSchema extends StandardSchemaV1 | undefined = undefined,
 *   TEventSchemaMap extends StandardSchemaMap | undefined = undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined = undefined
 * > =
 *   { context?: TContextSchema; events?: TEventSchemaMap; emitted?: TEmittedSchemaMap }
 */

// ✓ ResolveStoreContext: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveStoreContext(TContext: StoreContext, TContextSchema: StandardSchemaV1 | undefined): any {
  if (matches<StandardSchemaV1>(TContextSchema)) {
    return InferSchemaOutput(TContextSchema, StoreContext)
  }
  return TContext
}
/* compiles to:
 * export type ResolveStoreContext<
 *   TContext extends StoreContext,
 *   TContextSchema extends StandardSchemaV1 | undefined
 * > =
 *   TContextSchema extends StandardSchemaV1
 *     ? InferSchemaOutput<TContextSchema, StoreContext>
 *     : TContext
 */

// ✓ ResolveStoreEventPayloadMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveStoreEventPayloadMap(TEventPayloadMap: EventPayloadMap, TEventSchemaMap: StandardSchemaMap | undefined): any {
  if (matches<StandardSchemaMap>(TEventSchemaMap)) {
    return InferSchemaPayloadMap(TEventSchemaMap)
  }
  return TEventPayloadMap
}
/* compiles to:
 * export type ResolveStoreEventPayloadMap<
 *   TEventPayloadMap extends EventPayloadMap,
 *   TEventSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   TEventSchemaMap extends StandardSchemaMap
 *     ? InferSchemaPayloadMap<TEventSchemaMap>
 *     : TEventPayloadMap
 */

// ✓ ResolveStoreEmittedPayloadMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ResolveStoreEmittedPayloadMap(TEmittedPayloadMap: EventPayloadMap, TEmittedSchemaMap: StandardSchemaMap | undefined): any {
  if (matches<StandardSchemaMap>(TEmittedSchemaMap)) {
    return InferSchemaPayloadMap(TEmittedSchemaMap)
  }
  return TEmittedPayloadMap
}
/* compiles to:
 * export type ResolveStoreEmittedPayloadMap<
 *   TEmittedPayloadMap extends EventPayloadMap,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined
 * > =
 *   TEmittedSchemaMap extends StandardSchemaMap
 *     ? InferSchemaPayloadMap<TEmittedSchemaMap>
 *     : TEmittedPayloadMap
 */

// ✓ EventPayloadFromEvent: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventPayloadFromEvent(TEvent): any {
  if (matches<{ type: string; }>(TEvent)) {
    return DistributiveOmit(TEvent, 'type')
  }
  return TEvent
}
/* compiles to:
 * export type EventPayloadFromEvent<TEvent> =
 *   TEvent extends { type: string; } ? DistributiveOmit<TEvent, 'type'> : TEvent
 */

// ✓ EventPayloadMapFromTransitions: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventPayloadMapFromTransitions__v1(TTransitions, K): any {
  const m1 = matches<(context: any, event: Hole<"TEvent">, ...args: any[]) => unknown>(TTransitions[K])
  if (m1) {
    return EventPayloadFromEvent(m1.TEvent)
  }
  return {}
}
export function EventPayloadMapFromTransitions(TTransitions): any {
  const out = emptyObject
  for (const K in keySet(keyof(TTransitions) & string)) {
    out[K] = EventPayloadMapFromTransitions__v1(TTransitions, K)
  }
  return out
}
/* compiles to:
 * export type EventPayloadMapFromTransitions__v1<TTransitions, K> =
 *   TTransitions[K] extends (context: any, event: infer TEvent, ...args: any[]) => unknown
 *     ? EventPayloadFromEvent<TEvent>
 *     : {}
 * export type EventPayloadMapFromTransitions<TTransitions> = {
 *   [K in keyof TTransitions & string]: EventPayloadMapFromTransitions__v1<TTransitions, K>
 * }
 */

// ✓ StoreEventPayloadMapFromConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreEventPayloadMapFromConfig(TConfig: AnyStoreConfig): any {
  const m1 = matches<{ schemas: { events: Hole<"TEventSchemaMap">; }; }>(TConfig)
  if (m1) {
    if (matches<StandardSchemaMap>(m1.TEventSchemaMap)) {
      return InferSchemaPayloadMap(m1.TEventSchemaMap)
    }
    return {}
  }
  const m2 = matches<{ on: Hole<"TTransitions">; }>(TConfig)
  if (m2) {
    return EventPayloadMapFromTransitions(m2.TTransitions)
  }
  return {}
}
/* compiles to:
 * export type StoreEventPayloadMapFromConfig<TConfig extends AnyStoreConfig> =
 *   TConfig extends { schemas: { events: infer TEventSchemaMap; }; }
 *     ? TEventSchemaMap extends StandardSchemaMap ? InferSchemaPayloadMap<TEventSchemaMap> : {}
 *   : TConfig extends { on: infer TTransitions; } ? EventPayloadMapFromTransitions<TTransitions>
 *   : {}
 */

// ✓ EmitterFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EmitterFunction(TEmittedEvent: EventObject): any {
  return fnType([...matches<typeof TEmittedEvent>({ type: TEmittedEvent['type'] }) ? [optElem(DistributiveOmit(TEmittedEvent, 'type'))] : [DistributiveOmit(TEmittedEvent, 'type')]], voidType())
}
/* compiles to:
 * export type EmitterFunction<TEmittedEvent extends EventObject> =
 *   (
 *     ...a0: { type: TEmittedEvent['type'] } extends TEmittedEvent ? [DistributiveOmit<TEmittedEvent, 'type'>?] : [DistributiveOmit<TEmittedEvent, 'type'>]
 *   ) => void
 */

// ✓ TriggerFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TriggerFunction(TEvent: EventObject): any {
  return fnType([...matches<typeof TEvent>({ type: TEvent['type'] }) ? [optElem(DistributiveOmit(TEvent, 'type'))] : [DistributiveOmit(TEvent, 'type')]], voidType())
}
/* compiles to:
 * export type TriggerFunction<TEvent extends EventObject> =
 *   (
 *     ...a0: { type: TEvent['type'] } extends TEvent ? [DistributiveOmit<TEvent, 'type'>?] : [DistributiveOmit<TEvent, 'type'>]
 *   ) => void
 */

// ✓ CanFunction: verified type-identical to the original
/* @scripttype preserveParamNames */
export function CanFunction(TEvent: EventObject): any {
  return fnType([...matches<typeof TEvent>({ type: TEvent['type'] }) ? [optElem(DistributiveOmit(TEvent, 'type'))] : [DistributiveOmit(TEvent, 'type')]], boolean)
}
/* compiles to:
 * export type CanFunction<TEvent extends EventObject> =
 *   (
 *     ...a0: { type: TEvent['type'] } extends TEvent ? [DistributiveOmit<TEvent, 'type'>?] : [DistributiveOmit<TEvent, 'type'>]
 *   ) => boolean
 */

// ✓ EventFromPayload: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventFromPayload(TKey: string, TPayload): any {
  return merge({ type: TKey }, matches<null | undefined>(TPayload) ? {} : TPayload)
}
/* compiles to:
 * export type EventFromPayload<TKey extends string, TPayload> =
 *   { type: TKey } & (TPayload extends null | undefined ? {} : TPayload)
 */

// ✓ TriggerObject: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EventPayloadMap} TEventPayloadMap
 */
export function TriggerObject(TEventPayloadMap): any {
  const out = emptyObject
  for (const K in keySet(keyof(TEventPayloadMap) & string)) {
    out[K] = matches<typeof K>(string) ? fnType([...arrayOf(any)], voidType()) : TriggerFunction(EventFromPayload(K, TEventPayloadMap[K]))
  }
  return out
}
/* compiles to:
 * export type TriggerObject<TEventPayloadMap extends EventPayloadMap> = {
 *   [K in keyof TEventPayloadMap & string]: string extends K
 *     ? (...a0: any[]) => void
 *     : TriggerFunction<EventFromPayload<K, TEventPayloadMap[K]>>
 * }
 */

// ✓ CanObject: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EventPayloadMap} TEventPayloadMap
 */
export function CanObject(TEventPayloadMap): any {
  const out = emptyObject
  for (const K in keySet(keyof(TEventPayloadMap) & string)) {
    out[K] = matches<typeof K>(string) ? fnType([...arrayOf(any)], boolean) : CanFunction(EventFromPayload(K, TEventPayloadMap[K]))
  }
  return out
}
/* compiles to:
 * export type CanObject<TEventPayloadMap extends EventPayloadMap> = {
 *   [K in keyof TEventPayloadMap & string]: string extends K
 *     ? (...a0: any[]) => boolean
 *     : CanFunction<EventFromPayload<K, TEventPayloadMap[K]>>
 * }
 */

// ✓ StoreEffectEnqueue: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreEffectEnqueue(TContext: StoreContext = StoreContext, TEventPayloadMap: EventPayloadMap = {}): any {
  return { trigger: TriggerObject(TEventPayloadMap), send: fnType([EventObject], voidType()), getSnapshot: fnType([], StoreSnapshot(TContext)) }
}
/* compiles to:
 * export type StoreEffectEnqueue<
 *   TContext extends StoreContext = StoreContext,
 *   TEventPayloadMap extends EventPayloadMap = {}
 * > =
 *   {
 *     trigger: TriggerObject<TEventPayloadMap>
 *     send: (a0: EventObject) => void
 *     getSnapshot: () => StoreSnapshot<TContext>
 *   }
 */

// ✓ EnqueueObject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EnqueueObject(TContext: StoreContext, TEmittedEvent: EventObject, TEventPayloadMap: EventPayloadMap = {}): any {
  const out = emptyObject
  for (const E in keySet(TEmittedEvent)) {
    out[E['type']] = EmitterFunction(E)
  }
  return { emit: out, trigger: TriggerObject(TEventPayloadMap), effect: fnType([fnType([StoreEffectEnqueue(TContext, TEventPayloadMap)], voidType())], voidType()) }
}
/* compiles to:
 * export type EnqueueObject<
 *   TContext extends StoreContext,
 *   TEmittedEvent extends EventObject,
 *   TEventPayloadMap extends EventPayloadMap = {}
 * > =
 *   {
 *     emit: { [E in TEmittedEvent as E['type']]: EmitterFunction<E> }
 *     trigger: TriggerObject<TEventPayloadMap>
 *     effect: (a0: (a0: StoreEffectEnqueue<TContext, TEventPayloadMap>) => void) => void
 *   }
 */

// ✓ StoreEffect: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreEffect(TEmitted: EventObject): any {
  return anyOf(fnType([optElem(StoreEffectEnqueue(any, any))], voidType()), TEmitted)
}
/* compiles to:
 * export type StoreEffect<TEmitted extends EventObject> =
 *   ((a0?: StoreEffectEnqueue<any, any>) => void) | TEmitted
 */

// ✓ StoreAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreAssigner(TContext: StoreContext, TEvent: EventObject, TEmitted: EventObject, TEventPayloadMap: EventPayloadMap = {}): any {
  return fnType([TContext, TEvent, EnqueueObject(TContext, TEmitted, TEventPayloadMap)], anyOf(TContext, voidType()))
}
/* compiles to:
 * export type StoreAssigner<
 *   TContext extends StoreContext,
 *   TEvent extends EventObject,
 *   TEmitted extends EventObject,
 *   TEventPayloadMap extends EventPayloadMap = {}
 * > =
 *   (a0: TContext, a1: TEvent, a2: EnqueueObject<TContext, TEmitted, TEventPayloadMap>) => | TContext
 *   | void
 */

// ✓ StoreProducerAssigner: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreProducerAssigner(TContext: StoreContext, TEvent: EventObject, TEmitted: EventObject, TEventPayloadMap: EventPayloadMap = {}): any {
  return fnType([TContext, TEvent, EnqueueObject(TContext, TEmitted, TEventPayloadMap)], voidType())
}
/* compiles to:
 * export type StoreProducerAssigner<
 *   TContext extends StoreContext,
 *   TEvent extends EventObject,
 *   TEmitted extends EventObject,
 *   TEventPayloadMap extends EventPayloadMap = {}
 * > =
 *   (a0: TContext, a1: TEvent, a2: EnqueueObject<TContext, TEmitted, TEventPayloadMap>) => void
 */

// ✓ Snapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Snapshot(TOutput): any {
  return anyOf({ status: 'active', output: Undefined, error: Undefined }, { status: 'done', output: TOutput, error: Undefined }, { status: 'error', output: Undefined, error: unknown }, { status: 'stopped', output: Undefined, error: Undefined })
}
/* compiles to:
 * export type Snapshot<TOutput> =
 *   | { status: 'active'; output: undefined; error: undefined }
 *   | { status: 'done'; output: TOutput; error: undefined }
 *   | { status: 'error'; output: undefined; error: unknown }
 *   | { status: 'stopped'; output: undefined; error: undefined }
 */

// ✓ StoreSnapshot: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreSnapshot(TContext): any {
  return merge(Snapshot(Undefined), { context: TContext })
}
/* compiles to:
 * export type StoreSnapshot<TContext> = Snapshot<undefined> & { context: TContext }
 */

// ✓ StoreTransition: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreTransition(TContext: StoreContext, TEvent: EventObject, TEmitted: EventObject): any {
  return fnType([StoreSnapshot(TContext), TEvent], StoreTransitionResult(StoreSnapshot(TContext), TEmitted))
}
/* compiles to:
 * export type StoreTransition<
 *   TContext extends StoreContext,
 *   TEvent extends EventObject,
 *   TEmitted extends EventObject
 * > =
 *   (a0: StoreSnapshot<TContext>, a1: TEvent) => StoreTransitionResult<
 *     StoreSnapshot<TContext>,
 *     TEmitted
 *   >
 */

// ✓ StoreTransitionResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreTransitionResult(TSnapshot: StoreSnapshot<any>, TEmitted: EventObject): any {
  return merge([TSnapshot, arrayOf(StoreEffect(TEmitted))], { _allowed: optional(boolean) })
}
/* compiles to:
 * export type StoreTransitionResult<
 *   TSnapshot extends StoreSnapshot<any>,
 *   TEmitted extends EventObject
 * > =
 *   [TSnapshot, StoreEffect<TEmitted>[]] & { _allowed?: boolean }
 */

// ✓ StoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {EventPayloadMap} TEventPayloadMap
 */
export function StoreConfig(TContext: StoreContext, TEventPayloadMap, TEmittedPayloadMap: EventPayloadMap = {}, TContextSchema: StandardSchemaV1 | undefined = Undefined, TEventSchemaMap: StandardSchemaMap | undefined = Undefined, TEmittedSchemaMap: StandardSchemaMap | undefined = Undefined): any {
  const out = emptyObject
  for (const K in keySet(keyof(ResolveStoreEventPayloadMap(TEventPayloadMap, TEventSchemaMap)) & string)) {
    out[K] = optional(StoreAssigner(ResolveStoreContext(TContext, TContextSchema), merge({ type: K }, ResolveStoreEventPayloadMap(TEventPayloadMap, TEventSchemaMap)[K]), ExtractEvents(ResolveStoreEmittedPayloadMap(TEmittedPayloadMap, TEmittedSchemaMap)), ResolveStoreEventPayloadMap(TEventPayloadMap, TEventSchemaMap)))
  }
  const out2 = emptyObject
  for (const K in keySet(keyof(TEventPayloadMap) & string)) {
    out2[K] = StoreAssigner(ResolveStoreContext(TContext, TContextSchema), merge({ type: K }, TEventPayloadMap[K]), ExtractEvents(ResolveStoreEmittedPayloadMap(TEmittedPayloadMap, TEmittedSchemaMap)), ResolveStoreEventPayloadMap(TEventPayloadMap, TEventSchemaMap))
  }
  return { context: ResolveStoreContext(TContext, TContextSchema), schemas: optional(StoreSchemas(TContextSchema, TEventSchemaMap, TEmittedSchemaMap)), on: matches<StandardSchemaMap>(TEventSchemaMap) ? out : out2 }
}
/* compiles to:
 * export type StoreConfig<
 *   TContext extends StoreContext,
 *   TEventPayloadMap extends EventPayloadMap,
 *   TEmittedPayloadMap extends EventPayloadMap = {},
 *   TContextSchema extends StandardSchemaV1 | undefined = undefined,
 *   TEventSchemaMap extends StandardSchemaMap | undefined = undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined = undefined
 * > =
 *   {
 *     context: ResolveStoreContext<TContext, TContextSchema>
 *     schemas?: StoreSchemas<TContextSchema, TEventSchemaMap, TEmittedSchemaMap>
 *     on: TEventSchemaMap extends StandardSchemaMap
 *       ? {
 *         [K in keyof ResolveStoreEventPayloadMap<TEventPayloadMap, TEventSchemaMap> & string]?: StoreAssigner<
 *           ResolveStoreContext<TContext, TContextSchema>,
 *           { type: K } & ResolveStoreEventPayloadMap<TEventPayloadMap, TEventSchemaMap>[K],
 *           ExtractEvents<ResolveStoreEmittedPayloadMap<TEmittedPayloadMap, TEmittedSchemaMap>>,
 *           ResolveStoreEventPayloadMap<TEventPayloadMap, TEventSchemaMap>
 *         >
 *       }
 *       : {
 *         [K1 in keyof TEventPayloadMap & string]: StoreAssigner<
 *           ResolveStoreContext<TContext, TContextSchema>,
 *           { type: K1 } & TEventPayloadMap[K1],
 *           ExtractEvents<ResolveStoreEmittedPayloadMap<TEmittedPayloadMap, TEmittedSchemaMap>>,
 *           ResolveStoreEventPayloadMap<TEventPayloadMap, TEventSchemaMap>
 *         >
 *       }
 *   }
 */

// ✓ SpecificStoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SpecificStoreConfig(TContext: StoreContext, TEvent: EventObject, TEmitted: EventObject, TContextSchema: StandardSchemaV1 | undefined = Undefined, TEventSchemaMap: StandardSchemaMap | undefined = Undefined, TEmittedSchemaMap: StandardSchemaMap | undefined = Undefined): any {
  const out = emptyObject
  for (const E in keySet(TEvent)) {
    out[E['type']] = optional(StoreAssigner(ResolveStoreContext(TContext, TContextSchema), E, TEmitted, EventPayloadMapFromEvents(TEvent)))
  }
  return { context: ResolveStoreContext(TContext, TContextSchema), schemas: optional(StoreSchemas(TContextSchema, TEventSchemaMap, TEmittedSchemaMap)), on: out }
}
/* compiles to:
 * export type SpecificStoreConfig<
 *   TContext extends StoreContext,
 *   TEvent extends EventObject,
 *   TEmitted extends EventObject,
 *   TContextSchema extends StandardSchemaV1 | undefined = undefined,
 *   TEventSchemaMap extends StandardSchemaMap | undefined = undefined,
 *   TEmittedSchemaMap extends StandardSchemaMap | undefined = undefined
 * > =
 *   {
 *     context: ResolveStoreContext<TContext, TContextSchema>
 *     schemas?: StoreSchemas<TContextSchema, TEventSchemaMap, TEmittedSchemaMap>
 *     on: {
 *       [E in TEvent as E['type']]?: StoreAssigner<
 *         ResolveStoreContext<TContext, TContextSchema>,
 *         E,
 *         TEmitted,
 *         EventPayloadMapFromEvents<TEvent>
 *       >
 *     }
 *   }
 */

// ✓ EventPayloadMapFromEvents: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventPayloadMapFromEvents(TEvent: EventObject): any {
  const out = emptyObject
  for (const E in keySet(TEvent)) {
    out[E['type']] = DistributiveOmit(E, 'type')
  }
  return out
}
/* compiles to:
 * export type EventPayloadMapFromEvents<TEvent extends EventObject> = {
 *   [E in TEvent as E['type']]: DistributiveOmit<E, 'type'>
 * }
 */

// ✓ Compute: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Compute(A): any {
  if (matches<any>(A)) {
    const out = emptyObject
    for (const K in keyof(A)) {
      out[K] = A[K]
    }
    return out
  }
  return never
}
/* compiles to:
 * export type Compute<A> = A extends any ? { [K in keyof A]: A[K] } : never
 */

// ✓ SnapshotFromStore: verified type-identical to the original
/* @scripttype preserveParamNames */
export function SnapshotFromStore(TStore: Store<any, any, any>): any {
  const m1 = matches<Store<Hole<"TContext">, any, any>>(TStore)
  if (m1) {
    return StoreSnapshot(m1.TContext)
  }
  return never
}
/* compiles to:
 * export type SnapshotFromStore<TStore extends Store<any, any, any>> =
 *   TStore extends Store<infer TContext, any, any> ? StoreSnapshot<TContext> : never
 */

// ✓ EventFromStore: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventFromStore(TStore: Store<any, any, any>): any {
  const m1 = matches<Store<Hole<"_TContext">, Hole<"TEventPayloadMap">, Hole<"_TEmitted">>>(TStore)
  if (m1) {
    return ExtractEvents(m1.TEventPayloadMap)
  }
  return never
}
/* compiles to:
 * export type EventFromStore<TStore extends Store<any, any, any>> =
 *   TStore extends Store<any, infer TEventPayloadMap, any>
 *     ? ExtractEvents<TEventPayloadMap>
 *     : never
 */

// ✓ Observer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Observer(T): any {
  return { next: optional(fnType([T], voidType())), error: optional(fnType([unknown], voidType())), complete: optional(fnType([], voidType())) }
}
/* compiles to:
 * export type Observer<T> = {
 *   next?: (a0: T) => void
 *   error?: (a0: unknown) => void
 *   complete?: () => void
 * }
 */

// ✓ Values: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Values(T): any {
  return T[keyof(T)]
}
/* compiles to:
 * export type Values<T> = T[keyof T]
 */

// ✓ Selector: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Selector(TContext, TSelected): any {
  return fnType([TContext], TSelected)
}
/* compiles to:
 * export type Selector<TContext, TSelected> = (a0: TContext) => TSelected
 */

// ✓ Selection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Selection(TSelected): any {
  return Readable(TSelected)
}
/* compiles to:
 * export type Selection<TSelected> = Readable<TSelected>
 */

// ✓ StoreSelectorsConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreSelectorsConfig(TContext: StoreContext): any {
  return Record(string, Selector(TContext, any))
}
/* compiles to:
 * export type StoreSelectorsConfig<TContext extends StoreContext> = Record<
 *   string,
 *   Selector<TContext, any>
 * >
 */

// ✓ ResolvedStoreSelectors: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {StoreContext} TContext
 * @param {StoreSelectorsConfig<typeof TContext>} TSelectors
 */
export function ResolvedStoreSelectors(TContext, TSelectors): any {
  const out = emptyObject
  for (const K in keyof(TSelectors)) {
    out[K] = Selection(ReturnType(TSelectors[K]))
  }
  return out
}
/* compiles to:
 * export type ResolvedStoreSelectors<
 *   TContext extends StoreContext,
 *   TSelectors extends StoreSelectorsConfig<TContext>
 * > =
 *   { [K in keyof TSelectors]: Selection<ReturnType<TSelectors[K]>> }
 */

// ✓ StoreWithSelectors: verified type-identical to the original
/* @scripttype preserveParamNames */
/**
 * @param {StoreContext} TContext
 * @param {StoreSelectorsConfig<typeof TContext>} TSelectors
 */
export function StoreWithSelectors(TContext, TEventPayloadMap: EventPayloadMap, TEmitted: EventObject, TSelectors): any {
  return merge(Omit(Store(TContext, TEventPayloadMap, TEmitted), 'with'), { selectors: ResolvedStoreSelectors(TContext, TSelectors), with: genericMethodType(['TNewEventPayloadMap extends EventPayloadMap'], [StoreExtension(TContext, TEventPayloadMap, TNewEventPayloadMap, TEmitted)], StoreWithSelectors(TContext, merge(TEventPayloadMap, TNewEventPayloadMap), TEmitted, TSelectors)) })
}
/* compiles to:
 * export type StoreWithSelectors<
 *   TContext extends StoreContext,
 *   TEventPayloadMap extends EventPayloadMap,
 *   TEmitted extends EventObject,
 *   TSelectors extends StoreSelectorsConfig<TContext>
 * > =
 *   & Omit<Store<TContext, TEventPayloadMap, TEmitted>, 'with'>
 *   & {
 *       selectors: ResolvedStoreSelectors<TContext, TSelectors>
 *       with<TNewEventPayloadMap extends EventPayloadMap>(a0: StoreExtension<TContext, TEventPayloadMap, TNewEventPayloadMap, TEmitted>): StoreWithSelectors<
 *         TContext,
 *         TEventPayloadMap & TNewEventPayloadMap,
 *         TEmitted,
 *         TSelectors
 *       >
 *     }
 */

// ✓ StoreFromStoreLogicCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreFromStoreLogicCreator(TLogic: AnyStoreLogicCreator): any {
  const m1 = matches<StoreLogicCreator<Hole<"TContext">, Hole<"TEventPayloadMap">, Hole<"TEmitted">, any, Hole<"TSelectors">>>(TLogic)
  if (m1) {
    return StoreWithSelectors(m1.TContext, m1.TEventPayloadMap, m1.TEmitted, m1.TSelectors)
  }
  return never
}
/* compiles to:
 * export type StoreFromStoreLogicCreator<TLogic extends AnyStoreLogicCreator> =
 *   TLogic extends StoreLogicCreator<infer TContext, infer TEventPayloadMap, infer TEmitted, any, infer TSelectors>
 *     ? StoreWithSelectors<TContext, TEventPayloadMap, TEmitted, TSelectors>
 *     : never
 */

// ✓ InputFromStoreLogicCreator: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InputFromStoreLogicCreator(TLogic: AnyStoreLogicCreator): any {
  const m1 = matches<StoreLogicCreator<any, any, any, Hole<"TInput">, any>>(TLogic)
  if (m1) {
    return m1.TInput
  }
  return never
}
/* compiles to:
 * export type InputFromStoreLogicCreator<TLogic extends AnyStoreLogicCreator> =
 *   TLogic extends StoreLogicCreator<any, any, any, infer TInput, any> ? TInput : never
 */

// ✓ ValueFromAtomConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ValueFromAtomConfig(TConfig: AnyAtomConfig): any {
  const m1 = matches<AtomConfig<Hole<"TValue">, any>>(TConfig)
  if (m1) {
    return m1.TValue
  }
  return never
}
/* compiles to:
 * export type ValueFromAtomConfig<TConfig extends AnyAtomConfig> =
 *   TConfig extends AtomConfig<infer TValue, any> ? TValue : never
 */

// ✓ InputFromAtomConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InputFromAtomConfig(TConfig: AnyAtomConfig): any {
  const m1 = matches<AtomConfig<any, Hole<"TInput">>>(TConfig)
  if (m1) {
    return m1.TInput
  }
  return never
}
/* compiles to:
 * export type InputFromAtomConfig<TConfig extends AnyAtomConfig> =
 *   TConfig extends AtomConfig<any, infer TInput> ? TInput : never
 */

// ✓ DistributiveOmit: verified type-identical to the original
/* @scripttype preserveParamNames */
export function DistributiveOmit(T, K: PropertyKey): any {
  if (matches<any>(T)) {
    return Omit(T, K)
  }
  return never
}
/* compiles to:
 * export type DistributiveOmit<T, K extends PropertyKey> = T extends any ? Omit<T, K> : never
 */

// ✓ StoreLogic: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreLogic(TSnapshot: StoreSnapshot<any>, TEvent: EventObject, TEmitted: EventObject): any {
  return { eventTypes: optional(readonlyArrayOf(string)), schemas: optional(StoreSchemas(any, any, any)), getInitialSnapshot: fnType([], TSnapshot), transition: fnType([TSnapshot, TEvent], StoreTransitionResult(TSnapshot, TEmitted)) }
}
/* compiles to:
 * export type StoreLogic<
 *   TSnapshot extends StoreSnapshot<any>,
 *   TEvent extends EventObject,
 *   TEmitted extends EventObject
 * > =
 *   {
 *     eventTypes?: readonly string[]
 *     schemas?: StoreSchemas<any, any, any>
 *     getInitialSnapshot: () => TSnapshot
 *     transition: (a0: TSnapshot, a1: TEvent) => StoreTransitionResult<TSnapshot, TEmitted>
 *   }
 */

// ✓ StoreExtension: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreExtension(TContext: StoreContext, TEventPayloadMap: EventPayloadMap, TNewEventPayloadMap: EventPayloadMap, TEmitted: EventObject): any {
  return fnType([StoreLogic(StoreSnapshot(TContext), ExtractEvents(TEventPayloadMap), TEmitted)], StoreLogic(StoreSnapshot(TContext), anyOf(ExtractEvents(TEventPayloadMap), ExtractEvents(TNewEventPayloadMap)), TEmitted))
}
/* compiles to:
 * export type StoreExtension<
 *   TContext extends StoreContext,
 *   TEventPayloadMap extends EventPayloadMap,
 *   TNewEventPayloadMap extends EventPayloadMap,
 *   TEmitted extends EventObject
 * > =
 *   (a0: StoreLogic<StoreSnapshot<TContext>, ExtractEvents<TEventPayloadMap>, TEmitted>) => StoreLogic<
 *     StoreSnapshot<TContext>,
 *     ExtractEvents<TEventPayloadMap> | ExtractEvents<TNewEventPayloadMap>,
 *     TEmitted
 *   >
 */

// ✓ EventFromStoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EventFromStoreConfig(TStore: AnyStoreConfig): any {
  const m1 = matches<StoreConfig<any, Hole<"TEventPayloadMap">, any, any, Hole<"TEventSchemaMap">, any>>(TStore)
  if (m1) {
    return ExtractEvents(ResolveStoreEventPayloadMap(m1.TEventPayloadMap, m1.TEventSchemaMap))
  }
  return never
}
/* compiles to:
 * export type EventFromStoreConfig<TStore extends AnyStoreConfig> =
 *   TStore extends StoreConfig<any, infer TEventPayloadMap, any, any, infer TEventSchemaMap, any>
 *     ? ExtractEvents<ResolveStoreEventPayloadMap<TEventPayloadMap, TEventSchemaMap>>
 *     : never
 */

// ✓ EmitsFromStoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EmitsFromStoreConfig(TStore: AnyStoreConfig): any {
  const m1 = matches<StoreConfig<any, any, Hole<"TEmittedPayloadMap">, any, any, Hole<"TEmittedSchemaMap">>>(TStore)
  if (m1) {
    return ExtractEvents(ResolveStoreEmittedPayloadMap(m1.TEmittedPayloadMap, m1.TEmittedSchemaMap))
  }
  return never
}
/* compiles to:
 * export type EmitsFromStoreConfig<TStore extends AnyStoreConfig> =
 *   TStore extends StoreConfig<any, any, infer TEmittedPayloadMap, any, any, infer TEmittedSchemaMap>
 *     ? ExtractEvents<ResolveStoreEmittedPayloadMap<TEmittedPayloadMap, TEmittedSchemaMap>>
 *     : never
 */

// ✓ ContextFromStoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ContextFromStoreConfig(TStore: AnyStoreConfig): any {
  const m1 = matches<StoreConfig<Hole<"TContext">, any, any, Hole<"TContextSchema">, any, any>>(TStore)
  if (m1) {
    return ResolveStoreContext(m1.TContext, m1.TContextSchema)
  }
  return never
}
/* compiles to:
 * export type ContextFromStoreConfig<TStore extends AnyStoreConfig> =
 *   TStore extends StoreConfig<infer TContext, any, any, infer TContextSchema, any, any>
 *     ? ResolveStoreContext<TContext, TContextSchema>
 *     : never
 */

// ✓ StoreFromStoreConfig: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreFromStoreConfig(TConfig: AnyStoreConfig): any {
  const m1 = matches<StoreConfig<Hole<"TContext">, any, Hole<"TEmittedPayloadMap">, Hole<"TContextSchema">, any, Hole<"TEmittedSchemaMap">>>(TConfig)
  if (m1) {
    return Store(ResolveStoreContext(m1.TContext, m1.TContextSchema), StoreEventPayloadMapFromConfig(TConfig), ExtractEvents(ResolveStoreEmittedPayloadMap(m1.TEmittedPayloadMap, m1.TEmittedSchemaMap)))
  }
  return never
}
/* compiles to:
 * export type StoreFromStoreConfig<TConfig extends AnyStoreConfig> =
 *   TConfig extends StoreConfig<infer TContext, any, infer TEmittedPayloadMap, infer TContextSchema, any, infer TEmittedSchemaMap>
 *     ? Store<
 *       ResolveStoreContext<TContext, TContextSchema>,
 *       StoreEventPayloadMapFromConfig<TConfig>,
 *       ExtractEvents<ResolveStoreEmittedPayloadMap<TEmittedPayloadMap, TEmittedSchemaMap>>
 *     >
 *     : never
 */

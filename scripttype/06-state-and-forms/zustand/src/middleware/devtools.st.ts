/**
 * ScriptType for 06-state-and-forms/zustand/src/middleware/devtools.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 6 type-level aliases: 5 covered, 1 raw
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const StoreApi: any
type StoreApi<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✓ Cast: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Cast(T, U) {
  if (matches<typeof U>(T)) {
    return T
  }
  return U
}
/* compiles to:
 * export type Cast<T, U> = T extends U ? T : U
 */

// ✓ Write: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Write(T, U) {
  return Omit(T, keyof(U)) & U
}
/* compiles to:
 * export type Write<T, U> = Omit<T, keyof U> & U
 */

// ✓ TakeTwo: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TakeTwo(T) {
  if (matches<{ length: 0; }>(T)) {
    return [Undefined, Undefined]
  }
  if (matches<{ length: 1; }>(T)) {
    return [Cast(T, arrayOf(unknown)), Undefined]
  }
  if (matches<{ length: 0 | 1; }>(T)) {
    return [Cast(T, arrayOf(unknown)), Undefined]
  }
  if (matches<{ length: 2; }>(T)) {
    return T
  }
  if (matches<{ length: 1 | 2; }>(T)) {
    return T
  }
  if (matches<{ length: 0 | 1 | 2; }>(T)) {
    return T
  }
  const m1 = matches<[ Hole<"A0">, Hole<"A1">, ...unknown[] ]>(T)
  if (m1) {
    return [m1.A0, m1.A1]
  }
  const m2 = matches<[ Hole<"A0">, (Hole<"A1">)?, ...unknown[] ]>(T)
  if (m2) {
    return [m2.A0, optElem(m2.A1)]
  }
  const m3 = matches<[ (Hole<"A0">)?, (Hole<"A1">)?, ...unknown[] ]>(T)
  if (m3) {
    return [optElem(m3.A0), optElem(m3.A1)]
  }
  return never
}
/* compiles to:
 * export type TakeTwo<T> =
 *   T extends { length: 0; } ? [undefined, undefined]
 *   : T extends { length: 1; } ? [Cast<T, unknown[]>, undefined]
 *   : T extends { length: 0 | 1; } ? [Cast<T, unknown[]>, undefined]
 *   : T extends { length: 2; } ? T
 *   : T extends { length: 1 | 2; } ? T
 *   : T extends { length: 0 | 1 | 2; } ? T
 *   : T extends [infer A0, infer A1, ...unknown[]] ? [A0, A1]
 *   : T extends [infer A0, infer A1, ...unknown[]] ? [A0, A1?]
 *   : T extends [infer A0, infer A1, ...unknown[]] ? [A0?, A1?]
 *   : never
 */

// ✓ WithDevtools: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithDevtools(S) {
  return Write(S, StoreDevtools(S))
}
/* compiles to:
 * export type WithDevtools<S> = Write<S, StoreDevtools<S>>
 */

// ✗ StoreDevtools: uses raw() — language gap, does not count as covered
//   gap: object member MethodSignature
/* @scripttype preserveParamNames */
export function StoreDevtools(S) {
  const m1 = matches<{ setState: { (...args: Hole<"Sa1">): Hole<"Sr1">; (...args: Hole<"Sa2">): Hole<"Sr2">; }; }>(S)
  if (m1) {
    return raw('{ setState(...args: [ ...args: TakeTwo<Sa1>, action?: Action ]): Sr1; setState(...args: [ ...args: TakeTwo<Sa2>, action?: Action ]): Sr2; devtools: { cleanup: () => void; }; }')
  }
  return never
}

// ✓ NamedSet: verified type-identical to the original
/* @scripttype preserveParamNames */
export function NamedSet(T) {
  return WithDevtools(StoreApi(T))['setState']
}
/* compiles to:
 * export type NamedSet<T> = WithDevtools<StoreApi<T>>['setState']
 */

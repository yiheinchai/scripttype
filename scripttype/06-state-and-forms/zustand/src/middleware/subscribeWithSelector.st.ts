/**
 * ScriptType for 06-state-and-forms/zustand/src/middleware/subscribeWithSelector.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 3 type-level aliases: 3 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare namespace m1 {
  export type T<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
}
// ✓ Write: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Write(T, U): any {
  return Omit(T, keyof(U)) & U
}
/* compiles to:
 * export type Write<T, U> = Omit<T, keyof U> & U
 */

// ✓ WithSelectorSubscribe: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WithSelectorSubscribe(S): any {
  const m1 = matches<{ getState: () => Hole<"T">; }>(S)
  if (m1) {
    return Write(S, StoreSubscribeWithSelector(m1.T))
  }
  return never
}
/* compiles to:
 * export type WithSelectorSubscribe<S> =
 *   S extends { getState: () => infer T; } ? Write<S, StoreSubscribeWithSelector<T>> : never
 */

// ✓ StoreSubscribeWithSelector: verified type-identical to the original
/* @scripttype preserveParamNames */
export function StoreSubscribeWithSelector(T): any {
  return { subscribe: { ...callSig([fnType([T, T], voidType())], fnType([], voidType())), ...genericCallSig(['U'], [fnType([T], U), fnType([U, U], voidType()), optElem({ equalityFn: optional(fnType([U, U], boolean)), fireImmediately: optional(boolean) })], fnType([], voidType())) } }
}
/* compiles to:
 * export type StoreSubscribeWithSelector<T> = {
 *   subscribe: {
 *     (a0: (a0: T, a1: T) => void): () => void
 *     <U>(a0: (a0: T) => U, a1: (a0: U, a1: U) => void, a2?: { equalityFn?: (a0: U, a1: U) => boolean; fireImmediately?: boolean }): (
 * 
 *     ) => void
 *   }
 * }
 */

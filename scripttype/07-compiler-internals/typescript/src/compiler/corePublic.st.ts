/**
 * ScriptType for 07-compiler-internals/typescript/src/compiler/corePublic.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 2 type-level aliases: 2 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const Comparison: any
type Comparison<A = any, B = any, C = any, D = any, E = any, F = any, G = any, H = any> = any
// ✓ EqualityComparer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function EqualityComparer(T) {
  return fnType([T, T], boolean)
}
/* compiles to:
 * export type EqualityComparer<T> = (a0: T, a1: T) => boolean
 */

// ✓ Comparer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Comparer(T) {
  return fnType([T, T], Comparison)
}
/* compiles to:
 * export type Comparer<T> = (a0: T, a1: T) => Comparison
 */

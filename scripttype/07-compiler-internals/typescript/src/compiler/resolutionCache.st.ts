/**
 * ScriptType for 07-compiler-internals/typescript/src/compiler/resolutionCache.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { ResolutionWithResolvedFileName } from '../../../../../07-compiler-internals/typescript/src/compiler/program.js'
import type { ResolutionWithFailedLookupLocations } from '../../../../../07-compiler-internals/typescript/src/compiler/resolutionCache.js'
declare const ResolutionWithFailedLookupLocations: any
declare const ResolutionWithResolvedFileName: any
// ✓ GetResolutionWithResolvedFileName: verified type-identical to the original
/* @scripttype preserveParamNames */
export function GetResolutionWithResolvedFileName(T: ResolutionWithFailedLookupLocations = ResolutionWithFailedLookupLocations, R: ResolutionWithResolvedFileName = ResolutionWithResolvedFileName): any {
  return fnType([T], anyOf(R, Undefined))
}
/* compiles to:
 * export type GetResolutionWithResolvedFileName<
 *   T extends ResolutionWithFailedLookupLocations = ResolutionWithFailedLookupLocations,
 *   R extends ResolutionWithResolvedFileName = ResolutionWithResolvedFileName
 * > =
 *   (a0: T) => R | undefined
 */

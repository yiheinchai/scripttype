/**
 * ScriptType for 07-compiler-internals/typescript/src/compiler/tsbuildPublic.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 1 type-level aliases: 1 covered
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Types this file references but does not define. Those whose declaration could be
// found are imported, so the check is against the real type rather than `any`; the
// rest are stubbed. Each also gets a value declaration, because ScriptType applies
// types in call position and a type-only import binds nothing in value space.
import type { BuilderProgram } from '../../../../../07-compiler-internals/typescript/src/compiler/builderPublic.js'
import type { BuildInvalidedProject, UpdateOutputFileStampsProject } from '../../../../../07-compiler-internals/typescript/src/compiler/tsbuildPublic.js'
declare const BuildInvalidedProject: any
declare const BuilderProgram: any
declare const UpdateOutputFileStampsProject: any
// ✓ InvalidatedProject: verified type-identical to the original
/* @scripttype preserveParamNames */
export function InvalidatedProject(T: BuilderProgram): any {
  return UpdateOutputFileStampsProject | BuildInvalidedProject(T)
}
/* compiles to:
 * export type InvalidatedProject<T extends BuilderProgram> =
 *   UpdateOutputFileStampsProject | BuildInvalidedProject<T>
 */

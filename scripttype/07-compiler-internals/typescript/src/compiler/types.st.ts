/**
 * ScriptType for 07-compiler-internals/typescript/src/compiler/types.ts
 *
 * Generated by the ScriptType decompiler from the corpus source above, then verified
 * by recompiling and comparing to the original with the TypeScript checker.
 * 14 type-level aliases: 12 covered, 1 compile-error, 1 typecheck-error
 *
 * Do not edit: regenerate with `tsx src/materialize.ts`.
 */
// Names this file references but does not define: types from elsewhere in the
// library, and local functions used in type position. Declared so the generated
// ScriptType typechecks standalone. They carry no runtime meaning.
declare const ConcretePragmaSpecs: any
declare const Expression: any
declare const Node: any
declare const OuterExpression: any
declare const PragmaArgumentSpecification: any
declare const TransformationContext: any
declare const __String: any
type ConcretePragmaSpecs<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Expression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type Node<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type OuterExpression<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type PragmaArgumentSpecification<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type TransformationContext<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
type __String<T1 = any, T2 = any, T3 = any, T4 = any, T5 = any, T6 = any, T7 = any, T8 = any, T9 = any, T10 = any, T11 = any, T12 = any, T13 = any, T14 = any, T15 = any, T16 = any> = any
// ✗ MatchingKeys: does not compile yet
//   'TRecord' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function MatchingKeys(TRecord, TMatch, K: keyof typeof TRecord = keyof(TRecord)) {
  if (matches<((typeof TRecord)[typeof K] extends typeof TMatch ? typeof K : never)>(K)) {
    return K
  }
  return never
}
/* compiles to:
 * export type MatchingKeys<TRecord, TMatch, K extends keyof TRecord = keyof TRecord> =
 *   K extends (typeof TRecord)[typeof K] extends typeof TMatch ? typeof K : never ? K : never
 */

// ✗ ReadonlyUnderscoreEscapedMap: the ScriptType does not itself typecheck as TypeScript
//   ReadonlyUnderscoreEscapedMap.st.ts(3:10) TS2693: 'ReadonlyMap' only refers to a type, but is being used as a value here.
/* @scripttype preserveParamNames */
export function ReadonlyUnderscoreEscapedMap(T) {
  return ReadonlyMap(__String, T)
}
/* compiles to:
 * export type ReadonlyUnderscoreEscapedMap<T> = ReadonlyMap<__String, T>
 */

// ✓ UnderscoreEscapedMap: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnderscoreEscapedMap(T) {
  return t<Map<__String, typeof T>>()
}
/* compiles to:
 * export type UnderscoreEscapedMap<T> = Map<__String, T>
 */

// ✓ ModuleImportResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ModuleImportResult(T = {}) {
  return anyOf({ module: T, modulePath: optional(string), error: Undefined }, { module: Undefined, modulePath: optional(Undefined), error: { stack: optional(string), message: optional(string) } })
}
/* compiles to:
 * export type ModuleImportResult<T = {}> =
 *   | { module: T; modulePath?: string; error: undefined }
 *   | { module: undefined; modulePath?: undefined; error: { stack?: string; message?: string } }
 */

// ✓ WrappedExpression: verified type-identical to the original
/* @scripttype preserveParamNames */
export function WrappedExpression(T: Expression) {
  return anyOf(merge(OuterExpression, { expression: readonlyProp(WrappedExpression(T)) }), T)
}
/* compiles to:
 * export type WrappedExpression<T extends Expression> =
 *   OuterExpression & { readonly expression: WrappedExpression<T> } | T
 */

// ✓ TransformerFactory: verified type-identical to the original
/* @scripttype preserveParamNames */
export function TransformerFactory(T: Node) {
  return fnType([TransformationContext], Transformer(T))
}
/* compiles to:
 * export type TransformerFactory<T extends Node> = (a0: TransformationContext) => Transformer<T>
 */

// ✓ Transformer: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Transformer(T: Node) {
  return fnType([T], T)
}
/* compiles to:
 * export type Transformer<T extends Node> = (a0: T) => T
 */

// ✓ Visitor: verified type-identical to the original
/* @scripttype preserveParamNames */
export function Visitor(TIn: Node = Node, TOut: Node | undefined = anyOf(TIn, Undefined)) {
  return fnType([TIn], VisitResult(TOut))
}
/* compiles to:
 * export type Visitor<TIn extends Node = Node, TOut extends Node | undefined = TIn | undefined> =
 *   (a0: TIn) => VisitResult<TOut>
 */

// ✓ VisitResult: verified type-identical to the original
/* @scripttype preserveParamNames */
export function VisitResult(T: Node | undefined) {
  return anyOf(T, readonlyArrayOf(Node))
}
/* compiles to:
 * export type VisitResult<T extends Node | undefined> = T | readonly Node[]
 */

// ✓ PragmaArgTypeMaybeCapture: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PragmaArgTypeMaybeCapture(TDesc) {
  if (matches<{ captureSpan: true; }>(TDesc)) {
    return { value: string, pos: number, end: number }
  }
  return string
}
/* compiles to:
 * export type PragmaArgTypeMaybeCapture<TDesc> =
 *   TDesc extends { captureSpan: true; } ? { value: string; pos: number; end: number } : string
 */

// ✓ PragmaArgTypeOptional: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PragmaArgTypeOptional(TDesc, TName: string) {
  if (matches<{ optional: true; }>(TDesc)) {
    const out = emptyObject
    for (const K in keySet(TName)) {
      out[K] = optional(PragmaArgTypeMaybeCapture(TDesc))
    }
    return out
  }
  const out2 = emptyObject
  for (const K in keySet(TName)) {
    out2[K] = PragmaArgTypeMaybeCapture(TDesc)
  }
  return out2
}
/* compiles to:
 * export type PragmaArgTypeOptional<TDesc, TName extends string> =
 *   TDesc extends { optional: true; }
 *     ? { [K in TName]?: PragmaArgTypeMaybeCapture<TDesc> }
 *     : { [K1 in TName]: PragmaArgTypeMaybeCapture<TDesc> }
 */

// ✓ UnionToIntersection: verified type-identical to the original
/* @scripttype preserveParamNames */
export function UnionToIntersection(U) {
  const m1 = matches<((k: Hole<"I">) => void)>(matches<any>(U) ? fnType([U], voidType()) : never)
  if (m1) {
    return m1.I
  }
  return never
}
/* compiles to:
 * export type UnionToIntersection<U> =
 *   (U extends any ? (a0: U) => void : never) extends (k: infer I) => void ? I : never
 */

// ✓ ArgumentDefinitionToFieldUnion: verified type-identical to the original
/* @scripttype preserveParamNames */
export function ArgumentDefinitionToFieldUnion__v1(T, K) {
  const m1 = matches<{ name: Hole<"TName">; }>(T[K])
  if (m1) {
    if (typeof m1.TName === 'string') {
      return m1.TName
    }
    return never
  }
  return never
}
export function ArgumentDefinitionToFieldUnion(T: readonly PragmaArgumentSpecification<any>[]) {
  const out = emptyObject
  for (const K in keyof(T)) {
    out[K] = PragmaArgTypeOptional(T[K], ArgumentDefinitionToFieldUnion__v1(T, K))
  }
  return out[Extract(keyof(T), number)]
}
/* compiles to:
 * export type ArgumentDefinitionToFieldUnion__v1<T, K> =
 *   T[K] extends { name: infer TName; } ? TName extends string ? TName : never : never
 * export type ArgumentDefinitionToFieldUnion<
 *   T extends readonly PragmaArgumentSpecification<any>[]
 * > =
 *   { [K in keyof T]: PragmaArgTypeOptional<T[K], ArgumentDefinitionToFieldUnion__v1<T, K>> }[Extract<keyof T, number>]
 */

// ✓ PragmaArgumentType: verified type-identical to the original
/* @scripttype preserveParamNames */
export function PragmaArgumentType(KPrag: keyof ConcretePragmaSpecs) {
  if (matches<{ args: readonly PragmaArgumentSpecification<any>[]; }>(ConcretePragmaSpecs[KPrag])) {
    return UnionToIntersection(ArgumentDefinitionToFieldUnion(ConcretePragmaSpecs[KPrag]['args']))
  }
  return never
}
/* compiles to:
 * export type PragmaArgumentType<KPrag extends keyof ConcretePragmaSpecs> =
 *   ConcretePragmaSpecs[KPrag] extends { args: readonly PragmaArgumentSpecification<any>[]; }
 *     ? UnionToIntersection<ArgumentDefinitionToFieldUnion<ConcretePragmaSpecs[KPrag]['args']>>
 *     : never
 */
